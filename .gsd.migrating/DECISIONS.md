# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | v0.2.0 Feature Completion | architecture | Agent service architecture and session model | Agent runs as SYSTEM in session 0; UI spawned into user sessions | SYSTEM session 0 cannot access user clipboard; UI process handles clipboard monitoring and notifications in user sessions. | No | human |
| D002 | v0.3.0 Operational Hardening | architecture | Configuration persistence strategy | Operator config (SIEM, alerts, agent config) lives in SQLite, not env vars | Hot-reload without restart; TUI manageable; persistent across restarts. Same pattern applied consistently across all operator-tunable config. | Yes | human |
| D003 | v0.3.0 Operational Hardening | pattern | Server state composition | AppState { db, siem } is the canonical axum state for dlp-server handlers | Single shared state struct avoids threading individual components through handler signatures. All handlers use State<Arc<AppState>>. | Yes | agent |
| D004 | v0.4.0 Policy Authoring | pattern | Import error handling strategy | Abort-on-first-failure for policy import | Import stops at first POST/PUT error showing 'Failed on policy {name}: {reason}'. Prior successes are already persisted server-side — admin does not get a rollback. | Yes | human |
| D005 | v0.5.0 Boolean Logic | architecture | Backward compatibility for boolean mode schema change | Legacy v0.4.0 policies default to mode=ALL via ALTER TABLE migration | Existing rows from v0.4.0 are populated with 'ALL' on migration without manual intervention. Serde defaults mode to ALL when absent on POST/PUT input. | No | agent |
| D006 | v0.6.0 Endpoint Hardening | architecture | USB enforcement evaluation ordering | UsbEnforcer check() fires before offline.evaluate() — None return is zero-cost fast-path | None return is zero-cost fast-path for non-USB drives; Some(DENY) short-circuits ABAC entirely. Avoids unnecessary policy evaluation for every I/O operation. | Yes | agent |
| D007 | v0.8.0 Application-Aware DLP | pattern | Test isolation pattern for global OnceLock state | Thread-local TEST_EVALUATOR_OVERRIDE for Chrome handler parallel test isolation | Eliminates parallel test races for Chrome handler ABAC tests without restructuring the entire evaluator to use dependency injection. | Yes | agent |
| D008 | v0.8.1 Deferred Items & Issue Debt | architecture | Mount-time blocking implementation approach | DefineDosDeviceW + IOCTL_VOLUME_OFFLINE hybrid for mount-time blocking | DefineDosDeviceW prevents drive letter assignment for new arrivals; IOCTL_VOLUME_OFFLINE handles volumes that already have handles. Both are user-mode APIs requiring no kernel driver. | Yes | agent |
| D009 | M017 planning | architecture | Cloud sync interception mechanism | User-mode API hooking (IAT) in sync client processes + WFP network egress blocking | True file interception without kernel driver or EV code signing. API hooks block writes at CreateFileW/NtCreateFile in sync client processes. WFP provides defense-in-depth for bypasses (direct syscalls, alternative upload paths). | Yes | agent |
| D010 | M017 planning | architecture | Print spooler interception approach | Spool directory watch + XPS content extraction + SetJob cancellation (medium approach) | User-mode only, no port monitor DLL. FindFirstPrinterChangeNotification detects jobs. SHD metadata + SPL XPS parsing extracts content for classification. SetJob(JOB_CONTROL_DELETE) cancels blocked jobs. EMF falls back to metadata-only. | Yes | agent |
| D011 | M017 planning | pattern | Cloud sync path discovery strategy | Registry-based dynamic discovery with hardcoded path fallback | Enterprise deployments redirect sync folders. Registry/shell APIs discover actual paths. Hardcoded defaults (%USERPROFILE%\OneDrive, etc.) as fallback when discovery fails. | Yes | agent |
| D012 | M017 planning | pattern | Cloud share link detection strategy | Clipboard URL pattern matching + stricter ABAC policy for sync-folder files | Local detection of share link creation is provider-specific and fragile. Clipboard monitoring catches share URLs after creation (1drv.ms, drive.google.com/share, dropbox.com/s, box.com/s). Stricter ABAC policy treats all files in sync folders as higher-risk context. | Yes | agent |
