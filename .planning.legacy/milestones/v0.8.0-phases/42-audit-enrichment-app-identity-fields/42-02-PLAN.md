---
phase: 42-audit-enrichment-app-identity-fields
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - dlp-agent/src/clipboard/listener.rs
  - dlp-agent/src/interception/drag_drop.rs
  - dlp-agent/src/detection/usb.rs
  - dlp-agent/src/usb_enforcer.rs
  - dlp-agent/src/chrome/handler.rs
autonomous: true
requirements:
  - AUDIT-04
  - AUDIT-04.2
  - AUDIT-04.3
must_haves:
  truths:
    - "Clipboard audit events include source_application and destination_application"
    - "Drag-and-drop audit events include source_application and destination_application"
    - "USB block audit events include device_identity fields"
    - "Chrome clipboard audit events already have source_origin/destination_origin from Phase 41"
  artifacts:
    - path: "dlp-agent/src/clipboard/listener.rs"
      provides: "Clipboard audit events with app identity"
      contains: "source_application"
      contains: "destination_application"
    - path: "dlp-agent/src/interception/drag_drop.rs"
      provides: "Drag-drop audit events with app identity"
      contains: "source_application"
      contains: "destination_application"
    - path: "dlp-agent/src/usb_enforcer.rs"
      provides: "USB block audit events with device identity"
      contains: "device_identity"
  key_links:
    - from: "Clipboard listener"
      to: "App identity"
      via: "SessionIdentityMap or process resolution"
      pattern: "source_application"
    - from: "USB enforcer"
      to: "Device identity"
      via: "UsbDetector.device_identities"
      pattern: "device_identity"
---

<objective>
Verify and complete audit enrichment for clipboard, drag-and-drop, and USB interception paths.

Purpose: Ensure these interception paths populate all applicable audit fields (app identity for clipboard/drag-drop, device identity for USB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-audit-enrichment-app-identity-fields/42-CONTEXT.md

# Source files to check/modify
@dlp-agent/src/clipboard/listener.rs
@dlp-agent/src/interception/drag_drop.rs
@dlp-agent/src/detection/usb.rs
@dlp-agent/src/usb_enforcer.rs
@dlp-agent/src/chrome/handler.rs

# Existing patterns:
# - clipboard/listener.rs: emits AuditEvent for clipboard operations
# - drag_drop.rs: already has app identity resolution from Phase 40
# - usb_enforcer.rs: emits AuditEvent for USB blocks
# - chrome/handler.rs: already populates source_origin/destination_origin from Phase 41
# - AuditEvent::with_source_application(), .with_destination_application(), .with_device_identity()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify clipboard audit enrichment</name>
  <files>dlp-agent/src/clipboard/listener.rs</files>
  <read_first>dlp-agent/src/clipboard/listener.rs</read_first>
  <action>
Read `dlp-agent/src/clipboard/listener.rs` and check all `AuditEvent::new()` calls.

1. **If source_application is NOT set:** Add enrichment using the session's resolved identity or the process that triggered the clipboard operation. The clipboard listener runs in the UI process (dlp-user-ui), so the "source" application is the process that the UI is monitoring, not the UI itself.

   If the clipboard listener has access to the target process PID or window handle, resolve via `get_application_metadata()`. Otherwise, use the AGENT-UNKNOWN sentinel.

2. **If destination_application is NOT set:** For paste operations, the destination is typically the same as the source (the focused application). Set it to the same identity as source_application, or AGENT-UNKNOWN if source is unknown.

3. **Verify source_origin/destination_origin:** These should already be populated by Phase 41 for Chrome clipboard events. For native clipboard events, they may be None.

4. **Add a debug trace** when AGENT-UNKNOWN is used, documenting why identity could not be resolved:
   ```rust
   tracing::debug!("Clipboard audit: source_application set to AGENT-UNKNOWN — process identity unavailable");
   ```
  </action>
  <verify>
    <automated>cargo test -p dlp-agent clipboard</automated>
  </verify>
  <done>
    - Clipboard audit events have source_application set
    - Clipboard audit events have destination_application set
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify drag-and-drop audit enrichment</name>
  <files>dlp-agent/src/interception/drag_drop.rs</files>
  <read_first>dlp-agent/src/interception/drag_drop.rs</read_first>
  <action>
Read `dlp-agent/src/interception/drag_drop.rs` and check all `AuditEvent::new()` calls.

Phase 40 already implemented app identity resolution for drag-and-drop. Verify:

1. **source_application is populated** from the drag source process.
2. **destination_application is populated** from the drop target process.
3. If either is missing, add the enrichment using the resolved app identity from the Phase 40 implementation.

If the Phase 40 implementation already populates these fields, confirm this with a code comment:
```rust
// App identity populated by Phase 40 drag-drop resolution.
```

If any gaps are found, fix them and add tests.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent drag_drop</automated>
  </verify>
  <done>
    - Drag-drop audit events have source_application set
    - Drag-drop audit events have destination_application set
    - Phase 40 enrichment confirmed or gaps fixed
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify USB audit enrichment</name>
  <files>dlp-agent/src/usb_enforcer.rs,dlp-agent/src/detection/usb.rs</files>
  <read_first>dlp-agent/src/usb_enforcer.rs,dlp-agent/src/detection/usb.rs</read_first>
  <action>
Read `dlp-agent/src/usb_enforcer.rs` and `dlp-agent/src/detection/usb.rs` and check all `AuditEvent::new()` calls for USB block events.

1. **Verify device_identity is populated** on all USB block audit events. The device identity should include VID, PID, serial, and description from the `DeviceIdentity` struct.

2. **If device_identity is missing:** Add it by looking up the device in the `UsbDetector.device_identities` map using the drive letter or device path.

3. **Verify source_application is populated** if the USB operation was triggered by a user process (e.g., file copy to USB). If the PID is available, resolve via `get_application_metadata()`.

4. **Add a test** that verifies USB block audit events include device_identity:
   ```rust
   #[test]
   fn test_usb_block_audit_includes_device_identity() {
       // Construct a mock USB block event and verify device_identity is present
   }
   ```
  </action>
  <verify>
    <automated>cargo test -p dlp-agent usb</automated>
  </verify>
  <done>
    - USB block audit events include device_identity
    - USB block audit events include source_application where applicable
    - Tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify Chrome handler audit enrichment</name>
  <files>dlp-agent/src/chrome/handler.rs</files>
  <read_first>dlp-agent/src/chrome/handler.rs</read_first>
  <action>
Read `dlp-agent/src/chrome/handler.rs` and verify the `emit_chrome_block_audit()` function.

Phase 41 already updated this function. Verify:

1. **source_origin is populated** from the Chrome request URL.
2. **destination_origin is set to None** (Chrome API v1 limitation) with a comment explaining why.
3. **source_application is set** to an AppIdentity representing Chrome (or AGENT-UNKNOWN).
4. **destination_application is set** appropriately.

If any field is missing, add it. If all fields are present, confirm with a comment.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent chrome::handler</automated>
  </verify>
  <done>
    - Chrome clipboard audit events have all applicable fields populated
    - Tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clipboard -> audit | Clipboard events may not have process context in all cases |
| USB -> audit | USB device identity comes from cached enumeration data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Information Disclosure | Device serial in audit log | accept | Device serials are not PII; audit logs are access-controlled |
| T-42-04 | Denial of Service | Missing process context for clipboard | mitigate | Falls back to AGENT-UNKNOWN |
</threat_model>

<verification>
- `cargo test -p dlp-agent` passes
- `cargo build -p dlp-agent` succeeds
- `cargo clippy -p dlp-agent -- -D warnings` passes
</verification>

<success_criteria>
- Clipboard audit events include source_application and destination_application
- Drag-and-drop audit events include source_application and destination_application
- USB block audit events include device_identity
- Chrome clipboard audit events have source_origin/destination_origin populated
- All tests pass, no compiler warnings
</success_criteria>

<output>
After completion, create `.planning/phases/42-audit-enrichment-app-identity-fields/42-02-SUMMARY.md`
</output>
