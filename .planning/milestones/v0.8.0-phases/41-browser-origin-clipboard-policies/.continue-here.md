---
context: phase
phase: 41-browser-origin-clipboard-policies
task: 3
total_tasks: 8
status: in_progress
last_updated: 2026-05-07T00:54:44.162Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: dlp-admin-cli compile errors** — Adding new `ConditionAttribute::SourceOrigin` and `ConditionAttribute::DestinationOrigin` variants to `app.rs` caused non-exhaustive match errors across `dispatch.rs` and `render.rs`. These errors are EXPECTED and will be resolved during Plan 41-04 (TUI conditions builder). Do NOT try to fix all compile errors before executing the plans in order. Plan 41-02 and 41-03 can be developed and tested independently of the admin-cli UI.

- [ ] **CONSTRAINT: Origin semantics limitation** — Chrome Content Analysis API v1 only provides ONE URL (the destination/paste page). `source_origin` in the ABAC context maps to this page URL. `destination_origin` is always `None`. Do NOT attempt to implement true clipboard source tracking (copy-from origin) — that requires API v2 or a native browser extension (deferred to v0.9.0+).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fixing all compile errors upfront | Adding new enum variants causes cascading match errors across the workspace. Fixing them all before plan execution destroys wave isolation. | blocking | Execute plans in wave order (41-01, 41-02, 41-03, 41-04). Only fix errors in the crate being modified by the current plan. Use `cargo check -p <crate>` not `cargo check --all`. |
| Confusing origin direction | The Chrome API URL is the paste DESTINATION page, but we label it `source_origin` for ABAC policy consistency. | advisory | Document this semantic clearly in code comments. The audit event notes `destination_origin: None` with an explanation. |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Phase 41 (Browser Origin Clipboard Policies) — Plan 41-01 complete, Plans 41-02 through 41-04 pending.

Plan 41-01 delivered:
- `PolicyCondition::SourceOrigin { op, value }` and `DestinationOrigin { op, value }` in `dlp-common/src/abac.rs`
- `source_origin: Option<String>` and `destination_origin: Option<String>` on `EvaluateRequest` and `AbacContext`
- `From<EvaluateRequest>` updated to forward origin fields
- `origin_matches(op, expected, origin)` helper added to `dlp-server/src/policy_store.rs`
- `ConditionAttribute::SourceOrigin` and `DestinationOrigin` added to admin-cli `app.rs`
- `condition_to_prefill()` and `condition_display()` updated in `dispatch.rs` for origin variants

Known compile errors: dlp-admin-cli has non-exhaustive matches in `render.rs` (draw_conditions_builder) and additional dispatch.rs match arms. These are plan 41-04 scope.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Add SourceOrigin/DestinationOrigin PolicyCondition variants — Done (commit 3a42956)
- Task 2: Extend EvaluateRequest/AbacContext with origin fields + From impl — Done (commit 3a42956)

</completed_work>

<remaining_work>

- Task 3: Plan 41-02 — Wire `origin_matches` into `condition_matches` + add comprehensive unit tests in `policy_store.rs`
- Task 4: Plan 41-03 — Chrome handler refactor: construct `EvaluateRequest` with origin, call policy cache, replace simple managed-origins check with ABAC evaluation
- Task 5: Plan 41-04 — Admin TUI conditions builder: add origin attribute picker support, operator selection (eq/ne/contains), match arms in render.rs/dispatch.rs
- Task 6: Fix remaining dlp-admin-cli compile errors (plan 41-04 will address most)
- Task 7: Run full test suite (`cargo test --all`)
- Task 8: Run clippy + fmt + sonar-scanner quality gates

</remaining_work>

<decisions_made>

- SourceOrigin/DestinationOrigin as new PolicyCondition variants (not nested like AppField) — follows exact pattern of SourceApplication/DestinationApplication from Phase 26
- source_origin = page URL from Chrome request; destination_origin = None — Chrome Content Analysis API v1 limitation
- Operators: eq, ne, contains for origin conditions — same operator set as ImagePath and Aumid
- No special "is managed origin" operator — origins are pure policy conditions; managed-origins cache is input to policy authoring, not a hardcoded check
- No pattern lists for origins — single value per condition; use ANY mode for OR-logic

</decisions_made>

<blockers>
- dlp-admin-cli compile errors from new ConditionAttribute variants. Expected — plan 41-04 covers these.
- No other blockers.
</blockers>

## Required Reading (in order)

1. `.planning/phases/41-browser-origin-clipboard-policies/41-CONTEXT.md` — Phase decisions and design
2. `.planning/phases/41-browser-origin-clipboard-policies/41-01-SUMMARY.md` — Plan 41-01 summary (if exists; otherwise the PLAN.md)
3. `.planning/phases/41-browser-origin-clipboard-policies/41-02-PLAN.md` — Next plan to execute
4. `dlp-common/src/abac.rs` — Current state of new PolicyCondition variants
5. `dlp-server/src/policy_store.rs` — Current state of origin_matches helper and condition_matches

## Critical Anti-Patterns (do NOT repeat these)

- **Fixing all compile errors upfront**: Adding new enum variants caused cascading match errors. The correct approach is plan-by-plan execution, fixing only the errors in the current plan's scope.
- **Running `cargo check --all` between every plan**: Use `cargo check -p <crate>` for the crate being modified. The full workspace won't compile until plan 41-04 completes.

## Infrastructure State

- No running services or background processes
- Git branch: `gsd/phase-38.2-usb-enforcement-fix` (note: this is the old branch name, not phase-41 specific)
- All changes from plan 41-01 are committed and pushed (commit 3a42956)

## Pre-Execution Critique Required

- Design artifact: `.planning/phases/41-browser-origin-clipboard-policies/41-CONTEXT.md`
- Critique focus: None — design is validated and committed. Ready for execution.
- Gate: N/A

<context>
Plan 41-01 is complete. The type foundation is solid. Next priority is Plan 41-02 (evaluator wiring + tests) which is a self-contained change in dlp-server. Plan 41-03 (Chrome handler) depends on 41-02. Plan 41-04 (TUI) is independent of 41-03 and can run in parallel with it if needed.

The origin_matches() function signature is already in policy_store.rs. It needs to be called from condition_matches() (also already done — I added the match arms during the session). What's missing for 41-02 is comprehensive unit tests for origin condition matching.
</context>

<next_action>
Start with: Read `dlp-server/src/policy_store.rs` to verify the current state of `origin_matches()` and `condition_matches()`, then execute Plan 41-02 from `.planning/phases/41-browser-origin-clipboard-policies/41-02-PLAN.md`.
</next_action>
