# UAT Results -- DLP v0.10.0

This document captures the full User Acceptance Test results for the DLP v0.10.0
milestone. All tests are executed on a real Windows 11 host with real hardware
and real cloud clients where applicable.

---

## Test Environment

| Field | Value |
|-------|-------|
| Host OS | |
| Host Hardware | |
| CPU | |
| RAM | |
| EDR Installed | |
| DLP Version | |
| Test Date | |
| Tester | |

---

## Prerequisites Checklist

### Required for UAT Completion (MUST have all)

- [ ] `dlp-server` running and reachable from the test endpoint
- [ ] `dlp-agent` running and connected to the server
- [ ] `DLP_ADMIN_JWT` environment variable set to a valid admin token
- [ ] T4 policy configured and active (at least one T4-classified path with a DENY rule)
- [ ] Protected Path configured (at least one T3 or T4 root registered in Protected Paths)
- [ ] Real printer installed and visible to Windows (test page prints successfully)
- [ ] Physical USB removable drive (mass storage, any size)
- [ ] EDR allowlist propagated (DLP binaries are excluded from AV/EDR quarantine)

### Required Only for Optional Tests (OK to skip if hardware unavailable)

- [ ] Cloud sync clients installed (OneDrive, Google Drive, Dropbox, or Box)
- [ ] Physical SD card and card reader
- [ ] Optical drive (CD/DVD/Blu-ray) with writable media
- [ ] Virtual drive mounted (VHD, VHDX, ISO via Explorer, or Daemon Tools)

Items in the "Required Only for Optional Tests" section are only needed if you
intend to run the corresponding optional test group. UAT can be considered
complete without them.

---

## Test Matrix

### Group 1: v0.9.0 Cloud Sync Regression (CS-01..CS-06)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| CS-01 | OneDrive upload of T4 file is blocked | `Uat-CloudSync.ps1` | Upload denied, audit event emitted | | [ ] | |
| CS-02 | Google Drive upload of T4 file is blocked | `Uat-CloudSync.ps1` | Upload denied, audit event emitted | | [ ] | |
| CS-03 | Dropbox upload of T4 file is blocked | `Uat-CloudSync.ps1` | Upload denied, audit event emitted | | [ ] | |
| CS-04 | Box upload of T4 file is blocked | `Uat-CloudSync.ps1` | Upload denied, audit event emitted | | [ ] | |
| CS-05 | Cloud share-link copy of T4 URL is cleared from clipboard | `Uat-CloudSync.ps1` | Clipboard cleared, audit event emitted | | [ ] | |
| CS-06 | T1 file upload to any cloud client is allowed | `Uat-CloudSync.ps1` | Upload succeeds, no audit event | | [ ] | |

### Group 2: v0.9.0 Print Enforcement (PR-01..PR-03)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| PR-01 | Print job for T4 document is cancelled | `Uat-PrintEnforce.ps1` | Job cancelled, audit event emitted | | [ ] | |
| PR-02 | Print job for T3 document is cancelled under DENY policy | `Uat-PrintEnforce.ps1` | Job cancelled, audit event emitted | | [ ] | |
| PR-03 | Print job for T1 document is allowed | `Uat-PrintEnforce.ps1` | Job prints successfully, no audit event | | [ ] | |

### Group 3: v0.10.0 Hook DLL Injection (HD-01..HD-05)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| HD-01 | New process receives hook DLL within 500 ms | `Uat-HookInjection.ps1` | Hook DLL loaded, pipe message received | | [ ] | |
| HD-02 | WoW64 process receives x86 hook DLL | `Uat-HookInjection.ps1` | `dlp_hook_dll_x86.dll` in module list | | [ ] | |
| HD-03 | x64 process receives x64 hook DLL | `Uat-HookInjection.ps1` | `dlp_hook_dll.dll` in module list | | [ ] | |
| HD-04 | Allowlisted process (e.g., EDR self-process) is skipped | `Uat-HookInjection.ps1` | Injection skipped, log entry visible | | [ ] | |
| HD-05 | PPL process is skipped without crash | `Uat-HookInjection.ps1` | Injection skipped, no WerFault event | | [ ] | |

### Group 4: v0.10.0 DACL Tripwire (DT-01..DT-05)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| DT-01 | Write to T4 Protected Path denied with agent stopped | `Uat-DaclTripwire.ps1` | `ERROR_ACCESS_DENIED` from NTFS kernel | | [ ] | |
| DT-02 | Delete on T3 Protected Path denied with agent stopped | `Uat-DaclTripwire.ps1` | `ERROR_ACCESS_DENIED` from NTFS kernel | | [ ] | |
| DT-03 | SYSTEM account can still write to Protected Path | `Uat-DaclTripwire.ps1` | Write succeeds as SYSTEM | | [ ] | |
| DT-04 | `icacls /reset` triggers tamper alert within 60 s | `Uat-DaclTripwire.ps1` | `DaclTamperDetected` audit event emitted | | [ ] | |
| DT-05 | Repair watcher restores canonical ACE order after tamper | `Uat-DaclTripwire.ps1` | Deny ACE re-applied, verified via `icacls` | | [ ] | |

### Group 5: v0.10.0 ETW + ntdll + Monitor Mode (ET-01..ET-03, NT-01..NT-03, MM-01..MM-03)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| ET-01 | ETW Kernel-File consumer starts without lost events | `Uat-EtwConsumer.ps1` | Zero Event ID 2 in Event Viewer | | [ ] | |
| ET-02 | Hook-uninstalled process write to Protected Path produces BypassAlert | `Uat-EtwConsumer.ps1` | `BypassAlert` row in `bypass_alerts` table | | [ ] | |
| ET-03 | Allowlisted PID produces no BypassAlert | `Uat-EtwConsumer.ps1` | No bypass alert for AV/EDR process | | [ ] | |
| NT-01 | Direct-syscall write to T4 file denied with ntdll patching ON | `Uat-NtdllPatch.ps1` | `STATUS_ACCESS_DENIED`, audit event emitted | | [ ] | |
| NT-02 | EDR-detected stub is skipped without false "clean" restore | `Uat-NtdllPatch.ps1` | Patch skipped, no DoppelGate risk | | [ ] | |
| NT-03 | EDR re-patch over trampoline triggers `BypassAlert` | `Uat-NtdllPatch.ps1` | `HookOverwritten` alert within 30 s | | [ ] | |
| MM-01 | Audit-mode policy produces `would_have_denied` event but allows operation | `Uat-MonitorMode.ps1` | Audit event with `would_have_denied=true`, file write succeeds | | [ ] | |
| MM-02 | Block-mode policy denies and emits audit event | `Uat-MonitorMode.ps1` | `ERROR_ACCESS_DENIED`, audit event emitted | | [ ] | |
| MM-03 | AuditAndBlock-mode policy denies and records mode in audit | `Uat-MonitorMode.ps1` | Deny + audit with `policy_mode=AuditAndBlock` | | [ ] | |

### Group 6: v0.10.0 Volume Class (VC-01..VC-04) -- OPTIONAL

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| VC-01 | SD card insertion produces `volume_class=SDCard` audit event | `Uat-VolumeClass.ps1` | Correct volume class in audit event | | [ ] | |
| VC-02 | Optical drive mount produces `volume_class=Optical` audit event | `Uat-VolumeClass.ps1` | Correct volume class in audit event | | [ ] | |
| VC-03 | Virtual drive mount produces `volume_class=Virtual` audit event | `Uat-VolumeClass.ps1` | Correct volume class in audit event | | [ ] | |
| VC-04 | Copy from LocalNTFS T4 to Optical is denied under policy | `Uat-VolumeClass.ps1` | `CopyFileExW` denied, audit event emitted | | [ ] | |

### Group 7: USB Enforcement (USB-01..USB-02)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| USB-01 | Blocked-tier USB drive denies writes with `ERROR_WRITE_PROTECT` | `Uat-UsbBlock.ps1` | Write blocked, `ERROR_WRITE_PROTECT` returned | | [ ] | |
| USB-02 | ReadOnly-tier USB drive allows reads but denies writes | `Uat-UsbBlock.ps1` | Read succeeds, write blocked | | [ ] | |

### Group 8: CRIT-04 Benchmark (BM-01..BM-02)

| TC-ID | Description | Script | Expected | Actual | Status | Notes |
|-------|-------------|--------|----------|--------|--------|-------|
| BM-01 | `cargo build --workspace --release` overhead <= 25% | `Uat-Benchmark.ps1` | Wall-clock within 125% of baseline | | [ ] | |
| BM-02 | Office app launch overhead <= 25% | `Uat-Benchmark.ps1` | Launch time within 125% of baseline | | [ ] | |

---

## Execution Instructions

1. Verify the test environment matches the "Test Environment" table above.
2. Complete all items in "Required for UAT Completion" before proceeding.
3. Run the prerequisites verification script if available:
   ```powershell
   .\scripts\Uat-PrereqCheck.ps1
   ```
4. Execute Group 1 (Cloud Sync Regression) first to validate the v0.9.0 baseline:
   ```powershell
   .\scripts\Uat-CloudSync.ps1
   ```
5. Execute Group 2 (Print Enforcement):
   ```powershell
   .\scripts\Uat-PrintEnforce.ps1
   ```
6. Execute Group 3 (Hook DLL Injection):
   ```powershell
   .\scripts\Uat-HookInjection.ps1
   ```
7. Execute Group 4 (DACL Tripwire):
   ```powershell
   .\scripts\Uat-DaclTripwire.ps1
   ```
8. Execute Group 5 (ETW + ntdll + Monitor Mode):
   ```powershell
   .\scripts\Uat-EtwConsumer.ps1
   .\scripts\Uat-NtdllPatch.ps1
   .\scripts\Uat-MonitorMode.ps1
   ```
9. If optional hardware is available, execute Group 6 (Volume Class):
   ```powershell
   .\scripts\Uat-VolumeClass.ps1
   ```
10. Execute Group 7 (USB Enforcement) with a physical USB drive inserted:
    ```powershell
    .\scripts\Uat-UsbBlock.ps1
    ```
11. Execute Group 8 (CRIT-04 Benchmark) last, after all functional tests pass:
    ```powershell
    .\scripts\Uat-Benchmark.ps1
    ```

---

## Actual Column Format Guide

Use the following conventions when filling in the "Actual" column:

- For PASS results: describe the observed behaviour in one sentence (e.g.,
  "Upload blocked with `ERROR_ACCESS_DENIED` and audit event ID 1234 emitted").
- For FAIL results: include the exact error message, HRESULT, or NTSTATUS code,
  plus any relevant log excerpts.
- For SKIP results: state the reason (e.g., "No optical drive available").
- For N/A results: mark the row as not applicable and explain why.
- Include timestamps (QPC or wall-clock) for any latency-sensitive tests
  (HD-01, BM-01, BM-02).
- Reference specific log files or database rows when relevant
  (e.g., "`bypass_alerts` row 4567, `correlation_reason=NoHookJournal`").

---

## UAT Pass Criteria

UAT is considered PASSED when ALL of the following are true:

1. Every test case in Groups 1-5 and 7-8 shows Status = PASS with no FAILs.
2. Group 6 (Volume Class) may be skipped if optional hardware is unavailable;
   if hardware is available, all VC tests must PASS.
3. The CRIT-04 benchmark gate (BM-01 and BM-02) shows <= 25% wall-clock
   overhead versus the baseline measurement.
4. No `WerFault` event log entries name `dlp_hook_dll.dll` across the entire
   test session.
5. All audit events route to SIEM without error (verify via `siem_connector`
   logs).
6. The `DLP_ADMIN_JWT` token remains valid for the entire session; no 401
   Unauthorized responses from the admin API.

If any FAIL appears:

- Capture the full error output and log excerpts in the Notes column.
- Re-run the failing test case in isolation.
- If the failure is reproducible, file a blocking issue before signing off.

---

## Sign-Off

| Role | Name | Date | Signature |
|------|------|------|-----------|
| Tester | | | |
| QA Lead | | | |
| Release Manager | | | |

---

*Template version: v0.10.0-UAT-2026-06-05*
*Cross-reference: `docs/operations/deployment-guide.md` UAT Test Matrix section*
