---
quick_id: 260628-1n2
slug: resolve-dlp-rust-539-fix-workspace-wide-
type: quick
date: 2026-06-27
---

# Quick Task 260628-1n2: Resolve workspace-wide test/clippy gate blockers

## Goal

Fix unrelated compilation and lint errors that currently block workspace-wide verification commands, as documented in beads issue dlp-rust-539.

## Blockers

1. **dlp-server test compile errors**
   - `dlp-server/src/alert_router.rs` — 5 `AuditEvent` struct literals missing `prev_hash` and `chain_hash` fields.
   - `dlp-server/src/audit_store.rs` — `AppState` test helper missing `diagnostic_store` field.
   - `dlp-server/src/db/repositories/audit_events.rs` — `AuditEventRow` test helper missing `content_sha256` field.

2. **dlp-common test compile error**
   - `dlp-common/src/hook_ipc.rs` — `test_hook_request_volume_class_roundtrip` `HookRequest` struct literal missing `pid` field.

3. **dlp-agent clippy warning**
   - `dlp-agent/src/health_aggregator.rs:77` — `type_complexity` warning on `alert_router` field.

## Approach

- Add missing fields to all broken struct literals, using `Default::default()` or explicit `None`/`0` where semantically appropriate.
- For the `type_complexity` warning, introduce a type alias for the alert router closure type to satisfy clippy without changing behavior.
- Run package-scoped tests and clippy to verify each fix.
- Run workspace-wide `cargo test` and `cargo clippy` before finishing.

## Acceptance Criteria

- `cargo test --workspace` passes
- `cargo clippy --workspace -- -D warnings` passes
- `cargo build --workspace` succeeds with zero warnings
