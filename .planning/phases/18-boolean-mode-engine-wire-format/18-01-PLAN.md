---
phase: 18-boolean-mode-engine-wire-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-server/src/policy_store.rs
  - dlp-server/src/admin_api.rs
  - dlp-server/src/db/mod.rs
autonomous: true
requirements:
  - POLICY-09
  - POLICY-12

must_haves:
  truths:
    - "All 15 evaluator mode tests pass (6 ALL mode + 4 ANY mode + 2 NONE mode + 3 empty-conditions edge cases)"
    - "Legacy parity test passes (v0.4.0-shaped Policy defaults to ALL behavior)"
    - "All 4 wire format serde tests pass (legacy default + ANY roundtrip + NONE roundtrip + response default)"
    - "Migration test passes (v0.4.0 schema -> add column -> backfill ALL -> idempotent re-run)"
    - "Full dlp-server lib test suite passes (629+ tests)"
    - "Build passes with zero warnings (cargo clippy --all -- -D warnings)"
    - "Decisions verified: D-01 (PolicyMode in dlp-common::abac), D-02 (SCREAMING wire form), D-03 (Default=ALL), D-04 (Default on Policy), D-05 (PolicyMode derives), D-06 (init_tables mode column), D-07 (run_migrations function), D-08 (swallow duplicate column), D-09 (backfill ALL), D-10 (migration test), D-11 (evaluate mode switch), D-12 (iterator semantics), D-13 (empty-conditions semantics), D-14 (serde default PolicyPayload), D-15 (serde default PolicyResponse), D-16 (serde default Policy), D-17 (PolicyRow mode field), D-18 (deserialize_policy_row mode parse), D-19 (mode-to-SQL helper), D-20 (create_policy wiring), D-21 (update_policy wiring), D-22 (invalidate unchanged), D-23 (evaluator tests), D-24 (wire format tests), D-25 (legacy parity test), D-26 (migration test)"
  artifacts:
    - path: "dlp-server/src/policy_store.rs"
      provides: "Evaluator mode tests (ALL/ANY/NONE + empty-conditions) + legacy parity test"
      contains: "test_evaluate_all_mode_all_conditions_match"
    - path: "dlp-server/src/policy_store.rs"
      provides: "Legacy v0.4.0 payload parity test"
      contains: "test_legacy_v040_policy_without_mode_behaves_like_all"
    - path: "dlp-server/src/admin_api.rs"
      provides: "Wire format serde tests for PolicyPayload/PolicyResponse"
      contains: "test_policy_payload_deserializes_without_mode_as_all"
    - path: "dlp-server/src/db/mod.rs"
      provides: "Migration idempotency test"
      contains: "test_migration_add_mode_column"
  key_links:
    - from: "dlp-server/src/policy_store.rs tests"
      to: "dlp-common/src/abac.rs PolicyMode"
      via: "PolicyMode::ALL/ANY/NONE variants in test fixtures"
    - from: "dlp-server/src/admin_api.rs tests"
      to: "dlp-common/src/abac.rs PolicyMode"
      via: "serde_json roundtrip of PolicyMode via PolicyPayload/PolicyResponse"
    - from: "dlp-server/src/db/mod.rs migration test"
      to: "dlp-server/src/db/mod.rs run_migrations"
      via: "PRAGMA table_info + SELECT mode assertions"
---

<objective>
Verify the existing Phase 18 implementation (types, DB schema, evaluator mode switch, wire format) by running the full test suite and confirming all tests pass. The implementation is already present in the codebase. This plan serves as the verification gate for Phase 18.

Purpose: Confirm that the Phase 18 boolean mode engine and wire format implementation is complete and correct.
Output: Verified test results confirming all 15 evaluator mode tests, 1 legacy parity test, 4 wire format tests, 1 migration test, and the full 629+ dlp-server lib test suite pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-boolean-mode-engine-wire-format/18-CONTEXT.md
@.planning/phases/18-boolean-mode-engine-wire-format/18-RESEARCH.md
@.planning/phases/18-boolean-mode-engine-wire-format/18-PATTERNS.md

# Implementation already present (verified by codebase inspection):
# - dlp-common/src/abac.rs: PolicyMode enum (ALL/ANY/NONE) with Default=ALL
# - dlp-common/src/abac.rs: Policy struct with mode: PolicyMode + #[serde(default)]
# - dlp-server/src/db/mod.rs: init_tables has mode TEXT NOT NULL DEFAULT 'ALL'
# - dlp-server/src/db/mod.rs: run_migrations with ALTER TABLE mode column
# - dlp-server/src/db/repositories/policies.rs: PolicyRow/PolicyUpdateRow with mode fields
# - dlp-server/src/policy_store.rs: evaluate() mode switch (ALL/ANY/NONE)
# - dlp-server/src/policy_store.rs: deserialize_policy_row parses mode
# - dlp-server/src/policy_store.rs: mode_str() helper
# - dlp-server/src/admin_api.rs: PolicyPayload/PolicyResponse with mode + #[serde(default)]
# - dlp-server/src/admin_api.rs: create_policy/update_policy wire mode through
# - dlp-server/src/admin_api.rs: mode_from_str() helper
# - dlp-server/src/admin_api.rs: list_policies/get_policy include mode in response
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify evaluator mode tests pass</name>
  <files>dlp-server/src/policy_store.rs</files>
  <read_first>
    - dlp-server/src/policy_store.rs (lines 707-1684, the #[cfg(test)] module)
    - dlp-common/src/abac.rs (lines 667-708, PolicyMode enum and Policy struct)
  </read_first>
  <action>
    Run the evaluator mode tests in dlp-server/src/policy_store.rs to confirm all 15 mode-specific tests pass:

    ALL mode (6 tests):
    - test_evaluate_all_mode_all_conditions_match
    - test_evaluate_all_mode_one_condition_misses
    - test_evaluate_all_mode_source_origin_misses_classification_matches
    - test_evaluate_all_mode_source_origin_and_classification
    - test_evaluate_all_mode_source_app_none_blocks_policy
    - test_evaluate_all_mode_source_app_and_classification_both_match

    ANY mode (4 tests):
    - test_evaluate_any_mode_one_condition_matches
    - test_evaluate_any_mode_no_condition_matches
    - test_evaluate_any_mode_source_app_none_classification_matches
    - test_evaluate_any_mode_source_origin_and_classification

    NONE mode (2 tests):
    - test_evaluate_none_mode_no_condition_matches
    - test_evaluate_none_mode_one_condition_matches

    Empty-conditions edge cases (3 tests):
    - test_evaluate_empty_conditions_all_mode_matches
    - test_evaluate_empty_conditions_any_mode_does_not_match
    - test_evaluate_empty_conditions_none_mode_matches

    Also verify the legacy parity test:
    - test_legacy_v040_policy_without_mode_behaves_like_all

    These tests are already present in the codebase. No code changes needed.
  </action>
  <verify>
    <automated>cargo test -p dlp-server --lib -- policy_store::tests::test_evaluate_all_mode && cargo test -p dlp-server --lib -- policy_store::tests::test_evaluate_any_mode && cargo test -p dlp-server --lib -- policy_store::tests::test_evaluate_none_mode && cargo test -p dlp-server --lib -- policy_store::tests::test_evaluate_empty_conditions && cargo test -p dlp-server --lib -- policy_store::tests::test_legacy_v040</automated>
  </verify>
  <done>All 15 evaluator mode tests and the legacy parity test pass with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Verify wire format and migration tests pass</name>
  <files>dlp-server/src/admin_api.rs, dlp-server/src/db/mod.rs</files>
  <read_first>
    - dlp-server/src/admin_api.rs (lines 5450-5670, the #[cfg(test)] module with wire format tests)
    - dlp-server/src/db/mod.rs (lines 1216-1287, test_migration_add_mode_column)
  </read_first>
  <action>
    Run the wire format tests in dlp-server/src/admin_api.rs to confirm all 4 serde tests pass:
    - test_policy_payload_deserializes_without_mode_as_all (POLICY-12)
    - test_policy_payload_json_with_mode_any_roundtrip
    - test_policy_response_deserializes_without_mode_as_all
    - test_policy_payload_none_mode_roundtrip

    Run the migration test in dlp-server/src/db/mod.rs:
    - test_migration_add_mode_column (v0.4.0 schema -> add mode column -> backfill ALL -> idempotent re-run)

    These tests are already present in the codebase. No code changes needed.
  </action>
  <verify>
    <automated>cargo test -p dlp-server --lib -- admin_api::tests::test_policy_payload_deserializes_without_mode_as_all && cargo test -p dlp-server --lib -- admin_api::tests::test_policy_payload_json_with_mode_any_roundtrip && cargo test -p dlp-server --lib -- admin_api::tests::test_policy_response_deserializes_without_mode_as_all && cargo test -p dlp-server --lib -- admin_api::tests::test_policy_payload_none_mode_roundtrip && cargo test -p dlp-server --lib -- db::tests::test_migration_add_mode_column</automated>
  </verify>
  <done>All 4 wire format tests and 1 migration test pass with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Full workspace build and clippy verification</name>
  <files></files>
  <read_first>
    - dlp-server/src/policy_store.rs (lines 1-50, imports and mode_str helper)
    - dlp-server/src/admin_api.rs (lines 40-60, PolicyPayload/PolicyResponse structs)
  </read_first>
  <action>
    Run the full workspace build and clippy to confirm zero warnings:
    1. cargo build --workspace -- must complete with no errors
    2. cargo clippy --all -- -D warnings -- must pass with no warnings
    3. cargo test --lib --all -- must pass with all tests green

    The Phase 18 implementation is already integrated. This task verifies the integration does not break the build or any existing tests.
  </action>
  <verify>
    <automated>cargo build --workspace && cargo clippy --all -- -D warnings && cargo test --lib --all</automated>
  </verify>
  <done>Full workspace builds cleanly, clippy passes with -D warnings, and all lib tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->API | Untrusted HTTP client sends PolicyPayload JSON; serde default shields missing mode field |
| DB->PolicyStore | DB row mode string parsed into PolicyMode; malformed values are skipped (fail-safe) |
| PolicyStore->evaluate | Read-lock held during evaluation; mode switch is O(1) enum dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | PolicyPayload JSON without mode | mitigate | #[serde(default)] on PolicyPayload.mode ensures missing field defaults to ALL (POLICY-12) |
| T-18-02 | Denial of Service | Migration re-run on every startup | mitigate | run_alter swallows only "duplicate column name" errors; all other errors bubble up |
| T-18-03 | Information Disclosure | Malformed mode in DB row logged | accept | Warn log contains policy_id only; no sensitive data exposed |
| T-18-04 | Elevation of Privilege | Empty-conditions ALL policy matches unconditionally | accept | Documented behavior per D-13; matches v0.4.0 semantics |
| T-18-05 | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this phase; slopcheck not triggered |
</threat_model>

<verification>
## Overall Phase Verification

1. Evaluator mode behavior: Run cargo test -p dlp-server --lib -- policy_store::tests::test_evaluate -- confirm 15+ tests pass including all mode-specific and empty-conditions tests.
2. Wire format backward compat: Run cargo test -p dlp-server --lib -- admin_api::tests::test_policy_payload -- confirm 4 tests pass including legacy default and roundtrip.
3. Migration idempotency: Run cargo test -p dlp-server --lib -- db::tests::test_migration_add_mode_column -- confirm 1 test passes.
4. Legacy parity: Run cargo test -p dlp-server --lib -- policy_store::tests::test_legacy_v040 -- confirm 1 test passes.
5. Full suite: Run cargo test --lib --all -- confirm 0 failures across all crates.
6. Lint gate: Run cargo clippy --all -- -D warnings -- confirm 0 warnings.
</verification>

<success_criteria>
## Measurable Completion

- [ ] 15 evaluator mode tests pass (6 ALL + 4 ANY + 2 NONE + 3 empty-conditions)
- [ ] 1 legacy parity test passes (v0.4.0-shaped Policy defaults to ALL behavior)
- [ ] 4 wire format tests pass (legacy default + ANY roundtrip + NONE roundtrip + response default)
- [ ] 1 migration test passes (v0.4.0 -> add column -> backfill -> idempotent)
- [ ] Full cargo test --lib --all passes with 0 failures
- [ ] cargo clippy --all -- -D warnings passes with 0 warnings
- [ ] cargo build --workspace passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/18-boolean-mode-engine-wire-format/18-01-SUMMARY.md when done
</output>

## Artifacts this phase produces

This verification plan confirms the following symbols already exist in the codebase:

### Types (dlp-common)
- dlp_common::abac::PolicyMode enum (variants: ALL, ANY, NONE) with Default=ALL
- dlp_common::abac::Policy.mode: PolicyMode field with #[serde(default)]

### Types (dlp-server)
- dlp_server::admin_api::PolicyPayload.mode: PolicyMode with #[serde(default)]
- dlp_server::admin_api::PolicyResponse.mode: PolicyMode with #[serde(default)]
- dlp_server::db::repositories::policies::PolicyRow.mode: String
- dlp_server::db::repositories::policies::PolicyUpdateRow.mode: &str

### Functions (dlp-server)
- dlp_server::policy_store::mode_str(mode: PolicyMode) -> &'static str
- dlp_server::policy_store::deserialize_policy_row (parses row.mode into PolicyMode)
- dlp_server::admin_api::mode_from_str(s: &str) -> PolicyMode
- dlp_server::db::mod::run_migrations(conn: &SqliteConn) -> anyhow::Result<()> (with mode ALTER TABLE)

### DB Schema
- policies table: mode TEXT NOT NULL DEFAULT 'ALL' column (fresh installs via init_tables)
- policies table: mode column added via run_migrations for existing v0.4.0 DBs

### Tests (already present and verified)
- policy_store::tests::test_evaluate_all_mode_all_conditions_match
- policy_store::tests::test_evaluate_all_mode_one_condition_misses
- policy_store::tests::test_evaluate_all_mode_source_origin_misses_classification_matches
- policy_store::tests::test_evaluate_all_mode_source_origin_and_classification
- policy_store::tests::test_evaluate_all_mode_source_app_none_blocks_policy
- policy_store::tests::test_evaluate_all_mode_source_app_and_classification_both_match
- policy_store::tests::test_evaluate_any_mode_one_condition_matches
- policy_store::tests::test_evaluate_any_mode_no_condition_matches
- policy_store::tests::test_evaluate_any_mode_source_app_none_classification_matches
- policy_store::tests::test_evaluate_any_mode_source_origin_and_classification
- policy_store::tests::test_evaluate_none_mode_no_condition_matches
- policy_store::tests::test_evaluate_none_mode_one_condition_matches
- policy_store::tests::test_evaluate_empty_conditions_all_mode_matches
- policy_store::tests::test_evaluate_empty_conditions_any_mode_does_not_match
- policy_store::tests::test_evaluate_empty_conditions_none_mode_matches
- policy_store::tests::test_legacy_v040_policy_without_mode_behaves_like_all
- admin_api::tests::test_policy_payload_deserializes_without_mode_as_all
- admin_api::tests::test_policy_payload_json_with_mode_any_roundtrip
- admin_api::tests::test_policy_response_deserializes_without_mode_as_all
- admin_api::tests::test_policy_payload_none_mode_roundtrip
- db::tests::test_migration_add_mode_column
