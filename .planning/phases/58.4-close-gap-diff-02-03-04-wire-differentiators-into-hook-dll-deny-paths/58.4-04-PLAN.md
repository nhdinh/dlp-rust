---
phase: 58.4-close-gap-diff-02-03-04-wire-differentiators-into-hook-dll-deny-paths
plan: 04
type: execute
wave: 2
depends_on:
  - 58.4-01
  - 58.4-02
  - 58.4-03
files_modified:
  - dlp-hook-dll/src/trampolines.rs
  - dlp-hook-dll/src/diagnostic_ring.rs
  - dlp-hook-dll/src/hash_compute.rs
  - dlp-hook-dll/src/perf_telemetry.rs
  - dlp-agent/src/diagnostic_aggregator.rs
  - dlp-agent/src/health_aggregator.rs
  - dlp-agent/tests/hook_ipc_integration.rs
  - dlp-agent/tests/volume_class_integration.rs
autonomous: true
requirements:
  - DIFF-02
  - DIFF-03
  - DIFF-04
must_haves:
  truths:
    - Unit tests prove diagnostic snapshot is pushed on DENY.
    - Unit tests prove SHA-256 hash matches known value for a test buffer.
    - Unit tests prove 100 MB truncation works.
    - Unit tests prove health counters increment and emit correctly.
    - Integration tests prove PullDiagnostics returns non-empty snapshots after a DENY.
    - Integration tests prove PullHealth returns a valid snapshot.
    - Integration tests prove blocked write audit events contain content_sha256 via HashEvidence frame ingestion.
  artifacts:
    - path: dlp-hook-dll/src/trampolines.rs
      provides: Deny branches with snapshot, hash, and health counter wiring.
    - path: dlp-agent/tests/hook_ipc_integration.rs
      provides: End-to-end tests for diagnostic, hash, and health data paths.
  key_links:
    - from: dlp-hook-dll/src/trampolines.rs
      to: dlp-agent/tests/hook_ipc_integration.rs
      via: mock HookIpcServer + mock agent handler verifying snapshot/hash/health data
      pattern: test_
---

<objective>
Add comprehensive end-to-end tests for all three differentiator data paths (DIFF-02, DIFF-03, DIFF-04) to prove the wiring works correctly across the hook DLL → agent → audit pipeline.

Purpose: D-15 and D-16 require automated verification that the differentiator infrastructure produces real data. Without tests, regressions in the deny-path wiring would go undetected.
Output: Unit tests in dlp-hook-dll and integration tests in dlp-agent covering snapshot capture, hash computation, health emission, and agent-side ingestion.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.4-close-gap-diff-02-03-04-wire-differentiators-into-hook-dll-deny-paths/58.4-CONTEXT.md
@.planning/phases/58.4-close-gap-diff-02-03-04-wire-differentiators-into-hook-dll-deny-paths/58.4-RESEARCH.md

# Source files that must be read before editing
dlp-hook-dll/src/trampolines.rs
dlp-hook-dll/src/diagnostic_ring.rs
dlp-hook-dll/src/hash_compute.rs
dlp-hook-dll/src/perf_telemetry.rs
dlp-agent/src/diagnostic_aggregator.rs
dlp-agent/src/health_aggregator.rs
dlp-agent/tests/hook_ipc_integration.rs

# Relevant prior SUMMARY for pattern reference
@.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for diagnostic snapshot capture on DENY</name>
  <files>dlp-hook-dll/src/trampolines.rs, dlp-hook-dll/src/diagnostic_ring.rs</files>
  <action>
    Add a #[cfg(test)] module in trampolines.rs (or extend the existing tests module) that:
    1. Starts a mock agent server that returns DENY for all requests.
    2. Calls classify_and_log_path with a test path and action.
    3. Asserts that the diagnostic ring contains at least one snapshot with:
       - hook_function matching the expected value (e.g., "WriteFile" or "CreateFileW")
       - abac_resource equal to the test path
       - abac_action equal to the test action
       - user_sid non-empty
       - timestamp_qpc > 0

    Also test classify_and_log_handle with a mock handle value and verify the snapshot is pushed with abac_resource containing "handle:".

    Use the existing start_agent_mock_server helper from lib.rs tests. Reset the diagnostic ring before each test by reinitializing the OnceLock (use DIAGNOSTIC_RING.take() if available, or use a test-only reset function).

    If resetting the global OnceLock is not feasible, test indirectly by calling push_snapshot directly and then drain_snapshots to verify the ring contains the expected snapshot.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --lib -- trampolines::tests::test_diagnostic_snapshot_on_deny --nocapture</automated>
  </verify>
  <done>
    - Unit test proves snapshot is pushed on DENY in classify_and_log_path.
    - Unit test proves snapshot is pushed on DENY in classify_and_log_handle.
    - Snapshot fields match expected values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for SHA-256 hash computation</name>
  <files>dlp-hook-dll/src/hash_compute.rs</files>
  <action>
    Extend the existing hash_compute.rs tests with:
    1. test_hash_known_value: compute_content_hash on a known buffer (e.g., b"hello world") and assert the hash matches the known SHA-256 hex string "b94d27b9934d3e08a52e52d7da7dabfac484efe37a5380ee9088f7ace2efcde9".
    2. test_hash_truncation_100mb: Create a buffer of HASH_CAP_BYTES + 1000, compute hash, assert truncated == true and the hash matches the SHA-256 of the first HASH_CAP_BYTES bytes.
    3. test_hash_pool_saturation: Simulate queue depth > 4 by setting HASH_QUEUE_DEPTH to 5 before calling compute_content_hash_offloaded, assert skipped == true and hash == None. Reset the counter after the test.
    4. test_hash_offloaded_large_buffer: Create a buffer of SMALL_BUFFER_THRESHOLD + 1, call compute_content_hash_offloaded, assert hash is Some and skipped == false.

    Ensure all tests use safe, test-only buffer pointers (Vec::as_ptr()) and run in the test thread.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --lib -- hash_compute::tests --nocapture</automated>
  </verify>
  <done>
    - Known-value hash test passes.
    - 100MB truncation test passes.
    - Pool saturation test passes.
    - Offloaded large buffer test passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for health counter emission</name>
  <files>dlp-hook-dll/src/perf_telemetry.rs</files>
  <action>
    Extend the existing perf_telemetry.rs tests with:
    1. test_health_counters_increment: Call record_cache_hit() 5 times, record_cache_miss() 3 times, then emit_health_snapshot() and assert pipe_round_trips_60s == 0 (or the expected value if record_pipe_round_trip was also called), cache_hit_rate_60s == 5.0 / 8.0.
    2. test_health_snapshot_fields: Set injected_pids and patched_modules, set_fail_state(1), call emit_health_snapshot() and assert all fields match the set values.
    3. test_health_snapshot_resets_counters: Call record_cache_hit(), then emit_health_snapshot(), then emit_health_snapshot() again and assert the second snapshot has cache_hit_rate_60s == 0.0 (counters were reset).
    4. test_health_snapshot_timestamp: Assert timestamp_secs is within 1 second of current Unix time.

    Use the public helper functions (record_cache_hit, record_cache_miss, set_fail_state, etc.) to manipulate counters before emission.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --lib -- perf_telemetry::tests --nocapture</automated>
  </verify>
  <done>
    - Health counter increment tests pass.
    - Health snapshot field tests pass.
    - Counter reset test passes.
    - Timestamp test passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add pre-schema integration tests for agent-side diagnostic and health data paths</name>
  <files>dlp-agent/tests/hook_ipc_integration.rs</files>
  <action>
    Per review finding (MEDIUM): Plan 04 integration test for blocked write audit contains hash is too heavy because HashCache and HashEvidence frame do not exist until Plan 02 schema changes are landed. Split the integration tests into pre-schema (this task) and post-schema (Task 5) waves.

    Add these integration tests to dlp-agent/tests/hook_ipc_integration.rs:

    1. test_pull_diagnostics_after_deny:
       - Start a mock HookIpcServer with a diagnostics handler that returns a DiagnosticsResponse containing one DiagnosticSnapshot.
       - Connect a mock client and send PullDiagnosticsRequest.
       - Assert the response contains at least one snapshot with hook_function == "WriteFile".

    2. test_pull_health_returns_snapshot:
       - Start a mock HookIpcServer with a health handler that returns a HealthResponse containing a HookHealthSnapshot with current_fail_state == 0.
       - Connect a mock client and send PullHealthRequest.
       - Assert the response snapshot has current_fail_state == 0 and timestamp_secs > 0.

    3. test_health_response_ingestion:
       - Start a mock HookIpcServer with a health_aggregator.
       - Send a one-way HealthResponse frame from a mock client.
       - Assert the HealthAggregator's history length is 1.

    For each test, use the existing mock server patterns from hook_ipc_integration.rs. Ensure tests are independent and clean up any global state.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test hook_ipc_integration -- --nocapture</automated>
  </verify>
  <done>
    - PullDiagnostics integration test returns non-empty snapshots.
    - PullHealth integration test returns a valid snapshot.
    - HealthResponse ingestion test proves agent-side ingestion works.
    - All pre-schema integration tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add post-schema integration test for hash evidence attachment</name>
  <files>dlp-agent/tests/hook_ipc_integration.rs, dlp-agent/src/hook_ipc.rs</files>
  <action>
    Per review finding (MEDIUM): The integration test for blocked write audit containing hash depends on the HashEvidence frame and HashCache from Plan 02. This test must run after Plan 02 schema changes are landed.

    Add a post-schema integration test to dlp-agent/tests/hook_ipc_integration.rs:

    test_blocked_write_audit_contains_hash:
    - Start a mock HookIpcServer with a HashCache and a handler that returns DENY for handle-based WRITE requests.
    - Connect a mock client and send a two-step sequence:
      1. Send a HandleHookRequest (or HookRequest) for a WRITE action.
      2. Send a HashEvidence frame with the same pid and handle_value, containing a known content_sha256.
    - Verify the agent's handler retrieves the hash from the HashCache and attaches it to the AuditEvent (or returns it in the HookResponse if that path is tested).
    - If the audit emitter is not easily testable in isolation, create a unit test in dlp-agent/src/hook_ipc.rs (in a #[cfg(test)] module) that tests the hash cache lookup and attachment logic directly:
      - Construct a mock HashCache, insert a (pid, handle_value) entry with a known hash.
      - Call the hash lookup helper (or the relevant portion of handle_hook_request) and assert the returned hash matches the inserted value.

    This test mocks the HashEvidence frame ingestion and verifies the audit event builder path without requiring a full end-to-end blocked write.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test hook_ipc_integration -- test_blocked_write_audit_contains_hash --nocapture</automated>
  </verify>
  <done>
    - Blocked write audit event contains content_sha256 after HashEvidence frame ingestion.
    - HashCache lookup and attachment logic is verified.
    - All post-schema integration tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Tests exercise the same deny paths as production but with mock data |
| mock agent → test assertions | Test assertions verify correct data flow without real IPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.4-10 | Information Disclosure | Test fixtures contain mock SIDs/paths | accept | Test data is synthetic; no real PII |
| T-58.4-11 | Denial of Service | Large buffer allocation in hash test | accept | 100MB + small margin; test runs once; deallocated after test |
| T-58.4-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
- cargo test -p dlp-hook-dll --lib passes
- cargo test -p dlp-agent --test hook_ipc_integration passes
- cargo clippy --all-targets -- -D warnings passes
</verification>

<success_criteria>
- Unit tests prove snapshot is pushed on DENY.
- Unit tests prove hash matches known value and truncation works.
- Unit tests prove health counters increment and emit correctly.
- Integration tests prove PullDiagnostics, PullHealth, and hash attachment work end-to-end.
- No regression in existing tests.
- All tests run in under 60 seconds.
</success_criteria>

<output>
Create `.planning/phases/58.4-close-gap-diff-02-03-04-wire-differentiators-into-hook-dll-deny-paths/58.4-04-SUMMARY.md` when done
</output>
