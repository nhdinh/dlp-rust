---
context: phase
phase: 40-drag-and-drop
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-05-06T23:57:54.792Z
---

# BLOCKING CONSTRAINTS

- [ ] CONSTRAINT: Worktree agent instability - Two executor agents failed silently.
- [ ] CONSTRAINT: Windows API type mismatches - GetApplicationUserModelId takes HANDLE not HWND.
- [ ] CONSTRAINT: Cargo.toml feature gates - Win32_UI_Shell must be added explicitly.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Worktree agent silent failure | Executor agents created files but worktree cleaned up before git commit | blocking | Use inline sequential execution for complex Win32 API plans |
| Wrong Windows API module paths | GetApplicationUserModelId is in Win32::Storage::Packaging::Appx | advisory | Grep windows-rs crate source to confirm paths |
| Missing Cargo.toml feature gates | Adding imports without features causes configured out errors | advisory | Run cargo check after every import addition |

## Current State

Phase 40 (Drag-and-Drop Enforcement), Plan 40-03 in progress.

DragDropEnforcer module created and committed (dlp-agent/src/interception/drag_drop.rs, ~914 lines).
Two tests fail due to requiring actual Windows APIs (SetWindowsHookExW) in test environment.

## Completed Work

- 40-01: Drag-and-Drop infrastructure (Action::DRAG_DROP, Pipe3UiMsg::DragDropAlert)
- 40-02: App Identity Resolution in dlp-agent (867 lines, 13 tests)
- 40-03 (partial): DragDropEnforcer core - hook, interception, ABAC, audit (12/14 tests pass)

## Remaining Work

- 40-03: Fix 2 failing tests (test_install_uninstall_drag_drop_hook, test_double_install_fails)
- 40-04: Integration - wire into service.rs, UI alerts, audit enrichment

## Decisions Made

- GetApplicationUserModelId requires OpenProcess HANDLE, not HWND
- Win32_UI_Shell feature needed for DragQueryFileW and HDROP
- Worktree agents unreliable for Win32 API plans; inline execution preferred

## Blockers

- 2 unit tests fail on Windows API calls without real interactive session

## Next Action

Fix 2 failing tests in drag_drop.rs, then proceed to plan 40-04.
