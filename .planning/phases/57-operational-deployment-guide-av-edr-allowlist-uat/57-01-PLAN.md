---
phase: 57-operational-deployment-guide-av-edr-allowlist-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/deployment-guide.md
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - Deployment guide exists at docs/operations/deployment-guide.md
    - Guide includes per-vendor AV/EDR allowlist procedures for 6 vendors
    - Each vendor section has detection behavior, allowlist steps, hash exclusion, verification
    - Guide follows dpapi-recovery.md style (prerequisites, numbered steps, PowerShell snippets)
    - Quick Start checklist present for experienced operators
    - Rollback procedure documented
    - Secure Boot/PPL/privilege/reboot sections contain placeholder headers referencing 57-04 for detailed content
  artifacts:
    - path: docs/operations/deployment-guide.md
      provides: Comprehensive deployment guide with AV/EDR allowlist procedures
      contains: "Microsoft Defender for Endpoint", "CrowdStrike Falcon", "SentinelOne"
---

<objective>
Create the v0.10.0 deployment guide covering per-vendor AV/EDR allowlist procedures for the top 6 enterprise EDR vendors. The guide is the primary operational artifact for milestone ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deployment guide scaffolding and Quick Start</name>
  <files>docs/operations/deployment-guide.md</files>
  <read_first>
    docs/operations/dpapi-recovery.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-CONTEXT.md
  </read_first>
  <action>
    Create `docs/operations/deployment-guide.md` with:
    1. A "Quick Start for Experienced Operators" checklist (10 bullets max):
       - Install signed MSI from release artifacts
       - Verify Authenticode signature via `signtool verify /pa`
       - Add hash exclusions for your EDR vendor (see Section 3; SHA-256 hashes from RELEASE_NOTES.md)
       - Grant SeSystemProfilePrivilege to agent service account
       - Reboot host (required for AppInit_DLLs / hook activation; see Section 4 for details)
       - Verify injection via `Get-Process | Where-Object {$_.Modules -match "dlp_hook_dll"}`
       - Test T4 file denial on a sample protected path
       - Check SIEM for `DlpAgentStarted` event
       - Verify Protected Paths screen shows expected entries
       - Confirm monitor mode is active (if deploying in Audit first)
    2. Major section headers matching dpapi-recovery.md style:
       - Prerequisites (hardware, software, AD setup, permissions)
       - Installation Steps (MSI install, service account, privilege assignment)
       - AV/EDR Allowlist Procedures (placeholder for 6 vendor subsections)
       - Secure Boot & PPL Considerations (placeholder headers only; see 57-04 for detailed content)
       - Post-Install Verification
       - Troubleshooting
       - Rollback Procedure
    3. Prerequisites section lists: Windows 11 Pro/Enterprise, .NET 8 runtime (for admin CLI), AD domain join, local admin rights, one supported EDR installed.
    4. Installation Steps section: download MSI, run installer, verify service registered (`Get-Service DlpAgent`), verify auto-start.
  </action>
  <verify>
    <automated>test -f docs/operations/deployment-guide.md && grep -q "Quick Start" docs/operations/deployment-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/operations/deployment-guide.md` exists and is >50 lines
    - File contains "Quick Start for Experienced Operators" checklist
    - File contains all 7 major section headers
    - Prerequisites section lists Windows 11, .NET 8, AD domain, admin rights
    - Quick Start explicitly references RELEASE_NOTES.md for SHA-256 hashes
  </acceptance_criteria>
  <done>Deployment guide scaffolding with Quick Start and major sections</done>
</task>

<task type="auto">
  <name>Task 2: Write per-vendor AV/EDR allowlist procedures</name>
  <files>docs/operations/deployment-guide.md</files>
  <read_first>
    docs/operations/deployment-guide.md
    dlp-agent/src/allowlist.rs
    .planning/phases/49-universal-injection-etw-process-watcher-allowlist-appinit-fa/49-CONTEXT.md
  </read_first>
  <action>
    Add per-vendor subsections under "AV/EDR Allowlist Procedures" for:
    1. **Microsoft Defender for Endpoint** — Windows Security app -> Virus & threat protection -> Manage settings -> Exclusions -> Add folder (`C:\Program Files\DLP\`) + Add file (by SHA-256 hash). Also cover Group Policy path for enterprise deployments.
    2. **CrowdStrike Falcon** — Falcon console -> Prevention -> Exclusions -> Add Global Exclusion (path + SHA-256 hash). Document the `SensorGroupingTag` approach for bulk deployment.
    3. **SentinelOne** — Management console -> Policies -> Exclusions -> Add exclusion (path + certificate hash). Note: SentinelOne uses certificate hash, not file hash. Include certificate thumbprint from RELEASE_NOTES.md Signing Certificate section.
    4. **Carbon Black (VMware)** — CB console -> Enforce -> Policies -> Add Exclusion (path + SHA-256 hash). Document the reputation override approach.
    5. **Sophos Intercept X** — Sophos Central -> Policies -> Threat Protection -> Add Exclusion (path + SHA-256). Document the tamper protection disable requirement.
    6. **Trend Micro Apex One** — Apex Central -> Policies -> Agent Configuration -> Scan Exclusions (path + SHA-256 hash). Document the smart scan vs. conventional scan difference.

    Each vendor section MUST include:
    - Expected detection behavior (what the EDR will flag without allowlist)
    - Console/UI steps with screenshot placeholders (e.g., `[Screenshot: CrowdStrike Falcon console -> Prevention -> Exclusions]`)
    - Hash exclusion examples using SHA-256 from RELEASE_NOTES.md (per D-05)
    - Verification command to confirm exclusion is active
    - Troubleshooting note for common issues
    - "Last verified" date placeholder and EDR version note (per D-20)

    Screenshot placeholder policy (per D-18, D-20):
    - Screenshots are acceptable as placeholders if noted "to be added during UAT execution"
    - Text-only procedures are acceptable for untested vendors
    - No fake screenshots
  </action>
  <verify>
    <automated>grep -c "## .*Microsoft\|CrowdStrike\|SentinelOne\|Carbon Black\|Sophos\|Trend Micro" docs/operations/deployment-guide.md | test $(cat) -ge 6</automated>
  </verify>
  <acceptance_criteria>
    - All 6 vendor subsections present
    - Each subsection has detection behavior, allowlist steps, hash exclusion, verification
    - Screenshot placeholders present for each vendor with UAT note
    - Verification commands included for each vendor
    - All hash references use SHA-256 per D-05
    - SentinelOne certificate hash exception documented with explanation
  </acceptance_criteria>
  <done>6 vendor-specific AV/EDR allowlist procedures written</done>
</task>

<task type="auto">
  <name>Task 3: Write placeholder headers for Secure Boot, PPL, and post-install sections</name>
  <files>docs/operations/deployment-guide.md</files>
  <read_first>
    docs/operations/deployment-guide.md
    dlp-agent/src/service.rs
    .planning/phases/49-universal-injection-etw-process-watcher-allowlist-appinit-fa/49-CONTEXT.md
  </read_first>
  <action>
    Add placeholder headers and minimal scaffolding ONLY (detailed content owned by 57-04 per D-24):
    1. **Secure Boot & PPL Considerations** section header with placeholder subsections:
       - `<!-- Detailed content: See Plan 57-04 for Secure Boot, PPL, DACL tripwire, privilege, and reboot documentation -->`
    2. **Post-Install Verification**:
       - Checklist of 10 verification steps: service running, injection visible in Process Hacker, T4 denial test, cloud sync blocking test, USB/SD insertion event, printer test, DACL tripwire visible in icacls, SIEM event received, admin TUI accessible, monitor mode confirmed.
    3. **Troubleshooting**:
       - "Hook not injecting" -> check EDR allowlist, verify SeSystemProfilePrivilege, check Event Viewer for `siem.appinit_dlls_disabled`
       - "T4 file still writable" -> check DACL tripwire, verify agent running, check Protected Paths screen
       - "High CPU" -> check ETW buffer size, verify allowlist coverage, check for injection loops
       - "Agent won't start" -> check JWT secret, verify SQLite permissions, check port binding
    4. **Rollback Procedure** (per D-25):
       - Step 1: Stop DlpAgent service (`Stop-Service DlpAgent`)
       - Step 2: Uninstall MSI (`msiexec /x {ProductCode}`)
       - Step 3: Restore original DACLs (`icacls /reset` on protected paths)
       - Step 4: Optional ProgramData cleanup (`Remove-Item -Recurse "C:\ProgramData\DLP"`)
       - Step 5: Verify no residual processes or services
  </action>
  <verify>
    <automated>grep -q "Rollback Procedure" docs/operations/deployment-guide.md && grep -q "Secure Boot" docs/operations/deployment-guide.md && grep -q "Troubleshooting" docs/operations/deployment-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - Secure Boot section contains placeholder reference to 57-04 (not detailed content)
    - Post-install verification has 10+ checklist items
    - Troubleshooting covers 4+ common issues with resolution steps
    - Rollback Procedure documents service stop, MSI uninstall, DACL restoration, ProgramData cleanup
  </acceptance_criteria>
  <done>Placeholder headers, post-install, troubleshooting, and rollback sections complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> operator action | Guide is advisory; operator must follow steps correctly |
| EDR console -> DLP allowlist | Vendor-specific UI; may change between versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Spoofing | Fake deployment guide | accept | Guide is in-tree; signed release tags protect integrity |
| T-57-02 | Denial of Service | EDR false-positive quarantine | mitigate | Allowlist procedures are the primary mitigation |
| T-57-03 | Information Disclosure | Screenshots leak internal console URLs | accept | Screenshots should be from test environments (per D-20) |
</threat_model>

<verification>
- docs/operations/deployment-guide.md exists and compiles (markdown lint)
- All 6 vendor subsections present with complete template
- Quick Start checklist present
- Troubleshooting section covers 4+ issues
- Rollback Procedure documented
- Secure Boot/PPL section contains placeholder reference to 57-04 (not duplicate content)
- No detailed Secure Boot/PPL/privilege/reboot content (owned by 57-04)
</verification>

<success_criteria>
- Deployment guide exists with complete scaffolding
- 6 vendor allowlist procedures written
- Post-install, troubleshooting, and rollback sections complete
- Secure Boot/PPL section has placeholder headers only (detailed content in 57-04)
- Guide follows dpapi-recovery.md style conventions
- All hash references use SHA-256 per D-05
</success_criteria>
