---
phase: 57-operational-deployment-guide-av-edr-allowlist-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/operations/deployment-guide.md
  - docs/RELEASE_NOTES.md
autonomous: true
requirements:
  - OPS-01
  - OPS-03
must_haves:
  truths:
    - "docs/operations/deployment-guide.md exists with full document structure"
    - "Pre-flight PowerShell checks section covers Secure Boot, SeSystemProfilePrivilege, signtool verify, and hash verification"
    - "Secure Boot reality is documented with AppInit_DLLs inert warning and siem.appinit_dlls_disabled audit event"
    - "PPL coverage gap is documented with lsass/MsMpEng/EDR-self examples and DACL-tripwire backstop explanation"
    - "Post-install reboot requirement is documented as mandatory, not optional"
    - "Explicit HTML comment placeholders exist for Plans 02-04 to target deterministically"
    - "Verification greps for each of the 10 expected section titles"
    - "Interface file existence checks validate cross-references or document fallback behavior"
  artifacts:
    - path: "docs/operations/deployment-guide.md"
      provides: "Master deployment guide with pre-flight checks and architecture reality sections"
      min_lines: 150
    - path: "docs/RELEASE_NOTES.md"
      provides: "Hash publishing template with SHA-256 and SHA-512 placeholders"
      contains: "## v0.10.0"
  key_links:
    - from: "docs/operations/deployment-guide.md"
      to: "docs/DEPLOYMENT.md"
      via: "cross-reference link"
      pattern: "DEPLOYMENT.md"
    - from: "docs/operations/deployment-guide.md"
      to: "docs/operations/dpapi-recovery.md"
      via: "cross-reference link"
      pattern: "dpapi-recovery.md"
---

<objective>
Create the master deployment guide foundation with document structure, pre-flight PowerShell checks, and architecture reality documentation (Secure Boot, PPL gaps, DACL backstop, SeSystemProfilePrivilege, reboot requirement).

Purpose: This is the foundational document that every operator will read before deploying v0.10.0. It must establish the deployment context and prerequisites before vendor-specific allowlist procedures are layered on top.
Output: `docs/operations/deployment-guide.md` with full structure and `docs/RELEASE_NOTES.md` with hash table template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-RESEARCH.md
@docs/operations/dpapi-recovery.md
@docs/DEPLOYMENT.md
@docs/OPERATIONAL.md
@docs/CHANGELOG.md
@.github/workflows/release.yml

<interfaces>
From docs/operations/dpapi-recovery.md:
- Document format: Overview, Prerequisites, Flow sections, PowerShell snippets, UAT Checklist, Rollback Procedures, References
- Uses `#Requires -RunAsAdministrator` in PowerShell examples
- Uses colour-coded Write-Result function pattern in scripts

From docs/DEPLOYMENT.md:
- MSI installer path: `installer/DLPAgent.wxs`, `installer/build.ps1`
- Binary paths: `C:\Program Files\DLP\`
- Service name: `dlp-agent`
- signtool sign command with DigiCert primary + Sectigo fallback
- signtool verify /pa for signature verification

From docs/OPERATIONAL.md:
- Log path: `C:\ProgramData\DLP\logs\audit.jsonl`
- Config path: `C:\ProgramData\DLP\agent-config.toml`
- Service management: `sc query dlp-agent`, `sc stop dlp-agent`
- SCM failure actions: restart after 60s x3, then log and stop

From 57-RESEARCH.md:
- Secure Boot: AppInit_DLLs is INERT when Secure Boot enabled
- PPL processes: lsass.exe, MsMpEng.exe, services.exe, csrss.exe cannot be injected
- DACL tripwire provides kernel-enforced backstop for T3/T4 paths
- SeSystemProfilePrivilege required for ETW trace session creation
- Post-install reboot required for complete hook coverage
- Pre-flight checks: Confirm-SecureBootUEFI, whoami /priv, signtool verify /pa, Get-FileHash
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/operations/deployment-guide.md document structure</name>
  <files>docs/operations/deployment-guide.md</files>
  <read_first>
    - docs/operations/dpapi-recovery.md (document format pattern)
    - docs/DEPLOYMENT.md (deployment overview, MSI paths, signtool commands)
    - docs/OPERATIONAL.md (service names, log paths, config paths)
  </read_first>
  <action>
    Create `docs/operations/deployment-guide.md` with the following sections in exact order:

    1. `# Deployment Guide -- DLP v0.10.0` -- title with version
    2. `## Overview` -- one-paragraph summary: this guide covers pre-flight checks, EDR allowlist procedures, hash verification, and UAT for v0.10.0 deployment. Cross-reference `docs/DEPLOYMENT.md` for build pipeline details and `docs/OPERATIONAL.md` for day-to-day operations.
    3. `## Prerequisites` -- table with: Windows 11 22H2+ (or Windows 10 22H2), PowerShell 5.1+, Administrator privileges, `signtool` (Windows SDK), physical or VM endpoint for UAT, EDR console access (at least one of the 6 vendors).
    4. `## Pre-Flight Checks` -- subsection for each check with exact PowerShell command and expected output:
       - `### Secure Boot Status` -- `Confirm-SecureBootUEFI` command, explain that `True` means AppInit_DLLs is INERT and ETW-driven injection is primary; `siem.appinit_dlls_disabled` will fire at boot. Include the UEFI CA 2023 certificate check from 57-RESEARCH.md.
       - `### SeSystemProfilePrivilege` -- `whoami /priv | findstr SeSystemProfilePrivilege` command, explain this is required for ETW trace session creation; document that MSI must preserve this privilege across upgrades.
       - `### Authenticode Signature Verification` -- `signtool verify /pa /v "C:\Program Files\DLP\dlp-agent.exe"` and `signtool verify /all /pa "C:\Program Files\DLP\dlp_hook_dll.dll"` commands; explain RFC-3161 timestamp verification.
       - `### Hash Verification` -- `Get-FileHash` commands for SHA-256 and SHA-512 against RELEASE_NOTES.md values; list all 6 binaries (dlp-agent.exe, dlp-user-ui.exe, dlp-admin-cli.exe, dlp-server.exe, dlp_hook_dll.dll, dlp_hook_dll_x86.dll).
    5. `## Architecture Reality Check` -- document the following realities an operator must understand:
       - `### Secure Boot and AppInit_DLLs` -- AppInit_DLLs is inert under Secure Boot; ETW-driven injection is primary; AppInit is tertiary fallback only on non-Secure-Boot endpoints; `siem.appinit_dlls_disabled` audit event fires at boot.
       - `### PPL Coverage Gap` -- lsass.exe, MsMpEng.exe, EDR self-processes are PPL-protected and cannot be injected; list these explicitly; explain that DACL tripwire (Phase 52) provides kernel-enforced backstop for T3/T4 paths even when hooks cannot inject.
       - `### DACL Tripwire Backstop` -- NTFS Deny ACE on T3/T4 root paths holds even when hook is unloaded or crashes; reference `docs/operations/dpapi-recovery.md` for repair watcher details.
       - `### SeSystemProfilePrivilege Preservation` -- MSI must preserve this privilege across upgrades; verification command; warning signs (ETW consumer fails after upgrade).
       - `### Post-Install Reboot Requirement` -- reboot is REQUIRED not optional; startup EnumProcesses sweep covers most processes but reboot ensures complete coverage; document warning signs.
    6. `## EDR Allowlist Procedures` -- placeholder heading with HTML comment markers for deterministic replacement:
       ```
       <!-- PLACEHOLDER: EDR-VENDORS-START -->
       See per-vendor sections below. Content added by Plans 02-03.
       <!-- PLACEHOLDER: EDR-VENDORS-END -->
       ```
    7. `## Hash Publishing and Verification` -- placeholder heading with HTML comment markers:
       ```
       <!-- PLACEHOLDER: HASH-PUBLISHING-START -->
       See RELEASE_NOTES.md and signtool verification section. Content added by Plan 04.
       <!-- PLACEHOLDER: HASH-PUBLISHING-END -->
       ```
    8. `## UAT Test Matrix` -- placeholder heading with HTML comment markers:
       ```
       <!-- PLACEHOLDER: UAT-MATRIX-START -->
       See `.planning/milestones/v0.10.0-UAT.md`. Content added by Plans 05-06.
       <!-- PLACEHOLDER: UAT-MATRIX-END -->
       ```
    9. `## Troubleshooting` -- common issues: EDR quarantine (check allowlist propagation), signature verification failure (check timestamp server), hash mismatch (re-download), Secure Boot confusion (re-read section above).
    10. `## References` -- list all cross-referenced documents: DEPLOYMENT.md, OPERATIONAL.md, dpapi-recovery.md, CHANGELOG.md.

    Follow the exact document format from `docs/operations/dpapi-recovery.md`: use `##` for top-level sections, `###` for subsections, fenced code blocks for PowerShell, tables for structured data. Do NOT use emojis. Use 4-space indentation in code blocks.
  </action>
  <verify>
    <automated>grep -c "^## " docs/operations/deployment-guide.md | grep -E "[8-9]|10"</automated>
  </verify>
  <done>
    - docs/operations/deployment-guide.md exists with all 10 sections
    - Pre-flight checks section has 4 subsections with exact PowerShell commands
    - Architecture Reality Check section has 5 subsections covering Secure Boot, PPL, DACL, SeSystemProfilePrivilege, reboot
    - HTML comment placeholders exist for EDR-VENDORS, HASH-PUBLISHING, and UAT-MATRIX
    - No emojis in the document
    - All cross-references to DEPLOYMENT.md, OPERATIONAL.md, dpapi-recovery.md are present
  </done>
</task>

<task type="auto">
  <name>Task 2: Create docs/RELEASE_NOTES.md hash publishing template</name>
  <files>docs/RELEASE_NOTES.md</files>
  <read_first>
    - docs/CHANGELOG.md (version format, existing release notes pattern)
    - .github/workflows/release.yml (list of shipped binaries)
  </read_first>
  <action>
    Create `docs/RELEASE_NOTES.md` with the following structure:

    1. `# Release Notes -- DLP v0.10.0` -- title
    2. `## Release Date` -- placeholder `[YYYY-MM-DD]`
    3. `## Binaries` -- table with columns: Binary, Architecture, Path in MSI
       - List all 6 binaries: dlp-agent.exe (x64), dlp-user-ui.exe (x64), dlp-admin-cli.exe (x64), dlp-server.exe (x64), dlp_hook_dll.dll (x64), dlp_hook_dll_x86.dll (x86)
       - Path column: `C:\Program Files\DLP\{binary_name}`
    4. `## SHA-256 Hashes` -- table with columns: Binary, SHA-256 Hash
       - All 6 binaries with placeholder `[TO BE FILLED AT RELEASE]`
    5. `## SHA-512 Hashes` -- table with columns: Binary, SHA-512 Hash
       - All 6 binaries with placeholder `[TO BE FILLED AT RELEASE]`
    6. `## Authenticode Verification` -- exact commands:
       - `signtool verify /pa /v "C:\Program Files\DLP\dlp-agent.exe"`
       - `signtool verify /all /pa "C:\Program Files\DLP\dlp_hook_dll.dll"`
       - Expected output pattern showing "sha256" and "RFC3161"
    7. `## WDSI Submission` -- reference to deployment-guide.md WDSI section (added in Plan 04)
    8. `## Known Issues` -- placeholder for any known issues at release time
    9. `## Upgrade Notes` -- reference to SeSystemProfilePrivilege preservation, reboot requirement, EDR allowlist re-verification

    The document must be reproducible: an operator with the release artifacts and this document can verify every binary independently.
  </action>
  <verify>
    <automated>grep -c "SHA-256\|SHA-512\|signtool verify" docs/RELEASE_NOTES.md</automated>
  </verify>
  <done>
    - docs/RELEASE_NOTES.md exists with SHA-256 and SHA-512 hash tables for all 6 binaries
    - Authenticode verification commands with expected output patterns are documented
    - WDSI submission section references deployment-guide.md
    - All placeholders are clearly marked [TO BE FILLED AT RELEASE]
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> Target endpoint | Operator runs pre-flight checks on endpoint; commands require admin privileges |
| Downloaded MSI -> Installed binary | Hash verification boundary; SHA-256/SHA-512 must match RELEASE_NOTES.md |
| Binary -> Authenticode signature | signtool verify /pa confirms signature + timestamp validity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Tampering | Downloaded MSI | mitigate | SHA-256/SHA-512 hash verification against RELEASE_NOTES.md per OPS-02 |
| T-57-02 | Tampering | RELEASE_NOTES.md itself | accept | Hosted in git repository with commit history; operator verifies git signature |
| T-57-03 | Denial of Service | Expired Authenticode signature | mitigate | RFC-3161 timestamp verification with signtool; documented in deployment guide |
| T-57-04 | Information Disclosure | Pre-flight check output reveals system config | accept | Commands run locally by admin; no network transmission |
</threat_model>

<verification>
- docs/operations/deployment-guide.md exists and has all 10 sections
- docs/RELEASE_NOTES.md exists with hash tables for all 6 binaries
- Both documents follow the dpapi-recovery.md format pattern
- No emojis in either document
- Cross-references to DEPLOYMENT.md, OPERATIONAL.md, dpapi-recovery.md are valid
- HTML comment placeholders exist for EDR-VENDORS, HASH-PUBLISHING, UAT-MATRIX
- Verification greps for each of the 10 expected section titles
</verification>

<success_criteria>
- An operator reading docs/operations/deployment-guide.md understands Secure Boot reality, PPL gaps, DACL backstop, and reboot requirement before proceeding to vendor allowlist steps
- An operator can run all pre-flight PowerShell commands without modification
- RELEASE_NOTES.md provides a complete hash verification template ready for release-time population
- Plans 02-04 can deterministically replace placeholder sections using HTML comment markers
</success_criteria>

<output>
Create `.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-01-SUMMARY.md` when done
</output>
