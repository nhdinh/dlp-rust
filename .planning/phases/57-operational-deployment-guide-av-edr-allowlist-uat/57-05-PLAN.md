---
phase: 57-operational-deployment-guide-av-edr-allowlist-uat
plan: 05
type: execute
wave: 3
depends_on:
  - 57-01
  - 57-02
  - 57-03
  - 57-04
files_modified:
  - scripts/Uat-CloudSync.ps1
  - scripts/Uat-PrintBlock.ps1
  - scripts/Uat-HookDll.ps1
  - scripts/Uat-DaclTripwire.ps1
  - scripts/Uat-EtwNtdll.ps1
  - scripts/Uat-Benchmark.ps1
autonomous: true
requirements:
  - OPS-04
must_haves:
  truths:
    - "Uat-CloudSync.ps1 exercises every v0.9.0 cloud-sync regression test against real OneDrive/Google Drive/Dropbox/Box clients"
    - "Uat-PrintBlock.ps1 exercises print enforcement against real printers"
    - "Uat-HookDll.ps1 exercises hook DLL injection scenarios (new process, x86/WoW64, AV/EDR skip, PPL skip, startup sweep)"
    - "Uat-DaclTripwire.ps1 exercises DACL tripwire (T4/T3 deny, SYSTEM allow, tamper alert, staged removal)"
    - "Uat-EtwNtdll.ps1 exercises ETW bypass detection and ntdll patching (where enabled) and monitor mode"
    - "Uat-Benchmark.ps1 measures CRIT-04 benchmark gate (<= 25% wall-clock overhead on cargo build + Office app launch)"
    - "All scripts follow the Uat-UsbBlock.ps1 pattern: strict mode, typed parameters, colour-coded output, comprehensive error handling"
    - "Benchmark script checks for Rust/cargo presence and skips gracefully with WARN if missing"
    - "CloudSync script warns operator about clipboard destruction before clearing"
    - "Benchmark runs all baseline measurements first, then all hooked measurements"
  artifacts:
    - path: "scripts/Uat-CloudSync.ps1"
      provides: "Cloud sync regression UAT script"
      min_lines: 100
    - path: "scripts/Uat-PrintBlock.ps1"
      provides: "Print enforcement UAT script"
      min_lines: 100
    - path: "scripts/Uat-HookDll.ps1"
      provides: "Hook DLL injection UAT script"
      min_lines: 100
    - path: "scripts/Uat-DaclTripwire.ps1"
      provides: "DACL tripwire UAT script"
      min_lines: 100
    - path: "scripts/Uat-EtwNtdll.ps1"
      provides: "ETW bypass + ntdll + monitor mode UAT script"
      min_lines: 100
    - path: "scripts/Uat-Benchmark.ps1"
      provides: "CRIT-04 benchmark measurement script"
      min_lines: 80
  key_links:
    - from: "scripts/Uat-CloudSync.ps1"
      to: "dlp-server admin API"
      via: "Invoke-RestMethod"
      pattern: "Invoke-RestMethod.*admin"
    - from: "scripts/Uat-Benchmark.ps1"
      to: "CRIT-04 requirement"
      via: "25% threshold check"
      pattern: "25"
---

<objective>
Create six UAT PowerShell scripts that exercise every shipped v0.9.0 and v0.10.0 capability on real Windows hardware: cloud-sync regression, print enforcement, hook DLL injection, DACL tripwire, ETW bypass/ntdll/monitor mode, and CRIT-04 benchmark measurement.

Purpose: The milestone ship gate requires real-hardware validation. These scripts automate the test execution and produce PASS/FAIL output that feeds into the UAT results capture template. The original overloaded ActiveBlocking script is split into three focused scripts (HookDll, DaclTripwire, EtwNtdll) per review feedback.
Output: scripts/Uat-CloudSync.ps1, Uat-PrintBlock.ps1, Uat-HookDll.ps1, Uat-DaclTripwire.ps1, Uat-EtwNtdll.ps1, Uat-Benchmark.ps1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/Uat-UsbBlock.ps1 (pattern for UAT scripts)
@scripts/Uat-ReadMe.md (UAT documentation pattern)
@.planning/ROADMAP.md (Phase 57 success criteria, v0.9.0 and v0.10.0 features)
@.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-RESEARCH.md

<interfaces>
From scripts/Uat-UsbBlock.ps1:
- Pattern: #Requires -RunAsAdministrator
- Pattern: [CmdletBinding()] param block with typed parameters
- Pattern: $ErrorActionPreference = 'Stop'; Set-StrictMode -Version Latest
- Pattern: Write-Result function with PASS/FAIL/INFO/WARN levels and colour coding
- Pattern: Helper functions with doc comments
- Pattern: Main orchestration with try/finally for cleanup
- Pattern: Summary with pass/fail counts and exit codes

From ROADMAP.md Phase 57 success criteria:
- v0.9.0 cloud-sync regression: OneDrive, Google Drive, Dropbox, Box clients
- v0.10.0 active-blocking: hook DLL, DACL tripwire, ETW bypass detection
- CRIT-04 benchmark gate: <= 25% wall-clock overhead on cargo build + Office app launch
- Real printers, USB/SD/optical/virtual drives

From 57-RESEARCH.md:
- Benchmark preconditions: disable Windows Update, pause AV scans, close unnecessary apps, run 3 times take median
- Cloud clients: OneDrive, Google Drive, Dropbox, Box
- Volume classes: LocalNTFS, USBRemovable, SDCard, Optical, Virtual, NetworkShare
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/Uat-CloudSync.ps1</name>
  <files>scripts/Uat-CloudSync.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern, parameter style, error handling)
    - docs/CHANGELOG.md (v0.9.0 cloud-sync features: CLOUD_UPLOAD, SHARE_LINK, WFP egress blocking)
  </read_first>
  <action>
    Create `scripts/Uat-CloudSync.ps1` following the exact pattern from `Uat-UsbBlock.ps1`:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "Cloud sync client regression UAT for DLP v0.10.0"
    - `.DESCRIPTION`: Tests cloud upload blocking for T3/T4 files via OneDrive, Google Drive, Dropbox, and Box clients. Verifies that v0.9.0 cloud-sync enforcement continues to work under v0.10.0 universal hook. IMPORTANT: The clipboard clearing test (SHARE_LINK) will DESTROY the current clipboard contents. The operator should save any important clipboard data before running this test.
    - Parameters: ServerUrl (default http://127.0.0.1:9090), JwtToken (default $env:DLP_ADMIN_JWT), SkipOneDrive, SkipGoogleDrive, SkipDropbox, SkipBox switches
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Test-CloudClientInstalled: checks if each cloud client process exists (OneDrive.exe, GoogleDriveFS.exe, Dropbox.exe, BoxDrive.exe)
      - Test-CloudUploadBlocked: creates a T4 test file, attempts copy to cloud sync folder, verifies denial and audit event
      - Test-ShareLinkBlocked: copies a shareable URL to clipboard, verifies clipboard cleared on DENY
      - Get-AuditEvents: queries audit.jsonl for cloud-related events
    - Main flow:
      1. Validate JWT token
      2. Detect installed cloud clients
      3. For each installed client (not skipped):
         a. Create a T4 test file in a monitored path
         b. Attempt to copy to the client's sync folder
         c. Verify the copy is denied (ERROR_ACCESS_DENIED or specific error)
         d. Verify an audit event was emitted with action=CLOUD_UPLOAD
         e. Clean up test file
      4. For each installed client (not skipped):
         a. Create a T4 file with shareable content
         b. Copy a share link URL to clipboard
         c. Verify clipboard is cleared (SHARE_LINK denial)
         d. Verify audit event with action=SHARE_LINK
      5. Output PASS/FAIL summary
      6. Exit 0 if all pass, exit 1 if any fail

    Cloud client sync folder paths:
    - OneDrive: $env:OneDrive (or $env:OneDriveConsumer / $env:OneDriveCommercial)
    - Google Drive: $env:USERPROFILE\Google Drive (or $env:USERPROFILE\My Drive)
    - Dropbox: $env:USERPROFILE\Dropbox
    - Box: $env:USERPROFILE\Box

    Test file creation: use a temporary T4-classified file. The script does NOT need to register the classification via the server; it assumes the test endpoint has T4 policies configured. Document this assumption in the .DESCRIPTION.

    Use the same colour-coded output and strict error handling as Uat-UsbBlock.ps1.
  </action>
  <verify>
    <automated>grep -c "Test-CloudUploadBlocked" scripts/Uat-CloudSync.ps1 && grep -c "Test-ShareLinkBlocked" scripts/Uat-CloudSync.ps1 && grep -c "Write-Result" scripts/Uat-CloudSync.ps1</automated>
  </verify>
  <done>
    - Uat-CloudSync.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include ServerUrl, JwtToken, and Skip* switches for each client
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Test-CloudUploadBlocked and Test-ShareLinkBlocked functions present
    - Main flow tests all 4 cloud clients (OneDrive, Google Drive, Dropbox, Box)
    - Clipboard destruction warning in .DESCRIPTION
    - Cleanup in finally block
    - Exit code 0 on all pass, 1 on fail
    - No emojis in the script
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/Uat-PrintBlock.ps1</name>
  <files>scripts/Uat-PrintBlock.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern, parameter style)
    - docs/CHANGELOG.md (v0.9.0 print features: PRINT action, PrintWatcher, XPS parser)
  </read_first>
  <action>
    Create `scripts/Uat-PrintBlock.ps1` following the Uat-UsbBlock.ps1 pattern:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "Print enforcement UAT for DLP v0.10.0"
    - `.DESCRIPTION`: Tests print blocking for T3/T4 files via the Windows print spooler. Requires a real printer (local USB or network). Verifies that v0.9.0 print enforcement continues to work under v0.10.0.
    - Parameters: ServerUrl, JwtToken, PrinterName (optional -- auto-detect if not specified), SkipBlockedTest, SkipAuditTest
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Get-InstalledPrinters: lists printers via Get-WmiObject Win32_Printer, presents numbered menu
      - Test-PrintBlocked: creates a T4 test file, sends to printer via Out-Printer or .NET PrintDocument, verifies job is cancelled or denied
      - Test-PrintAuditEvent: queries audit.jsonl for PRINT action events
      - Get-PrintJobStatus: checks if print job exists in queue (should be deleted on DENY)
    - Main flow:
      1. Validate JWT token
      2. Detect installed printers (local and network)
      3. If PrinterName not specified, present numbered menu
      4. If no printers detected, emit WARN and exit
      5. Create a T4 test file
      6. Send to selected printer
      7. Verify the print job is blocked (job cancelled or not created)
      8. Verify audit event with action=PRINT
      9. Clean up test file
      10. Output PASS/FAIL summary

    Document that this test requires:
    - A real printer (USB or network) installed and online
    - The printer must be accessible from the test account
    - T4 print policy must be configured on the server

    Use the same colour-coded output and strict error handling as Uat-UsbBlock.ps1.
  </action>
  <verify>
    <automated>grep -c "Test-PrintBlocked" scripts/Uat-PrintBlock.ps1 && grep -c "Get-InstalledPrinters" scripts/Uat-PrintBlock.ps1 && grep -c "Write-Result" scripts/Uat-PrintBlock.ps1</automated>
  </verify>
  <done>
    - Uat-PrintBlock.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include ServerUrl, JwtToken, PrinterName, Skip* switches
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Get-InstalledPrinters and Test-PrintBlocked functions present
    - Main flow tests print blocking on a real printer
    - Cleanup in finally block
    - Exit code 0 on all pass, 1 on fail
    - No emojis in the script
  </done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/Uat-HookDll.ps1</name>
  <files>scripts/Uat-HookDll.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern, parameter style)
    - .planning/ROADMAP.md (v0.10.0 features: hook DLL injection, x86/WoW64, AV/EDR skip, PPL skip, startup sweep)
  </read_first>
  <action>
    Create `scripts/Uat-HookDll.ps1` following the Uat-UsbBlock.ps1 pattern:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "Hook DLL injection UAT for DLP v0.10.0"
    - `.DESCRIPTION`: Tests universal hook DLL injection scenarios: new process injection, x86/WoW64 injection, AV/EDR process skip, PPL process skip, and startup EnumProcesses sweep coverage. Does NOT test DACL, ETW bypass, ntdll patching, or monitor mode -- those are in separate scripts.
    - Parameters: ServerUrl, JwtToken, SkipNewProcessTest, SkipX86Test, SkipAvEdrTest, SkipPplTest, SkipStartupSweepTest
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Test-HookDllInjectedNewProcess: starts a new notepad.exe process, waits 500ms for ETW watcher injection, verifies dlp_hook_dll.dll is in the module list, cleans up notepad process
      - Test-HookDllInjectedX86: starts a 32-bit process (e.g., notepad.exe from SysWOW64), verifies dlp_hook_dll_x86.dll is loaded
      - Test-AvEdrProcessesSkipped: enumerates known AV/EDR processes (MsMpEng, csagent, SentinelAgent, etc.), verifies no hook DLL is loaded
      - Test-PplProcessesSkipped: enumerates known PPL processes (lsass, services, csrss), verifies no hook DLL is loaded
      - Test-StartupSweepCoverage: enumerates all running non-allowlisted user processes, verifies hook DLL is present in each
      - Get-ProcessModules: helper to enumerate loaded modules in a process
    - Main flow:
      1. Validate JWT token
      2. Test new process injection (if not skipped)
      3. Test x86/WoW64 injection (if not skipped)
      4. Test AV/EDR process skip (if not skipped)
      5. Test PPL process skip (if not skipped)
      6. Test startup sweep coverage (if not skipped)
      7. Output PASS/FAIL summary

    Document prerequisites:
    - dlp-agent must be running and connected to dlp-server
    - At least one non-allowlisted user process must be running for sweep test

    Use the same colour-coded output and strict error handling as Uat-UsbBlock.ps1.
  </action>
  <verify>
    <automated>grep -c "Test-HookDllInjectedNewProcess" scripts/Uat-HookDll.ps1 && grep -c "Test-AvEdrProcessesSkipped" scripts/Uat-HookDll.ps1 && grep -c "Test-PplProcessesSkipped" scripts/Uat-HookDll.ps1</automated>
  </verify>
  <done>
    - Uat-HookDll.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include ServerUrl, JwtToken, and Skip* switches for each test
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Test-HookDllInjectedNewProcess, Test-HookDllInjectedX86, Test-AvEdrProcessesSkipped, Test-PplProcessesSkipped, Test-StartupSweepCoverage functions present
    - Main flow tests all 5 hook injection scenarios
    - Cleanup in finally block
    - Exit code 0 on all pass, 1 on fail
    - No emojis in the script
  </done>
</task>

<task type="auto">
  <name>Task 4: Create scripts/Uat-DaclTripwire.ps1</name>
  <files>scripts/Uat-DaclTripwire.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern, parameter style)
    - .planning/ROADMAP.md (v0.10.0 features: DACL tripwire, Protected Paths, repair watcher)
  </read_first>
  <action>
    Create `scripts/Uat-DaclTripwire.ps1` following the Uat-UsbBlock.ps1 pattern:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "DACL tripwire UAT for DLP v0.10.0"
    - `.DESCRIPTION`: Tests DACL tripwire behavior: T4/T3 write denial with agent stopped, SYSTEM write allowance, tamper alert on icacls /reset, and staged removal safety. Does NOT test hook injection, ETW bypass, ntdll patching, or monitor mode -- those are in separate scripts.
    - Parameters: ServerUrl, JwtToken, ProtectedPath (default "C:\Protected\T4"), SkipDenyTest, SkipSystemAllowTest, SkipTamperAlertTest, SkipStagedRemovalTest
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Test-T4WriteDeniedAgentStopped: stops dlp-agent service, creates a T4 file under ProtectedPath, verifies write is denied (ERROR_ACCESS_DENIED from NTFS), restarts dlp-agent service
      - Test-T3WriteDeniedAgentStopped: same as above but for T3 path
      - Test-SystemWriteAllowed: verifies SYSTEM can still write to T4 path even with agent stopped
      - Test-IcaclsResetTriggersAlert: runs `icacls /reset` on ProtectedPath, queries audit events for DaclTamperDetected within 60 seconds
      - Test-StagedRemovalSafe: removes DACL deny ACE using proper staged removal procedure, verifies no tamper alert within 60 seconds
      - Get-AuditEvents: queries audit.jsonl or server API for DACL-related events
    - Main flow:
      1. Validate JWT token
      2. Test T4 write denied with agent stopped (if not skipped)
      3. Test T3 write denied with agent stopped (if not skipped)
      4. Test SYSTEM write allowed (if not skipped)
      5. Test icacls /reset tamper alert (if not skipped)
      6. Test staged removal safety (if not skipped)
      7. Output PASS/FAIL summary

    Document prerequisites:
    - dlp-agent must be running and connected to dlp-server
    - At least one Protected Path must be configured (T3 or T4)
    - The test account must have admin privileges to stop/start the agent service

    Use the same colour-coded output and strict error handling as Uat-UsbBlock.ps1.
  </action>
  <verify>
    <automated>grep -c "Test-T4WriteDeniedAgentStopped" scripts/Uat-DaclTripwire.ps1 && grep -c "Test-IcaclsResetTriggersAlert" scripts/Uat-DaclTripwire.ps1 && grep -c "Test-StagedRemovalSafe" scripts/Uat-DaclTripwire.ps1</automated>
  </verify>
  <done>
    - Uat-DaclTripwire.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include ServerUrl, JwtToken, ProtectedPath, and Skip* switches
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Test-T4WriteDeniedAgentStopped, Test-T3WriteDeniedAgentStopped, Test-SystemWriteAllowed, Test-IcaclsResetTriggersAlert, Test-StagedRemovalSafe functions present
    - Main flow tests all 5 DACL tripwire scenarios
    - Cleanup in finally block (restores agent service)
    - Exit code 0 on all pass, 1 on fail
    - No emojis in the script
  </done>
</task>

<task type="auto">
  <name>Task 5: Create scripts/Uat-EtwNtdll.ps1</name>
  <files>scripts/Uat-EtwNtdll.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern, parameter style)
    - .planning/ROADMAP.md (v0.10.0 features: ETW bypass detection, ntdll patching, monitor mode)
  </read_first>
  <action>
    Create `scripts/Uat-EtwNtdll.ps1` following the Uat-UsbBlock.ps1 pattern:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "ETW bypass detection, ntdll patching, and monitor mode UAT for DLP v0.10.0"
    - `.DESCRIPTION`: Tests ETW bypass detection, ntdll syscall-stub patching (if enabled), and monitor-only mode. Does NOT test hook injection or DACL tripwire -- those are in separate scripts.
    - Parameters: ServerUrl, JwtToken, SkipEtwTest, SkipNtdllTest, SkipMonitorModeTest
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Test-EtwBypassDetection: creates a test process that performs a file write to a Protected Path without the hook DLL loaded, queries bypass alerts API, verifies BypassAlert with correlation_reason=NoHookJournal within 5 seconds. NOTE: This test does NOT require a custom bypass binary. It uses a simple approach: start a process, suspend it before hook injection, perform the write, then resume and check alerts.
      - Test-NtdllPatching: checks if enable_ntdll_patching is true in agent config. If enabled, verifies that a direct syscall attempt is blocked (STATUS_ACCESS_DENIED). If disabled, skips with INFO. NOTE: This test does NOT read process memory to verify JMP trampolines. It verifies behavior (direct syscall blocked) rather than implementation detail.
      - Test-MonitorMode: creates a test policy in Audit mode via admin API, waits for policy sync, attempts a T4 file write that would be denied in Block mode, verifies the write succeeds, verifies audit event shows policy_mode=Audit and would_have_denied=true, restores policy to Block mode
      - Get-BypassAlerts: queries server API for bypass alerts
      - Get-AgentConfig: reads agent-config.toml or queries server for current config
    - Main flow:
      1. Validate JWT token
      2. Test ETW bypass detection (if not skipped)
      3. Test ntdll patching (if not skipped and enabled in config)
      4. Test monitor mode (if not skipped)
      5. Output PASS/FAIL summary

    Document prerequisites:
    - dlp-agent must be running and connected to dlp-server
    - At least one Protected Path must be configured (T3 or T4)
    - For ntdll test: requires enable_ntdll_patching=true in agent config (test auto-skips if disabled)
    - For monitor mode test: requires a test policy that can be temporarily set to Audit

    Use the same colour-coded output and strict error handling as Uat-UsbBlock.ps1.
  </action>
  <verify>
    <automated>grep -c "Test-EtwBypassDetection" scripts/Uat-EtwNtdll.ps1 && grep -c "Test-NtdllPatching" scripts/Uat-EtwNtdll.ps1 && grep -c "Test-MonitorMode" scripts/Uat-EtwNtdll.ps1</automated>
  </verify>
  <done>
    - Uat-EtwNtdll.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include ServerUrl, JwtToken, and Skip* switches for each test
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Test-EtwBypassDetection, Test-NtdllPatching, Test-MonitorMode functions present
    - ETW bypass test does NOT require custom bypass binary (uses suspend/resume approach)
    - ntdll test verifies behavior (syscall blocked) not implementation (JMP trampolines)
    - ntdll test auto-skips if patching is disabled in config
    - Main flow tests all 3 ETW/ntdll/monitor scenarios
    - Cleanup in finally block (restores policies)
    - Exit code 0 on all pass, 1 on fail
    - No emojis in the script
  </done>
</task>

<task type="auto">
  <name>Task 6: Create scripts/Uat-Benchmark.ps1</name>
  <files>scripts/Uat-Benchmark.ps1</files>
  <read_first>
    - scripts/Uat-UsbBlock.ps1 (Write-Result pattern)
    - .planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-RESEARCH.md (benchmark preconditions)
  </read_first>
  <action>
    Create `scripts/Uat-Benchmark.ps1` following the Uat-UsbBlock.ps1 pattern:

    - `#Requires -RunAsAdministrator`
    - `.SYNOPSIS`: "CRIT-04 benchmark measurement for DLP v0.10.0"
    - `.DESCRIPTION`: Measures wall-clock overhead of DLP hook DLL on representative workloads: cargo build and Office application launch. Compares with-hooks vs without-hooks performance. The benchmark gate is <= 25% overhead.
    - Parameters: Workloads (array, default @("cargo","office")), Runs (int, default 3), ThresholdPercent (double, default 25.0), SkipPreconditionCheck
    - Write-Result function identical to Uat-UsbBlock.ps1
    - Helper functions:
      - Test-Preconditions: checks Windows Update is not running, no AV scan active, sufficient free memory (>4GB), dlp-agent is running
      - Test-RustAvailable: checks if `cargo` is available in PATH. If not, emits WARN and skips cargo workload.
      - Measure-CargoBuild: clones a small Rust repo (or uses existing), runs `cargo build`, returns elapsed milliseconds
      - Measure-OfficeLaunch: launches winword.exe or excel.exe, measures time to window visible, closes application
      - Calculate-Overhead: computes percentage overhead from baseline and hooked measurements
      - Format-Results: produces a markdown table of results
    - Main flow:
      1. Check preconditions (unless skipped):
         - Windows Update service stopped or not downloading
         - No active AV scan (check Windows Defender MpCmdRun status)
         - Free memory > 4GB
         - dlp-agent service is running
      2. Check Rust availability (cargo in PATH). If missing, skip cargo workload with WARN.
      3. For each workload:
         a. Run N times with dlp-agent STOPPED (baseline) -- run ALL baseline measurements first
         b. Start dlp-agent service
         c. Run N times with dlp-agent RUNNING (hooked) -- run ALL hooked measurements after baselines
         d. Calculate median for both sets
         e. Compute overhead percentage: ((hooked_median - baseline_median) / baseline_median) * 100
         f. Record PASS if overhead <= threshold, FAIL otherwise
      4. Output results table:
         | Workload | Baseline (ms) | Hooked (ms) | Overhead (%) | Status |
      5. Output overall result: PASS if all workloads pass, FAIL otherwise
      6. Save results to a file: C:\ProgramData\DLP\logs\uat-benchmark-{timestamp}.json

    Cargo build test:
    - Use a small Rust project (e.g., hello-world or the dlp-rust workspace itself)
    - Run `cargo clean` before each build
    - Measure from process start to "Finished dev" message
    - Use Measure-Command { cargo build --release }
    - If cargo is not available, skip with WARN and continue with Office test

    Office launch test:
    - Launch winword.exe or excel.exe (whichever is installed)
    - Measure from process start to main window visible
    - Use .NET ProcessStartInfo and WaitForInputIdle
    - Close the application after measurement
    - If Office is not installed, skip this test with WARN

    Document benchmark preconditions from 57-RESEARCH.md:
    - Disable Windows Update
    - Pause AV real-time scans
    - Close unnecessary applications
    - Ensure > 4GB free RAM
    - Run 3 times, take median
    - Exclude outliers > 2 standard deviations

    The script must be reproducible: an operator on any Windows 11 machine with Rust and Office can run it and get comparable results.
  </action>
  <verify>
    <automated>grep -c "Measure-CargoBuild" scripts/Uat-Benchmark.ps1 && grep -c "Measure-OfficeLaunch" scripts/Uat-Benchmark.ps1 && grep -c "ThresholdPercent" scripts/Uat-Benchmark.ps1</automated>
  </verify>
  <done>
    - Uat-Benchmark.ps1 exists with #Requires -RunAsAdministrator
    - Parameters include Workloads, Runs, ThresholdPercent, SkipPreconditionCheck
    - Write-Result function with PASS/FAIL/INFO/WARN levels
    - Measure-CargoBuild and Measure-OfficeLaunch functions present
    - Test-RustAvailable checks for cargo and skips gracefully with WARN
    - Precondition checks for Windows Update, AV scan, free memory
    - Baseline measurements run FIRST, then hooked measurements (not interleaved)
    - Results table with baseline, hooked, overhead, and status columns
    - Results saved to C:\ProgramData\DLP\logs\uat-benchmark-{timestamp}.json
    - Overall PASS/FAIL based on <= 25% threshold
    - No emojis in the script
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT script -> dlp-server | JWT-authenticated API calls over localhost |
| UAT script -> Target process | Hook injection and process manipulation |
| UAT script -> File system | Test file creation and deletion in monitored paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-17 | Denial of Service | UAT script leaves test files or policies in bad state | mitigate | Cleanup in finally block; restore policies and services |
| T-57-18 | Tampering | UAT script modifies production policies | mitigate | Test policies are clearly named; restored after test |
| T-57-19 | Information Disclosure | UAT script logs JWT token | accept | Token is short-lived admin JWT; script runs locally |
| T-57-20 | Denial of Service | Benchmark runs during active system use, skewing results | mitigate | Precondition checks for Windows Update, AV scans, free memory |
</threat_model>

<verification>
- scripts/Uat-CloudSync.ps1 exists with cloud client detection and upload/share-link tests
- scripts/Uat-PrintBlock.ps1 exists with printer detection and print blocking tests
- scripts/Uat-HookDll.ps1 exists with hook injection, x86, AV/EDR skip, PPL skip, startup sweep tests
- scripts/Uat-DaclTripwire.ps1 exists with T4/T3 deny, SYSTEM allow, tamper alert, staged removal tests
- scripts/Uat-EtwNtdll.ps1 exists with ETW bypass, ntdll patching (behavioral), monitor mode tests
- scripts/Uat-Benchmark.ps1 exists with cargo build and Office launch benchmarks
- All scripts follow Uat-UsbBlock.ps1 pattern: strict mode, typed parameters, colour-coded output
- Benchmark script checks for Rust/cargo and skips gracefully
- CloudSync script warns about clipboard destruction
- Benchmark runs baseline first, then hooked (not interleaved)
- All scripts have cleanup in finally blocks
- All scripts exit 0 on pass, 1 on fail
- No emojis in any script
</verification>

<success_criteria>
- An operator can run Uat-CloudSync.ps1 against real cloud clients and get PASS/FAIL results
- An operator can run Uat-PrintBlock.ps1 against a real printer and get PASS/FAIL results
- An operator can run Uat-HookDll.ps1 on a real Windows 11 endpoint and validate hook injection
- An operator can run Uat-DaclTripwire.ps1 and validate DACL tripwire behavior
- An operator can run Uat-EtwNtdll.ps1 and validate ETW bypass, ntdll, and monitor mode
- An operator can run Uat-Benchmark.ps1 and get overhead percentages with PASS/FAIL against the 25% gate
- All scripts are self-contained and require no external dependencies beyond PowerShell 5.1+
- No script requires custom test binaries (all tests use standard Windows tools)
</success_criteria>

<output>
Create `.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-05-SUMMARY.md` when done
</output>
