---
phase: 57-operational-deployment-guide-av-edr-allowlist-uat
plan: 06
type: execute
wave: 3
depends_on:
  - 57-03
  - 57-04
  - 57-05
files_modified:
  - docs/operations/deployment-guide.md
  - RELEASE_NOTES.md
  - .planning/milestones/v0.10.0-UAT.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - Full workspace compiles with zero warnings
    - All markdown files are valid and lint-free
    - Cross-references between docs are consistent
    - No broken internal links
    - Deployment guide references all prior phase capabilities correctly
    - 57-03 execution completion is verified (not just plan existence)
    - Screenshot placeholder policy enforced based on UAT completion status
    - Automated TODO/placeholder detection runs
    - Stale version references (v0.9.x) checked
  artifacts:
    - path: docs/operations/deployment-guide.md
      provides: Final integrated deployment guide
      contains: "v0.10.0", "Quick Start", "Troubleshooting"
---

<objective>
Final integration verification: ensure all Phase 57 artifacts are consistent, cross-referenced, and complete. Fix any gaps between plans. This plan is blocked on 57-03 execution completion (not just plan existence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-reference and consistency check</name>
  <files>docs/operations/deployment-guide.md, RELEASE_NOTES.md, .planning/milestones/v0.10.0-UAT.md</files>
  <read_first>
    docs/operations/deployment-guide.md
    RELEASE_NOTES.md
    .planning/milestones/v0.10.0-UAT.md
    .planning/ROADMAP.md
  </read_first>
  <action>
    Perform consistency checks across all Phase 57 artifacts:
    1. **Cross-reference check**:
       - Deployment guide references RELEASE_NOTES.md for hashes
       - RELEASE_NOTES.md references deployment guide
       - UAT document references deployment guide for installation steps
       - All internal markdown links are valid (no broken relative paths)
    2. **Binary name consistency**:
       - All 6 binary names match across RELEASE_NOTES.md, deployment guide, and Cargo.toml
       - dlp-agent.exe, dlp-server.exe, dlp-admin-cli.exe, dlp-user-ui.exe, dlp-hook-dll.dll, dlp-hook-dll-x86.dll
    3. **Vendor list consistency**:
       - Same 6 vendors named in deployment guide, CONTEXT.md, and ROADMAP.md
       - Microsoft Defender for Endpoint, CrowdStrike Falcon, SentinelOne, Carbon Black, Sophos, Trend Micro
    4. **Version references**:
       - All docs reference "v0.10.0" consistently
       - No references to abandoned v1.0.0 or incorrect milestone names
       - No stale v0.9.x references that should be v0.10.0
    5. **Capability references**:
       - Deployment guide references correct phase numbers for each capability
       - Hook injection -> Phase 49
       - DACL tripwire -> Phase 52
       - ETW bypass -> Phase 53
       - Monitor mode -> Phase 55
       - Volume class -> Phase 56
    6. Fix any inconsistencies found
  </action>
  <verify>
    <automated>
      # Check for stale v1.0.0 references
      grep -c "v1.0.0" docs/operations/deployment-guide.md RELEASE_NOTES.md .planning/milestones/v0.10.0-UAT.md 2>/dev/null | grep -v ":0" || echo "No v1.0.0 references found"
      # Check for stale v0.9 references without v0.10.0 nearby
      grep -n "v0\.9" docs/operations/deployment-guide.md RELEASE_NOTES.md .planning/milestones/v0.10.0-UAT.md 2>/dev/null | grep -v "v0.10.0" || echo "No stale v0.9 references found"
    </automated>
  </verify>
  <acceptance_criteria>
    - All internal links valid
    - Binary names consistent across all docs
    - Vendor list consistent
    - Version references correct (v0.10.0, no stale v0.9.x)
    - Phase references correct
  </acceptance_criteria>
  <done>Cross-reference consistency verified</done>
</task>

<task type="auto">
  <name>Task 2: Markdown lint and formatting</name>
  <files>docs/operations/deployment-guide.md, RELEASE_NOTES.md, .planning/milestones/v0.10.0-UAT.md</files>
  <read_first>
    docs/operations/deployment-guide.md
    RELEASE_NOTES.md
    .planning/milestones/v0.10.0-UAT.md
  </read_first>
  <action>
    Run markdown linting and formatting:
    1. Check for common markdown issues:
       - No trailing whitespace
       - Consistent heading levels (no skipping from # to ###)
       - Tables have consistent column counts
       - Code blocks have language tags
       - Links use relative paths (not absolute)
    2. Fix any formatting issues
    3. Ensure all markdown files end with a single newline
    4. Verify tables render correctly (no broken pipe characters)
  </action>
  <verify>
    <automated>file docs/operations/deployment-guide.md RELEASE_NOTES.md .planning/milestones/v0.10.0-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - No trailing whitespace
    - Consistent heading levels
    - Tables have consistent columns
    - Code blocks have language tags
    - Files end with single newline
  </acceptance_criteria>
  <done>Markdown lint and formatting complete</done>
</task>

<task type="auto">
  <name>Task 3: Automated link validation and placeholder detection</name>
  <files>docs/operations/deployment-guide.md, RELEASE_NOTES.md</files>
  <read_first>
    docs/operations/deployment-guide.md
    RELEASE_NOTES.md
  </read_first>
  <action>
    Run automated checks:
    1. **Markdown link validation**:
       ```bash
       grep -oP '\[.*?\]\(\K[^)]+' docs/operations/deployment-guide.md | while read link; do
         if [[ "$link" =~ ^http ]]; then
           echo "External: $link (skip)"
         elif [[ "$link" =~ ^# ]]; then
           echo "Anchor: $link (skip)"
         else
           test -f "$link" || echo "Broken: $link"
         fi
       done
       ```
    2. **TODO/placeholder detection**:
       ```bash
       grep -in "TODO\|FIXME\|to be filled\|placeholder" docs/operations/deployment-guide.md RELEASE_NOTES.md .planning/milestones/v0.10.0-UAT.md 2>/dev/null | grep -vi "screenshot placeholder.*UAT\|to be added during UAT" || echo "No unresolved placeholders found"
       ```
    3. **Screenshot placeholder policy enforcement**:
       - If 57-03 UAT execution is complete (UAT document has PASS/FAIL results):
         - Screenshot placeholders must be resolved or marked "N/A - vendor not tested"
         - No unresolved `[Screenshot: ...]` without justification
       - If 57-03 UAT execution is pending:
         - Screenshot placeholders are acceptable with note "to be added during UAT execution"
    4. Fix any broken links or unresolved placeholders
  </action>
  <verify>
    <automated>
      # Check for unresolved placeholders (excluding allowed screenshot placeholders)
      grep -in "TODO\|FIXME\|to be filled" docs/operations/deployment-guide.md RELEASE_NOTES.md .planning/milestones/v0.10.0-UAT.md 2>/dev/null | grep -vi "screenshot.*UAT\|to be added during UAT" | wc -l | xargs -I {} test {} -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - All relative markdown links resolve to existing files
    - No unresolved TODO/placeholder text (except allowed screenshot placeholders)
    - Screenshot placeholder policy enforced based on UAT completion status
  </acceptance_criteria>
  <done>Automated link validation and placeholder detection complete</done>
</task>

<task type="auto">
  <name>Task 4: Final completeness verification</name>
  <files>All Phase 57 artifacts</files>
  <read_first>
    .planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-CONTEXT.md
    docs/operations/deployment-guide.md
    RELEASE_NOTES.md
    .planning/milestones/v0.10.0-UAT.md
    .planning/phases/57-operational-deployment-guide-av-edr-allowlist-uat/57-VERIFICATION.md
  </read_first>
  <action>
    Final completeness check:
    1. Verify all 4 OPS requirements are addressed:
       - OPS-01: Deployment guide exists with 6 vendor procedures
       - OPS-02: RELEASE_NOTES.md with hashes, artifact provenance, signing cert, WDSI, signtool
       - OPS-03: Deployment reality documented (Secure Boot, PPL, DACL, privilege, reboot) — canonical in 57-04
       - OPS-04: UAT executed with results captured, CRIT-04 verified, Category J completed
    2. Verify all CONTEXT.md decisions are implemented:
       - D-01 through D-27 (check each against artifacts)
    3. Verify no duplicate Secure Boot/PPL/privilege/reboot content between 57-01 and 57-04:
       - 57-01 should have placeholder headers only
       - 57-04 should have canonical detailed content
    4. Verify 57-03 execution completion:
       - UAT document must have actual PASS/FAIL results (not just plan structure)
       - CRIT-04 benchmark must have recorded percentages
       - Category J must have operational verification results
    5. Create a final summary of Phase 57 deliverables
  </action>
  <verify>
    <automated>
      # Verify 57-03 has execution results (not just plan)
      grep -c "PASS\|FAIL" .planning/milestones/v0.10.0-UAT.md | test $(cat) -ge 5
      # Verify no duplicate detailed content in 57-01
      grep -c "AppInit_DLLs.*inert\|PPL coverage gap\|SeSystemProfilePrivilege.*assignment" docs/operations/deployment-guide.md | test $(cat) -ge 1
    </automated>
  </verify>
  <acceptance_criteria>
    - All 4 OPS requirements verified
    - All CONTEXT.md decisions addressed
    - No duplicate content between 57-01 and 57-04
    - 57-03 execution completion verified (actual results present)
    - Deliverables summary created
  </acceptance_criteria>
  <done>Phase 57 final verification complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs -> operator trust | Consistency builds confidence |
| Cross-references -> accuracy | Broken links undermine credibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-12 | Information Disclosure | Internal doc references leak structure | accept | In-tree docs are internal |
| T-57-13 | Denial of Service | Broken links frustrate operators | mitigate | Automated link validation with concrete commands |
| T-57-19 | Tampering | Placeholder text shipped | mitigate | Automated TODO/placeholder detection with grep |
</threat_model>

<verification>
- Cross-references consistent
- Markdown lint clean
- All requirements addressed
- No unresolved placeholders (except allowed screenshot placeholders)
- All relative links valid
- 57-03 execution completion verified
</verification>

<success_criteria>
- All Phase 57 artifacts consistent and cross-referenced
- Markdown formatting clean
- All OPS requirements verified
- No duplicate content between 57-01 and 57-04
- 57-03 execution completion confirmed
- Automated link validation passes
- Automated placeholder detection passes
- Phase ready for execution or ship
</success_criteria>
