---
status: complete
phase: 50.1-close-gap-fail-01-02-03-verify-isolated-resync-healthy-recovery-at-runtime
source:
  - 50.1-01-SUMMARY.md
started: 2026-06-21T17:00:40Z
updated: 2026-06-21T17:00:40Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Workspace compiles cleanly with cargo build --all. All crates build without errors.
result: pass

### 2. ISOLATED -> RESYNC via Background Thread
expected: After 10 pipe failures put the DLL in ISOLATED state, a new cache version from the agent flips the DLL to RESYNC within one background-thread cycle.
result: pass

### 3. RESYNC -> HEALTHY Hysteresis
expected: The DLL stays in RESYNC until 5 consecutive pipe successes occur, then transitions to HEALTHY.
result: pass

### 4. Full Cycle End-to-End
expected: HEALTHY -> DEGRADED -> ISOLATED -> RESYNC -> HEALTHY cycle completes in a single integration test, with state transitions visible via FailModeState snapshots.
result: pass

### 5. Cross-Crate Checksum Validation
expected: An agent-written CacheHeader is accepted by the hook-dll reader; the checksum field is identical on both sides (128-byte ABI alignment verified).
result: pass

### 6. In-Flight Decision Uses Old Cache
expected: During RESYNC, a decision started before the cache flip still uses the old cache version and returns a consistent result; no panic or torn read occurs.
result: pass

### 7. Odd Version During Rebuild Ignored
expected: When the agent rebuilds the cache, the DLL ignores odd (in-progress) version words and only flips on even version words.
result: pass

### 8. Stale Cache Triggers ISOLATED Helper
expected: A cache that exceeds its tier-specific staleness budget (T4=30s, T3=60s, T2=5min, T1=30min) forces pipe fallback / ISOLATED behavior.
result: pass

### 9. RESYNC Pipe Failure Returns ISOLATED
expected: If a pipe call fails while in RESYNC state, the DLL returns to ISOLATED rather than staying in RESYNC or advancing to HEALTHY.
result: pass

### 10. LRU Flush on RESYNC
expected: The per-thread LRU cache invalidates cached entries when the global cache version flips during RESYNC.
result: pass

### 11. Asymmetric Decisions in ISOLATED
expected: In ISOLATED state, T3/T4 writes are denied, T1/T2 writes are allowed, and reads are always allowed.
result: pass

## Summary

total: 11
passed: 11
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
