---
phase: 58.9-close-gap-diff-04-agent-diagnostics-producer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/diagnostic_aggregator.rs
  - dlp-agent/src/server_client.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - "DIFF-04 criterion 1 (client half): `ServerClient::submit_diagnostic_snapshot` POSTs `{pid, snapshots}` to `POST /agents/{id}/diagnostics` with header `Authorization: DLP-AGENT {agent_id}:{agent_auth_hash}` and maps non-2xx (incl. 429) to `ServerClientError::ServerError { status, body }` (best-effort; loop retries next interval) — per RESEARCH A4."
    - "`DiagnosticAggregator::drain_all(&self) -> Vec<DiagnosticSnapshot>` is destructive: it removes every key from the backing `DashMap`, returns all snapshots sorted by `timestamp_qpc` descending, and leaves `dll_count()==0` / `total_snapshot_count()==0`; a second call returns empty (proves no duplicate re-push to the append-only server store) — per RESEARCH Pitfall 2."
    - "`drain_all` returns an OWNED `Vec` and drops every `DashMap` guard before returning, so callers can `.await` the HTTP POST without holding a non-`Send` guard — per RESEARCH Pitfall 4."
  artifacts:
    - path: dlp-agent/src/diagnostic_aggregator.rs
      provides: Destructive batch drain used by the periodic push loop
      exports:
        - fn drain_all
    - path: dlp-agent/src/server_client.rs
      provides: Authenticated agent diagnostics POST helper
      exports:
        - fn submit_diagnostic_snapshot
  key_links:
    - from: dlp-agent/src/server_client.rs::submit_diagnostic_snapshot
      to: dlp-server POST /agents/{id}/diagnostics (shipped 58.8-02)
      via: "reqwest POST .json(&DiagnosticsPayload { pid, snapshots }) with header `DLP-AGENT {agent_id}:{agent_auth_hash}`; body shape MUST match server `AgentDiagnosticsIngest { pid: u32, snapshots: Vec<DiagnosticSnapshot> }` (admin_api.rs:5611)"
    - from: dlp-agent/src/diagnostic_aggregator.rs::drain_all
      to: dlp-agent/src/diagnostic_aggregator.rs::snapshots (Arc<DashMap<String, Vec<DiagnosticSnapshot>>>)
      via: "collect keys via .iter().map(key.clone), pre-size Vec::with_capacity(total), remove each key, append, sort by timestamp_qpc desc; NO guard held across an await"
---

<objective>
Build the agent-side producer primitives for DIFF-04 that every later plan depends on: (1) a destructive `DiagnosticAggregator::drain_all()` that empties the in-process buffer and returns owned snapshots sorted newest-first, and (2) a `ServerClient::submit_diagnostic_snapshot` that POSTs a `{pid, snapshots}` batch to the authenticated server endpoint shipped in 58.8-02. Both are unit-tested in isolation (no server crate dependency for the client tests; a local axum mock + the existing `unreachable_client()` harness). This plan is `dlp-agent`-only and is parallelizable with Plan 02 (the `dlp-hook-dll` emit side).

Purpose: DIFF-04 criterion 1 requires an authenticated agent push of drained diagnostics; these two primitives are the drain source and the transport. They must exist before Plan 03 wires the periodic loop and the IPC ingest arm.
Output: `DiagnosticAggregator::drain_all` + 1 unit test; `ServerClient::submit_diagnostic_snapshot` + 3 unit tests.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.9/58.9-RESEARCH.md
@.planning/phases/58.9/58.9-PATTERNS.md
@.planning/phases/58.9/58.9-VALIDATION.md
@dlp-agent/src/diagnostic_aggregator.rs
@dlp-agent/src/server_client.rs
@dlp-common/src/hook_ipc.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add destructive DiagnosticAggregator::drain_all and unit test</name>
  <files>dlp-agent/src/diagnostic_aggregator.rs</files>
  <read_first>
    - dlp-agent/src/diagnostic_aggregator.rs (full file: struct at 46-52, ingest at 85-95, get_snapshots at 108-120, get_snapshots_paginated at 134-144, dll_count/total_snapshot_count at 148-156, existing tests at 223-409)
    - .planning/phases/58.9/58.9-PATTERNS.md section "`dlp-agent/src/diagnostic_aggregator.rs` — add `drain_all`" (the exact target method + doc-comment shape) and "Shared Patterns: DashMap guard discipline"
    - .planning/phases/58.9/58.9-RESEARCH.md "Anti-Patterns: Non-destructive read" and "Pitfall 2 / Pitfall 4"
  </read_first>
  <behavior>
    - `drain_all(&self) -> Vec<DiagnosticSnapshot>` removes every entry from `self.snapshots` and returns all previously buffered snapshots sorted by `timestamp_qpc` descending (newest first).
    - After the call, `dll_count() == 0` and `total_snapshot_count() == 0`; a second `drain_all()` returns an empty `Vec` (destructive — no duplicate re-push to the append-only `diagnostic_store`).
    - No `dashmap::mapref` guard is held across an await point: keys are collected first, then each key is `remove`d, results appended into a pre-sized `Vec`, then sorted; the method returns an owned `Vec`.
  </behavior>
  <action>
    In `dlp-agent/src/diagnostic_aggregator.rs`, add a public method `pub fn drain_all(&self) -> Vec<DiagnosticSnapshot>` immediately after `get_snapshots_paginated` (~line 144). Implement per PATTERNS.md (collect keys via `self.snapshots.iter().map(|e| e.key().clone()).collect::<Vec<String>>()`; compute `total` for `Vec::with_capacity`; for each key, `if let Some((_, mut v)) = self.snapshots.remove(&key) { out.append(&mut v) }`; finally `out.sort_by(|a, b| b.timestamp_qpc.cmp(&a.timestamp_qpc)); out`). Mark `#[must_use]`. Add a full doc comment per CLAUDE.md 9.3 covering `# Returns` and the destructive/guard discipline rationale (do NOT inline the word that the commit-time negative gate scans for inside this prose; describe it as "the periodic server-push task" and "append-only server store").
    In the existing `#[cfg(test)] mod tests` (~223-409), add `test_drain_all`: ingest across 2-3 distinct keys (different `{pid}_{agent_id}`), call `drain_all()`, assert (a) the returned `Vec` is sorted by `timestamp_qpc` descending, (b) `dll_count() == 0` and `total_snapshot_count() == 0` afterward, and (c) a second `drain_all()` is empty. Mirror the snapshot-construction helper used by `test_ingest_multiple_dlls` / `test_dll_count_and_total`. Do not use `.unwrap()`; use `?` or direct assertions.
    GitNexus (CLAUDE.md): before editing, run `impact` on `DiagnosticAggregator::ingest` and report blast radius in the SUMMARY; run `detect_changes()` before committing.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent diagnostic_aggregator -- --nocapture && cargo clippy -p dlp-agent -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `dlp-agent/src/diagnostic_aggregator.rs` contains `pub fn drain_all(&self) -> Vec<DiagnosticSnapshot>` with a `# Returns` doc comment and `#[must_use]`.
    - Behavior: `cargo test -p dlp-agent test_drain_all` exits 0 and asserts sorted-desc order, emptied map (`dll_count()==0`, `total_snapshot_count()==0`), and an empty second drain.
    - Behavior: `cargo clippy -p dlp-agent -- -D warnings` exits 0 (no `unwrap`, no held-guard-across-await pattern).
    - Source guard: the method body contains `self.snapshots.remove(&key)` (destructive) and does NOT call `get_snapshots` (non-destructive clone) as its implementation.
  </acceptance_criteria>
  <done>`DiagnosticAggregator::drain_all` exists, is destructive, returns owned newest-first snapshots, drops all guards before returning, and is covered by `test_drain_all`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ServerClient::submit_diagnostic_snapshot with auth-header/serde/unreachable tests</name>
  <files>dlp-agent/src/server_client.rs</files>
  <read_first>
    - dlp-agent/src/server_client.rs (ServerClientError enum at 54-71; agent_auth_hash field at 335; agent_id() at 417; set_agent_auth_hash at 425; submit_health_snapshot at 443-481; unreachable_client() at 1369-1379; the three test_submit_health_snapshot_* tests at 1984-2122)
    - .planning/phases/58.9/58.9-PATTERNS.md section "`dlp-agent/src/server_client.rs` — add `submit_diagnostic_snapshot`" and "Shared Patterns: Authentication header / Error handling"
    - .planning/phases/58.9/58.9-RESEARCH.md "Don't Hand-Roll: Authenticated server POST" and "Pitfall 3 (401 vs 400)" and Assumption A4
  </read_first>
  <behavior>
    - `submit_diagnostic_snapshot(&self, agent_id: &str, pid: u32, snapshots: &[DiagnosticSnapshot]) -> Result<(), ServerClientError>` POSTs to `{base_url}/agents/{agent_id}/diagnostics` with header `Authorization: DLP-AGENT {agent_id}:{self.agent_auth_hash}` and a JSON body of shape `{ "pid": <u32>, "snapshots": <[DiagnosticSnapshot]> }`.
    - 2xx -> `Ok(())`; any non-2xx (including 429 rate-limited and 5xx) -> `Err(ServerClientError::ServerError { status, body })` so the periodic loop treats it as a logged, non-fatal retry-next-interval case (A4: rate limit honored by the 60 s cadence + this non-fatal mapping).
    - Network/serialization errors map via the existing `#[from] reqwest::Error` / `#[from] serde_json::Error` variants (no new error variant, no `.context()`).
  </behavior>
  <action>
    In `dlp-agent/src/server_client.rs`, add `pub async fn submit_diagnostic_snapshot(&self, agent_id: &str, pid: u32, snapshots: &[dlp_common::hook_ipc::DiagnosticSnapshot]) -> Result<(), ServerClientError>` immediately after `submit_health_snapshot` (~line 481). Build the URL with `format!("{}/agents/{}/diagnostics", self.base_url, agent_id)`. Define a private `#[derive(Serialize)] struct DiagnosticsPayload<'a> { pid: u32, snapshots: &'a [dlp_common::hook_ipc::DiagnosticSnapshot] }` (lifetime-tied slice to avoid cloning the batch). Send with `self.client.post(&url).header("Authorization", format!("DLP-AGENT {}:{}", agent_id, self.agent_auth_hash)).json(&DiagnosticsPayload { pid, snapshots }).send().await?`. On non-success, read status + body (`resp.text().await.unwrap_or_else(|_| "<no body>".to_string())`) and return `ServerClientError::ServerError { status, body }`. On success, `tracing::debug!(agent_id, pid, count = snapshots.len(), "diagnostic snapshots submitted")` then `Ok(())`. Do NOT add `.context(...)`, do NOT add a new `ServerClientError` variant, do NOT log snapshot contents (paths/SIDs are sensitive per CLAUDE.md 9.13) — log counts + ids only. Add a full doc comment (`# Arguments`, `# Errors`).
    Add three unit tests in the existing `#[cfg(test)] mod tests`, cloned from `test_submit_health_snapshot_*` (1984-2122) and reusing `unreachable_client()` (1369-1379) plus the local-axum `Router` on `127.0.0.1:0` capture harness: (1) `test_submit_diagnostic_snapshot_unreachable_server` -> expects `Err`; (2) `test_submit_diagnostic_snapshot_payload_serde` -> capture body, assert JSON has top-level `pid` (u32) and `snapshots` array keys and round-trips a `DiagnosticSnapshot`; (3) `test_submit_diagnostic_snapshot_sets_agent_auth_header` -> capture headers, assert `authorization == DLP-AGENT <agent_id>:<hash>`. Use `client.set_agent_auth_hash("hash-123".to_string())` as at 1986.
    GitNexus (CLAUDE.md): before editing, run `impact` on `submit_health_snapshot` (the cloned hot-path) and report blast radius; run `detect_changes()` before committing.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent server_client -- --nocapture && cargo clippy -p dlp-agent -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `dlp-agent/src/server_client.rs` contains `pub async fn submit_diagnostic_snapshot(&self, agent_id: &str, pid: u32, snapshots: &[dlp_common::hook_ipc::DiagnosticSnapshot]) -> Result<(), ServerClientError>`.
    - Source: the request uses header literal `Authorization` with value formatted `DLP-AGENT {}:{}` from `agent_id` and `self.agent_auth_hash`, and body type carries fields `pid: u32` and `snapshots`.
    - Behavior: `cargo test -p dlp-agent test_submit_diagnostic_snapshot_unreachable_server test_submit_diagnostic_snapshot_payload_serde test_submit_diagnostic_snapshot_sets_agent_auth_header` exits 0 (unreachable -> Err; body has `pid` + `snapshots`; header present).
    - Behavior: `cargo clippy -p dlp-agent -- -D warnings` exits 0; no `.context(` and no new `ServerClientError` variant added.
  </acceptance_criteria>
  <done>Agent can POST a `{pid, snapshots}` batch to the authenticated diagnostics endpoint using the project's existing error model, with three unit tests proving the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent in-process aggregator -> ServerClient | Snapshots may contain file paths / SIDs (sensitive); must never be logged, only counted. |
| agent -> server POST /agents/{id}/diagnostics | Agent-authenticated ingest endpoint; bearer is the configured `agent_auth_hash`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.9-01 | Information Disclosure | `submit_diagnostic_snapshot` logging | mitigate | Log only `agent_id`, `pid`, and `count`; never log snapshot bodies (CLAUDE.md 9.13). Snapshot contents at debug-or-lower are forbidden. |
| T-58.9-02 | Repudiation / Spoofing | agent POST to server | accept (server-side) | Server `agent_auth_middleware` verifies `DLP-AGENT {id}:{hash}` against the enrolled `DLPAuthHash`; spoofing requires the secret (handled server-side in 58.8-02, not changed here). |
| T-58.9-03 | Tampering | duplicate snapshot inflation | mitigate | `drain_all` is destructive so each snapshot is forwarded exactly once to the append-only store (RESEARCH Pitfall 2). |
| T-58.9-04 | Denial of Service | memory growth in aggregator between pushes | mitigate | `drain_all` empties the map each interval; per-DLL cap already enforced in `ingest` (max_entries_per_dll). |
| T-58.9-SC | Tampering | cargo installs | mitigate | No new external packages; all dependencies already locked (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p dlp-agent diagnostic_aggregator` passes (incl. `test_drain_all`).
- `cargo test -p dlp-agent server_client` passes (incl. the three `test_submit_diagnostic_snapshot_*` tests).
- `cargo clippy -p dlp-agent -- -D warnings` passes.
- `cargo fmt -p dlp-agent --check` passes.
</verification>

<success_criteria>
- DIFF-04 criterion 1 client half is satisfied: `submit_diagnostic_snapshot` posts `{pid, snapshots}` with the `DLP-AGENT {id}:{hash}` header and treats non-2xx (incl. 429) as a non-fatal `ServerError`.
- `drain_all` is destructive, sorted newest-first, empties the map, and drops all `DashMap` guards before returning.
- Both primitives are unit-tested; the touched crate is clippy-clean and fmt-clean.
</success_criteria>

<output>
Create `.planning/phases/58.9/58.9-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **Aggregator method:**
  - `dlp_agent::diagnostic_aggregator::DiagnosticAggregator::drain_all(&self) -> Vec<dlp_common::hook_ipc::DiagnosticSnapshot>` (destructive; newest-first; empties map; `#[must_use]`).
- **Client method:**
  - `dlp_agent::server_client::ServerClient::submit_diagnostic_snapshot(&self, agent_id: &str, pid: u32, snapshots: &[DiagnosticSnapshot]) -> Result<(), ServerClientError>` (header `Authorization: DLP-AGENT {agent_id}:{agent_auth_hash}`; body `{pid, snapshots}`; non-2xx -> `ServerError { status, body }`).
  - Private `DiagnosticsPayload<'a> { pid: u32, snapshots: &'a [DiagnosticSnapshot] }` (module-local `#[derive(Serialize)]`).
- **New unit tests:**
  - `dlp_agent::diagnostic_aggregator::tests::test_drain_all`
  - `dlp_agent::server_client::tests::test_submit_diagnostic_snapshot_unreachable_server`
  - `dlp_agent::server_client::tests::test_submit_diagnostic_snapshot_payload_serde`
  - `dlp_agent::server_client::tests::test_submit_diagnostic_snapshot_sets_agent_auth_header`

## Assumption resolutions (recorded for convergence)

- **A1 (cross-crate scope):** The `dlp-hook-dll -> dlp-agent` feed is IN-SCOPE (handled in Plan 02). This plan is `dlp-agent`-only and does not by itself make `GET /admin/diagnostics` observable — that requires Plan 02 (emit) + Plan 03 (ingest + push). ROADMAP criterion 3 is proven end-to-end in Plan 04.
- **A4 (rate limit):** "Honoring the per-agent rate limit" = the 60 s push cadence (Plan 03) plus mapping 429 to a non-fatal `ServerError` so the loop retries next interval. A single explicit retry on `StatusCode::TOO_MANY_REQUESTS` is acceptable to add in Plan 03's loop if the reviewer requires it; the client method itself stays a single-shot POST (mirror `submit_health_snapshot`).
