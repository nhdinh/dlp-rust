---
status: complete
phase: 58.9-close-gap-diff-04-agent-diagnostics-producer
source: [58.9-01-SUMMARY.md, 58.9-02-SUMMARY.md, 58.9-03-SUMMARY.md, 58.9-04-PLAN.md]
started: 2026-07-10T20:43:36Z
updated: 2026-07-10T21:20:00Z
mode: backend-infrastructure
notes: |
  Pure backend Rust phase (DIFF-04). Ground-truthed by running cargo test gates.
  dlp-agent --lib (--test-threads=1): 986 passed / 0 failed.
  dlp-hook-dll --lib: 333 passed / 0 failed / 8 ignored.
  dlp-server health_api_integration: 9 passed / 0 failed.
  dlp-agent hook_ipc_integration: 17 passed / 1 failed (idle_injected_process_...
    is a pre-existing real-DLL-injection EnumProcessModules environment failure,
    out of scope for 58.9).
  Gap G1 (WR-02 test-harness regression) RESOLVED in commit aeb1b13f.
---

## Current Test

[testing complete]

## Tests

### 1. Operator confirmation — diagnostics pipeline observable end-to-end
expected: DIFF-04 end-to-end observable + auth enforced; WR-01..WR-04 fixes hold; all coverage gates green
result: pass
source: automated
verification: Tests 2-12 green (drain_all, submit_diagnostics, DLL emit, ingest, push_loop, lifecycle, 401/400 auth, round-trip, WR-01..04 hold). Full DLL->agent->server->admin path observable with auth enforced; G1 harness regression resolved in aeb1b13f.

### 2. [58.9-01 D1] drain_all destructive + newest-first + at-most-once
expected: drain_all empties DashMap, returns snapshots sorted by timestamp_qpc desc; second call empty; no DashMap guard held across await
result: pass
source: automated
coverage_id: 58.9-01/D1
verification: cargo test -p dlp-agent --lib (985 passed; test_drain_all ok)

### 3. [58.9-01 D2] submit_diagnostic_snapshot auth + non-fatal non-2xx
expected: POSTs {pid,snapshots} with header `DLP-AGENT {id}:{hash}`; non-2xx (incl 429) -> non-fatal ServerError; logs only agent_id/pid/count
result: pass
source: automated
coverage_id: 58.9-01/D2
verification: cargo test -p dlp-agent --lib (test_submit_diagnostic_snapshot_{unreachable_server,payload_serde,sets_agent_auth_header} ok)

### 4. [58.9-02 D1] pipe_client::send_diagnostics bincode-stable + best-effort
expected: reuses IpcPayloadV1::DiagnosticsResponse (discriminant-stable) via send_raw_oneway; no pipe -> Err(ConnectionRefused) without panic
result: pass
source: automated
coverage_id: 58.9-02/D1
verification: cargo test -p dlp-hook-dll --lib (333 passed; send_diagnostics_no_pipe_is_best_effort ok)

### 5. [58.9-02 D2] diagnostic emit cadence counter
expected: DIAGNOSTIC_EMIT_INTERVAL/COUNTER incremented and zeroed by reset_perf_counters
result: pass
source: automated
coverage_id: 58.9-02/D2
verification: cargo test -p dlp-hook-dll --lib (emit_state_transition_* ok)

### 6. [58.9-02 D3] cadence drains ring best-effort; deny path never blocked
expected: on cadence drains ring + one-way pushes; no pipe -> returns normally, ring drained, deny path never blocked
result: pass
source: automated
coverage_id: 58.9-02/D3
verification: cargo test -p dlp-hook-dll --lib (trampolines::tests::diagnostics_cadence_drains_ring_best_effort ok)

### 7. [58.9-03 D1] DiagnosticsResponse ingests into aggregator (PID anti-spoof)
expected: one-way DiagnosticsResponse frame to HookIpcServer ingested under key {pid}_{agent_id}, incrementing total_snapshot_count()
result: pass
source: automated
coverage_id: 58.9-03/D1
verification: cargo test -p dlp-agent --test hook_ipc_integration -> diagnostics_response_ingests_into_aggregator ok (after aeb1b13f switched the test to the one-way no-read send + aggregator side-effect assertion).

### 8. [58.9-03 D2] diagnostic_push_loop drains+pushes+flush-and-stop
expected: drains via drain_all + POSTs via submit_diagnostic_snapshot(agent_id,0,snapshots); on shutdown exactly one final drain+push, aggregator empty
result: pass
source: automated
coverage_id: 58.9-03/D2
verification: cargo test -p dlp-agent --lib (service::diagnostic_push_loop_drains_pushes_and_shuts_down ok)

### 9. [58.9-03 D3] lifecycle spawn populates handles iff ServerClient present
expected: None client -> (None,None); Some(client) -> (Some,Some); clean shutdown on watch signal
result: pass
source: automated
coverage_id: 58.9-03/D3
verification: cargo test -p dlp-agent --lib (service::diagnostic_push_spawn_populates_handles_when_server_present ok)

### 10. [58.9-04] auth boundary: 401 unauthenticated + 400 valid-but-mismatched id (never 500)
expected: no/wrong Authorization -> 401; valid hash for DIFFERENT agent_id -> 400 (NOT 401, NOT 500)
result: pass
source: automated
coverage_id: 58.9-04/auth-boundary
verification: cargo test -p dlp-server --test health_api_integration (9 passed; post_agent_diagnostics_rejects_{unauthenticated,mismatched_agent_id} + unprovisioned_auth_hash_returns_401_not_leaking_500 ok)

### 11. [58.9-04] end-to-end round-trip preserves snapshot fields
expected: POST {pid,snapshots} valid -> 200 {received:true}; GET /admin/diagnostics -> hook_function/user_sid unchanged
result: pass
source: automated
coverage_id: 58.9-04/round-trip
verification: cargo test -p dlp-server --test health_api_integration (post_agent_diagnostics_surfaces_in_admin_diagnostics ok)

### 12. [review-fix] WR-01..WR-04 hold after fixes
expected: WR-01 re-buffer on failure; WR-02 no ACK for one-way frames; WR-03 chunk <64 MiB; WR-04 bounded shutdown
result: pass
source: automated
verification: WR-01/03/04 hold (service re-buffer + bounded-shutdown compile+pass; hook-dll trampolines chunking green). WR-02 holds AND its test harnesses are now consistent with the one-way contract (aeb1b13f): dlp-agent --lib (--test-threads=1) 986 passed incl. bypass test; hook_ipc_integration 6 one-way PUSH tests pass. WR-05 deferred (documented in REVIEW-FIX).

## Summary

total: 12
passed: 12
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "WR-02 (skip ACK for one-way IPC frames) must hold WITHOUT breaking the agent test suites"
  status: resolved
  resolution_commit: aeb1b13f
  reason: "WR-02 changed the server contract (5 one-way frame types -> 'continue', no ACK) which matches the real DLL, but left agent test harnesses on the old request-response contract; affected tests read an ACK that never comes and hung."
  severity: major
  test: 12
  root_cause: "Test-harness/contract drift after WR-02 (lib bypass test + 6 integration PUSH tests used send_raw/send_envelope which read a response for genuinely one-way frames)."
  fix_applied: "Added send_raw_oneway() helper; switched bypass lib test + 6 integration PUSH tests (HealthResponse x4, DiagnosticsResponse, HashEvidence) to no-read send + aggregator/HashCache side-effect assertions. Request-response tests unchanged. dlp-agent --lib 986 passed (--test-threads=1); hook_ipc_integration 17 passed (1 pre-existing unrelated injection-env failure)."
  acknowledged_residuals:
    - "dlp-agent --lib under DEFAULT parallel threads aborts with STATUS_STACK_BUFFER_OVERRUN in some service::* reconciliation/unhook tests — PRE-EXISTING global-state test-isolation flakiness (tests share global shutdown/unhook signals without #[serial]); passes cleanly with --test-threads=1. Out of scope for 58.9 (58.5/earlier lifecycle); not a 58.9 deliverable and not introduced by this phase."
    - "hook_ipc_integration::idle_injected_process_sends_poll_control_with_creation_time fails with EnumProcessModules failed during real DLL injection — PRE-EXISTING environment/elevation sensitivity (58.5 unhook/injection path). Out of scope for 58.9."
