---
status: partial
phase: 58.9-close-gap-diff-04-agent-diagnostics-producer
source: [58.9-01-SUMMARY.md, 58.9-02-SUMMARY.md, 58.9-03-SUMMARY.md, 58.9-04-PLAN.md]
started: 2026-07-10T20:43:36Z
updated: 2026-07-10T21:05:00Z
mode: backend-infrastructure
notes: |
  Pure backend Rust phase (DIFF-04). Ground-truthed by running cargo test gates.
  dlp-agent --lib: 985 passed / 0 failed (1 stale test skipped).
  dlp-hook-dll --lib: 333 passed / 0 failed / 8 ignored.
  dlp-server health_api_integration: 9 passed / 0 failed.
  dlp-agent hook_ipc_integration: HANGS (WR-02 test-contract regression) — see Gaps.
---

## Current Test

[testing paused — 1 issue (test-harness regression from WR-02) + 2 blocked confirmations outstanding]

## Tests

### 1. Operator confirmation — diagnostics pipeline observable end-to-end
expected: DIFF-04 end-to-end observable + auth enforced; WR-01..WR-04 fixes hold; all coverage gates green
result: blocked
blocked_by: other
reason: "Gated on Gap G1 (WR-02 test-harness regression). Production behavior verified correct; agent test harnesses must be updated to the one-way contract before the dlp-agent integration suite can complete."

### 2. [58.9-01 D1] drain_all destructive + newest-first + at-most-once
expected: drain_all empties DashMap, returns snapshots sorted by timestamp_qpc desc; second call empty; no DashMap guard held across await
result: pass
source: automated
coverage_id: 58.9-01/D1
verification: cargo test -p dlp-agent --lib (985 passed; test_drain_all ok)

### 3. [58.9-01 D2] submit_diagnostic_snapshot auth + non-fatal non-2xx
expected: POSTs {pid,snapshots} with header `DLP-AGENT {id}:{hash}`; non-2xx (incl 429) -> non-fatal ServerError; logs only agent_id/pid/count
result: pass
source: automated
coverage_id: 58.9-01/D2
verification: cargo test -p dlp-agent --lib (test_submit_diagnostic_snapshot_{unreachable_server,payload_serde,sets_agent_auth_header} ok)

### 4. [58.9-02 D1] pipe_client::send_diagnostics bincode-stable + best-effort
expected: reuses IpcPayloadV1::DiagnosticsResponse (discriminant-stable) via send_raw_oneway; no pipe -> Err(ConnectionRefused) without panic
result: pass
source: automated
coverage_id: 58.9-02/D1
verification: cargo test -p dlp-hook-dll --lib (333 passed; send_diagnostics_no_pipe_is_best_effort ok)

### 5. [58.9-02 D2] diagnostic emit cadence counter
expected: DIAGNOSTIC_EMIT_INTERVAL/COUNTER incremented and zeroed by reset_perf_counters
result: pass
source: automated
coverage_id: 58.9-02/D2
verification: cargo test -p dlp-hook-dll --lib (emit_state_transition_* ok)

### 6. [58.9-02 D3] cadence drains ring best-effort; deny path never blocked
expected: on cadence drains ring + one-way pushes; no pipe -> returns normally, ring drained, deny path never blocked
result: pass
source: automated
coverage_id: 58.9-02/D3
verification: cargo test -p dlp-hook-dll --lib (trampolines::tests::diagnostics_cadence_drains_ring_best_effort ok)

### 7. [58.9-03 D1] DiagnosticsResponse ingests into aggregator (PID anti-spoof)
expected: one-way DiagnosticsResponse frame to HookIpcServer ingested under key {pid}_{agent_id}, incrementing total_snapshot_count()
result: blocked
blocked_by: other
reason: "Integration test diagnostics_response_ingests_into_aggregator uses the send_envelope() helper (reads an ACK) for a frame the server no longer ACKs after WR-02 -> harness hangs before assertion. Production ingest path is correct (DLL uses send_raw_oneway); the test artifact is stale. See Gap G1."
coverage_id: 58.9-03/D1
verification: dlp-agent/tests/hook_ipc_integration.rs#diagnostics_response_ingests_into_aggregator (BLOCKED — hangs)

### 8. [58.9-03 D2] diagnostic_push_loop drains+pushes+flush-and-stop
expected: drains via drain_all + POSTs via submit_diagnostic_snapshot(agent_id,0,snapshots); on shutdown exactly one final drain+push, aggregator empty
result: pass
source: automated
coverage_id: 58.9-03/D2
verification: cargo test -p dlp-agent --lib (service::diagnostic_push_loop_drains_pushes_and_shuts_down ok)

### 9. [58.9-03 D3] lifecycle spawn populates handles iff ServerClient present
expected: None client -> (None,None); Some(client) -> (Some,Some); clean shutdown on watch signal
result: pass
source: automated
coverage_id: 58.9-03/D3
verification: cargo test -p dlp-agent --lib (service::diagnostic_push_spawn_populates_handles_when_server_present ok)

### 10. [58.9-04] auth boundary: 401 unauthenticated + 400 valid-but-mismatched id (never 500)
expected: no/wrong Authorization -> 401; valid hash for DIFFERENT agent_id -> 400 (NOT 401, NOT 500)
result: pass
source: automated
coverage_id: 58.9-04/auth-boundary
verification: cargo test -p dlp-server --test health_api_integration (9 passed; post_agent_diagnostics_rejects_{unauthenticated,mismatched_agent_id} + unprovisioned_auth_hash_returns_401_not_leaking_500 ok)

### 11. [58.9-04] end-to-end round-trip preserves snapshot fields
expected: POST {pid,snapshots} valid -> 200 {received:true}; GET /admin/diagnostics -> hook_function/user_sid unchanged
result: pass
source: automated
coverage_id: 58.9-04/round-trip
verification: cargo test -p dlp-server --test health_api_integration (post_agent_diagnostics_surfaces_in_admin_diagnostics ok)

### 12. [review-fix] WR-01..WR-04 hold after fixes
expected: WR-01 re-buffer on failure; WR-02 no ACK for one-way frames; WR-03 chunk <64 MiB; WR-04 bounded shutdown
result: issue
reported: "WR-01/03/04 hold (agent lib 985 ok incl. service re-buffer/bounded-shutdown paths compile+pass; hook-dll 333 ok incl. trampolines chunking). WR-02 is PRODUCTION-CORRECT (real DLL sends all 5 frame types via send_raw_oneway, so skipping the ACK is right) BUT the fix did not update the agent test harnesses to the one-way contract: dlp-agent lib test test_bypass_alert_routes_through_server_to_receiver uses send_raw (reads ACK) and HANGS; dlp-agent hook_ipc_integration tests that send DiagnosticsResponse/HealthResponse via the send_envelope() helper (which reads a response) also HANG. The full dlp-agent --lib run aborts with STATUS_STACK_BUFFER_OVERRUN after the bypass test hangs >60s."
severity: major

## Summary

total: 12
passed: 9
issues: 1
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "WR-02 (skip ACK for one-way IPC frames) must hold WITHOUT breaking the agent test suites"
  status: failed
  reason: "WR-02 changed the server contract (BypassAlert/JournalDegraded/HashEvidence/HealthResponse/DiagnosticsResponse -> 'continue', no ACK) which matches the real DLL (all send via send_raw_oneway), but left agent test harnesses on the old request-response contract. Affected tests read an ACK that never comes and hang."
  severity: major
  test: 12
  root_cause: "Test-harness/contract drift after WR-02: (a) dlp-agent/src/hook_ipc.rs test test_bypass_alert_routes_through_server_to_receiver sends BypassAlert via send_raw() (which reads a response) and asserts an ACK the server no longer writes; (b) dlp-agent/tests/hook_ipc_integration.rs helper send_envelope() (line ~33) returns IpcEnvelope (reads a response) and is used to send genuinely one-way frames (DiagnosticsResponse, HealthResponse), so those tests block on the read. Production code is correct; only the test doubles assume the obsolete always-ACK contract."
  artifacts:
    - path: "dlp-agent/src/hook_ipc.rs"
      issue: "test_bypass_alert_routes_through_server_to_receiver uses send_raw + ACK deserialize/assert for a one-way frame (hangs)"
    - path: "dlp-agent/tests/hook_ipc_integration.rs"
      issue: "send_envelope() helper reads a response; used by diagnostics_response_ingests_into_aggregator, test_consolidated_server_routes_diagnostics_frame_with_snapshots, test_consolidated_server_routes_health_frame, test_consolidated_server_ingests_health_response, test_consolidated_server_health_response_without_aggregator, test_health_response_builds_aggregator_history, test_health_response_ingestion (all send one-way frames -> hang)"
  missing:
    - "Update harnesses to match the production one-way contract: send BypassAlert/HealthResponse/DiagnosticsResponse (and JournalDegraded/HashEvidence where tested) via a no-read send (send_raw_oneway equivalent) and drop ACK-read assertions, while KEEPING request-response for truly two-way frames (RequestOverride->Response, PullDiagnostics->DiagnosticsResponse reply, PullHealth->HealthResponse reply, PollControl, UnhookAck)."
    - "Re-run cargo test -p dlp-agent --lib (expect 986 incl. previously-skipped bypass test) and cargo test -p dlp-agent --test hook_ipc_integration (expect all green, no hang) to close the gap."
  debug_session: ""
