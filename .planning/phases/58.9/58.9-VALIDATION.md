---
phase: 58.9
slug: close-gap-diff-04-agent-diagnostics-producer
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-10
---

# Phase 58.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: derived from `## Validation Architecture` in `58.9-RESEARCH.md`.
> Requirement in scope: **DIFF-04**.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `cargo test` (workspace) |
| **Config file** | `Cargo.toml` (workspace root) — no extra test config |
| **Quick run command** | `cargo test -p dlp-agent -p dlp-server --lib` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~60–180 seconds (workspace; environment-dependent) |

---

## Sampling Rate

- **After every task commit:** Run the affected package tests, e.g. `cargo test -p dlp-agent` / `-p dlp-server` / `-p dlp-hook-dll`
- **After every plan wave:** Run `cargo clippy --workspace -- -D warnings`, `cargo fmt --check`, and `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full workspace suite must be green
- **Max feedback latency:** ~180 seconds (full workspace)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| _populated by planner/verifier per PLAN.md_ | — | — | DIFF-04 | — | aggregator→auth-endpoint→store round-trip; 401 on identity mismatch | integration | `cargo test -p dlp-server --test '*diagnostic*'` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `dlp-server/tests/` diagnostic round-trip test (mirror of `health_api_integration.rs:227-341`) — aggregator snapshot → `POST /agents/{id}/diagnostics` → `diagnostic_store` → retrieve; plus 401 identity-mismatch path
- [ ] `dlp-agent` unit test for `DiagnosticAggregator::drain_all()` (destructive drain semantics)
- [ ] `dlp-agent` integration test for the periodic diagnostic push loop lifecycle (start on run, flush-and-stop on shutdown)

*Existing DIFF-04 server scaffolding (58.8) covers the ingest/store half; Wave 0 here covers the agent producer + end-to-end round-trip.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end deny-path workload → `GET /admin/diagnostics` returns pushed snapshots | DIFF-04 (criterion 3) | Requires running the agent against a live deny-path workload with the hook DLL injected | Run agent + injected workload that triggers denies; then `dlp-admin-cli diagnostics` (or `GET /admin/diagnostics`) and confirm the pushed snapshots appear |
| sonar-scanner Quality Gate | criterion 5 | Environment-blocked per STATE.md (best-effort, non-gating) | Run `sonar-scanner` with `SONAR_TOKEN` exported where tooling permits |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (drain_all, push-loop lifecycle, server round-trip)
- [ ] No watch-mode flags
- [ ] Feedback latency < 180s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
