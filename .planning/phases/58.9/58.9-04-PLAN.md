---
phase: 58.9-close-gap-diff-04-agent-diagnostics-producer
plan: 04
type: execute
wave: 3
depends_on:
  - 58.9-03
files_modified:
  - dlp-server/tests/health_api_integration.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - "DIFF-04 criterion 3 (end-to-end): after an aggregator snapshot is pushed through the authenticated `POST /agents/{id}/diagnostics`, `GET /admin/diagnostics` returns it with the snapshot fields round-tripping unchanged (e.g. `hook_function`, `user_sid`) — proving the full DLL->agent->server->admin path is observable, not just the server half."
    - "DIFF-04 criterion 4 (auth boundary): `POST /agents/{id}/diagnostics` returns 401 when the `Authorization` header is absent/wrong (agent_auth_middleware) and 400 when a VALID hash authenticates a DIFFERENT agent_id than the path (handler `agent_id != path_agent_id` at admin_api.rs:5668) — the two rejections are distinct and neither is 500."
    - "DIFF-04 criterion 5 (quality gate): `cargo test --workspace`, `cargo clippy --workspace -- -D warnings`, and `cargo fmt --check` are all green; `sonar-scanner` Quality Gate is attempted and logged (environment-blocked per STATE.md 'Blockers' — non-gating, best-effort)."
    - "All phase must_haves from Plans 01-03 are verified present by source/behavior assertion and recorded in the SUMMARY verification matrix (drain_all destructive; submit_diagnostic_snapshot contract; DLL one-way emit reusing DiagnosticsResponse; ingest arm PID-keyed; existing RunLoopContext fields populated; diag_for_ingest clone-before-move)."
  artifacts:
    - path: dlp-server/tests/health_api_integration.rs
      provides: Diagnostics auth-boundary tests (401/400) + strengthened 200 round-trip assertions
      contains:
        - post_agent_diagnostics_rejects_unauthenticated
        - post_agent_diagnostics_rejects_mismatched_agent_id
    - path: .planning/phases/58.9/58.9-04-SUMMARY.md
      provides: must_haves verification matrix + quality-gate results + sonar attempt log
  key_links:
    - from: dlp-server/tests 401 test
      to: dlp-server/src/admin_api.rs agent_auth_middleware (129-190)
      via: "POST /agents/{TEST_AGENT_ID}/diagnostics with NO Authorization header -> 401 UNAUTHORIZED"
    - from: dlp-server/tests 400 test
      to: dlp-server/src/admin_api.rs handler agent_id==path check (5668)
      via: "POST with header `DLP-AGENT different-agent:{TEST_AGENT_AUTH_HASH}` to /agents/{TEST_AGENT_ID}/diagnostics -> 400 BAD_REQUEST (NOT 401)"
    - from: dlp-server/tests 200 test (extended)
      to: dlp-server/src/diagnostic_store.rs ingest -> GET /admin/diagnostics
      via: "POST {pid:1234, snapshots:[diag]} with valid header -> 200 {received:true}; GET /admin/diagnostics (admin JWT) -> snapshot present with hook_function/user_sid unchanged"
---

<objective>
Prove DIFF-04 end-to-end and lock the quality gate. (1) Add the diagnostics auth-boundary tests (401 unauthenticated, 400 mismatched-but-valid identity) beside the existing 200 round-trip test, and strengthen the 200 test with field-level assertions so criterion 3 is observed unchanged through `diagnostic_store`. (2) Run the full workspace quality gate (tests, clippy, fmt) and attempt `sonar-scanner` (best-effort), then verify every phase must_have from Plans 01-03 by source/behavior assertion and record the matrix in the SUMMARY. Depends on Plan 03 (the producer path must exist so the end-to-end story is complete).

Purpose: ROADMAP criteria 3, 4, and 5 are proven here. Criteria 1 and 2 are proven in Plans 01/03; this plan closes the phase by demonstrating the observable, authenticated, retrievable round-trip and a green workspace.
Output: 2 new server integration tests + strengthened 200 assertions; green workspace gate; SUMMARY verification matrix + sonar attempt log.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.9/58.9-RESEARCH.md
@.planning/phases/58.9/58.9-PATTERNS.md
@.planning/phases/58.9/58.9-VALIDATION.md
@.planning/phases/58.9/58.9-01-PLAN.md
@.planning/phases/58.9/58.9-02-PLAN.md
@.planning/phases/58.9/58.9-03-PLAN.md
@dlp-server/tests/health_api_integration.rs
@dlp-server/tests/diagnostics_api_integration.rs
@dlp-server/src/admin_api.rs
@dlp-server/src/diagnostic_store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostics 401 + 400 auth-boundary tests and strengthen the 200 round-trip assertions</name>
  <files>dlp-server/tests/health_api_integration.rs</files>
  <read_first>
    - dlp-server/tests/health_api_integration.rs (build_test_app at ~105-114 seeds diagnostic_store: Some(...); agent_auth_header helper at 140-142; TEST_AGENT_ID / TEST_AGENT_AUTH_HASH consts; the 200 diagnostics test post_agent_diagnostics_surfaces_in_admin_diagnostics at 227-291; the 401 template post_agent_health_rejects_unauthenticated at 298-314; the 400 template post_agent_health_rejects_mismatched_agent_id at 321-341)
    - dlp-server/src/admin_api.rs (agent_auth_middleware at 129-190 -> 401; AgentDiagnosticsIngest at 5611; the diagnostics handler agent_id!=path check at 5668 -> 400; diagnostic_store ingest at 5675; list_diagnostics_handler at 5563-5590)
    - dlp-server/tests/diagnostics_api_integration.rs (only if it holds the shared helpers instead — confirm which file owns agent_auth_header/build_test_app/TEST_AGENT_* by grep before editing; do NOT duplicate helpers)
    - .planning/phases/58.9/58.9-PATTERNS.md section "dlp-server/tests/health_api_integration.rs — add diagnostics 401/400" (status-boundary caution) and "200 path to extend"
    - .planning/phases/58.9/58.9-RESEARCH.md "Pitfall 3 (401 vs 400 confusion)" and Validation Architecture criterion-3/4 rows
  </read_first>
  <behavior>
    - `post_agent_diagnostics_rejects_unauthenticated`: POST `/agents/{TEST_AGENT_ID}/diagnostics` with body `{ "pid": 1234, "snapshots": [] }` and NO `Authorization` header -> assert `StatusCode::UNAUTHORIZED` (middleware).
    - `post_agent_diagnostics_rejects_mismatched_agent_id`: POST to `/agents/{TEST_AGENT_ID}/diagnostics` with header `agent_auth_header("different-agent", TEST_AGENT_AUTH_HASH)` (valid hash, wrong identity) -> assert `StatusCode::BAD_REQUEST` (handler), NOT 401.
    - Strengthen the existing 200 test (`post_agent_diagnostics_surfaces_in_admin_diagnostics`): in addition to `received==true` and presence in `GET /admin/diagnostics`, assert the retrieved snapshot's `hook_function == "WriteFile"` and `user_sid == "S-1-5-21-1"` (or whatever the posted fixture uses) round-trip unchanged — i.e. field-level observability, not mere presence.
  </behavior>
  <action>
    First, `rg -n "post_agent_diagnostics_surfaces_in_admin_diagnostics|fn agent_auth_header|TEST_AGENT_ID|fn build_test_app" dlp-server/tests` to locate the single file that owns the shared helpers and the 200 diagnostics test (expected: `dlp-server/tests/health_api_integration.rs`). Edit THAT file only; reuse its existing `agent_auth_header`, `build_test_app`, `TEST_AGENT_ID`, `TEST_AGENT_AUTH_HASH` — do not redeclare them.
    Add `#[tokio::test] async fn post_agent_diagnostics_rejects_unauthenticated()` mirroring `post_agent_health_rejects_unauthenticated` (298-314) but with URI `format!("/agents/{}/diagnostics", TEST_AGENT_ID)` and body `serde_json::json!({ "pid": 1234, "snapshots": [] })`; assert `resp.status() == StatusCode::UNAUTHORIZED`.
    Add `#[tokio::test] async fn post_agent_diagnostics_rejects_mismatched_agent_id()` mirroring `post_agent_health_rejects_mismatched_agent_id` (321-341): same URI, header `agent_auth_header("different-agent", TEST_AGENT_AUTH_HASH)`, assert `StatusCode::BAD_REQUEST`. Do NOT assert 401 here (Pitfall 3).
    Extend `post_agent_diagnostics_surfaces_in_admin_diagnostics` (227-291): after `GET /admin/diagnostics`, deserialize the JSON and assert the first returned snapshot for the agent has `hook_function` and `user_sid` equal to the values POSTed (read the test's fixture to get the exact expected strings; if they differ from "WriteFile"/"S-1-5-21-1", assert the fixture's actual values). Use `serde_json::Value` indexing + `assert_eq!`; no `.unwrap()` outside tests is fine, but tests may use `.expect("...")`.
    GitNexus (CLAUDE.md): test-only change; run `detect_changes()` before committing to confirm scope is limited to the test file.
  </action>
  <verify>
    <automated>cargo test -p dlp-server --test health_api_integration diagnostics -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - Source: the file that owns `post_agent_diagnostics_surfaces_in_admin_diagnostics` now also contains `post_agent_diagnostics_rejects_unauthenticated` (asserts 401) and `post_agent_diagnostics_rejects_mismatched_agent_id` (asserts 400), reusing the existing helpers (no duplicate `agent_auth_header`/`TEST_AGENT_*`).
    - Source: the 200 test asserts field-level round-trip (`hook_function`, `user_sid`) on the retrieved snapshot, not just presence.
    - Behavior: `cargo test -p dlp-server --test health_api_integration` exits 0 incl. the two new tests and the strengthened 200 test; the 401 test uses no header and the 400 test uses a valid-hash/wrong-id header (distinct status codes).
    - Behavior: `cargo clippy -p dlp-server --tests -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>The diagnostics endpoint's 401/400 auth boundary and field-level 200 round-trip are proven by server integration tests, satisfying criteria 3 and 4.</done>
</task>

<task type="auto">
  <name>Task 2: Workspace quality gate + phase must_haves verification matrix + sonar attempt</name>
  <files>
    .planning/phases/58.9/58.9-04-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/58.9/58.9-01-PLAN.md (must_haves: drain_all destructive; submit_diagnostic_snapshot contract)
    - .planning/phases/58.9/58.9-02-PLAN.md (must_haves: send_diagnostics reuses DiagnosticsResponse; cadence best-effort; counter reset)
    - .planning/phases/58.9/58.9-03-PLAN.md (must_haves: ingest arm PID-keyed; existing RunLoopContext fields populated; diag_for_ingest clone-before-move; push loop flush-and-stop)
    - .planning/ROADMAP.md lines 634-650 (the 5 success criteria)
    - .planning/STATE.md "Blockers" (sonar-scanner environment-blocked)
  </read_first>
  <behavior>
    - The full workspace quality gate is green: `cargo test --workspace`, `cargo clippy --workspace -- -D warnings`, `cargo fmt --check`.
    - `sonar-scanner` is attempted with the exported `SONAR_TOKEN`; the result (pass / environment-blocked reason) is logged in the SUMMARY. It is NON-gating per STATE.md (JAVA_HOME / SonarCloud `Not authorized`).
    - A must_haves verification matrix is written into the SUMMARY mapping each ROADMAP criterion and each Plan 01-03 invariant to a concrete source/behavior assertion that was run and its result.
  </behavior>
  <action>
    Run and capture (fix any failures before completing; this task does not introduce source changes, it verifies Plans 01-03):
    - `cargo fmt --check` (workspace). If it fails, run `cargo fmt` and commit the formatting as part of this task.
    - `cargo clippy --workspace -- -D warnings`. Fix all warnings across `dlp-agent`, `dlp-hook-dll`, `dlp-server`, `dlp-common` (the four crates this phase touches).
    - `cargo test --workspace`. Must be green; pay particular attention to `dlp-agent` (drain_all, submit_diagnostic_snapshot, hook_ipc_integration::diagnostics_response_ingests_into_aggregator, diagnostic_push lifecycle, RunLoopContext), `dlp-hook-dll` (send_diagnostics, perf_telemetry counter, trampolines emit), and `dlp-server --test health_api_integration` (200 + 401 + 400).
    - Attempt `sonar-scanner` (with `SONAR_TOKEN` exported). Record exact outcome in the SUMMARY (Quality Gate status, or the precise environment blocker such as missing JAVA_HOME / `Not authorized`). Do NOT block the phase on it.
    Verify must_haves by source assertion (grep) and record each as PASS/FAIL in the SUMMARY matrix:
      - Criterion 1: `rg "fn submit_diagnostic_snapshot" dlp-agent/src/server_client.rs` present; header literal `DLP-AGENT {}:{}` present; body has `pid` + `snapshots`.
      - Criterion 2: `rg "fn diagnostic_push_loop|DIAGNOSTIC_PUSH_INTERVAL" dlp-agent/src/service.rs` present; `diagnostic_push_shutdown`/`diagnostic_push_handle` return-site literal (~2871-2873) no longer `None`; stop section present in `run_loop_shutdown`.
      - Criterion 3: end-to-end proven by `post_agent_diagnostics_surfaces_in_admin_diagnostics` (field-level) + `diagnostics_response_ingests_into_aggregator` (consumer fed) + `send_diagnostics`/`record_pipe_round_trip_and_maybe_emit` (producer emit).
      - Criterion 4: `post_agent_diagnostics_rejects_unauthenticated` (401) + `post_agent_diagnostics_rejects_mismatched_agent_id` (400) present and green.
      - Invariants: `rg "fn drain_all" dlp-agent/src/diagnostic_aggregator.rs` present and uses `self.snapshots.remove(&key)`; `rg "let diag_for_ingest = Arc::clone\(&diag\);" dlp-agent/src/service.rs` present before line 1961; `rg "IpcPayloadV1::DiagnosticsResponse(ref resp)" dlp-agent/src/hook_ipc.rs` ingest arm present and uses `named_pipe_client_pid(pipe)`; `IpcPayloadV1` variant set in `dlp-common/src/hook_ipc.rs` unchanged (A2).
    Write the matrix and gate results to `.planning/phases/58.9/58.9-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `cargo fmt --check` exits 0.
    - CLI: `cargo clippy --workspace -- -D warnings` exits 0.
    - CLI: `cargo test --workspace` exits 0 (all four touched crates green).
    - Behavior: `sonar-scanner` attempted; SUMMARY records pass OR the exact environment blocker; phase NOT blocked on sonar.
    - Source/Behavior: SUMMARY contains a must_haves verification matrix with every ROADMAP criterion (1-5) and every Plan 01-03 invariant marked PASS with the assertion/command that proved it.
  </acceptance_criteria>
  <done>Workspace is green (fmt, clippy, tests), sonar is attempted and logged, and every phase must_have is verified and recorded — DIFF-04 producer side is complete end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> server POST /agents/{id}/diagnostics | Authenticated ingest endpoint; the two rejection layers (middleware 401 vs handler 400) must remain distinct. |
| server diagnostic_store -> admin GET /admin/diagnostics | Admin-JWT-gated read of stored snapshots (paths/SIDs) — privileged operational data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.9-17 | Spoofing | unauthenticated agent POST | mitigate (verified) | 401 test proves `agent_auth_middleware` rejects missing/wrong hash before the handler runs. |
| T-58.9-18 | Elevation of Privilege | agent posts snapshots for another agent_id | mitigate (verified) | 400 test proves the handler `agent_id != path_agent_id` check rejects cross-agent posts (valid hash, wrong identity) — distinct from 401. |
| T-58.9-19 | Information Disclosure | diagnostics visible to non-admin | accept | `GET /admin/diagnostics` is admin-JWT-gated (shipped 58.8-02); not changed here. Snapshots are operational DLP data, not end-user PII export. |
| T-58.9-20 | Tampering | regression in the authenticated contract | mitigate | Field-level 200 assertion + distinct 401/400 tests pin the exact contract so future changes cannot silently weaken it. |
| T-58.9-SC | Tampering | cargo installs | mitigate | No new external packages; all dependencies already locked (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p dlp-server --test health_api_integration` passes (200 field-level + 401 + 400).
- `cargo fmt --check` passes.
- `cargo clippy --workspace -- -D warnings` passes.
- `cargo test --workspace` passes.
- `sonar-scanner` attempted and logged (non-gating).
- SUMMARY must_haves verification matrix complete with PASS for criteria 1-5 and all invariants.
</verification>

<success_criteria>
- DIFF-04 criteria 3 and 4 proven by server integration tests; criteria 1 and 2 (Plans 01/03) re-confirmed via the must_haves matrix.
- Workspace fmt/clippy/tests green; sonar attempted and logged.
- Phase 58.9 ready for `/gsd-verify-work`.
</success_criteria>

<output>
Create `.planning/phases/58.9/58.9-04-SUMMARY.md` when done (contains the must_haves verification matrix + quality-gate + sonar log).
</output>

## Artifacts this phase produces

- **Server integration tests (dlp-server/tests/health_api_integration.rs):**
  - `post_agent_diagnostics_rejects_unauthenticated` (401 — no `Authorization` header).
  - `post_agent_diagnostics_rejects_mismatched_agent_id` (400 — valid hash, wrong identity).
  - Strengthened `post_agent_diagnostics_surfaces_in_admin_diagnostics` with field-level round-trip assertions (`hook_function`, `user_sid`).
- **SUMMARY (dlp-rust/.planning/phases/58.9/58.9-04-SUMMARY.md):**
  - must_haves verification matrix (criteria 1-5 + invariants) with PASS assertions.
  - Quality-gate results (fmt/clippy/tests) + sonar attempt log.

## Assumption resolutions (recorded for convergence)

- **A1/A2/A3/A4:** All resolved in Plans 01-03 and re-verified here by the must_haves matrix. No new assumptions introduced. If the end-to-end test reveals the dashboard groups strictly by POST `pid` (A3 risk), the remediation (group drained snapshots by originating key and POST one batch per pid) is a Plan-01/03 follow-up, not a Plan-04 change — flag at convergence if observed.
