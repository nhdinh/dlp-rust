---
phase: 58.9-close-gap-diff-04-agent-diagnostics-producer
plan: 03
type: execute
wave: 2
depends_on:
  - 58.9-01
  - 58.9-02
files_modified:
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/service.rs
  - dlp-agent/tests/hook_ipc_integration.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - "DIFF-04 criterion 2: a periodic `diagnostic_push_loop` drains `DiagnosticAggregator` via `drain_all`, batches, and POSTs via `submit_diagnostic_snapshot(&agent_id, 0, &snapshots)`; it is wired into the service lifecycle (start on run when `server_client` present; flush-and-stop on shutdown) by POPULATING the EXISTING `RunLoopContext::diagnostic_push_shutdown` / `diagnostic_push_handle` fields (service.rs:1437/1440, init None at 2871/2873) — NO new `RunLoopContext` fields are added (PATTERNS callout 1)."
    - "DIFF-04 A1 consumer half: `HookIpcServer` gains a `DiagnosticsResponse` one-way ingest arm that calls `diagnostic_aggregator.ingest(agent_id, pid, resp.snapshots.clone())`, with `pid = named_pipe_client_pid(pipe).unwrap_or(0)` (anti-spoof — the connecting client's REAL PID, hook_ipc.rs:370) and `agent_id` threaded alongside the aggregator (PATTERNS callout 2)."
    - "In `spawn_hook_ipc_server`, `diag_for_ingest = Arc::clone(&diag)` is taken BEFORE `diag` is moved into `diag_handler` (service.rs:1961) and `.with_diagnostic_aggregator(agent_id, diag_for_ingest)` is called after `.with_health_aggregator(health)` (:2099) (PATTERNS callout 3)."
    - "The aggregator key agent_id used for ingest is the SAME agent_id used for the server push, sourced from `HookIpcServerConfig`, so local keys and the remote identity are consistent (A3)."
    - "Load-bearing invariant: `diagnostic_push_loop` calls `drain_all` (owned Vec; guards dropped) BEFORE awaiting `submit_diagnostic_snapshot` — no `DashMap` guard is ever held across `.await` (RESEARCH Pitfall 4)."
  artifacts:
    - path: dlp-agent/src/hook_ipc.rs
      provides: One-way DiagnosticsResponse ingest into DiagnosticAggregator with PID-keyed anti-spoof attribution
      contains:
        - with_diagnostic_aggregator
        - IpcPayloadV1::DiagnosticsResponse ingest arm
    - path: dlp-agent/src/service.rs
      provides: Periodic diagnostics push loop + lifecycle wiring + aggregator threading into HookIpcServer
      contains:
        - DIAGNOSTIC_PUSH_INTERVAL
        - fn diagnostic_push_loop
    - path: dlp-agent/tests/hook_ipc_integration.rs
      provides: Integration test proving a one-way DiagnosticsResponse frame increments DiagnosticAggregator
      contains:
        - diagnostics_response_ingests_into_aggregator
  key_links:
    - from: dlp-agent/src/hook_ipc.rs IpcPayloadV1::DiagnosticsResponse arm
      to: dlp-agent/src/diagnostic_aggregator.rs::ingest
      via: "ingest(agent_id.as_ref(), named_pipe_client_pid(pipe).unwrap_or(0), resp.snapshots.clone()) -> key '{pid}_{agent_id}'"
    - from: dlp-agent/src/service.rs::diagnostic_push_loop
      to: dlp-agent/src/diagnostic_aggregator.rs::drain_all + dlp-agent/src/server_client.rs::submit_diagnostic_snapshot
      via: "every DIAGNOSTIC_PUSH_INTERVAL: snapshots = drain_all(); if non-empty submit_diagnostic_snapshot(&agent_id, 0, &snapshots).await; warn-and-continue on Err"
    - from: dlp-agent/src/service.rs::run_loop_init spawn
      to: dlp-agent/src/service.rs::RunLoopContext diagnostic_push_shutdown/diagnostic_push_handle
      via: "populate the EXISTING fields (return-site literal :2871-2873 changed from None to the new bindings); stop section in run_loop_shutdown signals watch + joins handle with 5s timeout"
    - from: dlp-agent/src/service.rs::spawn_hook_ipc_server
      to: dlp-agent/src/hook_ipc.rs::HookIpcServer::with_diagnostic_aggregator
      via: "diag_for_ingest cloned before :1961 move; builder call after :2099; agent_id sourced from HookIpcServerConfig"
---

<objective>
Close the consumer/orchestration half of DIFF-04: (1) teach `HookIpcServer` to ingest one-way `DiagnosticsResponse` frames into `DiagnosticAggregator` with PID-keyed anti-spoof attribution, and (2) add the periodic `diagnostic_push_loop` that drains the aggregator and POSTs to the server, wired into the service lifecycle by POPULATING the two `RunLoopContext` fields that 58.8 already reserved (`diagnostic_push_shutdown`/`diagnostic_push_handle`). Depends on Plan 01 (provides `drain_all` + `submit_diagnostic_snapshot`) and Plan 02 (the DLL emit that produces the frames this arm ingests). Together with Plans 01/02 this completes the end-to-end producer path proven in Plan 04.

Purpose: ROADMAP criterion 2 (periodic drain+push with graceful shutdown) and the consumer half of criterion 3 (the aggregator is actually FED). The two reserved `RunLoopContext` fields mean this plan introduces almost no struct-literal drift (PATTERNS callout 1).
Output: `HookIpcServer::with_diagnostic_aggregator` + threaded fields + ingest arm; `diagnostic_push_loop` + const + spawn/stop/wire; one IPC integration test + one lifecycle test.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.9/58.9-RESEARCH.md
@.planning/phases/58.9/58.9-PATTERNS.md
@.planning/phases/58.9/58.9-01-PLAN.md
@.planning/phases/58.9/58.9-02-PLAN.md
@dlp-agent/src/hook_ipc.rs
@dlp-agent/src/service.rs
@dlp-agent/src/diagnostic_aggregator.rs
@dlp-agent/src/server_client.rs
@dlp-agent/tests/hook_ipc_integration.rs
@dlp-common/src/hook_ipc.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HookIpcServer DiagnosticsResponse ingest arm + aggregator/agent_id threading + IPC integration test</name>
  <files>
    dlp-agent/src/hook_ipc.rs
    dlp-agent/tests/hook_ipc_integration.rs
  </files>
  <read_first>
    - dlp-agent/src/hook_ipc.rs (struct 103-122; the four `health_aggregator: None` constructor literals at 138/174/201/229; with_health_aggregator at 262-268; run_with_ready destructure+accept_loop call at 315-330; named_pipe_client_pid at 370 [cfg(not(test))] and 394 [cfg(test)]; accept_loop signature ~440-510; handle_connection signature 518-530 and the frame-dispatch match 540-720, esp. the PullDiagnostics->DiagnosticsResponse REPLY arm at 563-567 and the HealthResponse one-way arm at 648-672)
    - dlp-agent/tests/hook_ipc_integration.rs (test_pull_diagnostics_after_deny at 605-661 for the IPC harness; how a HookIpcServer is built in tests; how a client sends a one-way frame)
    - .planning/phases/58.9/58.9-PATTERNS.md section "hook_ipc.rs — field + builder + frame arm" (Edits 1-4) and "agent/tests/hook_ipc_integration.rs — add DiagnosticsResponse ingest test"
    - .planning/phases/58.9/58.9-RESEARCH.md "Pitfall 1 / Pitfall 5" and "Don't Hand-Roll: Hooked-process pid"
  </read_first>
  <behavior>
    - `HookIpcServer` carries two new fields — `diagnostic_aggregator: Option<Arc<DiagnosticAggregator>>` and `agent_id: Option<Arc<str>>` — set by a single builder `with_diagnostic_aggregator(agent_id: impl Into<Arc<str>>, aggregator: Arc<DiagnosticAggregator>)`. Both initialize to `None` in all four constructor literals.
    - Both fields are threaded through `run_with_ready -> accept_loop -> handle_connection` EXACTLY mirroring `health_aggregator`: `run_with_ready` moves `self.diagnostic_aggregator` and `self.agent_id` into `accept_loop` (owned `Option<Arc<_>>`); `accept_loop` forwards `diagnostic_aggregator.as_ref()` and `agent_id.as_ref()` to `handle_connection` (`Option<&Arc<_>>`).
    - A new match arm `IpcPayloadV1::DiagnosticsResponse(ref resp) => { ... }` (ADDITIVE — distinct from the existing `PullDiagnostics(req)` arm at 563-567 which produces a `DiagnosticsResponse` as its REPLY) resolves `pid = named_pipe_client_pid(pipe).unwrap_or(0)`, logs `debug!(pid, count = resp.snapshots.len(), "Hook IPC: diagnostics received from DLL")`, and when BOTH `diagnostic_aggregator` and `agent_id` are `Some`, calls `aggregator.ingest(agent_id.as_ref(), pid, resp.snapshots.clone())`; otherwise `warn!("Hook IPC: diagnostics received but no aggregator configured")`. It returns an empty ALLOW ACK `HookResponse` (mirror 665-671) so the DLL is never blocked.
    - The new integration test `diagnostics_response_ingests_into_aggregator` builds a `HookIpcServer` with `.with_diagnostic_aggregator("agent-test", Arc::clone(&aggregator))`, drives ONE client that sends a one-way `IpcPayloadV1::DiagnosticsResponse { snapshots: vec![one] }`, and asserts `aggregator.total_snapshot_count()` increments by 1 (holds out RESEARCH Pitfall 1: the aggregator is actually fed). It must NOT use the `PullDiagnostics` request path.
  </behavior>
  <action>
    In `dlp-agent/src/hook_ipc.rs` (FOUR coordinated edits in ONE commit — RESEARCH Pitfall 5):
    (1) Struct (103-122): add `/// Phase 58.9: Diagnostic aggregator for one-way DiagnosticsResponse frame ingestion (DIFF-04).` then `diagnostic_aggregator: Option<Arc<crate::diagnostic_aggregator::DiagnosticAggregator>>,` after the `health_aggregator` field (112), and `/// Phase 58.9: Agent identity used to key the diagnostic aggregator ('{pid}_{agent_id}').` then `agent_id: Option<Arc<str>>,` after it. Add `diagnostic_aggregator: None,` and `agent_id: None,` to all four constructor literals (138/174/201/229), each immediately after the existing `health_aggregator: None,`.
    (2) Builder (after with_health_aggregator, ~268): add `pub fn with_diagnostic_aggregator(mut self, agent_id: impl Into<Arc<str>>, aggregator: Arc<crate::diagnostic_aggregator::DiagnosticAggregator>) -> Self { self.agent_id = Some(agent_id.into()); self.diagnostic_aggregator = Some(aggregator); self }` with a doc comment referencing DIFF-04.
    (3) Threading: in `run_with_ready`'s `accept_loop(...)` call (315-330), add `self.diagnostic_aggregator,` and `self.agent_id,` arguments immediately after `self.health_aggregator,` (324). In `accept_loop`'s signature (~440) add `diagnostic_aggregator: Option<Arc<crate::diagnostic_aggregator::DiagnosticAggregator>>,` and `agent_id: Option<Arc<str>>,` after `health_aggregator`, and where it forwards into `handle_connection` (~503) add `diagnostic_aggregator.as_ref(),` and `agent_id.as_ref(),` after `health_aggregator.as_ref(),`. In `handle_connection` (518-530) add `diagnostic_aggregator: Option<&Arc<crate::diagnostic_aggregator::DiagnosticAggregator>>,` and `agent_id: Option<&Arc<str>>,` after the `health_aggregator` param (526), so they are in scope inside the frame-dispatch match.
    (4) Ingest arm: in the `match msg.payload` (540-720), add a NEW arm `IpcPayloadV1::DiagnosticsResponse(ref resp) => { ... }` adjacent to the `HealthResponse` arm (648-672) — NOT inside the `PullDiagnostics` arm (563-567). Resolve `let pid = named_pipe_client_pid(pipe).unwrap_or(0);`, `debug!(pid, count = resp.snapshots.len(), "Hook IPC: diagnostics received from DLL")`, then `if let (Some(aggregator), Some(aid)) = (diagnostic_aggregator, agent_id) { aggregator.ingest(aid.as_ref(), pid, resp.snapshots.clone()); } else { warn!("Hook IPC: diagnostics received but no aggregator configured"); }` and return `IpcPayloadV1::Response(HookResponse { decision: dlp_common::Decision::ALLOW, reason: "diagnostics ingested".to_string(), cache_hint: None, cache_version: 0, approval_override: None })`. Do NOT log snapshot contents (paths/SIDs — CLAUDE.md 9.13); log `pid` + `count` only. The arm must not read from the pipe after writing the ACK (the DLL already closed it — fire-and-forget, like RequestOverride at 558-561 is fine to `continue`, but the empty-ACK pattern at 665-671 is the closer mirror; use the empty-ACK return, NOT `continue`).
    In `dlp-agent/tests/hook_ipc_integration.rs`, add `#[tokio::test] async fn diagnostics_response_ingests_into_aggregator()` mirroring the `test_pull_diagnostics_after_deny` harness (605-661): construct `let aggregator = Arc::new(crate::diagnostic_aggregator::DiagnosticAggregator::new());`, build the server with `.with_diagnostic_aggregator("agent-test", Arc::clone(&aggregator))` plus the other builders the harness requires, drive ONE client that bincode-sends `IpcEnvelope::V1(IpcMessageV1 { payload: IpcPayloadV1::DiagnosticsResponse(DiagnosticsResponse { snapshots: vec![one_snapshot()] }) })` via the file's existing send-oneway helper, then `assert_eq!(aggregator.total_snapshot_count(), 1);`. Use the `#[cfg(test)] named_pipe_client_pid` override (382-394) so PID attribution is deterministic.
    GitNexus (CLAUDE.md): run `impact` on `handle_connection`/`handle_frame` and `DiagnosticAggregator::ingest` before editing (hot dispatch path) and surface blast radius in the SUMMARY; run `detect_changes()` before committing.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test hook_ipc_integration diagnostics_response_ingests_into_aggregator -- --nocapture && cargo clippy -p dlp-agent -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `HookIpcServer` has `diagnostic_aggregator: Option<Arc<DiagnosticAggregator>>` and `agent_id: Option<Arc<str>>` fields; `with_diagnostic_aggregator(agent_id, aggregator)` sets both; all four constructor literals init both to `None`.
    - Source: `run_with_ready`/`accept_loop`/`handle_connection` thread both new values mirroring `health_aggregator` (owned in accept_loop, `Option<&Arc<_>>` in handle_connection); compile passes.
    - Source: a NEW `IpcPayloadV1::DiagnosticsResponse(ref resp) =>` arm exists (separate from the 563-567 PullDiagnostics reply arm) that calls `named_pipe_client_pid(pipe).unwrap_or(0)` and `aggregator.ingest(aid.as_ref(), pid, resp.snapshots.clone())`.
    - Behavior: `cargo test -p dlp-agent --test hook_ipc_integration diagnostics_response_ingests_into_aggregator` exits 0 and `total_snapshot_count()` increments by 1 after one one-way frame.
    - Behavior: `cargo clippy -p dlp-agent -- -D warnings` exits 0 (no too_many_arguments — if `handle_connection` crosses the lint threshold, refactor to a small `DispatchContext` struct the way 58.8-03 handled `handle_override_request`; note in SUMMARY).
  </acceptance_criteria>
  <done>`HookIpcServer` ingests one-way `DiagnosticsResponse` frames into `DiagnosticAggregator` keyed by the real client PID + agent_id, proven by an integration test that asserts the aggregator is actually fed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: diagnostic_push_loop + populate existing RunLoopContext fields + wire aggregator into spawn_hook_ipc_server + lifecycle test</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    - dlp-agent/src/service.rs (HEALTH_PUSH_INTERVAL at 1705; health_push_loop at 1721-1748; health spawn block at 2303-2322; spawn_hook_ipc_server at 1952 with `let diag = Arc::clone` at 1953, diag_handler move at 1961-1967, builder chain 2095-2101; RunLoopContext diagnostic fields at 1437/1440 + health 1443/1446; return-site literal at 2865-2877; health stop block at 5126-5138; HookIpcServerConfig struct def + its construction ~2482; health lifecycle test at 8542; RunLoopContext test literal at 6612-6620)
    - dlp-agent/src/server_client.rs (submit_diagnostic_snapshot from Plan 01; agent_id() at 417)
    - dlp-agent/src/diagnostic_aggregator.rs (drain_all from Plan 01; ingest at 85)
    - .planning/phases/58.9/58.9-PATTERNS.md section "service.rs — loop + spawn + stop + wire" (Edits 1-4) and the "CRITICAL PRE-STAGING NOTE" that RunLoopContext fields already exist
    - .planning/phases/58.9/58.9-RESEARCH.md "Pattern 2: Periodic drain-and-push" and "Pitfall 4 / Pitfall 5" and Assumption A3
  </read_first>
  <behavior>
    - `const DIAGNOSTIC_PUSH_INTERVAL: Duration = Duration::from_secs(60);` (sibling of HEALTH_PUSH_INTERVAL). `async fn diagnostic_push_loop(server_client: ServerClient, diagnostic_aggregator: Arc<DiagnosticAggregator>, agent_id: String, interval: Duration, mut shutdown: watch::Receiver<bool>)` loops on `select! { interval.tick() => drain+push, shutdown.changed() => flush+return }`. Each tick: `let snapshots = diagnostic_aggregator.drain_all();` (owned Vec; guards dropped before await); if empty, `debug!("no diagnostic snapshots to push")` and `continue`; else `if let Err(e) = server_client.submit_diagnostic_snapshot(&agent_id, 0, &snapshots).await { warn!(error=%e, count=snapshots.len(), "failed to submit diagnostics"); }`. On shutdown: drain once more and best-effort submit (flush-and-stop), then return. Batch pid is `0` (aggregate — A3; per-snapshot attribution lives inside each snapshot's fields).
    - In `run_loop_init`, after the health spawn block (2322), add an equivalent `let (diagnostic_push_shutdown, diagnostic_push_handle) = if let Some(ref sc) = server_client { ... tokio::spawn(diagnostic_push_loop(...)) ... (Some(tx), Some(handle)) } else { (None, None) };` binding to the EXISTING field names; change the return-site literal at 2871-2873 from `diagnostic_push_shutdown: None, diagnostic_push_handle: None,` to `diagnostic_push_shutdown, diagnostic_push_handle,` (mirror health at 2875-2877). NO new RunLoopContext fields.
    - In `run_loop_shutdown`, after the health stop block (5138), add an identical stop section for `ctx.diagnostic_push_shutdown` / `ctx.diagnostic_push_handle` (signal `tx.send(true)`, join handle with `tokio::time::timeout(Duration::from_secs(5), handle)`, log clean/panic/timeout).
    - In `spawn_hook_ipc_server` (1952-2101): add `let diag_for_ingest = Arc::clone(&diag);` immediately after `let diag = Arc::clone(&config.diagnostic_aggregator);` (1953) and BEFORE the `diag_handler` closure (1961) that moves `diag`; source the IPC `agent_id` from `HookIpcServerConfig` (read the struct: if it carries `server_client`/`agent_id`, use that; otherwise add `agent_id: Arc<str>` to `HookIpcServerConfig` and populate it at the construction site ~2482 from `server_client.agent_id()`); append `.with_diagnostic_aggregator(agent_id_for_ipc, diag_for_ingest)` after `.with_health_aggregator(health)` (2099).
    - Lifecycle test `diagnostic_push_loop_drains_pushes_and_shuts_down` mirrors `health_push_loop_starts_pushes_and_shuts_down` (8542): 50 ms interval, the same mock/unreachable `ServerClient` observation approach the health test uses, pre-seed the aggregator with `ingest("agent-test", 1234, vec![one])` before spawning, assert >=1 push attempt observed and clean shutdown via the `watch` channel; add a flush-on-shutdown assertion (signal shutdown with snapshots buffered -> final submit observed exactly once). Add a `RunLoopContext` assertion mirroring the 6612-6620 literal proving the diagnostic fields are populated when `server_client` is present.
  </behavior>
  <action>
    In `dlp-agent/src/service.rs`:
    (1) After `HEALTH_PUSH_INTERVAL` (1705) add `const DIAGNOSTIC_PUSH_INTERVAL: Duration = Duration::from_secs(60);`. After `health_push_loop` (1748) add `async fn diagnostic_push_loop(server_client: crate::server_client::ServerClient, diagnostic_aggregator: Arc<crate::diagnostic_aggregator::DiagnosticAggregator>, agent_id: String, interval: Duration, mut shutdown: tokio::sync::watch::Receiver<bool>)` with the `tokio::select!` body per PATTERNS (interval.tick drain+push with pid 0; shutdown.changed flush-and-stop). Use `tracing::debug!`/`tracing::warn!` only; never log snapshot contents. The function takes an OWNED `ServerClient` (matches `sc.clone()` at the spawn site) — verify the health loop's exact param type and mirror it.
    (2) In `run_loop_init`, immediately after the health spawn block (2303-2322), clone it: `let (diagnostic_push_shutdown, diagnostic_push_handle) = if let Some(ref sc) = server_client { let diagnostic_push_client = sc.clone(); let diagnostic_push_aggregator = Arc::clone(&diagnostic_aggregator); let diagnostic_push_agent_id = sc.agent_id().to_string(); let (tx, rx) = tokio::sync::watch::channel(false); let handle = tokio::spawn(async move { diagnostic_push_loop(diagnostic_push_client, diagnostic_push_aggregator, diagnostic_push_agent_id, DIAGNOSTIC_PUSH_INTERVAL, rx).await; }); (Some(tx), Some(handle)) } else { (None, None) };`. Confirm `diagnostic_aggregator` is the in-scope `Arc<DiagnosticAggregator>` at this point (mirror how `health_aggregator` is in scope at 2306); if the binding name differs, use the correct one (read the surrounding `run_loop_init`). Update the return-site literal at 2871-2873 to use the new bindings.
    (3) In `run_loop_shutdown`, after the health stop (5138), add the diagnostic stop section (signal + 5 s join + logs) mirroring 5127-5138 exactly.
    (4) In `spawn_hook_ipc_server`: add `let diag_for_ingest = Arc::clone(&diag);` after line 1953 (before the 1961 move). Read `HookIpcServerConfig` to locate the agent identity: prefer an existing field (`server_client` or `agent_id`); if neither exists, add `agent_id: Arc<str>` to the struct and set it at the construction site (~2482) from `server_client.agent_id()` (the same identity used at 2307). Append `.with_diagnostic_aggregator(agent_id_for_ipc, diag_for_ingest)` after `.with_health_aggregator(health)` (2099). The `agent_id_for_ipc` MUST equal the agent_id used for the push so aggregator keys and the remote identity stay consistent.
    (5) Tests: in the service test module, add `diagnostic_push_loop_drains_pushes_and_shuts_down` (mirror 8542 verbatim for the observation mechanism) and a `RunLoopContext` population assertion (mirror 6612-6620). Use a current-thread tokio runtime if the health test does (audit-capture thread-local tokens per STATE decision).
    GitNexus (CLAUDE.md): run `impact` on `health_push_loop` (cloned hot-path) and `spawn_hook_ipc_server` before editing and surface blast radius; run `detect_changes()` before committing.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent diagnostic_push run_loop_context -- --nocapture && cargo clippy -p dlp-agent -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source: `DIAGNOSTIC_PUSH_INTERVAL` const and `async fn diagnostic_push_loop(...)` exist; the loop calls `diagnostic_aggregator.drain_all()` BEFORE `server_client.submit_diagnostic_snapshot(&agent_id, 0, &snapshots).await` (no guard across await) and has a `shutdown.changed()` flush-and-stop arm.
    - Source: `run_loop_init` spawns the loop bound to `diagnostic_push_shutdown`/`diagnostic_push_handle`; the return-site literal at ~2871-2873 no longer hardcodes `None` for those two fields; `run_loop_shutdown` has a diagnostic stop section mirroring the health one; `spawn_hook_ipc_server` clones `diag_for_ingest` before :1961 and calls `.with_diagnostic_aggregator(...)` after :2099.
    - Source: NO new field is added to `RunLoopContext` (diff shows only the two existing fields changing from `None` to populated at the return site).
    - Behavior: `cargo test -p dlp-agent diagnostic_push_loop_drains_pushes_and_shuts_down` exits 0 (>=1 push + clean stop + flush-on-shutdown); the RunLoopContext population assertion exits 0.
    - Behavior: `cargo clippy -p dlp-agent -- -D warnings` exits 0.
  </acceptance_criteria>
  <done>Agent periodically drains `DiagnosticAggregator` and pushes to the server with graceful shutdown, the reserved `RunLoopContext` fields are populated (no literal drift), and the IPC server is wired with the aggregator + agent_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook DLL (named-pipe CLIENT) -> agent HookIpcServer (SERVER) | One-way `DiagnosticsResponse` frame; client identity attributed by the OS via `GetNamedPipeClientProcessId`, not by a DLL-supplied field. |
| agent in-process aggregator -> ServerClient -> server | Snapshots (paths/SIDs) cross from agent memory to the authenticated server endpoint. |
| service shutdown -> diagnostic_push_loop | Must flush in-flight snapshots before exit within the SCM `SHUTDOWN_TIMEOUT` budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.9-11 | Spoofing | cross-process pid attribution on the pipe | mitigate | Ingest keyed by `named_pipe_client_pid(pipe)` (OS-resolved real client PID), never a DLL-supplied field; mirrors the `UnhookAck` anti-spoof at hook_ipc.rs:718. |
| T-58.9-12 | Tampering | replayed/forged one-way frame influencing enforcement | accept | Diagnostics are non-authoritative telemetry; deny decisions are never derived from them (same disposition as `HealthResponse`/`BypassAlert`). |
| T-58.9-13 | Information Disclosure | snapshot contents in agent logs | mitigate | Ingest arm logs `pid` + `count` only; push loop logs `agent_id` + `count` only; snapshot bodies never logged (CLAUDE.md 9.13). |
| T-58.9-14 | Denial of Service | push loop blocked / aggregator guard across await | mitigate | `drain_all` returns an owned Vec and drops all `DashMap` guards before any `.await`; push failure is `warn!` + continue (best-effort); shutdown join is bounded to 5 s. |
| T-58.9-15 | Denial of Service | lost snapshots on shutdown | mitigate | `shutdown.changed()` arm performs a final `drain_all` + best-effort submit (flush-and-stop) before returning; lifecycle test asserts the final submit is observed exactly once. |
| T-58.9-16 | Tampering | struct-literal drift breaking cross-crate/test builds | mitigate | Populate the EXISTING `RunLoopContext::diagnostic_push_*` fields (no new fields); all four `HookIpcServer` constructor literals updated in the same commit (Pitfall 5). |
| T-58.9-SC | Tampering | cargo installs | mitigate | No new external packages; all dependencies already locked (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p dlp-agent --test hook_ipc_integration diagnostics_response_ingests_into_aggregator` passes.
- `cargo test -p dlp-agent diagnostic_push` passes (lifecycle + flush-on-shutdown).
- `cargo test -p dlp-agent run_loop_context` passes (diagnostic fields populated).
- `cargo check -p dlp-agent` and `cargo clippy -p dlp-agent -- -D warnings` pass.
- `cargo fmt -p dlp-agent --check` passes.
- `cargo test -p dlp-agent --tests --no-run` passes (catches any `HookIpcServer`/`RunLoopContext` literal drift across the agent test suite).
</verification>

<success_criteria>
- DIFF-04 criterion 2 satisfied: periodic drain+push loop wired into service lifecycle via the EXISTING `RunLoopContext` fields, with flush-and-stop and a lifecycle test.
- DIFF-04 criterion 3 consumer half satisfied: `HookIpcServer` ingests one-way `DiagnosticsResponse` frames into `DiagnosticAggregator` keyed by real client PID + agent_id, proven by an integration test.
- No `RunLoopContext`/`HookIpcServer` struct-literal drift; agent crate clippy-clean and fmt-clean.
</success_criteria>

<output>
Create `.planning/phases/58.9/58.9-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **HookIpcServer (dlp-agent/src/hook_ipc.rs):**
  - New fields `diagnostic_aggregator: Option<Arc<DiagnosticAggregator>>` and `agent_id: Option<Arc<str>>` (init `None` in all four constructors).
  - `HookIpcServer::with_diagnostic_aggregator(agent_id: impl Into<Arc<str>>, aggregator: Arc<DiagnosticAggregator>) -> Self`.
  - Threading of both fields through `run_with_ready -> accept_loop -> handle_connection` (owned in accept_loop; `Option<&Arc<_>>` in handle_connection).
  - New `IpcPayloadV1::DiagnosticsResponse(ref resp) =>` ingest arm (additive; separate from the `PullDiagnostics` reply arm) using `named_pipe_client_pid(pipe)` + `aggregator.ingest(agent_id, pid, snapshots)`.
- **Service (dlp-agent/src/service.rs):**
  - `const DIAGNOSTIC_PUSH_INTERVAL: Duration = Duration::from_secs(60);`
  - `async fn diagnostic_push_loop(server_client: ServerClient, diagnostic_aggregator: Arc<DiagnosticAggregator>, agent_id: String, interval: Duration, shutdown: watch::Receiver<bool>)` (drain+push pid 0; flush-and-stop).
  - Spawn block in `run_loop_init` populating the EXISTING `RunLoopContext::diagnostic_push_shutdown` / `diagnostic_push_handle` (return-site literal :2871-2873 changed from `None` to bindings).
  - Stop section in `run_loop_shutdown` (signal watch + 5 s join).
  - `spawn_hook_ipc_server`: `let diag_for_ingest = Arc::clone(&diag);` before :1961; `.with_diagnostic_aggregator(agent_id_for_ipc, diag_for_ingest)` after :2099; optional `agent_id: Arc<str>` on `HookIpcServerConfig` if not already present.
- **Tests:**
  - `dlp-agent/tests/hook_ipc_integration.rs::diagnostics_response_ingests_into_aggregator`.
  - `dlp_agent::service::tests::diagnostic_push_loop_drains_pushes_and_shuts_down` (+ RunLoopContext population assertion).

## Assumption resolutions (recorded for convergence)

- **A1 (cross-crate scope):** This plan is the consumer half; combined with Plan 02 (emit) the feed is complete. Criterion 3 end-to-end observability is proven in Plan 04.
- **A3 (per-pid vs aggregate batch):** Single aggregate batch to the server with `pid = 0`. Rationale: `drain_all` flattens all `{pid}_{agent_id}` keys and per-snapshot attribution is preserved inside each `DiagnosticSnapshot`'s fields (`abac_*`, `user_sid`); `list_diagnostics_handler` returns stored snapshots (admin_api.rs:5563-5590) without requiring a per-pid POST key. If review shows the dashboard groups strictly by the POST `pid`, switch Plan 01's loop to group drained snapshots by originating key and POST one batch per pid — flag at convergence. Default stands: pid 0.
- **A4 (rate limit):** Honored by the 60 s cadence + non-fatal 429 mapping in `submit_diagnostic_snapshot`; the loop retries next interval. (Optional single explicit retry on `StatusCode::TOO_MANY_REQUESTS` may be added inside the loop if review requires it.)
