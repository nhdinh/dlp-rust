---
phase: 54-admin-tui-protected-paths-bypass-alerts-screens
plan: 06
type: execute
wave: 3
depends_on:
  - 54-01
  - 54-02
  - 54-03
  - 54-04
  - 54-05
files_modified:
  - dlp-admin-cli/src/screens/dispatch.rs
  - dlp-admin-cli/src/screens/render.rs
  - dlp-admin-cli/src/app.rs
  - dlp-admin-cli/src/client.rs
  - dlp-admin-cli/src/screens/mod.rs
  - dlp-admin-cli/src/screens/protected_paths.rs
  - dlp-admin-cli/src/screens/bypass_alerts.rs
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - Full workspace builds with zero warnings
    - All dlp-admin-cli tests pass
    - Menu navigation works end-to-end (MainMenu -> SystemMenu -> both new screens -> back)
    - Screen transitions are correct (TextInput -> ProtectedPathList, Confirm -> ProtectedPathList, BypassAlertDetail -> BypassAlertList)
    - No orphaned imports, unused variables, or dead code warnings
    - Clippy passes with -D warnings
  artifacts:
    - path: dlp-admin-cli/src/screens/dispatch.rs
      provides: Complete event dispatch with all routing arms
    - path: dlp-admin-cli/src/screens/render.rs
      provides: Complete rendering with all draw functions
    - path: dlp-admin-cli/src/app.rs
      provides: Complete enum definitions
    - path: dlp-admin-cli/src/client.rs
      provides: Complete HTTP client methods
  key_links:
    - from: "All 7 modified files"
      to: "cargo build --workspace"
      via: "compilation"
      pattern: "zero warnings"
    - from: "All test modules"
      to: "cargo test -p dlp-admin-cli"
      via: "test execution"
      pattern: "all tests pass"
---

<objective>
Final integration plan: verify the complete Phase 54 implementation compiles, passes all tests, and meets quality gates. Fix any integration issues (orphaned imports, missing routing, menu drift, etc.) discovered when all plans come together.

Purpose: Ensure the vertical slices from Plans 01-05 integrate cleanly into a working whole.
Output: Green build, green tests, green clippy, updated ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-CONTEXT.md
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-RESEARCH.md
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-UI-SPEC.md

<!-- Integration checklist from RESEARCH.md pitfalls -->
Pitfalls to verify:
1. Borrow checker conflicts in dispatch handlers — all handlers use two-phase pattern
2. Menu index drift — SystemMenu has exactly 14 items (indices 0-13), "Back" at 13
3. Missing handle_event routing arms — all 3 new Screen variants have arms
4. Filter enum wire format mismatch — BypassAlertSeverityFilter::as_str() returns "crit"/"warn"/"info"
5. Pagination off-by-one — 0-based internal, 1-based display, div_ceil for total_pages
6. Optimistic UI revert on ack failure — handler reverts acknowledged field on error
7. Orphaned imports — remove any #[allow(dead_code)] that are no longer needed
8. Missing mod declarations — protected_paths and bypass_alerts declared in mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and fix compilation issues</name>
  <files>all dlp-admin-cli source files</files>
  <action>
Run `cargo build -p dlp-admin-cli` and fix any compilation errors or warnings:

1. **Missing imports**: Ensure all new types are imported where used:
   - `BypassAlertSeverityFilter` imported in dispatch.rs and render.rs
   - `PROTECTED_PATH_LIST_HINTS`, `PROTECTED_PATH_LIST_EMPTY` imported in dispatch.rs and render.rs
   - `BYPASS_ALERT_LIST_HINTS`, `BYPASS_ALERT_LIST_EMPTY`, `BYPASS_ALERT_DETAIL_HINTS` imported in dispatch.rs and render.rs
   - `InputPurpose::AddProtectedPath` and `ConfirmPurpose::DeleteProtectedPath` available in on_text_confirmed / on_confirm_yes / on_confirm_cancel

2. **Orphaned #[allow(dead_code)]**: After Plans 03-05, client methods are called from dispatch.rs. Remove `#[allow(dead_code)]` from:
   - `list_protected_paths`
   - `create_protected_path`
   - `update_protected_path`
   - `delete_protected_path`
   - `sync_protected_paths`
   - `list_bypass_alerts`
   - `ack_bypass_alert`

3. **Missing routing arms**: Verify handle_event match covers:
   - `Screen::ProtectedPathList { .. } => handle_protected_path_list(app, key),`
   - `Screen::BypassAlertList { .. } => handle_bypass_alert_list(app, key),`
   - `Screen::BypassAlertDetail { .. } => handle_bypass_alert_detail(app, key),`

4. **draw_screen routing arms**: Verify draw_screen match covers:
   - `Screen::ProtectedPathList { .. } => draw_protected_path_list(...)`
   - `Screen::BypassAlertList { .. } => draw_bypass_alert_list(...)`
   - `Screen::BypassAlertDetail { .. } => draw_bypass_alert_detail(...)`

5. **Menu consistency**: Verify SystemMenu has exactly 14 items with labels in this order:
   0. Server Status
   1. Agent List
   2. SIEM Config
   3. Alert Config
   4. LDAP Config
   5. USB Enforcement
   6. Cloud Config
   7. Print Config
   8. Label Review Queue
   9. Approval Management
   10. Protected Paths
   11. Bypass Alerts
   12. Syslog Config
   13. Back

   And the render.rs draw_menu call matches this order exactly.

6. **handle_text_input Esc routing**: Verify all InputPurpose variants have return routes:
   - `InputPurpose::AddProtectedPath => Screen::SystemMenu { selected: 10 }`

7. **on_confirm_cancel routing**: Verify all ConfirmPurpose variants have cancel routes:
   - `ConfirmPurpose::DeleteProtectedPath { .. } => action_load_protected_path_list(app, 0)`
  </action>
  <verify>
    <automated>cargo build -p dlp-admin-cli 2>&1 | grep -c "warning:" | grep -q "^0$" && echo "ZERO WARNINGS" || echo "HAS WARNINGS"</automated>
  </verify>
  <done>Build succeeds with zero warnings. All imports, routing arms, and menu entries are consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and fix failures</name>
  <files>all dlp-admin-cli source files</files>
  <action>
Run `cargo test -p dlp-admin-cli` and fix any test failures:

1. **Existing tests must still pass**: No regressions in existing functionality
2. **New tests must pass**:
   - protected_paths.rs constants tests
   - bypass_alerts.rs constants tests
   - app.rs enum construction tests
   - app.rs filter cycle tests
   - client.rs method signature tests
   - dispatch.rs protected path handler tests
   - dispatch.rs bypass alert handler tests
   - dispatch.rs menu navigation tests
   - render.rs protected path list render tests
   - render.rs bypass alert list render tests
   - render.rs bypass alert detail render tests

3. **Common test failure patterns to watch for**:
   - TestBackend buffer assertions failing due to text wrapping differences
   - Missing tokio runtime in test App construction
   - KeyEvent construction differences (use `KeyEvent::from(KeyCode::X)`)
   - Import path issues for test modules

4. **If tests fail**: Fix the underlying code, not the tests. Tests are the spec.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli</automated>
  </verify>
  <done>All tests pass — both existing and new. No regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Run clippy and fix all lints</name>
  <files>all dlp-admin-cli source files</files>
  <action>
Run `cargo clippy -p dlp-admin-cli -- -D warnings` and fix all lints:

1. **Common clippy issues for this phase**:
   - `unwrap_or_default().to_string()` can often be simplified
   - `format!("{}", var)` vs `var.to_string()` — prefer the latter for simple cases
   - `if let Some(x) = ...` chains that could use `and_then`
   - `match` on bool that could be `if`
   - Unused imports (after removing #[allow(dead_code)])
   - `clone()` on Copy types
   - `.iter().map(...).collect::<Vec<_>>()` that could be `.into_iter()`

2. **Specific patterns to check**:
   - `action_load_protected_path_list`: `all_paths[start..end].to_vec()` — clippy may suggest `.into()`
   - `draw_bypass_alert_list`: long `if let` chains for ack optimistic update
   - `format_relative_time`: match on duration could be simplified

3. **If clippy suggests a change that reduces readability**: Use `#[allow(clippy::lint_name)]` with a comment explaining why. The codebase already does this for `enum_variant_names`.
  </action>
  <verify>
    <automated>cargo clippy -p dlp-admin-cli -- -D warnings</automated>
  </verify>
  <done>Clippy passes with zero warnings. Any allowed lints have explanatory comments.</done>
</task>

<task type="auto">
  <name>Task 4: Verify workspace build and run sonar-scanner</name>
  <files>workspace Cargo.toml, all crates</files>
  <action>
1. Run `cargo build --workspace` to verify the entire workspace compiles:
   - dlp-admin-cli changes don't break dlp-server, dlp-agent, dlp-hook-dll, etc.
   - No breaking changes to shared types (dlp-common)

2. Run `cargo test --workspace` to verify all workspace tests pass:
   - Focus on dlp-admin-cli tests
   - Server tests should be unaffected (no server changes in this phase)

3. Run `sonar-scanner` if SONAR_TOKEN is available:
   - Check Quality Gate status
   - Fix any bugs or code smells reported
   - Address test coverage gaps by ensuring all new functions have tests

4. Run `cargo fmt --check` to verify formatting:
   - All new code follows rustfmt conventions
   - 4-space indentation, 100-char line length
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test --workspace && cargo fmt --check</automated>
  </verify>
  <done>Full workspace builds and tests pass. Code is formatted. SonarQube Quality Gate passes (or issues documented).</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md and create SUMMARY</name>
  <files>.planning/ROADMAP.md, .planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-06-SUMMARY.md</files>
  <action>
1. Update `.planning/ROADMAP.md` Phase 54 section:
   - Mark all 6 plans as complete
   - Update status to "Complete"
   - Update "Completed" date

2. Create `.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-06-SUMMARY.md`:
   - List all files modified
   - List all new files created
   - Summarize key decisions implemented
   - Document any deviations from plan
   - Note test coverage
   - Record any known issues or follow-up items

3. Verify all plan SUMMARYs exist (54-01 through 54-06):
   - Each executor should have created their SUMMARY.md
   - If any are missing, create them from the plan files

4. Commit all changes:
   ```bash
   git add -A
   git commit -m "feat(54): Admin TUI Protected Paths + Bypass Alerts Screens

   - ProtectedPathList: scrollable table with [A]/[M] badges, add/delete/sync
   - BypassAlertList: paginated feed with severity filters, optimistic ack
   - BypassAlertDetail: read-only detail with all forensic fields
   - 7 new EngineClient methods with unit tests
   - SystemMenu expanded to 14 items
   - Full test coverage for dispatch, render, and client layers

   Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
   ```
  </action>
  <verify>
    <automated>git status --short | wc -l | grep -q "^0$" && echo "ALL COMMITTED" || echo "UNCOMMITTED CHANGES"</automated>
  </verify>
  <done>ROADMAP updated, SUMMARY created, all changes committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full workspace | All crates compile together; no breaking changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-14 | Tampering | Incomplete integration leaving dead code | mitigate | Full build + clippy + test suite verifies no orphaned code |
| T-54-15 | Denial of Service | Build breaking other workspace crates | mitigate | `cargo build --workspace` verifies cross-crate compatibility |
| T-54-16 | Information Disclosure | Debug statements or credentials in committed code | mitigate | `cargo fmt --check` + manual review of diff before commit |
</threat_model>

<verification>
- `cargo build --workspace` succeeds with zero warnings
- `cargo test --workspace` passes all tests
- `cargo clippy -p dlp-admin-cli -- -D warnings` passes
- `cargo fmt --check` passes
- `sonar-scanner` Quality Gate passes (or issues documented)
- All 6 plan SUMMARYs exist
- ROADMAP.md updated
- Git commit created
</verification>

<success_criteria>
- [ ] Full workspace builds with zero warnings
- [ ] All workspace tests pass
- [ ] Clippy passes with -D warnings
- [ ] Code is formatted (cargo fmt --check passes)
- [ ] SonarQube Quality Gate passes (or issues documented)
- [ ] ROADMAP.md Phase 54 marked complete
- [ ] All 6 plan SUMMARYs exist
- [ ] Git commit created with descriptive message
- [ ] No uncommitted changes remain
</success_criteria>

<output>
Create `.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-06-SUMMARY.md` when done
</output>
