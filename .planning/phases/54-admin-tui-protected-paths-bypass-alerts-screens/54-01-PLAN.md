---
phase: 54-admin-tui-protected-paths-bypass-alerts-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-admin-cli/src/app.rs
  - dlp-admin-cli/src/screens/mod.rs
  - dlp-admin-cli/src/screens/protected_paths.rs
  - dlp-admin-cli/src/screens/bypass_alerts.rs
autonomous: true
requirements:
  - UX-01
  - UX-02
must_haves:
  truths:
    - Screen enum has variants for ProtectedPathList, BypassAlertList, BypassAlertDetail
    - BypassAlertSeverityFilter enum cycles All -> Crit -> Warn -> Info -> All
    - InputPurpose::AddProtectedPath and ConfirmPurpose::DeleteProtectedPath exist
    - Constants files contain hint strings and empty-state messages
    - screens/mod.rs declares new submodule modules
  artifacts:
    - path: dlp-admin-cli/src/app.rs
      provides: Screen variants, filter enum, purpose variants
      contains: "ProtectedPathList", "BypassAlertList", "BypassAlertDetail", "BypassAlertSeverityFilter", "AddProtectedPath", "DeleteProtectedPath"
    - path: dlp-admin-cli/src/screens/protected_paths.rs
      provides: Protected path screen constants
      contains: "PROTECTED_PATH_LIST_HINTS", "PROTECTED_PATH_LIST_EMPTY"
    - path: dlp-admin-cli/src/screens/bypass_alerts.rs
      provides: Bypass alert screen constants
      contains: "BYPASS_ALERT_LIST_HINTS", "BYPASS_ALERT_LIST_EMPTY", "BYPASS_ALERT_DETAIL_HINTS"
    - path: dlp-admin-cli/src/screens/mod.rs
      provides: Module declarations
      contains: "protected_paths", "bypass_alerts"
  key_links:
    - from: "app.rs Screen enum"
      to: "dispatch.rs handle_event match"
      via: "variant routing"
      pattern: "Screen::ProtectedPathList"
    - from: "app.rs BypassAlertSeverityFilter"
      to: "client.rs list_bypass_alerts query param"
      via: "as_str() -> Option<&str>"
      pattern: "severity=\"crit\""
    - from: "protected_paths.rs constants"
      to: "render.rs/dispatch.rs"
      via: "pub const import"
      pattern: "PROTECTED_PATH_LIST_HINTS"
---

<objective>
Add all enum variants, filter types, purpose types, and constants files needed by the downstream TUI screens. This is the foundation plan — everything else builds on these types.

Purpose: Without these types, no downstream plan can compile. This plan creates the contracts.
Output: Extended Screen enum, BypassAlertSeverityFilter, new purpose variants, two constants files, updated mod.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-CONTEXT.md
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-RESEARCH.md
@.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-UI-SPEC.md

<!-- Key interfaces from existing codebase -->
From dlp-admin-cli/src/app.rs (existing patterns):
- Screen enum ends at line 1074 with `Allowlist { screen: ... }`
- InputPurpose enum ends at line 126 with `AddDiskRegistryModel { ... }`
- ConfirmPurpose enum ends at line 166 with `RevokeApproval { id }`
- LabelFilter (lines 408-441) and ApprovalFilter (lines 443-479) are the models for BypassAlertSeverityFilter

From dlp-admin-cli/src/screens/mod.rs:
- `pub mod labels;` and `pub mod approvals;` are the existing constants-file modules

From dlp-admin-cli/src/screens/labels.rs:
- Pattern: `pub const LABEL_LIST_HINTS: &str = "..."` with `#[cfg(test)]` module verifying string contents

From dlp-admin-cli/src/screens/approvals.rs:
- Pattern: `pub const APPROVAL_LIST_HINTS: &str = "..."` with `#[cfg(test)]` module verifying string contents
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants files for Protected Paths and Bypass Alerts screens</name>
  <files>dlp-admin-cli/src/screens/protected_paths.rs, dlp-admin-cli/src/screens/bypass_alerts.rs</files>
  <action>
Create `dlp-admin-cli/src/screens/protected_paths.rs` with:
- `PROTECTED_PATH_LIST_HINTS` = "[a] Add  [d] Delete  [s] Sync  [r] Refresh  [PgUp/PgDn] Page  [Esc] Back"
- `PROTECTED_PATH_LIST_EMPTY` = "No protected paths configured. Press [a] to add one."
- `#[cfg(test)]` module with tests verifying each key substring exists in hints (follow labels.rs pattern)

Create `dlp-admin-cli/src/screens/bypass_alerts.rs` with:
- `BYPASS_ALERT_LIST_HINTS` = "[a] Ack  [f] Filter Severity  [h] Hide Ack'd  [r] Refresh  [Enter] Detail  [PgUp/PgDn] Page  [Esc] Back"
- `BYPASS_ALERT_LIST_EMPTY` = "No bypass alerts found."
- `BYPASS_ALERT_DETAIL_HINTS` = "[Enter/Esc] Back to list"
- `#[cfg(test)]` module with tests verifying each key substring exists in hints

Both files MUST follow the exact doc comment pattern from labels.rs/approvals.rs:
`//! Shared constants for the {X} screens.`
`//! Used by both \`render.rs\` (draw) and \`dispatch.rs\` (event handling).`
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli protected_paths -- --test-threads=1 && cargo test -p dlp-admin-cli bypass_alerts -- --test-threads=1</automated>
  </verify>
  <done>Both constants files exist, compile, and tests pass. Hint strings contain all required keybindings per D-13/D-14.</done>
</task>

<task type="auto">
  <name>Task 2: Add Screen enum variants, filter enum, and purpose variants to app.rs</name>
  <files>dlp-admin-cli/src/app.rs</files>
  <action>
Add the following to `dlp-admin-cli/src/app.rs`:

1. **BypassAlertSeverityFilter enum** (after ApprovalFilter, around line 479):
   - Variants: `All` (default), `Crit`, `Warn`, `Info`
   - Methods: `next(self) -> Self` cycling All->Crit->Warn->Info->All
   - `as_str(self) -> Option<&'static str>` returning None for All, Some("crit"|"warn"|"info") for others
   - `label(self) -> &'static str` returning "All", "Critical", "Warning", "Info"
   - Derive: `Debug, Clone, Copy, PartialEq, Eq, Default`

2. **InputPurpose variant** (after existing variants, around line 126):
   - `AddProtectedPath,`

3. **ConfirmPurpose variant** (after existing variants, around line 166):
   - `DeleteProtectedPath { id: String },`
   - Add to the `#[allow(clippy::enum_variant_names)]` attribute (already present, just add the variant)

4. **Screen enum variants** (before the closing `Allowlist` variant, around line 1068):
   ```rust
   /// Protected path management list screen.
   /// Pattern: LabelList — scrollable table with CRUD actions and sync.
   ProtectedPathList {
       paths: Vec<serde_json::Value>,
       selected: usize,
       page: usize,
       page_size: usize,
       total: usize,
   },
   /// Bypass alert triage list screen.
   /// Pattern: ApprovalList — scrollable table with ack action and filters.
   BypassAlertList {
       alerts: Vec<serde_json::Value>,
       selected: usize,
       filter: BypassAlertSeverityFilter,
       hide_acknowledged: bool,
       page: usize,
       page_size: usize,
       total: usize,
       status_message: String,
   },
   /// Bypass alert detail popup (read-only).
   /// Pattern: ApprovalDetail — full-screen read-only view.
   BypassAlertDetail {
       alert: serde_json::Value,
   },
   ```

5. **Tests** in the existing `#[cfg(test)] mod tests` block:
   - Test `bypass_alert_severity_filter_next_cycles` verifying the cycle
   - Test `bypass_alert_severity_filter_as_str` verifying wire format
   - Test `bypass_alert_severity_filter_label` verifying display labels
   - Test `screen_protected_path_list_constructible` verifying variant construction
   - Test `screen_bypass_alert_list_constructible` verifying variant construction
   - Test `screen_bypass_alert_detail_constructible` verifying variant construction
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli app::tests -- --test-threads=1 && cargo test -p dlp-admin-cli bypass_alert_severity -- --test-threads=1</automated>
  </verify>
  <done>All new enum variants compile, tests pass, and the filter cycles correctly. No existing tests broken.</done>
</task>

<task type="auto">
  <name>Task 3: Update screens/mod.rs to declare new modules</name>
  <files>dlp-admin-cli/src/screens/mod.rs</files>
  <action>
Add `mod protected_paths;` and `mod bypass_alerts;` to `dlp-admin-cli/src/screens/mod.rs` alongside the existing `mod labels;` and `mod approvals;`.

The module declarations should be alphabetically ordered:
```rust
pub mod allowlist;
mod approvals;
mod bypass_alerts;
mod cloud_config;
mod dispatch;
mod labels;
mod print_config;
mod protected_paths;
mod render;
mod syslog_config;
mod usb_enforcement;
```
  </action>
  <verify>
    <automated>cargo check -p dlp-admin-cli</automated>
  </verify>
  <done>mod.rs compiles with new module declarations. No warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI -> Server API | Untrusted operator input crosses to server; server validates all paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | Path input via TextInput | mitigate | Server validates with GetFullPathNameW (Phase 52); TUI does not pre-validate per D-03 |
| T-54-02 | Information Disclosure | Error toasts showing paths | accept | Error toasts show server error body; server must not leak internal paths (server-side concern) |
| T-54-03 | Elevation of Privilege | Delete of auto-derived paths | mitigate | TUI checks source=="manual" before showing confirm; server also enforces (defense in depth) |
</threat_model>

<verification>
- `cargo test -p dlp-admin-cli` passes (all existing + new tests)
- `cargo check -p dlp-admin-cli` produces no warnings
- `cargo clippy -p dlp-admin-cli -- -D warnings` passes
</verification>

<success_criteria>
- [ ] `protected_paths.rs` and `bypass_alerts.rs` exist with correct constants and tests
- [ ] `app.rs` has 3 new Screen variants, 1 filter enum, 2 purpose variants
- [ ] `mod.rs` declares both new modules
- [ ] All tests pass, no compiler warnings
- [ ] Downstream plans (02-06) can reference these types without modification
</success_criteria>

<output>
Create `.planning/phases/54-admin-tui-protected-paths-bypass-alerts-screens/54-01-SUMMARY.md` when done
</output>
