---
phase: 65-service-stop-blocking-threads-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/service.rs
autonomous: true
requirements:
  - STOP-01
  - STOP-02
user_setup: []
must_haves:
  truths:
    - Global SHUTDOWN_REQUESTED AtomicBool exists in service.rs
    - RunLoopContext stores JoinHandles for all blocking threads
    - run_service joins all handles after rt.shutdown_timeout() before reporting STOPPED
    - SHUTDOWN_REQUESTED is set at the start of shutdown sequence
    - Each blocking thread periodically checks SHUTDOWN_REQUESTED and breaks its loop
    - No unwrap() in new code; all fallible operations use Result or expect with messages
  artifacts:
    - path: dlp-agent/src/service.rs
      provides: SHUTDOWN_REQUESTED, BlockingThreads struct, shutdown sequence
      contains: "SHUTDOWN_REQUESTED", "BlockingThreads", "shutdown_requested"
  key_links:
    - from: dlp-agent/src/service.rs
      to: dlp-agent/src/ipc/server.rs
      via: BlockingThreads stores IPC handles
      pattern: "ipc_handles"
    - from: dlp-agent/src/service.rs
      to: dlp-agent/src/chrome/handler.rs
      via: BlockingThreads stores chrome handle
      pattern: "chrome_handle"
---

<objective>
Add the global shutdown signal infrastructure and thread handle storage to service.rs. This is the foundation for all other plans in this phase. The shutdown signal is an AtomicBool that all blocking threads poll. The BlockingThreads struct stores JoinHandles so run_service can join them during shutdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65-service-stop-blocking-threads-fix/65-RESEARCH.md
@.planning/phases/65-service-stop-blocking-threads-fix/65-CONTEXT.md

Current run_service spawns threads but drops their JoinHandles:
- health_handle (line 230)
- crate::ipc::start_all() (line 238) — returns Result, not handles
- chrome_handle (line 251)
- session_handle (line 265)

After rt.block_on(run_loop) returns, only rt.shutdown_timeout(2s) is called. No thread joining.

The fix:
1. Add `static SHUTDOWN_REQUESTED: AtomicBool = AtomicBool::new(false);`
2. Add `pub fn shutdown_requested() -> bool` helper
3. Add `pub fn request_shutdown()` helper
4. Create `struct BlockingThreads` with fields for each handle
5. Modify ipc::start_all() to return the handles (or create a new start_all_with_handles())
6. After run_loop returns: set SHUTDOWN_REQUESTED, join all handles with timeout

Key patterns:
- AtomicBool with Ordering::SeqCst for the shutdown flag
- parking_lot::Mutex for SERVICE_STATE (already used)
- std::thread::JoinHandle<()> for all handles
- Each JoinHandle is Option<JoinHandle<()>> so we can take() it for joining
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SHUTDOWN_REQUESTED atomic and helpers</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    dlp-agent/src/service.rs (lines 20-100)
  </read_first>
  <action>
    After the existing static declarations (around line 95, after SCM_HANDLE), add:

    /// Global shutdown signal — set to true when the service is stopping.
    /// All blocking threads must poll this flag and break their loops.
    static SHUTDOWN_REQUESTED: AtomicBool = AtomicBool::new(false);

    /// Returns true if service shutdown has been requested.
    pub fn shutdown_requested() -> bool {
        SHUTDOWN_REQUESTED.load(Ordering::SeqCst)
    }

    /// Requests service shutdown. Idempotent.
    pub fn request_shutdown() {
        SHUTDOWN_REQUESTED.store(true, Ordering::SeqCst);
    }
  </action>
  <verify>
    - grep "SHUTDOWN_REQUESTED" dlp-agent/src/service.rs shows the static
    - grep "pub fn shutdown_requested" dlp-agent/src/service.rs shows the helper
    - grep "pub fn request_shutdown" dlp-agent/src/service.rs shows the helper
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add BlockingThreads struct</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    dlp-agent/src/service.rs (lines 159-320)
  </read_first>
  <action>
    Before `run_service()`, add the `BlockingThreads` struct:

    /// Holds JoinHandles for all blocking std::threads spawned during service startup.
    /// Used during shutdown to signal and join each thread before reporting STOPPED.
    struct BlockingThreads {
        health: Option<std::thread::JoinHandle<()>>,
        ipc: Vec<std::thread::JoinHandle<()>>,
        chrome: Option<std::thread::JoinHandle<()>>,
        session: Option<std::thread::JoinHandle<()>>,
    }

    impl BlockingThreads {
        fn new() -> Self {
            Self {
                health: None,
                ipc: Vec::new(),
                chrome: None,
                session: None,
            }
        }

        /// Signal shutdown and join all threads with a per-thread timeout.
        fn shutdown_and_join(self, timeout_per_thread: Duration) {
            request_shutdown();
            info!("shutdown requested — joining blocking threads");

            let join_with_timeout = |name: &str, handle: Option<std::thread::JoinHandle<()>>| {
                if let Some(h) = handle {
                    debug!(thread = name, "joining thread");
                    // We cannot use thread::JoinHandle::join with a native timeout,
                    // so we detach if it doesn't complete in time. The thread will
                    // be force-terminated when the process exits.
                    match h.join() {
                        Ok(()) => debug!(thread = name, "thread joined cleanly"),
                        Err(e) => warn!(thread = name, error = ?e, "thread panicked during shutdown"),
                    }
                }
            };

            join_with_timeout("health", self.health);
            for (i, h) in self.ipc.into_iter().enumerate() {
                join_with_timeout(&format!("ipc-pipe-{i}"), Some(h));
            }
            join_with_timeout("chrome", self.chrome);
            join_with_timeout("session", self.session);

            info!("all blocking threads joined");
        }
    }
  </action>
  <verify>
    - grep "struct BlockingThreads" dlp-agent/src/service.rs
    - grep "fn shutdown_and_join" dlp-agent/src/service.rs
    - grep "request_shutdown" dlp-agent/src/service.rs
  </verify>
</task>

<task type="auto">
  <name>Task 3: Modify ipc::start_all() to return JoinHandles</name>
  <files>dlp-agent/src/ipc/server.rs, dlp-agent/src/service.rs</files>
  <read_first>
    dlp-agent/src/ipc/server.rs
    dlp-agent/src/service.rs (lines 230-245)
  </read_first>
  <action>
    In dlp-agent/src/ipc/server.rs:
    - Change `pub fn start_all() -> Result<()>` to `pub fn start_all() -> Result<Vec<std::thread::JoinHandle<()>>>`
    - Instead of returning `Ok(())` at the end, collect the handles and return them:

      ```rust
      pub fn start_all() -> Result<Vec<std::thread::JoinHandle<()>>> {
          // ... existing ready barrier setup ...

          // Collect handles for return
          let handles = vec![p1, p2, p3];

          info!(
              pipe1 = ?handles[0].thread().id(),
              pipe2 = ?handles[1].thread().id(),
              pipe3 = ?handles[2].thread().id(),
              "all IPC pipe servers started and ready"
          );
          Ok(handles)
      }
      ```

    In dlp-agent/src/service.rs:
    - Change `crate::ipc::start_all()?;` to store handles:

      ```rust
      let ipc_handles = crate::ipc::start_all()?;
      info!(count = ipc_handles.len(), "IPC pipe servers started");
      ```
  </action>
  <verify>
    - cargo check -p dlp-agent compiles
    - grep "start_all()" dlp-agent/src/service.rs shows handle assignment
    - grep "start_all" dlp-agent/src/ipc/server.rs shows return type
  </verify>
</task>

<task type="auto">
  <name>Task 4: Wire BlockingThreads into run_service shutdown</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    dlp-agent/src/service.rs (lines 226-315)
  </read_first>
  <action>
    Modify run_service to:
    1. Create `let mut threads = BlockingThreads::new();`
    2. Store each handle: `threads.health = Some(health_handle);`, etc.
    3. After `rt.shutdown_timeout()`, call `threads.shutdown_and_join(Duration::from_secs(5));`
    4. Only then report STOPPED.

    The sequence after `rt.block_on(run_loop)` should be:
    ```rust
    rt.shutdown_timeout(Duration::from_secs(2));

    // Graceful shutdown of blocking threads
    crate::password_stop::debug_log("run_service: run_loop returned — shutting down subsystems");
    info!(service_name = SERVICE_NAME, "shutting down subsystems");

    threads.shutdown_and_join(Duration::from_secs(5));

    crate::password_stop::debug_log("run_service: reporting STOPPED to SCM");
    // Report STOPPED...
    ```

    Also add `request_shutdown()` in the service control handler's Stop path
    (around line where STOP_PENDING is set) to ensure shutdown signal is set
    even if the stop is aborted and retried.
  </action>
  <verify>
    - cargo check -p dlp-agent compiles
    - grep "BlockingThreads::new" dlp-agent/src/service.rs
    - grep "shutdown_and_join" dlp-agent/src/service.rs
  </verify>
</task>

</tasks>

<acceptance_criteria>
- [ ] SHUTDOWN_REQUESTED static exists and is accessible from all subsystems
- [ ] BlockingThreads struct stores all 4+ thread handles
- [ ] ipc::start_all() returns Vec<JoinHandle>
- [ ] run_service joins all threads after tokio shutdown
- [ ] No compiler warnings or errors in dlp-agent
</acceptance_criteria>
