---
phase: 65-service-stop-blocking-threads-fix
plan: 05
type: gap_closure
wave: 3
depends_on: []
files_modified:
  - dlp-agent/src/service.rs
autonomous: true
gap_closure: true
requirements:
  - STOP-01
  - STOP-02
  - STOP-03
user_setup: []
must_haves:
  truths:
    - Watchdog timer spawns in shutdown_and_join with timeout = SHUTDOWN_TIMEOUT * 4 + 5s
    - Watchdog calls std::process::exit(1) on timeout
    - Ordering::Acquire for load, Ordering::Release for store on SHUTDOWN_REQUESTED
    - No unwrap() in new code (use expect() with messages or if let)
    - Per-thread elapsed duration logged via debug! macro
    - reset_shutdown_signal() called at start of run_service before any thread spawning
    - All existing tests still pass
  artifacts:
    - path: dlp-agent/src/service.rs
      provides: Fixed shutdown_and_join with watchdog, Acquire/Release ordering, no unwrap, per-thread logging, reset at startup
      contains: "watchdog", "Ordering::Acquire", "Ordering::Release"
---

<objective>
Fix 5 verification gaps found in Phase 65 execution:
1. Add watchdog timer to BlockingThreads::shutdown_and_join
2. Change AtomicBool ordering from SeqCst to Acquire/Release
3. Replace unwrap() with expect() in thread ID logging
4. Add per-thread elapsed duration logging
5. Call reset_shutdown_signal() at start of run_service
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
The verifier found these gaps in the executed code:

1. **Watchdog timer missing** — shutdown_and_join has no timeout fallback. If a thread is blocked in ConnectNamedPipeW with no client, the service hangs forever.

2. **SeqCst ordering** — Lines 102, 107, 115 use Ordering::SeqCst. Reviewers required Acquire/Release.

3. **unwrap() in new code** — Lines 301, 331, 338 use .unwrap() on Option<JoinHandle> for thread ID logging.

4. **Per-thread elapsed missing** — join_with_timeout logs "thread joined cleanly" without duration.

5. **reset_shutdown_signal() missing from run_service()** — Not called at startup. Required for in-process restart safety.

All fixes are in dlp-agent/src/service.rs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add watchdog timer to shutdown_and_join</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    Add a watchdog thread inside shutdown_and_join that:
    - Computes timeout = SHUTDOWN_TIMEOUT.saturating_mul(4).saturating_add(Duration::from_secs(5))
    - Spawns a thread that sleeps for that duration
    - Calls std::process::exit(1) with error! log if still running

    Add SHUTDOWN_TIMEOUT constant if not present (use Duration::from_secs(10) as default).
  </action>
  <acceptance_criteria>
    - Watchdog thread spawned before joining begins
    - Timeout computed as SHUTDOWN_TIMEOUT * 4 + 5s
    - Calls std::process::exit(1) on timeout with error! log
    - cargo check -p dlp-agent compiles with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix atomic ordering to Acquire/Release</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    Change:
    - SHUTDOWN_REQUESTED.load(Ordering::SeqCst) → Ordering::Acquire
    - SHUTDOWN_REQUESTED.store(true, Ordering::SeqCst) → Ordering::Release
    - SHUTDOWN_REQUESTED.store(false, Ordering::SeqCst) → Ordering::Release
  </action>
  <acceptance_criteria>
    - All three ordering usages changed to Acquire/Release
    - cargo check -p dlp-agent compiles with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace unwrap() with expect() in thread ID logging</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    Lines 301, 331, 338 use .unwrap() on Option<JoinHandle> for thread ID logging.
    Replace with .expect("health monitor thread ID") etc.
    Or better: use if let Some(h) = ... { info!(...) } to avoid panic entirely.
  </action>
  <acceptance_criteria>
    - No .unwrap() calls in the new shutdown-related code
    - cargo check -p dlp-agent compiles with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Add per-thread elapsed duration logging</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    In join_with_timeout closure, add elapsed duration to the success log:
    ```rust
    Ok(()) => {
        let elapsed = thread_start.elapsed();
        debug!(thread = name, ?elapsed, "thread joined cleanly");
    }
    ```
    Where thread_start is Instant::now() at the start of each join.
  </action>
  <acceptance_criteria>
    - Per-thread elapsed duration logged via debug! macro
    - cargo check -p dlp-agent compiles with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Call reset_shutdown_signal() at run_service startup</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    Add `reset_shutdown_signal();` as the first statement in run_service(),
    before any thread spawning. Add a comment explaining why (supports in-process restart).
  </action>
  <acceptance_criteria>
    - reset_shutdown_signal() called at the very start of run_service
    - Comment explains in-process restart support
    - cargo check -p dlp-agent compiles with zero warnings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and create SUMMARY.md</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
    - cargo test -p dlp-agent
    - cargo clippy -p dlp-agent -- -D warnings
    - Create 65-05-SUMMARY.md
  </action>
  <acceptance_criteria>
    - All dlp-agent tests pass
    - Clippy clean with -D warnings
    - SUMMARY.md created
  </acceptance_criteria>
</task>

</tasks>

<acceptance_criteria>
- [ ] Watchdog timer exists in shutdown_and_join with correct timeout
- [ ] Ordering is Acquire/Release
- [ ] No unwrap() in new code
- [ ] Per-thread elapsed duration logged
- [ ] reset_shutdown_signal() called at run_service startup
- [ ] All tests pass, clippy clean
- [ ] SUMMARY.md created
</acceptance_criteria>
