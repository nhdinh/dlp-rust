---
phase: 58.6-targeted-hook-injection-only-file-operation-processes
plan: 08
type: execute
wave: 7
depends_on:
  - 58.6-01
  - 58.6-02
  - 58.6-03
  - 58.6-04
  - 58.6-05
  - 58.6-06
  - 58.6-07
files_modified:
  - dlp-agent/src/targeted_injection_integration.rs
  - dlp-agent/src/lib.rs
  - .planning/phases/58.6/58.6-VERIFICATION.md
autonomous: true
requirements:
  - TGT-01
  - TGT-02
  - TGT-03
  - TGT-04
  - TGT-05
  - TGT-06
user_setup: []
must_haves:
  truths:
    - "Integration tests prove immediate injection for file-operation-likely processes in Targeted mode"
    - "Integration tests prove lazy injection catches LowConfidence processes on first T3/T4 ETW event"
    - "Universal mode and Monitor mode behaviors are regression-tested"
    - "Allowlist safety and PID-reuse safety are regression-tested"
    - "PPL safety is regression-tested only if a mockable process_protection seam is added; otherwise PPL claims are limited to unit tests of the seam"
    - "TargetedInjectionTelemetry audit event round-trips through the audit capture sink"
    - "Workspace passes cargo test, clippy, fmt, and sonar-scanner quality gates"
  artifacts:
    - path: dlp-agent/src/targeted_injection_integration.rs
      provides: "End-to-end targeted injection integration tests"
      contains: "test_targeted_inject_immediate"
    - path: dlp-agent/src/lib.rs
      provides: "test module declaration"
      contains: "targeted_injection_integration"
    - path: .planning/phases/58.6/58.6-VERIFICATION.md
      provides: "Phase verification record"
      contains: "Observable Truths"
  key_links:
    - from: dlp-agent/src/targeted_injection_integration.rs
      to: dlp-agent/src/process_classifier.rs
      via: "Constructs ProcessClassifier with mock PeScoreSource/CliSignalSource"
    - from: dlp-agent/src/targeted_injection_integration.rs
      to: dlp-agent/src/lazy_injector.rs
      via: "Feeds synthetic EtwFileEvent stream and asserts Injected(Lazy) state"
    - from: dlp-agent/src/targeted_injection_integration.rs
      to: dlp-agent/src/audit_emitter.rs
      via: "Enables test capture and asserts emitted TargetedInjectionTelemetry / LazyInjectionAttempt"
    - from: dlp-agent/src/targeted_injection_integration.rs
      to: dlp-agent/src/classification_cache.rs
      via: "Uses MockTierResolver implementing TierResolver trait seam from Plan 05"
---

<objective>
Validate the complete targeted-injection subsystem with in-crate integration tests that exercise the classifier, lazy injector, universal fallback, and telemetry audit path, then run the workspace quality gates and record the phase verification.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.6/58.6-REVIEWS.md
@dlp-agent/src/process_classifier.rs
@dlp-agent/src/lazy_injector.rs
@dlp-agent/src/universal_injector.rs
@dlp-agent/src/process_registry.rs
@dlp-agent/src/audit_emitter.rs
@dlp-agent/src/service.rs
@dlp-common/src/audit.rs
@dlp-agent/src/hook_injector.rs
@dlp-agent/src/allowlist.rs
@dlp-agent/src/config.rs
@dlp-agent/src/classification_cache.rs
@dlp-agent/src/inject_process.rs
@dlp-agent/src/process_utils.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the integration test module and test helpers</name>
  <files>dlp-agent/src/targeted_injection_integration.rs, dlp-agent/src/lib.rs</files>
  <read_first>
    - dlp-agent/src/universal_injector.rs
    - dlp-agent/src/process_classifier.rs
    - dlp-agent/src/hook_injector.rs
    - dlp-agent/src/audit_emitter.rs
    - dlp-agent/src/classification_cache.rs
    - dlp-agent/src/inject_process.rs
    - dlp-agent/src/process_utils.rs
  </read_first>
  <action>
    Create `dlp-agent/src/targeted_injection_integration.rs` as an in-crate test module so it can access crate-private types and the audit capture sink.

    - Declare the module in `dlp-agent/src/lib.rs` with `#[cfg(test)] pub mod targeted_injection_integration;`.
    - Inside the module, define test doubles:
      - `CountingInjector` implementing `InjectProcess` (from Plan 01) with an `Arc<AtomicUsize>` count and a `DashMap<u32, u64>` recording injected PIDs.
      - `MockPeSource` implementing `PeScoreSource` (from Plan 04) with a configurable `Option<u32>` score.
      - `MockCliSource` implementing `CliSignalSource` (from Plan 04) with configurable `basename_match` and `command_line_match`.
      - `MockTierResolver` implementing `TierResolver` (from Plan 05) with a configurable `Fn(path: &str) -> Option<Classification>` so lazy-injection tests do not depend on the real shared-memory `ClassificationCache` (review concern).
      - `MockProcessProtection` implementing a `ProcessProtectionSource` trait seam (if not already available) so PPL tests can be mocked; otherwise document that PPL safety is covered by unit tests of `detect_ppl` and the classifier's allowlist/PPL precedence (review fix: do not claim PPL integration coverage without a seam).
    - Add helpers:
      - `build_test_classifier(mode, pe_score, cli_basename, cli_cmdline, config)` constructs a `ProcessClassifier` from the mocks.
      - `build_test_universal_injector(registry, matcher, injector, classifier)` constructs the injector with a dummy retry channel and the classifier attached.
      - `build_test_emit_context()` returns an `EmitContext` for audit capture tests.
      - `run_lazy_until_closed(lazy_injector, rx)` drops the sender and joins the lazy thread via `run_until_channel_closed` (review fix: clean shutdown helper).
    - Keep all helpers `pub(crate)` and gated by `#[cfg(test)]`.
  </action>
  <acceptance_criteria>
    - `dlp-agent/src/lib.rs` declares `#[cfg(test)] pub mod targeted_injection_integration;`.
    - `dlp-agent/src/targeted_injection_integration.rs` exists and compiles as an in-crate test module.
    - `CountingInjector` implements `InjectProcess` and records injected PIDs and a total count.
    - `MockPeSource` implements `PeScoreSource` with a configurable score.
    - `MockCliSource` implements `CliSignalSource` with configurable `basename_match` and `command_line_match`.
    - `MockTierResolver` implements `TierResolver` with configurable path-to-tier mapping.
    - A `run_lazy_until_closed` helper drops the sender before joining the lazy thread.
    - PPL is either tested via a `MockProcessProtection` seam or explicitly scoped out of integration tests with a documented rationale.
    - All test helpers are gated by `#[cfg(test)]` and not exported from release builds.
    - `cargo check -p dlp-agent --tests` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo check -p dlp-agent --tests</automated>
  </verify>
  <done>`cargo check -p dlp-agent --tests` passes and the integration test module compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the targeted-injection behavior matrix tests</name>
  <files>dlp-agent/src/targeted_injection_integration.rs</files>
  <behavior>
    - Test: `ClassificationMode::Targeted` with high PE score records `Injected { injection_path: Immediate }` and emits `TargetedImmediateInjection`.
    - Test: `ClassificationMode::Targeted` with only a basename match records `Skipped(LowConfidence)`; a subsequent T3 ETW event causes lazy injection and records `Injected { injection_path: Lazy }`.
    - Test: `ClassificationMode::Universal` injects a low-score process immediately (backward compat).
    - Test: `ClassificationMode::Monitor` records `Skipped(Monitoring)` and never injects.
    - Test: An allowlisted image path returns `SkipAllowlisted` and is never injected in any mode.
    - Test: PID reuse (same PID, different `creation_time`) does not reuse the previous classification cache entry.
    - Test: `ProcessCounts` and `TargetedInjectionTelemetry` match the mixed scenario outcomes.
    - Test: Lazy-injection test uses `run_lazy_until_closed` and asserts the lazy injector terminates when the sender is dropped.
  </behavior>
  <action>
    Implement the integration tests in `dlp-agent/src/targeted_injection_integration.rs`.

    1. **Immediate-injection test**:
       - Build a `Targeted` classifier with `pe_score` above threshold and `command_line_match = false`.
       - Build a `ProcessEvent` with a unique PID/image path.
       - Call `universal_injector.classify_and_inject(..., Etw, None, false, None).await`.
       - Assert the counting injector recorded the PID exactly once.
       - Assert registry state is `Injected { injection_path: Immediate, .. }`.
       - Enable audit capture, rerun, and assert a `TargetedImmediateInjection` event was captured.

    2. **Lazy-injection test**:
       - Build a `Targeted` classifier with `pe_score = 0` and no command-line match so it returns `SkipLowConfidence`.
       - Classify and record `Skipped(LowConfidence)` in the registry.
       - Build a `LazyInjector` using the same registry and counting injector, with a `MockTierResolver` that returns `Some(Classification::T4)` for a test path.
       - Send a synthetic `EtwFileEvent { pid, file_name: test_t4_path, op: FileOp::Create }` through a crossbeam channel.
       - Use `run_lazy_until_closed(lazy_injector, lazy_rx)` to run the lazy injector on a blocking thread and shut it down cleanly.
       - Assert the counting injector recorded the PID exactly once.
       - Assert registry state transitions to `Injected { injection_path: Lazy, .. }`.
       - Assert the registry `t3t4_event_missed` counter is 1 (first event) and `t3t4_event_covered` is 0 before injection.
       - Assert a `LazyInjectionAttempt` audit event was captured.

    3. **Mode regression tests**:
       - `Universal`: low PE score still results in immediate injection.
       - `Monitor`: high PE score results in `Skipped(Monitoring)` and zero injections.

    4. **Allowlist safety test**:
       - Add an allowlist entry matching the test image path.
       - For `Universal`, `Targeted`, and `Monitor`, assert the decision is `SkipAllowlisted` and the injector count remains zero.

    5. **PID-reuse test**:
       - Classify PID 12345 with `creation_time = 100` as immediate inject.
       - Classify PID 12345 with `creation_time = 200` as low confidence and assert the registry state for the second key is `Skipped(LowConfidence)`.

    6. **Counter test**:
       - Run a mixed scenario (two immediate, one lazy, one monitoring, one allowlist, one low-confidence skipped).
       - Assert `registry.counts()` and `registry.targeted_telemetry_snapshot()` reflect the expected values.

    7. **Coverage math test**:
       - Drive 1000 T3/T4 events across 10 low-confidence PIDs through `LazyInjector`.
       - Assert steady-state coverage (events after first per PID) is 100% and overall coverage is at least 99% (review fix: valid coverage proof).

    Use `audit_emitter::enable_test_capture` and `drain_test_events` to assert audit events. Hold `audit_emitter::audit_test_lock()` when asserting to avoid interleaving with other tests.
  </action>
  <acceptance_criteria>
    - All behavior matrix tests pass.
    - Tests use `InjectProcess`, `TierResolver`, `PeScoreSource`, and `CliSignalSource` seams instead of concrete production types.
    - Lazy-injection test uses `MockTierResolver` and does not require real shared-memory classification cache.
    - Lazy-injection test shuts down cleanly using `run_lazy_until_closed`.
    - Audit capture assertions verify `TargetedImmediateInjection`, `LazyInjectionAttempt`, and `TargetedSkipAllowlisted` events.
    - Coverage math test uses at least 1000 events across 10 processes and asserts mathematically valid >=99% overall coverage.
    - `cargo test -p dlp-agent targeted_injection_integration -- --test-threads=1` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dlp-agent targeted_injection_integration -- --test-threads=1</automated>
  </verify>
  <done>All targeted-injection integration tests pass; behavior matrix covers Targeted, Universal, Monitor, lazy injection, allowlist safety, PID reuse, and valid coverage math.</done>
</task>

<task type="auto">
  <name>Task 3: Run workspace quality gates and create VERIFICATION.md</name>
  <files>.planning/phases/58.6/58.6-VERIFICATION.md</files>
  <action>
    Execute the full quality gate sequence and record the phase verification.

    1. Run the dlp-agent test suite and the new integration tests:
       - `cargo test -p dlp-agent`
       - `cargo test -p dlp-agent targeted_injection_integration -- --test-threads=1`

    2. Run workspace-wide checks (review fix: use `cargo test --workspace`):
       - `cargo test --workspace`
       - `cargo check --workspace`
       - `cargo clippy --workspace -- -D warnings`
       - `cargo fmt --check`

    3. Run `sonar-scanner` with the exported `SONAR_TOKEN`. Inspect the quality gate output. If the gate reports bugs, smells, or insufficient coverage, fix them in this task by adding tests or refactoring, then re-scan until the gate passes. If `SONAR_TOKEN` is not exported, record this as a CI-only gate in `58.6-VERIFICATION.md` and do not block the local verification record (review concern: do not assume the token is available locally).

    4. Create `.planning/phases/58.6/58.6-VERIFICATION.md` containing:
       - Phase goal restatement.
       - Observable truths mapped to test commands and pass/fail evidence.
       - Test counts and failure summary.
       - Quality gate results (cargo test, clippy, fmt, sonar).
       - A statement that all requirements TGT-01..TGT-06 are satisfied, with no remaining gaps.
  </action>
  <acceptance_criteria>
    - `cargo test -p dlp-agent` passes.
    - `cargo test --workspace` passes.
    - `cargo clippy --workspace -- -D warnings` passes.
    - `cargo fmt --check` passes.
    - Sonar quality gate passes or is explicitly documented as a CI-only gate with the reason.
    - `.planning/phases/58.6/58.6-VERIFICATION.md` exists and documents all observable truths.
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace && cargo clippy --workspace -- -D warnings && cargo fmt --check</automated>
  </verify>
  <done>Workspace tests pass, clippy and fmt are clean, sonar quality gate is satisfied or documented as CI-only, and `.planning/phases/58.6/58.6-VERIFICATION.md` is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> Production code | Tests must not weaken production trust boundaries or expose dangerous internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.6-21 | Tampering | Test-only helpers exposed in release | mitigate | Gate the integration test module and all helpers with `#[cfg(test)]`; never export test doubles from release builds. |
| T-58.6-22 | Repudiation | Missing verification record | mitigate | `58.6-VERIFICATION.md` captures observable truths, test commands, evidence, and quality-gate results. |
| T-58.6-23 | Information Disclosure | Sonar scanner output | accept | Sonar results contain no secrets; the token is provided via the exported `SONAR_TOKEN` env var per CLAUDE.md. Local runs without the token are documented as CI-only. |
| T-58.6-R10 | Spoofing | PPL integration test without seam | mitigate | Either add a `ProcessProtectionSource` trait seam for PPL or explicitly limit PPL claims to unit-tested `detect_ppl` behavior. |
</threat_model>

<verification>
- `cargo test -p dlp-agent targeted_injection_integration -- --test-threads=1` passes.
- `cargo test -p dlp-agent` passes.
- `cargo test --workspace` passes.
- `cargo clippy --workspace -- -D warnings` passes.
- `cargo fmt --check` passes.
- `sonar-scanner` quality gate passes, or its absence is documented as a CI-only gate in `58.6-VERIFICATION.md`.
- `.planning/phases/58.6/58.6-VERIFICATION.md` exists and documents all observable truths.
</verification>

<success_criteria>
- Integration tests cover Targeted, Universal, Monitor, lazy-injection, allowlist safety, PID reuse, counter accuracy, and valid coverage math.
- Tests use the `InjectProcess` and `TierResolver` trait seams and audit capture sink.
- `MockTierResolver` makes lazy-injection tests deterministic without real shared-memory cache.
- Lazy-injection tests shut down cleanly via `run_lazy_until_closed`.
- Workspace compiles with zero warnings, passes clippy, fmt, and sonar quality gates (or sonar is documented as CI-only).
- Phase 58.6 verification document is complete with evidence for every observable truth.
</success_criteria>

## Artifacts this phase produces

- `dlp-agent/src/targeted_injection_integration.rs` in-crate integration test module.
- `dlp-agent/src/lib.rs` test-only module declaration.
- `CountingInjector`, `MockPeSource`, `MockCliSource`, and `MockTierResolver` test doubles.
- `run_lazy_until_closed` shutdown helper.
- `.planning/phases/58.6/58.6-VERIFICATION.md` phase verification record.

<output>
Create `.planning/phases/58.6/58.6-08-SUMMARY.md` and `.planning/phases/58.6/58.6-VERIFICATION.md` when done.
</output>
