---
phase: 58.6-targeted-hook-injection-only-file-operation-processes
plan: 08
type: execute
wave: 7
depends_on:
  - 58.6-01
  - 58.6-04
  - 58.6-05
  - 58.6-06
  - 58.6-07
files_modified:
  - dlp-server/src/db/repositories/audit_events.rs
  - dlp-server/src/audit_store.rs
  - dlp-server/src/db/schema/audit_events.sql
  - dlp-server/src/alert_router.rs
autonomous: true
requirements:
  - TGT-03
  - TGT-05
user_setup: []
must_haves:
  truths:
    - "dlp-server persists `details` in SQLite, returns it in queries, and forwards it in SIEM/alert payloads"
  artifacts:
    - path: dlp-server/src/db/repositories/audit_events.rs
      provides: "AuditEventRow.details persistence"
      contains: "details: Option<String>"
    - path: dlp-server/src/audit_store.rs
      provides: "store_events_sync maps details"
      contains: "details: event.details.clone()"
    - path: dlp-server/src/db/schema/audit_events.sql
      provides: "details TEXT column"
      contains: "details TEXT"
---

<objective>
Update the server-side persistence layer (SQLite schema, repository row, batch ingest, query response, SIEM/alert serialization) so it preserves the new `AuditEvent.details` payload instead of dropping it.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.6/58.6-REVIEWS.md
@dlp-server/src/db/repositories/audit_events.rs
@dlp-server/src/audit_store.rs
@dlp-server/src/db/schema/audit_events.sql
@dlp-server/src/alert_router.rs
@dlp-common/src/audit.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist `AuditEvent.details` on the server side</name>
  <files>dlp-server/src/db/repositories/audit_events.rs, dlp-server/src/audit_store.rs, dlp-server/src/db/schema/audit_events.sql, dlp-server/src/alert_router.rs</files>
  <read_first>
    - dlp-server/src/db/repositories/audit_events.rs
    - dlp-server/src/audit_store.rs
    - dlp-server/src/db/schema/audit_events.sql
    - dlp-server/src/alert_router.rs
    - dlp-common/src/audit.rs
  </read_first>
  <behavior>
    - Test: `AuditEventRow` constructed from an event with `details: Some(json)` preserves the JSON.
    - Test: `store_events_sync` stores `details` in the database and it round-trips through `AuditEventRepository::list_by_filters`.
    - Test: Legacy events without `details` deserialize and store as `NULL`.
    - Test: SIEM relay includes the `details` field in the forwarded payload.
    - Test: Alert router includes `details` when emitting `LazyInjectionFailure` alerts.
  </behavior>
  <action>
    Update the dlp-server persistence and routing layers so `AuditEvent.details` is not dropped (review HIGH concern).

    1. In `dlp-server/src/db/schema/audit_events.sql` (or the migration file that creates the `audit_events` table):
       - Add `details TEXT` column after `chain_hash` (or at the end) with no `NOT NULL` constraint.
       - Create a migration that adds the column if the table already exists (e.g., `ALTER TABLE audit_events ADD COLUMN details TEXT;`).

    2. In `dlp-server/src/db/repositories/audit_events.rs`:
       - Add `pub details: Option<String>` to `AuditEventRow` after `chain_hash`.
       - Update `insert_batch` SQL and parameter binding to include `details`.
       - Update any `SELECT` helper methods that construct `AuditEventRow` from query rows to include the new column.

    3. In `dlp-server/src/audit_store.rs`:
       - Update `store_events_sync` to map `event.details.clone()` into `AuditEventRow::details` (review fix: do not drop the field).

    4. In `dlp-server/src/alert_router.rs`:
       - Find any exhaustive `AuditEvent` struct literal (around line 338) and add `details: None` (or the appropriate value) for compilation.
       - Ensure alert payloads forward `details` when present.

    5. Search the workspace for all remaining exhaustive `AuditEvent` literals (server, admin-cli, agent tests) and add `details: None` so compilation succeeds.

    6. Add server-side unit/integration tests covering details persistence and SIEM forwarding.
  </action>
  <acceptance_criteria>
    - `audit_events` table schema includes a nullable `details TEXT` column.
    - `AuditEventRow` includes `details: Option<String>`.
    - `store_events_sync` maps `event.details` to `AuditEventRow::details`.
    - Query methods return `details` when present.
    - SIEM relay and alert router include `details` in forwarded payloads.
    - All exhaustive `AuditEvent` literals in dlp-server, dlp-admin-cli, and agent tests compile.
    - `cargo test -p dlp-server audit` passes.
    - `cargo check --workspace` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p dlp-server audit && cargo check --workspace</automated>
  </verify>
  <done>dlp-server persists `AuditEvent.details`, returns it in queries, and forwards it in SIEM/alert payloads; workspace compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Server audit ingest | `details` payload must survive persistence and routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.6-H03 | Information Disclosure | Server drops `details` payload | mitigate | Add `details` column to SQLite schema, `AuditEventRow`, `store_events_sync`, query responses, SIEM relay, and alert router. |
| T-58.6-R2 | Tampering | Audit schema change breaks deserialization | mitigate | `details` is optional with `serde(default)`; new `EventType` variants are appended and serialized as SCREAMING_SNAKE_CASE. |
</threat_model>

<verification>
- `cargo test -p dlp-server audit` passes.
- `cargo check --workspace` passes.
- `cargo clippy -p dlp-server -- -D warnings` passes.
- `cargo fmt --check` passes.
</verification>

<success_criteria>
- dlp-server SQLite schema, `AuditEventRow`, `store_events_sync`, query responses, SIEM relay, and alert router all preserve `details`.
</success_criteria>

## Artifacts this phase produces

- `audit_events` SQLite schema with nullable `details TEXT` column.
- `AuditEventRow.details` field.
- `store_events_sync` mapping `event.details` to `AuditEventRow::details`.
- SIEM relay and alert router forwarding `details`.

<output>
Create `.planning/phases/58.6/58.6-08-SUMMARY.md` when done.
</output>
