---
phase: 58.6-targeted-hook-injection-only-file-operation-processes
plan: 07
type: execute
wave: 6
depends_on:
  - 58.6-01
  - 58.6-05
  - 58.6-06
files_modified:
  - dlp-common/src/audit.rs
  - dlp-agent/src/process_registry.rs
  - dlp-agent/src/service.rs
autonomous: true
requirements:
  - TGT-05
user_setup: []
must_haves:
  truths:
    - "ProcessRegistry counts classified_injected, lazy_injected, and skipped_low_confidence"
    - "Agent emits a periodic TargetedInjectionTelemetry audit event with coverage counters"
    - "Lazy injection attempts and failures emit dedicated SIEM audit events"
  artifacts:
    - path: dlp-common/src/audit.rs
      provides: "New EventType variants"
      contains: "TargetedInjectionTelemetry"
    - path: dlp-agent/src/process_registry.rs
      provides: "Extended telemetry counters"
      contains: "classified_injected"
    - path: dlp-agent/src/service.rs
      provides: "Telemetry emission wiring"
      contains: "event_type = \"TargetedInjectionTelemetry\""
  key_links:
    - from: dlp-agent/src/process_registry.rs
      to: dlp-common/src/audit.rs
      via: "Counts feed into TargetedInjectionTelemetry payload"
    - from: dlp-agent/src/service.rs
      to: dlp-agent/src/process_registry.rs
      via: "Telemetry task reads ProcessRegistry::counts() or new targeted_counts() method"
    - from: dlp-agent/src/lazy_injector.rs
      to: dlp-common/src/audit.rs
      via: "Emits LazyInjectionAttempt / LazyInjectionFailure"
---

<objective>
Provide observability that proves the targeted injection policy is working and that T3/T4 file-event coverage remains >= 99%.

Purpose: Operators need telemetry to tune thresholds and detect coverage gaps; SIEM integration is required by project CLAUDE.md.
Output: New `EventType` variants in `dlp-common`, extended `ProcessCounts`, and wired emission in `dlp-agent`.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.6-targeted-hook-injection-only-file-operation-processes/58.6-RESEARCH.md
@dlp-common/src/audit.rs
@dlp-agent/src/process_registry.rs
@dlp-agent/src/service.rs
@dlp-agent/src/audit_emitter.rs
@dlp-agent/src/lazy_injector.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add targeted-injection EventType variants to dlp-common</name>
  <files>dlp-common/src/audit.rs</files>
  <action>
    Extend `dlp_common::audit::EventType` in `dlp-common/src/audit.rs`.

    Add the following variants after `UnhookFailure`:
    - `TargetedInjectionTelemetry` — periodic aggregated telemetry.
    - `LazyInjectionAttempt` — a process was lazily injected after a T3/T4 ETW event.
    - `LazyInjectionFailure` — lazy injection attempt failed.

    Update `routed_to_siem()` to include all three.
    Update `triggers_alert()` to include `LazyInjectionFailure` only.

    Add unit tests in `dlp-common/src/audit.rs` verifying:
    - All three new variants route to SIEM.
    - Only `LazyInjectionFailure` triggers_alert.
  </action>
  <verify>
    <automated>cargo test -p dlp-common audit::tests</automated>
  </verify>
  <done>`cargo test -p dlp-common audit::tests` passes; new EventType variants have correct SIEM and alert routing.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ProcessRegistry telemetry counters</name>
  <files>dlp-agent/src/process_registry.rs</files>
  <action>
    Extend `dlp-agent/src/process_registry.rs` to support targeted-injection telemetry.

    1. In `ProcessCounts`, add:
       - `classified_injected: u64`
       - `lazy_injected: u64`
       - `skipped_low_confidence: u64`
       - `unhooked_t3t4_event_count: u64` (reserved for Plan 08 / lazy integration tests)
       - `missed_t3t4_event_count: u64` (reserved for Plan 08)

    2. Update `counts()` to populate these from the new `ProcessState` variants:
       - `Injected { injection_path: InjectionPath::Immediate, .. }` increments `classified_injected`.
       - `Injected { injection_path: InjectionPath::Lazy, .. }` increments `lazy_injected`.
       - `Skipped(SkipReason::LowConfidence)` increments `skipped_low_confidence`.

    3. Add a new method `targeted_telemetry_snapshot(&self) -> TargetedInjectionTelemetry` that returns a struct with:
       - `classified_injected`
       - `lazy_injected`
       - `skipped_low_confidence`
       - `total_tracked`
       - `coverage_percent` = (classified_injected + lazy_injected) / (classified_injected + lazy_injected + skipped_low_confidence + skipped_failed) * 100.0

    4. Add unit tests verifying the new counters.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent process_registry::tests</automated>
  </verify>
  <done>`cargo test -p dlp-agent process_registry::tests` passes; new counters are populated correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Wire telemetry emission in service.rs and lazy injector</name>
  <files>dlp-agent/src/service.rs, dlp-agent/src/lazy_injector.rs</files>
  <action>
    1. In `dlp-agent/src/service.rs`, replace the existing periodic telemetry aggregation task (around lines 3230–3247) with an enhanced version:
       - Read both `registry.telemetry_snapshot()` and `registry.targeted_telemetry_snapshot()`.
       - Emit `tracing::info!` with `event_type = "injection_telemetry"` as before (backward compatible).
       - If the audit emitter / emit context is available in scope, emit an `AuditEvent` of type `EventType::TargetedInjectionTelemetry` with:
         - `user_sid = "SYSTEM"`, `user_name = "SYSTEM"`, `resource_path = ""`, `classification = Classification::Public`, `action_attempted = Action::Read`, `decision = Decision::Allow`.
         - Add a JSON `details` field (or use existing flexible fields) containing the counters.
       - If no emit context is readily available in the task closure, add the emit context clone to the closure. Do not block the telemetry loop on emit failures.

    2. In `dlp-agent/src/lazy_injector.rs`, emit audit events:
       - On successful lazy injection: `EventType::LazyInjectionAttempt` with `resource_path` set to the triggering file path and `details` containing pid and creation_time.
       - On failed lazy injection: `EventType::LazyInjectionFailure` with `triggers_alert=true`, same fields plus error text.
       - The `LazyInjector` needs an optional `emit_context: Option<Arc<crate::audit_emitter::EmitContext>>` field. If `None`, skip emission but continue operation.

    3. Update `LazyInjector::new` signature and the call site in `service.rs` to pass the emit context.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent lazy_injector process_registry::tests</automated>
  </verify>
  <done>Telemetry task and lazy injector compile and tests pass; audit events are emitted for lazy success/failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent telemetry -> SIEM | Telemetry must not leak sensitive file paths or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.6-18 | Information Disclosure | Telemetry file paths | mitigate | Telemetry counters aggregate counts only; `LazyInjectionAttempt` may include the triggering file path — ensure it is needed and avoid user content. |
| T-58.6-19 | Denial of Service | Audit emit blocks telemetry loop | mitigate | Emit is best-effort; failures are logged but do not block the periodic task. |
| T-58.6-20 | Repudiation | Missing telemetry | mitigate | Periodic emission ensures coverage is observable even if lazy events are rare. |
</threat_model>

<verification>
- `cargo test -p dlp-agent lazy_injector process_registry::tests` passes.
- `cargo test -p dlp-common audit::tests` passes.
- `cargo check -p dlp-agent` passes.
- `cargo clippy -p dlp-agent -- -D warnings` and `cargo clippy -p dlp-common -- -D warnings` pass.
- `cargo fmt --check` passes.
</verification>

<success_criteria>
- `EventType::TargetedInjectionTelemetry`, `LazyInjectionAttempt`, and `LazyInjectionFailure` exist and route to SIEM.
- `ProcessRegistry` exposes targeted telemetry counters.
- Service telemetry task emits periodic targeted injection telemetry.
- Lazy injector emits attempt/failure audit events.
</success_criteria>

<output>
Create `.planning/phases/58.6-targeted-hook-injection-only-file-operation-processes/58.6-07-SUMMARY.md` when done.
</output>
