---
phase: 58.3-close-gap-ops-04-execute-physical-windows-11-uat
plan: 03
type: execute
wave: 3
depends_on:
  - "58.3-02"
files_modified:
  - ".planning/milestones/v0.10.0-UAT.md"
autonomous: false
requirements:
  - OPS-04

must_haves:
  truths:
    - "Every placeholder in .planning/milestones/v0.10.0-UAT.md is replaced with actual data from execution"
    - "CRIT-04 benchmark results are correctly transcribed into Category I"
    - "The UAT Sign-Off section is complete with tester name, date, host details, and approval signatures"
    - "Any blocking failures are documented with defect IDs and remediation plans"
    - "The updated milestone document is committed as the Phase 58.3 deliverable"
    - "The scenario count in the sign-off table reads 36 (not 30)"
  artifacts:
    - path: ".planning/milestones/v0.10.0-UAT.md"
      provides: "Fully populated UAT plan with actual results, pass/fail status, notes, and sign-off"
      contains: "No 'TO BE FILLED DURING UAT EXECUTION' placeholders"
    - path: "C:\UAT\v0.10.0\results.json"
      provides: "Canonical machine-readable results for audit trail"
      min_lines: 36
    - path: "C:\UAT\v0.10.0\crit04_results.json"
      provides: "Canonical CRIT-04 benchmark measurements"
      contains: "overhead_percent, baseline_median, hooked_median"
  key_links:
    - from: "C:\UAT\v0.10.0\results.json"
      to: ".planning/milestones/v0.10.0-UAT.md"
      via: "Human tester transcribes each scenario's result, notes, and artifacts into the milestone document"
      pattern: "Actual Result, Pass/Fail, Notes, Artifacts Captured fields filled"
    - from: "C:\UAT\v0.10.0\UAT-Summary.md"
      to: ".planning/milestones/v0.10.0-UAT.md"
      via: "Summary counts and CRIT-04 results are copied into the milestone document's sign-off section"
      pattern: "Results Overview table, CRIT-04 Benchmark Results, Ship/No-Ship Recommendation"
---

<objective>
Transcribe all UAT execution artifacts from the test host into the canonical milestone document, correct the scenario count from 30 to 36, complete the ship/no-ship analysis using the formal decision matrix (not the script's built-in recommendation), obtain sign-off, and commit the result as the Phase 58.3 deliverable.

Purpose: The milestone document is the human-auditable ship record. Machine-readable artifacts on the test host are canonical but not part of the repository. This step bridges the two.
Output: A fully populated, signed-off .planning/milestones/v0.10.0-UAT.md committed to the repository.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.3-close-gap-ops-04-execute-physical-windows-11-uat/58.3-CONTEXT.md
@.planning/milestones/v0.10.0-UAT.md
@C:\UAT\v0.10.0\results.json
@C:\UAT\v0.10.0\crit04_results.json
@C:\UAT\v0.10.0\UAT-Summary.md
@C:\UAT\v0.10.0\environment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scenario count and transcribe script artifacts into milestone UAT document</name>
  <read_first>
    - .planning/milestones/v0.10.0-UAT.md (milestone template with placeholders)
    - C:\UAT\v0.10.0\results.json (per-scenario results from Plan 02)
    - C:\UAT\v0.10.0\crit04_results.json (script-generated benchmark)
    - C:\UAT\v0.10.0\UAT-Summary.md (summary counts and ship recommendation)
  </read_first>
  <acceptance_criteria>
    - Total Scenarios field in UAT Sign-Off reads 36 (not 30)
    - Environment table is fully populated from environment.json
    - Peripheral Availability table is filled with Present/N-A and justifications
    - All 36 scenarios have Actual Result, Pass/Fail, Notes, and Artifacts Captured filled
    - Category I (I01, I02) contains CRIT-04 overhead percentages and raw median values
    - I02 uses manual COM automation benchmark results if available, otherwise script word_launch
    - No "TO BE FILLED DURING UAT EXECUTION" placeholders remain
    - Expected Result fields remain unchanged
  </acceptance_criteria>
  <files>.planning/milestones/v0.10.0-UAT.md, C:\UAT\v0.10.0\results.json, C:\UAT\v0.10.0\crit04_results.json, C:\UAT\v0.10.0\UAT-Summary.md</files>
  <action>
    Copy the execution results from the test host into .planning/milestones/v0.10.0-UAT.md per D-15. Replace every unfilled placeholder in the milestone document with actual data.

    1. Fix the scenario count: Change the "Total Scenarios" field in the UAT Sign-Off section from 30 to 36. The script and plan both define 36 scenarios (A01-J05); the milestone template incorrectly lists 30. This must be corrected before transcription to avoid an inconsistent ship record.
    2. Environment table: Fill Host OS, OS Build, CPU, RAM, Disk, EDR Installed, EDR Version, OneDrive Version, Google Drive Version, Dropbox Version, Box Drive Version, Policy Bundle Version, Tester Name, and Test Date using data from C:\UAT\v0.10.0\environment.json.
    3. Peripheral Availability table: Fill the Present/N-A columns and N-A Justification column using the peripheral checklist from Plan 01.
    4. For each of the 36 scenarios (A01-J05):
       - Copy the "Actual Result" from the tester's notes in results.json into the Actual Result field.
       - Copy the Pass/Fail/N-A result into the Pass/Fail field.
       - Copy the notes into the Notes field.
       - List the captured artifacts (screenshots, agent log snippets, SIEM payloads) in the Artifacts Captured field.
    5. Category I (Performance): Fill the Actual Result and Pass/Fail for I01 and I02 using the CRIT-04 benchmark data. For I01, use the cargo_build results from C:\UAT\v0.10.0\crit04_results.json. For I02, use the word_launch results from C:\UAT\v0.10.0\crit04_word_com_fallback.json if it exists (the manual COM automation benchmark from Plan 02 Task 2), otherwise use the script's word_launch results from crit04_results.json. Include the raw baseline and hooked median values, the overhead percentage, and whether the <=25% gate passed.
    6. Ensure the document retains its markdown structure and all tables are properly formatted.

    Do NOT modify the Expected Result fields — those are the reference criteria and must remain unchanged.
  </action>
  <verify>
    <automated>(Select-String -Path '.planning/milestones/v0.10.0-UAT.md' -Pattern 'TO BE FILLED DURING UAT EXECUTION' -SimpleMatch).Count returns 0</automated>
  </verify>
  <done>
    Every placeholder in v0.10.0-UAT.md is replaced with actual data. All 36 scenarios have Actual Result, Pass/Fail, Notes, and Artifacts Captured filled. The Total Scenarios field in the sign-off table reads 36. CRIT-04 results are transcribed into Category I using the manual COM automation benchmark for I02 if available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete ship/no-ship analysis and sign-off</name>
  <read_first>
    - C:\UAT\v0.10.0\UAT-Summary.md (summary counts, blocking failures, script recommendation)
    - .planning/milestones/v0.10.0-UAT.md (UAT Sign-Off section template)
  </read_first>
  <acceptance_criteria>
    - Tester Sign-Off table has real names, dates, version "v0.10.0", host details, EDR version, and counts
    - Total Scenarios = 36 (corrected from 30)
    - Blocking Failures list includes scenario ID, defect ID, description, and remediation plan
    - Major Issues and Minor Issues are documented with workarounds where applicable
    - Overall Pass/Fail uses the formal decision matrix (not the script's built-in recommendation)
    - Known Limitations documents N/A justifications and environmental constraints
    - Engineering Lead and QA Lead signatures and dates are present
    - Git commit exists for the updated milestone document
  </acceptance_criteria>
  <files>.planning/milestones/v0.10.0-UAT.md</files>
  <action>
    Complete the UAT Sign-Off section per D-16, using the summary data from C:\UAT\v0.10.0\UAT-Summary.md. Use the formal decision matrix (NOT the script's built-in recommendation, which conflates N/A count with blocking/major/minor distinction).

    1. Tester Sign-Off table:
       - Fill Tester Name with the actual tester's name.
       - Fill Test Date with the UAT execution date.
       - Fill Version Tested with "v0.10.0".
       - Fill Host Details with a summary (e.g., "Windows 11 Pro Build 22631, Intel i7-12700, 32GB RAM").
       - Fill EDR Version with the actual Windows Defender version or other EDR version.
       - Fill Total Scenarios with 36 (corrected from the template's 30).
       - Fill Passed, Failed, and N/A counts from UAT-Summary.md.
    2. Blocking Failures: List every FAIL result for a blocking scenario (B01-B06, G01, H02, I01-I02, J01-J02, J04-J05). For each, include:
       - Scenario ID
       - Defect ID (if filed in the issue tracker)
       - Brief description of the failure
       - Remediation plan (e.g., "Fix in Phase 58.4", "Known limitation documented")
    3. Major Issues: List any non-blocking FAIL scenarios with workaround descriptions.
    4. Minor Issues: List any cosmetic or documentation issues that do not block ship.
    5. Overall Pass/Fail: Use the formal decision matrix (NOT the script's recommendation):
       - "PASS" if 0 blocking failures AND >=80% of non-N/A scenarios pass AND Engineering Lead + QA Lead both sign off.
       - "FAIL" if 1+ blocking failures.
       - "CONDITIONAL" if 0 blocking failures but major issues exist (engineering lead decides).
    6. Known Limitations: Document any N/A scenarios (e.g., "No optical drive available — E03 marked N/A"), environmental constraints, or test setup limitations.
    7. Approval Authority table:
       - Obtain Engineering Lead name, signature (physical or digital), and date.
       - Obtain QA Lead name, signature (physical or digital), and date.
       - Both signatures are required for ship per D-27.
    8. Final Notes: Add any additional observations, recommendations, or follow-up items.
    9. Commit the updated .planning/milestones/v0.10.0-UAT.md to the repository:
       - `git add .planning/milestones/v0.10.0-UAT.md`
       - `git commit -m "docs(uat): populate v0.10.0 UAT results from Phase 58.3 physical Windows 11 execution"`
       - `git push`
  </action>
  <verify>
    <automated>git diff --stat .planning/milestones/v0.10.0-UAT.md shows modifications; git log --oneline -1 shows the UAT commit</automated>
  </verify>
  <done>
    UAT Sign-Off section is fully populated with tester details, pass/fail counts (36 total), blocking/major/minor issues, ship recommendation using the formal decision matrix, and both Engineering Lead and QA Lead signatures. The updated document is committed and pushed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify milestone document accuracy before finalizing</name>
  <files>.planning/milestones/v0.10.0-UAT.md</files>
  <action>Human operator reviews the fully populated milestone document and confirms accuracy before finalizing Phase 58.3.</action>
  <verify>
    <automated>Human confirms no unfilled placeholders remain and sign-off is complete</automated>
  </verify>
  <done>Operator approves milestone document accuracy; Phase 58.3 is finalized.</done>
  <what-built>Fully populated .planning/milestones/v0.10.0-UAT.md with actual results, CRIT-04 measurements, ship analysis using the formal decision matrix, and completed sign-off.</what-built>
  <how-to-verify>
    1. Open .planning/milestones/v0.10.0-UAT.md and confirm no placeholder text that reads "TO BE FILLED DURING UAT EXECUTION" remains.
    2. Verify all 36 scenarios have Actual Result, Pass/Fail, Notes, and Artifacts Captured fields filled.
    3. Verify Category I (I01, I02) contains the actual CRIT-04 overhead percentages and raw median values. For I02, verify the results reflect the Word COM automation open/save benchmark (not just process launch) if Word was installed.
    4. Verify the UAT Sign-Off section has:
       - Completed Tester Sign-Off table with real names and counts
       - Total Scenarios = 36 (not 30)
       - Blocking Failures list (or "None")
       - Major and Minor Issues lists
       - Overall Pass/Fail verdict using the formal decision matrix (not the script's built-in recommendation)
       - Known Limitations
       - Engineering Lead and QA Lead signatures and dates
    5. Verify the git commit exists with the correct message.
    6. Confirm the document is ready for the v0.10.0 ship decision.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize Phase 58.3, or describe any corrections needed in the milestone document.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host artifacts -> Repository | UAT results move from test host to committed milestone document |
| Human tester -> Milestone doc | Tester transcribes and interprets results into the ship record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.3-10 | Tampering | Results transcribed incorrectly into milestone doc | mitigate | Cross-reference results.json against the milestone doc; verify counts match UAT-Summary.md |
| T-58.3-11 | Repudiation | Ship decision lacks proper sign-off | mitigate | Both Engineering Lead and QA Lead signatures required per D-27; document names and dates |
| T-58.3-12 | Information Disclosure | UAT doc committed with sensitive host details | accept | Host details are operational metadata (CPU, RAM, OS build) — no PII or credentials |
| T-58.3-13 | Denial of Service | N/A — no service disruption risk in documentation | accept | N/A |
| T-58.3-14 | Elevation of Privilege | N/A — no privilege changes in this task | accept | N/A |
</threat_model>

<verification>
- (Select-String -Path '.planning/milestones/v0.10.0-UAT.md' -Pattern 'TO BE FILLED DURING UAT EXECUTION' -SimpleMatch).Count returns 0
- All 36 scenarios have Actual Result, Pass/Fail, Notes, and Artifacts Captured filled.
- CRIT-04 benchmark results (overhead_percent, baseline_median, hooked_median) are present in Category I.
- UAT Sign-Off section contains completed Tester Sign-Off table with Total Scenarios = 36, Blocking Failures list, Major/Minor Issues lists, Overall Pass/Fail using formal decision matrix, Known Limitations, and both Engineering Lead and QA Lead signatures.
- Git commit exists for the updated milestone document.
</verification>

<success_criteria>
- Every placeholder in .planning/milestones/v0.10.0-UAT.md is replaced with actual UAT data.
- CRIT-04 benchmark results are correctly transcribed with raw values and overhead percentages.
- Scenario count in sign-off table reads 36 (not 30).
- Ship/no-ship decision is documented using the formal decision matrix (not the script's built-in recommendation).
- Blocking failures have defect IDs and remediation plans.
- Engineering Lead and QA Lead have both signed off.
- The updated milestone document is committed and pushed to the repository.
</success_criteria>

<output>
Create `.planning/phases/58.3-close-gap-ops-04-execute-physical-windows-11-uat/58.3-03-SUMMARY.md` when done.
</output>
