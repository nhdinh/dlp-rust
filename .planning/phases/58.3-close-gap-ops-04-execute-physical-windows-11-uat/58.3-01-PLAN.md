---
phase: 58.3-close-gap-ops-04-execute-physical-windows-11-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "C:\UAT\v0.10.0\environment.json"
  - "C:\UAT\v0.10.0\TestData\"
  - "C:\UAT\v0.10.0\peripheral-checklist.md"
  - "C:\UAT\v0.10.0\print-config-check.md"
  - "C:\UAT\v0.10.0\cloud-client-check.md"
  - "C:\UAT\v0.10.0\RELEASE_NOTES.md"
  - "C:\UAT\v0.10.0\post-reboot-check.md"
  - "C:\UAT\v0.10.0\crit04_word_com_fallback.json"
  - "C:\Protected\Test\t4_template.docx"
  - "RELEASE_NOTES.md"
autonomous: false
requirements:
  - OPS-04
user_setup:
  - item: "Dedicated physical Windows 11 host"
    why: "UAT must run on real hardware with real peripherals and EDR interaction"
  - item: "Windows Defender installed and configured"
    why: "EDR under test per D-04; allowlist procedures from docs/operations/deployment-guide.md must be applied"
  - item: "Real signed-in cloud clients (OneDrive, Google Drive, Dropbox, Box)"
    why: "Scenarios C01-C04 require real cloud sync clients per D-05"
  - item: "Physical peripherals: USB 3.0 flash drive, SD card + reader, optical drive (or USB optical), virtual drive capability, network printer (or PDF printer), SMB network share"
    why: "Categories D and E require real peripherals per D-08 and D-10 N/A protocol"
  - item: "Microsoft Word installed"
    why: "CRIT-04 Word launch benchmark (I02) requires Office COM automation"
  - item: "Rust toolchain installed on test host"
    why: "CRIT-04 cargo build benchmark (I01) requires cargo"
  - item: "Windows SDK installed (signtool.exe in PATH)"
    why: "Scenario J01 requires Authenticode verification via signtool"
  - item: "dlp-agent v0.10.0 installed and service registered"
    why: "All scenarios exercise the installed agent per D-17"
  - item: "Process Hacker or Process Explorer installed"
    why: "Scenarios A01-A04 and G01 require module inspection and DLL unloading"

must_haves:
  truths:
    - "A dedicated physical Windows 11 host is provisioned and accessible"
    - "All prerequisite checks (Windows 11, admin, dlp-agent, signtool, EDR allowlist) pass"
    - "Test data directory C:\UAT\v0.10.0\TestData\ contains T1, T2, and T4 files"
    - "Peripheral availability is documented with N/A justifications for any missing items"
    - "Cloud clients are signed in or marked N/A with justification"
    - "Protected path C:\Protected\Test\ is registered with T4 classification via admin TUI/API"
    - "Host has been rebooted after agent installation per deployment guide Section 4.7"
    - "RELEASE_NOTES.md v0.10.0 placeholders are populated with real SHA-256, SHA-512, build provenance, and Authenticode certificate thumbprint"
    - "print_enabled is verified true in the active agent config before E01-E05"
    - "A T4 Word template exists at C:\Protected\Test\t4_template.docx for the mandatory COM automation benchmark"
    - "The COM automation benchmark checklist is initialed by the tester, confirming canonical I02 evidence will be produced"
  artifacts:
    - path: "C:\UAT\v0.10.0\environment.json"
      provides: "Host environment snapshot (OS, CPU, RAM, EDR, cloud client versions)"
      min_lines: 15
    - path: "C:\UAT\v0.10.0\TestData\"
      provides: "UAT test files (T1, T2, T4 classifications)"
      contains: "t1_test.txt, t2_test.txt, t4_test.txt"
    - path: "C:\UAT\v0.10.0\peripheral-checklist.md"
      provides: "Peripheral availability matrix with N/A justifications"
      contains: "USB 3.0 flash drive | SD card + reader | Optical drive | Virtual drive | Network printer | SMB share"
    - path: "C:\UAT\v0.10.0\print-config-check.md"
      provides: "print_enabled verification result"
      contains: "print_enabled = true"
    - path: "C:\UAT\v0.10.0\cloud-client-check.md"
      provides: "Cloud client sign-in and sync-folder verification results"
      contains: "OneDrive | Google Drive | Dropbox | Box"
    - path: "RELEASE_NOTES.md"
      provides: "Populated v0.10.0 hash and provenance data"
      contains: "No '[TO BE FILLED AT RELEASE]' placeholders in the v0.10.0 section"
    - path: "C:\Protected\Test\t4_template.docx"
      provides: "T4 Word template for mandatory COM automation benchmark"
      contains: "Word document with T4 classification"
    - path: "C:\UAT\v0.10.0\crit04_word_com_fallback.json"
      provides: "Canonical I02 evidence (will be produced in Plan 02, but pre-positioning verified in Plan 01)"
      contains: "baseline_times, hooked_times, baseline_median, hooked_median, overhead_percent, passed, target_percent"
  key_links:
    - from: "Host preparation"
      to: "UAT script execution"
      via: "Prerequisites verified and documented before script runs"
      pattern: "Test-Admin, Test-Windows11, Test-AgentService, Test-Signtool all return true"
    - from: "Protected path registration"
      to: "B/F/H scenario execution"
      via: "C:\Protected\Test\ registered with T4 classification enables file-blocking and DACL-tripwire scenarios"
      pattern: "Admin TUI/API shows C:\Protected\Test\ with T4 tier; icacls shows Deny ACE"
    - from: "RELEASE_NOTES.md"
      to: "J01/J05 evidence"
      via: "Populated hashes and certificate thumbprint enable Authenticode verification and hash comparison"
      pattern: "SHA-256 and SHA-512 values present; certificate thumbprint present"
    - from: "print_enabled check"
      to: "E01-E05 scenario execution"
      via: "print_enabled = true ensures print interception is active for print-blocking scenarios"
      pattern: "Agent config shows print_enabled = true"
    - from: "T4 Word template pre-positioning"
      to: "I02 canonical evidence"
      via: "C:\Protected\Test\t4_template.docx exists and is classified T4 before COM automation benchmark runs"
      pattern: "Test-Path 'C:\Protected\Test\t4_template.docx' returns True"
---

<objective>
Prepare the physical Windows 11 test host, verify all prerequisites, populate RELEASE_NOTES.md v0.10.0 placeholders, verify print_enabled is true, verify cloud client sign-in states, register protected paths with T4 classification, pre-position a T4 Word template for the mandatory COM automation benchmark, reboot per deployment guide, and stage test data and peripherals so the UAT execution script can run without interruption.

Purpose: A failed prerequisite discovered mid-UAT wastes the test session and invalidates results. Strict pre-checks — including populated release artifacts, enabled print enforcement, and verified cloud clients — ensure the host is ready before any scenarios execute.
Output: C:\UAT\v0.10.0\ environment with verified prerequisites, populated RELEASE_NOTES.md, confirmed print_enabled, verified cloud clients, registered protected path, pre-positioned T4 Word template, test data, and documented peripheral availability.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.3-close-gap-ops-04-execute-physical-windows-11-uat/58.3-CONTEXT.md
@.planning/milestones/v0.10.0-UAT.md
@scripts/Uat-Execute-v0.10.0.ps1
@docs/operations/deployment-guide.md
@RELEASE_NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate RELEASE_NOTES.md v0.10.0 placeholders and verify no placeholders remain</name>
  <read_first>
    - RELEASE_NOTES.md (v0.10.0 section, lines 170-209)
    - scripts/Uat-Execute-v0.10.0.ps1 (lines 1-50 for binary list)
    - docs/operations/deployment-guide.md (Section 2 Step 2 Authenticode verification)
  </read_first>
  <acceptance_criteria>
    - RELEASE_NOTES.md v0.10.0 Artifact Provenance table contains real Build ID, Commit SHA, Pipeline, Built By, and Build Date
    - RELEASE_NOTES.md v0.10.0 Signing Certificate table contains real Thumbprint, Issuer, Subject, Valid From, and Valid To
    - RELEASE_NOTES.md v0.10.0 Binaries table contains real SHA-256 and SHA-512 for all 6 binaries
    - (Select-String -Path 'RELEASE_NOTES.md' -Pattern '\[TO BE FILLED AT RELEASE\]' -SimpleMatch).Count returns 0
    - RELEASE_NOTES.md is committed to git with message "docs(release): populate v0.10.0 hashes and provenance"
  </acceptance_criteria>
  <files>RELEASE_NOTES.md</files>
  <action>
    Populate the v0.10.0 section of RELEASE_NOTES.md with real artifact data before UAT execution begins. J01 (Authenticode verification) and J05 (hash verification) require real values; placeholder text blocks evidence generation.

    1. Build the 6 release binaries (dlp-agent.exe, dlp-server.exe, dlp-admin-cli.exe, dlp-user-ui.exe, dlp_hook_dll.dll, dlp_hook_dll_x86.dll) using the project's release build pipeline or local cargo build --release.
    2. Generate SHA-256 and SHA-512 for each binary using the Hash Generation script in RELEASE_NOTES.md lines 20-40. Record the outputs.
    3. Sign all binaries with the project's Authenticode certificate. Record the certificate thumbprint and issuer.
    4. Record build provenance: Build ID (pipeline run ID), Commit SHA (git rev-parse HEAD), Pipeline name, Built By (builder identity), and Build Date.
    5. Edit RELEASE_NOTES.md and replace every `[TO BE FILLED AT RELEASE]` in the v0.10.0 section with the real values from steps 2-4. Do NOT modify any other release section.
    6. Run the Hash Verification script from RELEASE_NOTES.md lines 46-96 to confirm locally computed hashes match the published values in the document. The script must output `=== ALL HASHES VERIFIED ===`.
    7. Verify no placeholders remain: run PowerShell `(Select-String -Path 'RELEASE_NOTES.md' -Pattern '\[TO BE FILLED AT RELEASE\]' -SimpleMatch).Count` and confirm it returns 0.
    8. Commit the updated RELEASE_NOTES.md: `git add RELEASE_NOTES.md && git commit -m "docs(release): populate v0.10.0 hashes and provenance"`.
    9. Push the commit: `git push`.

    If any binary fails hash verification or any placeholder cannot be filled, abort UAT preparation and escalate to release engineering. Do NOT proceed with UAT until RELEASE_NOTES.md is fully populated.
  </action>
  <verify>
    <automated>powershell -Command "(Select-String -Path 'RELEASE_NOTES.md' -Pattern '\[TO BE FILLED AT RELEASE\]' -SimpleMatch).Count" returns 0</automated>
  </verify>
  <done>
    RELEASE_NOTES.md v0.10.0 section fully populated with real SHA-256, SHA-512, build provenance, and Authenticode certificate details. No placeholders remain. Document committed and pushed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify print_enabled, cloud client sign-in, and register protected path with test data</name>
  <read_first>
    - scripts/Uat-Execute-v0.10.0.ps1 (lines 1-50 for directory structure and constants)
    - docs/operations/deployment-guide.md (Section 4.4 DACL Tripwire as Backstop)
    - .planning/milestones/v0.10.0-UAT.md (Peripheral Availability table and N/A Protocol)
    - .planning/phases/58.3-close-gap-ops-04-execute-physical-windows-11-uat/58.3-CONTEXT.md (D-05, D-07, D-10, D-11)
  </read_first>
  <acceptance_criteria>
    - `C:\UAT\v0.10.0\TestData\t1_test.txt` exists
    - `C:\UAT\v0.10.0\TestData\t2_test.txt` exists
    - `C:\UAT\v0.10.0\TestData\t4_test.txt` exists
    - `C:\Protected\Test\t4_doc.txt` exists
    - `icacls "C:\Protected\Test\"` output contains a Deny ACE
    - `C:\UAT\v0.10.0\peripheral-checklist.md` exists and documents all 6 peripherals
    - `C:\UAT\v0.10.0\peripheral-checklist.md` does NOT contain any unmarked absences
    - `C:\UAT\v0.10.0\print-config-check.md` documents print_enabled = true (or escalation if false)
    - `C:\UAT\v0.10.0\cloud-client-check.md` documents each cloud client's sign-in state and sync folder path
    - `C:\Protected\Test\t4_template.docx` exists and is classified T4
  </acceptance_criteria>
  <files>C:\UAT\v0.10.0\TestData\, C:\UAT\v0.10.0\peripheral-checklist.md, C:\UAT\v0.10.0\print-config-check.md, C:\UAT\v0.10.0\cloud-client-check.md, C:\Protected\Test\t4_template.docx</files>
  <action>
    Prepare dedicated UAT test data, verify print enforcement is enabled, verify cloud client sign-in states with automated checks, register the protected path with T4 classification, and pre-position a T4 Word template for the mandatory COM automation benchmark per D-07, D-05, and D-10.

    1. Create test data directory: `C:\UAT\v0.10.0\TestData\`
    2. Create T1 test file: `Set-Content -Path 'C:\UAT\v0.10.0\TestData\t1_test.txt' -Value 'T1 public test data'`
    3. Create T2 test file: `Set-Content -Path 'C:\UAT\v0.10.0\TestData\t2_test.txt' -Value 'T2 internal test data'`
    4. Create T4 test file: `Set-Content -Path 'C:\UAT\v0.10.0\TestData\t4_test.txt' -Value 'T4 restricted test data'`
    5. Create protected path test directory: `New-Item -ItemType Directory -Path 'C:\Protected\Test' -Force`
    6. Copy T4 test file to protected path: `Copy-Item 'C:\UAT\v0.10.0\TestData\t4_test.txt' 'C:\Protected\Test\t4_doc.txt'`
    7. Register `C:\Protected\Test\` as a protected path with T4 classification via the admin TUI (`dlp-admin-cli.exe`) or admin API. Navigate to the Protected Paths screen, add the path, set classification to T4, and confirm. Wait for one policy sync cycle (60 seconds) or restart the dlp-agent service to force sync.
    8. Verify the protected path is active: Run `icacls "C:\Protected\Test\"` and confirm a Deny ACE exists for Authenticated Users (or the DLP service account). If no Deny ACE appears, wait another 60 seconds and recheck. Document the icacls output.
    9. Verify print_enabled in the active agent config:
       - Locate the agent config file (typically `C:\ProgramData\DLP\config\agent.json` or equivalent).
       - Read the config and check the `print_enabled` field. If `print_enabled` is not present or is `false`, set it to `true` and restart the dlp-agent service: `Restart-Service dlp-agent; Start-Sleep -Seconds 30`.
       - Document the result in `C:\UAT\v0.10.0\print-config-check.md`. If print_enabled could not be set to true, escalate to engineering before running E01-E05.
    10. Verify cloud client sign-in with automated checks (per D-05):
        - OneDrive: Run `Get-Process -Name OneDrive -ErrorAction SilentlyContinue` to confirm the process is running. Check `Test-Path (Join-Path $env:OneDrive 'Test')` or the user's OneDrive folder for a sync path. Document sign-in state and sync folder path.
        - Google Drive: Run `Get-Process -Name GoogleDriveFS -ErrorAction SilentlyContinue` to confirm the process is running. Check `Test-Path (Join-Path $env:USERPROFILE 'Google Drive')` or `Test-Path (Join-Path $env:USERPROFILE 'My Drive')` for the sync folder. Document sign-in state and sync folder path.
        - Dropbox: Run `Get-Process -Name Dropbox -ErrorAction SilentlyContinue` to confirm the process is running. Check `Test-Path (Join-Path $env:USERPROFILE 'Dropbox')` for the sync folder. Document sign-in state and sync folder path.
        - Box Drive: Run `Get-Process -Name BoxDrive -ErrorAction SilentlyContinue` to confirm the process is running. Check `Test-Path (Join-Path $env:USERPROFILE 'Box')` for the sync folder. Document sign-in state and sync folder path.
        - For each client, if the process is not running or the sync folder does not exist, mark it as not signed in and document the reason in `C:\UAT\v0.10.0\cloud-client-check.md`.
    11. Verify peripheral availability:
        - USB 3.0 flash drive: Insert and confirm Windows Explorer shows the drive.
        - SD card + reader: Insert and confirm Windows Explorer shows the drive.
        - Optical drive: Check Device Manager for DVD/CD-ROM drives; insert a disc if available.
        - Virtual drive capability: Confirm `Mount-DiskImage` cmdlet is available (built into Windows 11).
        - Network printer: Run `Get-Printer` and confirm at least one printer is listed.
        - SMB share: Confirm `\\server\share` is accessible or mark N/A.
    12. Create `C:\UAT\v0.10.0\peripheral-checklist.md` documenting each peripheral as Present/Absent with justification for any absences.
    13. Pre-position the T4 Word template for the mandatory COM automation benchmark:
        - Open Microsoft Word.
        - Create a new blank document.
        - Save it to `C:\Protected\Test\t4_template.docx`.
        - Ensure the file is classified T4 (the protected path registration should automatically classify new files; if not, use the admin TUI to set the classification).
        - Verify `Test-Path 'C:\Protected\Test\t4_template.docx'` returns True.
    14. Verify the script file exists and is readable: `Test-Path scripts\Uat-Execute-v0.10.0.ps1` returns True, and `Get-Command scripts\Uat-Execute-v0.10.0.ps1` resolves. Optionally run a PowerShell syntax check: `Get-Command scripts\Uat-Execute-v0.10.0.ps1` confirms the script is syntactically valid (no parse errors). Do NOT run `-WhatIf`; per D-02, the script is executed in full interactive mode in Plan 02.

    If a cloud client is not signed in, mark it N/A in the checklist with the reason. If a peripheral is absent, mark it N/A with justification per D-10. If the protected path registration fails, this is a critical setup failure — abort and escalate. If print_enabled cannot be verified as true, escalate before E01-E05.
  </action>
  <verify>
    <automated>Test-Path 'C:\UAT\v0.10.0\TestData\t4_test.txt' returns True; Test-Path 'C:\UAT\v0.10.0\peripheral-checklist.md' returns True; Test-Path 'C:\UAT\v0.10.0\print-config-check.md' returns True; Test-Path 'C:\UAT\v0.10.0\cloud-client-check.md' returns True; Test-Path 'C:\Protected\Test\t4_template.docx' returns True</automated>
  </verify>
  <done>
    Test data directory populated with T1/T2/T4 files; protected path `C:\Protected\Test\` created, registered with T4 classification, and verified with Deny ACE via icacls; print_enabled verified true and documented; cloud client sign-in states verified and documented; peripheral checklist completed; T4 Word template pre-positioned for mandatory COM automation benchmark; script file verified present and readable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reboot host and verify post-reboot state</name>
  <read_first>
    - docs/operations/deployment-guide.md (Section 4.7 Post-Install Reboot Requirement)
  </read_first>
  <acceptance_criteria>
    - `(Get-Service dlp-agent).Status` returns `Running`
    - `Get-Process | Where-Object {$_.Modules -match "dlp_hook_dll"} | Select-Object Name, Id -First 5` returns at least one match
    - `icacls "C:\Protected\Test\"` still shows a Deny ACE
    - `Get-MpPreference | Select-Object -ExpandProperty ExclusionPath` contains `C:\Program Files\DLP\`
    - `C:\UAT\v0.10.0\post-reboot-check.md` exists and documents all post-reboot checks
    - `Test-Path 'C:\Protected\Test\t4_template.docx'` returns True after reboot
  </acceptance_criteria>
  <files>docs/operations/deployment-guide.md</files>
  <action>
    Reboot the test host per docs/operations/deployment-guide.md Section 4.7 (reboot is REQUIRED for first-install AppInit_DLLs hook activation and RECOMMENDED for clean ETW provider registration). After reboot, verify the agent state and that the T4 Word template persists:

    1. Reboot the host: `Restart-Computer -Force`
    2. After reboot, log in and wait 60 seconds for agent service auto-start.
    3. Verify dlp-agent service is running: `Get-Service dlp-agent` should show `Status = Running`.
    4. Verify hook injection is active: `Get-Process | Where-Object {$_.Modules -match "dlp_hook_dll"} | Select-Object Name, Id -First 5` should return at least one match.
    5. Verify the protected path Deny ACE survived reboot: `icacls "C:\Protected\Test\"` should still show the Deny ACE.
    6. Verify EDR allowlist survived reboot: `Get-MpPreference | Select-Object -ExpandProperty ExclusionPath` should still list `C:\Program Files\DLP\`.
    7. Verify the T4 Word template survived reboot: `Test-Path 'C:\Protected\Test\t4_template.docx'` should return True.
    8. Record post-reboot verification results in `C:\UAT\v0.10.0\post-reboot-check.md`.

    If any post-reboot check fails, diagnose and fix before proceeding. A missing Deny ACE after reboot indicates the DACL tripwire is not persisting — escalate to engineering.
  </action>
  <verify>
    <automated>powershell -Command "(Get-Service dlp-agent).Status -eq 'Running'" returns True</automated>
  </verify>
  <done>
    Host rebooted; dlp-agent service auto-started and running; hook injection confirmed in at least one process; protected path Deny ACE persists; EDR allowlist persists; T4 Word template persists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify host readiness before UAT execution</name>
  <files>C:\UAT\v0.10.0\environment.json, C:\UAT\v0.10.0\peripheral-checklist.md, C:\UAT\v0.10.0\post-reboot-check.md, C:\UAT\v0.10.0\print-config-check.md, C:\UAT\v0.10.0\cloud-client-check.md, RELEASE_NOTES.md, C:\Protected\Test\t4_template.docx</files>
  <action>Human operator reviews the prepared host state and confirms readiness before UAT execution begins.</action>
  <verify>
    <automated>Human confirms all critical prerequisites pass, protected path is registered with T4, print_enabled is true, cloud clients are verified, RELEASE_NOTES.md has no placeholders, and T4 Word template is pre-positioned.</automated>
  </verify>
  <done>Operator approves host readiness; no blocking gaps remain.</done>
  <what-built>Physical Windows 11 test host prepared with verified prerequisites, populated RELEASE_NOTES.md, confirmed print_enabled, verified cloud clients, registered protected path with T4 classification, rebooted, test data staged, T4 Word template pre-positioned, and documented peripheral/cloud client availability.</what-built>
  <how-to-verify>
    1. Review `C:\UAT\v0.10.0\environment.json` (or the console output from Task 1) and confirm all critical prerequisites show PASS.
    2. Review `C:\UAT\v0.10.0\peripheral-checklist.md` and confirm all required peripherals are either Present or have a valid N/A justification.
    3. Review `C:\UAT\v0.10.0\print-config-check.md` and confirm `print_enabled = true` is documented.
    4. Review `C:\UAT\v0.10.0\cloud-client-check.md` and confirm each cloud client is either signed in with a verified sync folder or marked N/A with justification.
    5. Verify the script file was validated as present and readable in Task 2 (not executed via -WhatIf per D-02).
    6. Confirm the test host is the dedicated physical machine (not the developer's primary workstation) per D-03.
    7. Verify `icacls "C:\Protected\Test\"` shows a Deny ACE confirming the protected path is registered with T4 classification.
    8. Verify `C:\UAT\v0.10.0\post-reboot-check.md` shows dlp-agent running and hook injection active after reboot.
    9. Verify `RELEASE_NOTES.md` v0.10.0 section contains real SHA-256, SHA-512, build provenance, and certificate thumbprint — no `[TO BE FILLED AT RELEASE]` placeholders remain.
    10. Verify `C:\Protected\Test\t4_template.docx` exists and is a valid Word document classified T4.
    11. Confirm the tester initials the COM automation benchmark checklist, acknowledging that the manual Word COM automation benchmark is mandatory for canonical I02 evidence.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to UAT execution, or describe any prerequisite gaps that must be resolved first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test host -> Repository | UAT artifacts generated on test host are later copied into the repo milestone doc |
| Human tester -> Script | Tester inputs pass/fail judgments into the execution script |
| Release engineer -> RELEASE_NOTES.md | Populated hashes and certificate data must match actual signed binaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.3-01 | Tampering | UAT script on test host | mitigate | Use committed script exactly as-is per D-01; verify SHA-256 of script matches git before execution |
| T-58.3-02 | Repudiation | Tester results input | mitigate | Script timestamps every result; tester name recorded in environment.json |
| T-58.3-03 | Information Disclosure | Agent logs in artifacts | accept | Agent logs contain file paths but no passwords or tokens; acceptable for internal UAT |
| T-58.3-04 | Denial of Service | EDR quarantine during UAT | mitigate | Apply EDR allowlist per deployment-guide.md before starting; monitor EDR console during execution |
| T-58.3-05 | Tampering | RELEASE_NOTES.md placeholders not filled | mitigate | Add explicit task to populate placeholders; verify with Select-String that no placeholders remain |
| T-58.3-06 | Denial of Service | print_enabled defaults false, causing E01-E05 to spuriously fail | mitigate | Add pre-check task to verify print_enabled = true; escalate if false before UAT |
| T-58.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase; N/A |
</threat_model>

<verification>
- `C:\UAT\v0.10.0\` directory tree exists with A-J subdirectories.
- `C:\UAT\v0.10.0\TestData\` contains t1_test.txt, t2_test.txt, t4_test.txt.
- `C:\UAT\v0.10.0\peripheral-checklist.md` documents all 6 peripherals with Present/Absent status.
- `C:\UAT\v0.10.0\print-config-check.md` documents print_enabled = true.
- `C:\UAT\v0.10.0\cloud-client-check.md` documents each cloud client's sign-in state and sync folder.
- Script file is verified present and readable (not executed via -WhatIf per D-02).
- All critical prerequisites (Windows 11, admin, dlp-agent, signtool, EDR allowlist) pass.
- `C:\Protected\Test\` is registered as a protected path with T4 classification and shows a Deny ACE via icacls.
- Host has been rebooted and post-reboot checks confirm dlp-agent running, hook injection active, and Deny ACE persisting.
- RELEASE_NOTES.md v0.10.0 section contains real SHA-256, SHA-512, build provenance, and certificate thumbprint; no placeholders remain.
- `C:\Protected\Test\t4_template.docx` exists and is classified T4 for the mandatory COM automation benchmark.
</verification>

<success_criteria>
- Physical Windows 11 host is dedicated and accessible.
- All critical prerequisites verified and documented.
- Test data staged in C:\UAT\v0.10.0\TestData\.
- Peripheral availability documented with N/A justifications for any gaps.
- Cloud client sign-in status verified with automated checks and documented.
- UAT script is verified present and readable (not executed via -WhatIf per D-02).
- Protected path C:\Protected\Test\ registered with T4 classification and verified via icacls.
- Host rebooted per deployment guide; post-reboot state verified.
- RELEASE_NOTES.md v0.10.0 placeholders fully populated and committed.
- print_enabled verified true in active agent config.
- T4 Word template pre-positioned at C:\Protected\Test\t4_template.docx for mandatory COM automation benchmark.
- Tester initials COM automation benchmark checklist, confirming canonical I02 evidence will be produced.
</success_criteria>

<output>
Create `.planning/phases/58.3-close-gap-ops-04-execute-physical-windows-11-uat/58.3-01-SUMMARY.md` when done.
</output>
