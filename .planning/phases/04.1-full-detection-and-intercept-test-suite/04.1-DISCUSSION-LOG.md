# Phase 04.1: Full Detection and Intercept Test Suite — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-11
**Phase:** 04.1-full-detection-and-intercept-test-suite
**Areas discussed:** Scope, Priority, Test location, Windows API strategy

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full pipeline integration tests | End-to-end only | |
| Per-module unit tests with mocks | Unit tests only | |
| Server side evaluation tests | Server only | |
| Everything | Per-module + server + E2E | ✓ |

**User's choice:** Everything — "full and comprehensive test for all detection and intercept with resource access"

---

## Test Waves Priority

| Option | Description | Selected |
|--------|-------------|----------|
| All 3 waves | Detection unit + server eval + E2E pipeline | ✓ |
| Detection modules only | USB, network, clipboard | |
| Server evaluation only | Policy CRUD, audit ingest | |
| E2E pipeline only | File action → classify → eval → audit | |

**User's choice:** All 3 waves

---

## Test Location

| Option | Description | Selected |
|--------|-------------|----------|
| Add to existing test files | comprehensive.rs (detection), integration.rs (e2e), server tests (new/existing) | ✓ |
| Separate new test files | New top-level test files per wave | |

**User's choice:** Add to existing test files — extends current structure

---

## Windows API Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Mock Windows APIs | Stub GetDriveTypeW, WNetEnumResourceW | |
| Real Windows APIs (windows-only) | Real calls inside `#[cfg(windows)]` | ✓ |

**User's choice:** Real Windows APIs with `#[cfg(windows)]` guards — DLP-Rust is Windows-only

---

## Deferred Ideas

- Load/stress testing — future phase
- Multi-agent scenarios — future phase
- AD integration tests — Phase 7
