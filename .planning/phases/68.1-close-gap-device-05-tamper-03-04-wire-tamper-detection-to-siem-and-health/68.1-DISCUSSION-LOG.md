# Phase 68.1: Close gap: DEVICE-05/TAMPER-03/04 — wire tamper detection to SIEM and health - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-13
**Phase:** 68.1-close-gap-device-05-tamper-03-04-wire-tamper-detection-to-siem-and-health
**Areas discussed:** Tamper notification channel, Synthetic event SIEM/syslog routing, Live device health in ABAC, Admin TUI integrity screen

---

## Tamper notification channel (server → agent)

| Option | Description | Selected |
|--------|-------------|----------|
| Synchronous ingest response | Extend `POST /audit/events` response with `tamper_detected_for_agent`; agent calls `report_tamper_detected()` on match. | ✓ |
| Server-to-agent push channel | Add WebSocket/long-poll alert channel for real-time tamper notification. | |
| Agent-side verification | Have the agent verify its own chain and self-report tamper; server remains secondary. | |
| Agent polls integrity endpoint | Agent periodically calls `GET /admin/audit/integrity` for its own agent_id. | |

**User's choice:** Synchronous ingest response (recommended default)
**Notes:** Reuses existing request/response path, avoids new push infrastructure, and is sufficient for the gap-close scope. Agent only reacts to its own chain breaks.

---

## Synthetic ChainBreakDetected routing

| Option | Description | Selected |
|--------|-------------|----------|
| Append to relay/syslog event lists | Add synthetic events to the collections passed to `SyslogQueueRepository` and `SiemConnector::relay_events` before spawning background tasks. | ✓ |
| Separate synthetic-event relay path | Create a dedicated relay/spawn for synthetic events only. | |
| Rely on alert_router only | Keep current behavior (excludes SIEM/syslog). | |
| Persist then re-query for relay | After persistence, query audit_events for synthetic rows and relay them. | |

**User's choice:** Append to relay/syslog event lists (recommended default)
**Notes:** Minimal change, preserves deduplication, and leverages existing `routed_to_siem()` logic. Synthetic events are added before the clone into syslog/SIEM tasks.

---

## Live device health in ABAC EvaluateRequest

| Option | Description | Selected |
|--------|-------------|----------|
| Use `current_health()` everywhere | Replace hardcoded `DeviceHealthStatus::default()` with `device_identity::current_health()` in `identity.rs` and `interception/mod.rs`. | ✓ |
| Use heartbeat-cached health only | Use the last heartbeat value, which may lag behind local state transitions. | |
| Keep hardcoded Healthy | Leave behavior as-is and document as known limitation. | |
| Add health snapshot to hook request | Pass health through hook DLL IPC so it is evaluated at decision time in the DLL. | |

**User's choice:** Use `current_health()` everywhere (recommended default)
**Notes:** `current_health()` is the same source used by heartbeats and endpoint identity, so all contexts stay consistent. It is a non-blocking `AtomicU8` read.

---

## Admin TUI Audit Integrity screen

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal list + detail screen | Follow `BypassAlertsList` pattern: per-agent summary table with Enter-key detail popup. | ✓ |
| Rich dashboard with charts | Add visual status indicators and historical trend charts. | |
| CLI-only integrity command | Add a shell command instead of a TUI screen. | |
| Defer TUI screen to Phase 67+ | Only wire SIEM/health in this phase; add TUI screen later. | |

**User's choice:** Minimal list + detail screen (recommended default)
**Notes:** Keeps the phase focused on closing the documented TAMPER-04 gap. Mirrors existing screen patterns for consistency and testability.

---

## Claude's Discretion

- Tamper notification is pull-based via the ingest response to avoid scope expansion.
- Synthetic events are appended to relay lists after persistence so relay failures do not affect audit-log integrity.
- The deprecated `WindowsIdentity::to_subject()` is updated alongside `to_subject_with_ad()` because it remains compiled and referenced.
- Integrity screen filter cycling starts with "All agents" and does not add free-text search in this gap-close phase.

## Deferred Ideas

- Real-time server-to-agent tamper push channel.
- Agent-side independent chain verification.
- Merkle-tree or signature-based audit integrity.
- Bulk integrity export / scheduled report jobs.
- Rich dashboard charts for integrity trends.

---

*Auto-generated in --auto mode. All gray areas were auto-selected and resolved using recommended defaults.*
