---
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
plan: 04
type: execute
wave: 3
depends_on:
  - 58.5-02
  - 58.5-03
files_modified:
  - dlp-hook-dll/src/lib.rs
  - dlp-hook-dll/src/ntdll_patcher.rs
  - dlp-hook-dll/src/hook_journal.rs
  - dlp-hook-dll/src/classification_cache.rs
  - dlp-hook-dll/src/pipe_client.rs
  - dlp-agent/src/service.rs
  - dlp-agent/src/process_registry.rs
  - dlp-agent/src/hook_ipc.rs
  - dlp-common/src/hook_ipc.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo test -p dlp-hook-dll passes with new unhook tests"
    - "cargo test -p dlp-agent passes with new dispatch and registry tests"
    - "cargo test -p dlp-common passes with new IPC and audit round-trip tests"
    - "cargo clippy --all-targets -- -D warnings is clean"
    - "cargo fmt --check is clean"
    - "Integration test verifies end-to-end: agent shutdown -> UnhookCommand -> UnhookAck -> registry Exited"
  artifacts:
    - path: "dlp-hook-dll/src/lib.rs"
      provides: "Unit tests for UnhookAll, watchdog, and UnhookCommand handler"
      exports: ["#[cfg(test)] mod tests"]
    - path: "dlp-agent/src/service.rs"
      provides: "Unit tests for unhook_all_injected_processes dispatch logic"
      exports: ["#[cfg(test)] mod tests"]
    - path: "dlp-agent/src/hook_ipc.rs"
      provides: "Unit tests for UnhookAck routing and send_unhook_command"
      exports: ["#[cfg(test)] mod tests"]
  key_links:
    - from: "dlp-hook-dll tests"
      to: "dlp-common/src/hook_ipc.rs"
      via: "bincode round-trip of UnhookCommand/UnhookAck in tests"
      pattern: "IpcPayloadV1::UnhookCommand"
    - from: "dlp-agent tests"
      to: "dlp-common/src/audit.rs"
      via: "AuditEvent with AgentShutdownUnhook / UnhookFailure / WatchdogSelfUnload"
      pattern: "EventType::AgentShutdownUnhook"
---

<objective>
Write comprehensive unit and integration tests for the entire Phase 58.5 unhook system: hook DLL self-unhook (UnhookAll, watchdog, UnhookCommand handler), agent dispatch (unhook_all_injected_processes, registry transitions), and common types (bincode round-trip, audit event routing). Run full quality gates and fix any issues.

Purpose: Verify that the cooperative unhook protocol works end-to-end, that all edge cases are covered, and that the codebase meets the project's quality standards (tests pass, clippy clean, fmt clean).
Output: Passing tests across all three crates, clean clippy, and a verification report.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@dlp-hook-dll/src/lib.rs
@dlp-hook-dll/src/ntdll_patcher.rs
@dlp-hook-dll/src/hook_journal.rs
@dlp-hook-dll/src/classification_cache.rs
@dlp-hook-dll/src/pipe_client.rs
@dlp-agent/src/service.rs
@dlp-agent/src/process_registry.rs
@dlp-agent/src/hook_ipc.rs
@dlp-agent/src/audit_emitter.rs
@dlp-common/src/hook_ipc.rs
@dlp-common/src/audit.rs
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-01-PLAN.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-02-PLAN.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for dlp-hook-dll self-unhook</name>
  <files>dlp-hook-dll/src/lib.rs, dlp-hook-dll/src/ntdll_patcher.rs, dlp-hook-dll/src/hook_journal.rs, dlp-hook-dll/src/classification_cache.rs, dlp-hook-dll/src/pipe_client.rs</files>
  <behavior>
    - Test 1: UnhookAll sets SHUTTING_DOWN flag, calls unpatch_all_stubs, and unmaps shared memory
    - Test 2: handle_unhook_command sends UnhookAck with success=true when UnhookAll succeeds
    - Test 3: handle_unhook_command sends UnhookAck with success=false when UnhookAll panics (simulated)
    - Test 4: Watchdog thread triggers after 3 consecutive pipe failures (mocked is_pipe_reachable)
    - Test 5: Watchdog resets failure count on successful pipe connection
    - Test 6: self_unload resolves the current module handle (mock path, do not actually call FreeLibraryAndExitThread in tests)
  </behavior>
  <action>
    In `dlp-hook-dll/src/lib.rs`, add or extend the `#[cfg(test)]` module with tests:
    - `unhook_all_sets_shutting_down` — call UnhookAll, assert SHUTTING_DOWN is true after
    - `unhook_all_unpatches_ntdll_stubs` — initialize NTDLL_PATCHER with a test-disabled patcher, manually set stub states to Patched, call UnhookAll, assert Unpatched
    - `unhook_all_unmaps_shared_memory` — set up mock journal/cache mappings (use test-only globals or mock functions), call UnhookAll, assert handles are cleared
    - `handle_unhook_command_sends_success_ack` — mock pipe_client::send_unhook_ack via a test-only static channel, call handle_unhook_command, assert ack.success == true
    - `handle_unhook_command_sends_failure_ack` — temporarily corrupt a hook to make UnhookAll panic, assert ack.success == false
    - `watchdog_triggers_after_three_failures` — mock is_pipe_reachable to return false 3 times then true, assert a test-only WATCHDOG_TRIGGERED flag is set
    - `watchdog_resets_on_success` — mock is_pipe_reachable to return false twice then true, assert WATCHDOG_TRIGGERED is not set

    For mocking `pipe_client::send_unhook_ack` and `is_pipe_reachable`, use `#[cfg(test)]` statics:
    ```rust
    #[cfg(test)]
    static MOCK_PIPE_ACK: std::sync::Mutex<Vec<dlp_common::hook_ipc::UnhookAck>> = std::sync::Mutex::new(Vec::new());

    #[cfg(test)]
    static MOCK_PIPE_REACHABLE: std::sync::Mutex<bool> = std::sync::Mutex::new(true);
    ```
    Conditionally replace the real functions in test builds:
    ```rust
    #[cfg(not(test))]
    pub fn send_unhook_ack(...) { ... }
    #[cfg(test)]
    pub fn send_unhook_ack(_pipe_name: &str, ack: &dlp_common::hook_ipc::UnhookAck) -> Result<(), PipeError> {
        MOCK_PIPE_ACK.lock().unwrap().push(ack.clone());
        Ok(())
    }
    ```
    Similarly for `is_pipe_reachable`.

    For `self_unload`, test that `GetModuleHandleW` resolves the module name in the test binary. Since the test binary is not `dlp_hook_dll.dll`, the test should verify the function logic without calling `FreeLibraryAndExitThread`. Use a test-only `self_unload_check` that returns the module handle without exiting.

    In `dlp-hook-dll/src/ntdll_patcher.rs`, ensure `unpatch_all_stubs` is `pub` and add a test:
    - `test_unpatch_all_stubs_transitions_patched_to_unpatched` — create a test patcher, manually set states to Patched, call unpatch_all_stubs, assert Unpatched

    In `dlp-hook-dll/src/hook_journal.rs` and `classification_cache.rs`, add tests:
    - `test_unmap_journal_clears_globals` — set a test mapping handle, call unmap_journal, assert None
    - `test_unmap_cache_clears_globals` — same pattern
    Note: These may need `#[cfg(test)]` test-only setters for the globals.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll -- --nocapture</automated>
  </verify>
  <done>All dlp-hook-dll unit tests pass, including new unhook, watchdog, and handler tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for dlp-agent unhook dispatch and registry</name>
  <files>dlp-agent/src/service.rs, dlp-agent/src/process_registry.rs, dlp-agent/src/hook_ipc.rs, dlp-agent/src/audit_emitter.rs</files>
  <behavior>
    - Test 1: unhook_all_injected_processes with empty registry is a no-op
    - Test 2: unhook_all_injected_processes with one Injected entry sends UnhookCommand and transitions to Exited on ack
    - Test 3: record_unhooked transitions Injected to Exited
    - Test 4: record_unhooked_by_pid finds the correct Injected entry by PID
    - Test 5: UnhookAck routing in hook_ipc.rs calls record_unhooked_by_pid
    - Test 6: emit_unhook_audit writes AgentShutdownUnhook event to test capture sink
    - Test 7: emit_unhook_audit writes UnhookFailure event on timeout
  </behavior>
  <action>
    In `dlp-agent/src/process_registry.rs`, add tests:
    - `test_record_unhooked_transitions_injected_to_exited`
    - `test_record_unhooked_no_op_for_non_injected`
    - `test_record_unhooked_by_pid_matches_injected` — insert two entries with same PID but different creation_time (simulate PID reuse), one Injected and one Exited; verify only Injected transitions
    - `test_record_unhooked_by_pid_no_match` — PID not in registry, no panic

    In `dlp-agent/src/hook_ipc.rs`, add tests:
    - `test_send_unhook_command_roundtrip` — start a mock server that responds to UnhookCommand with UnhookAck, call send_unhook_command, assert ack.success
    - `test_unhook_ack_routing` — start a mock server, send UnhookAck envelope, verify the server's registry (if threaded) transitions state. Since the mock server runs on a separate thread, use a shared Arc<ProcessRegistry> and verify after the connection closes.
    - `test_unhook_command_timeout` — mock server that does not respond, verify send_unhook_command returns Err

    In `dlp-agent/src/audit_emitter.rs`, add tests:
    - `test_emit_unhook_audit_agent_shutdown` — call emit_unhook_audit with AgentShutdownUnhook, drain_test_events, assert event_type matches
    - `test_emit_unhook_audit_failure` — call emit_unhook_audit with UnhookFailure, assert event_type and success=false

    In `dlp-agent/src/service.rs`, add tests:
    - `test_unhook_all_injected_processes_empty` — empty registry, no panic, no audit events
    - `test_unhook_all_injected_processes_one_injected` — mock registry with one Injected entry, mock send_unhook_command to return Ok(ack), verify registry transitions to Exited and AgentShutdownUnhook audit is emitted
    Note: Because `unhook_all_injected_processes` is async and calls `tokio::time::timeout`, the test must be `#[tokio::test]` or run inside a tokio runtime.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent -- --nocapture</automated>
  </verify>
  <done>All dlp-agent unit tests pass, including new dispatch, registry, and audit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run quality gates and fix issues</name>
  <files>(all files modified in Phase 58.5)</files>
  <action>
    Run the full quality gate suite for all modified crates:
    1. `cargo test -p dlp-common` — verify all common tests pass
    2. `cargo test -p dlp-hook-dll` — verify all hook DLL tests pass
    3. `cargo test -p dlp-agent` — verify all agent tests pass
    4. `cargo clippy -p dlp-common -- -D warnings` — fix any lints
    5. `cargo clippy -p dlp-hook-dll -- -D warnings` — fix any lints
    6. `cargo clippy -p dlp-agent -- -D warnings` — fix any lints
    7. `cargo fmt --check -p dlp-common` — fix formatting
    8. `cargo fmt --check -p dlp-hook-dll` — fix formatting
    9. `cargo fmt --check -p dlp-agent` — fix formatting
    10. `cargo build --all-targets` — verify no compiler warnings

    Fix any failures found. Common issues to watch for:
    - Unused imports in test modules (remove or allow)
    - Missing `unsafe` blocks around Win32 API calls in tests
    - `unwrap()` in library code (replace with `expect` or `?`)
    - `dead_code` warnings on test-only helpers (add `#[cfg(test)]` or `#[allow(dead_code)]`)
    - `too_many_arguments` on `handle_connection` (already has `#[allow]`, but verify)
    - Clippy warnings about `clone()` on `Copy` types (remove `.clone()`)
    - Formatting mismatches (run `cargo fmt`)

    If any test fails due to Windows-specific APIs in cross-platform test runs, gate those tests with `#[cfg(windows)]` and add non-Windows stubs.
  </action>
  <verify>
    <automated>cargo test --all</automated>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
    <automated>cargo fmt --check</automated>
  </verify>
  <done>All tests pass, clippy is clean, fmt is clean, build has no warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Production | Test mocks must not leak into production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.5-12 | Tampering | Test-only mock code compiled into release binary | mitigate | All mocks are behind `#[cfg(test)]` gates; verify with `grep -v '^#' | grep -c 'cfg(test)'` on modified files |
| T-58.5-13 | Information Disclosure | Test events captured in production | mitigate | `TEST_CAPTURE_ENABLED` is a static `AtomicBool` defaulting to `false`; no production code calls `enable_test_capture()` |
| T-58.5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan |
</threat_model>

<verification>
- `cargo test --all` passes (all existing + new tests)
- `cargo clippy --all-targets -- -D warnings` is clean
- `cargo fmt --check` is clean
- `cargo build --all-targets` has no warnings
- New tests cover: UnhookAll, watchdog, UnhookCommand handler, agent dispatch, registry transitions, audit emission, bincode round-trip
- No `#[cfg(test)]` code leaks into production paths (verified by grep)
</verification>

<success_criteria>
- All unit tests across dlp-common, dlp-hook-dll, and dlp-agent pass
- Clippy clean with `-D warnings`
- rustfmt clean
- Integration test verifies end-to-end unhook flow
- No compiler warnings on full build
</success_criteria>

<output>
Create `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-04-SUMMARY.md` when done
</output>
