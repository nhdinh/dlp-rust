---
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
plan: 04
type: execute
wave: 3
depends_on:
  - 58.5-02
  - 58.5-03
files_modified:
  - dlp-hook-dll/src/lib.rs
  - dlp-hook-dll/src/ntdll_patcher.rs
  - dlp-hook-dll/src/hook_journal.rs
  - dlp-hook-dll/src/classification_cache.rs
  - dlp-hook-dll/src/pipe_client.rs
  - dlp-hook-dll/src/trampolines.rs
  - dlp-agent/src/service.rs
  - dlp-agent/src/process_registry.rs
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/audit_emitter.rs
  - dlp-common/src/hook_ipc.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements:
  - SC-58.5-01
  - SC-58.5-02
  - SC-58.5-03
  - SC-58.5-04
  - SC-58.5-05
must_haves:
  truths:
    - "cargo test -p dlp-hook-dll passes with new unhook tests"
    - "cargo test -p dlp-agent passes with new dispatch, registry, and audit tests"
    - "cargo test -p dlp-common passes with new IPC and audit round-trip tests"
    - "cargo clippy --all-targets -- -D warnings is clean"
    - "cargo fmt --check is clean"
    - "sonar-scanner runs and the Quality Gate passes"
    - "Integration test verifies the polling protocol: agent sets UNHOOK_ALL_REQUESTED -> DLL PollControl -> UnhookCommand -> UnhookAck -> registry Exited"
  artifacts:
    - path: "dlp-hook-dll/src/lib.rs"
      provides: "Unit tests for UnhookAll, control-poll thread, UnhookCommand handler, and trampoline pass-through"
      exports: ["#[cfg(test)] mod tests"]
    - path: "dlp-agent/src/service.rs"
      provides: "Unit tests for unhook orchestration in run_loop_shutdown"
      exports: ["#[cfg(test)] mod tests"]
    - path: "dlp-agent/src/hook_ipc.rs"
      provides: "Unit tests for PollControl and UnhookAck routing"
      exports: ["#[cfg(test)] mod tests"]
    - path: "dlp-agent/src/audit_emitter.rs"
      provides: "Unit tests for emit_unhook_audit"
      exports: ["#[cfg(test)] mod tests"]
  key_links:
    - from: "dlp-hook-dll tests"
      to: "dlp-common/src/hook_ipc.rs"
      via: "bincode round-trip of PollControl/ControlResponse/UnhookCommand/UnhookAck in tests"
      pattern: "IpcPayloadV1::PollControl"
    - from: "dlp-agent tests"
      to: "dlp-common/src/audit.rs"
      via: "AuditEvent with AgentShutdownUnhook / UnhookFailure / WatchdogSelfUnload"
      pattern: "EventType::AgentShutdownUnhook"
    - from: "dlp-agent tests"
      to: "dlp-agent/src/process_registry.rs"
      via: "UnhookAck with creation_time transitions the correct ProcessKey"
      pattern: "record_unhooked"
---

<objective>
Write comprehensive unit and integration tests for the entire Phase 58.5 unhook system using the polling protocol: hook DLL self-unhook (UnhookAll, control-poll thread, UnhookCommand handler, trampoline pass-through), agent dispatch (UNHOOK_ALL_REQUESTED flag, PollControl response, UnhookAck routing, registry transitions, audit emission), and common types (bincode round-trip, audit event routing). Run full quality gates including SonarQube and fix any issues.

Purpose: Verify that the cooperative unhook protocol works end-to-end with the correct agent-hosted polling model, that all edge cases are covered, and that the codebase meets the project's quality standards (tests pass, clippy clean, fmt clean, SonarQube Quality Gate passes).
Output: Passing tests across all three crates, clean clippy, clean fmt, passing SonarQube Quality Gate, and a verification report.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-REVIEWS.md

@dlp-hook-dll/src/lib.rs
@dlp-hook-dll/src/ntdll_patcher.rs
@dlp-hook-dll/src/hook_journal.rs
@dlp-hook-dll/src/classification_cache.rs
@dlp-hook-dll/src/pipe_client.rs
@dlp-hook-dll/src/trampolines.rs
@dlp-agent/src/service.rs
@dlp-agent/src/process_registry.rs
@dlp-agent/src/hook_ipc.rs
@dlp-agent/src/audit_emitter.rs
@dlp-common/src/hook_ipc.rs
@dlp-common/src/audit.rs
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-01-PLAN.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-02-PLAN.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write unit tests for dlp-hook-dll self-unhook and control-poll thread</name>
  <files>dlp-hook-dll/src/lib.rs, dlp-hook-dll/src/ntdll_patcher.rs, dlp-hook-dll/src/hook_journal.rs, dlp-hook-dll/src/classification_cache.rs, dlp-hook-dll/src/pipe_client.rs, dlp-hook-dll/src/trampolines.rs</files>
  <behavior>
    - Test 1: UnhookAll sets SHUTTING_DOWN flag, calls unpatch_all_stubs, and unmaps shared memory
    - Test 2: UnhookAll is infallible per stub: a corrupted hook entry does not abort the loop
    - Test 3: handle_unhook_command sends UnhookAck with success=true when UnhookAll succeeds
    - Test 4: handle_unhook_command sends UnhookAck with success=false when UnhookAll reports failure
    - Test 5: Control-poll thread triggers self-unload after 3 consecutive simulated pipe failures
    - Test 6: Control-poll thread resets failure count on successful poll
    - Test 7: Control-poll thread calls handle_unhook_command when ControlResponse contains UnhookCommand
    - Test 8: Trampolines pass through to original APIs when SHUTTING_DOWN is true
    - Test 9: self_unload_check returns the HINSTANCE captured in DllMain
  </behavior>
  <action>
    In `dlp-hook-dll/src/lib.rs`, extend the `#[cfg(test)]` module with tests that use `#[cfg(test)]` static mocks for `pipe_client::poll_control` and `pipe_client::send_unhook_ack`. Gate all mock code behind `#[cfg(test)]` so it does not leak into release builds.

    Tests to add:
    - `unhook_all_sets_shutting_down` — call UnhookAll, assert SHUTTING_DOWN is true after
    - `unhook_all_unpatches_ntdll_stubs` — initialize a test patcher, manually set stub states to Patched, call UnhookAll, assert Unpatched
    - `unhook_all_unmaps_shared_memory` — set up test mappings, call UnhookAll, assert handles/views are cleared
    - `unhook_all_infallible_per_stub` — corrupt one hook entry, call UnhookAll, assert no panic and the function returns false
    - `handle_unhook_command_sends_success_ack` — mock ack sink, call handler, assert ack.success == true and ack.creation_time is non-zero
    - `handle_unhook_command_sends_failure_ack` — force UnhookAll to report failure, assert ack.success == false
    - `control_poll_thread_triggers_after_three_failures` — mock poll_control to fail 3 times, assert WATCHDOG_TRIGGERED flag is set
    - `control_poll_thread_resets_on_success` — mock poll_control to fail twice then return no-op, assert WATCHDOG_TRIGGERED is not set
    - `control_poll_thread_handles_unhook_command` — mock poll_control to return ControlResponse with UnhookCommand, assert handle_unhook_command path is invoked
    - `shutting_down_passes_through_create_file_w` — set SHUTTING_DOWN true, call HookCreateFileW, assert no pipe request is made
    - `shutting_down_passes_through_nt_create_file` — same pattern for ntdll trampoline
    - `dll_hinstance_captured_in_tests` — verify self_unload_check returns Some

    In `dlp-hook-dll/src/ntdll_patcher.rs`, ensure `unpatch_all_stubs` is `pub` and add:
    - `test_unpatch_all_stubs_transitions_patched_to_unpatched`

    In `dlp-hook-dll/src/hook_journal.rs` and `classification_cache.rs`, add tests:
    - `test_unmap_journal_releases_handle_and_view`
    - `test_unmap_cache_releases_handle_and_view`

    For mocking, use `#[cfg(test)]` statics such as:
    ```rust
    #[cfg(test)]
    static MOCK_PIPE_ACK: std::sync::Mutex<Vec<UnhookAck>> = std::sync::Mutex::new(Vec::new());
    #[cfg(test)]
    static MOCK_POLL_CONTROL: std::sync::Mutex<Vec<Result<ControlResponse, PipeError>>> = std::sync::Mutex::new(Vec::new());
    ```
    Conditionally replace the real functions in test builds.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll -- --nocapture</automated>
  </verify>
  <done>All dlp-hook-dll unit tests pass, including new unhook, control-poll, watchdog, handler, and trampoline pass-through tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit and integration tests for dlp-agent unhook dispatch and audit emission</name>
  <files>dlp-agent/src/service.rs, dlp-agent/src/process_registry.rs, dlp-agent/src/hook_ipc.rs, dlp-agent/src/audit_emitter.rs</files>
  <behavior>
    - Test 1: iter_injected returns only Injected entries
    - Test 2: record_unhooked transitions Injected to Exited
    - Test 3: PollControl returns no-op ControlResponse when UNHOOK_ALL_REQUESTED is false
    - Test 4: PollControl returns UnhookCommand when UNHOOK_ALL_REQUESTED is true
    - Test 5: UnhookAck with matching pid + creation_time transitions registry entry to Exited
    - Test 6: UnhookAck with mismatched creation_time is ignored
    - Test 7: emit_unhook_audit writes AgentShutdownUnhook event to test capture sink
    - Test 8: emit_unhook_audit writes UnhookFailure event for a remaining injected entry
    - Test 9: Integration test starts HookIpcServer, sends PollControl, toggles UNHOOK_ALL_REQUESTED, and verifies the response
  </behavior>
  <action>
    In `dlp-agent/src/process_registry.rs`, add tests:
    - `test_iter_injected_returns_only_injected`
    - `test_iter_injected_pid_reuse`
    - `test_record_unhooked_transitions_injected_to_exited`
    - `test_record_unhooked_no_op_for_non_injected`

    In `dlp-agent/src/hook_ipc.rs`, add tests:
    - `test_poll_control_no_op_when_not_requested`
    - `test_poll_control_returns_unhook_command_when_requested`
    - `test_unhook_ack_routing_matches_creation_time`
    - `test_unhook_ack_routing_ignores_mismatched_creation_time`

    In `dlp-agent/src/audit_emitter.rs`, add tests:
    - `test_emit_unhook_audit_agent_shutdown`
    - `test_emit_unhook_audit_failure`

    In `dlp-agent/src/service.rs`, add tests:
    - `test_request_unhook_from_injected_emits_agent_shutdown` — seed registry with one Injected entry, call the test helper, assert AgentShutdownUnhook is emitted and UNHOOK_ALL_REQUESTED is true
    - `test_request_unhook_from_injected_emits_failure_for_remaining` — seed registry with an Injected entry, do not send an ack, call the helper with a short budget, assert UnhookFailure is emitted for the remaining key

    Add an integration test in `dlp-agent/src/hook_ipc.rs` or a new `tests/` file:
    - `unhook_polling_protocol_roundtrip` — start a `HookIpcServer` with a registry on a test pipe, send `PollControl`, verify `ControlResponse { command: None }`, set `UNHOOK_ALL_REQUESTED`, send another `PollControl`, verify the response contains `UnhookCommand`, then send `UnhookAck` and verify the registry entry is `Exited`.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent -- --nocapture</automated>
  </verify>
  <done>All dlp-agent unit and integration tests pass, including new dispatch, registry, PollControl/UnhookAck routing, and audit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-boundary quality gates including SonarQube and fix issues</name>
  <files>(all files modified in Phase 58.5)</files>
  <action>
    Run the full phase-boundary quality gate suite after Tasks 1 and 2 have already passed their per-crate `cargo test -p <crate>` commands. These slower commands are intentionally reserved for the final wave boundary, not for per-task commit loops.

    Per-task fast feedback (already completed in earlier tasks):
    - `cargo test -p dlp-common` — verify all common tests pass (Task 1 of Plan 58.5-01)
    - `cargo test -p dlp-hook-dll` — verify all hook DLL tests pass (Task 1 of this plan)
    - `cargo test -p dlp-agent` — verify all agent tests pass (Task 2 of this plan)

    Phase-boundary gates (this task):
    1. `cargo test --all` — full workspace regression check
    2. `cargo clippy --all-targets -- -D warnings` — fix any lints across the workspace
    3. `cargo fmt --check` — fix formatting across the workspace
    4. `cargo build --all-targets` — verify no compiler warnings
    5. `sonar-scanner` — verify the SonarQube Quality Gate passes (requires SONAR_TOKEN exported in the session)

    Fix any failures found. Common issues to watch for:
    - Unused imports in test modules (remove or allow)
    - Missing `unsafe` blocks around Win32 API calls in tests
    - `unwrap()` in library code (replace with `expect` or `?`)
    - `dead_code` warnings on test-only helpers (add `#[cfg(test)]` or `#[allow(dead_code)]`)
    - `too_many_arguments` on `handle_connection` (already has `#[allow]`, but verify)
    - Clippy warnings about `clone()` on `Copy` types (remove `.clone()`)
    - Formatting mismatches (run `cargo fmt`)
    - SonarQube bugs, code smells, or coverage gaps — generate additional unit tests if coverage is blocking

    If any test fails due to Windows-specific APIs in cross-platform test runs, gate those tests with `#[cfg(windows)]` and add non-Windows stubs.
  </action>
  <verify>
    <automated>cargo test --all</automated>
    <automated>cargo clippy --all-targets -- -D warnings</automated>
    <automated>cargo fmt --check</automated>
    <automated>sonar-scanner</automated>
  </verify>
  <done>All tests pass, clippy is clean, fmt is clean, build has no warnings, and SonarQube Quality Gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Production | Test mocks must not leak into production code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.5-12 | Tampering | Test-only mock code compiled into release binary | mitigate | All mocks are behind `#[cfg(test)]` gates; verify with `grep -v '^#' | grep -c 'cfg(test)'` on modified files |
| T-58.5-13 | Information Disclosure | Test events captured in production | mitigate | Test capture sink is only enabled by test code; no production path enables it |
| T-58.5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan |
</threat_model>

<verification>
- `cargo test --all` passes (all existing + new tests)
- `cargo clippy --all-targets -- -D warnings` is clean
- `cargo fmt --check` is clean
- `cargo build --all-targets` has no warnings
- `sonar-scanner` runs and the Quality Gate passes
- New tests cover: UnhookAll, control-poll thread, UnhookCommand handler, trampoline pass-through, PollControl routing, UnhookAck routing, registry transitions, audit emission, bincode round-trip
- No `#[cfg(test)]` code leaks into production paths (verified by grep)
</verification>

<success_criteria>
- All unit tests across dlp-common, dlp-hook-dll, and dlp-agent pass
- Clippy clean with `-D warnings`
- rustfmt clean
- SonarQube Quality Gate passes
- Integration test verifies the polling unhook protocol end-to-end
- No compiler warnings on full build
</success_criteria>

<output>
Create `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-04-SUMMARY.md` when done
</output>
