---
status: resolved
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
source: 58.5-01-SUMMARY.md, 58.5-02-SUMMARY.md, 58.5-03-SUMMARY.md, 58.5-04-SUMMARY.md, 58.5-05-SUMMARY.md, 58.5-06-SUMMARY.md
started: 2026-07-01T00:00:00Z
updated: 2026-07-02T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. dlp-common unhook IPC and audit type tests
expected: Running `cargo test -p dlp-common` completes with all tests passing and no failures.
result: pass

### 2. dlp-hook-dll cooperative self-unhook tests
expected: Running `cargo test -p dlp-hook-dll --lib` completes with all tests passing and no failures.
result: pass

### 3. dlp-agent unhook orchestration tests
expected: Running `cargo test -p dlp-agent` completes with all tests passing and no failures.
result: pass

### 4. Workspace quality gates
expected: Running `cargo test --workspace --lib --tests --bins --no-fail-fast -- --test-threads=1`, then `cargo clippy --all-targets -- -D warnings`, then `cargo fmt --check`, then `cargo build --all-targets` all complete cleanly with no errors or warnings.
result: pass

### 5. Coverage auto-passed — Plan 03 agent-side orchestration
expected: Automated coverage from 58.5-03-SUMMARY.md (D1-D6) is verified by passing unit/integration tests.
result: pass
source: automated
coverage_id: D1

### 6. Coverage auto-passed — Plan 04 hook DLL and agent test coverage
expected: Automated coverage from 58.5-04-SUMMARY.md (U1-U7, I1) is verified by passing unit/integration tests.
result: pass
source: automated
coverage_id: U1

### 7. Live Windows DLL unload verification
expected: After stopping the dlp-agent service, any previously injected dlp-hook-dll.dll module unloads from a test process within the watchdog timeout.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0

## Gaps

- truth: "Running `cargo test -p dlp-hook-dll --lib` completes with all tests passing and no failures."
  status: resolved
  reason: "Extended PHASE_58_5_TEST_LOCK serialization to all tests that mutate process-global state, added reset helpers, switched to parking_lot::Mutex to prevent cascade poisoning, and hardened watchdog timing tests under parallel load. `cargo test -p dlp-hook-dll --lib` now passes reliably."
  severity: blocker
  test: 2
  root_cause: "PHASE_58_5_TEST_LOCK serializes only the Phase 58.5 tests, but pre-existing tests mutate the same process-global state (SHUTTING_DOWN, ACTIVE_CALLS, HASH_QUEUE_DEPTH) without acquiring the lock. Under default parallel execution, unprotected tests race with protected ones; a delayed Phase 58.5 test holder blocks all subsequent locked tests past the 60-second harness timeout."
  artifacts:
    - path: "dlp-hook-dll/src/lib.rs"
      issue: "defines PHASE_58_5_TEST_LOCK, SHUTTING_DOWN, ACTIVE_CALLS, UnhookAll drain; tests shutdown_flag_stops_new_hook_calls and active_call_guard_balanced are unprotected"
    - path: "dlp-hook-dll/src/trampolines.rs"
      issue: "tests active_call_counter_increments_and_decrements and active_call_guard_skips_when_shutting_down mutate SHUTTING_DOWN/ACTIVE_CALLS without the lock"
    - path: "dlp-hook-dll/src/hash_compute.rs"
      issue: "test_hash_skipped_on_pool_saturation mutates global HASH_QUEUE_DEPTH without the lock"
    - path: "dlp-hook-dll/src/control_thread.rs"
      issue: "control-thread lifecycle is shared between protected and unprotected tests"
  missing:
    - "Extend serialization to all tests that touch process-global state, or rename PHASE_58_5_TEST_LOCK to a crate-wide test lock"
    - "Wrap affected non-Phase-58.5 tests (shutdown_flag_stops_new_hook_calls, active_call_guard_balanced, active_call_counter_increments_and_decrements, active_call_guard_skips_when_shutting_down, test_hash_skipped_on_pool_saturation) with the same lock"
  debug_session: ".planning/debug/cargo-test-dlp-hook-dll-hang.md"
- truth: "After stopping the dlp-agent service, any previously injected dlp-hook-dll.dll module unloads from a test process within the watchdog timeout."
  status: resolved
  reason: "Agent startup and backstop sweeps now record the real process creation time in ProcessKey, and HookInjector starts the hook DLL control-poll thread immediately after LoadLibraryW via a new StartDlpControlThread export. This lets idle injected processes poll during shutdown and receive UnhookCommand."
  severity: blocker
  test: 7
  root_cause: "Processes injected by startup/backstop sweeps are registered in ProcessRegistry with creation_time: 0, while PollControl frames carry the real process creation time. The PollControl handler requires an exact (pid, creation_time) match against an Injected registry entry before returning UnhookCommand, so sweep-injected DLLs receive command: None during shutdown and never self-unhook. Injected idle processes also never start the control-poll thread, so they have no watchdog path either."
  artifacts:
    - path: "dlp-agent/src/service.rs"
      issue: "startup_sweep and backstop_sweep construct ProcessKey { pid, creation_time: 0 }"
    - path: "dlp-agent/src/hook_ipc.rs"
      issue: "PollControl handler only returns UnhookCommand on exact Injected ProcessKey match"
    - path: "dlp-hook-dll/src/control_thread.rs"
      issue: "watchdog grace window only begins after agent exit/pipe failure"
    - path: "dlp-hook-dll/src/lib.rs"
      issue: "control-poll thread starts lazily on first hooked API call"
    - path: "dlp-agent/src/bypass_correlator.rs"
      issue: "contains working get_creation_time_for_pid helper that should be reused"
  missing:
    - "Obtain real process creation time in startup_sweep and backstop_sweep via OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION) + GetProcessTimes"
    - "Ensure registry keys match the values the DLL sends in PollControl"
    - "Consider starting the control-poll thread for every injected process so idle processes can unhook"
  debug_session: ".planning/debug/dll-unload-after-agent-stop-001.md"
