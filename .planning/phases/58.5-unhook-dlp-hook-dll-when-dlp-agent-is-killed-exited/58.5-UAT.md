---
status: diagnosed
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
source: 58.5-01-SUMMARY.md, 58.5-02-SUMMARY.md, 58.5-03-SUMMARY.md, 58.5-04-SUMMARY.md, 58.5-05-SUMMARY.md, 58.5-06-SUMMARY.md
started: 2026-07-02T13:14:19Z
updated: 2026-07-03T08:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. dlp-common unhook IPC and audit type tests
expected: Running `cargo test -p dlp-common` completes with all tests passing and no failures.
result: pass

### 2. dlp-hook-dll cooperative self-unhook tests
expected: Running `cargo test -p dlp-hook-dll --lib` completes with all tests passing and no failures.
result: pass

### 3. dlp-agent unhook orchestration tests
expected: Running `cargo test -p dlp-agent` completes with all tests passing and no failures.
result: pass

### 4. Workspace quality gates
expected: Running `cargo test --workspace --lib --tests --bins --no-fail-fast -- --test-threads=1`, then `cargo clippy --all-targets -- -D warnings`, then `cargo fmt --check`, then `cargo build --all-targets` all complete cleanly with no errors or warnings.
result: pass

### 5. Coverage auto-passed — Plan 03 agent-side orchestration
expected: Automated coverage from 58.5-03-SUMMARY.md (D1-D6) is verified by passing unit/integration tests.
result: pass
source: automated
coverage_id: D1

### 6. Coverage auto-passed — Plan 04 hook DLL and agent test coverage
expected: Automated coverage from 58.5-04-SUMMARY.md (U1-U7, I1) is verified by passing unit/integration tests.
result: pass
source: automated
coverage_id: U1

### 7. Live Windows DLL unload verification
expected: After stopping the dlp-agent service, any previously injected dlp-hook-dll.dll module unloads from a test process within the watchdog timeout.
result: issue
reported: "no"
severity: blocker

## Summary

total: 7
passed: 6
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "After stopping the dlp-agent service, any previously injected dlp-hook-dll.dll module unloads from a test process within the watchdog timeout."
  status: failed
  reason: "User reported: no"
  severity: blocker
  test: 7
  root_cause: "The cooperative unhook protocol is client-driven and requires a running hook-DLL control-poll thread in every target process. The agent's HookInjector only attempts to start this thread as a best-effort second remote thread after LoadLibraryW, and inject_into_process still reports success when that remote thread fails or is skipped. Idle injected processes that never trigger a hooked API call therefore never start a control thread, never send PollControl, and never receive UnhookCommand. The 58.5-07 reordering/budget fix only moved the server-side dispatch earlier; it did not make control-thread start reliable, nor did it reconcile already-loaded processes that lack a control thread. A secondary failure mode is that startup/backstop sweeps may LoadLibraryW into processes that already have dlp_hook_dll.dll loaded, leaving a reference count > 1 so FreeLibraryAndExitThread does not fully unload the module."
  artifacts:
    - path: "dlp-agent/src/service.rs"
      issue: "request_unhook_from_injected depends on client PollControl; startup_sweep/backstop_sweep re-inject without is_module_loaded check"
    - path: "dlp-agent/src/hook_injector.rs"
      issue: "inject_into_process returns Ok even when start_remote_control_thread fails or is skipped"
    - path: "dlp-agent/src/hook_ipc.rs"
      issue: "PollControl handler only replies UnhookCommand when registry ProcessKey is Injected and UNHOOK_ALL_REQUESTED is true"
    - path: "dlp-hook-dll/src/lib.rs"
      issue: "enter_hook_call lazily starts control thread only on hooked API call; DllMain does not start it; self_unload calls FreeLibraryAndExitThread only once"
    - path: "dlp-hook-dll/src/control_thread.rs"
      issue: "watchdog self-unload requires a running control_thread_loop and waits ~30 seconds after MAX_FAILURES consecutive errors"
  missing:
    - "Make control-thread start a hard success criterion for a successful injection"
    - "Add a reconciliation pass that ensures every Injected registry entry has a running control thread before shutdown"
    - "Avoid re-LoadLibraryW into processes that already have dlp_hook_dll.dll loaded (use is_module_loaded, as the sync-client watcher already does)"
    - "Consider making the hook DLL self-unload decrement the module reference count until the module actually disappears"
  debug_session: ".planning/debug/dll-remains-after-agent-kill.md"
