---
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/hook_ipc.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements:
  - SC-58.5-01
  - SC-58.5-05
must_haves:
  truths:
    - "IpcPayloadV1 has UnhookCommand, UnhookAck, PollControl, and ControlResponse variants that bincode round-trip"
    - "UnhookAck carries pid and creation_time so the agent can correlate acks with ProcessKey"
    - "EventType enum has AgentShutdownUnhook, WatchdogSelfUnload, and UnhookFailure variants"
    - "New audit event types route to SIEM and have correct triggers_alert semantics"
  artifacts:
    - path: "dlp-common/src/hook_ipc.rs"
      provides: "UnhookCommand + UnhookAck + PollControl + ControlResponse payload variants"
      exports: ["IpcPayloadV1::UnhookCommand", "IpcPayloadV1::UnhookAck", "IpcPayloadV1::PollControl", "IpcPayloadV1::ControlResponse"]
    - path: "dlp-common/src/audit.rs"
      provides: "Unhook lifecycle event types"
      exports: ["EventType::AgentShutdownUnhook", "EventType::WatchdogSelfUnload", "EventType::UnhookFailure"]
  key_links:
    - from: "dlp-common/src/hook_ipc.rs"
      to: "dlp-agent/src/hook_ipc.rs"
      via: "handle_connection routes PollControl and replies with ControlResponse/UnhookCommand"
      pattern: "IpcPayloadV1::PollControl"
    - from: "dlp-common/src/hook_ipc.rs"
      to: "dlp-hook-dll/src/lib.rs"
      via: "control poll thread sends PollControl and receives ControlResponse"
      pattern: "IpcPayloadV1::ControlResponse"
    - from: "dlp-common/src/audit.rs"
      to: "dlp-agent/src/service.rs"
      via: "emit_audit_event called with AgentShutdownUnhook / WatchdogSelfUnload / UnhookFailure"
      pattern: "EventType::AgentShutdownUnhook"
---

<objective>
Add the cooperative unhook protocol types to dlp-common: agent-to-DLL control payloads (`UnhookCommand`, `UnhookAck`, `PollControl`, `ControlResponse`) and three new audit event types (`AgentShutdownUnhook`, `WatchdogSelfUnload`, `UnhookFailure`) with correct SIEM routing and alert semantics.

Purpose: These shared types are the foundation for the agent-hosted polling unhook protocol and the audit trail for unhook lifecycle events. Without them, Plans 02 and 03 cannot compile.
Output: Extended `IpcPayloadV1` enum, extended `EventType` enum, and passing unit tests for serialization and routing semantics.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-RESEARCH.md
@.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-REVIEWS.md

@dlp-common/src/hook_ipc.rs
@dlp-common/src/audit.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UnhookCommand, UnhookAck, PollControl, and ControlResponse to IpcPayloadV1</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <behavior>
    - Test 1: IpcEnvelope::V1(IpcPayloadV1::UnhookCommand(...)) bincode-serializes and round-trips with matching fields
    - Test 2: IpcEnvelope::V1(IpcPayloadV1::UnhookAck(...)) round-trips and preserves pid + creation_time
    - Test 3: IpcEnvelope::V1(IpcPayloadV1::PollControl(...)) round-trips
    - Test 4: IpcEnvelope::V1(IpcPayloadV1::ControlResponse(...)) round-trips with Some(UnhookCommand) and None
    - Test 5: Adding variants after HashEvidence does not change bincode indexes of existing variants
  </behavior>
  <action>
    Add payload structs and four new `IpcPayloadV1` variants in `dlp-common/src/hook_ipc.rs` after the existing `HashEvidence` variant. Addresses review concern: agent-to-DLL command path must use the existing agent-hosted pipe topology; PollControl lets the DLL poll the agent rather than requiring the agent to connect to each injected process.

    Structs to add:
    - `UnhookReason` enum with variants `AgentShutdown` and `WatchdogTimeout`
    - `UnhookCommand { reason: UnhookReason, timestamp_secs: u64 }`
    - `UnhookAck { pid: u32, creation_time: u64, success: bool, error: Option<String> }` — include creation_time per review concern C2/C3 to preserve the registry's PID-reuse-safe composite key
    - `PollControl { pid: u32, creation_time: u64 }`
    - `ControlResponse { command: Option<UnhookCommand> }`

    Add to `IpcPayloadV1`:
    - `UnhookCommand(UnhookCommand)` — agent replies to a control poll
    - `UnhookAck(UnhookAck)` — hook DLL reports unhook outcome
    - `PollControl(PollControl)` — hook DLL asks the agent for pending control commands
    - `ControlResponse(ControlResponse)` — agent replies to PollControl

    Place variants after `HashEvidence` to preserve existing bincode enum indexes. Derive `Debug`, `Clone`, `PartialEq`, `Serialize`, `Deserialize` on all new structs/enums.

    Add unit tests in the `#[cfg(test)]` module:
    - `unhook_command_roundtrip`
    - `unhook_ack_roundtrip_includes_creation_time`
    - `poll_control_roundtrip`
    - `control_response_with_command_roundtrip`
    - `control_response_no_op_roundtrip`
    - `existing_variant_indexes_unchanged` — serialize a Request/Response pair and assert the discriminant bytes match pre-change values
  </action>
  <verify>
    <automated>cargo test -p dlp-common unhook_ poll_control_ control_response_ -- --nocapture</automated>
  </verify>
  <done>IpcPayloadV1 has the four new variants; 6+ unit tests pass; bincode round-trip verified; creation_time is part of UnhookAck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unhook lifecycle event types to audit.rs</name>
  <files>dlp-common/src/audit.rs</files>
  <behavior>
    - Test 1: EventType::AgentShutdownUnhook routes to SIEM
    - Test 2: EventType::WatchdogSelfUnload routes to SIEM
    - Test 3: EventType::UnhookFailure routes to SIEM
    - Test 4: EventType::UnhookFailure triggers_alert
    - Test 5: EventType::AgentShutdownUnhook does NOT trigger_alert
    - Test 6: EventType::WatchdogSelfUnload does NOT trigger_alert
    - Test 7: All three variants serde round-trip through JSON
  </behavior>
  <action>
    Add three new variants to the `EventType` enum in `dlp-common/src/audit.rs`, after `DeviceHealthChange`. Addresses review concern: audit integration is missing and must be observable in SIEM.

    Variants:
    - `AgentShutdownUnhook` — agent initiated unhook of all injected processes during graceful shutdown
    - `WatchdogSelfUnload` — hook DLL watchdog detected agent absence and self-unhooked
    - `UnhookFailure` — unhook command failed or ack was not received

    Update `routed_to_siem()` to include all three new variants.
    Update `triggers_alert()`:
    - `UnhookFailure` → true
    - `AgentShutdownUnhook` → false
    - `WatchdogSelfUnload` → false

    Add unit tests in the `#[cfg(test)]` module following the existing ETW event type test pattern:
    - `test_agent_shutdown_unhook_routed_to_siem`
    - `test_watchdog_self_unload_routed_to_siem`
    - `test_unhook_failure_routed_to_siem`
    - `test_unhook_failure_triggers_alert`
    - `test_agent_shutdown_unhook_does_not_trigger_alert`
    - `test_watchdog_self_unload_does_not_trigger_alert`
    - `test_unhook_event_types_serde_roundtrip`
  </action>
  <verify>
    <automated>cargo test -p dlp-common test_unhook_ -- --nocapture</automated>
  </verify>
  <done>EventType has three new variants; routed_to_siem and triggers_alert match arms updated; 7 unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Hook DLL named pipe | UnhookCommand crosses from trusted agent to injected process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.5-01 | Tampering | bincode deserialization of UnhookCommand | mitigate | `UnhookReason` is a bounded enum; invalid bytes fail deserialization and the DLL ignores the message |
| T-58.5-02 | Information Disclosure | UnhookAck error field | accept | Error strings contain only generic messages, no PII or paths |
| T-58.5-03 | Spoofing | Fake PollControl from non-DLL process receives UnhookCommand | accept | UnhookCommand is only data; a process without the hook DLL loaded cannot be unhooked and the command has no effect on it |
| T-58.5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced; slopcheck not needed |
</threat_model>

<verification>
- `cargo test -p dlp-common` passes (all existing + new tests)
- `cargo clippy -p dlp-common -- -D warnings` is clean
- `cargo fmt --check -p dlp-common` is clean
- New variants appear in bincode round-trip tests
- New event types appear in SIEM routing and alert trigger tests
- UnhookAck includes `creation_time` field
</verification>

<success_criteria>
- IpcPayloadV1::UnhookCommand, UnhookAck, PollControl, and ControlResponse exist and bincode round-trip
- UnhookAck carries pid + creation_time for PID-reuse-safe correlation
- EventType::AgentShutdownUnhook, WatchdogSelfUnload, UnhookFailure exist with correct SIEM routing
- All dlp-common tests pass
- No compiler warnings
</success_criteria>

<output>
Create `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-01-SUMMARY.md` when done
</output>
