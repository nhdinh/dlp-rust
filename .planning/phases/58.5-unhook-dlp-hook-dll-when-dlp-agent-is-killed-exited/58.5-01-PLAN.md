---
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/hook_ipc.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "IpcPayloadV1 has UnhookCommand and UnhookAck variants that bincode round-trip"
    - "EventType enum has AgentShutdownUnhook, WatchdogSelfUnload, and UnhookFailure variants"
    - "New audit event types route to SIEM and have correct triggers_alert semantics"
  artifacts:
    - path: "dlp-common/src/hook_ipc.rs"
      provides: "UnhookCommand + UnhookAck payload variants"
      exports: ["IpcPayloadV1::UnhookCommand", "IpcPayloadV1::UnhookAck"]
    - path: "dlp-common/src/audit.rs"
      provides: "Unhook lifecycle event types"
      exports: ["EventType::AgentShutdownUnhook", "EventType::WatchdogSelfUnload", "EventType::UnhookFailure"]
  key_links:
    - from: "dlp-common/src/hook_ipc.rs"
      to: "dlp-agent/src/hook_ipc.rs"
      via: "IpcPayloadV1::UnhookCommand deserialized in handle_connection match arm"
      pattern: "IpcPayloadV1::UnhookCommand"
    - from: "dlp-common/src/hook_ipc.rs"
      to: "dlp-hook-dll/src/lib.rs"
      via: "pipe_client::send_request sends UnhookCommand to agent pipe"
      pattern: "UnhookCommand"
    - from: "dlp-common/src/audit.rs"
      to: "dlp-agent/src/service.rs"
      via: "emit_audit_event called with AgentShutdownUnhook / WatchdogSelfUnload / UnhookFailure"
      pattern: "EventType::AgentShutdownUnhook"
---

<objective>
Add the cooperative unhook protocol types to dlp-common: `UnhookCommand`/`UnhookAck` IPC payload variants and three new audit event types (`AgentShutdownUnhook`, `WatchdogSelfUnload`, `UnhookFailure`) with correct SIEM routing and alert semantics.

Purpose: These shared types are the foundation for the agent-to-hook-DLL unhook protocol and the audit trail for unhook lifecycle events. Without them, Plans 02 and 03 cannot compile.
Output: Extended `IpcPayloadV1` enum, extended `EventType` enum, and passing unit tests for serialization and routing semantics.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@dlp-common/src/hook_ipc.rs
@dlp-common/src/audit.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UnhookCommand and UnhookAck to IpcPayloadV1</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <behavior>
    - Test 1: IpcEnvelope::V1(IpcPayloadV1::UnhookCommand(...)) bincode-serializes and round-trips with matching fields
    - Test 2: IpcEnvelope::V1(IpcPayloadV1::UnhookAck(...)) bincode-serializes and round-trips with matching fields
    - Test 3: UnhookCommand with reason=GracefulShutdown serializes correctly
    - Test 4: UnhookAck with success=false and error=Some("...") round-trips
  </behavior>
  <action>
    Add `UnhookCommand` and `UnhookAck` structs and two new `IpcPayloadV1` variants:

    ```rust
    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct UnhookCommand {
        pub reason: UnhookReason,
        pub timestamp_secs: u64,
    }

    #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
    pub enum UnhookReason {
        GracefulShutdown,
        AgentCrashDetected,
    }

    #[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
    pub struct UnhookAck {
        pub pid: u32,
        pub success: bool,
        pub error: Option<String>,
    }
    ```

    Add to `IpcPayloadV1`:
    - `UnhookCommand(UnhookCommand)` — agent sends to hook DLL
    - `UnhookAck(UnhookAck)` — hook DLL responds to agent

    Place variants after `HashEvidence` in the enum. All fields use `#[serde(default)]` where needed for backward compat (not needed for new structs, but follow the pattern).

    Add unit tests in the `#[cfg(test)]` module at the bottom of the file:
    - `unhook_command_roundtrip` — bincode serialize/deserialize
    - `unhook_ack_roundtrip` — bincode serialize/deserialize
    - `unhook_command_envelope_roundtrip` — full IpcEnvelope wrap
    - `unhook_ack_envelope_roundtrip` — full IpcEnvelope wrap

    Do NOT add `#[serde(default)]` to required fields of new structs — they are always populated by the sender.
  </action>
  <verify>
    <automated>cargo test -p dlp-common unhook_ -- --nocapture</automated>
  </verify>
  <done>IpcPayloadV1 has UnhookCommand and UnhookAck variants; 4 unit tests pass; bincode round-trip verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unhook lifecycle event types to audit.rs</name>
  <files>dlp-common/src/audit.rs</files>
  <behavior>
    - Test 1: EventType::AgentShutdownUnhook routes to SIEM (routed_to_siem() returns true)
    - Test 2: EventType::WatchdogSelfUnload routes to SIEM (routed_to_siem() returns true)
    - Test 3: EventType::UnhookFailure routes to SIEM (routed_to_siem() returns true)
    - Test 4: EventType::UnhookFailure triggers_alert (triggers_alert() returns true)
    - Test 5: EventType::AgentShutdownUnhook does NOT trigger_alert (triggers_alert() returns false)
    - Test 6: EventType::WatchdogSelfUnload does NOT trigger_alert (triggers_alert() returns false)
    - Test 7: All three variants serde round-trip through JSON
  </behavior>
  <action>
    Add three new variants to the `EventType` enum in `dlp-common/src/audit.rs`, after `DeviceHealthChange`:

    ```rust
    /// Phase 58.5: Agent initiated unhook of all injected processes during graceful shutdown.
    AgentShutdownUnhook,
    /// Phase 58.5: Hook DLL watchdog detected agent absence and self-unhooked.
    WatchdogSelfUnload,
    /// Phase 58.5: Unhook command failed (e.g., pipe unreachable, process suspended).
    UnhookFailure,
    ```

    Update `routed_to_siem()` to include all three new variants:
    - `AgentShutdownUnhook` → true
    - `WatchdogSelfUnload` → true
    - `UnhookFailure` → true

    Update `triggers_alert()`:
    - `UnhookFailure` → true (operator should know when unhook fails)
    - `AgentShutdownUnhook` → false (expected behavior)
    - `WatchdogSelfUnload` → false (expected behavior after agent crash)

    Add unit tests in the `#[cfg(test)]` module:
    - `test_agent_shutdown_unhook_routed_to_siem`
    - `test_watchdog_self_unload_routed_to_siem`
    - `test_unhook_failure_routed_to_siem`
    - `test_unhook_failure_triggers_alert`
    - `test_agent_shutdown_unhook_does_not_trigger_alert`
    - `test_watchdog_self_unload_does_not_trigger_alert`
    - `test_unhook_event_types_serde_roundtrip` — JSON serialize/deserialize all three

    Follow the exact test pattern used for Phase 53 ETW event types (e.g., `test_etw_consumer_started_routed_to_siem`).
  </action>
  <verify>
    <automated>cargo test -p dlp-common test_unhook_ -- --nocapture</automated>
  </verify>
  <done>EventType has three new variants; routed_to_siem and triggers_alert match arms updated; 7 unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Hook DLL named pipe | UnhookCommand crosses from trusted agent to injected process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.5-01 | Tampering | bincode deserialization of UnhookCommand | mitigate | `UnhookReason` is a bounded enum; invalid bytes fail deserialization and are ignored by the hook DLL (no action taken) |
| T-58.5-02 | Information Disclosure | UnhookAck error field | accept | Error strings contain only generic messages ("pipe unreachable", "process suspended"), no PII or paths |
| T-58.5-03 | Denial of Service | Fake UnhookCommand from non-agent | mitigate | Pipe is protected by `PipeSecurity` descriptor; only SYSTEM and service SID can connect. Hook DLL ignores UnhookCommand from unknown senders (if distinguishable) |
| T-58.5-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced; slopcheck not needed |
</threat_model>

<verification>
- `cargo test -p dlp-common` passes (all existing + new tests)
- `cargo clippy -p dlp-common -- -D warnings` is clean
- `cargo fmt --check -p dlp-common` is clean
- New variants appear in bincode round-trip tests
- New event types appear in SIEM routing and alert trigger tests
</verification>

<success_criteria>
- IpcPayloadV1::UnhookCommand and IpcPayloadV1::UnhookAck exist and bincode round-trip
- EventType::AgentShutdownUnhook, WatchdogSelfUnload, UnhookFailure exist with correct SIEM routing
- All dlp-common tests pass
- No compiler warnings
</success_criteria>

<output>
Create `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-01-SUMMARY.md` when done
</output>
