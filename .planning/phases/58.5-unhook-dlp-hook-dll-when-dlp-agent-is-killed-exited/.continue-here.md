---
context: phase
phase: 58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited
task: 7
total_tasks: 7
status: blocked
last_updated: 2026-07-02T14:17:49.072Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Workspace test serialization — `cargo test --workspace --lib --tests --bins --no-fail-fast -- --test-threads=1` initially failed because `crash_guard::tests::reentrancy_guard_*` tests did not reset process-global `SHUTTING_DOWN`/`ACTIVE_CALLS` state before asserting. Fix: acquire `PHASE_58_5_TEST_LOCK` and call `crate::reset_hook_globals()` at the start of each reentrancy test.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Test isolation drift | Tests that call `with_reentrancy_guard` / `enter_hook_call` mutate process-global state but were not wrapped with the crate-wide test lock and reset helper. Under `--test-threads=1` this caused deterministic fallback assertions. | blocking | Every test that exercises hook entry/exit or reentrancy must acquire `PHASE_58_5_TEST_LOCK` and call `reset_hook_globals()` before assertions. |

<current_state>
Phase 58.5 UAT restart is in progress. Tests 1-6 passed. Test 7 (live Windows DLL unload verification) is blocked because it requires a live Windows endpoint / VM with the dlp-agent service installed and running to observe real DLL unload after service stop. The automated tests prove the protocol works; the final observational checkpoint cannot be completed from the dev environment.
</current_state>

<completed_work>

Completed Tasks:
- Restarted Phase 58.5 UAT from scratch (reset previous resolved UAT file).
- Created fresh 58.5-UAT.md with 7 checkpoints.
- Test 1: dlp-common unhook IPC and audit type tests — passed.
- Test 2: dlp-hook-dll cooperative self-unhook tests — passed.
- Test 3: dlp-agent unhook orchestration tests — passed.
- Test 4: Workspace quality gates — initially failed under `--test-threads=1` due to `crash_guard` reentrancy test isolation bug; fixed and verified.
- Test 5: Coverage auto-passed — Plan 03 agent-side orchestration — passed.
- Test 6: Coverage auto-passed — Plan 04 hook DLL and agent test coverage — passed.
</completed_work>

<remaining_work>

- Test 7: Live Windows DLL unload verification — blocked, needs physical/VM Windows endpoint.
- After Test 7 resolves: update UAT.md status to complete or partial, commit, and push.
- File/commit the crash_guard fix (already edited; needs commit).
</remaining_work>

<decisions_made>

- Chose to fix the root cause of the workspace test failure (test isolation) rather than skip the quality gate or change the command.
- Added `PHASE_58_5_TEST_LOCK` + `reset_hook_globals()` to the three `crash_guard` reentrancy tests; this aligns with the pattern already used in other global-state mutating tests.
</decisions_made>

<blockers>
- Test 7 blocked by lack of live Windows endpoint/VM for real DLL unload observation. Tracked as `blocked_by: physical-device` in 58.5-UAT.md.
</blockers>

## Required Reading (in order)
1. `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-UAT.md` — current UAT state and outstanding Test 7.
2. `dlp-hook-dll/src/crash_guard.rs` — reentrancy test fix.
3. `.planning/phases/58.5-unhook-dlp-hook-dll-when-dlp-agent-is-killed-exited/58.5-06-SUMMARY.md` — describes the immediate control-thread start mechanism that enables idle injected processes to poll during shutdown.

## Infrastructure State
- No running services or background processes.
- All quality gates pass after fix except the physically blocked Test 7.

## Pre-Execution Critique Required
None — this is verification/execution completion, not design.

<context>
We are at the final checkpoint of a restarted Phase 58.5 UAT. The code fix for test serialization is applied and verified; only a live Windows service test remains. The next session should either obtain a Windows test endpoint to complete Test 7 or decide to mark it blocked and proceed with phase closure.
</context>

<next_action>
Resume `/gsd-verify-work 58.5` on a machine with a live Windows endpoint/VM, run Test 7 (stop dlp-agent service and confirm dlp-hook-dll.dll unloads from an injected process), then complete the UAT session and commit/push.
</next_action>
