---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/chrome/frame.rs
  - dlp-agent/src/offline.rs
  - dlp-agent/src/audit_emitter.rs
  - dlp-agent/src/offline_audit_queue.rs
  - dlp-agent/src/clipboard/listener.rs
  - dlp-agent/src/server_client.rs
  - dlp-agent/src/disk_enforcer.rs
  - dlp-agent/src/etw_kernel_file.rs
autonomous: true
gap_closure: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "cargo doc -p dlp-agent --no-deps generates zero warnings from chrome/frame.rs, offline.rs, audit_emitter.rs, offline_audit_queue.rs, clipboard/listener.rs, server_client.rs, disk_enforcer.rs, and etw_kernel_file.rs"
  artifacts:
    - path: "dlp-agent/src/chrome/frame.rs"
      provides: "MAX_PAYLOAD private link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/offline.rs"
      provides: "offline_decision link and redundant link target fixed"
      min_lines: 1
    - path: "dlp-agent/src/audit_emitter.rs"
      provides: "SessionIdentityMap link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/offline_audit_queue.rs"
      provides: "AuditEvent link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/clipboard/listener.rs"
      provides: "EmitContext link and redundant link target fixed"
      min_lines: 1
    - path: "dlp-agent/src/server_client.rs"
      provides: "FLUSH_THRESHOLD and FLUSH_INTERVAL private links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/disk_enforcer.rs"
      provides: "DiskEnumerator links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/etw_kernel_file.rs"
      provides: "EventType::EtwConsumerLostEvents links fixed"
      min_lines: 1
  key_links:
    - from: "cargo doc --no-deps"
      to: "all 8 source files in this plan"
      via: "rustdoc intra-doc link resolution"
      pattern: "grep -c 'warning:.*(chrome/frame|offline|audit_emitter|offline_audit_queue|clipboard/listener|server_client|disk_enforcer|etw_kernel_file)\\.rs' == 0"
---

<objective>
Close a portion of UAT Gap 2: resolve rustdoc broken intra-doc link warnings in chrome/frame, offline, audit, clipboard, server_client, disk_enforcer, and etw_kernel_file modules. These are documentation-only fixes with no functional changes.

Purpose: Eliminate rustdoc warnings from 8 dlp-agent source files so that `cargo doc -p dlp-agent --no-deps` moves closer to zero warnings.
Output: Eight files with resolved intra-doc link issues.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-UAT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rustdoc warnings in chrome, offline, and audit modules</name>
  <files>
    dlp-agent/src/chrome/frame.rs
    dlp-agent/src/offline.rs
    dlp-agent/src/audit_emitter.rs
    dlp-agent/src/offline_audit_queue.rs
  </files>
  <action>
    Fix rustdoc warnings in four files by converting unresolved or private intra-doc links to plain backticks:

    **dlp-agent/src/chrome/frame.rs** (1 warning):
    - Line 29: `MAX_PAYLOAD` is a private item linked from public docs. Convert to plain backticks.

    **dlp-agent/src/offline.rs** (2 warnings):
    - Line 12: `offline_decision` does not resolve. Convert to plain backticks.
    - Line 4: redundant explicit link target. Remove redundant target (convert `[text](path)` to `[text]` where implicit target suffices).

    **dlp-agent/src/audit_emitter.rs** (1 warning):
    - Line 321: `SessionIdentityMap` does not resolve. Convert to plain backticks.

    **dlp-agent/src/offline_audit_queue.rs** (1 warning):
    - Line 207: `AuditEvent` does not resolve. Convert to plain backticks or use `dlp_common::audit::AuditEvent` if the path is known.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these four files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(chrome/frame|offline|audit_emitter|offline_audit_queue)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from chrome/frame.rs, offline.rs, audit_emitter.rs, or offline_audit_queue.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rustdoc warnings in clipboard and server_client modules</name>
  <files>
    dlp-agent/src/clipboard/listener.rs
    dlp-agent/src/server_client.rs
  </files>
  <action>
    Fix rustdoc warnings in two files by converting unresolved or private intra-doc links to plain backticks:

    **dlp-agent/src/clipboard/listener.rs** (2 warnings):
    - Line 369: `EmitContext` does not resolve. Convert to plain backticks.
    - Line 8: redundant explicit link target. Remove redundant target.

    **dlp-agent/src/server_client.rs** (4 warnings):
    - Lines 1010, 1039, 1109: `FLUSH_THRESHOLD`, `FLUSH_INTERVAL` are private items linked from public docs. Convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these two files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(clipboard/listener|server_client)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from clipboard/listener.rs or server_client.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Fix rustdoc warnings in disk_enforcer and etw_kernel_file</name>
  <files>
    dlp-agent/src/disk_enforcer.rs
    dlp-agent/src/etw_kernel_file.rs
  </files>
  <action>
    Fix rustdoc warnings in two files by converting unresolved intra-doc links to plain backticks or full paths:

    **dlp-agent/src/disk_enforcer.rs** (3 warnings):
    - Lines 3, 13, 60: `DiskEnumerator` and `DiskEnumerator::is_ready` do not resolve. Convert to plain backticks.

    **dlp-agent/src/etw_kernel_file.rs** (2 warnings):
    - Lines 15, 361: `EventType::EtwConsumerLostEvents` does not resolve. Use `dlp_common::EventType::EtwConsumerLostEvents` if the type is in dlp-common, otherwise convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these two files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(disk_enforcer|etw_kernel_file)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from disk_enforcer.rs or etw_kernel_file.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-comment -> rustdoc | Documentation comments are untrusted input to rustdoc; broken links degrade developer experience but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-05-01 | Information Disclosure | doc comments | accept | Broken intra-doc links do not leak sensitive data; they are developer-facing documentation only |
| T-67.1-05-02 | Denial of Service | build pipeline | accept | cargo doc warnings do not block cargo build; no runtime impact |
| T-67.1-05-SC | Tampering | source files | mitigate | All changes are documentation only; no logic changes. Verify via cargo test and cargo clippy |
</threat_model>

<verification>
- `cargo doc -p dlp-agent --no-deps` reports zero warnings from all 8 files in this plan
- `cargo clippy -p dlp-agent -- -D warnings` passes clean
- `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed
</verification>

<success_criteria>
Rustdoc warnings in 8 dlp-agent files are resolved:
1. chrome/frame.rs, offline.rs, audit_emitter.rs, offline_audit_queue.rs — all link warnings resolved.
2. clipboard/listener.rs, server_client.rs — all link warnings resolved.
3. disk_enforcer.rs, etw_kernel_file.rs — all link warnings resolved.
4. No regressions in tests or clippy.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-05-SUMMARY.md` when done
</output>
