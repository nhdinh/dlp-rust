---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/classification_cache.rs
  - dlp-agent/src/print_watermark.rs
  - dlp-agent/src/print_watermark_directwrite.rs
  - dlp-agent/src/detection/disk.rs
  - dlp-agent/src/detection/encryption.rs
  - dlp-agent/src/detection/usb.rs
  - dlp-agent/src/offline.rs
  - dlp-agent/src/audit_emitter.rs
  - dlp-agent/src/offline_audit_queue.rs
  - dlp-agent/src/clipboard/listener.rs
  - dlp-agent/src/server_client.rs
  - dlp-agent/src/device_controller.rs
  - dlp-agent/src/device_registry.rs
  - dlp-agent/src/dacl_tripwire.rs
  - dlp-agent/src/chrome/cache.rs
  - dlp-agent/src/chrome/frame.rs
  - dlp-agent/src/disk_enforcer.rs
  - dlp-agent/src/device_identity.rs
  - dlp-agent/src/etw_kernel_file.rs
autonomous: true
gap_closure: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "cargo fmt --check -p dlp-agent reports no diffs (import order in classification_cache.rs fixed)"
    - "cargo doc -p dlp-agent --no-deps generates docs with zero warnings"
  artifacts:
    - path: "dlp-agent/src/classification_cache.rs"
      provides: "Correctly ordered dlp_common imports per rustfmt"
    - path: "dlp-agent/src/print_watermark.rs"
      provides: "FixedPageNs made pub, redundant link target removed"
    - path: "dlp-agent/src/print_watermark_directwrite.rs"
      provides: "Redundant explicit link target removed"
    - path: "dlp-agent/src/etw_kernel_file.rs"
      provides: "EventType::EtwConsumerLostEvents links fixed"
    - path: "dlp-agent/src/offline.rs"
      provides: "offline_decision and redundant link target fixed"
    - path: "dlp-agent/src/clipboard/listener.rs"
      provides: "EmitContext link and redundant link target fixed"
    - path: "dlp-agent/src/server_client.rs"
      provides: "Private-item links to FLUSH_THRESHOLD/FLUSH_INTERVAL removed"
    - path: "dlp-agent/src/detection/disk.rs"
      provides: "EmitContext and VolumeDetector links fixed"
    - path: "dlp-agent/src/detection/encryption.rs"
      provides: "DiskEnumerator, EventType, mark_first_check_complete links fixed"
    - path: "dlp-agent/src/detection/usb.rs"
      provides: "get_volume_class_for_pipe_query and REGISTRY_RUNTIME_HANDLE links fixed"
    - path: "dlp-agent/src/audit_emitter.rs"
      provides: "SessionIdentityMap link fixed"
    - path: "dlp-agent/src/offline_audit_queue.rs"
      provides: "AuditEvent link fixed"
    - path: "dlp-agent/src/device_controller.rs"
      provides: "disable_usb_device link fixed"
    - path: "dlp-agent/src/device_registry.rs"
      provides: "trust_tier_for_with_sid and REGISTRY_POLL_INTERVAL links fixed"
    - path: "dlp-agent/src/dacl_tripwire.rs"
      provides: "DENIED_MASK private link removed"
    - path: "dlp-agent/src/chrome/cache.rs"
      provides: "ORIGINS_POLL_INTERVAL private link removed"
    - path: "dlp-agent/src/chrome/frame.rs"
      provides: "MAX_PAYLOAD private link removed"
    - path: "dlp-agent/src/disk_enforcer.rs"
      provides: "DiskEnumerator links fixed"
    - path: "dlp-agent/src/device_identity.rs"
      provides: "VPN_KEYWORDS private link removed"
  key_links:
    - from: "cargo fmt --check"
      to: "dlp-agent/src/classification_cache.rs import order"
      via: "rustfmt reordering rules"
    - from: "cargo doc --no-deps"
      to: "all dlp-agent source files"
      via: "rustdoc intra-doc link resolution"
---

<objective>
Close two diagnosed gaps from Phase 67.1 UAT: (1) cargo fmt import order in classification_cache.rs, and (2) rustdoc broken intra-doc links across 19 dlp-agent source files. These are automated formatting and documentation fixes with no functional changes.

Purpose: Ensure Phase 67.1 passes all UAT gates (formatting and documentation) so the milestone can be marked complete.
Output: All UAT tests pass — cargo fmt clean, cargo doc clean, cargo clippy clean, cargo test clean.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-UAT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cargo fmt import order in classification_cache.rs</name>
  <files>dlp-agent/src/classification_cache.rs</files>
  <action>
    In dlp-agent/src/classification_cache.rs, swap the two dlp_common imports at lines 47-48 so that `dlp_common::classification_cache::{...}` comes before `dlp_common::Classification`. This satisfies rustfmt's default import reordering (module paths before crate re-exports). After swapping, run `cargo fmt -p dlp-agent` to ensure the file is fully formatted, then verify with `cargo fmt --check -p dlp-agent`.
  </action>
  <verify>
    <automated>cargo fmt --check -p dlp-agent</automated>
  </verify>
  <done>`cargo fmt --check -p dlp-agent` reports no diffs; the two dlp_common imports are in correct order.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rustdoc broken intra-doc links across dlp-agent</name>
  <files>
    dlp-agent/src/print_watermark.rs
    dlp-agent/src/print_watermark_directwrite.rs
    dlp-agent/src/detection/disk.rs
    dlp-agent/src/detection/encryption.rs
    dlp-agent/src/detection/usb.rs
    dlp-agent/src/offline.rs
    dlp-agent/src/audit_emitter.rs
    dlp-agent/src/offline_audit_queue.rs
    dlp-agent/src/clipboard/listener.rs
    dlp-agent/src/server_client.rs
    dlp-agent/src/device_controller.rs
    dlp-agent/src/device_registry.rs
    dlp-agent/src/dacl_tripwire.rs
    dlp-agent/src/chrome/cache.rs
    dlp-agent/src/chrome/frame.rs
    dlp-agent/src/disk_enforcer.rs
    dlp-agent/src/device_identity.rs
    dlp-agent/src/etw_kernel_file.rs
  </files>
  <action>
    Fix all 38 rustdoc warnings from `cargo doc -p dlp-agent --no-deps` by applying these rules per file:

    **dlp-agent/src/print_watermark.rs** (2 warnings):
    - Make `FixedPageNs` enum `pub` (it is currently private but linked from public docs at line 48 and 534).
    - Remove redundant explicit link target at the module doc comment (line ~4 has a redundant link like `[text](path)` where `[text]` alone suffices).

    **dlp-agent/src/print_watermark_directwrite.rs** (1 warning):
    - Remove redundant explicit link target at the module doc comment (line ~4).

    **dlp-agent/src/detection/disk.rs** (3 warnings):
    - Lines 11, 44, 550: `EmitContext` and `VolumeDetector` do not exist in this crate. Convert `[`EmitContext`]` and `[`VolumeDetector`]` to plain backticks (`` `EmitContext` ``) or remove the brackets entirely so they are not treated as links.

    **dlp-agent/src/detection/encryption.rs** (7 warnings):
    - Lines 4, 622, 630, 662: `DiskEnumerator`, `mark_first_check_complete`, `EmitContext` do not exist in this crate. Convert to plain backticks.
    - Lines 18, 23: `EventType::Alert` and `EventType::DiskDiscovery` do not resolve. Convert to plain backticks or use full crate path `dlp_common::EventType::Alert` if the type is in dlp-common and the crate is a dependency.

    **dlp-agent/src/detection/usb.rs** (2 warnings):
    - Line 100: `get_volume_class_for_pipe_query` does not resolve. Convert to plain backticks.
    - Line 1678: `REGISTRY_RUNTIME_HANDLE` is a private item linked from public docs. Convert to plain backticks.

    **dlp-agent/src/offline.rs** (2 warnings):
    - Line 12: `offline_decision` does not resolve. Convert to plain backticks.
    - Line 4: redundant explicit link target. Remove redundant target.

    **dlp-agent/src/audit_emitter.rs** (1 warning):
    - Line 321: `SessionIdentityMap` does not resolve. Convert to plain backticks.

    **dlp-agent/src/offline_audit_queue.rs** (1 warning):
    - Line 207: `AuditEvent` does not resolve. Convert to plain backticks or use `dlp_common::audit::AuditEvent` if the path is known.

    **dlp-agent/src/clipboard/listener.rs** (2 warnings):
    - Line 369: `EmitContext` does not resolve. Convert to plain backticks.
    - Line 8: redundant explicit link target. Remove redundant target.

    **dlp-agent/src/server_client.rs** (4 warnings):
    - Lines 1010, 1039, 1109: `FLUSH_THRESHOLD`, `FLUSH_INTERVAL` are private items linked from public docs. Convert to plain backticks.

    **dlp-agent/src/device_controller.rs** (1 warning):
    - Line 245: `disable_usb_device` does not resolve. Convert to plain backticks.

    **dlp-agent/src/device_registry.rs** (2 warnings):
    - Line 12: `trust_tier_for_with_sid` does not resolve. Convert to plain backticks.
    - Lines 5, 306: `REGISTRY_POLL_INTERVAL` is private. Convert to plain backticks.

    **dlp-agent/src/dacl_tripwire.rs** (1 warning):
    - Line 147: `DENIED_MASK` is private. Convert to plain backticks.

    **dlp-agent/src/chrome/cache.rs** (1 warning):
    - Line 84: `ORIGINS_POLL_INTERVAL` is private. Convert to plain backticks.

    **dlp-agent/src/chrome/frame.rs** (1 warning):
    - Line 29: `MAX_PAYLOAD` is private. Convert to plain backticks.

    **dlp-agent/src/disk_enforcer.rs** (3 warnings):
    - Lines 3, 13, 60: `DiskEnumerator` and `DiskEnumerator::is_ready` do not resolve. Convert to plain backticks.

    **dlp-agent/src/device_identity.rs** (1 warning):
    - Line 369: `VPN_KEYWORDS` is private. Convert to plain backticks.

    **dlp-agent/src/etw_kernel_file.rs** (2 warnings):
    - Lines 15, 361: `EventType::EtwConsumerLostEvents` does not resolve. Use `dlp_common::EventType::EtwConsumerLostEvents` if the type is in dlp-common, otherwise convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify zero warnings.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "warning:" | grep -q "^0$"</automated>
  </verify>
  <done>`cargo doc -p dlp-agent --no-deps` produces zero warnings; all 38 intra-doc link issues are resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full workspace quality gates pass</name>
  <files></files>
  <action>
    Run the full quality gate suite to confirm no regressions from the formatting and documentation fixes:
    1. `cargo test -p dlp-agent print_watermark` — confirm 40 tests still pass
    2. `cargo clippy -p dlp-agent -- -D warnings` — confirm no new warnings
    3. `cargo fmt --check -p dlp-agent` — confirm formatting is clean
    4. `cargo doc -p dlp-agent --no-deps` — confirm zero warnings
    5. `cargo build -p dlp-agent` — confirm compilation succeeds
    6. `sonar-scanner` — confirm the SonarQube Quality Gate passes (requires SONAR_TOKEN exported in the session)
  </action>
  <verify>
    <automated>cargo test -p dlp-agent print_watermark && cargo clippy -p dlp-agent -- -D warnings && cargo fmt --check -p dlp-agent && cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "warning:" | grep -q "^0$" && cargo build -p dlp-agent && sonar-scanner</automated>
  </verify>
  <done>All quality gates pass: tests (40/40), clippy clean, fmt clean, doc clean (0 warnings), build clean, and SonarQube Quality Gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-comment -> rustdoc | Documentation comments are untrusted input to rustdoc; broken links degrade developer experience but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-03-01 | Information Disclosure | doc comments | accept | Broken intra-doc links do not leak sensitive data; they are developer-facing documentation only |
| T-67.1-03-02 | Denial of Service | build pipeline | accept | `cargo doc` warnings do not block `cargo build`; no runtime impact |
| T-67.1-03-SC | Tampering | source files | mitigate | All changes are formatting and documentation only; no logic changes. Verify via `cargo test` and `cargo clippy` |
</threat_model>

<verification>
- `cargo fmt --check -p dlp-agent` reports zero diffs
- `cargo doc -p dlp-agent --no-deps` reports zero warnings
- `cargo clippy -p dlp-agent -- -D warnings` passes clean
- `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed
- `cargo build -p dlp-agent` completes with zero errors
- `sonar-scanner` completes and the SonarQube Quality Gate passes
</verification>

<success_criteria>
Both UAT gaps are closed:
1. Gap 1 (minor): cargo fmt import order in classification_cache.rs is fixed and verified.
2. Gap 2 (major): All 38 rustdoc broken intra-doc link warnings across 19 dlp-agent source files are resolved.
3. Full quality gate suite passes with no regressions, including the SonarQube Quality Gate.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-03-SUMMARY.md` when done
</output>
