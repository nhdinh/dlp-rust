---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/classification_cache.rs
  - dlp-agent/src/print_watermark.rs
  - dlp-agent/src/print_watermark_directwrite.rs
autonomous: true
gap_closure: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "cargo fmt --check -p dlp-agent reports no diffs (import order fixed)"
    - "print_watermark.rs rustdoc warnings are resolved (FixedPageNs pub, redundant link removed)"
    - "print_watermark_directwrite.rs rustdoc warnings are resolved (redundant link removed)"
  artifacts:
    - path: "dlp-agent/src/classification_cache.rs"
      provides: "Correctly ordered dlp_common imports per rustfmt"
      min_lines: 1
    - path: "dlp-agent/src/print_watermark.rs"
      provides: "FixedPageNs made pub, redundant link target removed"
      min_lines: 1
    - path: "dlp-agent/src/print_watermark_directwrite.rs"
      provides: "Redundant explicit link target removed"
      min_lines: 1
  key_links:
    - from: "cargo fmt --check"
      to: "dlp-agent/src/classification_cache.rs import order"
      via: "rustfmt reordering rules"
      pattern: "dlp_common::Classification before dlp_common::classification_cache"
---

<objective>
Close UAT Gap 1 (cargo fmt import order) and the first portion of UAT Gap 2 (rustdoc warnings in print watermark files). These are automated formatting and documentation fixes with no functional changes.

Purpose: Fix the formatting violation in classification_cache.rs and resolve rustdoc intra-doc link warnings in the two print watermark modules.
Output: Three files with clean formatting and documentation.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-UAT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cargo fmt import order in classification_cache.rs</name>
  <files>dlp-agent/src/classification_cache.rs</files>
  <action>
    In dlp-agent/src/classification_cache.rs, swap the two dlp_common imports so that `dlp_common::Classification` comes before `dlp_common::classification_cache::{...}`. This satisfies rustfmt's default import reordering (lexicographic order: uppercase C sorts before lowercase c). After swapping, run `cargo fmt -p dlp-agent` to ensure the file is fully formatted, then verify with `cargo fmt --check -p dlp-agent`.
  </action>
  <verify>
    <automated>cargo fmt --check -p dlp-agent</automated>
  </verify>
  <done>`cargo fmt --check -p dlp-agent` reports no diffs; the two dlp_common imports are in correct lexicographic order.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rustdoc warnings in print_watermark.rs</name>
  <files>dlp-agent/src/print_watermark.rs</files>
  <action>
    Fix the 2 rustdoc warnings in dlp-agent/src/print_watermark.rs:
    - Make `FixedPageNs` enum `pub` (it is currently private but linked from public docs).
    - Remove redundant explicit link target at the module doc comment (convert `[text](path)` to `[text]` where the link target is implicit).
    After fixes, run `cargo doc -p dlp-agent --no-deps` and confirm the warnings for this file are gone.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "print_watermark.rs" | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from print_watermark.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Fix rustdoc warnings in print_watermark_directwrite.rs</name>
  <files>dlp-agent/src/print_watermark_directwrite.rs</files>
  <action>
    Fix the 1 rustdoc warning in dlp-agent/src/print_watermark_directwrite.rs:
    - Remove redundant explicit link target at the module doc comment (convert `[text](path)` to `[text]` where the link target is implicit).
    After fixes, run `cargo doc -p dlp-agent --no-deps` and confirm the warnings for this file are gone.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "print_watermark_directwrite.rs" | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from print_watermark_directwrite.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-comment -> rustdoc | Documentation comments are untrusted input to rustdoc; broken links degrade developer experience but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-03-01 | Information Disclosure | doc comments | accept | Broken intra-doc links do not leak sensitive data; they are developer-facing documentation only |
| T-67.1-03-02 | Denial of Service | build pipeline | accept | cargo doc warnings do not block cargo build; no runtime impact |
| T-67.1-03-SC | Tampering | source files | mitigate | All changes are formatting and documentation only; no logic changes. Verify via cargo test and cargo clippy |
</threat_model>

<verification>
- `cargo fmt --check -p dlp-agent` reports zero diffs
- `cargo doc -p dlp-agent --no-deps` reports zero warnings from print_watermark.rs and print_watermark_directwrite.rs
- `cargo clippy -p dlp-agent -- -D warnings` passes clean
- `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed
</verification>

<success_criteria>
UAT Gap 1 is closed and the print watermark portion of UAT Gap 2 is closed:
1. cargo fmt import order in classification_cache.rs is fixed and verified.
2. rustdoc warnings in print_watermark.rs and print_watermark_directwrite.rs are resolved.
3. No regressions in tests or clippy.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-03-SUMMARY.md` when done
</output>
