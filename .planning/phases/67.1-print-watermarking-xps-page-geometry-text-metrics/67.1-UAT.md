---
status: diagnosed
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
source: 67.1-01-SUMMARY.md, 67.1-02-SUMMARY.md
started: 2026-06-13T00:00:00Z
updated: 2026-06-20T16:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Unit tests pass — page geometry, font metrics, and watermark injection
expected: Running `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed.
result: pass

### 2. Build is clean for dlp-agent
expected: `cargo build -p dlp-agent` completes with zero errors.
result: pass

### 3. Clippy is clean for dlp-agent
expected: `cargo clippy -p dlp-agent -- -D warnings` passes with no warnings.
result: pass

### 4. Code formatting is clean for dlp-agent
expected: `cargo fmt --check -p dlp-agent` reports no diffs.
result: issue
reported: "Diff in dlp-agent/src/classification_cache.rs:44 — dlp_common import order not formatted"
severity: minor

### 5. Documentation builds without warnings
expected: `cargo doc -p dlp-agent --no-deps` generates docs with no warnings.
result: issue
reported: "User reported: no"
severity: major

## Summary

total: 5
passed: 3
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "`cargo fmt --check -p dlp-agent` reports no diffs"
  status: failed
  reason: "User reported: Diff in dlp-agent/src/classification_cache.rs:44 — dlp_common import order not formatted"
  severity: minor
  test: 4
  root_cause: "The two dlp_common imports in dlp-agent/src/classification_cache.rs are in the wrong order for rustfmt's default import reordering. dlp_common::classification_cache::{...} should come before dlp_common::Classification."
  artifacts:
    - path: "dlp-agent/src/classification_cache.rs"
      issue: "import order reversed at lines 47-48"
  missing:
    - "Swap the two dlp_common imports so classification_cache comes before Classification, or run cargo fmt -p dlp-agent"
  debug_session: ".planning/debug/cargo-fmt-import-order-001.md"
- truth: "`cargo doc -p dlp-agent --no-deps` generates docs with no warnings"
  status: failed
  reason: "User reported: no"
  severity: major
  test: 5
  root_cause: "Doc comments in dlp-agent use incorrect rustdoc intra-doc link syntax: bare method names without struct prefix, links to private items from public docs, missing paths for cross-module items, and a reference to a non-existent symbol UI_CONNECT_TIMEOUT_SECS."
  artifacts:
    - path: "dlp-agent/src/config.rs"
      issue: "[`load_default`] is a method on AgentConfig but lacks struct prefix"
    - path: "dlp-agent/src/ui_spawner.rs"
      issue: "[`enumerate_active_sessions`] links to private function from public docs"
    - path: "dlp-agent/src/ipc/pipe2.rs"
      issue: "[`remove_client`] is a method on Broadcaster but lacks struct prefix"
    - path: "dlp-agent/src/hook_ipc.rs"
      issue: "[`ClassificationCache`], [`BypassAlert`], [`OfflineManager::offline_decision`] have missing or incorrect paths"
    - path: "dlp-agent/src/password_stop.rs"
      issue: "[`UI_CONNECT_TIMEOUT_SECS`] references a non-existent constant"
  missing:
    - "Fix intra-doc links: add full paths for cross-module references, use Self:: prefix for methods in impl blocks, convert private or non-existent links to plain backticks"
  debug_session: ".planning/debug/rustdoc-warnings-gap2-67.1.md"
