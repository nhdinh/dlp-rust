---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 02
type: execute
wave: 2
depends_on:
  - 67.1-01-PLAN.md
files_modified:
  - dlp-agent/src/print_watermark.rs
autonomous: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "Watermark XML is injected into every .fpage in an XPS ZIP without corrupting existing markup"
    - "Injected element is a Canvas containing a single Glyphs child with UnicodeString, FontRenderingEmSize, OriginX, OriginY, Fill, and Opacity attributes"
    - "Streaming reader-writer pattern preserves all original XML events verbatim except the injection point"
    - "Injection occurs immediately before the </FixedPage> end tag"
    - "Rewritten ZIP is returned as a new in-memory Vec<u8>; original bytes are never modified in-place"
    - "All public items have doc comments per CLAUDE.md section 9.3"
  artifacts:
    - path: "dlp-agent/src/print_watermark.rs"
      provides: "inject_watermark, inject_watermark_into_fpage, rewrite_xps_with_watermark"
      exports:
        - "inject_watermark"
        - "inject_watermark_into_fpage"
        - "rewrite_xps_with_watermark"
  key_links:
    - from: "print_watermark.rs::inject_watermark"
      to: "print_watermark.rs::extract_page_geometry"
      via: "per-page geometry extraction"
      pattern: "extract_page_geometry(&xml_bytes)"
    - from: "print_watermark.rs::inject_watermark"
      to: "print_watermark.rs::compute_watermark_geometry"
      via: "per-page geometry computation"
      pattern: "compute_watermark_geometry(page_width, page_height, text, metrics, padding)"
    - from: "print_watermark.rs::inject_watermark_into_fpage"
      to: "quick_xml::Writer"
      via: "streaming XML rewrite"
      pattern: "Event::End(e) if e.name().as_ref() == b\"FixedPage\""

user_setup: []
---

<objective>
Implement the XPS ZIP watermark injection module that rewrites every `.fpage` in an XPS archive by streaming XML reader-writer injection of a `Canvas`/`Glyphs` watermark element.

Purpose: Complete the Phase 67.1 foundational modules by adding the ZIP traversal + XML injection capability that Phase 67 will call after policy evaluation to embed watermarks into spool files.
Output: Extended `print_watermark.rs` with `inject_watermark`, `inject_watermark_into_fpage`, and `rewrite_xps_with_watermark` functions, plus comprehensive unit tests with in-memory XPS fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-CONTEXT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-RESEARCH.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-PATTERNS.md
@dlp-agent/src/print_xps_parser.rs
@dlp-agent/src/print_watermark.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement XPS ZIP watermark injection functions</name>
  <files>dlp-agent/src/print_watermark.rs</files>
  <read_first>
    <file>dlp-agent/src/print_watermark.rs</file>
    <file>dlp-agent/src/print_xps_parser.rs</file>
  </read_first>
  <action>
    Extend `dlp-agent/src/print_watermark.rs` (created in Plan 01) with the following public functions, following the streaming reader-writer pattern from RESEARCH.md Pattern 1 and the ZIP traversal pattern from `print_xps_parser.rs`:

    1. `inject_watermark_into_fpage(xml: &[u8], geometry: &WatermarkGeometry) -> Result<Vec<u8>>`:
       - Create `quick_xml::Reader` from `xml` with `trim_text(true)`.
       - Create `quick_xml::Writer` backed by `Cursor::new(Vec::new())`.
       - Stream through XML events. On `Event::End(e)` where `e.name().as_ref() == b"FixedPage"`:
         a. Write `Event::Start(BytesStart::new("Canvas"))` with `Opacity="0.5"` attribute.
         b. Write `Event::Empty(BytesStart::new("Glyphs"))` with attributes:
            - `UnicodeString` = `geometry.truncated_text`
            - `FontRenderingEmSize` = `geometry.font_em_size` (format with sufficient precision, e.g., `format!("{:.4}", value)`)
            - `OriginX` = `geometry.origin_x` (format with sufficient precision)
            - `OriginY` = `geometry.origin_y` (format with sufficient precision)
            - `Fill` = `"#FF808080"` (per Claude's Discretion in CONTEXT.md)
         c. Write `Event::End(BytesEnd::new("Canvas"))`.
         d. Then write the original `Event::End(e)`.
       - For all other events (including `Event::Start(FixedPage)`, `Event::Empty(...)`, text, etc.), pass through verbatim: `writer.write_event(e)?`.
       - On `Event::Eof`, break the loop.
       - On XML parse error, return `Err` with `anyhow::bail!` including buffer position.
       - Return the writer's inner `Vec<u8>`.
       - Document with doc comments: `# Arguments`, `# Returns`, `# Errors`.

    2. `rewrite_xps_with_watermark(xps_bytes: &[u8], geometry_builder: &dyn Fn(usize, f64, f64) -> Result<WatermarkGeometry>) -> Result<Vec<u8>>`:
       - Open XPS as `zip::read::ZipArchive` from `Cursor::new(xps_bytes)`.
       - Create output buffer and `zip::write::ZipWriter`.
       - Iterate all entries in the input archive. For each entry:
         a. Copy non-`.fpage` entries verbatim to output (use `read_to_end` + `write_all` with same `SimpleFileOptions`).
         b. For `.fpage` entries (case-insensitive match on `.ends_with(".fpage")`):
            - Read XML bytes.
            - Call `extract_page_geometry(&xml_bytes)` to get `(width, height)`.
            - Call `geometry_builder(page_index, width, height)` to get `WatermarkGeometry` for this page. The `geometry_builder` closure receives the 0-based page index and the parsed dimensions.
            - Call `inject_watermark_into_fpage(&xml_bytes, &geometry)`.
            - Write the rewritten XML bytes to the output ZIP with the same file name and `SimpleFileOptions::default().compression_method(zip::CompressionMethod::Stored)` (preserve stored compression for spooler compatibility).
       - Call `zip.finish()` and return `buf.into_inner()`.
       - Document that this preserves all non-page files (`.fdoc`, `.fdseq`, `[Content_Types].xml`, resources) verbatim.

    3. `inject_watermark(xps_bytes: &[u8], text: &str, metrics: &dyn FontMetrics) -> Result<Vec<u8>>` (convenience wrapper):
       - Internally counts pages and calls `rewrite_xps_with_watermark` with a closure that:
         a. Calls `compute_watermark_geometry(page_width, page_height, text, metrics, 20.0)` (20 XPS units = 15 pt padding per D-03.2).
       - Returns the rewritten XPS bytes.
       - Document as the primary entry point for Phase 67 integration.

    4. All functions must use `?` for error propagation, `anyhow::Context` for context, and `tracing::warn!` for non-fatal skips (e.g., unreadable page XML — but in this phase, unreadable pages should return Err to caller per D-06.1).

    5. No `.unwrap()` in library code (per CLAUDE.md 9.4).
  </action>
  <verify>
    <automated>cargo test -p dlp-agent watermark::inject</automated>
  </verify>
  <done>
    - `inject_watermark_into_fpage` exists and compiles
    - `rewrite_xps_with_watermark` exists and compiles
    - `inject_watermark` convenience wrapper exists and compiles
    - `cargo test -p dlp-agent watermark::inject` passes
    - `cargo clippy -p dlp-agent -- -D warnings` passes
    - `cargo fmt --check -p dlp-agent` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for watermark injection</name>
  <files>dlp-agent/src/print_watermark.rs</files>
  <read_first>
    <file>dlp-agent/src/print_watermark.rs</file>
    <file>dlp-agent/src/print_xps_parser.rs</file>
  </read_first>
  <action>
    Add comprehensive unit tests in the existing `#[cfg(test)] mod tests` block in `print_watermark.rs`. Use the in-memory ZIP fixture pattern from `print_xps_parser.rs::tests::build_xps_fixture`.

    1. `test_inject_watermark_into_fpage_adds_canvas_glyphs`:
       - Build a single-page XPS fixture with `FixedPage Width="816" Height="1056"` containing one `Glyphs` element.
       - Create a `WatermarkGeometry` with known values: `origin_x=500.0`, `origin_y=1000.0`, `font_em_size=10.6667`, `truncated_text="Test Watermark"`, `padding=20.0`.
       - Call `inject_watermark_into_fpage` on the page XML bytes.
       - Parse the output XML with `quick_xml::Reader` and verify:
         a. The original `Glyphs` element is still present.
         b. A `Canvas` start tag appears before `FixedPage` end tag.
         c. A `Glyphs` empty tag appears inside the `Canvas` with the expected attributes.
         d. The `Canvas` end tag appears before `FixedPage` end tag.

    2. `test_inject_watermark_roundtrip`:
       - Build a two-page XPS fixture (reuse `build_xps_fixture` pattern).
       - Call `inject_watermark` with `TestFontMetrics { char_width: 5.0, line_height: 10.0 }` and text `"User | 2024-01-01 | abcdef12 | T3 | approval-123"`.
       - Verify the returned bytes are a valid ZIP (open with `ZipArchive::new(Cursor::new(...))`).
       - Verify both `.fpage` entries exist in the output.
       - Extract each page's XML and verify it contains the injected `Canvas`/`Glyphs`.

    3. `test_inject_watermark_preserves_non_page_files`:
       - Build a minimal XPS fixture with `[Content_Types].xml`, `FixedDocumentSequence.fdseq`, and `FixedDocument.fdoc`.
       - Call `inject_watermark`.
       - Verify the output ZIP still contains all three non-page files with identical content.

    4. `test_inject_watermark_empty_text`:
       - Build a single-page XPS fixture.
       - Call `inject_watermark` with empty text `""`.
       - Verify the output still has the `Canvas`/`Glyphs` element (watermark of empty string is still a watermark; Phase 67 may filter empty text before calling).

    5. `test_inject_watermark_into_fpage_missing_fixedpage`:
       - Pass XML without a `FixedPage` element (e.g., just `<Root><Child/></Root>`).
       - Verify `inject_watermark_into_fpage` returns `Err`.

    6. `test_rewrite_xps_with_watermark_per_page_geometry`:
       - Build a two-page XPS where page 1 is 816x1056 and page 2 is 612x792 (different dimensions).
       - Use `rewrite_xps_with_watermark` with a closure that asserts the received `(width, height)` match each page's actual dimensions.
       - Verify both pages are rewritten.

    7. All tests must use `assert!` / `assert_eq!` with clear failure messages. Follow Arrange-Act-Assert pattern.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent watermark</automated>
  </verify>
  <done>
    - All 6+ injection tests pass
    - `cargo test -p dlp-agent watermark` passes (all tests in module)
    - `cargo clippy -p dlp-agent -- -D warnings` passes
    - `cargo fmt --check -p dlp-agent` passes
    - No `.unwrap()` in non-test code
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XPS bytes (untrusted input) → `rewrite_xps_with_watermark` | Malformed ZIP/XML from any print job crosses here |
| Output XPS bytes → caller (Phase 67) | Rewritten spool file must not corrupt existing content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-06 | Tampering | `inject_watermark_into_fpage` (watermark outside FixedPage) | mitigate | Injection only occurs on `Event::End(FixedPage)`; watermark is emitted BEFORE the original end tag, ensuring it is a child of FixedPage |
| T-67.1-07 | Tampering | `rewrite_xps_with_watermark` (corrupting non-page files) | mitigate | Non-`.fpage` entries are copied verbatim byte-for-byte; no modification |
| T-67.1-08 | Denial of Service | `rewrite_xps_with_watermark` (zip bomb, oversized XPS) | mitigate | `zip` crate validates CRC and path safety; caller (Phase 67) controls max page count and can limit input size |
| T-67.1-09 | Information Disclosure | Watermark text in rewritten XPS | accept | Intentional per WATERMARK-01/02; watermark text is meant to be visible on printed output |
| T-67.1-10 | Denial of Service | `inject_watermark_into_fpage` (malformed XML causes panic) | mitigate | All XML errors return `Err` via `anyhow::bail!`; no `.unwrap()` in library code per CLAUDE.md 9.4 |
</threat_model>

<verification>
- `cargo test -p dlp-agent watermark` passes all unit tests (geometry + metrics + injection)
- `cargo clippy -p dlp-agent -- -D warnings` passes
- `cargo fmt --check -p dlp-agent` passes
- `cargo build -p dlp-agent` passes with zero warnings
</verification>

<success_criteria>
1. `inject_watermark_into_fpage` injects a `Canvas` element containing a `Glyphs` child immediately before the `</FixedPage>` end tag.
2. The injected `Glyphs` has all required attributes: `UnicodeString`, `FontRenderingEmSize`, `OriginX`, `OriginY`, `Fill="#FF808080"`, `Opacity="0.5"`.
3. `rewrite_xps_with_watermark` traverses an XPS ZIP, rewrites every `.fpage`, and copies all other entries verbatim.
4. `inject_watermark` convenience wrapper computes geometry per-page and returns rewritten XPS bytes.
5. Unit tests verify: round-trip ZIP validity, preserved non-page files, per-page geometry passing, missing FixedPage error handling, and correct XML structure after injection.
6. All public functions have doc comments; no `.unwrap()` in library code; clippy and fmt clean.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md` when done
</output>
