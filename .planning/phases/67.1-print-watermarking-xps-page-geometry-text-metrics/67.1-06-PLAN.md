---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 06
type: execute
wave: 2
depends_on:
  - 67.1-03
  - 67.1-04
  - 67.1-05
files_modified:
  - dlp-agent/src/config.rs
  - dlp-agent/src/ui_spawner.rs
  - dlp-agent/src/ipc/pipe2.rs
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/password_stop.rs
autonomous: true
gap_closure: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "cargo doc -p dlp-agent --no-deps generates zero warnings from config.rs, ui_spawner.rs, ipc/pipe2.rs, hook_ipc.rs, and password_stop.rs"
    - "cargo doc -p dlp-agent --no-deps generates zero warnings across the entire dlp-agent package"
    - "sonar-scanner completes and the SonarQube Quality Gate passes"
  artifacts:
    - path: "dlp-agent/src/config.rs"
      provides: "load_default link resolved with proper struct prefix"
      min_lines: 1
    - path: "dlp-agent/src/ui_spawner.rs"
      provides: "enumerate_active_sessions private link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/ipc/pipe2.rs"
      provides: "remove_client link resolved with Broadcaster:: prefix"
      min_lines: 1
    - path: "dlp-agent/src/hook_ipc.rs"
      provides: "ClassificationCache, BypassAlert, OfflineManager::offline_decision links fixed with proper paths"
      min_lines: 1
    - path: "dlp-agent/src/password_stop.rs"
      provides: "UI_CONNECT_TIMEOUT_SECS link resolved or converted to plain backticks"
      min_lines: 1
  key_links:
    - from: "cargo doc --no-deps"
      to: "all 5 source files in this plan"
      via: "rustdoc intra-doc link resolution"
      pattern: "grep -c 'warning:' == 0 after all fixes"
---

<objective>
Close the remaining UAT Gap 2 rustdoc warnings in config.rs, ui_spawner.rs, ipc/pipe2.rs, hook_ipc.rs, and password_stop.rs, then run the full quality gate suite including sonar-scanner. These are documentation-only fixes with no functional changes.

Purpose: Eliminate the final rustdoc warnings from the dlp-agent package and verify all quality gates pass.
Output: Five files with resolved intra-doc link issues, plus verified clean cargo doc, cargo clippy, cargo test, cargo fmt, and SonarQube Quality Gate.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-UAT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rustdoc warnings in config.rs and ui_spawner.rs</name>
  <files>
    dlp-agent/src/config.rs
    dlp-agent/src/ui_spawner.rs
  </files>
  <action>
    Fix rustdoc warnings in two files by resolving or converting intra-doc links:

    **dlp-agent/src/config.rs** (1 warning):
    - Line 418: `[`load_default`]` is a method on `AgentConfig` but lacks struct prefix. Change to `[`AgentConfig::load_default`]` or `[`Self::load_default`]` from within the impl block context.

    **dlp-agent/src/ui_spawner.rs** (1 warning):
    - Line 109: `[`enumerate_active_sessions`]` links to a private function. Convert to plain backticks (`` `enumerate_active_sessions` ``) — private items cannot be linked in public docs.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these two files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(config|ui_spawner)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from config.rs or ui_spawner.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rustdoc warnings in ipc/pipe2.rs and hook_ipc.rs</name>
  <files>
    dlp-agent/src/ipc/pipe2.rs
    dlp-agent/src/hook_ipc.rs
  </files>
  <action>
    Fix rustdoc warnings in two files by resolving or converting intra-doc links:

    **dlp-agent/src/ipc/pipe2.rs** (1 warning):
    - Line 60: `[`remove_client`]` links to `Broadcaster::remove_client` but lacks the struct prefix. Change to `[`Broadcaster::remove_client`]` or `[`Self::remove_client`]` from within the impl block.

    **dlp-agent/src/hook_ipc.rs** (3 warnings):
    - Line 173: `[`ClassificationCache`]` does not resolve. Use full path `crate::classification_cache::ClassificationCache` if the type is in that module, or convert to plain backticks.
    - Line 216: `[`BypassAlert`]` is from `dlp_common::hook_ipc::BypassAlert` (external crate). Use full path `dlp_common::hook_ipc::BypassAlert` in the doc link, or convert to plain backticks.
    - Line 315: `[`OfflineManager::offline_decision`]` may not resolve. Use `crate::offline::OfflineManager::offline_decision` or convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these two files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(ipc/pipe2|hook_ipc)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from ipc/pipe2.rs or hook_ipc.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Fix rustdoc warning in password_stop.rs and run full quality gates</name>
  <files>dlp-agent/src/password_stop.rs</files>
  <action>
    Fix the 1 rustdoc warning in dlp-agent/src/password_stop.rs:
    - Line 262: `[`UI_CONNECT_TIMEOUT_SECS`]` references a non-existent or private constant. Verify if the constant exists; if it does not exist, remove the link and use plain backticks. If it exists but is private, either make it pub or convert to plain backticks.

    After the fix, run the full quality gate suite to confirm no regressions from all documentation fixes across Plans 03-06:
    1. `cargo test -p dlp-agent print_watermark` — confirm 40 tests still pass
    2. `cargo clippy -p dlp-agent -- -D warnings` — confirm no new warnings
    3. `cargo fmt --check -p dlp-agent` — confirm formatting is clean
    4. `cargo doc -p dlp-agent --no-deps` — confirm zero warnings across the entire dlp-agent package
    5. `cargo build -p dlp-agent` — confirm compilation succeeds
    6. `sonar-scanner` — confirm the SonarQube Quality Gate passes (requires SONAR_TOKEN exported in the session per CLAUDE.md section 9.16)
  </action>
  <verify>
    <automated>cargo test -p dlp-agent print_watermark && cargo clippy -p dlp-agent -- -D warnings && cargo fmt --check -p dlp-agent && cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "warning:" | grep -q "^0$" && cargo build -p dlp-agent && sonar-scanner</automated>
  </verify>
  <done>All quality gates pass: tests (40/40), clippy clean, fmt clean, doc clean (0 warnings across entire dlp-agent package), build clean, and SonarQube Quality Gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-comment -> rustdoc | Documentation comments are untrusted input to rustdoc; broken links degrade developer experience but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-06-01 | Information Disclosure | doc comments | accept | Broken intra-doc links do not leak sensitive data; they are developer-facing documentation only |
| T-67.1-06-02 | Denial of Service | build pipeline | accept | cargo doc warnings do not block cargo build; no runtime impact |
| T-67.1-06-SC | Tampering | source files | mitigate | All changes are documentation only; no logic changes. Verify via cargo test, cargo clippy, and sonar-scanner |
</threat_model>

<verification>
- `cargo doc -p dlp-agent --no-deps` reports zero warnings from all 5 files in this plan AND zero warnings across the entire dlp-agent package
- `cargo clippy -p dlp-agent -- -D warnings` passes clean
- `cargo fmt --check -p dlp-agent` reports zero diffs
- `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed
- `cargo build -p dlp-agent` completes with zero errors
- `sonar-scanner` completes and the SonarQube Quality Gate passes
</verification>

<success_criteria>
All UAT gaps are closed:
1. The final 5 rustdoc warning files (config.rs, ui_spawner.rs, ipc/pipe2.rs, hook_ipc.rs, password_stop.rs) are resolved.
2. `cargo doc -p dlp-agent --no-deps` produces zero warnings across the entire dlp-agent package.
3. Full quality gate suite passes with no regressions, including the SonarQube Quality Gate.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-06-SUMMARY.md` when done
</output>
