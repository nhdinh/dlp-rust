---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
verified: 2026-06-13T00:00:00Z
status: passed
score: 27/27 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 67.1: Print Watermarking — XPS Page Geometry + Text Metrics Verification Report

**Phase Goal:** Implement foundational XPS page geometry parser, text metrics trait, DirectWrite font measurement, and XPS ZIP watermark injection for Phase 67.1 print watermarking.

**Verified:** 2026-06-13
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Plan 01)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | Page geometry (width, height) is extracted from every .fpage XML in an XPS ZIP | VERIFIED | `extract_page_geometry()` in `print_watermark.rs:189` parses `FixedPage` Width/Height attributes via `quick_xml::Reader` streaming parse. Test `extract_page_geometry_valid` passes. |
| 2   | Missing page dimensions fall back to 816x1056 with a logged warning | VERIFIED | `print_watermark.rs:237-246` returns `(816.0, 1056.0)` with `tracing::warn!`. Test `extract_page_geometry_missing_dimensions_defaults` passes. |
| 3   | FontMetrics trait exists with measure(text, family, em_size) -> Result<(width, height)> in XPS units | VERIFIED | `print_watermark.rs:132-149` defines `FontMetrics: Send + Sync` with `measure()` method. Doc comment confirms XPS units. |
| 4   | TestFontMetrics stub returns deterministic per-character widths for CI | VERIFIED | `print_watermark.rs:156-169` implements `FontMetrics` with `text.chars().count() * char_width`. Tests pass. |
| 5   | DirectWriteFontMetrics measures text via windows-rs COM with ComGuard RAII | VERIFIED | `print_watermark_directwrite.rs:98-189` implements `DirectWriteFontMetrics` with internal `ComGuard`. 5 Windows tests pass. |
| 6   | DirectWrite module is gated with #[cfg(windows)] in lib.rs; non-Windows CI compiles stub | VERIFIED | `lib.rs:109-113` has `#[cfg(windows)]` and `#[cfg(not(windows))]` module declarations. Non-Windows stub at `print_watermark_directwrite.rs:195-204`. |
| 7   | WatermarkGeometry struct holds origin, font size, truncated text, and padding | VERIFIED | `print_watermark.rs:108-123` defines struct with all 7 fields. Derives `Debug, Clone, PartialEq`. |
| 8   | Watermark text is truncated with ellipsis when measured width exceeds available space | VERIFIED | `truncate_with_ellipsis()` at `print_watermark.rs:310-355` iteratively truncates. Tests `truncate_with_ellipsis_*` pass. |
| 9   | Origin is computed at bottom-right with 15 pt (20 XPS unit) padding | VERIFIED | `compute_watermark_geometry()` at `print_watermark.rs:377-409` computes `origin_x = page_width - padding - final_width`, `origin_y = page_height - padding`. Default padding = 20.0 XPS units. |
| 10  | All public items have doc comments per CLAUDE.md section 9.3 | VERIFIED | Every public function, struct, trait, and method has doc comments with `# Arguments`, `# Returns`, `# Errors`, and `# Examples` where applicable. |
| 11  | Unit conversion is explicit: pt_to_xps_units(pt) = pt * 96.0 / 72.0 | VERIFIED | `print_watermark.rs:82-84` implements exact formula. Doc comment documents 96 DPI basis. Tests `pt_to_xps_units_8pt` and `pt_to_xps_units_15pt` pass. |
| 12  | DirectWrite-to-XPS unit conversion documented as identity (DIPs == XPS units) | VERIFIED | `dip_to_xps_units()` at `print_watermark.rs:100-102` is identity function with doc comment explaining DIP == XPS unit equivalence. |
| 13  | COM initialization uses internal ComGuard RAII inside measure() | VERIFIED | `print_watermark_directwrite.rs:53-82` defines `ComGuard` with `CoInitializeEx`/`CoUninitialize`. `measure()` starts with `let _com = ComGuard::new()?`. |
| 14  | Every Windows test creates its own ComGuard | VERIFIED | All 5 Windows tests in `print_watermark_directwrite.rs:216-277` begin with `let _com = ComGuard::new()?`. |
| 15  | CoInitializeEx S_FALSE handling documented | VERIFIED | `print_watermark_directwrite.rs:12-14` and `measure()` doc comment document `S_FALSE` (0x1) is treated as success by windows-rs. `test_directwrite_com_guard_nested_init` verifies. |

### Observable Truths (Plan 02)

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 16  | Watermark XML is injected into every .fpage in an XPS ZIP without corrupting existing markup | VERIFIED | `inject_watermark_into_fpage()` at `print_watermark.rs:538-620` uses streaming reader-writer. Test `test_inject_watermark_roundtrip` validates both pages rewritten. |
| 17  | Injected element is a Canvas containing a Glyphs child with required attributes | VERIFIED | `write_watermark_canvas()` at `print_watermark.rs:645-710` emits `Canvas` start, `Glyphs` empty (with UnicodeString, FontRenderingEmSize, OriginX, OriginY, Fill, Opacity, FontUri), and `Canvas` end. Test `test_inject_watermark_into_fpage_adds_canvas_glyphs` verifies. |
| 18  | Streaming reader-writer preserves all original XML events verbatim except injection point | VERIFIED | `inject_watermark_into_fpage()` explicitly handles all `Event` variants (Start, Empty, End, Text, CData, Comment, Decl, PI, DocType) with verbatim passthrough. Test `test_inject_watermark_preserves_non_page_files` verifies byte-for-byte preservation. |
| 19  | Injection occurs before </FixedPage> using local-name matching | VERIFIED | `print_watermark.rs:574-578` matches `e.local_name().as_ref() == b"FixedPage"` on `Event::End`. Tests for namespaced, prefixed, and unqualified FixedPage all pass. |
| 20  | Rewritten ZIP returned as new Vec<u8>; original never modified in-place | VERIFIED | `rewrite_xps_with_watermark()` at `print_watermark.rs:738-783` creates `Cursor::new(Vec::new())` and returns `out_buf.into_inner()`. Input `xps_bytes: &[u8]` is immutable borrow. |
| 21  | Non-.fpage entries copied verbatim with original compression preserved | VERIFIED | `print_watermark.rs:771-778` reads `file.compression()` and passes same method to `SimpleFileOptions`. Test `test_inject_watermark_preserves_compression_method` verifies byte-identical preservation. |
| 22  | FontUri references existing font part; omission accepted if none found | VERIFIED | `find_font_uri_in_xps()` at `print_watermark.rs:449-505` scans for first `FontUri`. `inject_watermark()` passes `font_uri` as `Option`. Test `test_find_font_uri_in_xps_returns_none_when_no_font` verifies. |
| 23  | Namespace handling via FixedPageNs enum (Unqualified/Default/Prefixed) | VERIFIED | `FixedPageNs` enum at `print_watermark.rs:421-428`. `parse_fixed_page_ns()` at `623-642` parses `xmlns` and `xmlns:*`. Tests for default, prefixed, and unqualified namespaces all pass. |
| 24  | trim_text(false) preserves whitespace text nodes | VERIFIED | `print_watermark.rs:545` sets `reader.config_mut().trim_text(false)`. Test `test_inject_watermark_into_fpage_whitespace_preserved` verifies Text events present. |
| 25  | Missing FixedPage detection returns Err | VERIFIED | `print_watermark.rs:610-617` tracks `saw_fixed_page` and `injected_watermark` flags, returns `Err` if either condition fails. Tests `test_inject_watermark_into_fpage_missing_fixedpage` and `test_inject_watermark_truncated_fpage` pass. |
| 26  | OPC relationship deferred; font-substitution risk accepted and documented | VERIFIED | Module-level doc comment at `print_watermark.rs:19-59` documents FontUri strategy, OPC deferred work, namespace handling, and compression preservation. |
| 27  | Structural validation verifies output XPS integrity | VERIFIED | `validate_xps_structure()` at `print_watermark.rs:849-918` checks `[Content_Types].xml`, `.fpage` presence, and `FixedPage` tags. Tests `test_validate_xps_structure_passes_valid` and `test_validate_xps_structure_fails_no_fpage` pass. |

**Score:** 27/27 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `dlp-agent/src/print_watermark.rs` | WatermarkGeometry, FontMetrics, TestFontMetrics, extract_page_geometry, compute_watermark_geometry, build_watermark_text, truncate_with_ellipsis, pt_to_xps_units, dip_to_xps_units, find_font_uri_in_xps, inject_watermark_into_fpage, rewrite_xps_with_watermark, inject_watermark, validate_xps_structure, FixedPageNs | VERIFIED | 1762 lines. All 14 public symbols present. 40 tests pass. |
| `dlp-agent/src/print_watermark_directwrite.rs` | DirectWriteFontMetrics, ComGuard | VERIFIED | 290 lines. DirectWriteFontMetrics + ComGuard RAII on Windows. Non-Windows stub compiles. 5 Windows tests + 1 non-Windows stub test pass. |
| `dlp-agent/src/lib.rs` | Module declarations for print_watermark and print_watermark_directwrite | VERIFIED | Lines 107-113: `pub mod print_watermark` (unconditional), `#[cfg(windows)] pub mod print_watermark_directwrite`, `#[cfg(not(windows))] pub mod print_watermark_directwrite`. |
| `dlp-agent/Cargo.toml` | Win32_Graphics_DirectWrite feature flag | VERIFIED | Line 95: `"Win32_Graphics_DirectWrite"` in windows features list. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `print_watermark.rs::compute_watermark_geometry` | `print_watermark_directwrite.rs::DirectWriteFontMetrics` | FontMetrics trait object | WIRED | `compute_watermark_geometry` takes `&dyn FontMetrics`; `DirectWriteFontMetrics` implements `FontMetrics`. |
| `print_watermark.rs::extract_page_geometry` | `quick_xml::Reader` | streaming XML parse | WIRED | Uses `quick_xml::Reader` with `trim_text(true)` and `read_event_into()`. |
| `print_watermark.rs::inject_watermark_into_fpage` | `quick_xml::Writer` | streaming XML rewrite | WIRED | Uses `quick_xml::Writer` with `Cursor::new(Vec::new())`. |
| `print_watermark.rs::rewrite_xps_with_watermark` | `zip::write::ZipWriter` | ZIP archive rewrite | WIRED | Uses `zip::write::ZipWriter::new()` with `SimpleFileOptions`. |
| `print_watermark_directwrite.rs` | `windows::Win32::Graphics::DirectWrite` | windows-rs COM bindings | WIRED | Imports `DWriteCreateFactory`, `IDWriteFactory`, `IDWriteTextLayout`, `DWRITE_TEXT_METRICS`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `extract_page_geometry` | (width, height) | FixedPage XML attributes | Yes — parsed from XML via `f64::from_str` | FLOWING |
| `DirectWriteFontMetrics::measure` | (width, height) | DirectWrite COM `GetMetrics` | Yes — calls `DWriteCreateFactory` → `CreateTextFormat` → `CreateTextLayout` → `GetMetrics` | FLOWING |
| `compute_watermark_geometry` | WatermarkGeometry | `extract_page_geometry` + `metrics.measure` | Yes — delegates to real data sources | FLOWING |
| `inject_watermark_into_fpage` | rewritten XML | `inject_watermark_into_fpage` + geometry | Yes — constructs Canvas/Glyphs from geometry | FLOWING |
| `rewrite_xps_with_watermark` | output ZIP bytes | per-page geometry + input ZIP | Yes — rewrites each .fpage with computed geometry | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All print_watermark tests pass | `cargo test -p dlp-agent --lib print_watermark` | 40 passed, 0 failed | PASS |
| dlp-agent builds with zero warnings | `cargo build -p dlp-agent` | Finished with no warnings | PASS |
| Clippy passes with -D warnings | `cargo clippy -p dlp-agent -- -D warnings` | Finished clean | PASS |
| Format check passes | `cargo fmt --check -p dlp-agent` | No output (clean) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| WATERMARK-01 | 67.1-01-PLAN.md, 67.1-02-PLAN.md | Print watermarking: overlay user UPN, timestamp, device fingerprint, tier, approval ID on XPS output before spooling | SATISFIED | `build_watermark_text()` builds pipe-delimited metadata string. `inject_watermark()` rewrites XPS ZIP with Canvas/Glyphs watermark on every page. All foundational components implemented. |
| WATERMARK-02 | 67.1-01-PLAN.md, 67.1-02-PLAN.md | Watermark policy obligation: obligations field in EvaluateResponse includes watermark flag; print enforcer applies watermark when Decision::Allow + obligations.contains(Watermark) | PARTIALLY SATISFIED | Foundation complete: `inject_watermark()` is the entry point that Phase 67 will call. The `EvaluateResponse` obligation wiring and print enforcer integration are deferred to Phase 67 (the consumer phase). Phase 67.1 delivers the building blocks, not the full policy integration. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| None | — | — | — | No TBD/FIXME/XXX markers found. No placeholder code. No `.unwrap()` in library code. All `unwrap()` calls are in `#[cfg(test)]` modules only. |

### Human Verification Required

None. All behaviors are verifiable programmatically via unit tests and compilation checks.

### Gaps Summary

No gaps found. All 27 must-have truths from both plans are verified. All artifacts exist, are substantive, and are wired correctly. All 40 tests pass. Build, clippy, and fmt are clean.

---

_Verified: 2026-06-13_
_Verifier: Claude (gsd-verifier)_
