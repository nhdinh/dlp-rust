---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/detection/disk.rs
  - dlp-agent/src/detection/encryption.rs
  - dlp-agent/src/detection/usb.rs
  - dlp-agent/src/device_controller.rs
  - dlp-agent/src/device_registry.rs
  - dlp-agent/src/device_identity.rs
  - dlp-agent/src/dacl_tripwire.rs
  - dlp-agent/src/chrome/cache.rs
autonomous: true
gap_closure: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "cargo doc -p dlp-agent --no-deps generates zero warnings from detection/, device_, dacl_tripwire.rs, and chrome/cache.rs files"
  artifacts:
    - path: "dlp-agent/src/detection/disk.rs"
      provides: "EmitContext and VolumeDetector links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/detection/encryption.rs"
      provides: "DiskEnumerator, EventType, mark_first_check_complete links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/detection/usb.rs"
      provides: "get_volume_class_for_pipe_query and REGISTRY_RUNTIME_HANDLE links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/device_controller.rs"
      provides: "disable_usb_device link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/device_registry.rs"
      provides: "trust_tier_for_with_sid and REGISTRY_POLL_INTERVAL links converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/device_identity.rs"
      provides: "VPN_KEYWORDS private link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/dacl_tripwire.rs"
      provides: "DENIED_MASK private link converted to plain backticks"
      min_lines: 1
    - path: "dlp-agent/src/chrome/cache.rs"
      provides: "ORIGINS_POLL_INTERVAL private link converted to plain backticks"
      min_lines: 1
  key_links:
    - from: "cargo doc --no-deps"
      to: "all 8 source files in this plan"
      via: "rustdoc intra-doc link resolution"
      pattern: "grep -c 'warning:.*disk\\.rs\\|warning:.*encryption\\.rs\\|warning:.*usb\\.rs\\|warning:.*device_controller\\.rs\\|warning:.*device_registry\\.rs\\|warning:.*device_identity\\.rs\\|warning:.*dacl_tripwire\\.rs\\|warning:.*chrome/cache\\.rs' == 0"
---

<objective>
Close a portion of UAT Gap 2: resolve rustdoc broken intra-doc link warnings in detection, device, DACL, and chrome modules. These are documentation-only fixes with no functional changes.

Purpose: Eliminate rustdoc warnings from 8 dlp-agent source files so that `cargo doc -p dlp-agent --no-deps` moves closer to zero warnings.
Output: Eight files with resolved intra-doc link issues.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-UAT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rustdoc warnings in detection modules</name>
  <files>
    dlp-agent/src/detection/disk.rs
    dlp-agent/src/detection/encryption.rs
    dlp-agent/src/detection/usb.rs
  </files>
  <action>
    Fix rustdoc warnings in the three detection module files by converting unresolved intra-doc links to plain backticks:

    **dlp-agent/src/detection/disk.rs** (3 warnings):
    - Lines 11, 44, 550: `EmitContext` and `VolumeDetector` do not exist in this crate. Convert `[`EmitContext`]` and `[`VolumeDetector`]` to plain backticks (`` `EmitContext` ``) or remove the brackets entirely so they are not treated as links.

    **dlp-agent/src/detection/encryption.rs** (7 warnings):
    - Lines 4, 622, 630, 662: `DiskEnumerator`, `mark_first_check_complete`, `EmitContext` do not exist in this crate. Convert to plain backticks.
    - Lines 18, 23: `EventType::Alert` and `EventType::DiskDiscovery` do not resolve. Convert to plain backticks or use full crate path `dlp_common::EventType::Alert` if the type is in dlp-common and the crate is a dependency.

    **dlp-agent/src/detection/usb.rs** (2 warnings):
    - Line 100: `get_volume_class_for_pipe_query` does not resolve. Convert to plain backticks.
    - Line 1678: `REGISTRY_RUNTIME_HANDLE` is a private item linked from public docs. Convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these three files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*detection/(disk|encryption|usb)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from detection/disk.rs, detection/encryption.rs, or detection/usb.rs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix rustdoc warnings in device and DACL modules</name>
  <files>
    dlp-agent/src/device_controller.rs
    dlp-agent/src/device_registry.rs
    dlp-agent/src/device_identity.rs
    dlp-agent/src/dacl_tripwire.rs
  </files>
  <action>
    Fix rustdoc warnings in the four device/DACL files by converting unresolved or private intra-doc links to plain backticks:

    **dlp-agent/src/device_controller.rs** (1 warning):
    - Line 245: `disable_usb_device` does not resolve. Convert to plain backticks.

    **dlp-agent/src/device_registry.rs** (2 warnings):
    - Line 12: `trust_tier_for_with_sid` does not resolve. Convert to plain backticks.
    - Lines 5, 306: `REGISTRY_POLL_INTERVAL` is private. Convert to plain backticks.

    **dlp-agent/src/device_identity.rs** (1 warning):
    - Line 369: `VPN_KEYWORDS` is private. Convert to plain backticks.

    **dlp-agent/src/dacl_tripwire.rs** (1 warning):
    - Line 147: `DENIED_MASK` is private. Convert to plain backticks.

    After all fixes, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from these four files.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -E "warning:.*(device_controller|device_registry|device_identity|dacl_tripwire)\.rs" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from device_controller.rs, device_registry.rs, device_identity.rs, or dacl_tripwire.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Fix rustdoc warnings in chrome/cache.rs</name>
  <files>dlp-agent/src/chrome/cache.rs</files>
  <action>
    Fix the 1 rustdoc warning in dlp-agent/src/chrome/cache.rs:
    - Line 84: `ORIGINS_POLL_INTERVAL` is a private item linked from public docs. Convert to plain backticks.

    After fix, run `cargo doc -p dlp-agent --no-deps` and verify no warnings originate from this file.
  </action>
  <verify>
    <automated>cargo doc -p dlp-agent --no-deps 2>&1 | grep -c "chrome/cache.rs" | grep -q "^0$"</automated>
  </verify>
  <done>No rustdoc warnings originate from chrome/cache.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-comment -> rustdoc | Documentation comments are untrusted input to rustdoc; broken links degrade developer experience but do not affect runtime security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-04-01 | Information Disclosure | doc comments | accept | Broken intra-doc links do not leak sensitive data; they are developer-facing documentation only |
| T-67.1-04-02 | Denial of Service | build pipeline | accept | cargo doc warnings do not block cargo build; no runtime impact |
| T-67.1-04-SC | Tampering | source files | mitigate | All changes are documentation only; no logic changes. Verify via cargo test and cargo clippy |
</threat_model>

<verification>
- `cargo doc -p dlp-agent --no-deps` reports zero warnings from all 8 files in this plan
- `cargo clippy -p dlp-agent -- -D warnings` passes clean
- `cargo test -p dlp-agent print_watermark` shows 40 passed, 0 failed
</verification>

<success_criteria>
Rustdoc warnings in 8 dlp-agent files are resolved:
1. detection/disk.rs, detection/encryption.rs, detection/usb.rs — all link warnings resolved.
2. device_controller.rs, device_registry.rs, device_identity.rs, dacl_tripwire.rs — all link warnings resolved.
3. chrome/cache.rs — link warning resolved.
4. No regressions in tests or clippy.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-04-SUMMARY.md` when done
</output>
