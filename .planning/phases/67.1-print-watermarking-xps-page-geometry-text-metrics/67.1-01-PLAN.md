---
phase: 67.1-print-watermarking-xps-page-geometry-text-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/print_watermark.rs
  - dlp-agent/src/lib.rs
  - dlp-agent/Cargo.toml
autonomous: true
requirements:
  - WATERMARK-01
  - WATERMARK-02

must_haves:
  truths:
    - "Page geometry (width, height) is extracted from every .fpage XML in an XPS ZIP"
    - "Missing page dimensions fall back to 816x1056 with a logged warning"
    - "FontMetrics trait exists with measure(text, family, em_size) -> (width, height) in XPS units"
    - "TestFontMetrics stub returns deterministic per-character widths for CI"
    - "DirectWriteFontMetrics implementation measures text via windows-rs COM"
    - "WatermarkGeometry struct holds origin, font size, truncated text, and padding"
    - "Watermark text is truncated with ellipsis when measured width exceeds available space"
    - "Origin is computed at bottom-right with 15 pt (20 XPS unit) padding"
    - "All public items have doc comments per CLAUDE.md section 9.3"
  artifacts:
    - path: "dlp-agent/src/print_watermark.rs"
      provides: "WatermarkGeometry, FontMetrics trait, TestFontMetrics, geometry computation, text builder, truncation"
      exports:
        - "WatermarkGeometry"
        - "FontMetrics"
        - "TestFontMetrics"
        - "compute_watermark_geometry"
        - "build_watermark_text"
        - "extract_page_geometry"
    - path: "dlp-agent/src/print_watermark_directwrite.rs"
      provides: "DirectWriteFontMetrics implementation"
      exports:
        - "DirectWriteFontMetrics"
    - path: "dlp-agent/src/lib.rs"
      provides: "Module declarations for print_watermark and print_watermark_directwrite"
    - path: "dlp-agent/Cargo.toml"
      provides: "Win32_Graphics_DirectWrite feature flag"
  key_links:
    - from: "print_watermark.rs::compute_watermark_geometry"
      to: "print_watermark_directwrite.rs::DirectWriteFontMetrics"
      via: "FontMetrics trait object"
      pattern: "&dyn FontMetrics"
    - from: "print_watermark.rs::extract_page_geometry"
      to: "quick_xml::Reader"
      via: "streaming XML parse"
      pattern: "quick_xml::events::Event::Empty|Start"
    - from: "print_watermark_directwrite.rs"
      to: "windows::Win32::Graphics::DirectWrite"
      via: "windows-rs COM bindings"
      pattern: "DWriteCreateFactory|IDWriteFactory|IDWriteTextLayout"

user_setup: []
---

<objective>
Implement the foundational XPS page geometry parser, text metrics trait, and watermark geometry computation for Phase 67.1 print watermarking.

Purpose: Provide the core building blocks (page dimension extraction, font measurement abstraction, and placement math) that Phase 67 will call to inject watermarks into XPS spool files.
Output: `print_watermark.rs` with geometry/types/trait/test stub, `print_watermark_directwrite.rs` with DirectWrite implementation, updated `lib.rs` module declarations, and `Cargo.toml` feature flag.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-CONTEXT.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-RESEARCH.md
@.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-PATTERNS.md
@dlp-agent/src/print_xps_parser.rs
@dlp-agent/src/lib.rs
@dlp-agent/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Win32_Graphics_DirectWrite feature and module declarations</name>
  <files>dlp-agent/Cargo.toml, dlp-agent/src/lib.rs</files>
  <read_first>
    <file>dlp-agent/Cargo.toml</file>
    <file>dlp-agent/src/lib.rs</file>
  </read_first>
  <action>
    1. In `dlp-agent/Cargo.toml`, add `"Win32_Graphics_DirectWrite"` to the `windows` crate features list (per D-02, RESEARCH.md Open Question 1). Place it near the existing `"Win32_Graphics_Gdi"` and `"Win32_Graphics_Printing"` entries for readability.
    2. In `dlp-agent/src/lib.rs`, add two new module declarations following the existing `#[cfg(windows)]` print module pattern:
       - `pub mod print_watermark;`
       - `pub mod print_watermark_directwrite;`
       Place them after `print_enforcer` and before `device_controller` to keep print-related modules grouped.
    3. Verify `cargo check -p dlp-agent` compiles with no errors after these changes.
  </action>
  <verify>
    <automated>cargo check -p dlp-agent</automated>
  </verify>
  <done>
    - `Win32_Graphics_DirectWrite` appears in `dlp-agent/Cargo.toml` windows features list
    - `print_watermark` and `print_watermark_directwrite` module declarations exist in `lib.rs`
    - `cargo check -p dlp-agent` passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement print_watermark.rs core module</name>
  <files>dlp-agent/src/print_watermark.rs</files>
  <read_first>
    <file>dlp-agent/src/print_xps_parser.rs</file>
    <file>dlp-agent/src/lib.rs</file>
  </read_first>
  <action>
    Create `dlp-agent/src/print_watermark.rs` with the following public API, following the patterns from `print_xps_parser.rs` and CLAUDE.md Rust standards:

    1. `WatermarkGeometry` struct with fields: `origin_x: f64`, `origin_y: f64`, `font_em_size: f64`, `page_width: f64`, `page_height: f64`, `truncated_text: String`, `padding: f64`. Derive `Debug`, `Clone`, `PartialEq`.

    2. `FontMetrics` trait (per D-02.3): `pub trait FontMetrics: Send + Sync { fn measure(&self, text: &str, family: &str, em_size: f64) -> Result<(f64, f64)>; }`. Document that width and height are returned in XPS units (1/96 inch).

    3. `TestFontMetrics` struct (per D-02.3) with `char_width: f64`, `line_height: f64`. Implement `FontMetrics` returning `text.chars().count() as f64 * self.char_width` for width and `self.line_height` for height. Document that this is an approximation for CI on non-Windows runners.

    4. `extract_page_geometry(xml: &[u8]) -> Result<(f64, f64)>` (per D-03): Use `quick_xml::Reader` streaming parse to find the first `FixedPage` start/empty event. Extract `Width` and `Height` attributes via `decode_and_unescape_value`. Parse with `f64::from_str`. If missing, log `tracing::warn!` and return `(816.0, 1056.0)` default. If `FixedPage` not found, return `Err`. Follow the exact error handling pattern from `print_xps_parser.rs::parse_page_text`.

    5. `build_watermark_text(username, timestamp, device_fingerprint_prefix, tier_label, approval_id) -> String` (per D-05.3): Format as `"{username} | {timestamp} | {fp_prefix} | {tier} | {approval_id}"`.

    6. `truncate_with_ellipsis(text, metrics, em_size, available_width) -> Result<String>` (per D-04.2, Pitfall 5): Iteratively remove the last character and re-measure (text + "...") until width <= available_width or only "..." remains (return "..."). Guard against infinite loop by checking if text is empty after removal. Log `tracing::warn!` if truncation reaches minimum.

    7. `compute_watermark_geometry(page_width, page_height, text, metrics, padding) -> Result<WatermarkGeometry>` (per D-03, D-04): Compute `em_size = 8.0 * (96.0 / 72.0)` (≈10.6667 XPS units). Measure text via `metrics.measure(text, "Arial", em_size)`. If width > `page_width - 2*padding`, call `truncate_with_ellipsis`. Re-measure truncated text. Compute `origin_x = page_width - padding - final_width`, `origin_y = page_height - padding`. Return `WatermarkGeometry`.

    8. All functions must have doc comments with `# Arguments`, `# Returns`, `# Errors`, and `# Examples` where applicable (per CLAUDE.md 9.3).

    9. No `.unwrap()` in library code; use `?` or `.context()` with `anyhow` (per CLAUDE.md 9.4, 9.5).

    10. Unit tests in `#[cfg(test)] mod tests` covering:
        - `extract_page_geometry` with valid Width/Height
        - `extract_page_geometry` with missing dimensions (defaults to 816x1056)
        - `extract_page_geometry` with no FixedPage element (returns Err)
        - `TestFontMetrics::measure` returns deterministic values
        - `compute_watermark_geometry` with text that fits (no truncation)
        - `compute_watermark_geometry` with text that overflows (truncation with ellipsis)
        - `compute_watermark_geometry` origin at bottom-right with 20-unit padding
        - `build_watermark_text` produces correct pipe-delimited format
        - `truncate_with_ellipsis` handles edge case where even "..." exceeds width
  </action>
  <verify>
    <automated>cargo test -p dlp-agent print_watermark</automated>
  </verify>
  <done>
    - `cargo test -p dlp-agent print_watermark` passes all tests
    - `cargo clippy -p dlp-agent -- -D warnings` passes
    - `cargo fmt --check -p dlp-agent` passes
    - All public items have doc comments
    - No `.unwrap()` in non-test code
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement DirectWriteFontMetrics in print_watermark_directwrite.rs</name>
  <files>dlp-agent/src/print_watermark_directwrite.rs</files>
  <read_first>
    <file>dlp-agent/src/print_watermark.rs</file>
    <file>dlp-agent/src/print_job_info.rs</file>
  </read_first>
  <action>
    Create `dlp-agent/src/print_watermark_directwrite.rs` with:

    1. `DirectWriteFontMetrics` struct (empty, per RESEARCH.md Pattern 2). Implement `FontMetrics` for it.

    2. In `measure(&self, text, family, em_size)`:
       - Call `DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED)` to get `IDWriteFactory`.
       - Convert `family` to wide string (UTF-16 + null terminator) using `encode_utf16().chain(Some(0))`.
       - Call `factory.CreateTextFormat(PCWSTR(...), None, DWRITE_FONT_WEIGHT_NORMAL, DWRITE_FONT_STYLE_NORMAL, DWRITE_FONT_STRETCH_NORMAL, em_size as f32, w!("en-us"))`.
       - Convert `text` to wide string.
       - Call `factory.CreateTextLayout(&text_wide[..len-1], &text_format, f32::MAX, f32::MAX)`.
       - Call `layout.GetMetrics(&mut metrics)` where `metrics: DWRITE_TEXT_METRICS`.
       - Return `(metrics.width as f64, metrics.height as f64)`.
       - Wrap all unsafe COM calls in a single `unsafe` block with a `// SAFETY:` comment explaining that COM is initialized by the print watcher thread (per Pitfall 1, RESEARCH.md).
       - Use `anyhow::Context` on each fallible COM call with descriptive messages (e.g., `context("DWriteCreateFactory failed")`).
       - On `CreateTextFormat` failure, retry with `"Segoe UI"` family (per Claude's Discretion in CONTEXT.md) and log `tracing::warn!("Arial not available, falling back to Segoe UI")`.

    3. All functions must have doc comments (per CLAUDE.md 9.3).

    4. Unit tests (Windows-only, gated with `#[cfg(windows)]`):
       - `test_directwrite_measure_hello` — measure "Hello" with Arial, assert width > 0 and height > 0.
       - `test_directwrite_measure_empty` — measure "", assert width == 0 and height > 0 (line height still positive).
       - `test_directwrite_fallback_to_segoe` — measure with a nonexistent font family, verify fallback path is exercised (this may require mocking; if not feasible, document as manual-only in test comment).

    5. Non-Windows tests: `#[cfg(not(windows))]` module with a single test that `TestFontMetrics` still works (already covered in print_watermark.rs tests).
  </action>
  <verify>
    <automated>cargo test -p dlp-agent print_watermark_directwrite</automated>
  </verify>
  <done>
    - `cargo test -p dlp-agent print_watermark_directwrite` passes (Windows tests run on Windows, non-Windows stub compiles)
    - `cargo clippy -p dlp-agent -- -D warnings` passes
    - `cargo fmt --check -p dlp-agent` passes
    - COM safety comment present on unsafe block
    - Arial -> Segoe UI fallback path exists with warning log
    - No `.unwrap()` in non-test code
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| XPS bytes (untrusted input) → `extract_page_geometry` / `inject_watermark` | Malformed ZIP/XML from any print job crosses here |
| DirectWrite COM → `DirectWriteFontMetrics` | External Windows OS service; font loading is attacker-controlled input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-01 | Denial of Service | `extract_page_geometry` (malformed ZIP) | mitigate | `zip` crate validates CRC and path safety; max page limit enforced by caller pattern from `print_xps_parser.rs` |
| T-67.1-02 | Denial of Service | `extract_page_geometry` (XML entity expansion / Billion Laughs) | mitigate | `quick-xml` disables DTD/entity expansion by default; streaming parser limits memory |
| T-67.1-03 | Tampering | `inject_watermark` (corrupting existing page markup) | mitigate | Streaming reader-writer copies all original events verbatim; only appends at `</FixedPage>` (deferred to Phase 67, but design locked per D-01) |
| T-67.1-04 | Denial of Service | `DirectWriteFontMetrics` (font loading failure / missing Arial) | mitigate | Fallback to Segoe UI per Claude's Discretion; error returned to caller, never panic |
| T-67.1-05 | Information Disclosure | Watermark text contains username/timestamp/approval_id | accept | This is intentional metadata embedding per WATERMARK-01/02 requirements; not a bug. Redaction rule (only metadata in audit events) still applies to logs. |
| T-67.1-SC | Tampering | `windows` crate (npm/pip/cargo installs) | mitigate | All packages pre-approved in RESEARCH.md Package Legitimacy Audit; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
- `cargo test -p dlp-agent print_watermark` passes all unit tests
- `cargo test -p dlp-agent print_watermark_directwrite` passes (Windows tests on Windows, non-Windows compiles)
- `cargo clippy -p dlp-agent -- -D warnings` passes
- `cargo fmt --check -p dlp-agent` passes
- `cargo build -p dlp-agent` passes with zero warnings
</verification>

<success_criteria>
1. `extract_page_geometry` correctly parses `FixedPage/@Width` and `FixedPage/@Height` from `.fpage` XML.
2. Missing dimensions fall back to `(816.0, 1056.0)` with a `tracing::warn!` log.
3. `FontMetrics` trait is defined with `measure(text, family, em_size) -> Result<(f64, f64)>`.
4. `TestFontMetrics` returns deterministic per-character widths for CI testing.
5. `DirectWriteFontMetrics` measures text via `windows-rs` DirectWrite COM with Arial primary and Segoe UI fallback.
6. `compute_watermark_geometry` places watermark at bottom-right with 20 XPS unit padding, truncating with ellipsis on overflow.
7. All public functions have doc comments; no `.unwrap()` in library code; clippy and fmt clean.
</success_criteria>

<output>
Create `.planning/phases/67.1-print-watermarking-xps-page-geometry-text-metrics/67.1-01-SUMMARY.md` when done
</output>
