---
phase: 58
plan: 58-05
type: execute
wave: 3
depends_on:
  - 58-04
files_modified:
  - crates/dlp-admin-tui/src/screens/diagnostic_list.rs
  - crates/dlp-admin-tui/src/screens/self_health.rs
  - crates/dlp-admin-tui/src/screens/mod.rs
  - crates/dlp-admin-tui/src/dispatch.rs
  - crates/dlp-admin-tui/src/render.rs
  - crates/dlp-admin-tui/src/client.rs
  - crates/dlp-admin-tui/src/constants.rs
autonomous: true
requirements:
  - DIFF-02
  - DIFF-04
---

# Plan 58-05: Admin TUI Screens

## Objective

Build Diagnostic List and Self-Health Dashboard screens in `dlp-admin-tui` that read from the server's cached endpoints (`GET /admin/diagnostics` and `GET /admin/health`). Use the `severity` and `timestamp_utc` fields from `DiagnosticSnapshot`. Keep SystemMenu lists in `dispatch.rs` and `render.rs` synchronized.

## Background

The original plan invented a `severity` field that did not exist on `DiagnosticSnapshot` and used QPC timestamps for relative time display. The revised plan uses the actual `severity` and `timestamp_utc` fields added in 58-01. It also ensures both `dispatch.rs` and `render.rs` SystemMenu lists are updated together.

## must_haves

- truths:
  - Diagnostic List uses GET /admin/diagnostics with severity filtering
  - Self-Health Dashboard uses GET /admin/health with sparkline history
  - timestamp_utc drives relative time display ("2m ago")
  - SystemMenu in dispatch.rs and render.rs are synchronized
  - Four-file pattern (constants, dispatch, render, client) is followed

## Tasks

<task>
<type>execute</type>
<name>Add DiagnosticSeverityFilter and diagnostic constants</name>
<read_first>
- crates/dlp-admin-tui/src/constants.rs (existing constants)
- crates/dlp-admin-tui/src/screens/bypass_alerts.rs (analog: BypassAlertSeverityFilter)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md (severity filter requirements)
</read_first>
<action>
Update `crates/dlp-admin-tui/src/constants.rs`:

- Add `pub const DIAGNOSTIC_LIST_TITLE: &str = "Diagnostic List";`
- Add `pub const SELF_HEALTH_TITLE: &str = "Self-Health Dashboard";`
- Add `pub const DIAGNOSTIC_REFRESH_INTERVAL_MS: u64 = 5000;`
- Add `pub const HEALTH_REFRESH_INTERVAL_MS: u64 = 10000;`

- Define `pub enum DiagnosticSeverityFilter { All, Critical, Warning, Info }`
- Implement `impl DiagnosticSeverityFilter` with:
  - `fn next(&self) -> Self` (cycles All -> Critical -> Warning -> Info -> All)
  - `fn label(&self) -> &str`
  - `fn matches(&self, severity: &str) -> bool`

- Clone the pattern from `BypassAlertSeverityFilter` exactly.
</action>
<acceptance_criteria>
- `grep "DiagnosticSeverityFilter" crates/dlp-admin-tui/src/constants.rs` returns a match
- `grep "DIAGNOSTIC_LIST_TITLE" crates/dlp-admin-tui/src/constants.rs` returns a match
- `grep "SELF_HEALTH_TITLE" crates/dlp-admin-tui/src/constants.rs` returns a match
- `grep "next" crates/dlp-admin-tui/src/constants.rs` returns a match (cycle method)
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<name>Add diagnostic client methods</name>
<read_first>
- crates/dlp-admin-tui/src/client.rs (existing client methods)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md (API contract)
</read_first>
<action>
Update `crates/dlp-admin-tui/src/client.rs`:

- Add `pub async fn list_diagnostics(&self, agent_id: Option<String>, pid: Option<u32>, limit: Option<usize>) -> Result<Vec<DiagnosticSnapshot>>`:
  1. Build query string from optional parameters
  2. Send `GET /admin/diagnostics?{query}`
  3. Deserialize JSON response into `Vec<DiagnosticSnapshot>`
  4. Return sorted by `timestamp_utc` descending

- Add `pub async fn get_self_health(&self) -> Result<Vec<HealthResponse>>`:
  1. Send `GET /admin/health`
  2. Deserialize JSON response into `Vec<HealthResponse>`

- Both methods should use the existing HTTP client (reqwest or similar) with JWT auth header.
- Add error handling for 401/403 (re-auth) and 500 (retry with backoff).
</action>
<acceptance_criteria>
- `grep "list_diagnostics" crates/dlp-admin-tui/src/client.rs` returns a match
- `grep "get_self_health" crates/dlp-admin-tui/src/client.rs` returns a match
- `grep "GET /admin/diagnostics" crates/dlp-admin-tui/src/client.rs` returns a match
- `grep "GET /admin/health" crates/dlp-admin-tui/src/client.rs` returns a match
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<name>Create Diagnostic List screen</name>
<read_first>
- crates/dlp-admin-tui/src/screens/bypass_alerts.rs (analog: table rendering pattern)
- crates/dlp-admin-tui/src/constants.rs (DiagnosticSeverityFilter from previous task)
- crates/dlp-admin-tui/src/client.rs (list_diagnostics method)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md (Diagnostic List layout)
</read_first>
<action>
Create `crates/dlp-admin-tui/src/screens/diagnostic_list.rs`:

- Define `pub struct DiagnosticListState` with fields:
  - `snapshots: Vec<DiagnosticSnapshot>`
  - `filter: DiagnosticSeverityFilter`
  - `selected_index: usize`
  - `last_refresh: Instant`
  - `error_message: Option<String>`

- Implement `impl DiagnosticListState` with:
  - `fn new() -> Self`
  - `fn filtered_snapshots(&self) -> Vec<&DiagnosticSnapshot>` (applies severity filter)
  - `fn cycle_filter(&mut self)`
  - `fn select_next(&mut self)` / `fn select_prev(&mut self)`
  - `fn needs_refresh(&self) -> bool` (true if `last_refresh.elapsed() > DIAGNOSTIC_REFRESH_INTERVAL_MS`)

- Implement `pub fn draw_diagnostic_list(f: &mut Frame, state: &DiagnosticListState)`:
  - Title bar: "Diagnostic List — Filter: [All | Critical | Warning | Info]"
  - Table with columns: Time, PID, Source, Policy, Mode, Severity, Latency
  - Time column shows relative time from `timestamp_utc` (e.g., "2m ago", "15s ago")
  - Severity column color-coded: Critical=red, Warning=yellow, Info=green
  - Highlight selected row
  - Footer: "↑/↓: Navigate | F: Filter | R: Refresh | Enter: Details | Esc: Back"

- Implement `pub async fn refresh_diagnostic_list(state: &mut DiagnosticListState, client: &Client)`:
  - Call `client.list_diagnostics(None, None, Some(100))`
  - Update `snapshots` and `last_refresh`
  - Set `error_message` on failure

- Add to `crates/dlp-admin-tui/src/screens/mod.rs` with `pub mod diagnostic_list;`
</action>
<acceptance_criteria>
- `test -f crates/dlp-admin-tui/src/screens/diagnostic_list.rs`
- `grep "DiagnosticListState" crates/dlp-admin-tui/src/screens/diagnostic_list.rs` returns a match
- `grep "draw_diagnostic_list" crates/dlp-admin-tui/src/screens/diagnostic_list.rs` returns a match
- `grep "timestamp_utc" crates/dlp-admin-tui/src/screens/diagnostic_list.rs` returns a match
- `grep "severity" crates/dlp-admin-tui/src/screens/diagnostic_list.rs` returns a match
- `grep "2m ago" crates/dlp-admin-tui/src/screens/diagnostic_list.rs` returns a match (or similar relative time format)
- Module declared in `screens/mod.rs`: `grep "diagnostic_list" crates/dlp-admin-tui/src/screens/mod.rs` returns a match
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<name>Create Self-Health Dashboard screen</name>
<read_first>
- crates/dlp-admin-tui/src/screens/diagnostic_list.rs (same patterns)
- crates/dlp-admin-tui/src/client.rs (get_self_health method)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md (Self-Health Dashboard layout)
</read_first>
<action>
Create `crates/dlp-admin-tui/src/screens/self_health.rs`:

- Define `pub struct SelfHealthState` with fields:
  - `health_entries: Vec<HealthResponse>`
  - `selected_index: usize`
  - `last_refresh: Instant`
  - `error_message: Option<String>`
  - `history: VecDeque<Vec<HealthResponse>>` (last 12 readings for sparklines)

- Implement `impl SelfHealthState` with:
  - `fn new() -> Self`
  - `fn select_next(&mut self)` / `fn select_prev(&mut self)`
  - `fn needs_refresh(&self) -> bool` (true if `last_refresh.elapsed() > HEALTH_REFRESH_INTERVAL_MS`)
  - `fn update_history(&mut self)` — pushes current `health_entries` to history, evicts oldest if > 12

- Implement `pub fn draw_self_health(f: &mut Frame, state: &SelfHealthState)`:
  - Title bar: "Self-Health Dashboard"
  - Table with columns: Agent, PID, Status, Last Updated, Sparkline
  - Status column color-coded: Healthy=green, Degraded=yellow, Failed=red
  - Sparkline column shows last 12 health status readings (using `tui-rs` or `ratatui` Sparkline widget)
  - Highlight selected row
  - Footer: "↑/↓: Navigate | R: Refresh | D: Details | Esc: Back"

- Implement `pub async fn refresh_self_health(state: &mut SelfHealthState, client: &Client)`:
  - Call `client.get_self_health()`
  - Update `health_entries`, push to history, update `last_refresh`
  - Set `error_message` on failure

- Add to `crates/dlp-admin-tui/src/screens/mod.rs` with `pub mod self_health;`
</action>
<acceptance_criteria>
- `test -f crates/dlp-admin-tui/src/screens/self_health.rs`
- `grep "SelfHealthState" crates/dlp-admin-tui/src/screens/self_health.rs` returns a match
- `grep "draw_self_health" crates/dlp-admin-tui/src/screens/self_health.rs` returns a match
- `grep "Sparkline" crates/dlp-admin-tui/src/screens/self_health.rs` returns a match
- `grep "history" crates/dlp-admin-tui/src/screens/self_health.rs` returns a match
- Module declared in `screens/mod.rs`: `grep "self_health" crates/dlp-admin-tui/src/screens/mod.rs` returns a match
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<name>Update dispatch.rs with Diagnostic List and Self-Health navigation</name>
<read_first>
- crates/dlp-admin-tui/src/dispatch.rs (existing screen dispatch)
- crates/dlp-admin-tui/src/screens/diagnostic_list.rs (DiagnosticListState)
- crates/dlp-admin-tui/src/screens/self_health.rs (SelfHealthState)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-REVIEWS.md (review: SystemMenu sync)
</read_first>
<action>
Update `crates/dlp-admin-tui/src/dispatch.rs`:

- Add `DiagnosticList(DiagnosticListState)` and `SelfHealth(SelfHealthState)` variants to the main `Screen` enum.
- Add `SystemMenuItem::DiagnosticList` and `SystemMenuItem::SelfHealth` variants to `SystemMenuItem` enum.
- Update `SystemMenuItem::iter()` to include new items at positions 13 and 14:
  - Position 13: `SystemMenuItem::DiagnosticList`
  - Position 14: `SystemMenuItem::SelfHealth`
  - Shift existing items: Syslog Config moves from 12 to 15, Back moves from 13 to 16
- Add dispatch arms:
  - `SystemMenuItem::DiagnosticList` -> `Screen::DiagnosticList(DiagnosticListState::new())`
  - `SystemMenuItem::SelfHealth` -> `Screen::SelfHealth(SelfHealthState::new())`
- Add input handling for Diagnostic List screen:
  - `KeyCode::Char('f')` or `KeyCode::Char('F')` -> cycle severity filter
  - `KeyCode::Char('r')` or `KeyCode::Char('R')` -> trigger refresh
  - `KeyCode::Enter` -> show details popup (if implemented)
  - `KeyCode::Esc` -> return to SystemMenu
- Add input handling for Self-Health screen:
  - `KeyCode::Char('r')` or `KeyCode::Char('R')` -> trigger refresh
  - `KeyCode::Char('d')` or `KeyCode::Char('D')` -> show details popup (if implemented)
  - `KeyCode::Esc` -> return to SystemMenu
- Add async refresh calls in the main event loop for both screens when `needs_refresh()` is true.
</action>
<acceptance_criteria>
- `grep "DiagnosticList" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `grep "SelfHealth" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `grep "SystemMenuItem::DiagnosticList" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `grep "SystemMenuItem::SelfHealth" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `grep "KeyCode::Char('f')" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `grep "KeyCode::Char('r')" crates/dlp-admin-tui/src/dispatch.rs` returns a match
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

<task>
<type>execute</type>
<name>Update render.rs with Diagnostic List and Self-Health rendering</name>
<read_first>
- crates/dlp-admin-tui/src/render.rs (existing screen rendering)
- crates/dlp-admin-tui/src/dispatch.rs (SystemMenuItem order from previous task)
- .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-REVIEWS.md (review: SystemMenu render list sync)
</read_first>
<action>
Update `crates/dlp-admin-tui/src/render.rs`:

- Add rendering arms for `Screen::DiagnosticList(state)` and `Screen::SelfHealth(state)`:
  - `Screen::DiagnosticList(state)` -> call `draw_diagnostic_list(f, state)`
  - `Screen::SelfHealth(state)` -> call `draw_self_health(f, state)`
- Update the `SystemMenu` rendering list to match `dispatch.rs` exactly:
  - Items 1-12: unchanged
  - Item 13: "Diagnostic List"
  - Item 14: "Self-Health Dashboard"
  - Item 15: "Syslog Config" (was 12)
  - Item 16: "Back" (was 13)
- Add a unit test `test_system_menu_order_matches_dispatch` that:
  1. Iterates over `SystemMenuItem::iter()` from dispatch.rs
  2. Verifies the render list has the same items in the same order
  3. Fails if any mismatch is detected
- This test ensures dispatch.rs and render.rs stay synchronized.
</action>
<acceptance_criteria>
- `grep "DiagnosticList" crates/dlp-admin-tui/src/render.rs` returns a match
- `grep "SelfHealth" crates/dlp-admin-tui/src/render.rs` returns a match
- `grep "Diagnostic List" crates/dlp-admin-tui/src/render.rs` returns a match
- `grep "Self-Health Dashboard" crates/dlp-admin-tui/src/render.rs` returns a match
- `grep "Syslog Config" crates/dlp-admin-tui/src/render.rs` returns a match
- `grep "test_system_menu_order_matches_dispatch" crates/dlp-admin-tui/src/render.rs` returns a match
- `cargo test -p dlp-admin-tui system_menu_order` exits 0 with test passing
- `cargo check -p dlp-admin-tui` exits 0 with no errors
</acceptance_criteria>
</task>

## Verification

1. `cargo test -p dlp-admin-tui` passes
2. `cargo clippy -p dlp-admin-tui -- -D warnings` passes
3. Diagnostic List renders with severity filtering and relative timestamps
4. Self-Health Dashboard renders with sparklines and color-coded status
5. SystemMenu order matches between dispatch.rs and render.rs
6. Navigation keys work for both screens

## success_criteria

- [ ] DiagnosticSeverityFilter enum with cycle method
- [ ] Diagnostic List screen with table, filter, and relative timestamps
- [ ] Self-Health Dashboard screen with table, sparklines, and status colors
- [ ] client.rs has list_diagnostics and get_self_health methods
- [ ] dispatch.rs has navigation for both screens
- [ ] render.rs renders both screens and SystemMenu list matches dispatch.rs
- [ ] Unit test verifies SystemMenu order synchronization
- [ ] All unit tests pass
- [ ] No compiler warnings
