---
phase: 58-differentiators-bundle-override-diagnostic-hash-evidence-sel
plan: 05
type: execute
wave: 4
depends_on: ["58-01", "58-02", "58-03", "58-04"]
files_modified:
  - dlp-admin-cli/src/app.rs
  - dlp-admin-cli/src/client.rs
  - dlp-admin-cli/src/screens/diagnostic_list.rs
  - dlp-admin-cli/src/screens/self_health_dashboard.rs
  - dlp-admin-cli/src/screens/dispatch.rs
  - dlp-admin-cli/src/screens/render.rs
  - dlp-admin-cli/src/screens/mod.rs
autonomous: true
requirements:
  - DIFF-02
  - DIFF-04
must_haves:
  truths:
    - "Admin TUI shows a scrollable Diagnostic Events list with time, user, path, tier, policy, latency, source"
    - "Enter on a diagnostic row opens a detail popup with full ABAC context"
    - "Admin TUI shows a Self-Health Dashboard with current status and 5-min sparkline trends"
    - "System Menu has two new entries: Diagnostic Events (index 12) and Self-Health (index 13)"
    - "Both screens follow the existing BypassAlertList four-file pattern"
  artifacts:
    - path: "dlp-admin-cli/src/screens/diagnostic_list.rs"
      provides: "Diagnostic list constants and filter enum"
      exports: ["DIAGNOSTIC_LIST_HINTS", "DIAGNOSTIC_LIST_EMPTY", "DiagnosticSeverityFilter"]
    - path: "dlp-admin-cli/src/screens/self_health_dashboard.rs"
      provides: "Self-health dashboard constants"
      exports: ["SELF_HEALTH_HINTS", "SELF_HEALTH_EMPTY"]
    - path: "dlp-admin-cli/src/app.rs"
      provides: "Screen enum variants for DiagnosticList, DiagnosticDetail, SelfHealthDashboard"
      contains: "Screen::DiagnosticList, Screen::DiagnosticDetail, Screen::SelfHealthDashboard"
    - path: "dlp-admin-cli/src/client.rs"
      provides: "HTTP client methods for diagnostics and health APIs"
      exports: ["list_diagnostics", "get_self_health"]
  key_links:
    - from: "dlp-admin-cli/src/screens/dispatch.rs"
      to: "dlp-admin-cli/src/client.rs"
      via: "action_load_diagnostic_list calls client.list_diagnostics()"
    - from: "dlp-admin-cli/src/screens/dispatch.rs"
      to: "dlp-admin-cli/src/client.rs"
      via: "action_load_health_dashboard calls client.get_self_health()"
    - from: "dlp-admin-cli/src/screens/render.rs"
      to: "dlp-admin-cli/src/app.rs"
      via: "draw_diagnostic_list matches Screen::DiagnosticList variant"
---

<objective>
Build the admin TUI screens for DIFF-02 (Diagnostic Events) and DIFF-04 (Self-Health Dashboard). These are the final user-facing surfaces that make the differentiators visible to operators.

Purpose: Without TUI screens, the diagnostic and health data captured and aggregated by prior plans are invisible to operators.
Output: Two complete TUI screens following the BypassAlertList four-file pattern, plus all wiring in app.rs, client.rs, dispatch.rs, and render.rs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-CONTEXT.md
@.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-RESEARCH.md
@.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-PATTERNS.md
@.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md

# Key interfaces from codebase
From dlp-admin-cli/src/screens/bypass_alerts.rs:
- Constants: BYPASS_ALERT_LIST_HINTS, BYPASS_ALERT_LIST_EMPTY, BYPASS_ALERT_DETAIL_HINTS
- Pattern: constants module with #[cfg(test)] unit tests

From dlp-admin-cli/src/app.rs:
- Screen enum with BypassAlertList { alerts, selected, filter, hide_acknowledged, page, page_size, total, pending_ack_ids }
- Screen::BypassAlertDetail { alert }
- BypassAlertSeverityFilter enum with next() and as_str() methods

From dlp-admin-cli/src/client.rs:
- list_bypass_alerts(&self, severity, acknowledged, limit, offset) -> Result<Value>
- Pattern: build query string with urlencoding::encode

From dlp-admin-cli/src/screens/dispatch.rs:
- handle_bypass_alert_list(app, key) with Up/Down/Enter/Esc/f/h/r/PgUp/PgDn handling
- action_load_bypass_alert_list(app, filter, hide_acknowledged, page) with API call

From dlp-admin-cli/src/screens/render.rs:
- draw_bypass_alert_list() with Table, Row, Cell, severity badges
- draw_bypass_alert_detail() with Paragraph and Block::default().borders(Borders::ALL)
- draw_hints() for footer hint bar

From 58-UI-SPEC.md:
- Diagnostic list: 7 columns (Severity, Time, User, Path, Tier, Policy, Latency)
- Self-health: two-panel layout (45/55 split), status badge, sparklines
- Color scheme: Green=Healthy, Yellow=Degraded, Red=Critical, Cyan=selection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screen constant modules and app.rs variants</name>
  <files>dlp-admin-cli/src/screens/diagnostic_list.rs, dlp-admin-cli/src/screens/self_health_dashboard.rs, dlp-admin-cli/src/app.rs</files>
  <read_first>
    dlp-admin-cli/src/screens/bypass_alerts.rs
    dlp-admin-cli/src/app.rs (Screen enum, existing filter enums)
    dlp-admin-cli/src/screens/mod.rs
  </read_first>
  <action>
    Create dlp-admin-cli/src/screens/diagnostic_list.rs:
    1. Define DIAGNOSTIC_LIST_HINTS: "[Enter] Detail  [f] Filter Severity  [h] Hide Ack'd  [r] Refresh  [PgUp/PgDn] Page  [Esc] Back"
    2. Define DIAGNOSTIC_LIST_EMPTY: "No diagnostic events found."
    3. Define DIAGNOSTIC_DETAIL_HINTS: "[Enter/Esc] Back to list"
    4. Define DiagnosticSeverityFilter enum (All, Crit, Warn, Info) with:
       - next() cycling All -> Crit -> Warn -> Info -> All
       - as_str() returning Option<&'static str> (None for All, Some for others)
       - label() returning &'static str for display
    5. Add #[cfg(test)] module with tests verifying hints, empty message, filter cycling
    6. Add pub mod diagnostic_list; to dlp-admin-cli/src/screens/mod.rs

    Create dlp-admin-cli/src/screens/self_health_dashboard.rs:
    1. Define SELF_HEALTH_HINTS: "[r] Refresh  [Esc] Back to System Menu"
    2. Define SELF_HEALTH_EMPTY: "No health data available."
    3. Add #[cfg(test)] module with tests verifying hints and empty message
    4. Add pub mod self_health_dashboard; to dlp-admin-cli/src/screens/mod.rs

    Modify dlp-admin-cli/src/app.rs:
    1. Add DiagnosticSeverityFilter import/use from screens::diagnostic_list
    2. Add three new Screen enum variants (after BypassAlertDetail, before Allowlist):
       - DiagnosticList { events: Vec<Value>, selected: usize, filter: DiagnosticSeverityFilter, page: usize, page_size: usize, total: usize }
       - DiagnosticDetail { event: Value }
       - SelfHealthDashboard { snapshot: Option<Value>, history: Vec<Value>, last_refresh: Option<String> }
    3. Update SystemMenu item list in dispatch.rs and render.rs (see Task 3 for exact wiring)
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli diagnostic_list self_health</automated>
  </verify>
  <done>
    - diagnostic_list.rs and self_health_dashboard.rs exist with constants and filter enum
    - app.rs has three new Screen variants
    - Unit tests pass (diagnostic_list: 3 tests; self_health_dashboard: 2 tests)
    - cargo build -p dlp-admin-cli compiles with zero warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Add client methods and app.rs wiring</name>
  <files>dlp-admin-cli/src/client.rs, dlp-admin-cli/src/app.rs</files>
  <read_first>
    dlp-admin-cli/src/client.rs (list_bypass_alerts pattern)
    dlp-admin-cli/src/app.rs (Screen enum and existing variants)
  </read_first>
  <action>
    Modify dlp-admin-cli/src/client.rs:
    1. Add pub async fn list_diagnostics(
       &self,
       since: Option<&str>,
       user_sid: Option<&str>,
       policy_id: Option<&str>,
       limit: usize,
       offset: usize,
    ) -> Result<Value>:
       - Build path: "admin/diagnostics?limit={limit}&offset={offset}"
       - Append &since=, &user_sid=, &policy_id= query params if present (urlencoding::encode)
       - Call self.get(&path).await
    2. Add pub async fn get_self_health(&self) -> Result<Value>:
       - Build path: "admin/health" (or "agent/health" depending on API design — use "admin/health" for consistency)
       - Call self.get(&path).await
       - Note: If the health endpoint is on the agent rather than server, document this and use the appropriate path

    Modify dlp-admin-cli/src/app.rs (if needed):
    - Ensure Screen variants are fully defined with correct field types
    - No other changes needed if Task 1 already added variants
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli client</automated>
  </verify>
  <done>
    - client.rs has list_diagnostics and get_self_health methods
    - Both methods follow existing query parameter building pattern
    - Unit tests verify URL construction (mock or inline tests)
    - cargo build -p dlp-admin-cli compiles with zero warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement dispatch handlers and action loaders</name>
  <files>dlp-admin-cli/src/screens/dispatch.rs</files>
  <read_first>
    dlp-admin-cli/src/screens/dispatch.rs (handle_bypass_alert_list and action_load_bypass_alert_list)
    dlp-admin-cli/src/app.rs (Screen enum variants)
    dlp-admin-cli/src/screens/diagnostic_list.rs (filter enum)
  </read_first>
  <action>
    Modify dlp-admin-cli/src/screens/dispatch.rs:
    1. Add handle_diagnostic_list(app: &mut App, key: KeyEvent):
       - Extract (events, selected, filter, page, page_size, total) from Screen::DiagnosticList match
       - Key handling:
         - Up/Down: update selected within events.len() bounds
         - Enter: transition to Screen::DiagnosticDetail { event: events[selected].clone() }
         - Esc: app.screen = Screen::SystemMenu { selected: 12 }
         - 'f': cycle filter to next(), reload page 0 with new filter
         - 'h': toggle hide-acknowledged (if applicable), reload page 0
         - 'r': reload current page
         - PgUp: if page > 0, page -= 1, reload
         - PgDn: if (page + 1) * page_size < total, page += 1, reload
    2. Add action_load_diagnostic_list(app: &mut App, filter: DiagnosticSeverityFilter, page: usize):
       - page_size = 20
       - severity_filter = filter.as_str()
       - offset = page * page_size
       - Call app.rt.block_on(app.client.list_diagnostics(None, None, None, page_size, offset))
       - On success: parse events and total from JSON, set app.screen = Screen::DiagnosticList { ... }
       - On error: app.set_status(format!("Error loading diagnostics: {e}"), StatusKind::Error)
    3. Add handle_diagnostic_detail(app: &mut App, key: KeyEvent):
       - Enter or Esc: transition back to Screen::DiagnosticList (reload with defaults)
    4. Add handle_self_health_dashboard(app: &mut App, key: KeyEvent):
       - 'r': call action_load_health_dashboard(app)
       - Esc: app.screen = Screen::SystemMenu { selected: 13 }
    5. Add action_load_health_dashboard(app: &mut App):
       - Call app.rt.block_on(app.client.get_self_health())
       - On success: parse snapshot and history from JSON, set app.screen = Screen::SelfHealthDashboard { ... }
       - On error: app.set_status(format!("Error loading health data: {e}"), StatusKind::Error)
    6. Update the main handle_event match statement:
       - Add arm for Screen::DiagnosticList -> handle_diagnostic_list(app, key)
       - Add arm for Screen::DiagnosticDetail -> handle_diagnostic_detail(app, key)
       - Add arm for Screen::SelfHealthDashboard -> handle_self_health_dashboard(app, key)
    7. Update SystemMenu handling:
       - Add "Diagnostic Events" at index 12 -> action_load_diagnostic_list(app, DiagnosticSeverityFilter::All, 0)
       - Add "Self-Health" at index 13 -> action_load_health_dashboard(app)
       - Shift "Syslog Config" from 12 to 14, "Back" from 13 to 15
       - Update Esc return routing for all screens that go back to SystemMenu
    8. Add unit tests for dispatch handlers:
       - test_handle_diagnostic_list_nav: Up/Down changes selected
       - test_handle_diagnostic_list_enter: Enter transitions to DiagnosticDetail
       - test_handle_diagnostic_list_filter: 'f' cycles filter
       - test_handle_self_health_refresh: 'r' triggers reload
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli dispatch</automated>
  </verify>
  <done>
    - dispatch.rs has handle_diagnostic_list, handle_diagnostic_detail, handle_self_health_dashboard
    - Action loaders call client methods and handle errors
    - SystemMenu has two new items at indices 12 and 13
    - All screens route Esc correctly
    - Unit tests pass (4 dispatch tests)
    - cargo build -p dlp-admin-cli compiles with zero warnings
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement render functions</name>
  <files>dlp-admin-cli/src/screens/render.rs</files>
  <read_first>
    dlp-admin-cli/src/screens/render.rs (draw_bypass_alert_list and draw_bypass_alert_detail)
    dlp-admin-cli/src/app.rs (Screen variants)
    dlp-admin-cli/src/screens/diagnostic_list.rs (constants)
    dlp-admin-cli/src/screens/self_health_dashboard.rs (constants)
    .planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-UI-SPEC.md
  </read_first>
  <action>
    Modify dlp-admin-cli/src/screens/render.rs:
    1. Add draw_diagnostic_list():
       - Match Screen::DiagnosticList to extract events, selected, filter, page, page_size, total
       - If events.is_empty(): render centered Paragraph with DIAGNOSTIC_LIST_EMPTY, block title shows "Diagnostic Events (0)"
       - Table header: Row::new(["Severity", "Time", "User", "Path", "Tier", "Policy", "Latency"]).style(BOLD).bottom_margin(1)
       - For each event, extract fields from JSON Value:
         - severity: event["severity"].as_str().unwrap_or("info")
         - time: format relative time from event["timestamp_qpc"] or event["timestamp"]
         - user: truncate SID to last 12 chars with "..." prefix
         - path: truncate to first 22 chars + "..."
         - tier: event["classification"] or "-"
         - policy: truncate policy_id to first 14 chars + "..."
         - latency: "{N} us" from event["decision_latency_us"]
       - Severity cell styling: crit=Red+BOLD, warn=Yellow, info=Blue
       - Selected row: Black fg + Cyan bg + BOLD
       - Block title: "Diagnostic Events ({total})" with filter suffixes
       - Call draw_hints(frame, area, DIAGNOSTIC_LIST_HINTS)
    2. Add draw_diagnostic_detail():
       - Match Screen::DiagnosticDetail to extract event
       - Full-screen read-only view with Block::default().borders(Borders::ALL).title("Diagnostic Detail")
       - Paragraph with Wrap { trim: true }
       - Field order per 58-UI-SPEC.md:
         1. Time: raw ISO timestamp
         2. User SID: full SID
         3. Path: full path
         4. Classification Tier: T1/T2/T3/T4
         5. Matched Policy: "{policy_id} ({enforcement_mode})"
         6. Decision Latency: "{N} us"
         7. Classification Source: "CacheHit (age: {N} ms)" / "CacheMiss" / "Pipe"
         8. Blank line
         9. "-- ABAC Context --" (BOLD)
         10. Subject: JSON
         11. Resource: path
         12. Action: READ/WRITE/etc
         13. Environment: JSON
         14. Blank line
         15. "-- Hook Function --" (BOLD)
         16. Hook: function name
       - Call draw_hints(frame, area, DIAGNOSTIC_DETAIL_HINTS)
    3. Add draw_self_health_dashboard():
       - Match Screen::SelfHealthDashboard to extract snapshot, history
       - Use Layout::horizontal([Constraint::Percentage(45), Constraint::Percentage(55)])
       - Left panel (Current Status):
         - Title: "-- Current Status --"
         - Overall status badge: Paragraph inside Block with colored background
           - Healthy: Green bg + Black fg + BOLD, text "HEALTHY"
           - Degraded: Yellow bg + Black fg + BOLD, text "DEGRADED"
           - Critical: Red bg + Black fg + BOLD, text "CRITICAL"
           - Unknown: DarkGray bg + Black fg + BOLD, text "UNKNOWN"
         - Counter lines (label BOLD left-aligned, value right-aligned at col 25):
           - "Injected PIDs:     {N}"
           - "Patched Modules:   {N}"
           - "Pipe Round-Trips:  {N}"
           - "Cache Hit Rate:    {N}%"
           - "Fail State:        {state}"
       - Right panel (5-Min Trends):
         - Title: "-- 5-Min Trends --"
         - Cache Hit Rate sparkline:
           - data: Vec<u64> from history cache_hit_rate_60s values (multiply by 100 for percentage)
           - max: 100
           - style color: Green if last >= 80, Yellow if >= 60, Red if < 60
           - Y-axis labels: "0%" left, "50%" center, "100%" right
         - Pipe Round-Trips sparkline:
           - data: Vec<u64> from history pipe_round_trips_60s
           - max: auto-computed max * 1.1 (or 1 if no data)
           - style color: based on overall status
           - Y-axis labels: "0" left, "{mid}" center, "{max}" right
         - If no history: centered Paragraph "No trend data available. Poll begins on first agent connection."
       - If terminal < 80 cols or < 20 rows: centered warning "Terminal too small. Please resize to at least 80x20."
       - Call draw_hints(frame, area, SELF_HEALTH_HINTS)
    4. Update the main draw() match statement:
       - Add arms for Screen::DiagnosticList, Screen::DiagnosticDetail, Screen::SelfHealthDashboard
    5. Update SystemMenu rendering:
       - Add "Diagnostic Events" and "Self-Health" items at indices 12 and 13
    6. Add unit tests for render functions:
       - test_draw_diagnostic_list_empty: Renders empty state correctly
       - test_draw_diagnostic_list_with_data: Renders table with correct columns
       - test_draw_self_health_dashboard_empty: Renders unknown status and empty trends
       - test_draw_self_health_dashboard_healthy: Renders green badge and sparklines
       - test_draw_self_health_dashboard_critical: Renders red badge
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli render</automated>
  </verify>
  <done>
    - render.rs has draw_diagnostic_list, draw_diagnostic_detail, draw_self_health_dashboard
    - Diagnostic list renders 7-column table with severity badges and pagination
    - Detail popup shows full ABAC context per 58-UI-SPEC.md
    - Self-health dashboard shows two-panel layout with status badge and sparklines
    - Terminal size check renders warning when too small
    - SystemMenu renders 16 items with new entries at indices 12 and 13
    - Unit tests pass (5 render tests)
    - cargo build -p dlp-admin-cli compiles with zero warnings
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin TUI -> Server API | Authenticated via JWT; read-only for diagnostics |
| Admin TUI display | Trusted; no sensitive data beyond SID and path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-14 | Information Disclosure | Diagnostic detail popup | mitigate | Only shows data the admin already has access to via API; no additional privilege escalation |
| T-58-15 | Denial of Service | TUI render panic | mitigate | All JSON fields use .as_str().unwrap_or("-") fallbacks; sparkline handles empty data |
</threat_model>

<verification>
1. cargo test -p dlp-admin-cli passes (all tests including new screen tests)
2. cargo build -p dlp-admin-cli compiles with zero warnings
3. Full workspace build: cargo build --workspace passes
4. TUI screen count test verifies 16 SystemMenu items
</verification>

<success_criteria>
- Diagnostic list screen: scrollable table with 7 columns, filter cycling, pagination, detail popup
- Self-health dashboard: two-panel layout with color-coded status badge and sparkline trends
- SystemMenu has 16 items with Diagnostic Events at index 12 and Self-Health at index 13
- All Esc routing and key bindings work per 58-UI-SPEC.md
- Client methods for diagnostics and health APIs exist and are tested
- All dlp-admin-cli tests pass; clippy clean
</success_criteria>

<output>
Create `.planning/phases/58-differentiators-bundle-override-diagnostic-hash-evidence-sel/58-05-SUMMARY.md` when done
</output>
