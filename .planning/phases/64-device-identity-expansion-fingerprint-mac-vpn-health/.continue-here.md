---
context: phase
phase: 64-device-identity-expansion-fingerprint-mac-vpn-health
task: 4
total_tasks: 12
status: in_progress
last_updated: 2026-06-07T02:30:00Z
---

# BLOCKING CONSTRAINTS

- [x] CONSTRAINT: Windows crate API signatures differ from RESEARCH.md
- [x] CONSTRAINT: `IF_TYPE_TUNNEL` constant not exported by `windows` crate 0.62
- [x] CONSTRAINT: `DeviceHealthStatus` added to `Subject` struct causes E0063 across ALL crates
- [x] CONSTRAINT: `PolicyCondition::DeviceHealth` needs match arms in ALL `match cond` expressions
- [x] CONSTRAINT: dlp-admin-cli `ConditionAttribute` enum does NOT have `DeviceHealth` variant

# Current State

**Phase 64: Device Identity Expansion** — Wave 3 starting.

- Plan 01: COMPLETE
- Plan 02: COMPLETE
- Plan 03: COMPLETE (heartbeat + DB persistence)
- Plan 04: NOT STARTED (ABAC DeviceHealth evaluation + health state machine)

# Remaining Work

## Plan 04 (ABAC evaluation + health state machine)
1. Add `EventType::DeviceHealthChange` to dlp-common/src/audit.rs
2. Add `PolicyCondition::DeviceHealth` match arm to dlp-server/src/policy_store.rs `condition_matches`
3. Add health state machine to dlp-agent/src/device_identity.rs (AtomicU8, transition_health, persist_health_to_registry, transition_health_async, current_health, read/write from registry)
4. Update agent heartbeat loop: track consecutive failures (3=Degraded, 10=Offline, recovery=Healthy)
5. Add `report_tamper_detected()` with Phase 63 dependency comment
6. Emit AuditEvent on health transitions
7. Tests: state machine transitions, persistence roundtrip, async wrapper, ABAC DeviceHealth gt/lt operators

# Next Action

1. Read `64-04-PLAN.md` for detailed task breakdown
2. Start with dlp-common EventType addition
3. Then dlp-server policy_store.rs condition_matches extension
4. Then dlp-agent health state machine
