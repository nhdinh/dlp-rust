---
phase: 64-device-identity-expansion-fingerprint-mac-vpn-health
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/endpoint.rs
  - dlp-common/src/abac.rs
  - dlp-common/src/lib.rs
autonomous: true
requirements:
  - DEVICE-01
  - DEVICE-02
  - DEVICE-05
user_setup: []
must_haves:
  truths:
    - EndpointIdentity struct exists with fingerprint, mac_addresses, vpn_active, domain_joined, health_status fields
    - DeviceHealthStatus enum exists with Healthy, Degraded, Offline, Tampered variants
    - All new types derive Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default
    - All new types have #[serde(default)] or #[serde(rename_all)] for backward compat
    - dlp-common compiles with zero warnings
    - Unit tests verify serde round-trips for all new types
    - dlp-common/src/lib.rs re-exports EndpointIdentity and DeviceHealthStatus
  artifacts:
    - path: dlp-common/src/endpoint.rs
      provides: EndpointIdentity struct, DeviceHealthStatus enum
      contains: "pub struct EndpointIdentity", "pub enum DeviceHealthStatus"
    - path: dlp-common/src/abac.rs
      provides: DeviceHealth PolicyCondition variant
      contains: "DeviceHealth {"
    - path: dlp-common/src/lib.rs
      provides: Re-exports
      contains: "EndpointIdentity", "DeviceHealthStatus"
  key_links:
    - from: dlp-common/src/endpoint.rs
      to: dlp-common/src/lib.rs
      via: pub use endpoint::
      pattern: "pub use endpoint::.*EndpointIdentity"
    - from: dlp-common/src/abac.rs
      to: dlp-common/src/endpoint.rs
      via: DeviceHealthStatus import
      pattern: "use crate::endpoint::DeviceHealthStatus"
---

<objective>
Create core data types in dlp-common for expanded device identity: EndpointIdentity (machine-level identity), DeviceHealthStatus enum, and the DeviceHealth PolicyCondition variant. These types are the foundation for the rest of Phase 64.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-device-identity-expansion-fingerprint-mac-vpn-health/64-CONTEXT.md
@.planning/phases/64-device-identity-expansion-fingerprint-mac-vpn-health/64-RESEARCH.md
@.planning/phases/64-device-identity-expansion-fingerprint-mac-vpn-health/64-PATTERNS.md

The existing DeviceIdentity in dlp-common/src/endpoint.rs is USB-specific (vid, pid, serial, description). Phase 64 requires machine-level identity. NetworkLocation::CorporateVpn already exists in dlp-common/src/abac.rs — verify but do not modify. DeviceTrust already exists in dlp-common/src/abac.rs — verify but do not modify.

Key patterns from PATTERNS.md:
- Struct pattern: #[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)] + #[serde(default)]
- Enum pattern: #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)] + #[serde(rename_all = "snake_case")]
- PolicyCondition variant pattern: attribute name with op and value fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeviceHealthStatus enum and EndpointIdentity struct to endpoint.rs</name>
  <files>dlp-common/src/endpoint.rs</files>
  <read_first>
    dlp-common/src/endpoint.rs
    dlp-common/src/abac.rs
  </read_first>
  <action>
    Add to dlp-common/src/endpoint.rs AFTER the existing DeviceIdentity struct (line ~147) and BEFORE the #[cfg(test)] module:

    1. DeviceHealthStatus enum with #[serde(rename_all = "snake_case")]:
       - Healthy (#[default])
       - Degraded
       - Offline
       - Tampered

    2. EndpointIdentity struct with #[serde(default)]:
       - fingerprint: String (SHA-256 hex, lowercase)
       - mac_addresses: Vec<String> (sorted lexicographically, uppercase no-colon format e.g. "AABBCCDDEEFF")
       - vpn_active: bool
       - domain_joined: bool
       - health_status: DeviceHealthStatus

    Do NOT create newtype wrappers (DeviceFingerprint, MacAddress) — use plain String and Vec<String> per RESEARCH.md recommendation for simplicity and serde compatibility. The existing codebase does not use newtypes for similar fields (e.g., AgentInfoResponse.agent_id is String, not AgentId newtype).

    Add doc comments for each field following the existing endpoint.rs style.
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib endpoint</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "pub enum DeviceHealthStatus" dlp-common/src/endpoint.rs returns a line
    - grep -n "pub struct EndpointIdentity" dlp-common/src/endpoint.rs returns a line
    - grep -n "healthy" dlp-common/src/endpoint.rs shows snake_case serde annotation
    - cargo build -p dlp-common compiles with zero errors
    - DeviceHealthStatus has exactly 4 variants: Healthy, Degraded, Offline, Tampered
    - Default variant is Healthy
    - EndpointIdentity has exactly 5 fields: fingerprint, mac_addresses, vpn_active, domain_joined, health_status
    - #[serde(default)] present on EndpointIdentity
    - #[serde(rename_all = "snake_case")] present on DeviceHealthStatus
  </acceptance_criteria>
  <done>DeviceHealthStatus enum and EndpointIdentity struct added to endpoint.rs with correct derives and serde attributes</done>
</task>

<task type="auto">
  <name>Task 2: Add DeviceHealth PolicyCondition variant to abac.rs</name>
  <files>dlp-common/src/abac.rs</files>
  <read_first>
    dlp-common/src/abac.rs
    dlp-common/src/endpoint.rs
  </read_first>
  <action>
    Add a DeviceHealth variant to the PolicyCondition enum in dlp-common/src/abac.rs, following the exact pattern of DeviceTrust and NetworkLocation variants (lines 555-565):

    - Add "use crate::endpoint::DeviceHealthStatus;" at the top of abac.rs (after the existing "use crate::endpoint::AppIdentity;" line)
    - Add variant:
      ```rust
      /// Match by device health status.
      DeviceHealth {
          #[serde(rename = "op")]
          op: String,
          value: DeviceHealthStatus,
      },
      ```
    - Place it after the NetworkLocation variant and before AccessContext, maintaining alphabetical-ish ordering by attribute name

    Also add a device_health field to the Subject struct (after network_location, before the closing brace):
    ```rust
    /// The health status of the endpoint device.
    #[serde(default)]
    pub device_health: DeviceHealthStatus,
    ```

    Update the Default impl for Subject to include device_health: DeviceHealthStatus::default() (the #[serde(default)] on the struct handles this automatically, but verify the struct-level Default derive works correctly).
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib abac</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "DeviceHealth" dlp-common/src/abac.rs returns lines for the variant, the use statement, and the Subject field
    - grep -n "DeviceHealthStatus" dlp-common/src/abac.rs returns lines
    - cargo build -p dlp-common compiles with zero errors
    - PolicyCondition::DeviceHealth has op: String and value: DeviceHealthStatus fields
    - Subject struct has device_health: DeviceHealthStatus field with #[serde(default)]
    - The variant follows the same pattern as DeviceTrust and NetworkLocation (attribute name, op field, value field)
  </acceptance_criteria>
  <done>DeviceHealth PolicyCondition variant added to abac.rs with matching pattern; Subject struct extended with device_health field</done>
</task>

<task type="auto">
  <name>Task 3: Update lib.rs re-exports and add unit tests</name>
  <files>dlp-common/src/lib.rs, dlp-common/src/endpoint.rs, dlp-common/src/abac.rs</files>
  <read_first>
    dlp-common/src/lib.rs
    dlp-common/src/endpoint.rs
    dlp-common/src/abac.rs
  </read_first>
  <action>
    1. Update dlp-common/src/lib.rs line 35:
       Change:
       pub use endpoint::{AppIdentity, AppTrustTier, DeviceIdentity, SignatureState, UsbTrustTier};
       To:
       pub use endpoint::{AppIdentity, AppTrustTier, DeviceIdentity, DeviceHealthStatus, EndpointIdentity, SignatureState, UsbTrustTier};

    2. Add unit tests in dlp-common/src/endpoint.rs in the existing #[cfg(test)] module (after the last existing test, line ~339):

       a. test_device_health_status_serde_roundtrip — for each variant (Healthy, Degraded, Offline, Tampered), serialize to JSON, deserialize back, assert equality
       b. test_device_health_status_default_is_healthy — assert DeviceHealthStatus::default() == DeviceHealthStatus::Healthy
       c. test_device_health_status_snake_case_serde — assert serde_json::to_string(&DeviceHealthStatus::Healthy) == "\"healthy\""
       d. test_endpoint_identity_serde_roundtrip — create EndpointIdentity with all fields populated, serialize, deserialize, assert equality
       e. test_endpoint_identity_default — create EndpointIdentity::default(), assert fingerprint == "", mac_addresses is empty, vpn_active == false, domain_joined == false, health_status == DeviceHealthStatus::Healthy
       f. test_endpoint_identity_backward_compat_empty_json — deserialize {} into EndpointIdentity, assert all fields have default values

    3. Add unit tests in dlp-common/src/abac.rs in the existing #[cfg(test)] module:

       a. test_policy_condition_device_health_serde — create PolicyCondition::DeviceHealth { op: "eq".to_string(), value: DeviceHealthStatus::Tampered }, serialize, verify JSON contains "attribute":"device_health", deserialize back, assert equality
       b. test_subject_device_health_default — create Subject::default(), assert device_health == DeviceHealthStatus::Healthy
       c. test_abac_context_device_health_roundtrip — create AbacContext with subject.device_health = DeviceHealthStatus::Degraded, serialize, deserialize, assert equality
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib</automated>
  </verify>
  <acceptance_criteria>
    - dlp-common/src/lib.rs contains "DeviceHealthStatus" and "EndpointIdentity" in the pub use endpoint:: line
    - dlp-common/src/endpoint.rs test module has at least 6 new tests (a-f above)
    - dlp-common/src/abac.rs test module has at least 3 new tests (a-c above)
    - cargo test -p dlp-common --lib passes all tests (existing + new)
    - cargo clippy -p dlp-common -- -D warnings passes
    - cargo fmt --check passes
  </acceptance_criteria>
  <done>lib.rs re-exports updated; 9 new unit tests added across endpoint.rs and abac.rs; all tests pass; clippy clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type system | Strong types prevent accidental mixing of health status with other enums |
| Serde wire format | #[serde(default)] ensures old payloads deserialize without error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Spoofing | Health status in heartbeat spoofed | mitigate | Health transitions are agent-computed; server stores but does not trust blindly — tamper detection is defense-in-depth |
| T-64-02 | Information Disclosure | MAC addresses in JSON payload | accept | MACs are already broadcast on local network; HTTPS in transit encrypts the payload |
| T-64-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages required — all functionality uses existing workspace dependencies (sha2, serde, windows already present) |
</threat_model>

<verification>
- cargo test -p dlp-common --lib passes
- cargo clippy -p dlp-common -- -D warnings passes
- cargo fmt --check passes
- cargo build -p dlp-common compiles with zero errors
</verification>

<success_criteria>
- DeviceHealthStatus enum with 4 variants (Healthy, Degraded, Offline, Tampered) and snake_case serde
- EndpointIdentity struct with 5 fields (fingerprint, mac_addresses, vpn_active, domain_joined, health_status)
- PolicyCondition::DeviceHealth variant with op and value fields
- Subject struct extended with device_health field
- dlp-common/src/lib.rs re-exports EndpointIdentity and DeviceHealthStatus
- 9 unit tests pass (6 in endpoint.rs, 3 in abac.rs)
- Clippy clean on dlp-common
- Zero compiler warnings
</success_criteria>

<output>
Create `.planning/phases/64-device-identity-expansion-fingerprint-mac-vpn-health/64-01-SUMMARY.md` when done
</output>
