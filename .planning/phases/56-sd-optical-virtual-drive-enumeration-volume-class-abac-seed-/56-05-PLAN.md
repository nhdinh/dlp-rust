---
phase: 56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed
plan: 05
type: execute
wave: 2
depends_on:
  - 56-01
files_modified:
  - dlp-admin-cli/src/app.rs
  - dlp-admin-cli/src/screens/dispatch.rs
  - dlp-admin-cli/src/screens/render.rs
  - dlp-admin-cli/src/screens/allowlist.rs
autonomous: true
requirements:
  - DRIVE-02
  - DRIVE-04
user_setup: []
must_haves:
  truths:
    - ConditionAttribute enum has SourceVolumeClass and DestinationVolumeClass variants
    - ATTRIBUTES array includes both new variants in display order after DestinationOrigin
    - Conditions Builder Step 2 offers eq/ne/in operators for volume class attributes
    - Conditions Builder Step 3 shows a dropdown of six VolumeClass values
    - build_condition creates correct PolicyCondition::SourceVolumeClass/DestinationVolumeClass
    - condition_to_prefill correctly maps existing volume class conditions back to picker state
    - condition_display renders volume class conditions as readable strings
    - AllowlistScreen renders volume class badges for SD/Optical/Virtual entries
  artifacts:
    - path: dlp-admin-cli/src/app.rs
      provides: ConditionAttribute extension, ATTRIBUTES array, label() impl
      contains: "SourceVolumeClass", "DestinationVolumeClass", "ATTRIBUTES: [ConditionAttribute; 11]"
    - path: dlp-admin-cli/src/screens/dispatch.rs
      provides: operators_for, value_count_for, build_condition, condition_to_prefill, condition_display extensions
      contains: "SourceVolumeClass", "DestinationVolumeClass", "build_volume_class_condition", "volume_class_to_idx"
    - path: dlp-admin-cli/src/screens/render.rs
      provides: VOLUME_CLASS_VALUES constant, picker_items arm, step_flags
      contains: "VOLUME_CLASS_VALUES", "VolumeClass"
    - path: dlp-admin-cli/src/screens/allowlist.rs
      provides: volume_class badge rendering
      contains: "volume_class"
  key_links:
    - from: dlp-admin-cli/src/app.rs
      to: dlp-admin-cli/src/screens/dispatch.rs
      via: ConditionAttribute enum used in match arms
      pattern: "ConditionAttribute::SourceVolumeClass"
    - from: dlp-admin-cli/src/screens/dispatch.rs
      to: dlp-admin-cli/src/screens/render.rs
      via: value_count_for and picker_items coordination
      pattern: "value_count_for"
---

<objective>
Extend the admin TUI Conditions Builder with two new volume class attributes (SourceVolumeClass, DestinationVolumeClass) as dropdowns, and extend the allowlist screen to render volume class badges for SD/Optical/Virtual entries.

Purpose: Operators must be able to build ABAC policies using volume class conditions through the existing TUI, and see volume class information in the allowlist screens.
Output: Extended ConditionAttribute, ATTRIBUTES, operators_for, value_count_for, build_condition, condition_to_prefill, condition_display, picker_items, step_flags, and allowlist render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-CONTEXT.md
@.planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-PATTERNS.md
@.planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-RESEARCH.md

# Key interfaces from PATTERNS.md

From dlp-admin-cli/src/app.rs (existing pattern):
- ConditionAttribute enum with 9 variants
- ATTRIBUTES array with all variants in display order
- label() method on ConditionAttribute returning &'static str

From dlp-admin-cli/src/screens/dispatch.rs (existing pattern):
- operators_for(attr, field) returns &'static [(&'static str, bool)]
- value_count_for(attr, field) returns usize
- build_condition(...) creates PolicyCondition from picker state
- condition_to_prefill(...) maps PolicyCondition back to (attribute, op, picker_selected, text)
- condition_display(...) renders PolicyCondition as String

From dlp-admin-cli/src/screens/render.rs (existing pattern):
- CONST value arrays: CLASSIFICATION_VALUES, DEVICE_TRUST_VALUES, etc.
- picker_items(step, selected_attribute, ...) returns Vec<ListItem>
- step_flags(...) returns (bool, bool) for sub-step and text-input flags

From dlp-admin-cli/src/screens/allowlist.rs (existing pattern):
- AllowlistEntryUi struct with id, match_type, value, description, category, priority, enabled
- Render uses ListItem::new(format!(...)) with status badge
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ConditionAttribute enum, ATTRIBUTES array, and label() in app.rs</name>
  <files>dlp-admin-cli/src/app.rs</files>
  <read_first>
    dlp-admin-cli/src/app.rs
    .planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-PATTERNS.md
  </read_first>
  <action>
    Extend the admin TUI model in dlp-admin-cli/src/app.rs. Per D-10:

    1. Add two variants to `ConditionAttribute` enum (appended after `DestinationOrigin`):
       - `SourceVolumeClass`
       - `DestinationVolumeClass`

    2. Update `ATTRIBUTES` array from `[ConditionAttribute; 9]` to `[ConditionAttribute; 11]`:
       - Keep existing 9 variants in order
       - Append `ConditionAttribute::SourceVolumeClass`
       - Append `ConditionAttribute::DestinationVolumeClass`

    3. Add two arms to `ConditionAttribute::label()`:
       - `Self::SourceVolumeClass => "Source Volume Class"`
       - `Self::DestinationVolumeClass => "Destination Volume Class"`

    4. If there is a `value_count()` or similar method on ConditionAttribute, add arms returning 6 for both new variants.

    Do NOT change the order of existing variants.
    Do NOT remove any existing variants.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli app --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SourceVolumeClass" dlp-admin-cli/src/app.rs` returns a line in the enum definition
    - `grep -n "DestinationVolumeClass" dlp-admin-cli/src/app.rs` returns a line in the enum definition
    - `grep -n "ATTRIBUTES: \[ConditionAttribute; 11\]" dlp-admin-cli/src/app.rs` returns a line
    - `grep -n "Source Volume Class" dlp-admin-cli/src/app.rs` returns a line in label()
    - `grep -n "Destination Volume Class" dlp-admin-cli/src/app.rs` returns a line in label()
    - `cargo test -p dlp-admin-cli` compiles with zero errors
    - A unit test `test_attributes_count_is_eleven` exists and passes: asserts ATTRIBUTES.len() == 11
    - A unit test `test_source_volume_class_label` exists and passes: asserts ConditionAttribute::SourceVolumeClass.label() == "Source Volume Class"
  </acceptance_criteria>
  <done>ConditionAttribute enum extended; ATTRIBUTES array updated to 11 elements; labels added</done>
</task>

<task type="auto">
  <name>Task 2: Extend dispatch.rs with operators, builders, prefill, and display for volume class</name>
  <files>dlp-admin-cli/src/screens/dispatch.rs</files>
  <read_first>
    dlp-admin-cli/src/screens/dispatch.rs
    dlp-admin-cli/src/app.rs
    .planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-PATTERNS.md
  </read_first>
  <action>
    Extend the Conditions Builder dispatch logic in dlp-admin-cli/src/screens/dispatch.rs. Per D-10, D-12:

    1. In `operators_for()`:
       Add arm:
       ```rust
       ConditionAttribute::SourceVolumeClass | ConditionAttribute::DestinationVolumeClass => {
           &[("eq", true), ("ne", true), ("in", true)]
       }
       ```

    2. In `value_count_for()`:
       Add arm returning 6 for both SourceVolumeClass and DestinationVolumeClass.

    3. Add `build_volume_class_condition(op: String, picker_selected: usize, is_source: bool) -> Option<PolicyCondition>` helper:
       - Map picker_selected 0-5 to VolumeClass variants: LocalNTFS, USBRemovable, SDCard, Optical, Virtual, NetworkShare
       - If picker_selected > 5, return None
       - If is_source, return Some(PolicyCondition::SourceVolumeClass { op, value })
       - Else return Some(PolicyCondition::DestinationVolumeClass { op, value })

    4. In `build_condition()`:
       Add arms calling build_volume_class_condition:
       ```rust
       ConditionAttribute::SourceVolumeClass => {
           build_volume_class_condition(op, picker_selected, true)
       }
       ConditionAttribute::DestinationVolumeClass => {
           build_volume_class_condition(op, picker_selected, false)
       }
       ```

    5. Add `volume_class_to_idx(value: &VolumeClass) -> usize` helper:
       - LocalNTFS => 0, USBRemovable => 1, SDCard => 2, Optical => 3, Virtual => 4, NetworkShare => 5

    6. In `condition_to_prefill()`:
       Add arms:
       ```rust
       PolicyCondition::SourceVolumeClass { op, value } => (
           ConditionAttribute::SourceVolumeClass,
           op.clone(),
           volume_class_to_idx(value),
           String::new(),
       ),
       PolicyCondition::DestinationVolumeClass { op, value } => (
           ConditionAttribute::DestinationVolumeClass,
           op.clone(),
           volume_class_to_idx(value),
           String::new(),
       ),
       ```

    7. In `condition_display()`:
       Add arms:
       ```rust
       PolicyCondition::SourceVolumeClass { op, value } => {
           format!("SourceVolumeClass {op} {value}")
       }
       PolicyCondition::DestinationVolumeClass { op, value } => {
           format!("DestinationVolumeClass {op} {value}")
       }
       ```

    Ensure all imports for VolumeClass and PolicyCondition variants are present.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli dispatch --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SourceVolumeClass" dlp-admin-cli/src/screens/dispatch.rs` returns lines in operators_for, build_condition, condition_to_prefill, and condition_display
    - `grep -n "fn build_volume_class_condition" dlp-admin-cli/src/screens/dispatch.rs` returns a line
    - `grep -n "fn volume_class_to_idx" dlp-admin-cli/src/screens/dispatch.rs` returns a line
    - `cargo test -p dlp-admin-cli` compiles and passes
    - A unit test `test_build_source_volume_class_condition` exists and passes: builds SourceVolumeClass eq Optical from picker_selected=3, asserts correct PolicyCondition
    - A unit test `test_build_destination_volume_class_condition` exists and passes: builds DestinationVolumeClass ne LocalNTFS from picker_selected=0, asserts correct PolicyCondition
    - A unit test `test_volume_class_prefill_roundtrip` exists and passes: creates SourceVolumeClass eq SDCard, calls condition_to_prefill, asserts attribute==SourceVolumeClass, op=="eq", picker_selected==2
    - A unit test `test_condition_display_volume_class` exists and passes: asserts condition_display(SourceVolumeClass eq Optical) == "SourceVolumeClass eq Optical"
  </acceptance_criteria>
  <done>dispatch.rs fully extended for volume class conditions: operators, builder, prefill, display</done>
</task>

<task type="auto">
  <name>Task 3: Extend render.rs with volume class dropdown and allowlist.rs with badges</name>
  <files>dlp-admin-cli/src/screens/render.rs, dlp-admin-cli/src/screens/allowlist.rs</files>
  <read_first>
    dlp-admin-cli/src/screens/render.rs
    dlp-admin-cli/src/screens/allowlist.rs
    dlp-admin-cli/src/app.rs
    .planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-PATTERNS.md
  </read_first>
  <action>
    Extend the TUI rendering for volume class dropdowns and allowlist badges. Per D-10, D-11:

    1. In dlp-admin-cli/src/screens/render.rs:
       a. Add constant:
          ```rust
          const VOLUME_CLASS_VALUES: [&str; 6] = [
              "LocalNTFS", "USBRemovable", "SDCard", "Optical", "Virtual", "NetworkShare",
          ];
          ```
       b. In `picker_items()` Step 3 match on selected_attribute:
          Add arm:
          ```rust
          ConditionAttribute::SourceVolumeClass
          | ConditionAttribute::DestinationVolumeClass => VOLUME_CLASS_VALUES
              .iter()
              .map(|v| ListItem::new(v.to_string()))
              .collect(),
          ```
       c. In `step_flags()`:
          Volume class attributes do NOT have a sub-step (no AppField equivalent). They use the picker in Step 3, so no text input. Ensure the existing is_text_input_step3 logic does NOT include volume class attributes. No change may be needed if the logic is based on specific attribute variants.

    2. In dlp-admin-cli/src/screens/allowlist.rs:
       a. Add `volume_class: Option<String>` field to `AllowlistEntryUi` struct.
       b. In the render function where ListItem is constructed for each entry:
          Append volume class badge if present:
          ```rust
          let volume_badge = entry.volume_class.as_ref()
              .map(|v| format!(" | [{}]", v))
              .unwrap_or_default();
          ```
          And include it in the format string.
       c. Use colored badges per CONTEXT.md specifics:
          - LocalNTFS = blue (ratatui::style::Color::Blue)
          - USBRemovable = yellow (Color::Yellow)
          - SDCard = magenta (Color::Magenta)
          - Optical = cyan (Color::Cyan)
          - Virtual = red (Color::Red)
          - NetworkShare = green (Color::Green)
          Use Span::styled with the appropriate color for the badge text.

    Do NOT change the overall layout or navigation of the Conditions Builder.
    Do NOT break existing allowlist screen rendering for entries without volume_class.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli render --lib</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VOLUME_CLASS_VALUES" dlp-admin-cli/src/screens/render.rs` returns a line
    - `grep -n "SourceVolumeClass" dlp-admin-cli/src/screens/render.rs` returns a line in picker_items
    - `grep -n "volume_class: Option<String>" dlp-admin-cli/src/screens/allowlist.rs` returns a line
    - `cargo test -p dlp-admin-cli` compiles and passes
    - A unit test `test_picker_items_volume_class` exists and passes: calls picker_items with Step 3 and SourceVolumeClass, asserts 6 items returned with correct labels
    - A unit test `test_allowlist_render_with_volume_class_badge` exists and passes: creates AllowlistEntryUi with volume_class=Some("Virtual".to_string()), asserts rendered text contains "[Virtual]"
    - A unit test `test_allowlist_render_without_volume_class` exists and passes: creates AllowlistEntryUi with volume_class=None, asserts rendered text does NOT contain "| ["
  </acceptance_criteria>
  <done>render.rs shows volume class dropdown; allowlist.rs renders colored volume class badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin TUI -> PolicyCondition | Operator builds conditions; TUI generates PolicyCondition JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-14 | Tampering | Invalid picker index | mitigate | build_volume_class_condition returns None for index > 5, preventing out-of-bounds panic |
| T-56-15 | Spoofing | Volume class badge display | accept | Badge is display-only; actual enforcement happens server-side |
| T-56-16 | Denial of Service | Large ATTRIBUTES array | accept | 11 elements is negligible; no performance impact |
</threat_model>

<verification>
- cargo test -p dlp-admin-cli passes (all new + existing tests)
- cargo clippy -p dlp-admin-cli -- -D warnings passes
- cargo fmt --check passes
</verification>

<success_criteria>
- ConditionAttribute has 11 variants including SourceVolumeClass and DestinationVolumeClass
- Conditions Builder offers eq/ne/in for volume class and shows 6-value dropdown
- build_condition creates correct PolicyCondition variants
- condition_to_prefill round-trips volume class conditions correctly
- condition_display renders readable strings
- AllowlistScreen renders volume class badges with correct colors
- All dlp-admin-cli tests pass
</success_criteria>

<output>
Create `.planning/phases/56-sd-optical-virtual-drive-enumeration-volume-class-abac-seed-/56-05-SUMMARY.md` when done
</output>
