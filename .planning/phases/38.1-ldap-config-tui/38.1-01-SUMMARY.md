---
phase: 38.1-ldap-config-tui
plan: 01
subsystem: ui
tags: [ratatui, tui, ldap, active-directory, screen-enum, dlp-admin-cli]

# Dependency graph
requires:
  - phase: 07
    provides: "GET/PUT /admin/ldap-config endpoints + LdapConfigPayload struct"
  - phase: 28
    provides: "Screen enum pattern (SiemConfig, AlertConfig variants) used as the template for LdapConfig"
provides:
  - "Screen::LdapConfig variant in dlp-admin-cli matching SiemConfig/AlertConfig shape"
  - "Public constants LDAP_ROW_COUNT, LDAP_SAVE_ROW, LDAP_BACK_ROW for downstream dispatch + render plans"
affects: [38.1-02-dispatch, 38.1-03-render]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Screen variant follows the canonical SiemConfig/AlertConfig shape: { config, selected, editing, buffer }"
    - "Row-count / Save-row / Back-row consts exposed as `pub const` so dispatch and render share a single source of truth"

key-files:
  created: []
  modified:
    - "dlp-admin-cli/src/app.rs"

key-decisions:
  - "LDAP form has 7 rows: 5 editable fields + Save + Back, mirroring AlertConfig sizing"
  - "Editable field order locked to 0:ldap_url, 1:base_dn, 2:require_tls, 3:cache_ttl_secs, 4:vpn_subnets to match server LdapConfigPayload"
  - "cache_ttl_secs validation range [60, 3600] documented in the variant doc comment so dispatch (38.1-02) can read it without re-deriving"

patterns-established:
  - "Type-first scaffolding: introduce the Screen variant in a standalone wave-1 plan so dispatch + render plans can run in parallel against a stable contract"

requirements-completed: [ADMIN-05]

# Metrics
duration: 5min
completed: 2026-05-05
---

# Phase 38.1 Plan 01: Screen::LdapConfig Variant Summary

**Adds `Screen::LdapConfig { config, selected, editing, buffer }` and three public row-index constants to `dlp-admin-cli/src/app.rs`, establishing the type-level contract consumed by the parallel dispatch (38.1-02) and render (38.1-03) plans.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-05T02:20:00Z
- **Completed:** 2026-05-05T02:25:09Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `Screen::LdapConfig` variant with the same `{ config, selected, editing, buffer }` shape as `SiemConfig` / `AlertConfig`, including a doc comment that names the five JSON keys, fixes the editable-row order, and documents the `cache_ttl_secs` `[60, 3600]` validation range.
- Added three `pub const` row indices (`LDAP_ROW_COUNT = 7`, `LDAP_SAVE_ROW = 5`, `LDAP_BACK_ROW = 6`) so plans 38.1-02 and 38.1-03 share a single source of truth for the form geometry.
- Confirmed the build now reports ONLY the expected non-exhaustive-match `error[E0004]` against `Screen::LdapConfig` in `dispatch.rs` and `render.rs` — no syntax / parse / undefined-symbol errors. Downstream plans close those arms.

## Task Commits

1. **Task 1: Add Screen::LdapConfig variant + LDAP row constants to app.rs** — `1da8445` (feat)

## Files Created/Modified

- `dlp-admin-cli/src/app.rs` — Added `LDAP_ROW_COUNT` / `LDAP_SAVE_ROW` / `LDAP_BACK_ROW` consts (immediately after `SIMULATE_SUBMIT_ROW`) and inserted the `LdapConfig` variant in the `Screen` enum (immediately after `AlertConfig`, before `ConditionsBuilder`).

## Decisions Made

None - followed plan exactly as specified. Editable-field order, row counts, and validation range all came from the plan's interface block.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Verification Results

- `grep -c "    LdapConfig \\{"` -> 1 (variant present)
- `grep -c "    SiemConfig \\{"` -> 1 (preserved)
- `grep -c "    AlertConfig \\{"` -> 1 (preserved)
- `grep -c "LDAP_ROW_COUNT: usize = 7"` -> 1
- `grep -c "LDAP_SAVE_ROW: usize = 5"` -> 1
- `grep -c "LDAP_BACK_ROW: usize = 6"` -> 1
- `grep -c "ldap_url"` in `app.rs` -> 1 (in doc comment)
- `grep -c "cache_ttl_secs"` in `app.rs` -> 2 (in doc comment)
- `cargo check --package dlp-admin-cli` -> 2 `error[E0004]` non-exhaustive-match errors, both citing `Screen::LdapConfig` (expected; closed by 38.1-02 + 38.1-03). No other diagnostics.
- `cargo fmt --package dlp-admin-cli --check` against `app.rs` -> 0 issues. (Pre-existing fmt drift in `render.rs` and other unrelated files is out of scope per executor SCOPE BOUNDARY.)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 38.1-02 (dispatch) can now match against `Screen::LdapConfig` and reuse `LDAP_SAVE_ROW` / `LDAP_BACK_ROW` for keymap branching.
- Plan 38.1-03 (render) can now match against `Screen::LdapConfig` and reuse `LDAP_ROW_COUNT` for the form layout.
- The two plans can execute in parallel because both depend only on the variant + consts shipped here.

## Self-Check: PASSED

- File `dlp-admin-cli/src/app.rs` exists with all required tokens: FOUND
- Commit `1da8445` exists in `git log`: FOUND

---
*Phase: 38.1-ldap-config-tui*
*Completed: 2026-05-05*
