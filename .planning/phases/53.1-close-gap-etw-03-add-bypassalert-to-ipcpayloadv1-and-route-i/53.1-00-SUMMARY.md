---
phase: 53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i
plan: 00
wave: 0
subsystem: IPC Protocol + Bypass Correlator
status: complete
completed_date: 2026-06-17
---

# Phase 53.1 Plan 00: Wave 0 Test Stubs (Nyquist Compliance — Explicit Red State)

## Summary

Seeded 4 test stubs across 4 crates to satisfy the Nyquist Wave 0 requirement: every behavior-adding task must have a test that exists before the implementation. These stubs reference symbols that will be created in Waves 1-3 (IpcPayloadV1::BypassAlert variant, handle_connection bypass_tx routing, BypassCorrelator::submit_bypass_alert method, and build_bypass_alert_envelope helper). The stubs are behind `#[cfg(test)]` so the production build (`cargo build --workspace`) is unaffected. Test compilation fails only for the expected missing symbols — this is the intentional red state.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | test_ipc_payload_bypass_alert_roundtrip stub | 62ab5b9 | dlp-common/src/hook_ipc.rs |
| 2 | test_handle_connection_routes_bypass_alert stub | 8193d53 | dlp-agent/src/hook_ipc.rs |
| 3 | test_submit_bypass_alert_batches stub | 0db9f82 | dlp-agent/src/bypass_correlator.rs |
| 4 | test_build_bypass_alert_envelope stub | 38a7986 | dlp-hook-dll/src/ntdll_patcher.rs |

## Key Decisions

- **Red-state semantics preserved**: All 4 stubs reference not-yet-existing symbols. The test compilation failure is expected and will be resolved by Wave 1/2/3 implementation. Per REVIEW-M-06 and D-05.
- **Stub placement**: Each stub follows the exact pattern of existing tests in the same file (roundtrip tests in dlp-common, routing tests in dlp-agent, batch tests in bypass_correlator, pure helper tests in ntdll_patcher).
- **Production build unaffected**: `cargo build --workspace` passes cleanly (36.11s). Stubs are behind `#[cfg(test)]`.

## Verification Results

| Check | Expected | Actual | Status |
|-------|----------|--------|--------|
| `cargo build --workspace` | Pass | Pass | OK |
| `cargo test --workspace` | Fail only for missing symbols | Fail: IpcPayloadV1::BypassAlert not found | OK (red state) |
| `rg test_ipc_payload_bypass_alert_roundtrip` | >= 1 | 1 | OK |
| `rg test_handle_connection_routes_bypass_alert` | >= 1 | 1 | OK |
| `rg test_submit_bypass_alert_batches` | >= 1 | 1 | OK |
| `rg test_build_bypass_alert_envelope` | >= 1 | 1 | OK |

## Red State Details

The `cargo test --workspace` compilation fails with exactly 2 errors, both in `dlp-common/src/hook_ipc.rs`:

```
error[E0599]: no variant or associated item named `BypassAlert` found for enum `hook_ipc::IpcPayloadV1`
```

This is expected — the `IpcPayloadV1::BypassAlert(BypassAlert)` variant will be added in Wave 1 Plan 01. Once that variant exists, the other stubs (Tasks 2-4) will also compile since they depend on the same variant or downstream symbols that will be added in subsequent waves.

## Deviations from Plan

None — plan executed exactly as written.

## Threat Surface

No new production threat surface introduced. Test-only code behind `#[cfg(test)]`.

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-53.1-00 | Information Disclosure | test stubs | accept | Test code only; no production data or secrets |

## Next Steps

- Wave 1 Plan 01: Add `BypassAlert(BypassAlert)` variant to `IpcPayloadV1` in `dlp-common/src/hook_ipc.rs`
- Wave 2 Plan 02: Update `handle_connection` to accept `bypass_tx` and route `BypassAlert` payloads; add `submit_bypass_alert` to `BypassCorrelator`
- Wave 2 Plan 03: Add `build_bypass_alert_envelope` pure helper to `dlp-hook-dll`

## Self-Check: PASSED

- [x] All 4 test stubs exist in their respective `#[cfg(test)]` modules
- [x] Each stub follows the pattern of existing tests in the same file
- [x] Each stub references symbols that will be created in Wave 1/2
- [x] `cargo build --workspace` passes (production build unaffected)
- [x] `cargo test --workspace` fails only for expected missing symbols (explicit red state)
- [x] All 4 commits recorded with proper hashes
- [x] No file deletions in any commit
- [x] No untracked files left behind
