---
phase: 53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i
plan: 01
subsystem: ipc
tags: [ipc, bincode, serde, bypass-alert, dlp-common, hook-dll]

# Dependency graph
requires:
  - phase: 53-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i
    provides: "BypassAlert struct with v2 fields in dlp-common/src/hook_ipc.rs"
provides:
  - "IpcPayloadV1::BypassAlert(BypassAlert) variant for IPC protocol v1"
  - "bincode round-trip test proving serialization fidelity"
affects:
  - 53.1-02-PLAN.md
  - 53.1-03-PLAN.md
  - dlp-agent/src/hook_ipc.rs
  - dlp-hook-dll/src/ntdll_patcher.rs

# Tech tracking
tech-stack:
  added: []
  patterns: ["enum variant extension with backward compat via serde(default)"]

key-files:
  created: []
  modified:
    - dlp-common/src/hook_ipc.rs
    - dlp-agent/src/hook_ipc.rs
    - dlp-hook-dll/src/ntdll_patcher.rs

key-decisions:
  - "Added BypassAlert as 5th discriminant (not replacing existing) to preserve protocol stability"
  - "Reused existing BypassAlert struct with all v2 fields already present and #[serde(default)]"

patterns-established:
  - "IPC enum extension: add new variant at end, keep existing variants unchanged, use serde(default) for struct fields"

requirements-completed:
  - ETW-03

# Metrics
duration: 8min
completed: 2026-06-17
---

# Phase 53.1 Plan 01: Extend IpcPayloadV1 with BypassAlert Variant Summary

**IpcPayloadV1 enum extended with BypassAlert(BypassAlert) variant enabling hook-derived bypass alerts to traverse the agent named pipe IPC**

## Performance

- **Duration:** 8 min
- **Started:** 2026-06-17T00:00:00Z
- **Completed:** 2026-06-17T00:08:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Added `BypassAlert(BypassAlert)` as the 5th discriminant to `IpcPayloadV1` in `dlp-common/src/hook_ipc.rs`
- Verified `test_ipc_payload_bypass_alert_roundtrip` compiles and passes (Wave 0 Nyquist anchor test)
- Confirmed bincode round-trip serialization/deserialization preserves all 14 BypassAlert fields
- All 309 dlp-common tests pass, clippy clean (-D warnings), cargo fmt clean
- Cross-crate references in dlp-agent and dlp-hook-dll compile cleanly

## Task Commits

Each task was committed atomically:

1. **Task 1: Add BypassAlert(BypassAlert) variant to IpcPayloadV1** - `46f601b` (feat)
2. **Task 2: Make Wave 0 round-trip test compile and pass** - `83566be` (style - formatting fixes for cross-crate references)

## Files Created/Modified
- `dlp-common/src/hook_ipc.rs` - Added `BypassAlert(BypassAlert)` variant to `IpcPayloadV1` enum with doc comment; fixed rustfmt in test assertion
- `dlp-agent/src/hook_ipc.rs` - Rustfmt formatting fix for envelope construction referencing new variant
- `dlp-hook-dll/src/ntdll_patcher.rs` - Rustfmt formatting fix for `build_bypass_alert_envelope` calls referencing new variant

## Decisions Made
- Added BypassAlert as the 5th discriminant (after VolumeClassResponse) rather than reordering existing variants, preserving protocol stability and bincode layout compatibility
- Reused the existing `BypassAlert` struct already defined in `dlp-common/src/hook_ipc.rs` (lines 185-232) with all v2 fields and `#[serde(default)]` backward compatibility — no struct changes needed

## Deviations from Plan

None - plan executed exactly as written.

### Auto-fixed Issues

None.

## Issues Encountered
- `cargo fmt --check` flagged formatting issues in `dlp-agent/src/hook_ipc.rs` and `dlp-hook-dll/src/ntdll_patcher.rs` that reference the new variant. These were pre-existing files that rustfmt wanted reformatted after the variant addition. Fixed inline and committed as style commit.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- IpcPayloadV1 now carries BypassAlert — ready for Plan 02 (agent hook_ipc routing) and Plan 03 (hook DLL envelope wrapping)
- No blockers

## Self-Check: PASSED
- [x] `dlp-common/src/hook_ipc.rs` contains `BypassAlert(BypassAlert)` variant
- [x] `test_ipc_payload_bypass_alert_roundtrip` passes
- [x] All 309 dlp-common tests pass
- [x] Clippy clean on dlp-common, dlp-agent, dlp-hook-dll
- [x] cargo fmt clean
- [x] Commits `46f601b` and `83566be` exist in git log

---
*Phase: 53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i*
*Plan: 01*
*Completed: 2026-06-17*
