---
phase: 53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - dlp-common/src/hook_ipc.rs
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/bypass_correlator.rs
  - dlp-hook-dll/src/ntdll_patcher.rs
autonomous: true
requirements:
  - ETW-03

must_haves:
  truths:
    - "Wave 0 test stubs exist for all 4 test functions before Wave 1 implementation"
    - "Each stub calls not-yet-existing symbols that Wave 1/2 will provide"
    - "Tests compile (with expected errors) but serve as Nyquist verification anchors"
  artifacts:
    - path: "dlp-common/src/hook_ipc.rs"
      provides: "test_ipc_payload_bypass_alert_roundtrip stub"
      contains: "fn test_ipc_payload_bypass_alert_roundtrip"
    - path: "dlp-agent/src/hook_ipc.rs"
      provides: "test_handle_connection_routes_bypass_alert stub"
      contains: "fn test_handle_connection_routes_bypass_alert"
    - path: "dlp-agent/src/bypass_correlator.rs"
      provides: "test_submit_bypass_alert_batches stub"
      contains: "fn test_submit_bypass_alert_batches"
    - path: "dlp-hook-dll/src/ntdll_patcher.rs"
      provides: "test_emit_bypass_alert_uses_envelope stub"
      contains: "fn test_emit_bypass_alert_uses_envelope"
  key_links:
    - from: "Wave 0 test stubs"
      to: "Wave 1/2 implementation"
      via: "Nyquist verification chain"
      pattern: "test_ functions that call symbols created in later waves"
---

## Phase 53.1 — Plan 00: Wave 0 Test Stubs (Nyquist Compliance)

**Objective:** Create the 4 failing/compilation-stub test functions that Wave 1 and Wave 2 implementation will satisfy. This satisfies the Nyquist Wave 0 requirement: every behavior-adding task must have a test that exists before the implementation.

**Purpose:** Per the VALIDATION.md Wave 0 gap list, these tests are currently missing. Without them, the Wave 1 and Wave 2 plans have `<verify>` blocks that reference non-existent tests. This plan seeds the stubs so the verification chain is complete. Per D-05 (Testing decision).

**Output:** 4 test stubs in 4 files, each calling symbols that will exist after Wave 1/2.

---

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i/53.1-CONTEXT.md
@.planning/phases/53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i/53.1-RESEARCH.md
@.planning/phases/53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i/53.1-VALIDATION.md

@dlp-common/src/hook_ipc.rs
@dlp-agent/src/hook_ipc.rs
@dlp-agent/src/bypass_correlator.rs
@dlp-hook-dll/src/ntdll_patcher.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test_ipc_payload_bypass_alert_roundtrip stub to dlp-common</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <behavior>
    - Test 1: Construct a BypassAlert with all fields populated (reason=HookOverwritten, stub_name="NtCreateFile", pid=1234, timestamp_secs=now, version=1, agent_id="test-agent", image_path="C:\\test.exe", image_sha256=Some("abc123"), file_path="C:\\secret.txt", operation="Create", file_object=0xDEADBEEF, qpc_timestamp=9999, severity="crit", correlation_reason="HookSelfReported")
    - Test 2: Wrap it in IpcEnvelope::V1(IpcMessageV1 { payload: IpcPayloadV1::BypassAlert(alert) }) — this will fail to compile until Wave 1 adds the BypassAlert variant
    - Test 3: Serialize with bincode::serialize, deserialize with bincode::deserialize::<IpcEnvelope>, assert_eq round-trip
    - Test 4: Verify the deserialized payload is IpcPayloadV1::BypassAlert and all fields match original
  </behavior>
  <action>
    Add a `#[test]` function `test_ipc_payload_bypass_alert_roundtrip` in the `#[cfg(test)]` module at the bottom of `dlp-common/src/hook_ipc.rs`, following the exact pattern of the existing `test_ipc_payload_volume_class_query_roundtrip` and `test_ipc_payload_volume_class_response_roundtrip` tests (lines 625-646). The test must construct a fully populated `BypassAlert`, wrap it in the envelope, serialize, deserialize, and assert equality. Use `std::time::SystemTime::now()` for timestamp. The test will fail to compile until Wave 1 Plan 01 adds the `BypassAlert(BypassAlert)` variant to `IpcPayloadV1`. This is intentional — the stub serves as the Nyquist anchor. Per D-05.
  </action>
  <verify>
    <automated>grep -c "fn test_ipc_payload_bypass_alert_roundtrip" dlp-common/src/hook_ipc.rs</automated>
  </verify>
  <done>Test stub `test_ipc_payload_bypass_alert_roundtrip` exists in the `#[cfg(test)]` module, following the pattern of existing round-trip tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_handle_connection_routes_bypass_alert stub to dlp-agent hook_ipc</name>
  <files>dlp-agent/src/hook_ipc.rs</files>
  <behavior>
    - Test 1: Mock a pipe with a frame containing IpcEnvelope::V1(IpcPayloadV1::BypassAlert(alert)) — assert handle_connection sends the alert through the bypass_tx channel
    - Test 2: Mock a pipe with a frame containing IpcEnvelope::V1(IpcPayloadV1::Request(req)) — assert the HookHandler is called and a HookResponse is written back
    - Test 3: Mock a pipe with a malformed frame — assert the function logs a warning and continues (does not panic or break the loop)
    - Test 4: Mock a pipe with IpcPayloadV1::VolumeClassResponse — assert a warning is logged and the loop continues (hook DLL should never send this)
  </behavior>
  <action>
    Add a `#[test]` function `test_handle_connection_routes_bypass_alert` in the `#[cfg(test)]` module at the bottom of `dlp-agent/src/hook_ipc.rs` (around line 478). The test must mock a pipe connection, send an `IpcEnvelope::V1(IpcPayloadV1::BypassAlert(...))` frame, and verify that `handle_connection` routes the alert to a `crossbeam_channel::Sender<BypassAlert>`. The test will fail to compile until Wave 2 Plan 02 updates `handle_connection` to accept the `bypass_tx` parameter and match on `IpcPayloadV1::BypassAlert`. This is intentional — the stub serves as the Nyquist anchor. Per D-02 and D-05.
  </action>
  <verify>
    <automated>grep -c "fn test_handle_connection_routes_bypass_alert" dlp-agent/src/hook_ipc.rs</automated>
  </verify>
  <done>Test stub `test_handle_connection_routes_bypass_alert` exists in the `#[cfg(test)]` module</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add test_submit_bypass_alert_batches stub to dlp-agent bypass_correlator</name>
  <files>dlp-agent/src/bypass_correlator.rs</files>
  <behavior>
    - Test 1: Create a BypassCorrelator with a mock ServerClient and empty channels
    - Test 2: Call submit_bypass_alert with a fully populated BypassAlert (reason=HookOverwritten, stub_name="NtCreateFile", pid=1234, ...)
    - Test 3: Assert the alert appears in the internal alert_batch Vec<PendingAlert> within 100ms
    - Test 4: Verify the PendingAlert has a valid UUID batch_id and retry_count=0
    - Test 5: Verify that calling submit_bypass_alert does NOT trigger ETW correlation logic (no journal lookup, no path-hash computation)
  </behavior>
  <action>
    Add a `#[test]` function `test_submit_bypass_alert_batches` in the `#[cfg(test)]` module at the bottom of `dlp-agent/src/bypass_correlator.rs` (around line 946). The test must create a `BypassCorrelator`, call `submit_bypass_alert` with a fully populated `BypassAlert`, and verify the alert appears in the batch. The test will fail to compile until Wave 2 Plan 02 adds the `submit_bypass_alert` method to `BypassCorrelator`. This is intentional — the stub serves as the Nyquist anchor. Per D-03 and D-05.
  </action>
  <verify>
    <automated>grep -c "fn test_submit_bypass_alert_batches" dlp-agent/src/bypass_correlator.rs</automated>
  </verify>
  <done>Test stub `test_submit_bypass_alert_batches` exists in the `#[cfg(test)]` module</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add test_emit_bypass_alert_uses_envelope stub to dlp-hook-dll</name>
  <files>dlp-hook-dll/src/ntdll_patcher.rs</files>
  <behavior>
    - Test 1: `test_emit_bypass_alert_uses_envelope` — construct a BypassAlert, manually wrap it in IpcEnvelope::V1(IpcPayloadV1::BypassAlert(...)), serialize, and verify the bytes deserialize back to the same envelope
    - Test 2: Verify the deserialized payload variant is BypassAlert (not Request or VolumeClassQuery)
    - Test 3: Verify the alert fields survive round-trip: reason=HookOverwritten, stub_name="NtCreateFile", pid matches current process, version=1
    - Test 4: Test with a second reason variant (e.g., PatchRaced) to ensure the pattern works for all BypassReason values
  </behavior>
  <action>
    Add a `#[test]` function `test_emit_bypass_alert_uses_envelope` in a `#[cfg(test)]` module at the bottom of `dlp-hook-dll/src/ntdll_patcher.rs`. The test must construct a `BypassAlert`, wrap it in `IpcEnvelope::V1(IpcMessageV1 { payload: IpcPayloadV1::BypassAlert(alert) })`, serialize with `bincode::serialize`, deserialize with `bincode::deserialize::<IpcEnvelope>`, and assert the deserialized payload is `IpcPayloadV1::BypassAlert` with all fields matching. The test will fail to compile until Wave 1 Plan 01 adds the `BypassAlert(BypassAlert)` variant to `IpcPayloadV1`. This is intentional — the stub serves as the Nyquist anchor. If the `#[cfg(test)]` module does not exist, create one at the bottom of the file following the pattern from other modules in the crate. Per D-04 and D-05.
  </action>
  <verify>
    <automated>grep -c "fn test_emit_bypass_alert_uses_envelope" dlp-hook-dll/src/ntdll_patcher.rs</automated>
  </verify>
  <done>Test stub `test_emit_bypass_alert_uses_envelope` exists in the `#[cfg(test)]` module</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production code | Test stubs reference production symbols; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53.1-00 | Information Disclosure | test stubs | accept | Test code only; no production data or secrets |
</threat_model>

<verification>
- `grep -c "fn test_ipc_payload_bypass_alert_roundtrip" dlp-common/src/hook_ipc.rs` returns >= 1
- `grep -c "fn test_handle_connection_routes_bypass_alert" dlp-agent/src/hook_ipc.rs` returns >= 1
- `grep -c "fn test_submit_bypass_alert_batches" dlp-agent/src/bypass_correlator.rs` returns >= 1
- `grep -c "fn test_emit_bypass_alert_uses_envelope" dlp-hook-dll/src/ntdll_patcher.rs` returns >= 1
</verification>

<success_criteria>
- All 4 test stubs exist in their respective `#[cfg(test)]` modules
- Each stub follows the pattern of existing tests in the same file
- Each stub references symbols that will be created in Wave 1/2 (will not compile until then)
- No existing tests are broken
</success_criteria>

<output>
Create `.planning/phases/53.1-close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i/53.1-00-SUMMARY.md` when done
</output>
