---
phase: 53.1
slug: close-gap-etw-03-add-bypassalert-to-ipcpayloadv1-and-route-i
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-17
revised: 2026-06-17
---

# Phase 53.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Revised to address review concerns: explicit red-state semantics, portable validation commands,
> one-way helper testing, agent enrichment verification, and legacy fallback coverage.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Built-in `#[test]` + `cargo test` |
| **Config file** | None — tests embedded in source modules |
| **Quick run command** | `cargo test -p dlp-common hook_ipc -- --test-threads=1` |
| **Full suite command** | `cargo test --workspace` |
| **Estimated runtime** | ~60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p {crate} {module} -- --test-threads=1` for the crate modified by the task
- **After every plan wave:** Run `cargo test --workspace`
- **Before `/gsd-verify-work`:** Full workspace suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 53.1-00-01 | 00 | 0 | ETW-03 | — | `test_ipc_payload_bypass_alert_roundtrip` stub exists in dlp-common | unit stub | `rg -c "fn test_ipc_payload_bypass_alert_roundtrip" dlp-common/src/hook_ipc.rs` | ✅ W0 | ✅ seeded |
| 53.1-00-02 | 00 | 0 | ETW-03 | — | `test_handle_connection_routes_bypass_alert` stub exists in dlp-agent | unit stub | `rg -c "fn test_handle_connection_routes_bypass_alert" dlp-agent/src/hook_ipc.rs` | ✅ W0 | ✅ seeded |
| 53.1-00-03 | 00 | 0 | ETW-03 | — | `test_submit_bypass_alert_batches` stub exists in bypass_correlator | unit stub | `rg -c "fn test_submit_bypass_alert_batches" dlp-agent/src/bypass_correlator.rs` | ✅ W0 | ✅ seeded |
| 53.1-00-04 | 00 | 0 | ETW-03 | — | `test_build_bypass_alert_envelope` stub exists in dlp-hook-dll | unit stub | `rg -c "fn test_build_bypass_alert_envelope" dlp-hook-dll/src/ntdll_patcher.rs` | ✅ W0 | ✅ seeded |
| 53.1-00-05 | 00 | 0 | ETW-03 | — | `cargo build --workspace` passes (stubs behind #[cfg(test)]) | compile check | `cargo build --workspace` | ✅ W0 | ✅ seeded |
| 53.1-01-01 | 01 | 1 | ETW-03 | T-53.1-01 / — | `IpcPayloadV1::BypassAlert` round-trips through bincode without data loss | unit | `cargo test -p dlp-common test_ipc_payload_bypass_alert_roundtrip -- --test-threads=1` | ✅ W0 | ⬜ pending |
| 53.1-02-01 | 02 | 2 | ETW-03 | T-53.1-02 / — | Agent `handle_connection` routes `BypassAlert` payload to correlator, not ABAC | unit | `cargo test -p dlp-agent test_handle_connection_routes_bypass_alert -- --test-threads=1` | ✅ W0 | ⬜ pending |
| 53.1-02-02 | 02 | 2 | ETW-03 | T-53.1-03 / — | `BypassCorrelator::submit_bypass_alert` batches hook-derived alert with enrichment | unit | `cargo test -p dlp-agent test_submit_bypass_alert_batches -- --test-threads=1` | ✅ W0 | ⬜ pending |
| 53.1-02-03 | 02 | 2 | ETW-03 | T-53.1-02 / — | Legacy raw `HookRequest` frames still handled (backward compat) | unit | `cargo test -p dlp-agent hook_ipc -- --test-threads=1` (includes legacy fallback test) | ✅ W0 | ⬜ pending |
| 53.1-03-01 | 03 | 2 | ETW-03 | T-53.1-04 / — | `build_bypass_alert_envelope` returns correct `IpcEnvelope::V1` shape | unit | `cargo test -p dlp-hook-dll test_build_bypass_alert_envelope -- --test-threads=1` | ✅ W0 | ⬜ pending |
| 53.1-03-02 | 03 | 2 | ETW-03 | T-53.1-07 / — | `send_raw_oneway` writes and closes without reading | unit | `cargo test -p dlp-hook-dll pipe_client -- --test-threads=1` | ✅ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (Explicit Red State)

- [x] `dlp-common/src/hook_ipc.rs` — add `test_ipc_payload_bypass_alert_roundtrip` (seeded by Plan 00)
- [x] `dlp-agent/src/hook_ipc.rs` — add `test_handle_connection_routes_bypass_alert` (seeded by Plan 00)
- [x] `dlp-agent/src/bypass_correlator.rs` — add `test_submit_bypass_alert_batches` (seeded by Plan 00)
- [x] `dlp-hook-dll/src/ntdll_patcher.rs` — add `test_build_bypass_alert_envelope` (seeded by Plan 00)
- [x] `cargo build --workspace` passes before and after stub insertion (stubs are behind `#[cfg(test)]`)

All Wave 0 test stubs are seeded in Plan 00. They will fail to compile until Wave 1/2 implementation provides the symbols they call. This is the **expected red state** — the crate compiles, but `cargo test` fails for expected missing symbols. This satisfies Nyquist compliance per REVIEW-M-07.

---

## Review Concern Coverage

| Review Concern | Severity | Plan | Task | Validation Command |
|----------------|----------|------|------|---------------------|
| REVIEW-H-01: Legacy HookRequest clients break | HIGH | 02 | Task 2 | `cargo test -p dlp-agent test_handle_connection_routes_bypass_alert -- --test-threads=1` (includes legacy fallback test) |
| REVIEW-H-02: BypassAlert uses response-waiting helper | HIGH | 03 | Task 1 | `rg -c "fn send_raw_oneway" dlp-hook-dll/src/pipe_client.rs` |
| REVIEW-H-03: Hook-derived alerts have empty attribution | HIGH | 02 | Task 1 | `cargo test -p dlp-agent test_submit_bypass_alert_batches -- --test-threads=1` (enrichment assertions) |
| REVIEW-M-01: `send` described as non-blocking | MEDIUM | 02 | Task 3 | `rg -c "crossbeam_channel::unbounded" dlp-agent/src/service.rs` |
| REVIEW-M-05: VolumeClassQuery handling ambiguous | MEDIUM | 02 | Task 2 | `cargo test -p dlp-agent hook_ipc -- --test-threads=1` (VolumeClassQuery routing test) |
| REVIEW-M-06: HookIpcServer wiring underspecified | MEDIUM | 02 | Task 3 | `rg -c "TODO(WORKFLOW-04-followup)" dlp-agent/src/service.rs` (documented as not yet wired) |
| REVIEW-M-07: Wave 0 red-state semantics | MEDIUM | 00 | Task 5 | `cargo build --workspace` passes; `cargo test` fails only for expected missing symbols |
| REVIEW-M-08: Hook DLL test doesn't prove emit uses envelope | MEDIUM | 03 | Task 2 | `cargo test -p dlp-hook-dll test_build_bypass_alert_envelope -- --test-threads=1` (pure helper test) |
| REVIEW-M-09: Blocking crossbeam recv inside tokio | MEDIUM | 02 | Task 3 | `rg -c "spawn_blocking" dlp-agent/src/bypass_correlator.rs` |
| REVIEW-L-01: Validation commands portability | LOW | ALL | ALL | All verify commands use `rg -c` instead of `grep -c` |

---

## Manual-Only Verifications

All phase behaviors have automated verification.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter
- [x] Review concerns H-01, H-02, H-03, M-01, M-05, M-06, M-07, M-08, M-09, L-01 all have validation coverage

**Approval:** ready for execution
