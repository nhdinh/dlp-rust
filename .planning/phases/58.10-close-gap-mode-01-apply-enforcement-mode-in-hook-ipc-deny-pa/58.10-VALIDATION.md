---
phase: 58.10
slug: close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-12
---

# Phase 58.10 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Requirement under test: **MODE-01** (enforcement mode applied in the hook IPC deny path + DLL shared-cache fast-path).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust built-in `#[test]` + `cargo test` (workspace) |
| **Config file** | none — default cargo test harness; dlp-hook-dll integration tests use `serial_test` + `PHASE_58_5_TEST_LOCK` for process-global-state isolation |
| **Quick run command** | `cargo test -p dlp-agent --lib hook_ipc_mode_gate` and `cargo test -p dlp-hook-dll --lib classification_cache` |
| **Full suite command** | `cargo test -p dlp-agent -p dlp-hook-dll -p dlp-common` (dlp-hook-dll lib tests: `-- --test-threads=1` per Phase 58.5 isolation) |
| **Estimated runtime** | ~30 seconds (targeted) / ~3 minutes (three-crate suite) |

---

## Sampling Rate

- **After every task commit:** `cargo test -p dlp-agent --lib <module>` and `cargo test -p dlp-hook-dll --lib <module>` (targeted, < 30 s)
- **After every plan wave:** `cargo test -p dlp-agent -p dlp-hook-dll -p dlp-common` + `cargo clippy -p dlp-agent -p dlp-hook-dll -p dlp-common -- -D warnings` + `cargo fmt --check`
- **Before `/gsd-verify-work`:** Full three-crate lib suite must be green; dlp-hook-dll lib tests run `-- --test-threads=1` (Phase 58.5 isolation)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 58.10-01-01 | 01 | 1 | MODE-01 | T-17 | Global Audit + T3/T4 agent-cache-miss → ALLOW + `would_have_denied` audit (fail-open only under global Audit) | unit (closure gate) | `cargo test -p dlp-agent --lib hook_ipc_mode_gate` | ❌ W0 | ⬜ pending |
| 58.10-01-02 | 01 | 1 | MODE-01 | T-17 | Global Audit + T3/T4 agent-cache-HIT (per-policy Audit known via `enforcement_mode`) → ALLOW | unit (closure gate) | `cargo test -p dlp-agent --lib hook_ipc_mode_gate` | ❌ W0 | ⬜ pending |
| 58.10-01-03 | 01 | 1 | MODE-01 | T-17 | Global PerPolicy + T3/T4 cache-miss → still fail-closed DENY (T-17 control; per-policy mode unresolvable) | unit (closure gate) | `cargo test -p dlp-agent --lib hook_ipc_mode_gate` | ❌ W0 | ⬜ pending |
| 58.10-02-01 | 02 | 1 | MODE-01 | T-17 | DLL global Audit (`_reserved[0]=1`) + T3/T4 cache-hit → autonomous deny skipped (falls through to pipe) | unit (DLL fast-path) | `cargo test -p dlp-hook-dll --lib classification_cache` | ❌ W0 | ⬜ pending |
| 58.10-02-02 | 02 | 1 | MODE-01 | T-17 | DLL global Block (`_reserved[0]=0`) + T3/T4 cache-hit → deny preserved | unit (DLL fast-path) | `cargo test -p dlp-hook-dll --lib` | ❌ W0 | ⬜ pending |
| 58.10-02-03 | 02 | 1 | MODE-01 | T-17 | Header mode byte survives checksum + `CACHE_LAYOUT_VERSION` 1→2 bump; v1 reader rejects v2 header (fail-closed back-compat) | unit (ABI) | `cargo test -p dlp-common --lib classification_cache` | ❌ W0 | ⬜ pending |
| 58.10-03-01 | 03 | 2 | MODE-01 | T-17 | End-to-end: global Audit → `HookIpcServer` returns HookResponse ALLOW to the DLL (SC#2 evidence row) | integration | `cargo test -p dlp-agent --test hook_ipc_integration` | ✅ harness exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task IDs are indicative — the planner assigns final IDs/waves; the mode matrix (global {Audit,Block,PerPolicy} × cache {hit,miss} × tier {T3/T4,T1/T2}) must be fully sampled.*

---

## Wave 0 Requirements

> **Note:** The test modules below are NOT a separate Wave 0 plan — they are created inline by the phase plans during execution (Plan 01 Task 1 creates `hook_ipc_mode_gate`; Plan 02 Tasks 2–3 create the ABI + DLL `global_mode()` reader tests; Plan 03 Task 2 creates the e2e test). Wave 0 is therefore satisfied by the plans themselves.

- [x] New unit-test module for the closure mode gate (agent): global {Audit, Block, PerPolicy} × cache {hit, miss} × tier {T3/T4, T1/T2} matrix. → **Created by Plan 01 Task 1** (`apply_effective_mode_to_hook_decision` pure helper + `mod hook_ipc_mode_gate`).
- [x] New DLL fast-path test: `CacheView` over a test mapping with `_reserved[0] = 1` (Audit) → deny skipped; `_reserved[0] = 0` (Block) → deny preserved. Reuses `build_valid_header` + `set_cache_lookup_for_test` (`classification_cache.rs:1073-1158`). → **Created by Plan 02 Task 3 / Plan 03 Task 1** (`should_skip_autonomous_deny` predicate tests 0/1/2/3/0xFF).
- [x] New ABI test: `CACHE_LAYOUT_VERSION` 1→2; v1 reader rejects v2 header (fail-closed); checksum still validates with `_reserved[0]` set. → **Created by Plan 02 Task 2**.
- [x] Hook-path e2e test: global Audit → `HookResponse` ALLOW + `would_have_denied` audit; Block → DENY control, in one process via the extracted `pub fn evaluate_hook_request` (explicit `global_mode`, no CONFIG OnceLock). → **Created by Plan 03 Task 2** (depends on Plan 01's `pub` handoff).
- [x] Framework install: none — existing `cargo test` infrastructure covers all requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live Audit-mode T3/T4 file write via a hooked process (WriteFile/CopyFileExW) succeeds and emits a `would_have_denied` audit to SIEM | MODE-01 | Requires a live Windows host with the DLL injected and agent running; cannot run in CI | On a Phase 58.3-prepared host: set global mode Audit, write to a T3-classified path via Notepad/copy, confirm the op succeeds and the audit event shows `policy_mode=Audit`, `would_have_denied=true`. Fold into the v0.10.0 UAT (58.3) run. |

*(All other phase behaviors have automated verification above.)*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (every plan task has a `<verify><automated>` cargo command)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 7 tasks across 3 plans carry automated cargo verify)
- [x] Wave 0 covers all MISSING references (closure gate, DLL fast-path, ABI — all created inline by Plans 01/02/03)
- [x] No watch-mode flags (all commands are one-shot `cargo test`)
- [x] Feedback latency < 30s (targeted `--lib <module>` runs)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-12
