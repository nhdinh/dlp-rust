---
phase: 58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa
plan: 03
type: execute
wave: 2
depends_on: ["58.10-01", "58.10-02"]
files_modified:
  - dlp-hook-dll/src/trampolines.rs
  - dlp-agent/tests/hook_ipc_integration.rs
autonomous: true
requirements: [MODE-01]
must_haves:
  truths:
    - "DLL global Audit (_reserved[0]=1) + T3/T4 cache-hit SKIPS the autonomous decide() write-deny and falls through to the pipe at ALL THREE reachable cache-hit call sites: HEALTHY (trampolines.rs:554), DEGRADED (621), and RESYNC (705)"
    - "DLL global Audit + T3/T4 cache-hit in DEGRADED also skips the deny and FORCES a pipe attempt (bypasses the every-10th-call should_retry_pipe throttle) so the agent applies the mode + emits the audit (trampolines.rs:621)"
    - "DLL global Audit + T3/T4 cache-hit in RESYNC skips the inline deny at 705 and falls through to the pipe retry at 712 (mirrors the HEALTHY treatment) — closes the D-09 full-chain gap where RESYNC would otherwise ALLOW the first write but DENY every subsequent one"
    - "DLL global Block/AuditAndBlock/PerPolicy + T3/T4 cache-hit keeps the autonomous decide() deny at all three sites (T-17 + performance preserved) — implements D-11 (per-policy Audit on a DLL cache-hit still blocks; the DLL knows the tier but not the policy, so only a global Audit override relaxes the DLL fast-path)"
    - "decide_isolated / decide_degraded (pipe DOWN) stay fail-closed even under global Audit — no parity audit can be emitted without the agent (scoping A1); ISOLATED:675 is NOT touched"
    - "A single pure predicate should_skip_autonomous_deny(global_mode_byte) gates all three skip sites (DRY; unit-tested once, reused at HEALTHY/DEGRADED/RESYNC)"
    - "End-to-end: global Audit -> the real decision path (evaluate_hook_request from Plan 01) returns HookResponse ALLOW to the DLL for a T3/T4 write; global Block -> DENY (SC#2 evidence row)"
  artifacts:
    - path: "dlp-hook-dll/src/trampolines.rs"
      provides: "Global-Audit skip of the autonomous decide() deny at the HEALTHY (554), DEGRADED (621), and RESYNC (705) cache-hit branches, with forced pipe in DEGRADED; shared should_skip_autonomous_deny predicate"
      contains: "should_skip_autonomous_deny"
    - path: "dlp-agent/tests/hook_ipc_integration.rs"
      provides: "End-to-end test calling evaluate_hook_request directly with explicit global_mode: Audit -> ALLOW, Block -> DENY"
      contains: "evaluate_hook_request"
  key_links:
    - from: "dlp-hook-dll/src/trampolines.rs (HEALTHY/DEGRADED/RESYNC cache-hit)"
      to: "dlp-hook-dll/src/classification_cache.rs CacheView::global_mode"
      via: "should_skip_autonomous_deny(cache_lookup.global_mode()) gates skipping decide() and falling through to classify_path pipe at all three sites"
      pattern: "should_skip_autonomous_deny"
    - from: "dlp-hook-dll/src/trampolines.rs (pipe fall-through)"
      to: "dlp-agent hook IPC closure (Plan 01)"
      via: "classify_path round-trip reaches the mode-aware closure which flips Audit DENY->ALLOW and emits the parity audit"
      pattern: "classify_path"
    - from: "dlp-agent/tests/hook_ipc_integration.rs (e2e test)"
      to: "dlp-agent/src/service.rs evaluate_hook_request (Plan 01)"
      via: "test calls evaluate_hook_request directly with an explicit global_mode (Audit then Block) — no CONFIG OnceLock, no live pipe needed for the decision-path assertion"
      pattern: "evaluate_hook_request"
---

<objective>
Complete the full-chain MODE-01 fix on the DLL side: make the cache-hit fast-path in `trampolines.rs` mode-aware so that under a global Audit override the autonomous T3/T4 write-deny is skipped and the operation falls through to the agent pipe (which applies the mode and emits the parity audit) at ALL THREE reachable cache-hit call sites — HEALTHY, DEGRADED, and RESYNC — and add the end-to-end integration test that proves SC#2 on the hook path.

Purpose: CONTEXT D-09 mandates the full chain — cache-hits are the common case for classified files, so an agent-only fix would ALLOW the first write (miss) but DENY every subsequent write (hit) to the same T3/T4 file. The checker confirmed a THIRD autonomous deny site the original plan missed: the `FailState::Resync` branch (trampolines.rs:703-708) flushes LRU, resets counters, sets state to Healthy, then re-runs the cache-hit deny INLINE at line 705 with NO mode check and early-returns before the pipe retry at 712. `FailState::Resync` is live/reachable (entered from fail_mode.rs:192, exercised in background_thread.rs:392). This plan patches all three sites (HEALTHY:554, DEGRADED:621, RESYNC:705) using the `CacheView::global_mode()` reader from Plan 02 and a single shared `should_skip_autonomous_deny` predicate (DRY), and locks the two research scoping recommendations: A1 (pipe-down fail-mode paths stay fail-closed — ISOLATED:675 is NOT touched) and A3 (force the pipe attempt in DEGRADED under global Audit).

Output: A mode-aware DLL fast-path (global Audit skips the deny and pipes at HEALTHY/DEGRADED/RESYNC; Block/AuditAndBlock/PerPolicy keep it), the DEGRADED forced-pipe behavior, a shared `should_skip_autonomous_deny` predicate, and an end-to-end `hook_ipc_integration` test that drives the real `evaluate_hook_request` decision path (from Plan 01) with an explicit global_mode to assert Audit -> ALLOW and Block -> DENY.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-CONTEXT.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-RESEARCH.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-PATTERNS.md

# Gap sites (the two autonomous deny call sites) + the reader from Plan 02 + the e2e harness:
@dlp-hook-dll/src/trampolines.rs
@dlp-hook-dll/src/classification_cache.rs
@dlp-hook-dll/src/fail_mode.rs
@dlp-agent/tests/hook_ipc_integration.rs
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (excluded from drift verification as new):
- `dlp-hook-dll/src/trampolines.rs::should_skip_autonomous_deny(global_mode_byte: u8) -> bool` — new pure, file-local (or `pub(crate)`) predicate returning `global_mode_byte == 1` (Audit). Unit-tested ONCE and reused at all three skip sites (HEALTHY/DEGRADED/RESYNC) so the RESYNC fix is trivially consistent and cross-platform testable (INFO 6).
- `dlp-hook-dll/src/trampolines.rs` — the HEALTHY (line 554), DEGRADED (line 621), and RESYNC (line 705) cache-hit branches each gain a `should_skip_autonomous_deny(...)` guard plus a fall-through/force-pipe path. Any `force_pipe` boolean introduced for clarity is file-local and unnamed at module scope.
- `dlp-agent/tests/hook_ipc_integration.rs` — new test fn(s) calling `evaluate_hook_request` (from Plan 01) directly with an explicit `global_mode` (e.g. `global_audit_returns_allow_for_t3_write`, `global_block_preserves_deny_for_t3_write`).
No new constants, struct fields, or crates. `decide_isolated`/`decide_degraded` in fail_mode.rs are NOT modified (A1 — they stay fail-closed); the ISOLATED branch (trampolines.rs:675) is NOT touched.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DLL fast-path — shared should_skip_autonomous_deny predicate + skip the autonomous decide() deny under global Audit at HEALTHY:554, DEGRADED:621, and RESYNC:705 (force pipe in DEGRADED)</name>
  <files>dlp-hook-dll/src/trampolines.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (classify_and_log_path decision flow at 540-748: the HEALTHY cache-hit branch at 547-558 with the decide() call at 554; the cache-miss pipe round-trip at 560-610; the DEGRADED cache-hit branch at 612-625 with the decide() call at 621 and the should_retry_pipe() throttle at 628; the RESYNC branch at 694-748 with the INLINE cache-hit deny at 703-708 — `if let Some(deny) = cache_lookup.and_then(|c| c.decide(classification, op)) { return Some(deny); } return None;` — and the pipe retry at 712; the ISOLATED branch at 670-693 which is NOT touched)
    - dlp-hook-dll/src/classification_cache.rs (CacheView::global_mode() added in Plan 02; decide() at 211-224)
    - dlp-hook-dll/src/fail_mode.rs (decide_isolated at 409 and decide_degraded at 441 — confirm these stay fail-closed and are NOT modified, per A1; confirm FailState::Resync is reachable — entered from fail_mode.rs:192, exercised in background_thread.rs:392)
    - .planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-RESEARCH.md (Pattern 3 fall-through mechanics note at line 277; Pitfall 1 full-chain; Pitfall 3 / A1 / A3 scoping)
  </read_first>
  <behavior>
    - Shared predicate: `should_skip_autonomous_deny(1) == true` (Audit); `should_skip_autonomous_deny(0) == false` (Block); `should_skip_autonomous_deny(2) == false` (AuditAndBlock); `should_skip_autonomous_deny(3) == false` (PerPolicy); `should_skip_autonomous_deny(0xFF) == false` (unrecognized -> fail-closed, mirrors CacheView::global_mode default).
    - HEALTHY branch: when the predicate is true (global Audit) and there is a cache hit, the code does NOT call decide() and does NOT early-return; it falls through to the classify_path pipe round-trip so the agent applies the mode + emits the parity audit. When the predicate is false, the existing decide() deny + early-return behavior is unchanged.
    - DEGRADED branch: same skip on global Audit, AND the pipe attempt is FORCED (the should_retry_pipe() every-10th-call throttle is bypassed) so the agent is reached to make the mode-aware decision (A3). When the predicate is false, existing DEGRADED behavior (cache decide() deny, throttle) is unchanged.
    - RESYNC branch: same skip on global Audit — the inline cache-hit deny at 705 is NOT executed and the code falls through to the pipe retry at 712 (mirrors the HEALTHY treatment). When the predicate is false, the existing RESYNC inline deny + early-return is unchanged. (RESYNC flushes LRU + resets counters + sets Healthy first, then re-runs the cache-hit deny inline; without this skip it would ALLOW the first write via the pipe but DENY every subsequent write on the now-Healthy cache hit — the exact D-09 full-chain violation.)
    - decide_isolated / decide_degraded (pipe DOWN) are NOT relaxed: under global Audit with a down agent they still deny T3/T4 writes (A1 — no parity audit can be emitted without the agent). The ISOLATED branch (670-693) is byte-for-byte unchanged.
    - classification_source telemetry and the override_tier mirror at trampolines.rs:529 are not disturbed.
  </behavior>
  <action>
    In `dlp-hook-dll/src/trampolines.rs`:

    (1) Add a small pure predicate near the top of the module (or as a `pub(crate)` fn so it is unit-testable cross-platform): `fn should_skip_autonomous_deny(global_mode_byte: u8) -> bool { global_mode_byte == 1 }` with a doc comment citing D-10 (1 == Audit; only a global Audit override relaxes the fast-path, and only when the pipe is reachable so the agent emits the parity audit). Add a `#[cfg(test)]` unit test for the predicate covering 0/1/2/3/0xFF (this is the cross-platform-testable core — INFO 6).

    (2) HEALTHY cache-hit branch (lines 547-558): after `classification_source = ClassificationSource::CacheHit;` and the existing debug_log, compute `let skip = cache_lookup.map(|c| should_skip_autonomous_deny(c.global_mode())).unwrap_or(false);`. Wrap the existing deny+return in `if !skip { ... }` so that when `skip` is false the current logic runs verbatim (`if let Some(deny) = cache_lookup.and_then(|c| c.decide(classification, op)) { return Some(deny); } return None;`), and when `skip` is true the branch does NOT return — execution falls through to the cache-miss pipe round-trip at line 560 (`match crate::classify_path(...)`) so the agent applies the mode and emits the parity audit. Implement the fall-through with the minimal-diff approach (RESEARCH Open Question 3): a `force_pipe` boolean (or clearing `cache_classification`) that skips the early return and routes into the existing classify_path block, without disturbing `classification_source` telemetry or the `override_tier` mirror at line 529.

    (3) DEGRADED cache-hit branch (lines 612-625, the decide() call at line 621): apply the SAME `skip` guard. On global Audit, do NOT call decide()/return; instead FORCE the pipe attempt regardless of `should_retry_pipe()` (A3) so the agent is reached — i.e. when `skip` is true, bypass the `if fail_state.should_retry_pipe()` gate at line 628 and go straight to the classify_path round-trip. When `skip` is false, DEGRADED behavior is unchanged.

    (4) RESYNC branch (lines 694-748): after the existing flush/reset/set-Healthy (698-700), apply the SAME `skip` guard to the inline cache-hit deny at 703-708. Compute `let skip = cache_lookup.map(|c| should_skip_autonomous_deny(c.global_mode())).unwrap_or(false);` and wrap the inline deny in `if !skip { if let Some(deny) = cache_lookup.and_then(|c| c.decide(classification, op)) { return Some(deny); } return None; }`. When `skip` is true, do NOT execute the inline deny or early-return — fall through to the pipe retry at line 712 (`match crate::classify_path(...)`) so the agent applies the mode + emits the parity audit (mirrors the HEALTHY treatment). When `skip` is false, RESYNC behavior is unchanged.

    (5) Do NOT modify `decide_isolated`/`decide_degraded` in fail_mode.rs (A1 — they stay fail-closed under global Audit because no parity audit can be emitted without the agent). Do NOT touch the ISOLATED branch (670-693). Add a brief comment at all three skip sites citing D-10 (global Audit relaxes the fast-path only when the pipe is reachable) and, at the RESYNC site, a note that this closes the D-09 full-chain gap (RESYNC re-runs the cache-hit deny inline after recovery).

    (6) Add/extend DLL unit tests: reuse the `build_valid_header` + `set_cache_lookup_for_test` harness (classification_cache.rs:1073-1158) to drive a cache-hit with `_reserved[0]=1` (Audit) and assert the autonomous deny is skipped at each reachable site (the trampoline falls through to the pipe path rather than returning Some(DenyReturn)); and with `_reserved[0]=0` (Block) assert the deny is preserved. Guard with PHASE_58_5_TEST_LOCK and run `-- --test-threads=1`. If a full trampoline invocation is not unit-testable in isolation, the `should_skip_autonomous_deny` predicate unit test (step 1) plus the Plan-02 reader tests plus the Task-2 e2e test provide the behavioral coverage — assert the predicate gates the decide() call at each site.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --lib -- --test-threads=1</automated>
  </verify>
  <done>
    `should_skip_autonomous_deny` exists and is unit-tested (0/1/2/3/0xFF). All THREE cache-hit branches — HEALTHY (554), DEGRADED (621), and RESYNC (705) — skip the autonomous decide() deny when the predicate is true (global Audit) and fall through to the pipe; DEGRADED forces the pipe attempt (bypasses should_retry_pipe) under global Audit. Block/AuditAndBlock/PerPolicy keep the deny at all three sites. `decide_isolated`/`decide_degraded` and the ISOLATED branch (675) are unchanged (fail-closed). `cargo test -p dlp-hook-dll --lib -- --test-threads=1` exits 0; `cargo build -p dlp-hook-dll` succeeds with no new warnings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end decision-path test — drive evaluate_hook_request with explicit global_mode (Audit -> ALLOW, Block -> DENY) for SC#2 evidence</name>
  <files>dlp-agent/tests/hook_ipc_integration.rs</files>
  <read_first>
    - dlp-agent/src/service.rs (the extracted `evaluate_hook_request` from Plan 01 — its exact signature: `fn evaluate_hook_request(req: &HookRequest, caller_sid: String, offline: &OfflineManager, approval: &ApprovalCache, hash_cache: &HashCache, global_mode: EnforcementMode) -> (HookResponse, Option<AuditEvent>)`; the HookIpcServerConfig fields at 2043-2069 for constructing the offline/approval/hash_cache deps)
    - dlp-agent/src/offline.rs (OfflineManager::new at 61 — how to construct one for a test; offline_decision at 144-159 — a T3/T4 cache-miss returns fail_closed DENY)
    - dlp-agent/src/approval_cache.rs (ApprovalCache::new at 55/65 — construct an empty one so the approval-override early return does not fire)
    - dlp-agent/tests/hook_ipc_integration.rs (the existing harness imports and module layout — add the new tests alongside; the existing default_mock_handler tests stay as-is)
    - dlp-common/src/abac.rs (EnforcementMode::Audit / ::Block; EvaluateResponse.enforcement_mode; EvaluateRequest.resource.classification at 273 — set it to T3 to drive the fail-closed deny)
  </read_first>
  <behavior>
    - Calling `evaluate_hook_request` directly with `global_mode = EnforcementMode::Audit` and a HookRequest whose resource classification is T3 (so `offline_decision` fail-closed-DENYs on the cache miss) returns `(HookResponse { decision: ALLOW, .. }, Some(audit))` where audit.would_have_denied == true and audit.policy_mode == "Audit".
    - Calling it with `global_mode = EnforcementMode::Block` and the SAME request returns `(HookResponse { decision: DENY, .. }, Some(_))` — T-17 preserved end-to-end (control).
    - Both cases run in the SAME test process because `global_mode` is an explicit parameter (NOT the CONFIG OnceLock) — this is exactly why Plan 01 extracted the closure body.
    - The test does NOT boot a HookIpcServer or a live named pipe for the decision-path assertion (the pipe plumbing is already covered by the existing 5 harness tests); it exercises the real decision logic that the production closure delegates to.
  </behavior>
  <action>
    In `dlp-agent/tests/hook_ipc_integration.rs`, add end-to-end decision-path tests that call the REAL `evaluate_hook_request` (the function the production closure delegates to, extracted in Plan 01) directly with an explicit `global_mode`. This is the concrete mechanism that resolves the harness infeasibility: the existing `default_mock_handler` (injected via `HookIpcServer::new`) returns a hardcoded response and never runs `offline_decision` or the mode gate, and the production closure reads the mode via the `CONFIG: OnceLock` (set-once-per-process) — so neither the mock-handler path nor the OnceLock can drive both an Audit and a Block case. `evaluate_hook_request` takes `global_mode` as an explicit parameter, so a single test process can drive both.

    Concretely:
    (1) Build the dependencies `evaluate_hook_request` needs: construct an `OfflineManager` (via `OfflineManager::new` with a test `EngineClient`/`Cache` — mirror how other agent unit tests construct it; the key is that a T3/T4 path with no cached entry makes `offline_decision` return `fail_closed_response(T3)` = DENY with `enforcement_mode=None`), an empty `ApprovalCache` (so the approval-override early return does not fire), and a `HashCache` (test-default). If `OfflineManager::new` requires an engine client that is awkward to build in this integration-test crate, add a `#[cfg(test)]`/`pub(crate)` constructor or build it via the same fixture the service.rs unit tests use — the goal is a real `offline_decision` that fail-closed-DENYs a T3 write, not a mock.
    (2) Build a `HookRequest` for a T3 write: set `path`, `action = "WRITE"` (or the write action string `is_write_action` recognizes), `pid`, and ensure `hook_request_to_evaluate_request` maps it to a resource with `classification = T3`. If the classification is derived from the path/policy rather than set directly, construct the request/fixture so the resulting `evaluate_req.resource.classification == Classification::T3`.
    (3) Test `global_audit_returns_allow_for_t3_write`: call `let (resp, audit) = evaluate_hook_request(&req, caller_sid, &offline, &approval, &hash_cache, EnforcementMode::Audit);` and assert `resp.decision == dlp_common::Decision::ALLOW`, `audit.is_some()`, and on the audit `would_have_denied == true` and `policy_mode == "Audit"` (and classification == T3, the real tier).
    (4) Test `global_block_preserves_deny_for_t3_write` (control): same request, `EnforcementMode::Block`, assert `resp.decision == dlp_common::Decision::DENY`.
    (5) Keep the tests deterministic and self-contained. They are NOT `#[cfg(windows)]`-gated for the decision-path assertion (no named pipe is used) — run them on any platform; only the pre-existing pipe-routing harness tests stay `#[cfg(windows)]`. If `evaluate_hook_request` or its deps are not visible from the integration-test crate (it is a separate crate from the lib), expose them as `pub` (or `#[doc(hidden)] pub`) from `dlp-agent` so the integration test can call them — note this visibility requirement in the Plan-01 handoff.

    Run the integration test.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test hook_ipc_integration</automated>
  </verify>
  <done>
    A decision-path test proves `evaluate_hook_request` with global Audit -> HookResponse.decision == ALLOW (+ parity audit, would_have_denied=true, policy_mode=Audit, real tier) for a T3 write, and a Block-mode control proves DENY — both in one process via the explicit global_mode parameter (no OnceLock, no live pipe). `cargo test -p dlp-agent --test hook_ipc_integration` exits 0 (the new tests plus the existing 5 harness tests pass). This closes the MODE-01 verification gap (the SC#2 "Audit mode returns ALLOW to DLL" row now has hook-path evidence through the real decision path, not just an interception-path unit test).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DLL fast-path (in-process) -> agent pipe | On a global-Audit cache-hit the DLL now pipes to the agent instead of denying autonomously; the pipe is the trusted path to the ABAC authority + audit emitter. |
| shared-memory mode byte -> DLL skip decision | The DLL honors the global-mode byte (read from shared memory) to decide whether to skip the deny; a forged byte must fail closed. |
| pipe DOWN (DEGRADED/ISOLATED) | When the agent is unreachable there is no authority to apply the mode or emit the audit; relaxing the deny here would ALLOW writes with no audit trail. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.10-09 | Repudiation | global-Audit cache-hit ALLOWs a write with no audit trail | mitigate | The skip ALWAYS falls through to the pipe so the AGENT emits the full-parity audit (D-07); the DLL never emits its own audit (centralized, DRY). This holds at all three reachable cache-hit sites (HEALTHY:554, DEGRADED:621, RESYNC:705). Pipe-down fail-mode paths (decide_isolated/decide_degraded) and the ISOLATED branch (675) stay fail-closed (A1) precisely because no audit can be emitted without the agent. |
| T-58.10-10 | Elevation of Privilege | forged/stale mode byte causes the DLL to skip the deny | mitigate | The skip is gated on `should_skip_autonomous_deny(global_mode())` (== Audit) read from a header that passed full_validation (magic + layout_version==2 + checksum). Unrecognized bytes default to Block (Plan 02) and the predicate returns false for anything but 1. The mapping is SDDL-restricted (SYSTEM writes only) and the DLL maps read-only. Even if the byte were Audit, the fall-through still requires the agent pipe to ALLOW — the agent re-applies the mode authoritatively. |
| T-58.10-11 | Denial of Service | DEGRADED forced-pipe under global Audit adds pipe load | accept | A3 forces the pipe attempt in DEGRADED under global Audit (bypassing the every-10th-call throttle) so the agent makes the mode-aware decision. This adds pipe load only in the (global Audit AND DEGRADED) intersection, which is an operator-chosen audit configuration during a degraded window; the alternative (9/10 hits denied by decide_degraded) would break SC#2. Accepted as a deliberate tradeoff; documented at the skip site. |
| T-58.10-12 | Tampering | e2e test harness trusts a mock that bypasses the real closure | mitigate | The Task-2 test calls the REAL `evaluate_hook_request` decision path directly with an explicit `global_mode` (the function the production closure delegates to) — NOT the `default_mock_handler` (which returns a hardcoded response and never runs the mode gate) and NOT the CONFIG OnceLock (set-once-per-process). A Block-mode control test in the SAME process proves the DENY path is intact, so a no-op gate would fail the control. |
| T-58.10-13 | Elevation of Privilege | RESYNC recovery re-runs an ungated cache-hit deny (full-chain gap) | mitigate | The RESYNC branch (trampolines.rs:703-708) re-runs the cache-hit deny INLINE after flushing LRU/resetting counters/setting Healthy; without the skip it would ALLOW the first write (pipe) but DENY every subsequent write (now-Healthy cache hit) — the exact D-09 violation. The same `should_skip_autonomous_deny` guard is applied at RESYNC:705 so global Audit falls through to the pipe retry at 712 (mirrors HEALTHY). |
| T-58.10-SC | Tampering | cargo installs | accept | No new packages this phase (in-tree crates only); Package Legitimacy Gate trivially satisfied per RESEARCH. |
</threat_model>

<verification>
- `cargo test -p dlp-hook-dll --lib -- --test-threads=1` exits 0 (should_skip_autonomous_deny predicate tests + fast-path skip tests at HEALTHY/DEGRADED/RESYNC + full DLL lib suite green).
- `cargo build -p dlp-hook-dll` succeeds with no new warnings.
- `cargo test -p dlp-agent --test hook_ipc_integration` exits 0 (evaluate_hook_request: global Audit -> ALLOW + parity audit; Block -> DENY control — both in one process).
- Source assertion: `dlp-hook-dll/src/trampolines.rs` contains `should_skip_autonomous_deny` gating the decide() skip at all THREE cache-hit branches (HEALTHY:554, DEGRADED:621, RESYNC:705); `decide_isolated`/`decide_degraded` in fail_mode.rs and the ISOLATED branch (675) are byte-for-byte unchanged.
- Wave-merge gate: `cargo test -p dlp-agent -p dlp-hook-dll -p dlp-common` + `cargo clippy -p dlp-agent -p dlp-hook-dll -p dlp-common -- -D warnings` + `cargo fmt --check`.
</verification>

<success_criteria>
- DLL global Audit + T3/T4 cache-hit skips the autonomous deny and pipes to the agent at ALL THREE reachable sites (HEALTHY + DEGRADED + RESYNC); DEGRADED forces the pipe (A3); RESYNC falls through to the pipe retry at 712.
- DLL Block/AuditAndBlock/PerPolicy + T3/T4 cache-hit keeps the deny at all three sites (T-17 + performance).
- decide_isolated/decide_degraded and the ISOLATED branch (675) stay fail-closed under global Audit (A1).
- A single `should_skip_autonomous_deny` predicate gates all three sites (DRY; unit-tested cross-platform).
- End-to-end: `evaluate_hook_request` with global Audit -> ALLOW (+ parity audit, real tier); Block -> DENY control — both in one process via the explicit global_mode parameter (SC#2 hook-path evidence).
- Full three-crate suite + clippy + fmt green at wave merge.
</success_criteria>

<output>
Create `.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-03-SUMMARY.md` when done.
</output>
