---
phase: 58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/service.rs
autonomous: true
requirements: [MODE-01]
must_haves:
  truths:
    - "Global Audit + T3/T4 agent-cache-miss returns HookResponse ALLOW to the DLL (fail-open only under a global Audit override)"
    - "Global Audit + T3/T4 agent-cache-HIT with a per-policy Audit enforcement_mode returns ALLOW (policy mode known from cached EvaluateResponse)"
    - "Global PerPolicy + T3/T4 cache-miss stays fail-closed DENY (T-17 preserved; per-policy mode unresolvable offline)"
    - "Global Block and AuditAndBlock keep the DENY from offline_decision unchanged (no fail-open)"
    - "Exactly one audit is emitted per hook op: a full-parity Access audit (would_have_denied=true, policy_mode=Audit) when the flip fires, the existing Block audit otherwise"
  artifacts:
    - path: "dlp-agent/src/service.rs"
      provides: "Extracted pure helper apply_effective_mode_to_hook_decision + closure mode gate + Audit flip + parity audit"
      contains: "fn apply_effective_mode_to_hook_decision"
    - path: "dlp-agent/src/service.rs"
      provides: "Unit-test module hook_ipc_mode_gate covering the global x cache x tier matrix"
      contains: "mod hook_ipc_mode_gate"
  key_links:
    - from: "dlp-agent/src/service.rs (hook IPC closure)"
      to: "dlp-common/src/abac.rs compute_effective_mode"
      via: "apply_effective_mode_to_hook_decision calls compute_effective_mode(global_mode, policy_mode) then is_audit() flip"
      pattern: "compute_effective_mode"
    - from: "dlp-agent/src/service.rs (closure)"
      to: "crate::service::with_config"
      via: "global_mode read live inside the closure body every request (never captured at init)"
      pattern: "with_config\\(\\|cfg\\| cfg\\.enforcement\\.global_mode\\)"
---

<objective>
Close the agent-side half of gap MODE-01: make the hook IPC decision closure in `service.rs` enforcement-mode-aware so that under a global Audit override an Audit DENY from `offline_decision` is flipped to ALLOW and a full-parity audit is emitted — satisfying Phase 55 Success Criterion #2 on the hook path, not just the interception path.

Purpose: The hook IPC closure (`service.rs:2187`) currently returns `offline.offline_decision(&evaluate_req)` verbatim, ignoring enforcement mode. This is the cache-miss pipe path. The reference implementation (`interception/mod.rs:413-465`) already computes the effective mode and flips Audit DENY -> ALLOW; this plan mirrors that exact pattern in the closure and extracts the gate into a pure, unit-testable helper.

Output: A mode-aware hook IPC closure, an extracted pure helper `apply_effective_mode_to_hook_decision`, and a `hook_ipc_mode_gate` unit-test module sampling the global {Audit,Block,PerPolicy,AuditAndBlock} x cache {hit,miss} x tier {T3/T4,T1/T2} matrix.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-CONTEXT.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-RESEARCH.md
@.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-PATTERNS.md

# Reference implementation (the correct pattern to mirror) and the gap site:
@dlp-agent/src/interception/mod.rs
@dlp-agent/src/service.rs
@dlp-common/src/abac.rs
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (excluded from drift verification as new):
- `dlp-agent/src/service.rs::apply_effective_mode_to_hook_decision(global_mode: EnforcementMode, eval_resp: &EvaluateResponse) -> (Decision, bool)` — new pure helper (the mode gate, extracted for testability; mirrors how `compute_override_decision` was extracted at interception/mod.rs:582-620).
- `dlp-agent/src/service.rs` — new `#[cfg(test)] mod hook_ipc_mode_gate` test module.
No new constants, struct fields, or crates. The closure body gains a mode gate but no new named top-level symbol besides the helper and test module.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure helper apply_effective_mode_to_hook_decision with failing unit tests</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    - dlp-agent/src/service.rs (the hook IPC closure at lines 2184-2288 — the gap site; see the offline.offline_decision call at 2236 and the existing blocked-write audit at 2247-2285)
    - dlp-agent/src/interception/mod.rs (lines 408-465 the mode gate + approval ordering; lines 582-620 the compute_override_decision extraction pattern; lines 622-648 the existing compute_effective_mode test matrix to mirror)
    - dlp-common/src/abac.rs (compute_effective_mode at 450-459; EnforcementMode is_audit/is_blocking; EvaluateResponse at 461-495 carrying enforcement_mode and would_have_denied)
  </read_first>
  <behavior>
    - Test (global Audit, cache-miss T3): eval_resp with decision=DENY, enforcement_mode=None (fail_closed_response shape) -> returns (ALLOW, flipped=true). Mirrors compute_effective_mode(Audit, Block)==Audit.
    - Test (global Audit, cache-hit T3 per-policy Audit): eval_resp decision=DENY, enforcement_mode=Some(Audit) -> returns (ALLOW, flipped=true).
    - Test (global PerPolicy, cache-miss T3): eval_resp decision=DENY, enforcement_mode=None -> returns (DENY, flipped=false). T-17 control: per-policy mode unresolvable offline stays fail-closed.
    - Test (global Block, cache-miss T3): eval_resp decision=DENY, enforcement_mode=None -> returns (DENY, flipped=false).
    - Test (global AuditAndBlock, cache-miss T3): eval_resp decision=DENY, enforcement_mode=None -> returns (DENY, flipped=false). is_blocking() true, no fail-open.
    - Test (global Audit, T1/T2 ALLOW): eval_resp decision=ALLOW -> returns (ALLOW, flipped=false). No flip when not denied.
  </behavior>
  <action>
    Add a new pure free function `apply_effective_mode_to_hook_decision` in `dlp-agent/src/service.rs` (module scope, near the other hook-IPC helpers such as `hook_request_to_evaluate_request`). Signature: it takes `global_mode: dlp_common::abac::EnforcementMode` and `eval_resp: &dlp_common::EvaluateResponse`, and returns `(dlp_common::Decision, bool)` where the bool is `flipped` (true when an Audit DENY was flipped to ALLOW). Body: compute `policy_mode = eval_resp.enforcement_mode.unwrap_or(EnforcementMode::Block)`; compute `effective_mode = dlp_common::abac::compute_effective_mode(global_mode, policy_mode)`; then `let final_decision = if effective_mode.is_audit() { Decision::ALLOW } else { eval_resp.decision };` and `flipped = effective_mode.is_audit() && eval_resp.decision.is_denied()`. Return `(final_decision, flipped)`. This is a verbatim mirror of interception/mod.rs:413-427 with the None->Block default (D-02) and the cache-miss semantics (D-01/D-03). Do NOT re-implement mode logic; call `compute_effective_mode` directly (DRY per CLAUDE.md 9). Add a `#[cfg(test)] mod hook_ipc_mode_gate` module with the six tests above using hand-constructed `EvaluateResponse` values (set `decision`, `enforcement_mode`, `reason`, `matched_policy_id`, `would_have_denied`; other fields per the struct's Default or existing test constructors in interception/mod.rs:622-648). Run the tests; they MUST pass (this is the GREEN step for the pure helper — the helper is pure logic, so tests pass immediately once written correctly; if a test fails, the helper logic is wrong).
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --lib hook_ipc_mode_gate</automated>
  </verify>
  <done>
    `apply_effective_mode_to_hook_decision` exists in service.rs and is a pure function (no config access, no I/O). All six matrix tests pass: global Audit flips T3 DENY->ALLOW on both miss (enforcement_mode=None) and hit (enforcement_mode=Some(Audit)); PerPolicy/Block/AuditAndBlock keep DENY on a miss; an ALLOW response is never flipped. `cargo test -p dlp-agent --lib hook_ipc_mode_gate` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the mode gate + Audit flip + full-parity audit into the hook IPC closure</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    - dlp-agent/src/service.rs (the closure at 2184-2288; the offline.offline_decision call at 2236; the HookResponse construction at 2237-2243; the existing blocked-write audit at 2247-2285; the approval-cache early return at 2213-2222)
    - dlp-agent/src/interception/mod.rs (lines 413-465 mode gate + approval ordering; lines 486-524 the full-parity audit builder sequence with_policy/with_policy_mode/with_would_have_denied + enrich_audit_with_app_identity + set_destination_application; line 529 the no-BlockNotify-in-Audit note)
    - dlp-common/src/abac.rs (EvaluateResponse fields used in the audit: matched_policy_id, reason, would_have_denied)
  </read_first>
  <behavior>
    - Under global Audit, a T3/T4 cache-miss (offline_decision returns fail_closed DENY, enforcement_mode=None) now yields HookResponse.decision == ALLOW and emits exactly one Access audit with would_have_denied=true and policy_mode="Audit".
    - Under global Audit, a T3/T4 cache-hit whose cached EvaluateResponse has enforcement_mode=Some(Audit) yields ALLOW + the same parity audit.
    - Under global Block/AuditAndBlock/PerPolicy, the DENY is preserved and the EXISTING Block audit path (2247-2285) fires unchanged (no double audit).
    - The approval-cache early return at 2213-2222 is upstream of the gate and unaffected (returns ALLOW on override).
    - No BlockNotify UI notification is emitted in Audit mode (nothing physically blocked).
  </behavior>
  <action>
    In the hook IPC closure in `dlp-agent/src/service.rs`, immediately after `let eval_resp = offline.offline_decision(&evaluate_req);` (line 2236) and before constructing `HookResponse` (line 2237), insert the mode gate. Read the global mode live inside the closure body (NOT captured at construction — Pitfall 5): `let global_mode = crate::service::with_config(|cfg| cfg.enforcement.global_mode).unwrap_or(dlp_common::abac::EnforcementMode::Block);`. Then call the Task-1 helper: `let (final_decision, flipped) = apply_effective_mode_to_hook_decision(global_mode, &eval_resp);`. Build the `HookResponse` using `final_decision` for the `decision` field (instead of `eval_resp.decision`); keep `reason: eval_resp.reason.clone()`. Then restructure the audit emission so exactly ONE audit fires per op: when `flipped` is true, emit the full-parity Audit-mode Access audit reusing the interception builders — `AuditEvent::new(EventType::Access, caller_sid.clone(), "hook-dll".to_string(), req.path.clone(), classification, map_hook_action_to_abac(&req.action), final_decision, "AGENT-01".to_string(), 0).with_access_context(AuditAccessContext::Local).with_policy(policy_id_str, eval_resp.reason.clone()).with_policy_mode(format!("{:?}", effective_mode)).with_would_have_denied(true)` followed by `crate::audit_emitter::enrich_audit_with_app_identity(&mut audit_event, req.pid)` and `crate::audit_emitter::set_destination_application(&mut audit_event, None)` and `let _ = crate::audit_emitter::EMITTER.emit(&mut audit_event);`. Derive `effective_mode` again here for the policy_mode string (or have the helper also return it — prefer returning it to avoid recompute; if so update the helper signature to return `(Decision, bool, EnforcementMode)` and adjust Task-1 tests). The `classification` value is not directly available in the closure today (the existing Block audit hardcodes `Classification::T1` at line 2270); for the parity audit use the same classification source the existing audit uses (the hardcoded T1 placeholder) OR derive from the cached response if available — match the existing closure's classification handling to stay minimal and avoid scope creep; the parity requirement is on EventType/policy_mode/would_have_denied, not the tier field. When `flipped` is false, keep the EXISTING blocked-write audit block (2247-2285) exactly as-is (it already checks `response.decision.is_denied() && is_write_action(&req.action)`); ensure the flip path does NOT also run that block (guard it with `else` / the `flipped` flag) so no double audit is emitted (Pitfall 4). Do NOT add any BlockNotify/UI send on the flip path (D-08). Do NOT modify `offline_decision` or `fail_closed_response` (D-04 — those stay pure). Add one closure-level integration-flavored unit test if feasible without booting HookIpcServer; otherwise rely on the Task-1 helper matrix plus the Plan-03 end-to-end pipe test for closure coverage.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --lib hook_ipc_mode_gate &amp;&amp; cargo build -p dlp-agent</automated>
  </verify>
  <done>
    The closure computes the effective mode via the helper, flips Audit DENY->ALLOW, and emits exactly one audit per op (parity Access audit on flip, existing Block audit otherwise). `cargo build -p dlp-agent` succeeds with no warnings; `cargo test -p dlp-agent --lib hook_ipc_mode_gate` exits 0. The approval-cache early return and the existing Block audit path are unchanged in behavior for non-Audit modes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hooked process -> agent named pipe | HookRequest crosses from an untrusted (potentially compromised) user process into the agent over a local named pipe; the agent is the ABAC decision authority. |
| agent config (global_mode) -> decision | The enforcement mode read from live config drives fail-open vs fail-closed; a stale or spoofed mode would weaken T-17. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.10-01 | Elevation of Privilege | apply_effective_mode_to_hook_decision (cache-miss fail-open) | mitigate | Fail-open ONLY under a verified global Audit override: cache-miss defaults enforcement_mode to Block, so compute_effective_mode yields Audit only when global_mode==Audit (D-01/D-02). Block/AuditAndBlock/PerPolicy stay fail-closed (T-17 preserved). Unit test 58.10-01-03 (PerPolicy control) proves this. |
| T-58.10-02 | Tampering | global_mode read in closure | mitigate | Read live via `crate::service::with_config(|cfg| cfg.enforcement.global_mode)` inside the closure body every request (never captured at init), so a runtime server-pushed mode change takes effect immediately and a stale snapshot cannot be honored (Pitfall 5). |
| T-58.10-03 | Repudiation | Audit-mode ALLOW with no audit trail | mitigate | The flip ALWAYS emits the full-parity Access audit (would_have_denied=true, policy_mode=Audit) before returning ALLOW (D-07). Exactly one audit per op — the existing Block audit is guarded so it does not double-emit (Pitfall 4). |
| T-58.10-04 | Tampering | forged HookRequest fields (path/action/pid) | accept | Out of scope for this phase; hook IPC is a local named pipe restricted by SDDL, and pid->SID resolution already denies on failure (service.rs:2189-2204). This phase does not widen the request trust surface. |
| T-58.10-SC | Tampering | cargo installs | accept | No new packages this phase (in-tree crates only); Package Legitimacy Gate trivially satisfied per RESEARCH. |
</threat_model>

<verification>
- `cargo test -p dlp-agent --lib hook_ipc_mode_gate` exits 0 (six-test matrix green).
- `cargo build -p dlp-agent` succeeds with no new warnings.
- `cargo clippy -p dlp-agent -- -D warnings` clean for the touched module (run at wave merge).
- Source assertion: `dlp-agent/src/service.rs` contains `fn apply_effective_mode_to_hook_decision` and the closure calls `compute_effective_mode` (via the helper) and `with_config(|cfg| cfg.enforcement.global_mode)`.
</verification>

<success_criteria>
- Global Audit + T3/T4 cache-miss returns ALLOW + parity audit (fail-open only under global Audit).
- Global Audit + T3/T4 cache-hit (per-policy Audit known) returns ALLOW.
- Global PerPolicy/Block/AuditAndBlock + T3/T4 cache-miss stays fail-closed DENY (T-17 preserved).
- Exactly one audit per op; no BlockNotify in Audit mode.
- The gate is a pure, unit-testable helper mirroring the interception extraction pattern.
</success_criteria>

<output>
Create `.planning/phases/58.10-close-gap-mode-01-apply-enforcement-mode-in-hook-ipc-deny-pa/58.10-01-SUMMARY.md` when done.
</output>
