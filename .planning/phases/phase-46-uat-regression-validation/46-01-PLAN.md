---
phase: 46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/phase-46-uat-regression-validation/46-01-SUMMARY.md
  - .planning/phases/phase-46-uat-regression-validation/46-VERIFICATION.md
autonomous: true
requirements:
  - UAT-05
must_haves:
  truths:
    - "All workspace tests pass"
    - "SonarQube quality gate passes"
    - "No compiler warnings"
    - "Clippy passes with -D warnings"
    - "cargo fmt --check passes"
  artifacts:
    - path: "46-01-SUMMARY.md"
      provides: "Execution summary with test results"
    - path: "46-VERIFICATION.md"
      provides: "Phase verification report"
  key_links: []
---

<objective>
Run comprehensive validation suite across the entire workspace. Execute all tests, run static analysis, verify code quality gates, and document results.

Purpose: Ensure phases 43-45 introduced no regressions and the codebase meets quality standards.
Output: Validation report with test results and quality gate status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/phase-46-uat-regression-validation/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full workspace test suite</name>
  <files>N/A</files>
  <action>
Run `cargo test --workspace` to execute all tests across all crates.

Record results:
- Total tests passed/failed/ignored
- Any failing tests with error details
- Test duration
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <done>
    - All workspace tests pass
    - No unexpected failures
  </done>
</task>

<task type="auto">
  <name>Task 2: Run clippy with warnings as errors</name>
  <files>N/A</files>
  <action>
Run `cargo clippy --workspace -- -D warnings` to verify no lint violations.

If any warnings exist from phases 43-45 changes, fix them.
  </action>
  <verify>
    <automated>cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    - Clippy passes with no warnings
  </done>
</task>

<task type="auto">
  <name>Task 3: Check formatting</name>
  <files>N/A</files>
  <action>
Run `cargo fmt --check` to verify all code is properly formatted.

If formatting issues found, run `cargo fmt` to fix.
  </action>
  <verify>
    <automated>cargo fmt --check</automated>
  </verify>
  <done>
    - Formatting check passes
  </done>
</task>

<task type="auto">
  <name>Task 4: Run SonarQube scan</name>
  <files>N/A</files>
  <action>
Run `sonar-scanner` to perform static code analysis.

Check quality gate status. If it fails, fix issues and re-scan.
  </action>
  <verify>
    <automated>sonar-scanner</automated>
  </verify>
  <done>
    - SonarQube scan completes
    - Quality gate passes
  </done>
</task>

<task type="auto">
  <name>Task 5: Build all crates</name>
  <files>N/A</files>
  <action>
Run `cargo build --workspace` to verify everything compiles cleanly.

Check for any compilation warnings.
  </action>
  <verify>
    <automated>cargo build --workspace</automated>
  </verify>
  <done>
    - Clean build with no warnings
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace` passes
- `cargo clippy --workspace -- -D warnings` passes
- `cargo fmt --check` passes
- `sonar-scanner` quality gate passes
- `cargo build --workspace` passes with no warnings
</verification>

<success_criteria>
- All workspace tests pass
- SonarQube quality gate passes
- No compiler warnings
- Clippy passes with -D warnings
- cargo fmt --check passes
</success_criteria>

<output>
After completion, create `.planning/phases/phase-46-uat-regression-validation/46-01-SUMMARY.md`
</output>
