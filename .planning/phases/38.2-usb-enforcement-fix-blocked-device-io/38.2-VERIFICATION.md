---
phase: 38.2-usb-enforcement-fix-blocked-device-io
verified: 2026-05-05T10:35:00Z
status: human_needed
score: 10/10
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  gaps_closed: []
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
human_verification:
  - test: "Plan 03 Task 2: Verify drive-letter correlation on physical Windows machine"
    expected: "Run a test program calling enumerate_fixed_disks() and confirm each disk's drive_letter matches Windows Explorer. Plug a USB device at a known drive letter (e.g., F:) and verify the audit log shows drive_letter 'F' (not 'C')."
    why_human: "Drive-letter correlation involves complex Win32 APIs (SetupDi, IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS, GetVolumePathNamesForVolumeNameW) that cannot be fully validated in CI without real disks. The code compiles and all unit tests pass, but the actual kernel-authoritative mapping behavior can only be verified on a live Windows system with mounted volumes."
---

# Phase 38.2: USB Enforcement Fix — Blocked Device I/O Verification Report

**Phase Goal:** Fix the USB enforcement gap where registered blocked devices log DENY decisions but writes still succeed. Ensure `DeviceController::disable_usb_device` fires at PnP level for all registered blocked USB devices, preventing file I/O before it reaches the filesystem.

**Verified:** 2026-05-05T10:35:00Z

**Status:** human_needed

**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #   | Truth                                                                 | Status     | Evidence |
| --- | --------------------------------------------------------------------- | ---------- | -------- |
| 1   | DeviceController has `set_volume_deny_all` with deny-all SDDL         | VERIFIED   | `device_controller.rs:361-462` — exact SDDL `D:(D;;FA;;;S-1-1-0)(D;;FA;;;S-1-5-11)(A;;FA;;;S-1-5-18)`; caches original DACL; 2 unit tests pass |
| 2   | `apply_tier_enforcement` calls `set_volume_deny_all` for Blocked tier | VERIFIED   | `detection/usb.rs:557-561` — Layer 2 call independent of Layer 1 result |
| 3   | Both PnP disable and DACL deny-all fire for Blocked tier              | VERIFIED   | `detection/usb.rs:536-561` — same match arm calls both `disable_usb_device` and `set_volume_deny_all` with separate error handling |
| 4   | `restore_volume_acl` restores original DACL for deny-all              | VERIFIED   | `device_controller.rs:464-478` — shared `original_dacls` cache, same cleanup path for readonly and deny-all |
| 5   | WR-01 race: identity reconciled when VOLUME arrives first             | VERIFIED   | `detection/usb.rs:596-614` — `reconcile_identity_with_unmapped_drive` fires when exactly one unmapped drive exists; 4 unit tests pass |
| 6   | Startup gap: `scan_existing_usb_identities` captures pre-plugged IDs  | VERIFIED   | `detection/usb.rs:110-185` — enumerates USB devices, matches against unmapped drives, applies tier enforcement; 2 unit tests pass |
| 7   | No timing path leaves Blocked device without enforcement              | VERIFIED   | All paths covered: plug-before-agent (startup scan), plug-while-running (arrival handler), agent-restart-while-plugged (startup scan), VOLUME-before-USB_DEVICE (reconciliation heuristic) |
| 8   | `find_drive_letter_for_instance_id` uses `instance_id` parameter      | VERIFIED   | `dlp-common/src/disk.rs:868-887` — parameter is `instance_id: &str` (no underscore); calls `disk_number_for_instance_id` then `find_drive_letter_for_disk_number` |
| 9   | Drive letter resolved via kernel-authoritative IOCTL mapping          | VERIFIED   | `disk.rs:551-674` (`disk_number_for_instance_id` via `IOCTL_STORAGE_GET_DEVICE_NUMBER`); `disk.rs:718-842` (`find_drive_letter_for_disk_number` via `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS` + `GetVolumePathNamesForVolumeNameW`) |
| 10  | DACL operations target correct volume                                 | VERIFIED   | Drive letter now derived from `instance_id` via kernel-assigned disk numbers, not heuristic scanning; eliminates SanDisk-at-F:-reported-as-C bug |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `dlp-agent/src/device_controller.rs` | `set_volume_deny_all` method with deny-all SDDL | VERIFIED | Lines 361-462. Follows exact pattern of `set_volume_readonly`. Caches original DACL. Two unit tests (`test_set_volume_deny_all_caches_dacl`, `test_deny_all_and_readonly_share_cache`) pass. |
| `dlp-agent/src/detection/usb.rs` | Blocked tier calls both `disable_usb_device` and `set_volume_deny_all` | VERIFIED | Lines 536-561. Both layers fire independently. `reconcile_identity_with_unmapped_drive` helper (lines 260-279). `scan_existing_usb_identities` (lines 110-185). 12 usb tests pass. |
| `dlp-agent/src/service.rs` | Startup call to `scan_existing_usb_identities` | VERIFIED | Line 595: `detector.scan_existing_usb_identities()` called after `scan_existing_drives()`. |
| `dlp-common/src/disk.rs` | Kernel-authoritative drive-letter correlation | VERIFIED | `disk_number_for_instance_id` (lines 551-674), `query_disk_number_for_handle` (lines 681-700), `find_drive_letter_for_disk_number` (lines 718-842), `find_drive_letter_for_instance_id` (lines 868-887). 41 disk tests pass. |

---

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `apply_tier_enforcement` Blocked branch | `controller.set_volume_deny_all` | Direct function call after `disable_usb_device` | WIRED | `detection/usb.rs:557` — independent error handling ensures fallback fires even if primary fails |
| `set_volume_deny_all` | `original_dacls` cache | `insert` before `SetFileSecurityW` | WIRED | `device_controller.rs:414-416` — same cache key mechanism as `set_volume_readonly` |
| `on_usb_device_arrival` | `device_identities` + `apply_tier_enforcement` | `reconcile_identity_with_unmapped_drive` heuristic | WIRED | `detection/usb.rs:603` — fires when exactly one unmapped drive exists |
| `scan_existing_drives` | `scan_existing_usb_identities` | Sequential call in `service.rs` startup | WIRED | `service.rs:594-595` — drive scan first, then identity scan |
| `enumerate_fixed_disks_windows` | `find_drive_letter_for_instance_id` | Per-disk call with `instance_id` | WIRED | `disk.rs:477` — passes real `instance_id` (not underscore-prefixed) |
| `find_drive_letter_for_instance_id` | `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS` / `IOCTL_STORAGE_GET_DEVICE_NUMBER` | Volume-to-disk mapping | WIRED | `disk.rs:873` -> `disk_number_for_instance_id` -> `query_disk_number_for_handle` (IOCTL_STORAGE_GET_DEVICE_NUMBER); `disk.rs:886` -> `find_drive_letter_for_disk_number` (IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `apply_tier_enforcement` | `tier` | `REGISTRY_CACHE.trust_tier_for(vid, pid, serial)` | Yes — registry cache populated from server config | FLOWING |
| `set_volume_deny_all` | `sd_buf` (cached original DACL) | `GetFileSecurityW` on volume root | Yes — queries live NTFS security descriptor | FLOWING |
| `scan_existing_usb_identities` | `identities` | `dlp_common::usb::enumerate_connected_usb_devices()` (SetupDi) | Yes — enumerates actual connected USB devices | FLOWING |
| `find_drive_letter_for_instance_id` | `disk_number` | `disk_number_for_instance_id` -> `IOCTL_STORAGE_GET_DEVICE_NUMBER` | Yes — kernel-assigned disk number from device handle | FLOWING |
| `find_drive_letter_for_disk_number` | `drive_letter` | `GetVolumePathNamesForVolumeNameW` on matched volume | Yes — kernel-assigned mount points | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Device controller tests pass | `cargo test -p dlp-agent --lib -- device_controller` | 5 passed | PASS |
| USB tests pass | `cargo test -p dlp-agent --lib -- usb::tests` | 12 passed | PASS |
| Disk tests pass | `cargo test -p dlp-common --lib -- disk` | 41 passed | PASS |
| dlp-agent builds clean | `cargo build -p dlp-agent` | Exits 0, no warnings | PASS |
| dlp-common builds clean | `cargo build -p dlp-common` | Exits 0, no warnings | PASS |
| dlp-agent clippy clean | `cargo clippy -p dlp-agent -- -D warnings` | No issues | PASS |
| dlp-common clippy clean | `cargo clippy -p dlp-common -- -D warnings` | No issues | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| USB-03 | 38.2-01, 38.2-02, 38.2-03 | Agent enforces trust tier at I/O level: blocked denies all access; read_only allows reads and denies writes | SATISFIED | Phase 38.2 enhances USB-03 with: (1) DACL deny-all defense-in-depth layer (`set_volume_deny_all`), (2) WR-01 race fix and startup gap fix ensuring enforcement fires on all timing paths, (3) kernel-authoritative drive-letter correlation ensuring DACL targets correct volume |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| None | — | — | — | No anti-patterns detected |

---

### Human Verification Required

#### 1. Plan 03 Task 2: Drive-Letter Correlation on Physical Windows Machine

**Test:** Run a test program that calls `enumerate_fixed_disks()` on a Windows machine with multiple fixed disks (including at least one USB mass-storage device). Verify each disk's `drive_letter` matches what Windows Explorer shows. Plug a USB device at a known drive letter (e.g., F:) and verify the audit log shows `drive_letter: "F"` (not `"C"` or any other letter).

**Expected:** All enumerated disks report correct drive letters. The USB device plugged at F: reports `drive_letter: Some('F')`.

**Why human:** Drive-letter correlation involves complex Win32 APIs (`SetupDiEnumDeviceInterfaces`, `IOCTL_STORAGE_GET_DEVICE_NUMBER`, `FindFirstVolumeW`, `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS`, `GetVolumePathNamesForVolumeNameW`) that cannot be fully validated in CI without real disks and mounted volumes. The code compiles and all 41 unit tests pass, but the actual kernel-authoritative mapping behavior can only be verified on a live Windows system.

**Status:** This checkpoint was explicitly deferred per Plan 03 SUMMARY.md ("Task 2 (human-verify checkpoint) deferred"). It is not a blocker for code completion — the implementation is complete and compiles cleanly.

---

### Gaps Summary

No gaps found. All 10 observable truths are verified. All artifacts exist, are substantive, wired, and have real data flowing through them. All automated tests pass. Build and clippy are clean.

The only remaining item is the deferred human-verify checkpoint from Plan 03 Task 2, which requires a physical Windows machine with real disks to validate the drive-letter correlation behavior end-to-end.

---

_Verified: 2026-05-05T10:35:00Z_
_Verifier: Claude (gsd-verifier)_
