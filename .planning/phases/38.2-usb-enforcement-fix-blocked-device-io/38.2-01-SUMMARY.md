---
phase: 38.2-usb-enforcement-fix-blocked-device-io
plan: "01"
subsystem: agent
 tags: [usb, dacl, sddl, defense-in-depth, pnp, windows-security]

# Dependency graph
requires:
  - phase: 38.2-discuss
    provides: "PnP + Volume DACL as two real-time enforcement layers design decision"
provides:
  - "DeviceController::set_volume_deny_all method with deny-all SDDL"
  - "Blocked tier applies both PnP disable AND DACL deny-all (defense-in-depth)"
  - "Unit tests for deny-all cache behavior and shared cache isolation"
affects:
  - "38.2-02" # Phase 38.2 Plan 02
  - "38.2-03" # Phase 38.2 Plan 03

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Layered enforcement: primary (PnP disable) + fallback (DACL deny-all)"
    - "Shared original_dacls cache for both readonly and deny-all operations"
    - "SDDL string security descriptors for volume ACL manipulation"

key-files:
  created: []
  modified:
    - "dlp-agent/src/device_controller.rs"
    - "dlp-agent/src/detection/usb.rs"

key-decisions:
  - "Task 1 (set_volume_deny_all) was completed in a prior session (commit 8050a5e); this execution focused on Task 2 wiring"
  - "set_volume_deny_all replicates the exact pattern of set_volume_readonly for consistency and maintainability"
  - "Both PnP disable and DACL deny-all fire independently: even if primary layer fails, fallback still blocks I/O"

patterns-established:
  - "Volume DACL enforcement: query -> cache -> apply SDDL -> free descriptor"
  - "Defense-in-depth for Blocked tier: two independent enforcement layers with separate error handling"

requirements-completed:
  - USB-03

# Metrics
duration: 8min
completed: 2026-05-05
---

# Phase 38.2 Plan 01: Blocked Tier Defense-in-Depth Summary

**Volume DACL deny-all secondary enforcement layer wired into Blocked-tier USB device handling, providing defense-in-depth if PnP disable fails or is bypassed**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-05T09:41:00Z
- **Completed:** 2026-05-05T09:49:13Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- `DeviceController::set_volume_deny_all` method exists with exact SDDL `D:(D;;FA;;;S-1-1-0)(D;;FA;;;S-1-5-11)(A;;FA;;;S-1-5-18)` — denies Full Access to Everyone and Authenticated Users, allows Full Access to SYSTEM
- `apply_tier_enforcement` Blocked branch now calls BOTH `disable_usb_device` (Layer 1) and `set_volume_deny_all` (Layer 2) independently
- Two unit tests verify cache behavior: `test_set_volume_deny_all_caches_dacl` and `test_deny_all_and_readonly_share_cache`
- `restore_volume_acl` cleanup path unchanged — shared `original_dacls` cache handles both readonly and deny-all

## Task Commits

Each task was committed atomically:

1. **Task 1: Add set_volume_deny_all to DeviceController** — `8050a5e` (wip — completed in prior session)
   - Added `pub fn set_volume_deny_all` with deny-all SDDL
   - Added unit tests for cache behavior
2. **Task 2: Wire set_volume_deny_all into apply_tier_enforcement for Blocked tier** — `0e4519f` (feat)
   - Wired `set_volume_deny_all` call into `UsbTrustTier::Blocked` branch
   - Both layers fire independently with separate error handling and logging

## Files Created/Modified

- `dlp-agent/src/device_controller.rs` — `set_volume_deny_all` method (lines 340-462) with deny-all SDDL; unit tests (lines 587-650)
- `dlp-agent/src/detection/usb.rs` — `apply_tier_enforcement` Blocked branch now calls both `disable_usb_device` and `set_volume_deny_all` (lines 407-432)

## Decisions Made

- Task 1 was already implemented in a prior session (commit `8050a5e`). This execution session focused on Task 2 wiring only.
- The `set_volume_deny_all` call is independent of `disable_usb_device` result — defense-in-depth requires both layers to attempt enforcement regardless of individual success/failure.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Threat Surface Scan

No new threat surface introduced beyond what was already modeled in the plan's `<threat_model>`:

| Threat ID | Status | Notes |
|-----------|--------|-------|
| T-38.2-01-01 | Mitigated | SDDL explicitly allows SYSTEM (S-1-5-18) full access |
| T-38.2-01-02 | Mitigated | Drive letter from kernel-assigned VOLUME notification, not user-controlled |
| T-38.2-01-03 | Mitigated | Shared `original_dacls` cache with `restore_volume_acl` cleanup |
| T-38.2-01-04 | Accepted | Admin can stop dlp-agent service to clear DACL if needed |

## Known Stubs

None.

## Next Phase Readiness

- Plan 01 complete — defense-in-depth layer for Blocked tier is active
- Ready for Plan 02 (Phase 38.2 next plan)

---
*Phase: 38.2-usb-enforcement-fix-blocked-device-io*
*Completed: 2026-05-05*
