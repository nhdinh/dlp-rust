# Phase 38.3: AGENT-UNKNOWN Remediation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md -- this log preserves the alternatives considered.

**Date:** 2026-05-06
**Phase:** 38.3-AGENT-UNKNOWN Remediation
**Areas discussed:** AGENT-UNKNOWN sentinel format, File interception applicability, Metric counter design, Remediation path in schema

---

## AGENT-UNKNOWN Sentinel Format

| Option | Description | Selected |
|--------|-------------|----------|
| A -- Always emit AppIdentity::default() (empty strings, always present) | Remove skip_serializing_if. Missing identity = empty strings + unknown tiers. Predictable schema. | |
| B -- Explicit AGENT-UNKNOWN string (self-describing) | image_path='AGENT-UNKNOWN', publisher='AGENT-UNKNOWN'. Obvious in SIEM queries. Best for compliance. | X |
| C -- Hybrid (keep skip_serializing_if, use AGENT-UNKNOWN only when present but unresolved) | Preserve backward compat. Field omitted when not applicable. Sentinel only where resolution was attempted but failed. | |

**User's choice:** B -- Explicit AGENT-UNKNOWN string (self-describing)
**Notes:** User asked for detailed explanation first. Chose B after seeing concrete JSON examples. Rationale: explicit sentinel makes the gap obvious in SIEM queries and is best for compliance.

---

## File Interception Applicability

| Option | Description | Selected |
|--------|-------------|----------|
| A -- Populate source_application for file events too (full AppIdentity via WinVerifyTrust, matching clipboard) | Every file block/access event resolves full AppIdentity. Consistent schema across all event types. Adds WinVerifyTrust overhead per file operation. | |
| B -- File events use application_path only; source_application stays clipboard-only | Keep current behavior. File events emit AGENT-UNKNOWN sentinel in source_application. Lower overhead. | |
| C -- Best-effort: resolve full AppIdentity only for file BLOCK events (not ACCESS) | Block events are lower frequency and higher stakes -- worth the WinVerifyTrust cost. Access events (ALLOW) stay path-only. Hybrid approach. | X |

**User's choice:** C -- Best-effort: resolve full AppIdentity only for file BLOCK events (not ACCESS)
**Notes:** User selected directly. Rationale: BLOCK events are lower frequency and higher stakes, justifying the WinVerifyTrust cost. ACCESS events stay on the fast path.

---

## Metric Counter Design

| Option | Description | Selected |
|--------|-------------|----------|
| A -- Simple AtomicUsize per path, logged via tracing::info! periodically | Zero deps. Fastest. Reset on restart. Log-line parsing for SIEM. | |
| B -- Structured metrics module with labels + admin queryable | Queryable at runtime from admin TUI. More code + IPC plumbing. | |
| C -- Emit special audit event every N AGENT-UNKNOWN occurrences | SIEM aggregates. Durable. Audit log noise. Coarse granularity. | |
| D -- In-memory counters + periodic flush to audit log | Fast runtime counting + durable audit trail. Best of both. | X |

**User's choice:** D -- In-memory counters + periodic flush to audit log
**Notes:** User asked for detailed explanation first. Chose D after understanding tradeoffs. Rationale: best of both worlds -- fast runtime counting plus durable audit trail.

---

## Remediation Path in Schema

| Option | Description | Selected |
|--------|-------------|----------|
| A -- Add remediation_hint field to AuditEvent | Self-documenting events. SIEM dashboards show hint inline. Small schema change. | |
| B -- External documentation only | No code/schema change. Document in admin docs and SIEM runbooks. | X |
| C -- Event-type-dependent remediation_hint | Different hint per path (clipboard vs file vs Chrome). Most informative but most code change. | |

**User's choice:** B -- External documentation only
**Notes:** User asked "what is AGENT-UNKNOWN?" before deciding. After explanation, chose B. Rationale: no schema change needed; remediation path documented in external admin docs and SIEM runbooks.

---

## Claude's Discretion

- Flush threshold of 50 occurrences may be adjusted by planner based on expected event volume.
- Event type for metric flush (`ConfigChange` vs new `Metric` variant) at planner discretion.

## Deferred Ideas

None -- discussion stayed within phase scope.
