---
phase: 38.3-agent-unknown-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/audit.rs
  - dlp-common/src/endpoint.rs
autonomous: true
requirements:
  - AUDIT-05
---

<objective>
Schema changes for AGENT-UNKNOWN remediation: remove `skip_serializing_if` from `source_application`/`destination_application` in `AuditEvent`, add `AGENT_UNKNOWN_APP` sentinel constant to `endpoint.rs`, and update all affected tests.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Remove skip_serializing_if from AuditEvent app identity fields</name>
  <files>dlp-common/src/audit.rs</files>
  <action>
    Remove `#[serde(skip_serializing_if = "Option::is_none")]` from:
    - `source_application` field (line ~163)
    - `destination_application` field (line ~166)

    Update `test_skip_serializing_none_fields` to assert that `source_application` and `destination_application` ARE present as `null` when None (instead of being omitted).
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib -- audit::tests::test_skip_serializing_none_fields</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add AGENT_UNKNOWN_APP sentinel constant</name>
  <files>dlp-common/src/endpoint.rs</files>
  <action>
    Add a public constant `AGENT_UNKNOWN_APP` of type `AppIdentity` with:
    - image_path: "AGENT-UNKNOWN"
    - publisher: "AGENT-UNKNOWN"
    - trust_tier: AppTrustTier::Unknown
    - signature_state: SignatureState::Unknown

    Add a unit test verifying the constant fields.
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib -- endpoint::tests</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- `source_application` and `destination_application` are always present in AuditEvent JSON (even when null)
- `AGENT_UNKNOWN_APP` constant exists and is publicly accessible
- All dlp-common tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/38.3-agent-unknown-remediation/38.3-01-SUMMARY.md`
</output>