---
phase: 59-label-service
plan: 03
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - dlp-server/src/policy_store.rs
  - dlp-server/src/lib.rs
  - dlp-server/src/main.rs
  - dlp-server/src/admin_api.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements:
  - LABEL-05
must_haves:
  truths:
    - "PolicyStore::evaluate() resolves tier from LabelService when label_aware_evaluation_enabled is true"
    - "label_aware_evaluation_enabled flag is cached in AppState as AtomicBool, refreshed every 30s"
    - "When label-aware mode is OFF, existing behavior is preserved exactly (no changes to evaluation)"
    - "Unlabeled resources fallback to UnclassifiedBlocked when label-aware mode is ON"
    - "Label lookup failures are fail-closed (deny T4) — not fallback to request classification"
    - "LabelService None when flag is ON results in deny (T4) — no backward-compat fallback"
    - "Persisted audit event (not just tracing::info!) emitted for classification overrides"
    - "Evaluation audit uses correct UnitOfWork-based store_events_sync signature (not &Pool)"
    - "ClassificationOverride EventType is added to routed_to_siem()"
    - "D-14 amended to explicitly document evaluation-path audit exception"
    - "All evaluate() call sites documented, especially enforcement path"
  artifacts:
    - path: "dlp-server/src/policy_store.rs"
      provides: "Label-aware evaluation with fail-closed behavior matrix"
      contains: "evaluate() with ResolvedTier handling, behavior matrix comment"
    - path: "dlp-server/src/lib.rs"
      provides: "AppState with label_aware_enabled AtomicBool cache"
      contains: "label_aware_enabled: Arc<AtomicBool>"
    - path: "dlp-server/src/main.rs"
      provides: "Background task refreshing label_aware_evaluation_enabled from system_kv"
    - path: "dlp-common/src/audit.rs"
      provides: "ClassificationOverride EventType variant in routed_to_siem()"
  key_links:
    - from: "PolicyStore::evaluate()"
      to: "LabelService::resolve_tier()"
      via: "state.label_service.resolve_tier(resource_path)"
    - from: "AppState.label_aware_enabled"
      to: "PolicyStore::evaluate()"
      via: "flag read (no DB query per evaluation)"
    - from: "AbacContext.resource_path"
      to: "EvaluateRequest.resource.path"
      via: "copied at evaluation time"
---

<objective>
Fix ABAC integration to address HIGH and MEDIUM review concerns: (1) cache label_aware_enabled in AppState AtomicBool to eliminate per-evaluation DB reads, (2) implement fail-closed behavior matrix per D-11b (deny on ALL error/missing paths when flag is ON), (3) add persisted audit event for classification overrides using correct UnitOfWork signature (CR-01), (4) add ClassificationOverride to routed_to_siem() (WR-01), (5) amend D-14 to explicitly document evaluation-path audit exception (CR-02).

Purpose: The current implementation reads system_kv per evaluation call, falls back to request classification on errors (fail-open), only logs overrides via tracing, has a compile-error in the audit emission plan, and omits SIEM routing for the new event type. These are security, audit integrity, and compilation gaps.
Output: Updated policy_store.rs with cached flag, fail-closed matrix, and persisted audit with correct signature; AppState with AtomicBool cache and background refresh; D-14 amendment documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-label-service/59-CONTEXT.md
@.planning/phases/59-label-service/059-REVIEWS.md

## Current Code State

The existing `dlp-server/src/policy_store.rs` has:
- `evaluate()` accepts `Option<&LabelService>` parameter
- Reads `is_label_aware_enabled()` from system_kv PER CALL (not cached — review concern)
- On DB error in `resolve_tier`, logs error and uses request classification (fail-open — review concern)
- On `resolve_tier` error, logs and uses request classification (not fail-closed)
- `UnclassifiedBlocked` maps to `Classification::T4` (correct)
- NO `label_aware_enabled` cached in AppState as AtomicBool
- Audit trail: only `tracing::info!` / `tracing::error!` — no persisted audit events (review concern)

The existing `dlp-server/src/lib.rs` AppState has:
- `label_service: Arc<label_service::LabelService>`
- NO `label_aware_enabled: Arc<AtomicBool>` field

The existing `dlp-common/src/abac.rs` has:
- `AbacContext` with `resource_path: Option<String>` — ALREADY EXISTS (review WR-02 was a false alarm)

The existing `dlp-server/src/audit_store.rs` has:
- `store_events_sync(uow: &UnitOfWork<'_>, events: &[AuditEvent]) -> Result<(), AppError>` — takes &UnitOfWork, NOT &Pool (CR-01)

## Key Interfaces

From `dlp-server/src/label_service.rs` (from Plan 59-01):
```rust
pub enum ResolvedTier {
    Exact(Tier),
    Inherited { tier: Tier, parent_path: String },
    Fallback,
    LookupFailed,
}
impl ResolvedTier {
    pub fn tier(&self) -> Tier;
    pub fn source(&self) -> &'static str;
}
```

From `dlp-common/src/abac.rs`:
```rust
pub struct AbacContext {
    pub subject: Subject,
    pub resource: Resource,
    pub environment: Environment,
    pub action: Action,
    pub resource_path: Option<String>,  // Already exists — verified
}
pub struct Resource {
    pub path: String,
    pub classification: Classification,
}
```

From `dlp-server/src/db/repositories/system_kv.rs`:
```rust
pub fn get(conn: &Connection, key: &str) -> rusqlite::Result<Option<String>>;
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache label_aware_enabled in AppState with background refresh</name>
  <files>dlp-server/src/lib.rs, dlp-server/src/main.rs</files>
  <read_first>
    - dlp-server/src/lib.rs (AppState struct definition, Debug impl)
    - dlp-server/src/main.rs (AppState construction, existing background tasks)
    - dlp-server/src/db/repositories/system_kv.rs (system_kv::get signature)
  </read_first>
  <behavior>
    - Test: AppState includes `label_aware_enabled: Arc<AtomicBool>`
    - Test: `AppState::is_label_aware_enabled()` reads cached flag (no DB query)
    - Test: Background task refreshes the flag every 30 seconds from system_kv
    - Test: When system_kv flag changes, cached value updates within 30 seconds
    - Test: Background task wraps DB call in `tokio::task::spawn_blocking`
    - Test: Default value is false (off) when key missing
  </behavior>
  <action>
    Modify `dlp-server/src/lib.rs`:

    1. Add import: `use std::sync::atomic::{AtomicBool, Ordering};`

    2. Extend `AppState` with `label_aware_enabled: Arc<AtomicBool>` field. Document: "Cached label-aware evaluation flag. Refreshed from system_kv every 30s. Default false (off) until operator explicitly enables."

    3. Add `impl AppState` helper: `pub fn is_label_aware_enabled(&self) -> bool` that loads with `Ordering::Relaxed`.

    4. Update `Debug` impl to include the new field.

    Modify `dlp-server/src/main.rs`:

    1. After `label_service` creation, read the initial flag value from `system_kv::get` with key `"label_aware_evaluation_enabled"`. Store as `Arc<AtomicBool>`.

    2. Spawn a background `tokio::spawn` task with a `tokio::time::interval(Duration::from_secs(30))`. Inside each tick, wrap the `system_kv::get` call in `tokio::task::spawn_blocking`. On success, compare old vs new value and `store()` if changed, logging an `info!` event. On error, log `error!` and leave the cached value unchanged.

    3. Add `label_aware_enabled` to the `AppState` struct initialization.

    4. Update ALL existing `AppState` test initializers to include `label_aware_enabled: Arc::new(AtomicBool::new(false))`.

    Per review: Per-evaluation DB reads are a severe performance regression. Caching eliminates the DB query from the hot path.
    Per D-11: Default off until operator opts in via system_kv.
    Per review: Background task wraps DB call in `tokio::task::spawn_blocking` for SQLite blocking safety.
  </action>
  <verify>
    <automated>cargo test -p dlp-server policy_store::</automated>
  </verify>
  <done>AppState includes label_aware_enabled AtomicBool; background refresh task spawns with spawn_blocking; cached flag updates within 30s; no DB query per evaluation; all test initializers updated; tests pass</done>
  <acceptance_criteria>
    - `AppState` has `label_aware_enabled: Arc<AtomicBool>` field
    - `AppState::is_label_aware_enabled()` returns cached boolean (no DB access)
    - Background task spawns in `main.rs` with 30-second interval
    - Background task wraps `system_kv::get` in `tokio::task::spawn_blocking`
    - Initial value read from `system_kv` at startup; defaults to `false` if key missing
    - Value change logged at `info!` level
    - DB errors logged at `error!` level without panicking
    - All `AppState` test fixtures updated with the new field
    - `cargo test -p dlp-server policy_store::` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix fail-closed semantics and behavior matrix per D-11b</name>
  <files>dlp-server/src/policy_store.rs</files>
  <read_first>
    - dlp-server/src/policy_store.rs (current evaluate(), is_label_aware_enabled(), Resource, Classification usage)
    - dlp-server/src/label_service.rs (ResolvedTier enum, tier() and source() methods)
    - dlp-common/src/abac.rs (AbacContext, Resource, Classification)
  </read_first>
  <behavior>
    - Test: When label_aware_enabled is false, evaluate() uses request's classification unchanged
    - Test: When label_aware_enabled is true and resource has exact label, evaluate() uses label's tier
    - Test: When label_aware_enabled is true and resource has parent folder label, evaluate() uses parent's tier
    - Test: When label_aware_enabled is true and no label exists, evaluate() uses UnclassifiedBlocked (T4 deny)
    - Test: When label_aware_enabled is true and DB lookup fails, evaluate() uses UnclassifiedBlocked (T4 deny)
    - Test: When label_aware_enabled is true but resource_path is None, evaluate() uses T4 deny
    - Test: When label_aware_enabled is true but LabelService is None, evaluate() uses T4 deny (no backward compat)
    - Test: Tier::UnclassifiedBlocked causes DENY for all non-allowlist policies
  </behavior>
  <action>
    Modify `dlp-server/src/policy_store.rs`:

    1. Update `evaluate()` signature to accept the cached flag directly (no more internal DB read):
       ```rust
       pub fn evaluate(
           &self,
           ctx: &AbacContext,
           label_service: Option<&crate::label_service::LabelService>,
           label_aware_enabled: bool,
       ) -> EvaluateResponse
       ```
       Remove the `is_label_aware_enabled()` helper method (no longer needed).

    2. Add a behavior matrix comment at the start of the label-aware block documenting all flag/path/label combinations per D-11b:
       ```
       // | flag | LabelService | resource_path | exact label | inherited | no label | lookup failed |
       // |------|-------------|---------------|-------------|-----------|----------|---------------|
       // | off  | any         | any           | use request | use req   | use req  | use req       |
       // | on   | None        | any           | T4 deny     | T4 deny   | T4 deny  | T4 deny       |
       // | on   | Some        | None          | T4 deny     | T4 deny   | T4 deny  | T4 deny       |
       // | on   | Some        | Some          | label tier  | parent    | T4 deny  | T4 deny       |
       ```
       Also document the critical invariant: label-aware ABAC can only make the result STRICTER, never override NTFS deny.

    3. Implement the fail-closed logic:
       - If `label_aware_enabled` is false: skip all label resolution, use `ctx.resource.classification` unchanged.
       - If `label_aware_enabled` is true and `label_service` is None: set `resource.classification = Classification::T4` (deny).
       - If `label_aware_enabled` is true and `resource_path` is None: set `resource.classification = Classification::T4` (deny).
       - If `label_aware_enabled` is true and `label_service` is Some: call `resolve_tier(path)` and handle all variants:
         - `Exact(tier)` or `Inherited { tier, .. }`: convert tier to classification (T4 fallback for UnclassifiedBlocked)
         - `Fallback`: set `Classification::T4` (deny unlabeled resources)
         - `LookupFailed`: set `Classification::T4`, log error (fail-closed)
       - Track `classification_overridden` and `override_source` for audit emission in Task 3.

    4. Continue with existing policy evaluation using the (potentially modified) `resource`.

    5. Update ALL call sites of `evaluate()` to pass the new parameters:
       - Admin API evaluate handler: pass `Some(&state.label_service)` and `state.is_label_aware_enabled()`
       - Test fixtures: pass `None` and `false` for backward compatibility
       - Find call sites with `rg "\.evaluate\(" dlp-server/src/ --type rust`

    Per D-11b: Fail-closed behavior matrix implemented. All error/missing paths deny (T4).
    Per review MEDIUM: "LabelService None fallback" — now fails closed (T4 deny), not backward compat.
    Per review HIGH: "ResolvedTier import location" — uses `crate::label_service::ResolvedTier` (already fixed in Plan 59-01).
  </action>
  <verify>
    <automated>cargo test -p dlp-server policy_store::</automated>
  </verify>
  <done>evaluate() uses cached flag (no DB read); behavior matrix documented; all 8 fail-closed scenarios tested and passing; call sites updated; tests pass</done>
  <acceptance_criteria>
    - `evaluate()` signature accepts `label_service: Option<&LabelService>` and `label_aware_enabled: bool`
    - `is_label_aware_enabled()` helper method removed from PolicyStore
    - Behavior matrix comment exists in `evaluate()` documenting all 12 flag/path/label combinations
    - When `label_aware_enabled=false`: `resource.classification` is unchanged (request classification)
    - When `label_aware_enabled=true` and `label_service=None`: classification becomes T4 (deny)
    - When `label_aware_enabled=true` and `resource_path=None`: classification becomes T4 (deny)
    - When `label_aware_enabled=true` and `resolve_tier` returns `Fallback`: classification becomes T4 (deny)
    - When `label_aware_enabled=true` and `resolve_tier` returns `LookupFailed`: classification becomes T4 (deny)
    - When `label_aware_enabled=true` and `resolve_tier` returns `Exact` or `Inherited`: uses label tier
    - All `evaluate()` call sites compile with updated signature
    - `cargo test -p dlp-server policy_store::` passes (all 8+ fail-closed tests)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add persisted audit event for classification overrides with correct signature</name>
  <files>dlp-server/src/policy_store.rs, dlp-common/src/audit.rs</files>
  <read_first>
    - dlp-server/src/policy_store.rs (evaluate() method, current classification override logic)
    - dlp-server/src/audit_store.rs (store_events_sync signature — takes &UnitOfWork, NOT &Pool)
    - dlp-common/src/audit.rs (EventType enum, routed_to_siem() method)
  </read_first>
  <behavior>
    - Test: When classification is overridden by label service, a persisted audit event is created
    - Test: Audit event contains original classification, resolved classification, and source
    - Test: Audit event contains the resource path
    - Test: No audit event is created when label-aware mode is OFF
    - Test: No audit event is created when no override occurs (exact match same as request)
    - Test: Audit persistence failure logs error but does not change evaluation decision
    - Test: ClassificationOverride is in routed_to_siem() return true
  </behavior>
  <action>
    Modify `dlp-common/src/audit.rs`:

    1. Add `ClassificationOverride` variant to `EventType` enum:
       ```rust
       /// A classification was overridden by the label service during policy evaluation.
       ClassificationOverride,
       ```
       With serde rename: `#[serde(rename = "classification_override")]`

    2. Add `Self::ClassificationOverride` to the `routed_to_siem()` matches! expression.

    Modify `dlp-server/src/policy_store.rs`:

    3. After the label-aware resolution block (where `classification_overridden` is set), add persisted audit emission using the CORRECT signature (CR-01 fix):
       ```rust
       if classification_overridden {
           // Acquire a fresh connection and UnitOfWork for the audit event.
           // This is a separate transaction from the evaluation (which is read-only).
           // Evaluation audit is a mandatory attempt per D-14 amendment:
           // we ALWAYS try to persist, but failing closed on audit failure would
           // create a DoS vector. The mutation path (Plan 59-02) uses transactional
           // mandatory audit where failure rolls back the mutation.
           match self.pool.get() {
               Ok(mut conn) => {
                   match crate::db::UnitOfWork::new(&mut conn) {
                       Ok(uow) => {
                           let audit_event = dlp_common::AuditEvent::new(
                               dlp_common::EventType::ClassificationOverride,
                               String::new(),
                               "policy_engine".to_string(),
                               ctx.resource_path.clone().unwrap_or_default(),
                               original_classification,
                               dlp_common::Action::Evaluate,
                               dlp_common::Decision::ALLOW,
                               "label_aware_override".to_string(),
                               0,
                           );
                           if let Err(e) = audit_store::store_events_sync(&uow, &[audit_event]) {
                               tracing::error!(error = %e, "failed to persist classification override audit");
                           } else if let Err(e) = uow.commit() {
                               tracing::error!(error = %e, "failed to commit classification override audit");
                           }
                       }
                       Err(e) => tracing::error!(error = %e, "failed to create UOW for audit"),
                   }
               }
               Err(e) => tracing::error!(error = %e, "failed to acquire conn for audit"),
           }
       }
       ```

    4. Document the evaluation audit tradeoff explicitly in a code comment:
       ```
       // AUDIT TRADEOFF (D-14 amendment): Classification override audit is a mandatory
       // attempt. We ALWAYS try to persist the audit event. If persistence fails, we log
       // the failure at ERROR level and continue with the evaluation. Failing closed
       // on audit failure would create a DoS vector where an attacker exhausts the audit
       // store and denies all access. The mutation path (Plan 59-02) uses transactional
       // mandatory audit where failure rolls back the mutation. This distinction is
       // intentional, documented, and formally recorded in 59-CONTEXT.md D-14.
       ```

    5. Amend D-14 in `59-CONTEXT.md` to explicitly document the evaluation-path exception:
       Add after the existing D-14 text:
       ```
       // D-14 Amendment (Evaluation Path):
       // Classification override audit events (emitted during PolicyStore::evaluate())
       // are mandatory attempts, not transactional mandatory. The evaluation path is
       // read-only and cannot be rolled back. Failing closed on audit persistence
       // failure would create a DoS vector. The mutation path (admin API CRUD)
       // remains transactional mandatory per the original D-14.
       ```

    Per review CR-01: Uses correct `&UnitOfWork` signature (not `&Pool`).
    Per review CR-02: D-14 formally amended to document the evaluation-path exception.
    Per review MEDIUM: "Audit trail only tracing::info!" — now adds persisted audit event.
    Per review WR-01: ClassificationOverride added to routed_to_siem().
  </action>
  <verify>
    <automated>cargo test -p dlp-server policy_store::test_label_aware_audit_event && cargo test -p dlp-common audit::test_classification_override_routed_to_siem</automated>
  </verify>
  <done>Persisted audit event created for every classification override using correct UnitOfWork signature; ClassificationOverride in routed_to_siem(); D-14 amended; tests verify audit persistence and SIEM routing</done>
  <acceptance_criteria>
    - `EventType::ClassificationOverride` variant exists with serde rename
    - `ClassificationOverride` returns `true` from `routed_to_siem()`
    - When `classification_overridden=true`, `audit_store::store_events_sync` is called with `&UnitOfWork` (not `&Pool`)
    - Audit event includes original classification, resolved classification, override source, and resource path
    - Audit persistence failure logs `tracing::error!` but does NOT change the evaluation decision
    - No audit event is emitted when `label_aware_enabled=false`
    - No audit event is emitted when classification is not overridden
    - Code comment documents the audit tradeoff (mandatory attempt vs transactional mandatory)
    - D-14 in 59-CONTEXT.md is amended to explicitly document the evaluation-path exception
    - `cargo test -p dlp-server policy_store::test_label_aware_audit_event` passes
    - `cargo test -p dlp-common audit::test_classification_override_routed_to_siem` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EvaluateRequest->PolicyStore | Untrusted client classification can be overridden by label service |
| Cache->PolicyStore | Cached flag must not be stale longer than 30s refresh interval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-17 | Tampering | Client classification override | mitigate | Label service overrides client-provided classification; client cannot spoof tier |
| T-59-18 | Elevation | UnclassifiedBlocked fallback | mitigate | Default-off flag prevents surprise breakages; admin must explicitly enable |
| T-59-19 | Information Disclosure | Flag state leakage | accept | Flag is boolean operational config, not sensitive |
| T-59-20 | Denial of Service | Per-evaluation DB query | mitigate | Flag cached in AtomicBool; no DB query per evaluation |
| T-59-21 | Tampering | Stale flag cache | mitigate | 30s refresh interval; flag changes propagate quickly |
| T-59-22 | Elevation | Lookup failure fallback | mitigate | Fail-closed: LookupFailed -> deny (T4); never fallback to request classification |
| T-59-23 | Repudiation | Missing audit trail | mitigate | Every override persisted in audit_events table with original->resolved and source |
| T-59-24 | Elevation | Missing path when flag ON | mitigate | Fail-closed: no resource_path -> T4 deny; documented in behavior matrix |
| T-59-25 | Elevation | LabelService None when flag ON | mitigate | Fail-closed: no LabelService -> T4 deny; no backward-compat fallback |
| T-59-26 | Tampering | NTFS deny bypass | mitigate | Critical invariant: label-aware can only make result STRICTER; NTFS deny always wins |
</threat_model>

<verification>
- `cargo test -p dlp-server policy_store::` passes
- `cargo test -p dlp-server` passes
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo build --workspace` succeeds
</verification>

<success_criteria>
- AppState includes `label_aware_enabled: Arc<AtomicBool>` with 30s background refresh
- Background refresh wraps system_kv::get() in tokio::task::spawn_blocking
- `AppState::is_label_aware_enabled()` reads cached flag (no DB query)
- PolicyStore::evaluate() accepts optional LabelService and bool flag parameters
- Behavior matrix documented in code comment showing all flag/path/label combinations
- When label_aware_enabled=true, evaluate() resolves tier from LabelService
- When label_aware_enabled=false or missing, existing behavior preserved exactly
- Unlabeled resources with label-aware ON fallback to UnclassifiedBlocked (T4 for policy engine)
- Missing resource_path with label-aware ON fallback to T4 deny
- DB lookup failures return ResolvedTier::LookupFailed -> deny (fail-closed)
- LabelService None with label-aware ON -> deny (T4) — no backward-compat fallback
- Persisted audit event created for every classification override (in audit_events table)
- Audit event contains original classification, resolved classification, source, and resource path
- Audit emission uses correct `&UnitOfWork` signature (not `&Pool`)
- ClassificationOverride EventType is routed to SIEM
- Audit persistence failure is logged but does not block evaluation (documented tradeoff)
- D-14 in 59-CONTEXT.md amended to explicitly document evaluation-path exception
- All existing evaluate() call sites compile with updated signature
- Critical invariant documented: NTFS ALLOW + ABAC DENY = DENY
</success_criteria>

<output>
Create `.planning/phases/59-label-service/59-03-SUMMARY.md` when done
</output>
