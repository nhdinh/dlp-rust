---
phase: 59-label-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-server/src/db/mod.rs
  - dlp-server/src/label_service.rs
  - dlp-common/src/label.rs
autonomous: true
requirements:
  - LABEL-01
  - LABEL-02
  - LABEL-05
must_haves:
  truths:
    - "LabelService::resolve_tier returns ResolvedTier with source semantics preserved"
    - "Folder inheritance uses strictest-tier-wins: explicit child tier compared against inherited parent tier"
    - "Cache stores full resolution metadata (tier, source, parent_path) not just Tier"
    - "ResolvedTier enum lives in dlp-server (not dlp-common) per D-19"
    - "Tier strictness order enforced: UnclassifiedBlocked > T4 > T3 > T2 > T1"
    - "DB schema verified: labels table has CHECK constraints on tier, object_type, label_state and 6 indexes"
  artifacts:
    - path: "dlp-server/src/label_service.rs"
      provides: "ResolvedTier enum, strictness comparison, metadata cache"
      exports: ["ResolvedTier", "LabelService::resolve_tier", "LabelCache"]
    - path: "dlp-common/src/label.rs"
      provides: "Tier enum with UnclassifiedBlocked, strictness_rank method"
      exports: ["Tier", "Tier::strictness_rank"]
  key_links:
    - from: "LabelService::resolve_tier"
      to: "LabelRepository::get_by_path / find_parent_label"
      via: "DB queries in strictness-aware sequence"
      pattern: "exact_match -> strictness_compare -> parent_walk -> fallback"
    - from: "LabelCache"
      to: "ResolvedTier"
      via: "cache stores CacheEntry with source metadata"
      pattern: "HashMap<String, CacheEntry> where CacheEntry = {tier, source, parent_path}"
---

<objective>
Fix LabelService to address HIGH and MEDIUM review concerns: (1) verify DB schema has required CHECK constraints and indexes, (2) add ResolvedTier enum in dlp-server per D-19, (3) implement strictest-tier-wins inheritance per D-07b, (4) cache full resolution metadata instead of just Tier.

Purpose: The current resolve_tier returns only Tier, loses inheritance source semantics, does not compare strictness when both explicit and inherited labels exist, and the schema was assumed rather than verified. These are core requirement violations flagged in review CR-01 and CR-03.
Output: Verified schema, updated label_service.rs with ResolvedTier enum, strictness-aware resolution, and metadata-preserving cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-label-service/59-CONTEXT.md
@.planning/phases/59-label-service/059-REVIEWS.md

## Current Code State

The existing `dlp-server/src/label_service.rs` has:
- `LabelCache` storing `HashMap<String, (Tier, Instant)>` — only Tier, no source
- `resolve_tier` returning `rusqlite::Result<Tier>` — no source semantics
- Resolution order: exact match (returns immediately) -> parent walk -> fallback
- **BUG**: Exact match returns without comparing against parent tier strictness, violating LABEL-02
- **BUG**: Cache loses whether result was Exact or Inherited

The existing `dlp-server/src/db/mod.rs` has:
- `labels` table with CHECK constraints on `object_type`, `tier`, `label_state`
- 6 indexes: `idx_labels_path`, `idx_labels_tier`, `idx_labels_state`, `idx_labels_owner`, `idx_labels_parent`, `idx_labels_department`
- These were implemented in prior phases but never explicitly verified in plans

The existing `dlp-common/src/label.rs` has:
- `Tier` enum with T1/T2/T3/T4/UnclassifiedBlocked
- `to_classification()`, `from_classification()`, `is_sensitive()` methods
- **NO** `strictness_rank()` method (needed for comparison)

## Key Interfaces

From `dlp-common/src/label.rs`:
```rust
pub enum Tier { T1, T2, T3, T4, UnclassifiedBlocked }
impl Tier {
    pub fn to_classification(&self) -> Option<Classification>;
    pub fn from_classification(c: Classification) -> Self;
    pub fn is_sensitive(&self) -> bool;
}
```

From `dlp-server/src/db/repositories/labels.rs`:
```rust
pub struct LabelRow { pub id: String, pub path: String, pub object_type: String, pub tier: String, ... }
pub struct LabelRepository;
impl LabelRepository {
    pub fn get_by_path(pool: &Pool, path: &str) -> Result<Option<LabelRow>>;
    pub fn find_parent_label(pool: &Pool, child_path: &str) -> Result<Option<LabelRow>>;
}
```

Tier strictness order (per D-07b): UnclassifiedBlocked > T4 > T3 > T2 > T1
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify DB schema has required constraints and indexes</name>
  <files>dlp-server/src/db/mod.rs</files>
  <read_first>
    - dlp-server/src/db/mod.rs (init_tables function, labels table CREATE TABLE and CREATE INDEX statements)
  </read_first>
  <behavior>
    - Test: init_tables() creates labels table with CHECK(object_type IN ('file', 'folder', 'archive'))
    - Test: init_tables() creates labels table with CHECK(tier IN ('T1', 'T2', 'T3', 'T4', 'Unclassified-Blocked'))
    - Test: init_tables() creates labels table with CHECK(label_state IN ('temporary', 'confirmed', 'rejected', 'expired'))
    - Test: init_tables() creates 6 indexes: idx_labels_path, idx_labels_tier, idx_labels_state, idx_labels_owner, idx_labels_parent, idx_labels_department
    - Test: parent_label_id has REFERENCES labels(id) ON DELETE SET NULL
  </behavior>
  <action>
    Read `dlp-server/src/db/mod.rs` and verify the `init_tables()` function contains:

    1. `CREATE TABLE IF NOT EXISTS labels` with columns: id, path, object_type, tier, label_state, owner_sid, parent_label_id, acl_snapshot_id, hash, scanner_confidence, department, created_at, updated_at
    2. CHECK constraints:
       - `CHECK(object_type IN ('file', 'folder', 'archive'))`
       - `CHECK(tier IN ('T1', 'T2', 'T3', 'T4', 'Unclassified-Blocked'))`
       - `CHECK(label_state IN ('temporary', 'confirmed', 'rejected', 'expired'))`
    3. Foreign key: `parent_label_id TEXT REFERENCES labels(id) ON DELETE SET NULL`
    4. Six indexes:
       - `CREATE INDEX IF NOT EXISTS idx_labels_path ON labels(path)`
       - `CREATE INDEX IF NOT EXISTS idx_labels_tier ON labels(tier)`
       - `CREATE INDEX IF NOT EXISTS idx_labels_state ON labels(label_state)`
       - `CREATE INDEX IF NOT EXISTS idx_labels_owner ON labels(owner_sid)`
       - `CREATE INDEX IF NOT EXISTS idx_labels_parent ON labels(parent_label_id)`
       - `CREATE INDEX IF NOT EXISTS idx_labels_department ON labels(department)`

    If ANY of these are missing, add them to `init_tables()` before proceeding.

    Add a unit test `test_labels_schema_constraints` that creates an in-memory pool, attempts to insert invalid values (bad object_type, bad tier, bad label_state), and asserts each INSERT fails with a CHECK constraint error.

    Per review CR-01: Schema was claimed but not verified. This task explicitly validates the schema.
  </action>
  <verify>
    <automated>cargo test -p dlp-server test_labels_schema_constraints</automated>
  </verify>
  <done>All 3 CHECK constraints verified via test; all 6 indexes present in init_tables; FK constraint verified; test passes</done>
  <acceptance_criteria>
    - `init_tables()` contains labels table with all required columns
    - CHECK constraints on object_type, tier, and label_state exist
    - parent_label_id has REFERENCES labels(id) ON DELETE SET NULL
    - All 6 indexes are created
    - Unit test verifies CHECK constraints reject invalid values
    - `cargo test -p dlp-server test_labels_schema_constraints` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add strictness_rank to Tier and ResolvedTier enum with metadata</name>
  <files>dlp-common/src/label.rs, dlp-server/src/label_service.rs</files>
  <read_first>
    - dlp-common/src/label.rs (Tier enum definition, existing methods)
    - dlp-server/src/label_service.rs (current LabelCache, resolve_tier implementation)
  </read_first>
  <behavior>
    - Test: Tier::T4.strictness_rank() == 4, Tier::UnclassifiedBlocked.strictness_rank() == 5
    - Test: ResolvedTier::Exact(Tier::T3).tier() -> T3, is_inherited() -> false
    - Test: ResolvedTier::Inherited { tier: Tier::T4, parent_path: "C:\\Data".to_string() } has source metadata
    - Test: ResolvedTier::Fallback.tier() -> UnclassifiedBlocked
    - Test: ResolvedTier::LookupFailed.tier() -> UnclassifiedBlocked (fail-closed)
    - Test: CacheEntry { tier: Tier::T3, source: ResolutionSource::Exact, parent_path: None } round-trips through cache
  </behavior>
  <action>
    Modify `dlp-common/src/label.rs`:

    1. Add `strictness_rank(&self) -> u8` method to `Tier` impl:
       ```rust
       pub fn strictness_rank(&self) -> u8 {
           match self {
               Self::T1 => 1,
               Self::T2 => 2,
               Self::T3 => 3,
               Self::T4 => 4,
               Self::UnclassifiedBlocked => 5,
           }
       }
       ```
       Higher rank = stricter. Document: "Returns a numeric rank where higher values indicate stricter tiers. UnclassifiedBlocked (5) is the strictest."

    2. Add `is_stricter_than(&self, other: &Tier) -> bool` convenience method:
       ```rust
       pub fn is_stricter_than(&self, other: &Tier) -> bool {
           self.strictness_rank() > other.strictness_rank()
       }
       ```

    Modify `dlp-server/src/label_service.rs`:

    3. Add `ResolvedTier` enum (NOT in dlp-common — per D-19 and review concern #4):
       ```rust
       pub enum ResolvedTier {
           Exact(Tier),
           Inherited { tier: Tier, parent_path: String },
           Fallback,
           LookupFailed,
       }
       ```

    4. Add `ResolutionSource` enum for cache metadata:
       ```rust
       pub enum ResolutionSource {
           Exact,
           Inherited,
           Fallback,
           LookupFailed,
       }
       ```

    5. Add `CacheEntry` struct:
       ```rust
       pub struct CacheEntry {
           pub tier: Tier,
           pub source: ResolutionSource,
           pub parent_path: Option<String>,
           pub inserted: Instant,
       }
       ```

    6. Implement `ResolvedTier::tier(&self) -> Tier` returning the effective tier for all variants. For `Fallback` and `LookupFailed`, return `Tier::UnclassifiedBlocked`.

    7. Implement `ResolvedTier::source(&self) -> &'static str` for audit/logging:
       - Exact -> "exact"
       - Inherited -> "inherited"
       - Fallback -> "fallback"
       - LookupFailed -> "lookup_failed"

    8. Update `LabelCache` inner type from `HashMap<String, (Tier, Instant)>` to `HashMap<String, CacheEntry>`. Update `LabelCache::get` to return `Option<CacheEntry>`. Add `LabelCache::get_tier(&self, path: &str) -> Option<Tier>` convenience. Update `LabelCache::insert` to accept `CacheEntry`.

    Per review: "ResolvedTier import location" (HIGH) — defined in dlp-server, not dlp-common.
    Per review: "Cache stores only Tier" (MEDIUM) — now stores full metadata.
  </action>
  <verify>
    <automated>cargo test -p dlp-server label_service::tests::test_resolved_tier_enum && cargo test -p dlp-common label::tests::test_tier_strictness_rank</automated>
  </verify>
  <done>ResolvedTier enum compiles, CacheEntry stores source metadata, strictness_rank works, all existing tests still pass</done>
  <acceptance_criteria>
    - `Tier::strictness_rank()` returns 1..5 with UnclassifiedBlocked = 5
    - `Tier::is_stricter_than()` compares correctly
    - `ResolvedTier` enum exists with `Exact`, `Inherited`, `Fallback`, `LookupFailed` variants
    - `ResolvedTier::tier()` returns correct `Tier` for all variants
    - `ResolvedTier::source()` returns descriptive string for all variants
    - `LabelCache` stores `CacheEntry` (not just `Tier`) in its `HashMap`
    - `LabelCache::get` returns `Option<CacheEntry>`
    - `LabelCache::get_tier` convenience method exists
    - New unit tests verify all `ResolvedTier` variant behaviors and `Tier` strictness
    - `cargo test -p dlp-server label_service::` passes
    - `cargo test -p dlp-common label::` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix resolve_tier with strictness comparison</name>
  <files>dlp-server/src/label_service.rs</files>
  <read_first>
    - dlp-server/src/label_service.rs (current resolve_tier, LabelCache)
    - dlp-server/src/db/repositories/labels.rs (LabelRepository::get_by_path, find_parent_label)
    - dlp-common/src/label.rs (Tier enum, strictness_rank)
  </read_first>
  <behavior>
    - Test: Explicit T2 child under T4 folder -> resolves as Inherited T4 (stricter parent wins)
    - Test: Explicit T4 child under T2 folder -> resolves as Exact T4 (explicit is stricter, wins)
    - Test: Explicit T3 child under T3 folder -> resolves as Exact T3 (equal, explicit wins)
    - Test: No explicit label, T3 parent folder -> resolves as Inherited T3
    - Test: No labels at all -> resolves as Fallback (UnclassifiedBlocked)
    - Test: DB error -> resolves as LookupFailed (UnclassifiedBlocked)
    - Test: Cache hit returns Cached ResolvedTier with correct source
  </behavior>
  <action>
    Rewrite `LabelService::resolve_tier` to return `ResolvedTier` (not `rusqlite::Result<Tier>`) and implement strictness comparison per D-07b.

    New resolution logic:
    1. Check cache for `CacheEntry`. If hit and not expired, convert to `ResolvedTier` and return.
    2. Query DB for exact path match via `LabelRepository::get_by_path`. Query DB for parent folder label via `LabelRepository::find_parent_label` (always, not conditionally).
    3. If both exist:
       - Compare strictness using `Tier::strictness_rank()`.
       - If explicit_tier.is_stricter_than(&parent_tier) OR equal: return `ResolvedTier::Exact(explicit_tier)`
       - Else (parent is stricter): return `ResolvedTier::Inherited { tier: parent_tier, parent_path: parent.path }`
    4. If only exact exists: return `ResolvedTier::Exact(explicit_tier)`.
    5. If only parent exists: return `ResolvedTier::Inherited { tier: parent_tier, parent_path: parent.path }`.
    6. If neither: return `ResolvedTier::Fallback`.
    7. On DB error: return `ResolvedTier::LookupFailed` (do NOT propagate rusqlite::Error — fail closed).

    Update cache insertion to store `CacheEntry` with correct `source` and `parent_path`.

    Update `LabelService::invalidate_cache` — already exists, no change needed.

    Update ALL existing tests in label_service.rs to expect `ResolvedTier` instead of `Tier`.
    Add new tests for strictness scenarios:
    - `test_resolve_tier_explicit_lower_under_stricter_parent`: T2 child under T4 parent -> Inherited T4
    - `test_resolve_tier_explicit_stricter_under_lower_parent`: T4 child under T2 parent -> Exact T4
    - `test_resolve_tier_equal_strictness`: T3 child under T3 parent -> Exact T3

    Per review: "Folder inheritance strictness violation" (HIGH) — now compares strictness.
    Per review: "Cache stores only Tier" (MEDIUM) — cache now stores full metadata.
  </action>
  <verify>
    <automated>cargo test -p dlp-server label_service::</automated>
  </verify>
  <done>resolve_tier returns ResolvedTier, strictness comparison works, all 8+ tests pass including new strictness tests</done>
  <acceptance_criteria>
    - `resolve_tier` returns `ResolvedTier` (not `rusqlite::Result<Tier>`)
    - Explicit T2 child under T4 parent resolves as `Inherited { tier: T4, ... }` (stricter parent wins)
    - Explicit T4 child under T2 parent resolves as `Exact(T4)` (explicit stricter wins)
    - Equal strictness (T3 under T3) resolves as `Exact(T3)`
    - No labels returns `ResolvedTier::Fallback`
    - DB errors return `ResolvedTier::LookupFailed` (no panic, no propagation)
    - Cache stores `CacheEntry` with correct `ResolutionSource` and `parent_path`
    - All existing tests updated to expect `ResolvedTier`
    - New strictness comparison tests added and passing
    - `cargo test -p dlp-server label_service::` passes
    - `cargo clippy -p dlp-server -- -D warnings` clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> LabelService | Untrusted DB data (tier strings) parsed with fallback to UnclassifiedBlocked |
| LabelService -> PolicyStore | ResolvedTier consumed for classification override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | LabelCache | mitigate | TTL + explicit invalidation on CRUD; stale data bounded to 30s |
| T-59-02 | Information Disclosure | CacheEntry | accept | Cache stores only tier + source + parent_path — no PII beyond what labels table already holds |
| T-59-03 | Denial of Service | resolve_tier DB queries | mitigate | Two queries max (exact + parent); cache hit avoids DB entirely |
| T-59-04 | Elevation | Strictness comparison | mitigate | Explicit lower-tier child CANNOT weaken stricter parent folder (D-07b enforced) |
</threat_model>

<verification>
- `cargo test -p dlp-server label_service::` passes (all existing + new tests)
- `cargo test -p dlp-common label::` passes
- `cargo clippy -p dlp-server -- -D warnings` clean
- `cargo fmt --check` passes
</verification>

<success_criteria>
1. DB schema verified: labels table has CHECK constraints on object_type, tier, label_state and 6 indexes
2. `ResolvedTier` enum exists in `dlp-server/src/label_service.rs` with Exact, Inherited, Fallback, LookupFailed variants
3. `resolve_tier` returns `ResolvedTier` (not `Tier`) and implements strictness comparison
4. Explicit T2 child under T4 parent resolves as T4 (stricter parent wins)
5. Explicit T4 child under T2 parent resolves as T4 (explicit stricter wins)
6. Cache stores `CacheEntry` with `{tier, source, parent_path}` metadata
7. All existing tests updated and passing; new strictness tests added
</success_criteria>

<output>
Create `.planning/phases/59-label-service/59-01-SUMMARY.md` when done
</output>
