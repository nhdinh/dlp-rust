---
phase: 59-label-service
plan: 04
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - dlp-admin-cli/src/client.rs
  - dlp-admin-cli/src/screens/dispatch.rs
  - dlp-admin-cli/src/screens/render.rs
  - dlp-admin-cli/src/screens/labels.rs
  - dlp-admin-cli/src/app.rs
autonomous: true
requirements:
  - LABEL-04
  - LABEL-07
must_haves:
  truths:
    - "Admin TUI has LabelList screen with scrollable table, n/e/d/v/f/x actions"
    - "Admin TUI has LabelReviewQueue screen for temporary label confirm/reject"
    - "Label creation uses multi-step Screen::LabelForm (not InputPurpose) per D-13"
    - "Label edit pre-fills all steps with existing values"
    - "LabelList supports server-side pagination (limit/offset via PageUp/PageDown)"
    - "Expired state included in filter cycle (all -> temporary -> confirmed -> rejected -> expired)"
    - "Destructive actions (delete, expire) show confirmation with path and tier"
    - "HTTP errors display in status bar following existing error handling pattern"
    - "LabelDetail has no recursive enum issue (already fixed — no caller field needed)"
  artifacts:
    - path: "dlp-admin-cli/src/client.rs"
      provides: "expire_label client method, paginated list_labels"
    - path: "dlp-admin-cli/src/screens/dispatch.rs"
      provides: "handle_label_list with expire action, pagination key handlers"
    - path: "dlp-admin-cli/src/screens/render.rs"
      provides: "draw_label_list with pagination info"
    - path: "dlp-admin-cli/src/screens/labels.rs"
      provides: "Updated hint strings including expire and pagination"
    - path: "dlp-admin-cli/src/app.rs"
      provides: "Screen::LabelList with pagination fields, Screen::LabelReviewQueue with pagination fields"
  key_links:
    - from: "dispatch.rs label handlers"
      to: "client.rs label methods"
      via: "app.client.expire_label(id)"
    - from: "dispatch.rs handle_label_list"
      to: "Screen::LabelList"
      via: "PageUp/PageDown adjust page and reload"
---

<objective>
Fix admin TUI to address review concerns: (1) add expire action ('x' key) and client method, (2) add pagination support (PageUp/PageDown), (3) verify LabelDetail has no recursive enum issue. The TUI types (Screen variants, LabelForm, etc.) are already implemented — this plan focuses on gaps.

Purpose: The current TUI is missing the expire client method, has no pagination key handling, and the review flagged a recursive enum issue that is already fixed in the actual code. This plan closes the remaining gaps.
Output: Updated client.rs with expire_label and pagination; updated dispatch.rs with expire action and PageUp/PageDown; updated render.rs with pagination display; updated labels.rs hints; updated app.rs Screen variants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-label-service/59-CONTEXT.md
@.planning/phases/59-label-service/059-REVIEWS.md
@.planning/phases/59-label-service/59-UI-SPEC.md

## Current Code State

The existing TUI code in `dlp-admin-cli` has:
- `Screen::LabelList { labels, selected, filter }` — NO pagination fields (page, page_size, total)
- `Screen::LabelReviewQueue { labels, selected, department_filter, departments, department_index }` — NO pagination fields
- `Screen::LabelDetail { label: serde_json::Value }` — NO caller field (review HIGH concern #5 already fixed in actual code)
- `Screen::LabelForm { mode, step, path, object_type, tier, owner_sid, parent_label_id, existing_id }` — already implemented
- `LabelFilter` enum (All, Temporary, Confirmed, Rejected, Expired) with next() and label() — already implemented
- `LabelFormMode` enum (New, Edit) — already implemented
- Constants: OBJECT_TYPE_OPTIONS, TIER_OPTIONS — already implemented
- Client methods: list_labels, get_label, create_label, update_label, confirm_label, reject_label, delete_label, list_departments — already implemented
- **MISSING**: `expire_label(id)` client method (review HIGH concern #1)
- **MISSING**: Pagination in client `list_labels` (limit/offset params)
- **MISSING**: Pagination handling in dispatch (PageUp/PageDown)
- **MISSING**: Expire action in dispatch ('x' key)
- **MISSING**: Pagination display in render
- Render functions: draw_label_list, draw_label_review_queue, draw_label_detail, draw_label_form — already implemented
- Dispatch handlers: handle_label_list, handle_label_review_queue, handle_label_detail, handle_label_form — already implemented
- Action helpers: action_load_label_list, action_load_label_review_queue — already implemented

## Key Interfaces

From `dlp-admin-cli/src/client.rs` (existing):
```rust
pub async fn list_labels(&self, state_filter: Option<&str>, department_filter: Option<&str>) -> Result<Vec<serde_json::Value>>;
pub async fn confirm_label(&self, id: &str) -> Result<serde_json::Value>;
pub async fn reject_label(&self, id: &str) -> Result<serde_json::Value>;
pub async fn delete_label(&self, id: &str) -> Result<()>;
```

From `dlp-admin-cli/src/app.rs` (existing Screen variants):
```rust
LabelList { labels: Vec<serde_json::Value>, selected: usize, filter: LabelFilter },
LabelReviewQueue { labels: Vec<serde_json::Value>, selected: usize, department_filter: Option<String>, departments: Vec<String>, department_index: usize },
LabelDetail { label: serde_json::Value },  // NO caller field — already non-recursive
LabelForm { mode: LabelFormMode, step: u8, path: String, object_type: usize, tier: usize, owner_sid: String, parent_label_id: String, existing_id: Option<String> },
```

From `dlp-server/src/admin_api.rs` (from Plan 59-02):
```rust
GET /admin/labels?state=&limit=&offset= -> PaginatedLabelsResponse { labels, total, limit, offset }
POST /admin/labels/:id/expire -> LabelResponse
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add expire client method and pagination to list_labels</name>
  <files>dlp-admin-cli/src/client.rs</files>
  <read_first>
    - dlp-admin-cli/src/client.rs (existing list_labels, confirm_label, reject_label, delete_label signatures)
    - dlp-server/src/admin_api.rs (PaginatedLabelsResponse struct from Plan 59-02)
    - dlp-admin-cli/src/app.rs (Screen::LabelList, LabelFilter enum)
  </read_first>
  <behavior>
    - Test: `client.expire_label("id-123").await` sends POST /admin/labels/id-123/expire
    - Test: `client.list_labels(Some("temporary"), None, 50, 0).await` sends GET /admin/labels?state=temporary&limit=50&offset=0
    - Test: `client.list_labels(None, None, 50, 50).await` sends GET /admin/labels?limit=50&offset=50
    - Test: Response parsed as PaginatedLabelsResponse with labels, total, limit, offset fields
  </behavior>
  <action>
    Modify `dlp-admin-cli/src/client.rs`:

    1. Add `PaginatedLabelsResponse` type (if not already defined):
       ```rust
       #[derive(Debug, Clone, serde::Deserialize)]
       pub struct PaginatedLabelsResponse {
           pub labels: Vec<serde_json::Value>,
           pub total: i64,
           pub limit: usize,
           pub offset: usize,
       }
       ```

    2. Update `list_labels` signature to accept pagination params:
       ```rust
       pub async fn list_labels(
           &self,
           state_filter: Option<&str>,
           department_filter: Option<&str>,
           limit: usize,
           offset: usize,
       ) -> Result<PaginatedLabelsResponse>
       ```
       Build the query string with `limit` and `offset` as required params, then append optional `state` and `department` filters. Call `self.get(&path).await`.

       Note: Update all existing call sites of `list_labels` to pass `limit` and `offset` (e.g., `50, 0` for default). Search with `rg "list_labels\(" dlp-admin-cli/src/ --type rust`.

    3. Add `expire_label` method:
       ```rust
       pub async fn expire_label(&self, id: &str) -> Result<serde_json::Value> {
           self.post(&format!("admin/labels/{}/expire", id), &serde_json::json!({})).await
       }
       ```

    Per review HIGH: expire client method added for TUI consistency.
    Per review MEDIUM: Pagination support in client methods.
  </action>
  <verify>
    <automated>cargo check -p dlp-admin-cli</automated>
  </verify>
  <done>expire_label client method compiles; list_labels accepts limit/offset; PaginatedLabelsResponse type defined; all call sites updated; cargo check passes</done>
  <acceptance_criteria>
    - `PaginatedLabelsResponse` struct exists with `labels`, `total`, `limit`, `offset` fields
    - `list_labels` signature includes `limit: usize` and `offset: usize` parameters
    - `list_labels` builds query string with required limit/offset and optional state/department filters
    - `expire_label(id: &str)` method exists and sends POST to `admin/labels/{id}/expire`
    - All existing call sites of `list_labels` updated to pass limit/offset arguments
    - `cargo check -p dlp-admin-cli` passes with no errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add expire action and pagination to dispatch handlers</name>
  <files>dlp-admin-cli/src/screens/dispatch.rs, dlp-admin-cli/src/app.rs</files>
  <read_first>
    - dlp-admin-cli/src/app.rs (Screen::LabelList, Screen::LabelReviewQueue, Screen::Confirm, ConfirmPurpose enum definitions)
    - dlp-admin-cli/src/screens/dispatch.rs (handle_label_list, action_load_label_list, on_confirm_yes, handle_main_menu)
    - dlp-admin-cli/src/client.rs (list_labels signature, expire_label signature from Task 1)
  </read_first>
  <behavior>
    - Test: Pressing 'x' in LabelList shows confirm dialog for expire with path and tier
    - Test: Confirming expire calls client.expire_label(id) and reloads list
    - Test: Pressing PageUp decreases page (if > 0) and reloads list with new offset
    - Test: Pressing PageDown increases page (if more results) and reloads list
    - Test: LabelList state includes page, page_size, total fields
  </behavior>
  <action>
    Modify `dlp-admin-cli/src/app.rs`:

    1. Update `Screen::LabelList` variant to include pagination fields:
       Add `page: usize`, `page_size: usize`, `total: usize` to the existing variant.
       Update ALL existing constructions of `Screen::LabelList` to include these fields (default: page=0, page_size=50, total=0).

    2. Similarly update `Screen::LabelReviewQueue`:
       Add `page: usize`, `page_size: usize`, `total: usize`.
       Update all constructions.

    3. Add `ExpireLabel` variant to `ConfirmPurpose` enum (if not already present):
       ```rust
       ExpireLabel { id: String, path: String, tier: String }
       ```

    Modify `dlp-admin-cli/src/screens/dispatch.rs`:

    4. In `handle_label_list`, add key handling:
       - 'x': Show confirm dialog for expire. Extract id, path, tier from the selected label. Transition to `Screen::Confirm` with `ConfirmPurpose::ExpireLabel { id, path, tier }`.
       - PageUp: If page > 0, decrement page and call `action_load_label_list_paginated(app, filter, new_page, page_size)`.
       - PageDown: If (page + 1) * page_size < total, increment page and call `action_load_label_list_paginated(app, filter, new_page, page_size)`.

    5. Update `action_load_label_list` to use pagination:
       Rename to `action_load_label_list_paginated(app, filter, page, page_size)`. Calculate `offset = page * page_size`. Call `client.list_labels(state_filter, None, page_size, offset)`. On success, return `ActionResult::LabelListLoaded { labels, filter, page, page_size, total }` (add this variant if needed).

    6. Update `on_confirm_yes` to handle `ConfirmPurpose::ExpireLabel`:
       Call `client.expire_label(&id).await`. On success, set status message. Then reload the list via `action_load_label_list_paginated(app, LabelFilter::All, 0, DEFAULT_LABEL_PAGE_SIZE)`.

    7. Update `handle_main_menu` to pass pagination defaults when navigating to LabelList:
       `Screen::LabelList { labels: vec![], selected: 0, filter: LabelFilter::All, page: 0, page_size: DEFAULT_LABEL_PAGE_SIZE, total: 0 }`

    Per D-12: LabelList supports n/e/d/v/f/x actions. Expire ('x') now implemented.
    Per review: Pagination with PageUp/PageDown keys.
    Per review: Confirm dialog shows path and tier for destructive actions.
  </action>
  <verify>
    <automated>cargo check -p dlp-admin-cli</automated>
  </verify>
  <done>Expire action ('x') handled in LabelList with confirmation dialog; PageUp/PageDown adjust page and reload; pagination state tracked in Screen::LabelList; all constructions updated; cargo check passes</done>
  <acceptance_criteria>
    - `Screen::LabelList` includes `page`, `page_size`, `total` fields
    - `Screen::LabelReviewQueue` includes `page`, `page_size`, `total` fields
    - `ConfirmPurpose` has `ExpireLabel { id, path, tier }` variant
    - All existing `Screen::LabelList` constructions compile with new fields
    - All existing `Screen::LabelReviewQueue` constructions compile with new fields
    - `handle_label_list` handles 'x' key by showing confirm dialog with path and tier
    - `handle_label_list` handles PageUp/PageDown by adjusting page and reloading
    - `action_load_label_list_paginated` calls client with correct limit/offset
    - `on_confirm_yes` handles `ExpireLabel` by calling `client.expire_label` then reloading
    - `handle_main_menu` initializes LabelList with default pagination values
    - `cargo check -p dlp-admin-cli` passes with no errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update render to show pagination info and verify no recursive enum issue</name>
  <files>dlp-admin-cli/src/screens/render.rs, dlp-admin-cli/src/screens/labels.rs</files>
  <read_first>
    - dlp-admin-cli/src/screens/render.rs (draw_label_list, draw_label_review_queue functions)
    - dlp-admin-cli/src/screens/labels.rs (LABEL_LIST_HINTS constant)
    - dlp-admin-cli/src/app.rs (Screen::LabelDetail definition — verify no caller field)
  </read_first>
  <behavior>
    - Test: draw_label_list shows "Page N of M | K per page" in footer
    - Test: draw_label_review_queue shows pagination info
    - Test: LABEL_LIST_HINTS includes '[x] Expire' and '[PgUp/PgDn] Page'
    - Test: LabelDetail variant does NOT contain a Screen field (no recursive enum)
  </behavior>
  <action>
    1. Update `dlp-admin-cli/src/screens/labels.rs`:
       Update `LABEL_LIST_HINTS` to include "[x] Expire" and "[PgUp/PgDn] Page".
       Update tests to verify the new hints contain "[x] Expire" and "[PgUp/PgDn] Page".

    2. Update `dlp-admin-cli/src/screens/render.rs`:
       - In `draw_label_list`, add pagination display in the footer. Calculate `total_pages = if total == 0 { 1 } else { (total + page_size - 1) / page_size }`. Render text like "Page {page + 1} of {total_pages} | {page_size} per page" in the bottom-right or as part of the hints line.
       - In `draw_label_review_queue`, similarly add pagination display.

    3. Verify `LabelDetail` has no recursive enum issue:
       - Check `dlp-admin-cli/src/app.rs` for `Screen::LabelDetail` definition
       - Confirm it is `LabelDetail { label: serde_json::Value }` with NO `caller: Screen` field
       - If a `caller` field exists, it must use `Box<Screen>` per D-13b
       - The actual code already has `LabelDetail { label: serde_json::Value }` (no caller), so this is already fixed
       - Add a compile-time assertion test: `const _: () = assert!(std::mem::size_of::<Screen>() > 0);` (trivial, just to verify compilation)

    Per review HIGH: "Recursive enum variant" — verified fixed (no caller field in LabelDetail).
    Per review MEDIUM: "Form flow contradiction" — resolved: Screen::LabelForm is the implementation, InputPurpose variants are marked #[allow(dead_code)] and not used.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli</automated>
  </verify>
  <done>Render shows pagination info; hints include expire and page keys; LabelDetail verified non-recursive; all tests pass</done>
  <acceptance_criteria>
    - `LABEL_LIST_HINTS` contains "[x] Expire" substring
    - `LABEL_LIST_HINTS` contains "[PgUp/PgDn] Page" substring
    - `draw_label_list` renders pagination text showing current page, total pages, and page size
    - `draw_label_review_queue` renders pagination text
    - `Screen::LabelDetail` is `LabelDetail { label: serde_json::Value }` with no `Screen`-typed field
    - Compile-time size assertion test exists and passes
    - `cargo test -p dlp-admin-cli` passes
    - `cargo clippy --all-targets -- -D warnings` passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI->API | Admin input crosses to server API |
| API->TUI | Server error responses displayed to admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-27 | Repudiation | Label delete | mitigate | Confirm screen shows path + tier; requires explicit 'y' before delete |
| T-59-28 | Repudiation | Label expire | mitigate | Confirm screen shows path + tier; requires explicit 'y' before expire |
| T-59-29 | Information Disclosure | Label detail | accept | Detail screen shows same fields as API returns; no additional exposure |
| T-59-30 | Denial of Service | Filter cycling | accept | Filter is client-side only; no server impact |
| T-59-31 | Information Disclosure | Pagination | accept | Page size capped at server (1000 max); client requests 50 per page |
</threat_model>

<verification>
- `cargo test -p dlp-admin-cli` passes
- `cargo clippy --all-targets -- -D warnings` passes
- `cargo build --workspace` succeeds
</verification>

<success_criteria>
- MainMenu has "Label Management" item that navigates to LabelList (already implemented)
- SystemMenu has "Label Review Queue" item that navigates to LabelReviewQueue (already implemented)
- LabelList shows scrollable table with Path, Type, Tier, State, Owner columns (already implemented)
- LabelList supports n (new), e (edit), d (delete), v (view), f (filter), x (expire), Esc (back)
- LabelList supports PageUp/PageDown for server-side pagination
- LabelReviewQueue shows temporary labels with c (confirm), r (reject), Esc (back)
- Label creation uses 6-step Screen::LabelForm (already implemented)
- Label edit pre-fills all 6 steps with existing values (already implemented)
- Delete confirmation shows label path and tier (already implemented)
- Expire confirmation shows label path and tier
- All HTTP errors display in status bar (already implemented)
- Filter cycles through all 5 states including Expired (already implemented)
- Client methods support pagination (limit/offset)
- LabelDetail has no recursive enum issue (verified)
</success_criteria>

<output>
Create `.planning/phases/59-label-service/59-04-SUMMARY.md` when done
</output>
