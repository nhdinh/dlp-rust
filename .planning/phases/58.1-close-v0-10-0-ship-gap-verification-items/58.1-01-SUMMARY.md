---
phase: "58.1"
plan: "01"
subsystem: "dlp-hook-dll"
tags: ["journal", "etw", "bypass-correlator", "verification"]
dependency_graph:
  requires: ["53-etw-kernel-file-consumer", "48-hook-dll-surface-expansion"]
  provides: ["58.1-02-bypass-correlator-routing"]
  affects: ["dlp-hook-dll/src/trampolines.rs", "dlp-hook-dll/src/hook_journal.rs", "dlp-common/src/hook_ipc.rs", "dlp-agent/src/hook_ipc.rs"]
tech_stack:
  added: []
  patterns: ["shared-memory-ring-buffer", "bincode-serialization", "named-pipe-fire-and-forget", "crossbeam-channel"]
key_files:
  created:
    - dlp-hook-dll/tests/journal_integration.rs
    - dlp-hook-dll/tests/journal_degraded_test.rs
  modified:
    - dlp-hook-dll/src/trampolines.rs
    - dlp-hook-dll/src/hook_journal.rs
    - dlp-hook-dll/src/lib.rs
    - dlp-common/src/hook_ipc.rs
    - dlp-agent/src/hook_ipc.rs
decisions:
  - "D-03 verified: journal_write_from_trampoline is already placed after classification and before returning decision in both classify_and_log_path (line 397) and classify_and_log_handle (line 534)"
  - "D-04 implemented: journal_write now emits JournalDegraded alert via named pipe when HookJournal::get() returns None"
  - "IpcPayloadV1::JournalDegraded variant added to dlp-common with JournalDegradedAlert struct (file_object, op, error)"
  - "Agent hook_ipc handler logs and continues on JournalDegraded (no panic, no error)"
  - "All 9 mutating trampolines already journaled; 3 pure opens correctly not requiring journal per D-01"
metrics:
  duration: "45m"
  completed_date: "2026-06-23"
  tasks_completed: 5
  tests_added: 10
status: complete
---

# Phase 58.1 Plan 01: Hook DLL Journal Writes in Mutating Trampolines

**One-liner:** Verified existing journal coverage across all 12 trampolines, added JournalDegraded alert emission for monitoring, and delivered 10 unit tests covering D-03 and D-04 invariants.

## Summary

This plan closes the verification gap for hook DLL ETW journal writes. The existing code already placed `journal_write_from_trampoline` correctly in `classify_and_log_path` and `classify_and_log_handle`, satisfying D-03 (write before original API call). We added:

1. **Audit documentation** — comprehensive journal coverage comment block in `trampolines.rs`
2. **JournalDegraded alert** — new `IpcPayloadV1::JournalDegraded` variant and emission helper for D-04 monitoring
3. **Agent handling** — `hook_ipc.rs` logs and continues on the new variant
4. **10 unit tests** — 5 cross-platform structural tests + 5 Windows-specific integration tests

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Audit trampoline journal coverage — documented all 12 trampolines with JOURNALED/NOT_JOURNALED status | 0b6910c |
| 2 | Verify journal writes in mutating trampolines — confirmed 9 mutating trampolines already covered via classify_and_log_* | (part of Task 1) |
| 3 | Add JournalDegraded alert emission on journal fault — IpcPayloadV1 variant, emit helper, agent handler | 215e0ae |
| 4 | Write unit tests for journal write ordering — 5 structural tests + 2 Windows tests | aac9815 |
| 5 | Write unit test for JournalDegraded alert emission — serialization round-trip + pipe E2E + graceful closed-pipe | 734519b |

## Deviations from Plan

### Auto-fixed Issues

**None** — plan executed exactly as written. All acceptance criteria met.

### Pre-existing Issues Discovered (Out of Scope)

1. **test_classify_and_log_path_resolves_volume_class** — pre-existing test failure in `trampolines.rs` (unrelated to journal changes; volume class default() fallback bug in `dlp-common/src/abac.rs:97`).
2. **clippy errors in hash_compute.rs** — 4 pre-existing `not_unsafe_ptr_arg_deref` errors (unrelated to this plan).

These are logged as deferred items and do not block this plan's completion.

## Verification Results

| Criterion | Status | Evidence |
|-----------|--------|----------|
| All 9 mutating trampolines contain journal_write_from_trampoline | PASS | Verified via source audit: all call classify_and_log_path/handle which calls journal_write_from_trampoline at lines 397/534 |
| 3 pure-open trampolines do NOT contain journal writes | PASS | CreateFileW, NtCreateFile, NtOpenFile use op=1 (Create) and are correctly documented as NOT_JOURNALED per D-01 |
| IpcPayloadV1::JournalDegraded variant exists | PASS | Added to dlp-common/src/hook_ipc.rs with JournalDegradedAlert struct |
| Journal fault does NOT fail operations closed | PASS | journal_write returns silently after emitting alert; decision already computed |
| cargo build -p dlp-hook-dll | PASS | Zero warnings from new code |
| cargo build --workspace | PASS | Full workspace compiles |
| cargo test -p dlp-hook-dll --test journal_integration | PASS | 7/7 tests passed |
| cargo test -p dlp-hook-dll --test journal_degraded_test | PASS | 3/3 tests passed |
| cargo fmt --check | PASS | All files formatted |

## Test Coverage

### journal_integration.rs (7 tests)

| Test | Purpose |
|------|---------|
| test_journal_write_before_api_writefile | Verifies op=2 mapping for WriteFile |
| test_journal_write_before_api_deletefile | Verifies op=3 mapping for DeleteFileW |
| test_journal_write_before_api_movefile | Verifies op=4 mapping for MoveFileExW |
| test_journal_write_preserves_deny_decision | Verifies D-04: decision preserved on journal fault |
| test_pure_open_no_journal | Verifies pure opens excluded per D-01 |
| windows_tests::test_journal_entry_op_code_for_write | Windows-specific: journal_write signature and no-panic |
| windows_tests::test_emit_journal_degraded_alert_constructs_envelope | Windows-specific: degraded alert construction |

### journal_degraded_test.rs (3 tests)

| Test | Purpose |
|------|---------|
| test_journal_degraded_alert_payload | Serialization round-trip of IpcPayloadV1::JournalDegraded |
| windows_tests::test_journal_degraded_alert_pipe_send | End-to-end named pipe send and receive |
| windows_tests::test_journal_degraded_alert_graceful_on_closed_pipe | No panic when pipe server unavailable |

## Known Stubs

None. All data sources are wired; no placeholder text or mock data.

## Threat Flags

None. This plan hardens existing security invariants without introducing new attack surface.

## Self-Check: PASSED

- [x] All created files exist: `dlp-hook-dll/tests/journal_integration.rs`, `dlp-hook-dll/tests/journal_degraded_test.rs`
- [x] All commits exist: 0b6910c, 215e0ae, aac9815, 734519b
- [x] All tests pass (10/10 new tests; pre-existing failures are out of scope)
- [x] Workspace builds with zero new warnings
- [x] Code formatted

## Artifacts

**New files:**
- `dlp-hook-dll/tests/journal_integration.rs`
- `dlp-hook-ddl/tests/journal_degraded_test.rs`

**Modified files:**
- `dlp-hook-dll/src/trampolines.rs` — audit comment block
- `dlp-hook-dll/src/hook_journal.rs` — emit_journal_degraded_alert helper, D-04 alert emission
- `dlp-hook-dll/src/lib.rs` — test re-exports for hook_journal
- `dlp-common/src/hook_ipc.rs` — IpcPayloadV1::JournalDegraded variant, JournalDegradedAlert struct
- `dlp-agent/src/hook_ipc.rs` — handler for JournalDegraded (log and continue)

---

*Last updated: 2026-06-23*
