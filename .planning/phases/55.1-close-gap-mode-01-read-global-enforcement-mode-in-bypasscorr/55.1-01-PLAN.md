---
phase: 55.1-close-gap-mode-01-read-global-enforcement-mode-in-bypasscorr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/bypass_correlator.rs
  - dlp-agent/src/service.rs
autonomous: true
requirements:
  - MODE-01
  - ETW-01
  - ETW-02
must_haves:
  truths:
    - "CorrelatorConfig carries enforcement_mode field with Block as default"
    - "BypassCorrelator stores enforcement_mode from config at construction time"
    - "service.rs passes agent_config.enforcement.global_mode into CorrelatorConfig"
    - "CorrelatorConfig default backward compatibility is verified"
  artifacts:
    - path: "dlp-agent/src/bypass_correlator.rs"
      provides: "CorrelatorConfig with enforcement_mode field + Default impl"
      contains: "pub enforcement_mode: EnforcementMode"
    - path: "dlp-agent/src/service.rs"
      provides: "global_mode wired into correlator construction"
      contains: "enforcement_mode: agent_config.enforcement.global_mode"
  key_links:
    - from: "dlp-agent/src/service.rs"
      to: "dlp-agent/src/bypass_correlator.rs"
      via: "CorrelatorConfig constructor at line ~1615"
      pattern: "enforcement_mode:.*global_mode"
---

<objective>
Add `enforcement_mode: EnforcementMode` to `CorrelatorConfig` and `BypassCorrelator`, then wire `agent_config.enforcement.global_mode` through `service.rs` at correlator construction time. This is the structural prerequisite for Audit-mode suppression logic.

Purpose: Without this wiring, the correlator cannot know the global enforcement mode and will continue emitting false-positive bypass alerts in Audit mode (MODE-01 gap per v0.10.0 milestone audit INT-WARN-02).
Output: `CorrelatorConfig` and `BypassCorrelator` carry the mode; `service.rs` passes it from `AgentConfig`.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v0.10.0-MILESTONE-AUDIT.md
@.planning/phases/55.1-close-gap-mode-01-read-global-enforcement-mode-in-bypasscorr/55.1-RESEARCH.md
@.planning/phases/55.1-close-gap-mode-01-read-global-enforcement-mode-in-bypasscorr/55.1-VALIDATION.md
@.planning/phases/55-monitor-only-audit-only-per-policy-enforcement-mode/55-05-SUMMARY.md

## Source-of-truth files
- `dlp-agent/src/bypass_correlator.rs` — CorrelatorConfig (lines 73-109), BypassCorrelator::new (lines 342-367)
- `dlp-agent/src/service.rs` — Correlator construction (lines 1614-1621), apply_payload_to_config global mode (lines 725-749)
- `dlp-agent/src/config.rs` — EnforcementConfig (lines 92-110), AgentConfig.enforcement (line 325)
- `dlp-common/src/abac.rs` — EnforcementMode enum (lines 410-435), is_audit() (lines 433-435)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CorrelatorConfig with enforcement_mode field</name>
  <files>dlp-agent/src/bypass_correlator.rs</files>
  <read_first>
    dlp-agent/src/bypass_correlator.rs (CorrelatorConfig struct lines 73-109, Default impl lines 95-109)
    dlp-common/src/abac.rs (EnforcementMode enum lines 410-435)
  </read_first>
  <action>
    Add `pub enforcement_mode: EnforcementMode` to `CorrelatorConfig` in `dlp-agent/src/bypass_correlator.rs`. Import `dlp_common::abac::EnforcementMode` at the top of the file (or use fully-qualified path). Update the `Default` impl to set `enforcement_mode: EnforcementMode::Block` for backward compatibility. Add a doc comment: "Phase 55.1: Global enforcement mode to suppress bypass alerts in Audit mode." Verify the struct still derives `Debug, Clone, Copy` — EnforcementMode is Copy so this compiles. Add a sync unit test `test_correlator_config_default_mode_is_block` to the `#[cfg(test)] mod tests` block that asserts `CorrelatorConfig::default().enforcement_mode == EnforcementMode::Block`.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent test_correlator_config_default_mode_is_block</automated>
  </verify>
  <done>CorrelatorConfig has enforcement_mode field; Default sets Block; default-mode test passes; compiles with no warnings.</done>
  <acceptance_criteria>
    - `CorrelatorConfig` contains `pub enforcement_mode: EnforcementMode`
    - `Default for CorrelatorConfig` sets `enforcement_mode: EnforcementMode::Block`
    - `test_correlator_config_default_mode_is_block` is added to the test module and passes
    - `cargo test -p dlp-agent test_correlator_config_default_mode_is_block` passes
    - `cargo clippy -p dlp-agent -- -D warnings` clean
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire global_mode into CorrelatorConfig in service.rs</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    dlp-agent/src/service.rs (correlator construction lines 1614-1621)
    dlp-agent/src/config.rs (AgentConfig.enforcement field line 325, EnforcementConfig lines 92-110)
    dlp-agent/src/bypass_correlator.rs (CorrelatorConfig after Task 1 changes)
  </read_first>
  <action>
    In `dlp-agent/src/service.rs` at the correlator construction site (around line 1615), add `enforcement_mode: agent_config.enforcement.global_mode` to the `CorrelatorConfig` struct literal. The existing code is:
    ```
    let correlator = crate::bypass_correlator::BypassCorrelator::new(
        crate::bypass_correlator::CorrelatorConfig {
            reduced_mode,
            ..Default::default()
        },
    )
    ```
    Change to explicitly pass `enforcement_mode: agent_config.enforcement.global_mode` before `..Default::default()`. This ensures the correlator receives the live global mode from the agent config (which is populated by server policy_sync per Phase 55 D-02).
  </action>
  <verify>
    <automated>cargo test -p dlp-agent service</automated>
  </verify>
  <done>service.rs passes `agent_config.enforcement.global_mode` into `CorrelatorConfig`; compiles and existing service tests pass.</done>
  <acceptance_criteria>
    - `service.rs` CorrelatorConfig construction includes `enforcement_mode: agent_config.enforcement.global_mode`
    - `cargo test -p dlp-agent service` passes (existing tests)
    - `cargo clippy -p dlp-agent -- -D warnings` clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-config.toml -> service.rs | Config file is local-trusted; tampering requires admin access |
| service.rs -> bypass_correlator.rs | Internal agent module boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55.1-01 | Tampering | Attacker modifies agent-config.toml to set Audit mode | accept | Agent config file is in ProgramData and requires admin write access; this is the same threat model as all other config fields |
| T-55.1-02 | Information Disclosure | Audit-mode suppression hides real bypasses from SIEM | mitigate | Only Audit mode suppresses; Block and AuditAndBlock continue emitting alerts. The suppression is intentional per operator choice (monitoring mode). |
| T-55.1-03 | Denial of Service | Default Block mode means existing deployments without explicit config continue blocking | mitigate | Default is Block (backward compatible); only explicit Audit mode changes behavior |
</threat_model>

<verification>
- `cargo test -p dlp-agent test_correlator_config_default_mode_is_block` passes
- `cargo test -p dlp-agent service` passes
- `cargo clippy -p dlp-agent -- -D warnings` clean
- `cargo build -p dlp-agent` succeeds
</verification>

<success_criteria>
- CorrelatorConfig carries `enforcement_mode: EnforcementMode` with Block default
- BypassCorrelator stores the mode from config at construction
- service.rs passes `agent_config.enforcement.global_mode` into CorrelatorConfig
- All dlp-agent tests pass; clippy clean
</success_criteria>

<output>
Create `.planning/phases/55.1-close-gap-mode-01-read-global-enforcement-mode-in-bypasscorr/55.1-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New symbols and fields
- `CorrelatorConfig.enforcement_mode: EnforcementMode` — new field in `dlp-agent/src/bypass_correlator.rs`
- `CorrelatorConfig::default()` updated to set `enforcement_mode: EnforcementMode::Block`
- `service.rs` CorrelatorConfig construction now passes `enforcement_mode: agent_config.enforcement.global_mode`

### New tests
- `test_correlator_config_default_mode_is_block` — sync test verifying Default impl backward compatibility

### Modified files
- `dlp-agent/src/bypass_correlator.rs` — added field + Default impl + import + unit test
- `dlp-agent/src/service.rs` — wired global_mode into correlator construction
