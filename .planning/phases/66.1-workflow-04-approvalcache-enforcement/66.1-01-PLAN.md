---
phase: 66.1-workflow-04-approvalcache-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/abac.rs
  - dlp-common/src/audit.rs
autonomous: true
requirements:
  - WORKFLOW-04

must_haves:
  truths:
    - "EvaluateResponse carries matched_label_id for label-aware ABAC evaluations"
    - "EventType::ApprovalOverride routes to SIEM and does not trigger alerts"
    - "Old JSON EvaluateResponse without matched_label_id deserializes correctly"
  artifacts:
    - path: "dlp-common/src/abac.rs"
      provides: "EvaluateResponse with matched_label_id field"
      contains: "matched_label_id: Option<String>"
    - path: "dlp-common/src/audit.rs"
      provides: "EventType::ApprovalOverride variant"
      contains: "ApprovalOverride"
  key_links:
    - from: "dlp-common/src/abac.rs"
      to: "dlp-server/src/policy_store.rs"
      via: "EvaluateResponse returned from evaluate()"
      pattern: "matched_label_id"
    - from: "dlp-common/src/audit.rs"
      to: "dlp-agent/src/audit_emitter.rs"
      via: "EventType routed to SIEM"
      pattern: "routed_to_siem"
---

<objective>
Add shared types required for ApprovalCache enforcement integration.

Purpose: Provide the type-level contracts that downstream plans (server-side, agent core, enforcement paths) will implement against.
Output: `EvaluateResponse` with `matched_label_id`, `EventType::ApprovalOverride`, and backward-compat tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66.1-workflow-04-approvalcache-enforcement/66.1-CONTEXT.md
@.planning/phases/66.1-workflow-04-approvalcache-enforcement/66.1-RESEARCH.md

## Key interfaces from codebase

From `dlp-common/src/abac.rs` (lines 440-478):
```rust
pub struct EvaluateResponse {
    pub decision: Decision,
    pub matched_policy_id: Option<String>,
    pub reason: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub enforcement_mode: Option<EnforcementMode>,
    #[serde(default)]
    pub would_have_denied: bool,
}
```

From `dlp-common/src/audit.rs` (lines 162-286):
```rust
pub enum EventType {
    Access, Block, Alert, ConfigChange, SessionLogoff, AdminAction,
    ServiceStopFailed, DiskDiscovery, DiskMountBlocked, DiskQuarantineStarted,
    DiskQuarantineExpired, ApprovalRequest, ApprovalGrant, ApprovalRevoke,
    ApprovalUse, ApprovalExpiry, ApprovalBoardKeyUpdate, NtdllPatchingEnabled,
    // ... more variants
}
```

`routed_to_siem()` at line 236 — `ApprovalOverride` must be added.
`triggers_alert()` at line 274 — `ApprovalOverride` must return `false`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matched_label_id to EvaluateResponse</name>
  <files>dlp-common/src/abac.rs</files>
  <action>
    Add `matched_label_id: Option<String>` to `EvaluateResponse` per D-01.1 and D-01.2.
    - Place the new field AFTER `would_have_denied` (last field, preserves field order for JSON consumers).
    - Annotate with `#[serde(default, skip_serializing_if = "Option::is_none")]` for backward compat.
    - Update `default_deny()` and `default_allow()` constructors to set `matched_label_id: None`.
    - Add a unit test `test_evaluate_response_backward_compat` that deserializes old JSON without `matched_label_id` and asserts the field is `None`.
    - Add a unit test `test_evaluate_response_with_matched_label_id` that serializes/deserializes a response with the field set and asserts round-trip correctness.
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib abac -- test_evaluate_response_backward_compat test_evaluate_response_with_matched_label_id -x</automated>
  </verify>
  <done>EvaluateResponse has matched_label_id field, constructors updated, backward-compat tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add EventType::ApprovalOverride</name>
  <files>dlp-common/src/audit.rs</files>
  <action>
    Add `ApprovalOverride` variant to `EventType` enum per D-03.1.
    - Insert after `ApprovalBoardKeyUpdate` (maintain semantic grouping).
    - Update `routed_to_siem()` to include `Self::ApprovalOverride` (returns `true`).
    - Update `triggers_alert()` to include `Self::ApprovalOverride` returning `false` per D-03.3.
    - Add unit tests:
      - `test_approval_override_routed_to_siem` — assert `ApprovalOverride.routed_to_siem() == true`
      - `test_approval_override_does_not_trigger_alert` — assert `ApprovalOverride.triggers_alert() == false`
      - `test_approval_override_serde_round_trip` — serialize/deserialize the variant
  </action>
  <verify>
    <automated>cargo test -p dlp-common --lib audit -- test_approval_override_routed_to_siem test_approval_override_does_not_trigger_alert test_approval_override_serde_round_trip -x</automated>
  </verify>
  <done>EventType::ApprovalOverride exists, SIEM routing and alert suppression correct, tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dlp-common -> dlp-server | EvaluateResponse deserialized from server JSON; backward compat prevents deserialization failures on old cached responses |
| dlp-common -> dlp-agent | EventType serialized to SIEM relay; new variant must not break existing event routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66.1-01 | Tampering | EvaluateResponse JSON cache | mitigate | `#[serde(default)]` on matched_label_id ensures old cached responses deserialize safely |
| T-66.1-02 | Repudiation | ApprovalOverride audit event | mitigate | routed_to_siem=true ensures SIEM receives override events; triggers_alert=false prevents false-positive alert storms |
</threat_model>

<verification>
- `cargo test -p dlp-common --lib` passes (all existing + new tests)
- `cargo clippy -p dlp-common -- -D warnings` clean
- `cargo fmt --check` clean
</verification>

<success_criteria>
- EvaluateResponse carries `matched_label_id: Option<String>` with backward-compatible serde
- EventType::ApprovalOverride routes to SIEM and does not trigger alerts
- All dlp-common tests pass, clippy clean
</success_criteria>

<output>
Create `.planning/phases/66.1-workflow-04-approvalcache-enforcement/66.1-01-SUMMARY.md` when done
</output>
