# Phase 58.2 Replan Notes

**Date:** 2026-06-24
**Trigger:** Cross-AI plan review (`/gsd-plan-review-convergence 58.2 --codex`)
**Reviewer:** Claude Code

---

## Summary

The Phase 58.2 implementation is already complete in source code. The three plan files (58.2-01, 58.2-02, 58.2-03) contained stale line numbers, flawed verification commands, and references to non-existent tests. This replan updates the plans to match the current codebase reality.

---

## Findings Addressed

### HIGH

| ID | Plan | Finding | Resolution |
|----|------|---------|----------|
| H-01 | 02 | Verification grep `grep -c "S-1-5-18" dlp-agent/src/service.rs` is flawed because the non-Windows stub returns `S-1-5-18-test` and a legitimate `S-1-5-18` exists in the audit context at line ~3364. | Updated verification to: (1) `grep -c "S-1-5-18"` returns 1 (only the test stub), (2) `grep -n "S-1-5-18"` confirms the only match is inside `get_caller_sid` (not in the handler closure or audit context). Added note in Task 2 step 4 clarifying the audit context default SID is intentional and unrelated to handler identity resolution. |
| H-02 | 03 | Plan 03 Task 2 references `test_consolidated_server_routes_all_frames` but the test does not exist. | Added explicit gap acknowledgment in Task 2 step 1. Added new Task 4 to create `test_consolidated_server_routes_all_frames` as a follow-up. Updated `must_haves` artifacts to reflect existing tests (`test_hook_ipc_volume_class_matches_deny`) rather than the non-existent one. |
| H-03 | 03 | Plan 03 Task 1 describes unit tests that do not exist in `dlp-agent/src/service.rs`. | Added explicit gap acknowledgment in Task 1 step 1. The plan now states these tests must be added as part of the task. Updated `must_haves` to reflect that the tests are to be added, not that they already exist. |

### MEDIUM

| ID | Plan | Finding | Resolution |
|----|------|---------|----------|
| M-01 | 01 | Plan 01 Task 2 step 4 says "Verify that the old `spawn_hook_ipc_server` call site (outside `run_loop_init`) is removed or unified" but there is no such old call site. | Rewrote step 4 to clarify: "Verify that there is exactly one call to `spawn_hook_ipc_server` in all of `service.rs` — it lives inside `run_loop_init`. The inline block was the duplicate; there was never a second `spawn_hook_ipc_server` call site outside `run_loop_init`." |
| M-02 | 02 | Plan 02 Task 1 step 5 says "Add `warn!(...)` logging" but the `warn!` is actually placed inside the handler closure (Task 2), not in the conversion helper. | Clarified in Task 1 step 4: "Note: the `warn!` is emitted inside the handler closure (Task 2), not in the conversion helper, because the helper is pure and does not own the `req` action string at decision time. Ensure Task 2 step 3 verifies the warning fires." Also updated Task 2 step 3 to explicitly state the warning is part of the handler closure. |
| M-03 | 03 | Plan 03 Task 2 step 4 says "Verify that only one server thread exists by checking that a second `CreateNamedPipeW` on the same test pipe name fails with ERROR_PIPE_BUSY". This is fragile. | Replaced with a robust assertion: "Verify that only one server thread exists by asserting that `spawn_hook_ipc_server` returns `Some(JoinHandle)` and that the handle's thread name is `"hook-ipc-server"`. Do not rely on `ERROR_PIPE_BUSY` from `CreateNamedPipeW` — a second `CreateNamedPipeW` on the same name from the same process typically succeeds with a new handle, so that check is not a valid single-threadedness proof." |
| M-04 | 03 | Integration tests bypass the consolidated builder chain by using `start_mock_server` with raw handlers. | Added explicit acknowledgment in Task 2 step 1 and new Task 4 to create a test that exercises the real `spawn_hook_ipc_server` builder chain. Noted that `test_spawn_hook_ipc_server_starts_named_thread` in `service.rs` provides indirect unit-test coverage. |
| M-05 | 03 | Plan 03 verification does not mention `sonar-scanner` per CLAUDE.md 9.16. | Added `sonar-scanner` to the verification checklist with a note: "per CLAUDE.md 9.16 — required before committing; if the Quality Gate fails due to low coverage from missing unit tests, treat as blocking and add the tests from Task 1." |
| M-06 | 02 | Plan 02 Task 2 step 4 says "Ensure no hardcoded 'S-1-5-18' SYSTEM SID remains in the handler". The audit context at line ~3364 uses `user_sid: "S-1-5-18".to_string()` as a default. | Added note in Task 2 step 4: "Note: the audit context initialization at line ~3364 uses `user_sid: "S-1-5-18".to_string()` as a default before per-event override. This is intentional and unrelated to handler identity resolution — do not flag or remove it." Also updated the verification grep to exclude the audit context line. |

### LOW

| ID | Plan | Finding | Resolution |
|----|------|---------|----------|
| L-01 | 01 | Verification includes `grep -c "BlockingThreads"` which is redundant because the struct still exists (just without `hook_ipc`). | Changed to `grep -n "hook_ipc" dlp-agent/src/service.rs` and confirm the only matches are in `RunLoopContext::hook_ipc_handle`, `spawn_hook_ipc_server`, and `run_loop_shutdown`, not in `BlockingThreads`. |
| L-02 | VALIDATION | `nyquist_compliant: false` in frontmatter and sign-off checklist item unchecked. | **Not addressed** — this is expected for a draft plan and should be flipped to `true` when the plan is approved for execution, not during replanning. |
| L-03 | 03 | Plan 03 Task 2 step 2 says "Use a no-op or in-memory classification cache stub" but no existing stub is referenced. | Added reference to the `MockCache` struct in `dlp-agent/src/hook_ipc.rs` tests (line ~1008) as a suitable minimal stub. |
| L-04 | 02 | Plan 02 Task 1 step 3 says `get_caller_sid` is a "thin wrapper around the existing `get_process_user_sid`" but the actual implementation uses `#[cfg(windows)]` / `#[cfg(not(windows))]` stubs directly. | Updated description: "Add `fn get_caller_sid(pid: u32) -> Option<String>` with `#[cfg(windows)]` and `#[cfg(not(windows))]` stubs. On Windows it calls the existing `get_process_user_sid(pid)`. On non-Windows it returns `Some("S-1-5-18-test".to_string())` for testability." |
| L-05 | 03 | Plan 03 Task 3 step 5 wording about fail-closed behavior is misleading. | Rephrased: "The fail-closed behavior occurs because the `SourceVolumeClass`/`DestinationVolumeClass` conditions evaluate to `false` when the actual value is `None`, causing the policy to not match and the tiered default-deny to apply." |
| L-06 | 01 | Plan 01 context references stale line numbers (e.g., inline block at lines 1837-1929, `run_loop_init` at 1837-1929). | Updated line number references to match current source: `BlockingThreads` lines 182-258, `spawn_hook_ipc_server` lines 1286-1395, `run_loop_init` lines 1420-1646, `RunLoopContext` lines 949-1107, `run_loop_shutdown` lines 3437-3696. |
| L-07 | 02 | Plan 02 Task 1 step 1 says default fallback is `READ` (fail-closed) but code comment says "the policy engine may have a catch-all rule". | **Not addressed in plan** — the code comment is in source, not the plan. The plan already correctly describes `READ` as the conservative fallback. The code comment could be aligned in a future code cleanup but is not a plan issue. |

---

## New Tasks Added

| Plan | Task | Description | Reason |
|------|------|-------------|--------|
| 03 | Task 4 | Create `test_consolidated_server_routes_all_frames` | Addresses H-02 and M-04: the consolidated builder chain (`.with_diagnostics_handler`, `.with_health_handler`, `.with_override_handler`) is not exercised by any existing test because all integration tests use `start_mock_server` with raw handlers. |

---

## Findings Intentionally Not Addressed

| ID | Plan | Reason |
|----|------|--------|
| L-02 | VALIDATION | `nyquist_compliant: false` is expected for a draft plan. It should be flipped to `true` at execution approval time, not during replanning. |
| L-07 | 02 | The code comment in source (`dlp-agent/src/service.rs` line ~1231) is slightly inconsistent with the plan description, but the plan is correct. Aligning the code comment is a trivial source edit, not a plan issue. |

---

## Verification of Zero Unresolved Findings

### HIGH findings
- **H-01**: Resolved in Plan 02 verification section and Task 2 step 4.
- **H-02**: Resolved by adding gap acknowledgment in Plan 03 Task 2 and new Task 4 to create the missing test.
- **H-03**: Resolved by adding gap acknowledgment in Plan 03 Task 1 and updating `must_haves` to reflect tests must be added.

### Actionable MEDIUM findings
- **M-01**: Resolved in Plan 01 Task 2 step 4.
- **M-02**: Resolved in Plan 02 Task 1 step 4 and Task 2 step 3.
- **M-03**: Resolved in Plan 03 Task 2 step 4.
- **M-04**: Resolved by adding gap acknowledgment and new Task 4 in Plan 03.
- **M-05**: Resolved in Plan 03 verification section.
- **M-06**: Resolved in Plan 02 Task 2 step 4 and verification section.

### Actionable LOW findings
- **L-01**: Resolved in Plan 01 verification.
- **L-03**: Resolved in Plan 03 Task 2 step 2.
- **L-04**: Resolved in Plan 02 Task 1 step 3.
- **L-05**: Resolved in Plan 03 Task 3 step 5.
- **L-06**: Resolved in Plan 01 context section.

### Non-actionable / deferred findings
- **L-02**: Intentionally not addressed (see above).
- **L-07**: Intentionally not addressed (see above).

**Conclusion:** All HIGH findings are resolved. All actionable MEDIUM and LOW findings are resolved. The two non-actionable LOW findings (L-02, L-07) are intentionally deferred with documented rationale.

---

## Cycle 2 Replan Changes

**Date:** 2026-06-24
**Trigger:** Second-cycle review (`58.2-REVIEW-02.md`) found 0 HIGH, 4 actionable MEDIUM, and 5 LOW findings.

### Changes to 58.2-01-PLAN.md

| Finding | Change | Rationale |
|---------|--------|-----------|
| M-10 | Task 1 step 4 updated to reference the actual module name `hook_ipc_config` (lines 178-184 in `lib.rs`) and show the exact `#[cfg(test)] pub mod hook_ipc_config` re-export block. | The original text said "Export `HookIpcServerConfig` from `dlp-agent/src/lib.rs`" without mentioning the module name, which could mislead a developer to look at the crate root. |
| L-12 | Success criteria updated to clarify: "All four DIFF frame types (Request, PullDiagnostics, PullHealth, RequestOverride) route through the consolidated server, and BypassAlert/JournalDegraded frames continue to route through the bypass channel." | The original wording did not distinguish DIFF handlers from bypass frames, creating ambiguity about whether BypassAlert/JournalDegraded were also consolidated. |

### Changes to 58.2-02-PLAN.md

| Finding | Change | Rationale |
|---------|--------|-----------|
| M-09 | Task 2 step 1 reworded to clarify that cloning of `Arc<OfflineManager>`, `Arc<ApprovalCache>`, etc. happens at the top of `spawn_hook_ipc_server` (lines 1290-1295) into local variables before the closure is constructed, not inside the closure body. | The original wording "before moving into the closure" was ambiguous about whether the clone happened inside the closure or at the function level. |
| L-11 | Verification section updated: `grep -c "S-1-5-18" dlp-agent/src/service.rs` now correctly states it returns 2 (one in the `get_caller_sid` non-Windows stub at line ~1412, one in the audit context default at line ~3364). Added `grep -c "S-1-5-18-test"` returning 1 as the precise stub check. | The original claimed the count was 1, which would fail against the actual source because the audit context also contains `S-1-5-18`. |

### Changes to 58.2-03-PLAN.md

| Finding | Change | Rationale |
|---------|--------|-----------|
| M-07 | Task 2 step 2 updated with a note that `MockCache` in `dlp-agent/src/hook_ipc.rs` (line ~1008) is defined inside a `#[cfg(test)]` block and is not exported. Added instruction that the integration test must define its own `CacheAccessor` stub or `MockCache` must be moved to a shared test-helpers module. | The original text suggested copying or referencing `MockCache` directly, which would not compile from an integration test because the struct is not `pub`. |
| M-08 | Task 3 step 5 split into two steps. New step 6 explicitly instructs verifying the `warn!` path for missing volume classes on COPY/MOVE using `tracing-test` or a custom `tracing` subscriber capture. If tracing capture is unavailable, document it as a manual verification step with `RUST_LOG=warn`. | The original only asserted the DENY decision but did not verify the D-09 `warn!` emission, leaving a requirement untested. |
| L-08 | Task 1 step 1 reworded to say "Extend the existing `#[cfg(test)]` block in `dlp-agent/src/service.rs` (starting at line ~4947)" instead of "Add a `#[cfg(test)] mod tests` block". Added note that the existing block contains shutdown-signal and `BlockingThreads` tests and the new tests should be added after those. | The original wording could be misread as creating a new test module rather than extending the existing one at line ~4947. |
| L-09 | Task 2 step 2 config example updated to explicitly show `pipe_name: r"\\.\pipe\DlpHookPipeTestConsolidated".to_string()` in the `HookIpcServerConfig` construction. | The original did not show the `pipe_name` field being set, risking a collision with `DEFAULT_PIPE_NAME` in integration tests. |
| L-10 | Task 4 step 1 changed recommendation from `dlp-agent/tests/volume_class_integration.rs` to a new file `dlp-agent/tests/hook_ipc_integration.rs`. Updated the verify command to `cargo test -p dlp-agent --test hook_ipc_integration`. | The original suggested appending a server-routing test to a file focused on volume-class policy enforcement, which is semantically mismatched. |

### Verification of Zero Unresolved Findings (Post-Cycle-2)

**HIGH findings:** 0 unresolved (H-01, H-02, H-03 were resolved in Cycle 1; no new HIGH findings in Cycle 2).

**Actionable MEDIUM findings:** 0 unresolved (M-07, M-08, M-09, M-10 all addressed above).

**Actionable LOW findings:** 0 unresolved (L-08, L-09, L-10, L-11, L-12 all addressed above).

**Intentionally not addressed:**
- L-02 (`nyquist_compliant: false` expected for draft plan) — deferred per existing rationale.
- L-07 (code comment in source vs. plan wording) — deferred per existing rationale; this is a source edit, not a plan issue.

**Conclusion:** All Cycle 2 findings are resolved in plan files. The convergence loop can exit.
