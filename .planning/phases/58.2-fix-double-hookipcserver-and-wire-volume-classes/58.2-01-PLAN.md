---
phase: 58.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-agent/src/service.rs
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/lib.rs
autonomous: true
requirements:
  - TBD
user_setup: []

must_haves:
  truths:
    - "Only one HookIpcServer thread exists per agent lifecycle"
    - "All DIFF handlers (diagnostics, health, override) route through the consolidated server"
    - "Approval cache override behavior is preserved on the hook path"
    - "Service shutdown joins the single handle without double-join risk"
  artifacts:
    - path: "dlp-agent/src/service.rs"
      provides: "HookIpcServerConfig struct, rewritten spawn_hook_ipc_server, removed inline block, removed BlockingThreads::hook_ipc"
      contains: "struct HookIpcServerConfig"
    - path: "dlp-agent/src/hook_ipc.rs"
      provides: "HookIpcServer builder methods remain intact"
      contains: "with_diagnostics_handler"
    - path: "dlp-agent/src/lib.rs"
      provides: "Exports for integration tests"
  key_links:
    - from: "run_loop_init"
      to: "spawn_hook_ipc_server"
      via: "HookIpcServerConfig assembled in init, consumed by builder"
    - from: "RunLoopContext::hook_ipc_handle"
      to: "run_loop_shutdown"
      via: "Single handle stored in context, joined during shutdown"
---

<objective>
Eliminate the duplicate HookIpcServer initialization by introducing a HookIpcServerConfig builder, removing the inline block from run_loop_init, and deleting the duplicate BlockingThreads::hook_ipc field. The consolidated server preserves all DIFF-01/02/04 handlers and the real offline ABAC evaluation path.

Purpose: Fix the bug where two HookIpcServer instances race for DEFAULT_PIPE_NAME, leaving enforcement partially broken.
Output: Single server thread per agent lifecycle with all handlers intact.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-CONTEXT.md
@.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-RESEARCH.md

# Key source files to read before executing
- dlp-agent/src/service.rs (BlockingThreads lines 182-258, spawn_hook_ipc_server lines 1208-1290, run_loop_init inline block lines 1837-1929, RunLoopContext lines 958-1107, run_loop_shutdown lines 3437-3696)
- dlp-agent/src/hook_ipc.rs (HookIpcServer builder methods lines 92-233)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce HookIpcServerConfig and rewrite spawn_hook_ipc_server</name>
  <files>dlp-agent/src/service.rs, dlp-agent/src/lib.rs</files>
  <action>
  1. Define `HookIpcServerConfig` struct in `dlp-agent/src/service.rs` near the existing `spawn_hook_ipc_server` function. Fields per D-02: `pipe_name: String`, `cache: Arc<dyn crate::hook_ipc::CacheAccessor>`, `offline: Arc<crate::offline::OfflineManager>`, `bypass_tx: crossbeam_channel::Sender<dlp_common::hook_ipc::BypassAlert>`, `diagnostic_aggregator: Arc<crate::diagnostic_aggregator::DiagnosticAggregator>`, `health_aggregator: Arc<crate::health_aggregator::HealthAggregator>`, `override_tx: tokio::sync::mpsc::Sender<dlp_common::hook_ipc::OverrideRequest>`, `approval_cache: Arc<crate::approval_cache::ApprovalCache>`.
  2. Rewrite `spawn_hook_ipc_server` to accept `config: HookIpcServerConfig` and return `Option<std::thread::JoinHandle<()>>`. The builder chain per D-03: start with `HookIpcServer::with_cache_offline_and_bypass(config.pipe_name, cache, handler_closure, config.bypass_tx)`, then chain `.with_diagnostics_handler(...)`, `.with_health_handler(...)`, `.with_override_handler(...)`. The handler closure per D-04 must resolve caller SID from `req.pid`, check approval cache, and return cached decision if present; otherwise it will delegate to offline ABAC (wired in Plan 02).
  3. For now, the handler closure may use a placeholder `get_caller_sid` stub (to be implemented in Plan 02) and a placeholder `hook_request_to_evaluate_request` call. The important part is the builder structure and handler skeleton.
  4. Export `HookIpcServerConfig` from `dlp-agent/src/lib.rs` under a `#[cfg(test)]` or `pub` gate so integration tests can construct it.
  </action>
  <verify>
    <automated>cargo check -p dlp-agent</automated>
  </verify>
  <done>HookIpcServerConfig struct exists; spawn_hook_ipc_server compiles with new signature; builder chains all four handlers; lib.rs exports config.</done>
</task>

<task type="auto">
  <name>Task 2: Remove inline server from run_loop_init and delete BlockingThreads::hook_ipc</name>
  <files>dlp-agent/src/service.rs</files>
  <action>
  1. In `run_loop_init`, delete the entire inline `HookIpcServer` construction block (the second duplicate). Replace it with a single call to `spawn_hook_ipc_server(config)` where `config` is assembled from the local variables already available in `run_loop_init` (classification cache, offline manager, bypass_tx, diagnostic aggregator, health aggregator, override_tx, approval cache). Store the returned `Option<JoinHandle<()>>` in `RunLoopContext::hook_ipc_handle` per D-05.
  2. Remove the `hook_ipc: Option<std::thread::JoinHandle<()>>` field from `BlockingThreads` struct and its initialization site. Remove the join path for `hook_ipc` in `shutdown_and_join` (or equivalent shutdown function). Ensure no other code references `BlockingThreads::hook_ipc`.
  3. Verify that `run_loop_shutdown` still closes the named pipe and joins `RunLoopContext::hook_ipc_handle` as the single shutdown mechanism.
  4. Verify that the old `spawn_hook_ipc_server` call site (outside `run_loop_init`) is removed or unified — there should be exactly one place that creates the HookIpcServer.
  </action>
  <verify>
    <automated>cargo check -p dlp-agent && cargo test -p dlp-agent --lib -- --test-threads=1</automated>
  </verify>
  <done>Inline block removed; BlockingThreads::hook_ipc field and join path deleted; exactly one spawn_hook_ipc_server call exists; run_loop_shutdown joins only RunLoopContext::hook_ipc_handle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook DLL -> named pipe | Untrusted client input crosses here; HookIpcServer deserializes and routes frames |
| handler closure -> OfflineManager | ABAC evaluation runs synchronously on the hook IPC thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.2-01 | Tampering | Duplicate named-pipe listener | mitigate | Consolidate to single server; remove inline block and old spawn function |
| T-58.2-02 | Information Disclosure | Diagnostics/health/override handlers lost | mitigate | Builder chain explicitly attaches all three .with_* handlers per D-03 |
| T-58.2-03 | Denial of Service | Double-join on shutdown | mitigate | Remove BlockingThreads::hook_ipc; rely solely on RunLoopContext::hook_ipc_handle |
| T-58.2-SC | Tampering | Workspace crates (no new packages) | accept | No new external packages; all workspace crates already audited in prior phases |
</threat_model>

<verification>
- `cargo check -p dlp-agent` passes with zero errors
- `cargo test -p dlp-agent --lib` passes (existing unit tests remain green)
- `grep -c "HookIpcServer::new" dlp-agent/src/service.rs` returns 0 (no raw constructors left)
- `grep -c "BlockingThreads" dlp-agent/src/service.rs` followed by confirming no `hook_ipc` field remains
- Exactly one call to `spawn_hook_ipc_server` in service.rs
</verification>

<success_criteria>
- Only one HookIpcServer instance is created per agent lifecycle
- All four frame types (Request, PullDiagnostics, PullHealth, RequestOverride) route through the consolidated server
- Approval cache override behavior is preserved
- Service shutdown completes without double-join panic or hang
</success_criteria>

<output>
Create `.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-01-SUMMARY.md` when done
</output>
