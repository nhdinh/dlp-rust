---
phase: 58.2
plan: 03
type: execute
wave: 2
depends_on:
  - 58.2-01
  - 58.2-02
files_modified:
  - dlp-agent/src/service.rs
  - dlp-agent/tests/volume_class_integration.rs
  - dlp-agent/src/hook_ipc.rs
autonomous: true
requirements:
  - TBD
user_setup: []

must_haves:
  truths:
    - "Unit tests for hook_request_to_evaluate_request, map_hook_action_to_abac, and get_caller_sid are added to dlp-agent/src/service.rs"
    - "Integration tests prove end-to-end ALLOW/DENY based on volume class via the existing mock server pipeline"
    - "Existing tests remain green"
  artifacts:
    - path: "dlp-agent/src/service.rs"
      provides: "Unit tests for hook_request_to_evaluate_request, map_hook_action_to_abac, get_caller_sid"
      contains: "test_hook_request_to_evaluate_request_forwards_volume_classes"
    - path: "dlp-agent/tests/volume_class_integration.rs"
      provides: "End-to-end volume-class policy tests via start_mock_server"
      contains: "test_hook_ipc_volume_class_matches_deny"
  key_links:
    - from: "unit tests"
      to: "hook_request_to_evaluate_request"
      via: "Direct function calls with mock inputs and assertions on EvaluateRequest fields"
    - from: "integration tests"
      to: "consolidated server"
      via: "Uses start_mock_server with handler calling hook_request_to_evaluate_request + PolicyStore::evaluate"
---

<objective>
Validate the consolidation and wiring with unit tests for the new helpers and integration tests for the consolidated server. Ensure all four frame types route correctly and volume-class policies produce expected ALLOW/DENY decisions end-to-end.

Purpose: Prove the bug is fixed and the new wiring works correctly before closing the phase.
Output: Green test suite with new coverage for server consolidation, identity resolution, action mapping, and volume-class enforcement.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-CONTEXT.md
@.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-RESEARCH.md
@.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-VALIDATION.md

# Key source files to read before executing
- dlp-agent/src/service.rs (existing #[cfg(test)] modules for pattern reference)
- dlp-agent/tests/volume_class_integration.rs (existing end-to-end tests to extend)
- dlp-agent/src/hook_ipc.rs (HookIpcServer::start_mock_server or equivalent test helpers)
- dlp-common/src/hook_ipc.rs (IpcPayloadV1 variants, HookRequest, HookResponse)
- dlp-common/src/abac.rs (EvaluateRequest, EvaluateResponse, Decision, VolumeClass)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for hook_request_to_evaluate_request, map_hook_action_to_abac, and get_caller_sid</name>
  <files>dlp-agent/src/service.rs</files>
  <behavior>
    - Test hook_request_to_evaluate_request with a HookRequest containing source_volume_class=Some(LocalNTFS), destination_volume_class=Some(Optical), action="COPY", path="C:\\test.txt", and caller_sid="S-1-5-21-123". Assert EvaluateRequest has matching fields, action=COPY, subject.user_sid=caller_sid, resource.path="C:\\test.txt", agent=None.
    - Test map_hook_action_to_abac for all variants: READ, NT_READ -> READ; WRITE, CREATE, NT_WRITE -> WRITE; COPY -> COPY; DELETE, MOVE, RENAME, REPLACE, SET_INFO, NT_SET_INFO -> DELETE; unknown -> READ.
    - Test get_caller_sid on non-Windows returns Some("S-1-5-18-test"). On Windows, test with a known current process PID returns a valid SID string starting with "S-1-5-".
    - Test that hook_request_to_evaluate_request leaves source_application, destination_application, source_origin, destination_origin as None.
  </behavior>
  <action>
  1. Extend the existing `#[cfg(test)]` block in `dlp-agent/src/service.rs` (starting at line ~4947) with the following unit tests per D-13 and D-14. The existing block contains shutdown-signal and `BlockingThreads` tests; add the new tests after those.

  **Gap acknowledgment:** These unit tests do not currently exist in `dlp-agent/src/service.rs`. The existing `#[cfg(test)]` blocks (lines ~4947+) contain only shutdown-signal and BlockingThreads tests. The integration tests in `volume_class_integration.rs` partially cover `hook_request_to_evaluate_request` and `map_hook_action_to_abac`, but `get_caller_sid` is untested on both Windows and non-Windows. Add the following unit tests as part of this task:

  2. `test_hook_request_to_evaluate_request_forwards_volume_classes`: construct a HookRequest with both volume classes set, call `hook_request_to_evaluate_request(req, "S-1-5-21-123".to_string())`, assert the resulting EvaluateRequest has matching source_volume_class, destination_volume_class, action, path, and caller_sid.
  3. `test_map_hook_action_to_abac_all_variants`: iterate over all known action strings and assert correct ABAC Action mapping. Include an unknown action to verify the default READ fallback.
  4. `test_get_caller_sid_stub_on_non_windows`: when `#[cfg(not(windows))]`, verify `get_caller_sid(0)` returns `Some("S-1-5-18-test".to_string())`.
  5. `test_hook_request_to_evaluate_request_leaves_optional_fields_none`: verify agent, source_application, destination_application, source_origin, destination_origin are all None.
  6. Ensure tests compile and pass on both Windows and non-Windows targets. Use `#[cfg(windows)]` / `#[cfg(not(windows))]` where necessary.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent hook_request_to_evaluate_request map_hook_action_to_abac get_caller_sid -- --test-threads=1</automated>
  </verify>
  <done>All unit tests pass; coverage includes volume class forwarding, action mapping, identity resolution stub, and optional field invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test for consolidated server routing all four frame types</name>
  <files>dlp-agent/tests/volume_class_integration.rs, dlp-agent/src/hook_ipc.rs</files>
  <behavior>
    - Construct a consolidated HookIpcServer on a unique pipe name (not DEFAULT_PIPE_NAME) using a test HookIpcServerConfig with mock/stub dependencies.
    - Send IpcPayloadV1::Request(HookRequest) and assert it routes to the ABAC handler and returns a HookResponse.
    - Send IpcPayloadV1::PullDiagnostics and assert it routes to the diagnostics handler and returns a response.
    - Send IpcPayloadV1::PullHealth and assert it routes to the health handler and returns a response.
    - Send IpcPayloadV1::RequestOverride and assert it routes to the override handler.
    - Assert only one server thread is created (the test spawns one server; verify no second listener exists).
  </behavior>
  <action>
  1. Extend `dlp-agent/tests/volume_class_integration.rs` with `test_consolidated_server_routes_all_frames` per D-15. Use a unique pipe name (e.g., `\\.\pipe\DlpHookPipeTestConsolidated`) to avoid collision with a live service.

  **NOTE:** This test does not currently exist in the codebase. The existing integration tests use `start_mock_server` with raw `Arc<Fn(HookRequest) -> HookResponse>` handlers, which bypasses the consolidated `spawn_hook_ipc_server` builder chain entirely. Therefore the builder chain (`.with_diagnostics_handler`, `.with_health_handler`, `.with_override_handler`) is not exercised by any existing test.

  **Gap acknowledgment:** The `test_consolidated_server_routes_all_frames` test is scheduled as a follow-up (see Task 4 below). For now, the existing `start_mock_server`-based tests in `volume_class_integration.rs` provide coverage for Request frame routing and volume-class policy enforcement. The consolidated builder chain is tested indirectly via `test_spawn_hook_ipc_server_starts_named_thread` in `dlp-agent/src/service.rs` (unit test level).
  2. Build a minimal `HookIpcServerConfig` with mock/stub dependencies:
     - Use a no-op or in-memory classification cache stub. `HookIpcServerConfig` requires `Arc<dyn CacheAccessor>`. The `MockCache` struct in `dlp-agent/src/hook_ipc.rs` tests (line ~1008) is a suitable minimal stub, but it is defined inside a `#[cfg(test)]` block in `hook_ipc.rs` and is not exported. The integration test must define its own stub (copy the 3-line `MockCache` struct) or `MockCache` must be moved to a shared test-helpers module before this test can compile.
     - Use a mock `OfflineManager` that returns predetermined decisions (e.g., ALLOW for one test, DENY for another).
     - Use dummy `bypass_tx`, `diagnostic_aggregator`, `health_aggregator`, `override_tx`, and `approval_cache`.
     - Set `pipe_name: r"\\.\pipe\DlpHookPipeTestConsolidated".to_string()` in the config to avoid collision with a live service.
  3. Spawn the server via `spawn_hook_ipc_server(config)` and connect a client pipe. Send each IpcPayloadV1 variant and assert correct routing:
     - Request -> HookResponse with expected decision
     - PullDiagnostics -> non-empty or expected diagnostic response
     - PullHealth -> expected health status
     - RequestOverride -> accepted by override handler (or at least not dropped)
  4. Verify that only one server thread exists by asserting that `spawn_hook_ipc_server` returns `Some(JoinHandle)` and that the handle's thread name is `"hook-ipc-server"`. Do not rely on `ERROR_PIPE_BUSY` from `CreateNamedPipeW` — a second `CreateNamedPipeW` on the same name from the same process typically succeeds with a new handle, so that check is not a valid single-threadedness proof.
  5. Ensure the test cleans up by closing the pipe handle so subsequent tests are not blocked.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test volume_class_integration -- --test-threads=1</automated>
  </verify>
  <done>Integration test passes; all four frame types route correctly; only one server thread per test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end volume-class ALLOW/DENY integration test</name>
  <files>dlp-agent/tests/volume_class_integration.rs</files>
  <behavior>
    - Build a mock OfflineManager that evaluates volume-class conditions: ALLOW when source_volume_class=LocalNTFS and destination_volume_class=LocalNTFS; DENY when source=LocalNTFS and destination=Optical.
    - Send HookRequest with matching fields and assert the returned HookResponse decision matches the policy.
    - Test that a COPY action with None volume class returns DENY (fail-closed) and triggers the warn! path.
  </behavior>
  <action>
  1. Extend `dlp-agent/tests/volume_class_integration.rs` with `test_volume_class_policy_enforcement_end_to_end` per D-16. Use the consolidated server spawned in Task 2 as the test fixture.
  2. Configure the mock `OfflineManager` to return:
     - ALLOW when `evaluate_req.source_volume_class == Some(LocalNTFS)` and `evaluate_req.destination_volume_class == Some(LocalNTFS)`
     - DENY when `evaluate_req.destination_volume_class == Some(Optical)`
  3. Send a `HookRequest` with `action="COPY"`, `source_volume_class=Some(LocalNTFS)`, `destination_volume_class=Some(LocalNTFS)` and assert the response is ALLOW.
  4. Send a `HookRequest` with `action="COPY"`, `source_volume_class=Some(LocalNTFS)`, `destination_volume_class=Some(Optical)` and assert the response is DENY.
  5. Send a `HookRequest` with `action="COPY"`, `source_volume_class=None`, `destination_volume_class=None` and assert the response is DENY (fail-closed). The fail-closed behavior occurs because the `SourceVolumeClass`/`DestinationVolumeClass` conditions evaluate to `false` when the actual value is `None`, causing the policy to not match and the tiered default-deny to apply.
  6. Verify the `warn!` path: the handler closure emits `tracing::warn!` when COPY/MOVE has None volume classes (D-09). Use `tracing-test` (if available in `dev-dependencies`) or a `tracing` subscriber with a `MakeWriter` that captures to a `Vec<String>` to assert the warning contains the request path and action. If tracing capture is not available in the test environment, document this as a manual verification step: run the test with `RUST_LOG=warn` and visually confirm the warning appears in stderr.
  7. Keep the existing `PolicyStore` unit tests as regression guards (no modifications needed unless they break due to API changes).
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test volume_class_integration -- --test-threads=1</automated>
  </verify>
  <done>End-to-end test passes; volume-class policies produce expected ALLOW/DENY decisions via the consolidated server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create test_consolidated_server_routes_all_frames (follow-up)</name>
  <files>dlp-agent/tests/volume_class_integration.rs, dlp-agent/src/hook_ipc.rs</files>
  <behavior>
    - Construct a consolidated HookIpcServer via `spawn_hook_ipc_server` with a real `HookIpcServerConfig` (or a test variant) and assert all four `IpcPayloadV1` variants route correctly.
    - This test exercises the consolidated builder chain (`.with_diagnostics_handler`, `.with_health_handler`, `.with_override_handler`) which is currently bypassed by the `start_mock_server`-based tests.
  </behavior>
  <action>
  1. Create `test_consolidated_server_routes_all_frames` in a new file `dlp-agent/tests/hook_ipc_integration.rs` (not in `volume_class_integration.rs`, which is focused on volume-class policy enforcement). The test should:
     - Builds a `HookIpcServerConfig` with mock/stub dependencies (see Task 2 step 2 for stub references; note that `MockCache` from `hook_ipc.rs` is not exported, so the test must define its own `CacheAccessor` stub or the stub must be moved to a shared test-helpers module).
     - Calls `spawn_hook_ipc_server(config)` to start the server on a unique pipe name.
     - Sends `IpcPayloadV1::Request(HookRequest)` and asserts `HookResponse` with expected decision.
     - Sends `IpcPayloadV1::PullDiagnostics` and asserts a non-empty diagnostic response.
     - Sends `IpcPayloadV1::PullHealth` and asserts expected health status.
     - Sends `IpcPayloadV1::RequestOverride` and asserts it is accepted by the override handler.
     - Asserts that the returned `JoinHandle` thread name is `"hook-ipc-server"`.
  2. Ensure the test cleans up by closing the pipe handle so subsequent tests are not blocked.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent --test hook_ipc_integration -- --test-threads=1</automated>
  </verify>
  <done>Integration test passes; all four frame types route correctly through the consolidated builder chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mock -> real handler | Mock OfflineManager and stubs replace production dependencies; tests verify wiring only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.2-07 | Information Disclosure | Test pipe names collide with production | mitigate | Use unique pipe names (e.g., `DlpHookPipeTest{uuid}`) in all integration tests |
| T-58.2-08 | Denial of Service | Test server thread leaks, blocking subsequent tests | mitigate | Always close pipe handle and join server thread in test teardown; use `Drop` guards |
</threat_model>

<verification>
- `cargo test -p dlp-agent` passes (all unit + integration tests)
- `cargo test --workspace` passes (no regressions in other crates)
- `cargo clippy -p dlp-agent -- -D warnings` passes
- `cargo fmt --check` passes
- `sonar-scanner` passes (per CLAUDE.md 9.16 — required before committing; if the Quality Gate fails due to low coverage from missing unit tests, treat as blocking and add the tests from Task 1)
- New tests cover: hook_request_to_evaluate_request, map_hook_action_to_abac, get_caller_sid, consolidated server routing, end-to-end volume-class decisions
</verification>

<success_criteria>
- All new unit tests pass and cover the specified behaviors
- Integration tests prove the consolidated server routes all four frame types
- End-to-end volume-class tests produce correct ALLOW/DENY decisions
- Existing tests remain green; no regressions in workspace
</success_criteria>

<output>
Create `.planning/phases/58.2-fix-double-hookipcserver-and-wire-volume-classes/58.2-03-SUMMARY.md` when done
</output>
