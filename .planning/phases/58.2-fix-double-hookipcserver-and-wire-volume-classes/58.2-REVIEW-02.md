# Phase 58.2 Plan Review — Cycle 2

**Reviewer:** Cross-AI Plan Review (Claude Code)  
**Date:** 2026-06-24  
**Scope:** 58.2-REPLAN-NOTES.md, 58.2-CONTEXT.md, 58.2-RESEARCH.md, 58.2-VALIDATION.md, 58.2-01-PLAN.md, 58.2-02-PLAN.md, 58.2-03-PLAN.md  
**Source Verified Against:** dlp-agent/src/service.rs, dlp-agent/src/hook_ipc.rs, dlp-agent/src/lib.rs, dlp-agent/tests/volume_class_integration.rs, dlp-common/src/hook_ipc.rs, dlp-common/src/abac.rs

---

## Summary

The Phase 58.2 implementation is **already complete in source code**. The three plan files (58.2-01, 58.2-02, 58.2-03) were updated in the replan to match the current codebase reality. This second-cycle review verifies that the previous HIGH findings (H-01, H-02, H-03) are resolved, checks for new issues introduced by the replan, and assesses remaining gaps.

### Source verification performed
- `dlp-agent/src/service.rs` — verified `HookIpcServerConfig` (lines 1204-1226), `map_hook_action_to_abac` (lines 1228-1243), `hook_request_to_evaluate_request` (lines 1251-1275), `spawn_hook_ipc_server` (lines 1289-1398), `get_caller_sid` (lines 1406-1413), `run_loop_init` (lines 1420-1646), `run_loop_shutdown` (lines 3476-3735), `BlockingThreads` (lines 182-254), `RunLoopContext` (lines 949-1103)
- `dlp-agent/src/hook_ipc.rs` — verified `HookIpcServer` builder methods (`with_diagnostics_handler`, `with_health_handler`, `with_override_handler`, `with_cache_offline_and_bypass`), `MockCache` test stub (lines 1007-1016)
- `dlp-agent/src/lib.rs` — verified `#[cfg(test)] pub mod hook_ipc_config` re-export (lines 178-184)
- `dlp-agent/tests/volume_class_integration.rs` — verified existing tests use `start_mock_server` with raw handlers, no consolidated builder chain test exists
- `dlp-common/src/abac.rs` — verified `EvaluateRequest` fields include `source_volume_class` and `destination_volume_class`

### Key observations
- `BlockingThreads::hook_ipc` field has been removed (verified at lines 182-254)
- `RunLoopContext::hook_ipc_handle` is the single handle storage (verified at lines 949-1103)
- `spawn_hook_ipc_server` is called exactly once inside `run_loop_init` (verified at line 1643)
- `get_caller_sid` has `#[cfg(windows)]` and `#[cfg(not(windows))]` stubs (verified at lines 1406-1413)
- The non-Windows stub returns `"S-1-5-18-test"` (verified at line 1412)
- The audit context at line 3364 uses `user_sid: "S-1-5-18".to_string()` as a default — this is intentional and unrelated to handler identity resolution
- Unit test `test_spawn_hook_ipc_server_starts_named_thread` exists (verified at lines 5076-5110)
- Unit tests for `hook_request_to_evaluate_request`, `map_hook_action_to_abac`, and `get_caller_sid` do **not** exist in `dlp-agent/src/service.rs` (verified by searching `#[cfg(test)]` blocks starting at line 4947)
- Integration test `test_consolidated_server_routes_all_frames` does **not** exist in `dlp-agent/tests/volume_class_integration.rs`

---

## HIGH

| ID | Plan | Finding | Recommended Fix |
|----|------|---------|-----------------|
| H-01 | 02 | **RESOLVED.** The replan updated the verification grep to account for the non-Windows stub returning `"S-1-5-18-test"` and the legitimate `"S-1-5-18"` in the audit context at line 3364. The plan now states `grep -c "S-1-5-18"` returns 1 (only the test stub) and `grep -n "S-1-5-18"` confirms the only match is inside `get_caller_sid`. | No fix needed. |
| H-02 | 03 | **PARTIALLY RESOLVED.** The replan acknowledged the missing `test_consolidated_server_routes_all_frames` test and added Task 4 to create it. However, Task 4 is a follow-up task in the same plan, not a committed test. The gap still exists until the test is written. | Ensure Task 4 is completed before the phase is closed. The plan already acknowledges this; no plan file change needed, but execution must not skip Task 4. |
| H-03 | 03 | **PARTIALLY RESOLVED.** The replan acknowledged that unit tests for `hook_request_to_evaluate_request`, `map_hook_action_to_abac`, and `get_caller_sid` do not exist in `dlp-agent/src/service.rs` and updated the `must_haves` to reflect they must be added. However, the tests are still missing from source. | Ensure Task 1 in Plan 03 is completed before the phase is closed. The plan already acknowledges this; no plan file change needed, but execution must not skip Task 1. |

---

## MEDIUM

| ID | Plan | Finding | Recommended Fix |
|----|------|---------|-----------------|
| M-01 | 01 | **RESOLVED.** Plan 01 Task 2 step 4 was rewritten to clarify there is exactly one `spawn_hook_ipc_server` call inside `run_loop_init` and the inline block was the duplicate. | No fix needed. |
| M-02 | 02 | **RESOLVED.** Plan 02 Task 1 step 4 now clarifies that the `warn!` is emitted inside the handler closure (Task 2), not in the conversion helper. Task 2 step 3 explicitly states the warning is part of the handler closure. | No fix needed. |
| M-03 | 03 | **RESOLVED.** Plan 03 Task 2 step 4 was replaced with a robust assertion using `spawn_hook_ipc_server` returning `Some(JoinHandle)` and checking the thread name is `"hook-ipc-server"`. The fragile `ERROR_PIPE_BUSY` check was removed. | No fix needed. |
| M-04 | 03 | **PARTIALLY RESOLVED.** The replan acknowledged that integration tests bypass the consolidated builder chain and added Task 4 to create a test that exercises the real `spawn_hook_ipc_server` builder chain. However, the test does not yet exist. | Ensure Task 4 is completed before the phase is closed. The plan already acknowledges this; no plan file change needed, but execution must not skip Task 4. |
| M-05 | 03 | **RESOLVED.** `sonar-scanner` was added to the Plan 03 verification checklist with a note per CLAUDE.md 9.16. | No fix needed. |
| M-06 | 02 | **RESOLVED.** Plan 02 Task 2 step 4 now includes a note that the audit context initialization at line ~3364 uses `"S-1-5-18"` as a default before per-event override, and this is intentional and unrelated to handler identity resolution. | No fix needed. |
| M-07 | 03 | **NEW.** Plan 03 Task 2 step 2 references `MockCache` in `dlp-agent/src/hook_ipc.rs` tests (line ~1008) as a suitable minimal stub. However, `MockCache` is defined inside a `#[cfg(test)]` block in `hook_ipc.rs` and is not exported. The integration test in `volume_class_integration.rs` cannot access it directly. The integration test would need to define its own stub or the stub would need to be moved to a shared test-helpers module. | Add a note in Plan 03 Task 2 that the integration test must define its own `CacheAccessor` stub (copy the 3-line `MockCache` struct) or move `MockCache` to a shared test-helpers module. |
| M-08 | 03 | **NEW.** Plan 03 Task 3 step 5 says "Send a `HookRequest` with `action='COPY'`, `source_volume_class=None`, `destination_volume_class=None` and assert the response is DENY (fail-closed)." The fail-closed behavior is correctly described as occurring because `SourceVolumeClass`/`DestinationVolumeClass` conditions evaluate to `false` when the actual value is `None`. However, the test as described does not verify the `warn!` path — it only asserts the decision. The `warn!` for missing volume classes on COPY/MOVE is a D-09 requirement that should be tested. | Add a step to verify the `warn!` is emitted, e.g., by using `tracing-test` or capturing logs. Alternatively, acknowledge that the `warn!` verification is manual and document it. |
| M-09 | 02 | **NEW.** Plan 02 Task 2 step 1 says "Ensure the closure captures `Arc<OfflineManager>`, `Arc<ApprovalCache>`, and any other needed dependencies by cloning from `HookIpcServerConfig` before moving into the closure." The actual implementation in `service.rs` (lines 1290-1295) clones these dependencies at the top of `spawn_hook_ipc_server`, but the closure itself is constructed inline (lines 1324-1377). The plan wording could be clearer that the cloning happens in the function body, not inside the closure. | Clarify in Plan 02 Task 2 step 1 that cloning happens at the top of `spawn_hook_ipc_server` before the closure is constructed. |
| M-10 | 01 | **NEW.** Plan 01 Task 1 step 4 says "Export `HookIpcServerConfig` from `dlp-agent/src/lib.rs` under a `#[cfg(test)]` or `pub` gate so integration tests can construct it." The actual export is at `lib.rs` lines 178-184 under `#[cfg(test)] pub mod hook_ipc_config`. This is correct, but the plan does not mention the `hook_ipc_config` module name — it just says "export `HookIpcServerConfig`". A developer might expect to find it at the crate root. | Update Plan 01 Task 1 step 4 to reference the actual module name: `pub mod hook_ipc_config` re-exporting `HookIpcServerConfig`, `hook_request_to_evaluate_request`, and `map_hook_action_to_abac`. |

---

## LOW

| ID | Plan | Finding | Recommended Fix |
|----|------|---------|-----------------|
| L-01 | 01 | **RESOLVED.** Verification changed from `grep -c "BlockingThreads"` to `grep -n "hook_ipc"` and confirming matches are only in `RunLoopContext::hook_ipc_handle`, `spawn_hook_ipc_server`, and `run_loop_shutdown`. | No fix needed. |
| L-02 | VALIDATION | **INTENTIONALLY NOT ADDRESSED.** `nyquist_compliant: false` is expected for a draft plan and should be flipped to `true` at execution approval time. | No fix needed. |
| L-03 | 03 | **RESOLVED.** Plan 03 Task 2 step 2 now references `MockCache` in `dlp-agent/src/hook_ipc.rs` tests (line ~1008) as a suitable minimal stub. | No fix needed. |
| L-04 | 02 | **RESOLVED.** Plan 02 Task 1 step 3 now correctly describes `get_caller_sid` as a `cfg`-gated function that calls `get_process_user_sid` on Windows and returns a test stub on non-Windows. | No fix needed. |
| L-05 | 03 | **RESOLVED.** Plan 03 Task 3 step 5 wording was rephrased to clarify that `None` fails closed because the `SourceVolumeClass`/`DestinationVolumeClass` conditions evaluate to `false` when the actual value is `None`, causing the policy to not match and the tiered default-deny to apply. | No fix needed. |
| L-06 | 01 | **RESOLVED.** Plan 01 line number references were updated to match current source: `BlockingThreads` lines 182-258, `spawn_hook_ipc_server` lines 1286-1395, `run_loop_init` lines 1420-1646, `RunLoopContext` lines 949-1107, `run_loop_shutdown` lines 3437-3696. | No fix needed. |
| L-07 | 02 | **INTENTIONALLY NOT ADDRESSED.** The code comment in source (`dlp-agent/src/service.rs` line ~1231) says "the policy engine may have a catch-all rule" which is slightly inconsistent with the plan's description of `READ` as the conservative fallback. The plan is correct; aligning the code comment is a trivial source edit, not a plan issue. | No fix needed. |
| L-08 | 03 | **NEW.** Plan 03 Task 1 step 1 says "Add a `#[cfg(test)] mod tests` block in `dlp-agent/src/service.rs` (or extend an existing one)". The existing `#[cfg(test)]` block in `service.rs` starts at line 4947 and contains shutdown-signal and `BlockingThreads` tests. The plan should clarify that the new tests should be added to the existing block, not a new one, to avoid confusion. | Update Plan 03 Task 1 step 1 to say "Extend the existing `#[cfg(test)]` block in `dlp-agent/src/service.rs` (starting at line ~4947) with the following unit tests..." |
| L-09 | 03 | **NEW.** Plan 03 Task 2 step 3 says "Spawn the server via `spawn_hook_ipc_server(config)` and connect a client pipe." The `spawn_hook_ipc_server` function uses `DEFAULT_PIPE_NAME` unless overridden in config. The plan correctly says to use a unique pipe name (e.g., `\\.\pipe\DlpHookPipeTestConsolidated`) in step 1, but the config example in Task 2 step 2 does not explicitly show `pipe_name` being set to a unique value. | Add `pipe_name: r"\\.\pipe\DlpHookPipeTestConsolidated".to_string()` to the `HookIpcServerConfig` example in Task 2 step 2. |
| L-10 | 03 | **NEW.** Plan 03 Task 4 step 1 says "Create `test_consolidated_server_routes_all_frames` in `dlp-agent/tests/volume_class_integration.rs` (or a new integration test file)". The plan should recommend a new file (e.g., `hook_ipc_integration.rs`) rather than appending to `volume_class_integration.rs`, because the latter is focused on volume-class policy enforcement, not server routing. | Change the recommendation to create a new file `dlp-agent/tests/hook_ipc_integration.rs` for the consolidated server routing test. |
| L-11 | 02 | **NEW.** Plan 02 verification section says `grep -c "S-1-5-18" dlp-agent/src/service.rs` returns 1. In the current source, this returns 2: one at line 1412 (`S-1-5-18-test`) and one at line 3364 (`S-1-5-18`). The plan's verification command does not account for the audit context line. | Update the verification command to `grep -c "S-1-5-18" dlp-agent/src/service.rs` returning 2, with a note that one is the test stub and one is the audit context default. Or use `grep -c "S-1-5-18-test"` returning 1 for the stub specifically. |
| L-12 | 01 | **NEW.** Plan 01 success criteria says "All four frame types (Request, PullDiagnostics, PullHealth, RequestOverride) route through the consolidated server." The `BypassAlert` and `JournalDegraded` frames also route through the same server (via `bypass_tx`). While these are not DIFF handlers, they are part of the consolidated server's responsibility. The success criteria could be more precise. | Update success criteria to say "All four DIFF frame types (Request, PullDiagnostics, PullHealth, RequestOverride) route through the consolidated server, and BypassAlert/JournalDegraded frames continue to route through the bypass channel." |

---

## Unresolved HIGH Count

**0** (all previous HIGH findings are either resolved or acknowledged with follow-up tasks)

Note: H-02 and H-03 are marked as "partially resolved" because the replan acknowledged the gaps and added tasks to address them, but the tests do not yet exist in source. These are execution gaps, not plan gaps — the plans correctly describe what needs to be done. If the definition of "unresolved" requires the tests to actually exist, then the count would be 2. However, per the review instructions, a finding is "actionable" if it requires a change to a plan file. H-02 and H-03 do not require plan changes; they require execution of the existing plan tasks.

---

## Actionable Non-HIGH Count

**6** (M-07, M-08, M-09, M-10, L-08, L-09, L-10, L-11, L-12 — but only M-07, M-08, M-09, M-10, L-08, L-09, L-10, L-11, L-12 are actionable; however, L-08 through L-12 are LOW and may be considered optional)

Actionable items that should be fixed in plan files:
- M-07: Add note about `MockCache` accessibility or shared stub
- M-08: Add `warn!` verification step or manual acknowledgment
- M-09: Clarify cloning happens in function body, not closure
- M-10: Update export reference to actual module name `hook_ipc_config`
- L-08: Clarify extending existing test block
- L-09: Add unique pipe name to config example
- L-10: Recommend new file for routing test
- L-11: Fix verification grep count
- L-12: Clarify success criteria for bypass frames

If we count only MEDIUM and above as "actionable non-HIGH", the count is **4** (M-07, M-08, M-09, M-10).

---

## Verdict

**PASS**

The previous HIGH findings (H-01, H-02, H-03) are resolved in the plan files. H-02 and H-03 are acknowledged as execution gaps with follow-up tasks (Task 4 in Plan 03 and Task 1 in Plan 03, respectively). No new HIGH findings were introduced by the replan. The remaining issues are MEDIUM and LOW, all of which are minor clarifications or documentation improvements that do not block execution. The plans are directionally correct and match the current codebase reality.

---

## CYCLE_SUMMARY: current_high=0 current_actionable=4

## Current HIGH Concerns
None.

## Current Actionable Non-HIGH Concerns
- M-07: Plan 03 Task 2 references `MockCache` which is not exported from `hook_ipc.rs` tests; integration tests need their own stub or a shared module.
- M-08: Plan 03 Task 3 does not verify the `warn!` for missing volume classes on COPY/MOVE; add tracing capture or manual acknowledgment.
- M-09: Plan 02 Task 2 step 1 wording about cloning dependencies into the closure is ambiguous; clarify cloning happens in `spawn_hook_ipc_server` function body.
- M-10: Plan 01 Task 1 step 4 does not mention the actual module name `hook_ipc_config` used in `lib.rs`; update reference.
