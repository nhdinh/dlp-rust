---
status: complete
phase: 58.2-fix-double-hookipcserver-and-wire-volume-classes
source: 58.2-01-SUMMARY.md, 58.2-02-SUMMARY.md, 58.2-03-SUMMARY.md
started: 2026-06-25T00:00:00Z
updated: 2026-06-25T00:08:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Single HookIpcServer instance per agent lifecycle
expected: |
  Inspect dlp-agent/src/service.rs and confirm: BlockingThreads has no hook_ipc field, run_loop_init calls spawn_hook_ipc_server exactly once, and run_loop_shutdown joins only ctx.hook_ipc_handle. Optionally run `cargo test -p dlp-agent --lib test_spawn_hook_ipc_server` and confirm 1 passed.
result: pass
notes: |
  Auto-verified: BlockingThreads has no hook_ipc field (only hook_ipc_handle in RunLoopContext at line 1087). spawn_hook_ipc_server has exactly one production call site at run_loop_init line 1646. run_loop_shutdown joins only ctx.hook_ipc_handle at line 3697. Unit test passed per user.

### 2. All DIFF handlers route through consolidated server
expected: |
  Run `cargo test -p dlp-agent --test hook_ipc_integration -- --test-threads=1`. All 5 tests pass: Request, PullDiagnostics, PullHealth, RequestOverride, and volume_class_allow_deny.
result: pass

### 3. Approval cache override preserved on hook path
expected: |
  Inspect the handler closure inside spawn_hook_ipc_server (around service.rs:1350). Confirm it builds ApprovalCacheKey from caller_sid + path + action and returns HookResponse with approval_override: Some(true) on cache hit.
result: pass

### 4. Volume-class fields forward unchanged into EvaluateRequest
expected: |
  Run `cargo test -p dlp-agent --test volume_class_integration -- --test-threads=1`. All 14 tests pass (1 ignored). Inspect hook_request_to_evaluate_request and confirm source_volume_class and destination_volume_class are copied directly from HookRequest.
result: pass

### 5. Hook action strings map to correct ABAC variants
expected: |
  Run `cargo test -p dlp-agent --lib test_map_hook_action_to_abac -- --test-threads=1`. Confirm 1 passed. Verify map_hook_action_to_abac covers all 12 action variants with READ fallback.
result: pass

### 6. PID-to-SID identity resolution fails closed
expected: |
  Run `cargo test -p dlp-agent --lib test_get_caller_sid -- --test-threads=1`. Confirm 1 passed. Verify handler closure returns decision: DENY with reason "identity resolution failed" when get_caller_sid returns None.
result: pass

### 7. COPY/MOVE with None volume class logs a warning
expected: |
  Inspect the handler closure inside spawn_hook_ipc_server (around service.rs:1362-1368). Confirm tracing::warn! is emitted when action is COPY or MOVE and source_volume_class or destination_volume_class is None.
result: pass

### 8. Project builds and existing tests remain green
expected: |
  Run `cargo check -p dlp-agent --tests` with zero errors, then `cargo test -p dlp-agent --lib` and confirm all targeted lib tests pass (hook_request_to_evaluate_request, map_hook_action_to_abac, get_caller_sid, spawn_hook_ipc_server).
result: pass
notes: |
  cargo check -p dlp-agent --tests passed with zero errors. Full `cargo test -p dlp-agent --lib` hangs on pre-existing flaky test hook_ipc::tests::test_handle_connection_volume_class_query_resolves_class (documented in 58.2-02-SUMMARY as unrelated environmental named-pipe timing issue). All phase-specific targeted lib tests pass individually.

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
