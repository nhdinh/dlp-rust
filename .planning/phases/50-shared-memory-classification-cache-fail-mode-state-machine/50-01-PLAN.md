---
phase: 50-shared-memory-classification-cache-fail-mode-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/hook_ipc.rs
  - dlp-common/src/lib.rs
  - dlp-common/Cargo.toml
autonomous: true
requirements:
  - CACHE-03
must_haves:
  truths:
    - HookRequest carries cache_version for stale-DLL detection
    - HookResponse carries cache_hint for LRU warming
    - HookOp enum distinguishes read vs write operations
    - Bincode serialization remains backward-compatible
  artifacts:
    - path: dlp-common/src/hook_ipc.rs
      provides: Extended HookRequest/HookResponse/HookOp types
      exports: ["HookRequest", "HookResponse", "HookOp", "CacheHint"]
    - path: dlp-common/src/lib.rs
      provides: Re-exports for downstream crates
  key_links:
    - from: dlp-common/src/hook_ipc.rs
      to: dlp-agent/src/hook_ipc.rs
      via: import of HookRequest/HookResponse
    - from: dlp-common/src/hook_ipc.rs
      to: dlp-hook-dll/src/pipe_client.rs
      via: bincode serialization round-trip
---

<objective>
Extend the IPC protocol between hook DLL and agent to support cache versioning, cache hint warming, and operation-type discrimination.

Purpose: Enable the agent to detect stale DLLs (via cache_version), warm the DLL's LRU on pipe round-trips (via cache_hint), and distinguish read vs write operations for tier-gated fast-path decisions.
Output: Extended HookRequest/HookResponse types with backward-compatible bincode serialization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (CACHE-03 requirement)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-CONTEXT.md (D-03, D-12 decisions)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-RESEARCH.md (Pattern 1: bincode additive fields)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-REVIEWS.md (HIGH: bincode compatibility risk)

## Review Feedback Addressed
- **Bincode compatibility (HIGH):** Use versioned IPC envelope instead of relying on additive fields alone. Add explicit `protocol_version: u8` field to HookRequest.
- **CacheDelta inconsistency (HIGH):** Remove HookMessage::CacheDelta variant entirely. Cache updates are via SHM atomic flip only.
</context>

<interfaces>
From dlp-common/src/hook_ipc.rs (existing):
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct HookRequest {
    pub path: String,
    pub action: String,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct HookResponse {
    pub decision: crate::Decision,
    pub reason: String,
}
```

From dlp-common/src/classification.rs:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum Classification {
    T1, T2, T3, T4,
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add HookOp enum and CacheHint type</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <action>
    Add `HookOp` enum with variants `Read` and `Write` to distinguish operation types for tier-gated fast-path decisions.
    Add `CacheHint` struct with fields: `path: PathBuf`, `tier: Classification`, `ttl_secs: u32`.
    Derive `Debug`, `Clone`, `PartialEq`, `Serialize`, `Deserialize` for both.
    Place new types BEFORE existing structs in the file.
  </action>
  <verify>
    <automated>cargo test -p dlp-common hook_ipc --lib</automated>
  </verify>
  <done>
    - `HookOp` enum exists with `Read` and `Write` variants
    - `CacheHint` struct exists with correct fields
    - Both derive required traits
    - Existing tests still pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend HookRequest with cache_version and protocol_version</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <action>
    Extend `HookRequest` with additive fields:
    - `cache_version: u64` (default 0 for backward compatibility)
    - `protocol_version: u8` (set to 1 for this revision)
    - `op: HookOp` (default HookOp::Write for backward compatibility)
    Use `#[serde(default)]` on new fields. Add doc comments explaining each field.
    The agent uses `cache_version` to detect stale DLLs and proactively push fresh cache data.
    The DLL sends its last seen cache_version so the agent can detect staleness.
  </action>
  <verify>
    <automated>cargo test -p dlp-common hook_ipc::tests::request_roundtrip --lib</automated>
  </verify>
  <done>
    - `HookRequest` has `cache_version: u64` with `#[serde(default)]`
    - `HookRequest` has `protocol_version: u8` with `#[serde(default)]`
    - `HookRequest` has `op: HookOp` with `#[serde(default)]`
    - Old serialized HookRequest (without new fields) deserializes correctly
    - New HookRequest serializes and round-trips correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend HookResponse with cache_hint and cache_version</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <action>
    Extend `HookResponse` with additive fields:
    - `cache_hint: Option<CacheHint>` (default None)
    - `cache_version: u64` (default 0)
    Use `#[serde(default)]` on new fields. Add doc comments.
    When the agent classifies a path not in cache, it returns the classification + TTL in `cache_hint`.
    The DLL stores this in its thread-local LRU for future lookups.
    `cache_version` tells the DLL what version the agent considers current.
  </action>
  <verify>
    <automated>cargo test -p dlp-common hook_ipc::tests::response_roundtrip --lib</automated>
  </verify>
  <done>
    - `HookResponse` has `cache_hint: Option<CacheHint>` with `#[serde(default)]`
    - `HookResponse` has `cache_version: u64` with `#[serde(default)]`
    - Old serialized HookResponse deserializes correctly
    - New HookResponse with cache_hint serializes and round-trips correctly
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove HookMessage::CacheDelta variant</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <action>
    Remove the `HookMessage::CacheDelta` variant if it exists in the `HookMessage` enum.
    Per review feedback, cache updates are via shared-memory atomic version flip only.
    No pipe broadcast for cache deltas. Update any match arms that reference CacheDelta.
    If CacheDelta does not exist yet, add a comment to the HookMessage enum documenting
    that cache updates are SHM-only and no pipe broadcast variant exists by design.
  </action>
  <verify>
    <automated>grep -c "CacheDelta" dlp-common/src/hook_ipc.rs || echo "0"</automated>
  </verify>
  <done>
    - No `CacheDelta` variant in `HookMessage` enum
    - Comment documents SHM-only cache update design
    - All match arms compile without CacheDelta
  </done>
</task>

<task type="auto">
  <name>Task 5: Add protocol compatibility tests</name>
  <files>dlp-common/src/hook_ipc.rs</files>
  <action>
    Add unit tests for backward/forward compatibility:
    1. Serialize old HookRequest (path, action only), deserialize with new code — should succeed with defaults
    2. Serialize new HookRequest (with all fields), deserialize with new code — should succeed
    3. Same for HookResponse
    4. Verify protocol_version field exists and defaults to 1
    Use `bincode::serialize` and `bincode::deserialize` for round-trip tests.
  </action>
  <verify>
    <automated>cargo test -p dlp-common hook_ipc --lib</automated>
  </verify>
  <done>
    - Test `old_request_deserializes_with_defaults` passes
    - Test `new_request_roundtrips` passes
    - Test `old_response_deserializes_with_defaults` passes
    - Test `new_response_roundtrips` passes
    - Test `protocol_version_defaults_to_one` passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DLL -> Agent pipe | Untrusted input crosses here; bincode deserialization must not panic on malformed data |
| Agent -> DLL pipe | Trusted output; but DLL must validate all fields before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Tampering | HookRequest deserialization | mitigate | serde(default) ensures missing fields don't cause deserialization failure; protocol_version allows future evolution |
| T-50-02 | Information Disclosure | CacheHint in HookResponse | accept | CacheHint contains classification tier only (T1-T4), not sensitive policy details |
| T-50-03 | Denial of Service | Bincode deserialization of malformed pipe data | mitigate | Length limits on strings; deserialize into bounded types; catch deserialization errors |
</threat_model>

<verification>
- `cargo test -p dlp-common --lib` passes all hook_ipc tests
- `cargo clippy -p dlp-common -- -D warnings` is clean
- `cargo build -p dlp-common` succeeds
- Bincode round-trip tests verify backward compatibility
</verification>

<success_criteria>
- HookRequest carries cache_version, protocol_version, and op fields with serde(default)
- HookResponse carries cache_hint and cache_version fields with serde(default)
- Old serialized requests/responses deserialize correctly with new code
- No CacheDelta variant in HookMessage
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-01-SUMMARY.md` when done.
</output>
