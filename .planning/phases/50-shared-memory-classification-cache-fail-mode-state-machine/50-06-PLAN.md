---
phase: 50-shared-memory-classification-cache-fail-mode-state-machine
plan: 06
type: execute
wave: 3
depends_on: [50-01, 50-02, 50-03, 50-04, 50-05]
files_modified:
  - dlp-agent/src/hook_ipc.rs
  - dlp-agent/src/service.rs
  - dlp-common/src/hook_ipc.rs
  - dlp-e2e/tests/bincode_compat.rs
  - dlp-e2e/tests/cache_benchmark.rs
  - dlp-e2e/tests/phase50_requirements.rs
  - Cargo.toml
autonomous: true
requirements:
  - CACHE-03
  - CACHE-04
  - FAIL-01
  - FAIL-03
must_haves:
  truths:
    - Agent handler reads cache_version from HookRequest, detects stale DLLs
    - HookResponse carries cache_hint for LRU warming and cache_version for sync
    - Agent rebuilds cache on policy change, atomic flip publishes new version
    - p95 cache-hit latency <= 50us validated by benchmark harness
    - CRIT-04 overhead <= 25% on cargo build workload
    - Bincode backward compatibility verified (old request/responses deserialize with defaults)
    - All 9 requirements (CACHE-01..06, FAIL-01..03) mapped to tests
    - Adversarial tests cover corrupt SHM, rapid version flips, malformed headers, path bypass attempts
    - Injected-process benchmark exercises actual hook DLL reader path
    - Cache stores classification HINT only; ABAC authority is never bypassed
  artifacts:
    - path: dlp-agent/src/hook_ipc.rs
      provides: Extended IPC handler with cache_version awareness and cache_hint warming
      exports: ["handle_hook_request"]
    - path: dlp-e2e/tests/cache_benchmark.rs
      provides: p95 latency benchmark, hit-rate benchmark, CRIT-04 overhead benchmark, injected-process benchmark
      exports: []
    - path: dlp-e2e/tests/bincode_compat.rs
      provides: Bincode backward/forward compatibility tests with golden fixtures
      exports: []
    - path: dlp-e2e/tests/phase50_requirements.rs
      provides: Requirement-to-test mapping for all 9 requirements
      exports: []
  key_links:
    - from: dlp-agent/src/hook_ipc.rs
      to: dlp-common/src/hook_ipc.rs
      via: HookRequest/HookResponse serialization round-trip
    - from: dlp-agent/src/hook_ipc.rs
      to: dlp-agent/src/classification_cache.rs
      via: Reads current cache_version from ClassificationCache for HookResponse
    - from: dlp-agent/src/cache_pusher.rs
      to: dlp-agent/src/classification_cache.rs
      via: Rebuild trigger on policy change
---

<objective>
Integrate the shared-memory classification cache and fail-mode state machine across agent and hook DLL, with cache_version-aware IPC handling, cache_hint warming, policy-change rebuild triggers, and benchmark validation for p95 <= 50us and CRIT-04 <= 25% overhead. Include adversarial tests and injected-process benchmarks.

Purpose: Wire together all Phase 50 components (Plans 01-05) into a working system. Validate performance targets with reproducible benchmarks. Ensure security with adversarial test coverage.
Output: Extended agent IPC handler, integration tests, benchmark harness, and requirement-mapped acceptance tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (CACHE-03, CACHE-04, FAIL-01, FAIL-03)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-CONTEXT.md (D-11, D-13, D-16)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-RESEARCH.md (Validation Architecture)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-PATTERNS.md (Agent IPC handler pattern)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-REVIEWS.md (HIGH: bincode compatibility; MEDIUM: benchmark gates, adversarial tests, injected-process benchmark)

## Review Feedback Addressed
- **Bincode compatibility (HIGH):** Use versioned IPC envelope with protocol_version field. All new fields have #[serde(default)]. Compatibility tests verify old/new round-trips. Golden fixtures added.
- **CacheDelta inconsistency (HIGH):** No HookMessage::CacheDelta variant. Cache updates are via SHM atomic flip only. Agent does not send cache deltas through pipe.
- **Benchmark gates (MEDIUM):** Micro benchmark (p50/p95/p99 cache-hit latency with QPC), macro benchmark (cargo build overhead), and injected-process benchmark (actual hook DLL path) are separate test targets.
- **ABAC bypass (HIGH):** Cache stores classification hint only. Agent IPC handler still performs full ABAC evaluation on pipe round-trip. Cache hit = fast-path tier-gated decision; cache miss = full ABAC.
- **Adversarial tests (MEDIUM):** Add corrupt SHM, rapid version flips, partial writes, malformed headers, path bypass attempts, symlink/junction attacks.
- **CRIT-04 methodology (MEDIUM):** Define fixture project, number of runs, warmup policy, acceptable variance, CPU pinning.
- **Cache non-authoritative invariant:** Document that cache stores classification HINT only. ABAC authority is never bypassed.
</context>

<interfaces>
From dlp-common/src/hook_ipc.rs (from Plan 50-01):
```rust
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct HookRequest {
    pub path: String,
    pub action: String,
    pub cache_version: u64,
    pub protocol_version: u8,
    pub op: HookOp,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct HookResponse {
    pub decision: crate::Decision,
    pub reason: String,
    pub cache_hint: Option<CacheHint>,
    pub cache_version: u64,
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct CacheHint {
    pub path: std::path::PathBuf,
    pub tier: Classification,
    pub ttl_secs: u32,
}
```

From dlp-agent/src/classification_cache.rs (from Plan 50-02):
```rust
pub struct ClassificationCache {
    header: *mut CacheHeader,
    // ...
}

impl ClassificationCache {
    pub fn current_version(&self) -> u64 {
        let word = unsafe { (*self.header).version_word.load(Ordering::Acquire) };
        word >> 1
    }
}
```

From dlp-agent/src/service.rs (existing IPC startup):
```rust
crate::ipc::start_all()?;
info!("IPC pipe servers started");
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend agent IPC handler with cache_version awareness</name>
  <files>dlp-agent/src/hook_ipc.rs</files>
  <read_first>
    - dlp-agent/src/hook_ipc.rs (current state)
    - dlp-common/src/hook_ipc.rs (HookRequest/HookResponse types)
    - dlp-agent/src/classification_cache.rs (ClassificationCache::current_version)
  </read_first>
  <action>
    Modify `dlp-agent/src/hook_ipc.rs` to handle extended HookRequest:

    `handle_hook_request(req: HookRequest) -> Result<HookResponse, Error>`:
    1. Read current cache_version from ClassificationCache::current_version().
    2. If req.cache_version < current_version:
       - tracing::info!("stale DLL detected: req.version={}, current={}", ...)
       - The DLL will detect version mismatch on next cache lookup and refresh.
    3. Perform full ABAC evaluation (existing logic):
       - Look up path classification from policy_store.
       - Evaluate ABAC rules against subject/resource/action/environment.
       - Determine Decision (ALLOW / DENY / AllowWithLog).
    4. Build cache_hint:
       - If path is classified (has a tier), create CacheHint { path, tier, ttl_secs }.
       - ttl_secs based on tier: T4=30, T3=60, T2=300, T1=1800.
       - If unclassified, cache_hint = None.
    5. Return HookResponse {
         decision,
         reason,
         cache_hint,
         cache_version: current_version,
       }.

    Document: cache_version in request lets agent detect stale DLLs (for telemetry/audit only).
    Document: cache_hint in response warms DLL's thread-local LRU.
    Document: ABAC evaluation ALWAYS occurs on pipe round-trip — cache hit only skips pipe for tier-gated fast path.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent hook_ipc::tests::cache_version_handling --lib</automated>
  </verify>
  <done>
    - handle_hook_request reads current cache_version from ClassificationCache
    - Detects stale DLLs when req.cache_version < current_version
    - Performs full ABAC evaluation (no bypass)
    - Returns cache_hint with tier and ttl_secs
    - Returns current cache_version in HookResponse
    - Tests verify stale detection and cache_hint generation
  </done>
  <acceptance_criteria>
    - `grep "fn handle_hook_request" dlp-agent/src/hook_ipc.rs` returns a match
    - `grep "cache_version" dlp-agent/src/hook_ipc.rs` returns a match
    - `grep "stale DLL detected" dlp-agent/src/hook_ipc.rs` returns a match
    - `cargo test -p dlp-agent hook_ipc::tests::cache_version_handling --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement policy-change cache rebuild trigger</name>
  <files>dlp-agent/src/cache_pusher.rs, dlp-agent/src/service.rs</files>
  <read_first>
    - dlp-agent/src/cache_pusher.rs (CachePusher interface)
    - dlp-agent/src/service.rs (existing startup sequence)
    - dlp-agent/src/classification_cache.rs (ClassificationCache::rebuild)
  </read_first>
  <action>
    Ensure cache_pusher.rs (from Plan 50-02) integrates with service startup:

    In `dlp-agent/src/service.rs`:
    1. After ClassificationCache initialization, create CachePusher:
       ```rust
       let cache_pusher = Arc::new(CachePusher::new(classification_cache.clone()));
       ```
    2. Start CachePusher background thread:
       ```rust
       cache_pusher.start();
       ```
    3. Store both Arc<ClassificationCache> and cache_pusher in service state.

    In `dlp-agent/src/cache_pusher.rs`:
    - `on_policy_change()` collects all T3/T4 protected paths from policy_store.
    - Builds cache entries: Vec<(CacheKey, Classification, ttl_secs)>.
    - Calls `classification_cache.rebuild(entries)`.
    - On success: new version published via atomic flip.
    - On failure: tracing::error! and emit siem.cache_rebuild_failed event.
    - Debounce: 500ms timer prevents rapid successive rebuilds.

    Document: cache rebuild is rare (policy changes are infrequent).
    Document: rebuild holds RwLock briefly — atomic flip is the only hot-path operation.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent cache_pusher::tests::rebuild_trigger --lib</automated>
  </verify>
  <done>
    - CachePusher started in service startup sequence
    - on_policy_change rebuilds cache with T3/T4 paths
    - Atomic flip publishes new version
    - 500ms debounce prevents thrashing
    - Error telemetry on rebuild failure
    - Tests verify rebuild trigger and debounce
  </done>
  <acceptance_criteria>
    - `grep "CachePusher" dlp-agent/src/service.rs` returns a match
    - `grep "cache_pusher.start" dlp-agent/src/service.rs` returns a match
    - `cargo test -p dlp-agent cache_pusher::tests::rebuild_trigger --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add bincode compatibility integration tests</name>
  <files>dlp-e2e/tests/bincode_compat.rs</files>
  <read_first>
    - dlp-common/src/hook_ipc.rs (HookRequest/HookResponse types with IpcEnvelope)
    - dlp-e2e/Cargo.toml (test dependencies)
  </read_first>
  <action>
    Create `dlp-e2e/tests/bincode_compat.rs`:

    Tests:
    1. `old_request_deserializes_with_defaults`:
       - Serialize old HookRequest { path, action } (without cache_version, protocol_version, op).
       - Deserialize with new code.
       - Assert defaults: cache_version=0, protocol_version=1 (serde default), op=Write.
    2. `new_request_roundtrips`:
       - Serialize new HookRequest with all fields.
       - Deserialize and assert all fields preserved.
    3. `old_response_deserializes_with_defaults`:
       - Serialize old HookResponse { decision, reason }.
       - Deserialize with new code.
       - Assert defaults: cache_hint=None, cache_version=0.
    4. `new_response_roundtrips`:
       - Serialize new HookResponse with cache_hint and cache_version.
       - Deserialize and assert all fields preserved.
    5. `protocol_version_defaults_to_one`:
       - Create HookRequest with only path and action.
       - Assert protocol_version == 1.
    6. `envelope_v1_roundtrip`:
       - Serialize IpcEnvelope::V1 with request payload.
       - Deserialize and assert equality.
    7. `golden_fixture_stability`:
       - Load pre-computed golden bincode bytes from file.
       - Deserialize and assert expected values.
       - Golden fixtures stored in dlp-e2e/fixtures/ directory.
    8. `unknown_version_fallback`:
       - Document that unknown protocol versions trigger pipe-only fallback.

    Document: these tests verify backward compatibility for rolling upgrades (old DLL + new agent, new DLL + old agent).
  </action>
  <verify>
    <automated>cargo test -p dlp-e2e bincode_compat --test bincode_compat</automated>
  </verify>
  <done>
    - 8 compatibility tests covering old/new request/response round-trips
    - protocol_version defaults to 1
    - Golden fixture tests verify serialization stability
    - All old serialized data deserializes correctly with new code
    - Tests run in dlp-e2e integration test suite
  </done>
  <acceptance_criteria>
    - `ls dlp-e2e/fixtures/*.bin` shows golden fixture files
    - `cargo test -p dlp-e2e bincode_compat --test bincode_compat` exits 0
    - All 8 tests pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Implement p95 cache-hit latency micro-benchmark</name>
  <files>dlp-e2e/tests/cache_benchmark.rs</files>
  <read_first>
    - dlp-agent/src/classification_cache.rs (ClassificationCache interface)
    - dlp-hook-dll/src/classification_cache.rs (CacheLookup interface)
  </read_first>
  <action>
    Create `dlp-e2e/tests/cache_benchmark.rs`:

    `cache_hit_latency_benchmark()`:
    1. Create ClassificationCache in agent process.
    2. Pre-populate with 1000 entries (mix of T1-T4, prefix + hash).
    3. Simulate DLL cache lookup (read-only mapping):
       - OpenFileMappingW + MapViewOfFile.
       - Perform 10,000 cache lookups.
       - Measure each lookup with QueryPerformanceCounter.
    4. Compute p50, p95, p99 from QPC samples.
    5. Convert QPC ticks to microseconds (using QPFrequency).
    6. Assert p95 <= 50us.
    7. Print histogram for manual review.

    `cache_hit_rate_benchmark()`:
    1. Pre-populate cache with 500 entries.
    2. Perform 10,000 lookups (80% from cached set, 20% random).
    3. Measure hit rate.
    4. Assert hit rate >= 80%.

    `injected_process_benchmark()`:
    1. Inject hook DLL into a test process.
    2. Trigger file operations that exercise the actual hook path.
    3. Measure end-to-end latency including path normalization, cache lookup, and decision.
    4. Assert p95 <= 50us for cache-hit paths.
    5. Document: this benchmark validates the real hook DLL reader path, not synthetic lookups.

    Document: benchmark runs as integration test (not unit test) due to Windows API requirements.
    Document: p95 <= 50us is the CRIT-04 performance gate.
    Document: run with --release for accurate measurements.
  </action>
  <verify>
    <automated>cargo test -p dlp-e2e cache_benchmark --test cache_benchmark --release</automated>
  </verify>
  <done>
    - cache_hit_latency_benchmark measures p50/p95/p99 in microseconds
    - p95 assertion <= 50us
    - cache_hit_rate_benchmark verifies >= 80% hit rate
    - injected_process_benchmark exercises actual hook DLL path
    - Uses actual Windows shared-memory APIs
    - Runs in release mode for accurate timing
  </done>
  <acceptance_criteria>
    - `grep "fn cache_hit_latency_benchmark" dlp-e2e/tests/cache_benchmark.rs` returns a match
    - `grep "fn injected_process_benchmark" dlp-e2e/tests/cache_benchmark.rs` returns a match
    - `cargo test -p dlp-e2e cache_benchmark --test cache_benchmark --release` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Implement CRIT-04 macro overhead benchmark</name>
  <files>dlp-e2e/tests/cache_benchmark.rs</files>
  <read_first>
    - dlp-e2e/tests/cache_benchmark.rs (current state after Task 4)
  </read_first>
  <action>
    Add CRIT-04 workload benchmark to cache_benchmark.rs:

    `build_workload_overhead_benchmark()`:
    1. Baseline: run `cargo build --workspace` without hook DLL injected.
       - Measure wall-clock time (3 runs, take median).
    2. With hook: run same build with hook DLL injected into cargo/rustc processes.
       - Measure wall-clock time (3 runs, take median).
    3. Compute overhead = (with_hook - baseline) / baseline * 100%.
    4. Assert overhead <= 25%.
    5. Print baseline, with_hook, overhead percentages.

    Benchmark methodology:
    - Fixture project: use dlp-rust workspace itself as the build target
    - Number of runs: 3 (with outlier rejection)
    - Warmup: 1 warmup run before measured runs
    - CPU pinning: document if available (not required)
    - Acceptable variance: median of 3 runs; if variance > 10%, increase to 5 runs
    - Environment: Windows 11, release mode, no antivirus interference

    Document: this is a macro benchmark validating real-world build workload impact.
    Document: 25% overhead is the CRIT-04 requirement (mitigated by allowlist + cache fast path).
    Document: benchmark requires running on Windows with agent service active.
    Document: mark as #[ignore] if run in CI without Windows agent.
  </action>
  <verify>
    <automated>cargo test -p dlp-e2e build_workload_overhead --test cache_benchmark --release -- --ignored</automated>
  </verify>
  <done>
    - build_workload_overhead_benchmark measures cargo build with/without hook
    - Overhead assertion <= 25%
    - 3-run median for stability
    - Methodology documented (fixture, runs, warmup, variance)
    - Marked #[ignore] for CI environments without Windows agent
    - Prints detailed timing breakdown
  </done>
  <acceptance_criteria>
    - `grep "fn build_workload_overhead_benchmark" dlp-e2e/tests/cache_benchmark.rs` returns a match
    - `grep "#\[ignore" dlp-e2e/tests/cache_benchmark.rs` returns a match
    - `cargo test -p dlp-e2e build_workload_overhead --test cache_benchmark --release -- --ignored` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Add adversarial and negative test coverage</name>
  <files>dlp-e2e/tests/phase50_requirements.rs</files>
  <read_first>
    - dlp-e2e/tests/phase50_requirements.rs (if exists from prior work)
    - dlp-hook-dll/src/classification_cache.rs (CacheLookup interface)
    - dlp-hook-dll/src/fail_mode.rs (FailModeState interface)
  </read_first>
  <action>
    Add adversarial tests to `dlp-e2e/tests/phase50_requirements.rs`:

    Adversarial tests:
    1. `corrupt_shm_rejected`: Corrupt shared memory header (bad magic), verify DLL treats as cache miss
    2. `rapid_version_flips`: Perform 100 rapid cache rebuilds, verify DLL handles version flips correctly
    3. `partial_write_simulation`: Simulate agent crash during rebuild (odd version left), verify DLL recovers
    4. `malformed_header_rejected`: Corrupt checksum, layout_version, or offsets, verify DLL rejects
    5. `path_bypass_eight_three`: Use 8.3 short name, verify forces pipe fallback
    6. `path_bypass_symlink`: Use symlink path, verify forces pipe fallback
    7. `path_bypass_junction`: Use junction path, verify forces pipe fallback
    8. `path_bypass_volume_guid`: Use volume GUID path, verify forces pipe fallback
    9. `path_bypass_ads`: Use ADS stream path, verify forces pipe fallback
    10. `path_bypass_trailing_dots`: Use trailing dots, verify forces pipe fallback
    11. `cache_hint_non_authoritative`: Verify that cache hit does NOT bypass ABAC evaluation on pipe
    12. `fail_mode_abac_invariant`: Verify that fail-open T1/T2 does NOT bypass ABAC DENY when agent is available

    Document: these tests verify security invariants under adversarial conditions.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-e2e phase50_requirements --test phase50_requirements</automated>
  </verify>
  <done>
    - 12 adversarial tests added
    - Tests cover corrupt SHM, rapid flips, partial writes, malformed headers
    - Tests cover path bypass attempts (8.3, symlink, junction, volume GUID, ADS, trailing dots)
    - Tests cover cache-hint non-authoritative invariant
    - Tests cover fail-mode ABAC invariant
    - All tests pass
  </done>
  <acceptance_criteria>
    - `grep "fn corrupt_shm_rejected" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `grep "fn path_bypass_symlink" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `grep "fn cache_hint_non_authoritative" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `cargo test -p dlp-e2e phase50_requirements --test phase50_requirements` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Map all 9 requirements to tests and verify</name>
  <files>dlp-e2e/tests/phase50_requirements.rs</files>
  <read_first>
    - dlp-e2e/tests/phase50_requirements.rs (current state after Task 6)
    - .planning/REQUIREMENTS.md (CACHE-01..06, FAIL-01..03)
  </read_first>
  <action>
    Ensure `dlp-e2e/tests/phase50_requirements.rs` covers all 9 requirements:

    Requirement-to-test mapping:
    - CACHE-01: `shared_memory_created` — verify Global\DlpClassificationCache exists with correct size and ACL
    - CACHE-02: `dll_maps_cache_readonly` — verify DLL opens FILE_MAP_READ only
    - CACHE-03: `hook_request_extended` — verify cache_version, protocol_version, op fields round-trip
    - CACHE-04: `cache_rebuild_on_policy_change` — verify policy change triggers rebuild + version flip
    - CACHE-05: `system_allowlist_bypass` — verify System32 paths bypass cache and pipe
    - CACHE-06: `build_tool_allowlist` — verify cargo.exe in trusted dir bypasses pipe
    - FAIL-01: `fail_mode_transitions` — verify HEALTHY->DEGRADED->ISOLATED->RESYNC->HEALTHY
    - FAIL-02: `asymmetric_fail` — verify T3/T4 deny, T1/T2 allow in ISOLATED
    - FAIL-03: `staleness_budgets` — verify T4=30s, T3=60s, T2=5min, T1=30min enforcement

    Each test:
    - Has doc comment linking to requirement ID.
    - Uses Arrange-Act-Assert pattern.
    - Mocks external dependencies where possible.

    Add requirement traceability table at top of file:
    | Requirement | Test Function | File |
    |-------------|---------------|------|
    | CACHE-01 | shared_memory_created | phase50_requirements.rs |
    | ... | ... | ... |

    Document: this file serves as the Phase 50 acceptance test suite.
    Document: all 9 requirements must pass for phase completion.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-e2e phase50_requirements --test phase50_requirements</automated>
  </verify>
  <done>
    - 9 tests, one per requirement
    - Each test has requirement ID in doc comment
    - Requirement traceability table documented
    - Covers all CACHE-01..06 and FAIL-01..03
    - All tests pass
  </done>
  <acceptance_criteria>
    - `grep "CACHE-01" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `grep "FAIL-03" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `grep "Requirement-to-test mapping" dlp-e2e/tests/phase50_requirements.rs` returns a match
    - `cargo test -p dlp-e2e phase50_requirements --test phase50_requirements` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|------------|-------------|
| Agent IPC handler -> DLL | Bincode serialization must not panic on malformed data |
| Policy change -> Cache rebuild | Rebuild must not leave cache in inconsistent state |
| Benchmark -> Production | Benchmarks use test data only; no real policy or classified paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-21 | Denial of Service | Benchmark runs exhaust shared memory | mitigate | Tests use small fixed-size mappings; cleanup after each test |
| T-50-22 | Information Disclosure | Benchmark prints classified path examples | mitigate | Benchmark uses synthetic paths only (e.g., C:\Test\File.txt) |
| T-50-23 | Tampering | Malformed bincode in integration test | mitigate | Tests use serialize->deserialize round-trip; no untrusted input |
| T-50-35 | Elevation of Privilege | Adversarial path bypass in tests | mitigate | Tests verify pipe fallback for all bypass attempts |
| T-50-36 | Tampering | Corrupt SHM causes test crash | mitigate | Tests verify graceful handling of corrupt headers |
</threat_model>

<verification>
- `cargo test -p dlp-agent --lib` passes all hook_ipc and cache_pusher tests
- `cargo test -p dlp-e2e --test bincode_compat` passes all compatibility tests
- `cargo test -p dlp-e2e --test cache_benchmark --release` passes p95, hit-rate, and injected-process benchmarks
- `cargo test -p dlp-e2e --test phase50_requirements` passes all 9 requirement tests + 12 adversarial tests
- `cargo clippy -p dlp-agent -p dlp-hook-dll -p dlp-common -- -D warnings` is clean
- `cargo build --workspace` succeeds
</verification>

<success_criteria>
- Agent IPC handler detects stale DLLs via cache_version comparison
- HookResponse carries cache_hint (tier + ttl_secs) and cache_version
- Cache rebuild triggered on policy change with 500ms debounce
- Bincode backward compatibility: old requests/responses deserialize with defaults
- Golden fixture tests verify serialization stability
- p95 cache-hit latency <= 50us (micro-benchmark)
- Cache hit rate >= 80% (micro-benchmark)
- Injected-process benchmark validates real hook DLL path <= 50us p95
- CRIT-04 build workload overhead <= 25% (macro-benchmark, #[ignore] in CI)
- All 9 requirements (CACHE-01..06, FAIL-01..03) have passing tests
- 12 adversarial tests cover corrupt SHM, path bypass, cache-hint invariant
- All workspace tests pass
- Cache non-authoritative invariant documented: ABAC authority never bypassed
</success_criteria>

<output>
Create `.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-06-SUMMARY.md` when done.
</output>
