---
phase: 50-shared-memory-classification-cache-fail-mode-state-machine
plan: 05
type: execute
wave: 3
depends_on: [50-03, 50-04]
files_modified:
  - dlp-hook-dll/src/allowlist.rs
  - dlp-hook-dll/src/perf_telemetry.rs
  - dlp-hook-dll/src/lib.rs
  - dlp-hook-dll/src/trampolines.rs
  - dlp-hook-dll/Cargo.toml
autonomous: true
requirements:
  - CACHE-05
  - CACHE-06
  - FAIL-01
must_haves:
  truths:
    - Hardcoded allowlist: System32, SysWOW64, WinSxS, WindowsApps, Program Files\Common Files
    - Build-tool allowlist: devenv.exe, cargo.exe, msbuild.exe, rustc.exe, link.exe, gcc.exe, cl.exe
    - Operator-extended allowlist in dedicated 64 KiB shared-memory region
    - Allowlist checked BEFORE cache lookup (fastest path)
    - Full-path matching for build tools (not basename-only)
    - Build-tool parent directories must not be user-writable
    - Code-signer validation for build tools (Microsoft / trusted publisher)
    - Audit events emitted for all allowlist hits on T3/T4 or fail-mode decisions
    - QPC latency histogram: 10us, 50us, 100us, 500us, 1ms, 5ms, 10ms buckets
    - Telemetry emitted every 1000 calls via pipe; state transitions emitted immediately
    - State transition audit events: siem.fail_mode_transition
    - Cache stores classification HINT only; ABAC authority is never bypassed
  artifacts:
    - path: dlp-hook-dll/src/allowlist.rs
      provides: Hardcoded + shared-memory allowlist check with code-signer validation and audit events
      exports: ["AllowlistChecker", "is_allowlisted"]
    - path: dlp-hook-dll/src/perf_telemetry.rs
      provides: QPC latency measurement and histogram aggregation with immediate state-transition emission
      exports: ["PerfTelemetry", "record_latency", "emit_telemetry"]
  key_links:
    - from: dlp-hook-dll/src/allowlist.rs
      to: dlp-hook-dll/src/trampolines.rs
      via: classify_and_log_path calls is_allowlisted before cache lookup
    - from: dlp-hook-dll/src/allowlist.rs
      to: dlp-agent/src/classification_cache.rs
      via: Reads operator-extended allowlist from shared-memory region
    - from: dlp-hook-dll/src/perf_telemetry.rs
      to: dlp-hook-dll/src/trampolines.rs
      via: classify_and_log_path calls record_latency around cache lookup
    - from: dlp-hook-dll/src/perf_telemetry.rs
      to: dlp-agent/src/hook_ipc.rs
      via: Emits aggregated telemetry via pipe round-trip
---

<objective>
Build the hook DLL's trusted-path allowlist (hardcoded + operator-extended) and performance telemetry (QPC latency histogram) with hardened path matching, code-signer validation, user-writable directory detection, and full-path validation for build tools.

Purpose: Eliminate cache lookup and pipe round-trip for trusted system paths and build tools (mitigates CRIT-04 build-workload death-spiral). Measure cache lookup latency for p95 validation. Strengthen allowlist against bypass attacks.
Output: allowlist.rs module with hardcoded and shared-memory allowlist, perf_telemetry.rs module with QPC histogram, integrated into trampolines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (CACHE-05, CACHE-06, FAIL-01)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-CONTEXT.md (D-09, D-10)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-RESEARCH.md (Pattern 5, Anti-patterns)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-PATTERNS.md (Allowlist matching, prefix matching, thread-local buffers)
@.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-REVIEWS.md (HIGH: security descriptor, allowlist bypass risk, code-signer validation; MEDIUM: allowlist bypass risk, telemetry cost, path normalization)

## Review Feedback Addressed
- **Security descriptor (HIGH):** Allowlist does not bypass enforcement — it bypasses cache lookup and pipe for known-trusted paths. Classification is still T1 (Public) for allowlisted paths. No security downgrade.
- **Allowlist bypass risk (HIGH):** Build-tool matching uses FULL PATH (not basename-only). Prevents `C:\Users\attacker\cargo.exe` from matching. Code-signer validation (Microsoft / trusted publisher) for build tools. Deny user-writable directories.
- **Allowlist audit (HIGH):** Emit audit events for all allowlist hits on T3/T4 or fail-mode decisions. Log allowlist hits explicitly for audit trail.
- **Path normalization (MEDIUM):** Allowlist matching uses same normalize_path function as cache lookup (consistent handling of NT paths, case-folding, trailing separators).
- **Telemetry cost (MEDIUM):** Histogram uses thread-local pre-allocated bucket array (no allocation). Emission every 1000 calls reduces pipe traffic. State transitions emitted immediately (separate from periodic histogram). QPC measurement is two register reads (~20ns).
- **Cache non-authoritative invariant:** Document that cache stores classification HINT only. ABAC authority is never bypassed. Allowlist is for performance optimization only.
</context>

<interfaces>
From dlp-agent/src/allowlist.rs (existing prefix matching):
```rust
fn prefix_match_directory_boundary(prefix: &str, path: &str) -> bool {
    let prefix_norm = prefix.trim_end_matches('\\').to_lowercase();
    let path_norm = path.to_lowercase();
    if let Some(rest) = path_norm.strip_prefix(&prefix_norm) {
        rest.is_empty() || rest.starts_with('\\')
    } else {
        false
    }
}
```

From dlp-hook-dll/src/trampolines.rs (existing classify_and_log_path):
```rust
fn classify_and_log_path(path: &str, action: &str, fn_name: &str) -> Option<crate::fail_closed::DenyReturn> {
    let decision = crate::classify_path(path, action, crate::DEFAULT_PIPE_NAME);
    // ... match decision ...
}
```

From dlp-agent/src/classification_cache.rs (shared-memory allowlist region):
```rust
// Operator-extended allowlist entries at allowlist_offset in shared memory
#[repr(C)]
pub struct AllowlistEntry {
    pub prefix_len: u16,
    pub prefix: [u8; 260],
    pub category: u8,  // 0=system, 1=build_tool, 2=operator
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement hardcoded system-path allowlist</name>
  <files>dlp-hook-dll/src/allowlist.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (existing classify_and_log_path flow)
    - dlp-agent/src/classification_cache.rs (AllowlistEntry struct for reference)
  </read_first>
  <action>
    Create `dlp-hook-dll/src/allowlist.rs` with hardcoded allowlist:

    `SYSTEM_PATHS: &[&str]` — static array of trusted path prefixes:
    - `C:\Windows\System32`
    - `C:\Windows\SysWOW64`
    - `C:\Windows\WinSxS`
    - `C:\Windows\WindowsApps`
    - `C:\Program Files\Common Files`
    - `C:\Program Files (x86)\Common Files`

    `is_system_allowlisted(path: &str) -> bool`:
    - Normalize path (reuse normalize_path from classification_cache.rs).
    - Check if path starts with any SYSTEM_PATHS prefix (case-insensitive, directory boundary).
    - Return true if matched.

    Document: these paths contain only Windows-signed system binaries and are never classified above T1.
    Document: allowlist check is the FIRST check in classify_and_log_path (before cache, before pipe).
    Document: allowlist is for performance optimization only; ABAC authority is never bypassed.
    Document: cache stores classification HINT only.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll allowlist::tests::system_paths --lib</automated>
  </verify>
  <done>
    - SYSTEM_PATHS array with 6 trusted prefixes
    - is_system_allowlisted uses normalize_path + prefix matching
    - Case-insensitive, directory-boundary aware
    - Tests cover match, non-match, and case variations
  </done>
  <acceptance_criteria>
    - `grep "SYSTEM_PATHS" dlp-hook-dll/src/allowlist.rs` returns a match
    - `grep "fn is_system_allowlisted" dlp-hook-dll/src/allowlist.rs` returns a match
    - `cargo test -p dlp-hook-dll allowlist::tests::system_paths --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement build-tool allowlist with full-path and signer validation</name>
  <files>dlp-hook-dll/src/allowlist.rs</files>
  <read_first>
    - dlp-hook-dll/src/allowlist.rs (current state after Task 1)
    - dlp-common/src/classification.rs (Classification enum)
  </read_first>
  <action>
    Implement build-tool allowlist with full-path matching and code-signer validation:

    `BUILD_TOOL_NAMES: &[&str]` — static array of executable basenames:
    - `devenv.exe`
    - `cargo.exe`
    - `msbuild.exe`
    - `rustc.exe`
    - `link.exe`
    - `gcc.exe`
    - `cl.exe`

    `BUILD_TOOL_PARENT_DIRS: &[&str]` — trusted parent directories:
    - `C:\Program Files\Microsoft Visual Studio`
    - `C:\Program Files (x86)\Microsoft Visual Studio`
    - `C:\Program Files\Microsoft.NET`
    - `C:\Rust`
    - `C:\msys64`
    - `C:\mingw`

    `TRUSTED_SIGNERS: &[&str]` — trusted code signer subjects:
    - `Microsoft Corporation`
    - `Microsoft Windows`
    - `Rust Project`

    `is_build_tool_allowlisted(path: &str) -> bool`:
    - Normalize path.
    - Extract basename (last component after final backslash).
    - Check basename against BUILD_TOOL_NAMES (case-insensitive).
    - If basename matches, verify parent directory starts with one of BUILD_TOOL_PARENT_DIRS.
    - If parent matches, verify code signer via WinVerifyTrust or GetAuthenticodeSubject:
      - Call WinVerifyTrust on the file path
      - Extract signer subject from certificate
      - Check against TRUSTED_SIGNERS list
    - Only return true if ALL three checks pass (basename + parent directory + signer).

    `is_user_writable_directory(path: &str) -> bool`:
    - Check if parent directory is under user-writable locations (e.g., `C:\Users`, `C:\Temp`, `C:\Windows\Temp`).
    - Return true if user-writable (deny allowlist).
    - This is a defense-in-depth check.

    Document: full-path validation prevents rename attacks (e.g., `C:\Users\attacker\cargo.exe`).
    Document: code-signer validation prevents binary substitution attacks.
    Document: user-writable directory check prevents planted-binary attacks.
    Document: build tools are classified T1 (Public) — they operate on source code, not sensitive data.
    Document: operator can extend build-tool list via shared-memory operator allowlist.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll allowlist::tests::build_tools --lib</automated>
  </verify>
  <done>
    - BUILD_TOOL_NAMES and BUILD_TOOL_PARENT_DIRS arrays
    - TRUSTED_SIGNERS array with Microsoft and Rust Project
    - is_build_tool_allowlisted checks basename + parent directory + signer
    - is_user_writable_directory rejects user-writable locations
    - Rejects renamed binaries in untrusted directories
    - Tests cover valid paths, rename attacks, signer mismatch, and case variations
  </done>
  <acceptance_criteria>
    - `grep "TRUSTED_SIGNERS" dlp-hook-dll/src/allowlist.rs` returns a match
    - `grep "fn is_build_tool_allowlisted" dlp-hook-dll/src/allowlist.rs` returns a match
    - `grep "fn is_user_writable_directory" dlp-hook-dll/src/allowlist.rs` returns a match
    - `cargo test -p dlp-hook-dll allowlist::tests::build_tools --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement operator-extended allowlist from shared memory</name>
  <files>dlp-hook-dll/src/allowlist.rs</files>
  <read_first>
    - dlp-hook-dll/src/allowlist.rs (current state after Tasks 1-2)
    - dlp-agent/src/classification_cache.rs (AllowlistEntry struct and shared-memory layout)
  </read_first>
  <action>
    Implement shared-memory operator allowlist:

    `is_operator_allowlisted(path: &str, header: &CacheHeader) -> bool`:
    - Read allowlist_count and allowlist_offset from CacheHeader.
    - Bounds-check: allowlist_offset + allowlist_count * size_of::<AllowlistEntry>() <= total_size.
    - Iterate allowlist_count entries of type AllowlistEntry.
    - For each entry: check if normalized path starts with entry.prefix (case-insensitive, directory boundary).
    - Return true on first match.
    - If header validation fails or allowlist_offset is 0, return false.

    `AllowlistEntry` (#[repr(C)]):
    - prefix_len: u16
    - prefix: [u8; 260]
    - category: u8  // 0=system, 1=build_tool, 2=operator
    - _pad: [u8; 5]

    `is_allowlisted(path: &str, header: Option<&CacheHeader>) -> (bool, Option<u8>)`:
    - Check system paths first.
    - Check build tools second.
    - Check operator extensions third (if header available).
    - Return (true, Some(category)) if allowlisted, (false, None) otherwise.

    Document: operator allowlist is in a dedicated 64 KiB region of shared memory.
    Document: operator entries are canonicalized by agent before writing (prevent `C:\Windows\System32Fake`).
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll allowlist::tests::operator_allowlist --lib</automated>
  </verify>
  <done>
    - is_operator_allowlisted reads from shared-memory allowlist region
    - AllowlistEntry struct with category field
    - is_allowlisted checks system -> build_tool -> operator in order
    - Returns category for telemetry/audit
    - Tests cover operator entry match and miss
  </done>
  <acceptance_criteria>
    - `grep "fn is_operator_allowlisted" dlp-hook-dll/src/allowlist.rs` returns a match
    - `grep "fn is_allowlisted" dlp-hook-dll/src/allowlist.rs` returns a match
    - `cargo test -p dlp-hook-dll allowlist::tests::operator_allowlist --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Implement allowlist audit logging</name>
  <files>dlp-hook-dll/src/allowlist.rs</files>
  <read_first>
    - dlp-hook-dll/src/allowlist.rs (current state after Tasks 1-3)
  </read_first>
  <action>
    Add audit logging for allowlist hits:

    `emit_allowlist_hit(path: &str, category: u8, decision_context: &str)`:
    - tracing::info! with path, category, decision_context
    - If pipe available, send `siem.allowlist_hit` event with:
      - path: string (normalized)
      - category: string ("system", "build_tool", "operator")
      - decision_context: string (e.g., "HEALTHY", "DEGRADED", "ISOLATED")
      - pid: current process ID
      - image_path: GetModuleFileNameW(NULL)
      - timestamp: current epoch seconds
    - Emit immediately (not batched) for audit integrity.
    - Only emit for T3/T4 paths or when in fail-mode (DEGRADED/ISOLATED/RESYNC).
    - T1/T2 allowlist hits in HEALTHY state are silent (too noisy).

    Document: audit events ensure allowlist usage is traceable.
    Document: silent T1/T2 HEALTHY hits prevent log flooding.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll allowlist::tests::audit_logging --lib</automated>
  </verify>
  <done>
    - emit_allowlist_hit logs via tracing and sends siem event
    - Emits immediately for T3/T4 or fail-mode contexts
    - Silent for T1/T2 HEALTHY hits
    - Tests verify audit emission and silence conditions
  </done>
  <acceptance_criteria>
    - `grep "fn emit_allowlist_hit" dlp-hook-dll/src/allowlist.rs` returns a match
    - `grep "siem.allowlist_hit" dlp-hook-dll/src/allowlist.rs` returns a match
    - `cargo test -p dlp-hook-dll allowlist::tests::audit_logging --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Implement QPC latency histogram</name>
  <files>dlp-hook-dll/src/perf_telemetry.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (existing classify_and_log_path flow)
  </read_first>
  <action>
    Create `dlp-hook-dll/src/perf_telemetry.rs`:

    `LATENCY_BUCKETS: &[u64]` — QPC tick thresholds (calibrated to ~us):
    - bucket 0: 0-10us
    - bucket 1: 10-50us
    - bucket 2: 50-100us
    - bucket 3: 100-500us
    - bucket 4: 500us-1ms
    - bucket 5: 1ms-5ms
    - bucket 6: 5ms-10ms
    - bucket 7: >10ms

    `PerfTelemetry` struct (thread_local):
    - buckets: [AtomicU64; 8]
    - call_count: AtomicU64
    - cache_hits: AtomicU64
    - cache_misses: AtomicU64

    `thread_local!`:
    ```rust
    thread_local! {
        static TELEMETRY: RefCell<PerfTelemetry> = RefCell::new(PerfTelemetry::new());
    }
    ```

    `record_latency(elapsed_qpc: u64, is_cache_hit: bool)`:
    - Increment call_count.
    - Increment cache_hits or cache_misses.
    - Find bucket index for elapsed_qpc and increment.
    - If call_count % 1000 == 0, call emit_telemetry().

    `emit_telemetry()`:
    - Read all bucket counters (reset to 0 after reading).
    - If pipe available, send telemetry via pipe as HookRequest extension or debug output.
    - If pipe unavailable, emit tracing::info! with aggregated data.
    - Include: pid, call_count, cache_hit_rate, p50/p95 bucket indices.

    `measure<F, T>(f: F) -> (T, u64)`:
    - Read QueryPerformanceCounter before and after calling f.
    - Return (result, elapsed_qpc).

    Document: QPC is used for sub-microsecond precision (required for p95 <= 50us validation).
    Document: telemetry emission every 1000 calls keeps overhead < 0.1%.
    Document: no allocation in hot path — all counters are atomic u64 in fixed-size array.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll perf_telemetry::tests::histogram --lib</automated>
  </verify>
  <done>
    - 8 latency buckets from 10us to >10ms
    - Thread-local PerfTelemetry with atomic counters
    - record_latency increments counters and triggers emission every 1000 calls
    - emit_telemetry resets counters and sends aggregated data
    - measure() wrapper for QPC before/after
    - Tests verify bucket assignment and counter behavior
  </done>
  <acceptance_criteria>
    - `grep "LATENCY_BUCKETS" dlp-hook-dll/src/perf_telemetry.rs` returns a match
    - `grep "fn record_latency" dlp-hook-dll/src/perf_telemetry.rs` returns a match
    - `grep "fn emit_telemetry" dlp-hook-dll/src/perf_telemetry.rs` returns a match
    - `cargo test -p dlp-hook-dll perf_telemetry::tests::histogram --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Add immediate state transition telemetry emission</name>
  <files>dlp-hook-dll/src/perf_telemetry.rs</files>
  <read_first>
    - dlp-hook-dll/src/perf_telemetry.rs (current state after Task 5)
    - dlp-hook-dll/src/fail_mode.rs (FailState enum)
  </read_first>
  <action>
    Add immediate emission for state transitions (separate from periodic histogram):

    `emit_state_transition_immediate(old_state: FailState, new_state: FailState, reason: &str)`:
    - Emit immediately (not batched) via tracing::warn!.
    - If pipe available, send `siem.fail_mode_transition` event immediately.
    - Include: old_state, new_state, reason, pid, image_path, timestamp.
    - This ensures critical state transitions are never lost in the 1000-call batch.

    Document: state transitions are security-relevant and must be logged immediately.
    Document: periodic histogram (1000-call batch) is for performance telemetry only.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll perf_telemetry::tests::immediate_transition --lib</automated>
  </verify>
  <done>
    - emit_state_transition_immediate function exists
    - Emits immediately via tracing and pipe
    - Separate from periodic histogram emission
    - Tests verify immediate emission
  </done>
  <acceptance_criteria>
    - `grep "fn emit_state_transition_immediate" dlp-hook-dll/src/perf_telemetry.rs` returns a match
    - `grep "siem.fail_mode_transition" dlp-hook-dll/src/perf_telemetry.rs` returns a match
    - `cargo test -p dlp-hook-dll perf_telemetry::tests::immediate_transition --lib` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Integrate allowlist and telemetry into trampolines</name>
  <files>dlp-hook-dll/src/trampolines.rs, dlp-hook-dll/src/lib.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (existing classify_and_log_path)
    - dlp-hook-dll/src/lib.rs (existing module declarations)
    - dlp-hook-dll/src/allowlist.rs (is_allowlisted interface)
    - dlp-hook-dll/src/perf_telemetry.rs (record_latency interface)
  </read_first>
  <action>
    Modify `classify_and_log_path` in trampolines.rs:

    New flow:
    1. `measure(|| { ... })` — wrap entire decision logic for latency tracking.
    2. Check allowlist:
       a. `is_allowlisted(path, cache_header)`
       b. If allowlisted -> record_latency(hit=true), emit_allowlist_hit if T3/T4/fail-mode, return None (allow)
    3. Check cache (LRU -> shared memory):
       a. If cache hit -> record_latency(hit=true), decide, return
    4. Check fail-mode state:
       a. If HEALTHY or DEGRADED-retry -> attempt pipe
       b. If ISOLATED -> decide_isolated, record_latency(hit=false), return
    5. On pipe success:
       a. record_latency(hit=false)
       b. Warm LRU with cache_hint
    6. On pipe failure:
       a. record_latency(hit=false)
       b. Update fail-mode state

    In lib.rs:
    - Add `mod allowlist;` and `mod perf_telemetry;` after existing modules.

    Document: allowlist is fastest path (no cache lookup, no pipe).
    Document: latency measurement covers entire decision path (allowlist -> cache -> pipe/fail-mode).
    Document: cache hit rate telemetry validates CRIT-04 performance target.
    Document: cache stores classification HINT only; ABAC authority is never bypassed.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --lib</automated>
  </verify>
  <done>
    - classify_and_log_path checks allowlist before cache
    - measure() wraps entire decision for latency tracking
    - record_latency called on every path (allowlist hit, cache hit, cache miss, pipe, fail-mode)
    - emit_allowlist_hit called for T3/T4 or fail-mode allowlist hits
    - lib.rs declares allowlist and perf_telemetry modules
    - All hook DLL tests pass
  </done>
  <acceptance_criteria>
    - `grep "mod allowlist" dlp-hook-dll/src/lib.rs` returns a match
    - `grep "mod perf_telemetry" dlp-hook-dll/src/lib.rs` returns a match
    - `grep "measure" dlp-hook-dll/src/trampolines.rs` returns a match
    - `cargo test -p dlp-hook-dll --lib` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hardcoded allowlist -> DLL | Static arrays compiled into DLL; cannot be modified at runtime |
| Operator allowlist -> Shared Memory | Agent writes; DLL reads only; validated like cache header |
| Telemetry -> Pipe | Aggregated only; no sensitive path data in telemetry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-17 | Elevation of Privilege | Rename attack on build tool | mitigate | Full-path validation (basename + parent dir + signer) |
| T-50-18 | Tampering | Operator allowlist spoofed | mitigate | Shared-memory read-only mapping; header validation; bounds checking |
| T-50-19 | Denial of Service | Telemetry every 1000 calls causes pipe congestion | mitigate | Aggregated counters only; 1000-call batching; state transitions emitted immediately but separately |
| T-50-20 | Information Disclosure | Telemetry reveals cache hit patterns | accept | Aggregated histogram only; no per-path data |
| T-50-32 | Elevation of Privilege | Build tool in user-writable directory | mitigate | is_user_writable_directory check denies allowlist for user-writable paths |
| T-50-33 | Elevation of Privilege | Unsigned binary matches build tool name | mitigate | WinVerifyTrust code-signer validation against TRUSTED_SIGNERS |
| T-50-34 | Information Disclosure | Allowlist audit logs leak paths | accept | Audit logs contain normalized paths only; necessary for compliance |
</threat_model>

<verification>
- `cargo test -p dlp-hook-dll --lib` passes all allowlist and perf_telemetry tests
- `cargo clippy -p dlp-hook-dll -- -D warnings` is clean
- `cargo build -p dlp-hook-dll` succeeds
- Allowlist tests verify system paths, build tools (including rename attack rejection, signer mismatch, user-writable rejection), operator entries
- Telemetry tests verify bucket assignment, counter behavior, 1000-call emission trigger, immediate state transition emission
- Audit logging tests verify T3/T4/fail-mode emission and T1/T2 HEALTHY silence
</verification>

<success_criteria>
- Hardcoded allowlist covers System32, SysWOW64, WinSxS, WindowsApps, Common Files
- Build-tool allowlist uses full-path validation (basename + parent directory + code signer)
- User-writable directories rejected for build-tool allowlist
- Code-signer validation (Microsoft / trusted publisher) for build tools
- Operator-extended allowlist reads from shared-memory 64 KiB region
- Allowlist checked before cache lookup (fastest path)
- Audit events emitted for all allowlist hits on T3/T4 or fail-mode decisions
- QPC latency histogram with 8 buckets (10us to >10ms)
- Thread-local atomic counters, no allocation in hot path
- Telemetry emitted every 1000 calls; state transitions emitted immediately
- Cache hit/miss counters for hit-rate validation
- Cache non-authoritative invariant documented: ABAC authority never bypassed
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/50-shared-memory-classification-cache-fail-mode-state-machine/50-05-SUMMARY.md` when done.
</output>
