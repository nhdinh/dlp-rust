---
phase: 58.8-fix-diff-01-and-diff-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-hook-dll/src/trampolines.rs
  - dlp-hook-dll/src/lib.rs
  - dlp-hook-dll/tests/override_request_test.rs
autonomous: true
requirements:
  - DIFF-01
must_haves:
  truths:
    - Every DENY decision in the hook DLL emits an IpcPayloadV1::RequestOverride frame before returning the denial.
    - The original file operation is still denied while the override request is submitted asynchronously.
    - Repeated denials on the same path do not flood the agent/user UI with override requests.
  artifacts:
    - path: dlp-hook-dll/src/trampolines.rs
      provides: Override cooldown helper and deny-path RequestOverride emission
      contains:
        - fn emit_override_request
        - fn should_emit_override
        - static OVERRIDE_COOLDOWN
    - path: dlp-hook-dll/src/lib.rs
      provides: test-helpers re-exports for classify_and_log_path and cooldown reset
    - path: dlp-hook-dll/tests/override_request_test.rs
      provides: Integration test proving override request emission and cooldown
  key_links:
    - from: classify_and_log_path/classify_and_log_handle DENY branches
      to: emit_override_request
      via: "if result.is_some() { emit_override_request(...) }"
    - from: emit_override_request
      to: dlp-agent hook_ipc override_handler
      via: "IpcEnvelope::V1(IpcPayloadV1::RequestOverride) over named pipe"
    - from: dlp-hook-dll/src/lib.rs test-helpers
      to: OVERRIDE_COOLDOWN reset
      via: "reset_override_cooldown() exposed under cfg(test) / feature = test-helpers"
---

<objective>
Close DIFF-01 by wiring the hook DLL deny path to emit IpcPayloadV1::RequestOverride.
The agent-side override handler, user UI dialog, and server approval submission already exist; the only missing piece is the DLL-side trigger.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.8/58.8-CONTEXT.md
@.planning/phases/58.8/58.8-RESEARCH.md
@dlp-hook-dll/src/trampolines.rs
@dlp-hook-dll/src/pipe_client.rs
@dlp-common/src/hook_ipc.rs
@dlp-agent/src/hook_ipc.rs
@dlp-agent/src/service.rs
@dlp-hook-dll/src/lib.rs
@dlp-hook-dll/src/test_utils.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add override emission helper with per-path cooldown</name>
  <files>dlp-hook-dll/src/trampolines.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (full file; focus on classify_and_log_path, classify_and_log_handle, and the existing DIFF-02 diagnostic push block)
    - dlp-common/src/hook_ipc.rs (OverrideRequest, IpcEnvelope, IpcPayloadV1)
    - dlp-hook-dll/src/pipe_client.rs (send_raw_oneway)
  </read_first>
  <action>
    Add module-private helpers in dlp-hook-dll/src/trampolines.rs near the top of the file.
    - Define `static OVERRIDE_COOLDOWN: Mutex<std::collections::HashMap<String, Instant>>` (or a `std::sync::LazyLock`/`once_cell` equivalent if the workspace MSRV supports it) keyed by a composite string of `"{path}\0{action}"`.
    - Implement `fn should_emit_override(key: &str, now: Instant, ttl: Duration) -> bool` that returns true only when the key is absent or its stored instant is older than `ttl`. When true, insert/update the key with `now`.
    - Implement `fn emit_override_request(path: &str, action: &str, handle_value: u64)`:
      - Build an `OverrideRequest` with:
        - `requester_sid`: `crate::get_current_user_sid()`
        - `data_object_id`: `path` if non-empty, otherwise `format!("handle:{handle_value}")`
        - `action`: the action argument
        - `destination_scope`: None
        - `justification`: empty String
        - `resource_path`: `path` if non-empty, otherwise `format!("handle:{handle_value}")`
      - Wrap it in `IpcEnvelope::V1(IpcMessageV1 { payload: IpcPayloadV1::RequestOverride(req) })`, serialize with `bincode::serialize`, and send via `pipe_client::send_raw_oneway(crate::current_pipe_name(), &bytes)`.
      - Log send failures with `tracing::warn!` and never block or unwrap.
      - Apply a 30-second cooldown via `should_emit_override` so repeated DENYs on the same path/action do not spam the UI.
    - Add a test-helpers entry point `#[cfg(any(test, feature = "test-helpers"))] pub fn reset_override_cooldown()` that clears `OVERRIDE_COOLDOWN` so integration tests start fresh. If the static uses `std::sync::LazyLock`, lock and `clear()` the inner map.
    - Document each public/test-helper item with doc comments explaining fire-and-forget semantics and cooldown rationale.
  </action>
  <verify>
    <automated>cargo check -p dlp-hook-dll</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p dlp-hook-dll` completes with no errors.
    - `emit_override_request` exists and references `IpcPayloadV1::RequestOverride` and `pipe_client::send_raw_oneway`.
    - `reset_override_cooldown` exists under the test-helpers cfg.
    - No `.unwrap()` is introduced in the new code.
  </acceptance_criteria>
  <done>
    Helper compiles, constructs correct OverrideRequest fields, applies a 30-second per-path cooldown, and exposes a test-only reset.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire deny paths to emit override requests</name>
  <files>dlp-hook-dll/src/trampolines.rs</files>
  <read_first>
    - dlp-hook-dll/src/trampolines.rs (lines 163-520 and 614-845, especially the existing "DIFF-02: Push diagnostic snapshot" blocks)
  </read_first>
  <action>
    In `classify_and_log_path`, after the fail-mode match computes `result` and after the existing DIFF-02 diagnostic snapshot push block (the `if decision.is_some()` block), add:
    ```rust
    if result.is_some() {
        emit_override_request(path, action, handle_value);
    }
    ```
    In `classify_and_log_handle`, after the corresponding DIFF-02 diagnostic push block (the `if result.is_some()` block near line 815), add the same call using the function-local `path` and `handle_value` variables.
    Do not change any return values or fail-closed semantics. The override request is fire-and-forget; the original operation remains denied.
  </action>
  <verify>
    <automated>cargo check -p dlp-hook-dll && cargo clippy -p dlp-hook-dll -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `classify_and_log_path` and `classify_and_log_handle` each contain exactly one call to `emit_override_request` guarded by `if result.is_some()`.
    - `cargo clippy -p dlp-hook-dll -- -D warnings` passes.
  </acceptance_criteria>
  <done>
    Both deny paths emit a RequestOverride on every DENY while preserving the original denial.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove RequestOverride emission with an integration test</name>
  <files>dlp-hook-dll/tests/override_request_test.rs</files>
  <read_first>
    - dlp-hook-dll/src/test_utils.rs (MockAgentServer, unique_pipe_name, set_test_pipe_name, reset_for_test)
    - dlp-hook-dll/src/lib.rs (test-helpers re-exports)
    - dlp-agent/src/hook_ipc.rs (HookIpcServer::with_override_handler)
  </read_first>
  <behavior>
    - Test setup: reset global test state, clear the override cooldown, start a HookIpcServer on a unique pipe that returns DENY for all HookRequests and captures every RequestOverride in a crossbeam channel.
    - Call the path-based classification entry point with a unique test path and action "CREATE".
    - Expected result: the classification returns a deny, and the override handler receives an OverrideRequest whose resource_path and action match the inputs.
    - Test cooldown: a second deny on the same path within 30 seconds must not produce a second override request.
  </behavior>
  <action>
    Create dlp-hook-dll/tests/override_request_test.rs.
    - Use `dlp_hook_dll::reset_for_test()` and `dlp_hook_dll::set_test_pipe_name(...)` to target a unique pipe.
    - Call `dlp_hook_dll::reset_override_cooldown()` to clear the cooldown map before assertions.
    - Build a `dlp_agent::hook_ipc::HookIpcServer` with:
      - a request handler returning `HookResponse { decision: Decision::DENY, reason: "test deny".into(), cache_hint: None, cache_version: 0, approval_override: None }`
      - an override handler that sends the received `OverrideRequest` through a `crossbeam_channel::bounded(1)`
    - Expose a test-only entry point by adding `#[cfg(any(test, feature = "test-helpers"))] pub use trampolines::{classify_and_log_path, reset_override_cooldown};` in dlp-hook-dll/src/lib.rs and calling it with a unique path.
    - Assert the captured `OverrideRequest` has `action == "CREATE"` and `resource_path == test_path`.
    - Assert a second call with the same path does not produce a second request (cooldown).
    - Run the test with `cargo test -p dlp-hook-dll --test override_request_test -- --test-threads=1`.
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll --test override_request_test -- --test-threads=1</automated>
  </verify>
  <acceptance_criteria>
    - Integration test file exists and passes.
    - Captured OverrideRequest fields match the denied path and action.
    - Cooldown test confirms only one RequestOverride per 30-second window for the same path.
  </acceptance_criteria>
  <done>
    Automated test verifies that a hook DLL deny emits a RequestOverride and respects the cooldown.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook DLL -> agent named pipe | Untrusted user process sends override request; agent must treat it as unauthenticated user intent. |
| agent -> server approval API | Internal service-to-service over authenticated HTTP. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.8-01 | Tampering | hook DLL override request payload | accept | Request contains only user identity/path/action; server approval workflow verifies authorization before grant. |
| T-58.8-02 | Denial of Service | repeated DENY operations flooding UI | mitigate | 30-second per-path cooldown in emit_override_request limits UI spam. |
| T-58.8-03 | Information Disclosure | override request content in logs | mitigate | Log only request_id and resource_path at info level; full request details at debug only per CLAUDE.md 9.13. |
| T-58.8-04 | Elevation of Privilege | fire-and-forget bypassing ABAC | accept | Original operation remains denied; approval only helps future operations via HookResponse.approval_override. |
| T-58.8-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages required; all dependencies already locked. |
</threat_model>

<verification>
- `cargo test -p dlp-hook-dll --test override_request_test` passes.
- `cargo clippy -p dlp-hook-dll -- -D warnings` passes.
- `cargo fmt --check` passes.
</verification>

<success_criteria>
- Hook DLL deny path emits IpcPayloadV1::RequestOverride with correct requester_sid, resource_path, and action.
- Original operation is still denied immediately.
- Repeated denials on the same path are throttled to one override request per 30-second window.
</success_criteria>

<output>
Create `.planning/phases/58.8/58.8-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **Functions:**
  - `dlp_hook_dll::trampolines::emit_override_request`
  - `dlp_hook_dll::trampolines::should_emit_override` (cooldown check)
  - `dlp_hook_dll::trampolines::reset_override_cooldown` (test-helpers)
- **Static items:**
  - `dlp_hook_dll::trampolines::OVERRIDE_COOLDOWN`
- **Test files:**
  - `dlp-hook-dll/tests/override_request_test.rs`
- **Behavioral changes:**
  - `classify_and_log_path` and `classify_and_log_handle` call `emit_override_request` when `result.is_some()`.
