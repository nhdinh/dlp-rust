---
phase: 58.8-fix-diff-01-and-diff-04
plan: 04
type: execute
wave: 2
depends_on:
  - 58.8-02
files_modified:
  - dlp-server/tests/diagnostics_api_integration.rs
  - dlp-server/tests/bypass_alerts_integration.rs
  - dlp-server/tests/device_registry_integration.rs
  - dlp-server/tests/managed_origins_integration.rs
  - dlp-server/tests/admin_audit_integration.rs
  - dlp-server/tests/admin_password_sync_integration.rs
  - dlp-server/tests/enforcement_mode_integration.rs
  - dlp-server/tests/ldap_config_api.rs
  - dlp-server/tests/mode_end_to_end.rs
  - dlp-server/tests/secrets_encryption_integration.rs
  - dlp-server/tests/secrets_log_scan_integration.rs
  - dlp-server/tests/secrets_migration_integration.rs
  - dlp-server/tests/secrets_rotation_integration.rs
  - dlp-server/tests/volume_class_integration.rs
  - dlp-admin-cli/src/client.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - All existing `dlp-server/tests/*.rs` integration tests compile after `AppState` gains `health_snapshot_store`.
    - `dlp-admin-cli` `EngineClient::get_self_health()` succeeds against a running (mock) server and returns a non-empty snapshot.
  artifacts:
    - path: dlp-server/tests/diagnostics_api_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/bypass_alerts_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/device_registry_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/managed_origins_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/admin_audit_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/admin_password_sync_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/enforcement_mode_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/ldap_config_api.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/mode_end_to_end.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/secrets_encryption_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/secrets_log_scan_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/secrets_migration_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/secrets_rotation_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-server/tests/volume_class_integration.rs
      provides: Updated AppState literal with health_snapshot_store field
    - path: dlp-admin-cli/src/client.rs
      provides: Unit test exercising `EngineClient::get_self_health()` against a WireMock server
  key_links:
    - from: dlp-server/src/lib.rs AppState.health_snapshot_store field
      to: every listed dlp-server/tests/*.rs AppState literal
      via: "health_snapshot_store: None added to each struct literal"
    - from: dlp-admin-cli/src/client.rs::get_self_health
      to: WireMock GET /admin/health mock
      via: "EngineClient::for_test_with_url(server.uri()) + set_token"
---

<objective>
Make the `AppState` field addition from Plan 58.8-02 compile across the existing `dlp-server/tests/` suite, and prove that `dlp-admin-cli get_self_health()` (via `EngineClient::get_self_health()`) succeeds against a running server and returns a non-empty snapshot.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.8/58.8-CONTEXT.md
@.planning/phases/58.8/58.8-RESEARCH.md
@dlp-server/src/lib.rs
@dlp-server/tests/diagnostics_api_integration.rs
@dlp-admin-cli/src/client.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all existing dlp-server integration-test AppState literals</name>
  <files>
    dlp-server/tests/diagnostics_api_integration.rs
    dlp-server/tests/bypass_alerts_integration.rs
    dlp-server/tests/device_registry_integration.rs
    dlp-server/tests/managed_origins_integration.rs
    dlp-server/tests/admin_audit_integration.rs
    dlp-server/tests/admin_password_sync_integration.rs
    dlp-server/tests/enforcement_mode_integration.rs
    dlp-server/tests/ldap_config_api.rs
    dlp-server/tests/mode_end_to_end.rs
    dlp-server/tests/secrets_encryption_integration.rs
    dlp-server/tests/secrets_log_scan_integration.rs
    dlp-server/tests/secrets_migration_integration.rs
    dlp-server/tests/secrets_rotation_integration.rs
    dlp-server/tests/volume_class_integration.rs
  </files>
  <read_first>
    - dlp-server/src/lib.rs (AppState struct definition and field order)
    - dlp-server/tests/diagnostics_api_integration.rs (AppState literal pattern)
  </read_first>
  <action>
    In every listed `dlp-server/tests/*.rs` file that constructs an `AppState` struct literal:
    - Add `health_snapshot_store: None` to the literal in the same position as the struct field order in `dlp-server/src/lib.rs`.
    - Preserve all other fields exactly as they are.
    - Do not add imports; `None` is sufficient because the field type is `Option<Arc<HealthSnapshotStore>>`.
  </action>
  <verify>
    <automated>cargo test -p dlp-server --tests --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-server --tests --no-run` succeeds.
    - Every `AppState` literal in the listed test files includes the new `health_snapshot_store` field.
  </acceptance_criteria>
  <done>
    All existing server integration tests compile with the new AppState field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dlp-admin-cli get_self_health() client test</name>
  <files>dlp-admin-cli/src/client.rs</files>
  <read_first>
    - dlp-admin-cli/src/client.rs (EngineClient::for_test_with_url, set_token, get_self_health, and the existing client_tests module)
    - dlp-admin-cli/src/client.rs client_tests list_bypass_alerts_encodes_severity_in_query_string (wiremock pattern)
  </read_first>
  <behavior>
    - A unit test starts a WireMock server that responds to `GET /admin/health` with a JSON payload containing a non-empty `snapshot` and a `history` array.
    - The test creates an `EngineClient::for_test_with_url(server.uri())`, sets a bearer token, and awaits `get_self_health()`.
    - The returned JSON has a non-null `snapshot` object with an `overall_status` field.
  </behavior>
  <action>
    In `dlp-admin-cli/src/client.rs` inside the existing `#[cfg(test)] mod client_tests`:
    - Add a new test `get_self_health_returns_non_empty_snapshot`:
      - Build a tokio runtime with `tokio::runtime::Builder::new_current_thread().enable_all().build()`.
      - In `block_on`, start a `wiremock::MockServer`.
      - Mount a WireMock response for `GET /admin/health` with status 200 and body:
        ```json
        {
          "snapshot": {
            "overall_status": "healthy",
            "injected_pids": 1,
            "patched_modules": 2,
            "pipe_round_trips_60s": 10,
            "cache_hit_rate_60s": 0.95,
            "fail_state": 0,
            "timestamp_secs": 1000
          },
          "history": []
        }
        ```
      - Create `let mut client = EngineClient::for_test_with_url(server.uri()); client.set_token("test-token".to_string());`.
      - Call `let response = client.get_self_health().await.expect("get_self_health should succeed");`.
      - Assert `response.get("snapshot").is_some()` and `response["snapshot"]["overall_status"]` equals `"healthy"`.
    - Run with `cargo test -p dlp-admin-cli get_self_health -- --nocapture`.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli get_self_health -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-admin-cli get_self_health` passes.
    - The test exercises the real `EngineClient::get_self_health()` method (not a hand-rolled request).
    - The test asserts the returned snapshot is non-empty and contains `overall_status`.
  </acceptance_criteria>
  <done>
    `dlp-admin-cli get_self_health()` is verified against a running mock server and returns a non-empty snapshot.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin TUI -> server GET /admin/health | Admin JWT required; health data is operational, not PII. |
| test -> mock server | WireMock stands in for the real server; no production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.8-10 | Information Disclosure | HookHealthSnapshot over HTTP | accept | Health snapshots contain operational counters, not PII; transport is TLS in production. |
| T-58.8-11 | Repudiation | Agent submits forged health snapshot | accept | Telemetry is best-effort; no policy decision is made solely on health data. |
| T-58.8-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages required; `wiremock` is already a dev-dependency. |
</threat_model>

<verification>
- `cargo test -p dlp-server --tests --no-run` succeeds.
- `cargo test -p dlp-admin-cli get_self_health` passes.
- `cargo clippy -p dlp-server -p dlp-admin-cli -- -D warnings` passes.
- `cargo fmt --check` passes.
- Run `sonar-scanner` (with `SONAR_TOKEN` exported in the session per CLAUDE.md 9.16/9.17 and the Phase 58.8 success criteria) and confirm the Quality Gate passes before pushing. Fix any reported bugs, smells, or coverage gaps and re-scan.
</verification>

<success_criteria>
- Every existing `dlp-server/tests/*.rs` AppState literal includes `health_snapshot_store` and the full test suite compiles.
- `EngineClient::get_self_health()` returns a non-empty snapshot when the server responds with valid health data.
- `sonar-scanner` Quality Gate passes (CLAUDE.md 9.16/9.17; CONTEXT.md success criteria).
</success_criteria>

<output>
Create `.planning/phases/58.8/58.8-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **Updated integration-test literals:**
  - `health_snapshot_store: None` added to each `AppState` struct literal under `dlp-server/tests/`.
- **New client test:**
  - `dlp_admin_cli::client::client_tests::get_self_health_returns_non_empty_snapshot`
