---
phase: 58.8-fix-diff-01-and-diff-04
plan: 04
type: execute
wave: 2
depends_on:
  - 58.8-02
files_modified:
  - dlp-server/src/admin_api.rs
  - dlp-server/src/lib.rs
  - dlp-server/tests/*
  - dlp-admin-cli/src/client.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - Every `AppState` struct literal in `dlp-server/src` and `dlp-server/tests` compiles after adding `health_snapshot_store`.
    - `dlp-admin-cli` `EngineClient::get_self_health()` succeeds against a running (mock) server, returns a non-empty snapshot, and sends the admin bearer token.
    - Unauthenticated `GET /admin/health` returns `401 Unauthorized`.
  artifacts:
    - path: dlp-server/src/admin_api.rs
      provides: Updated AppState literals and a 401 auth test for GET /admin/health
    - path: dlp-server/src/lib.rs
      provides: Updated app_state_with_flag test helper
    - path: dlp-server/tests/*.rs
      provides: Updated AppState literals
    - path: dlp-admin-cli/src/client.rs
      provides: Unit test exercising get_self_health() with Authorization header assertion
  key_links:
    - from: dlp-server/src/lib.rs AppState.health_snapshot_store field
      to: every AppState struct literal in dlp-server/src and dlp-server/tests
      via: "rg -n 'AppState \\{' discovers all literals; add health_snapshot_store: None"
    - from: dlp-admin-cli/src/client.rs::get_self_health
      to: WireMock GET /admin/health mock
      via: "EngineClient::for_test_with_url(server.uri()) + set_token"
    - from: dlp-admin-cli test assertion
      to: Authorization: Bearer test-token
      via: "WireMock::verify(...).with_header('authorization', ...).times(1)"
---

<objective>
Make the `AppState` field addition from Plan 58.8-02 compile across the entire `dlp-server` source and test suite, prove that `dlp-admin-cli get_self_health()` sends authentication and succeeds against a running server, and prove that `GET /admin/health` rejects unauthenticated requests.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.8/58.8-CONTEXT.md
@.planning/phases/58.8/58.8-RESEARCH.md
@.planning/phases/58.8/58.8-REVIEWS.md
@dlp-server/src/lib.rs
@dlp-server/src/admin_api.rs
@dlp-server/tests/diagnostics_api_integration.rs
@dlp-admin-cli/src/client.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover and update every AppState literal</name>
  <files>
    dlp-server/src/admin_api.rs
    dlp-server/src/lib.rs
    dlp-server/tests/*.rs
  </files>
  <read_first>
    - dlp-server/src/lib.rs (AppState struct definition and field order)
    - dlp-server/src/admin_api.rs (AppState literal pattern)
    - dlp-server/tests/diagnostics_api_integration.rs (AppState literal pattern)
  </read_first>
  <action>
    - Run `rg -n "AppState \\{" dlp-server/src dlp-server/tests` to discover every struct literal. Do not rely on a fixed file list.
    - In every file that constructs an `AppState` struct literal, add `health_snapshot_store: None` in the same position as the struct field order in `dlp-server/src/lib.rs`.
    - Preserve all other fields exactly as they are.
    - Do not add imports; `None` is sufficient because the field type is `Option<Arc<HealthSnapshotStore>>`.
    - Pay special attention to:
      - `dlp-server/src/lib.rs` test helper `app_state_with_flag`
      - `dlp-server/src/admin_api.rs` test literals (there are many)
      - All `dlp-server/tests/*.rs` integration tests
  </action>
  <verify>
    <automated>cargo test -p dlp-server --tests --no-run && cargo test -p dlp-server --lib --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-server --tests --no-run` succeeds.
    - `cargo test -p dlp-server --lib --no-run` succeeds.
    - Every `AppState` literal in `dlp-server/src` and `dlp-server/tests` includes the new `health_snapshot_store` field.
  </acceptance_criteria>
  <done>
    All existing server source and test code compiles with the new AppState field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dlp-admin-cli get_self_health() client test with auth assertion</name>
  <files>dlp-admin-cli/src/client.rs</files>
  <read_first>
    - dlp-admin-cli/src/client.rs (EngineClient::for_test_with_url, set_token, get_self_health, apply_auth, and the existing client_tests module)
    - dlp-admin-cli/src/client.rs client_tests list_bypass_alerts_encodes_severity_in_query_string (wiremock pattern)
  </read_first>
  <behavior>
    - A unit test starts a WireMock server that responds to `GET /admin/health` with a JSON payload containing a non-empty `snapshot` and a `history` array.
    - The test creates an `EngineClient::for_test_with_url(server.uri())`, sets a bearer token, and awaits `get_self_health()`.
    - The returned JSON has a non-null `snapshot` object with an `overall_status` field.
    - WireMock verifies that exactly one request was received with an `Authorization: Bearer test-token` header.
  </behavior>
  <action>
    In `dlp-admin-cli/src/client.rs` inside the existing `#[cfg(test)] mod client_tests`:
    - Add a new test `get_self_health_returns_non_empty_snapshot_and_sends_auth`:
      - Build a tokio runtime with `tokio::runtime::Builder::new_current_thread().enable_all().build()`.
      - In `block_on`, start a `wiremock::MockServer`.
      - Mount a WireMock response for `GET /admin/health` with status 200 and body:
        ```json
        {
          "snapshot": {
            "overall_status": "healthy",
            "injected_pids": 1,
            "patched_modules": 2,
            "pipe_round_trips_60s": 10,
            "cache_hit_rate_60s": 0.95,
            "fail_state": 0,
            "timestamp_secs": 1000
          },
          "history": []
        }
        ```
      - Create `let mut client = EngineClient::for_test_with_url(server.uri()); client.set_token("test-token".to_string());`.
      - Call `let response = client.get_self_health().await.expect("get_self_health should succeed");`.
      - Assert `response.get("snapshot").is_some()` and `response["snapshot"]["overall_status"]` equals `"healthy"`.
      - Assert that the mock received exactly one request with header `authorization` equal to `Bearer test-token` using `Mock::verify(...)` or `server.received_requests().await`.
    - Run with `cargo test -p dlp-admin-cli get_self_health -- --nocapture`.
  </action>
  <verify>
    <automated>cargo test -p dlp-admin-cli get_self_health -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-admin-cli get_self_health` passes.
    - The test exercises the real `EngineClient::get_self_health()` method (not a hand-rolled request).
    - The test asserts the returned snapshot is non-empty and contains `overall_status`.
    - The test asserts the request included `Authorization: Bearer test-token`.
  </acceptance_criteria>
  <done>
    `dlp-admin-cli get_self_health()` is verified against a running mock server, returns a non-empty snapshot, and sends the admin bearer token.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 401 test for unauthenticated GET /admin/health</name>
  <files>dlp-server/src/admin_api.rs</files>
  <read_first>
    - dlp-server/src/admin_api.rs (diagnostics auth 401 test around line 12451)
    - dlp-server/src/admin_api.rs (GET /admin/health handler and protected route registration)
  </read_first>
  <behavior>
    - A unit test builds the admin router with `GET /admin/health` registered and asserts that a request without an `Authorization` header returns `401 Unauthorized`.
  </behavior>
  <action>
    In `dlp-server/src/admin_api.rs` inside the existing test module:
    - Add a new test `get_admin_health_requires_auth`:
      - Build an `AppState` with `health_snapshot_store: Some(Arc::new(HealthSnapshotStore::new()))` and any other required fields.
      - Build the admin router (or a focused router containing only the protected routes).
      - Send a `GET /admin/health` request with no `Authorization` header.
      - Assert the response status is `401 Unauthorized`.
    - Run with `cargo test -p dlp-server get_admin_health_requires_auth -- --nocapture`.
  </action>
  <verify>
    <automated>cargo test -p dlp-server get_admin_health_requires_auth -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-server get_admin_health_requires_auth` passes.
    - The test sends `GET /admin/health` without an `Authorization` header.
    - The response status is `401 Unauthorized`.
  </acceptance_criteria>
  <done>
    Unauthenticated access to `GET /admin/health` is rejected with 401.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin TUI -> server GET /admin/health | Admin JWT required; health data is operational, not PII. |
| test -> mock server | WireMock stands in for the real server; no production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.8-10 | Information Disclosure | HookHealthSnapshot over HTTP | accept | Health snapshots contain operational counters, not PII; transport is TLS in production. |
| T-58.8-11 | Repudiation | Agent submits forged health snapshot | accept | Telemetry is best-effort; no policy decision is made solely on health data. |
| T-58.8-12 | Spoofing | Unauthenticated admin health access | mitigate | 401 test ensures GET /admin/health is JWT-protected. |
| T-58.8-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages required; `wiremock` is already a dev-dependency. |
</threat_model>

<verification>
- `cargo test -p dlp-server --tests --no-run` succeeds.
- `cargo test -p dlp-server --lib --no-run` succeeds.
- `cargo test -p dlp-admin-cli get_self_health` passes.
- `cargo test -p dlp-server get_admin_health_requires_auth` passes.
- `cargo clippy -p dlp-server -p dlp-admin-cli -- -D warnings` passes.
- `cargo fmt --check` passes.
- Run `sonar-scanner` (with `SONAR_TOKEN` exported in the session per CLAUDE.md 9.16/9.17 and the Phase 58.8 success criteria) and confirm the Quality Gate passes before pushing. Fix any reported bugs, smells, or coverage gaps and re-scan.
</verification>

<success_criteria>
- Every `AppState` struct literal in `dlp-server/src` and `dlp-server/tests` includes `health_snapshot_store` and the full test suite compiles.
- `EngineClient::get_self_health()` returns a non-empty snapshot when the server responds with valid health data and sends the admin bearer token.
- Unauthenticated `GET /admin/health` returns `401 Unauthorized`.
- `sonar-scanner` Quality Gate passes (CLAUDE.md 9.16/9.17; CONTEXT.md success criteria).
</success_criteria>

<output>
Create `.planning/phases/58.8/58.8-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **Updated AppState literals:**
  - `health_snapshot_store: None` added to every `AppState` struct literal under `dlp-server/src` and `dlp-server/tests`, discovered via `rg -n "AppState \\{"`.
- **New client test:**
  - `dlp_admin_cli::client::client_tests::get_self_health_returns_non_empty_snapshot_and_sends_auth`
- **New server auth test:**
  - `dlp_server::admin_api::tests::get_admin_health_requires_auth`
