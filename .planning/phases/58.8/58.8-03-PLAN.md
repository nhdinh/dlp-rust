---
phase: 58.8-fix-diff-01-and-diff-04
plan: 03
type: execute
wave: 2
depends_on:
  - 58.8-02
files_modified:
  - dlp-agent/src/server_client.rs
  - dlp-agent/src/service.rs
  - dlp-server/tests/health_api_integration.rs
autonomous: true
requirements:
  - DIFF-04
must_haves:
  truths:
    - The agent can submit a HookHealthSnapshot to the server via POST /agent/health.
    - The agent pushes its latest aggregated health snapshot to the server on a bounded interval.
    - A new integration test proves the round-trip from agent POST to GET /admin/health response.
  artifacts:
    - path: dlp-agent/src/server_client.rs
      provides: ServerClient::submit_health_snapshot helper
      exports:
        - fn submit_health_snapshot
    - path: dlp-agent/src/service.rs
      provides: Periodic health push task
      contains:
        - health_push_loop
    - path: dlp-server/tests/health_api_integration.rs
      provides: End-to-end health API tests
  key_links:
    - from: dlp-agent health_aggregator
      to: ServerClient::submit_health_snapshot
      via: "health_aggregator.get_current_status()"
    - from: ServerClient::submit_health_snapshot
      to: POST /agent/health
      via: "HTTP JSON POST with agent_id and HookHealthSnapshot"
    - from: POST /agent/health
      to: GET /admin/health
      via: "HealthSnapshotStore ingest -> get_dashboard_snapshot"
---

<objective>
Close the agent-side half of DIFF-04: add a ServerClient helper that POSTs HookHealthSnapshot to the server, add a periodic health push task in the agent service, and add a focused integration test that proves the POST -> store -> GET /admin/health round-trip.
</objective>

<execution_context>
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/nhdinh/dev/dlp-rust/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/58.8/58.8-CONTEXT.md
@.planning/phases/58.8/58.8-RESEARCH.md
@dlp-agent/src/server_client.rs
@dlp-agent/src/service.rs
@dlp-agent/src/health_aggregator.rs
@dlp-server/src/admin_api.rs
@dlp-server/src/lib.rs
@dlp-server/src/health_snapshot_store.rs
@dlp-common/src/hook_ipc.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ServerClient::submit_health_snapshot</name>
  <files>dlp-agent/src/server_client.rs</files>
  <read_first>
    - dlp-agent/src/server_client.rs (submit_approval_request and send_heartbeat patterns)
    - dlp-common/src/hook_ipc.rs (HookHealthSnapshot serde definition)
    - dlp-agent/src/server_client.rs test module around line 1175
  </read_first>
  <behavior>
    - `ServerClient::submit_health_snapshot(snapshot: &HookHealthSnapshot) -> Result<()>` serializes the snapshot and POSTs it to `{base_url}/agent/health` with the client's `agent_id` in the JSON body.
    - On non-2xx, it returns a structured error with the status code.
    - On network error, it returns the underlying reqwest error wrapped with context.
  </behavior>
  <action>
    In `dlp-agent/src/server_client.rs`:
    - Add request/response structs (private to the module):
      - `struct HealthSnapshotPayload { agent_id: String, snapshot: HookHealthSnapshot }`
    - Implement `pub async fn submit_health_snapshot(&self, snapshot: &HookHealthSnapshot) -> Result<()>`:
      - Build the URL with `format!("{}/agent/health", self.base_url)`.
      - Build `HealthSnapshotPayload { agent_id: self.agent_id.clone(), snapshot: snapshot.clone() }`.
      - POST JSON via `self.client.post(url).json(&payload).send().await`.
      - Use `response.error_for_status()?` and return `Ok(())`.
      - Wrap errors with `.context("failed to submit health snapshot")`.
    - Add unit tests in the existing `#[cfg(test)] mod tests`:
      - `test_submit_health_snapshot_unreachable_server`: assert the method returns an error when the server is unreachable (reuse `unreachable_client()`).
      - `test_health_snapshot_payload_serde`: assert the payload serializes/deserializes correctly.
  </action>
  <verify>
    <automated>cargo test -p dlp-agent server_client -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-agent server_client` passes.
    - `submit_health_snapshot` exists and POSTs to `/agent/health`.
    - Unit tests cover unreachable-server error path and payload serialization.
  </acceptance_criteria>
  <done>
    Agent can submit health snapshots to the server over HTTP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add periodic health push task in agent service</name>
  <files>dlp-agent/src/service.rs</files>
  <read_first>
    - dlp-agent/src/service.rs (run_service and spawn sites for heartbeat/audit/config loops)
    - dlp-agent/src/health_aggregator.rs (HealthAggregator::get_current_status signature)
    - dlp-agent/src/server_client.rs (ServerClient methods)
  </read_first>
  <action>
    In `dlp-agent/src/service.rs`:
    - Define a new async function `async fn health_push_loop(server_client: Arc<ServerClient>, health_aggregator: Arc<HealthAggregator>, interval: Duration, shutdown: tokio::sync::watch::Receiver<bool>)`:
      - Loop until `shutdown` signals true.
      - Every `interval` (default 60 seconds), call `health_aggregator.get_current_status()`.
      - If a snapshot is returned, call `server_client.submit_health_snapshot(&snapshot).await` and log failures with `tracing::warn!` (do not fail the loop).
      - If no status is available yet, log at `tracing::debug!` and continue.
    - In the service startup path where `health_aggregator` and `server_client` are created, spawn `health_push_loop` as a tokio task with the same shutdown receiver used by other background loops.
    - Keep the interval configurable via `AgentConfig` if a field exists for it; otherwise use a constant `HEALTH_PUSH_INTERVAL: Duration = Duration::from_secs(60)`.
    - Ensure the task is awaited during graceful shutdown.
  </action>
  <verify>
    <automated>cargo check -p dlp-agent && cargo clippy -p dlp-agent -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check -p dlp-agent` succeeds.
    - `cargo clippy -p dlp-agent -- -D warnings` succeeds.
    - `health_push_loop` is spawned in the service startup path and submits snapshots via `ServerClient`.
  </acceptance_criteria>
  <done>
    Agent periodically mirrors local hook health to the server.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add health API round-trip integration test</name>
  <files>dlp-server/tests/health_api_integration.rs</files>
  <read_first>
    - dlp-server/tests/diagnostics_api_integration.rs (build_test_app helper pattern)
    - dlp-server/src/admin_api.rs (route paths and request/response types for reference)
    - dlp-common/src/hook_ipc.rs (HookHealthSnapshot construction)
    - dlp-server/src/health_snapshot_store.rs (store API)
  </read_first>
  <behavior>
    - A new integration test builds an AppState with `health_snapshot_store: Some(Arc::new(HealthSnapshotStore::new()))`.
    - POSTing a synthetic `HookHealthSnapshot` to `/agent/health` returns 200 and stores the snapshot.
    - GET `/admin/health` with an admin JWT returns a dashboard snapshot reflecting the ingested data and a non-empty history.
  </behavior>
  <action>
    - Create `dlp-server/tests/health_api_integration.rs`:
      - Build a test app with `health_snapshot_store: Some(Arc::new(HealthSnapshotStore::new()))`, a temporary SQLite pool, and a real admin token (reuse the diagnostics test helper pattern).
      - POST to `/agent/health` with a synthetic `HookHealthSnapshot` and `agent_id = "integration-test-agent"`.
      - Assert the response is 200 and `{ "received": true }`.
      - GET `/admin/health` with the admin token.
      - Assert the response is 200, `snapshot` is non-null, `snapshot.overall_status` is one of "healthy"/"degraded"/"critical", and `history` contains at least one entry matching the posted snapshot.
      - Run with `cargo test -p dlp-server --test health_api_integration -- --nocapture`.
  </action>
  <verify>
    <automated>cargo test -p dlp-server --test health_api_integration -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p dlp-server --test health_api_integration` passes.
    - The test AppState literal includes `health_snapshot_store: Some(...)`.
    - The test verifies both the agent ingest endpoint and the admin health endpoint.
  </acceptance_criteria>
  <done>
    Integration test proves the agent-to-server health round-trip.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> server POST /agent/health | Agent-authenticated public endpoint; per-agent rate limit prevents abuse. |
| admin TUI -> server GET /admin/health | Admin JWT required; health data is operational, not PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58.8-09 | Denial of Service | Agent health push interval too aggressive | mitigate | 60-second interval and per-agent rate limiting on the server cap push volume. |
| T-58.8-10 | Information Disclosure | HookHealthSnapshot over HTTP | accept | Health snapshots contain operational counters, not PII; transport is TLS in production. |
| T-58.8-11 | Repudiation | Agent submits forged health snapshot | accept | Telemetry is best-effort; no policy decision is made solely on health data. |
| T-58.8-SC | Tampering | npm/pip/cargo installs | mitigate | No new external packages required; all dependencies already locked. |
</threat_model>

<verification>
- `cargo test -p dlp-agent server_client` passes.
- `cargo check -p dlp-agent` succeeds.
- `cargo test -p dlp-server --test health_api_integration` passes.
- `cargo clippy -p dlp-agent -p dlp-server -- -D warnings` passes.
- `cargo fmt --check` passes.
- Run `sonar-scanner` (with `SONAR_TOKEN` exported in the session per CLAUDE.md 9.16/9.17 and the Phase 58.8 success criteria) and confirm the Quality Gate passes before pushing. Fix any reported bugs, smells, or coverage gaps and re-scan.
</verification>

<success_criteria>
- `ServerClient::submit_health_snapshot` POSTs `HookHealthSnapshot` to `/agent/health`.
- Agent service pushes health snapshots every 60 seconds.
- Integration test verifies POST -> store -> GET /admin/health round-trip.
- `sonar-scanner` Quality Gate passes (CLAUDE.md 9.16/9.17; CONTEXT.md success criteria).
</success_criteria>

<output>
Create `.planning/phases/58.8/58.8-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- **Agent client method:**
  - `dlp_agent::server_client::ServerClient::submit_health_snapshot`
- **Agent background task:**
  - `dlp_agent::service::health_push_loop`
- **New integration test file:**
  - `dlp-server/tests/health_api_integration.rs`
