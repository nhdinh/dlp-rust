---
phase: 53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring
plan: 06
type: execute
wave: 4
depends_on:
  - 53-05-PLAN.md
files_modified:
  - dlp-server/src/siem_connector.rs
  - dlp-server/src/alert_router.rs
  - dlp-common/src/audit.rs
  - dlp-agent/tests/etw_consumer_integration.rs
  - dlp-server/tests/bypass_alerts_integration.rs
autonomous: true
requirements:
  - ETW-05

must_haves:
  truths:
    - "BypassAlertDetected events route through siem_connector::relay with correct EventType"
    - "crit severity bypass alerts trigger alert_router::send in addition to SIEM relay"
    - "warn/info severity bypass alerts route to SIEM only (no alert router)"
    - "BypassAlert struct serializes correctly for SIEM relay JSON payload"
    - "Integration test verifies end-to-end: ETW event -> correlator -> bypass alert -> SIEM relay -> alert router"
    - "Integration test verifies alert router is NOT called for warn severity"
    - "routed_to_siem() returns true for BypassAlertDetected"
    - "triggers_alert() returns true for BypassAlertDetected"
  artifacts:
    - path: "dlp-server/src/siem_connector.rs"
      provides: "SIEM relay handling for BypassAlertDetected events"
      exports: []
    - path: "dlp-server/src/alert_router.rs"
      provides: "Alert router handling for crit severity bypass alerts"
      exports: []
    - path: "dlp-common/src/audit.rs"
      provides: "BypassAlertDetected event type with correct routing"
      exports:
        - "EventType::BypassAlertDetected"
    - path: "dlp-agent/tests/etw_consumer_integration.rs"
      provides: "End-to-end ETW consumer + correlator integration test"
      exports: []
    - path: "dlp-server/tests/bypass_alerts_integration.rs"
      provides: "End-to-end server-side SIEM + alert router integration test"
      exports: []
  key_links:
    - from: "dlp-server/src/admin_api.rs"
      to: "dlp-server/src/siem_connector.rs"
      via: "bypass_batch_ingest_handler calls relay_event"
      pattern: "state.siem.relay_event"
    - from: "dlp-server/src/admin_api.rs"
      to: "dlp-server/src/alert_router.rs"
      via: "crit severity calls send_alert"
      pattern: "state.alert.send_alert"
    - from: "dlp-common/src/audit.rs"
      to: "dlp-server/src/siem_connector.rs"
      via: "EventType::BypassAlertDetected routed_to_siem=true"
      pattern: "routed_to_siem() match arm"
---

<objective>
Wire bypass alerts through the existing SIEM relay and alert router pipelines, and verify end-to-end integration. This plan ensures that bypass alerts produced by the correlator actually reach the operator's SIEM and alerting channels (email/webhook) with correct severity-based routing.

Purpose: Complete the alert pipeline so bypass detection produces actionable notifications, not just database rows.
Output: SIEM + alert router integration verified, integration tests for end-to-end flow, and any missing event type wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-CONTEXT.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-RESEARCH.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-PATTERNS.md

## Key patterns from codebase

### SIEM relay (from `dlp-server/src/siem_connector.rs`)
```rust
pub async fn relay_event(&self, event: &AuditEvent) -> Result<(), SiemError> {
    if !event.event_type.routed_to_siem() { return Ok(()); }
    // ... relay to Splunk/ELK
}
```

### Alert router (from `dlp-server/src/alert_router.rs`)
```rust
pub async fn send_alert(&self, event: &AuditEvent) -> Result<(), AlertError> {
    // SMTP + webhook paths
}
```

### Audit event construction (from existing handlers)
```rust
let audit = AuditEvent {
    event_type: EventType::BypassAlertDetected,
    timestamp: Utc::now(),
    agent_id: Some(agent_id),
    details: json!({ "reason": "no_hook_journal", "severity": "crit" }),
    // ...
};
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and complete BypassAlertDetected SIEM + alert router wiring</name>
  <files>dlp-common/src/audit.rs, dlp-server/src/siem_connector.rs, dlp-server/src/alert_router.rs</files>
  <read_first>
    - dlp-common/src/audit.rs (EventType enum, routed_to_siem(), triggers_alert())
    - dlp-server/src/siem_connector.rs (relay_event implementation)
    - dlp-server/src/alert_router.rs (send_alert implementation)
    - .planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-CONTEXT.md (D-11)
  </read_first>
  <action>
    Verify and complete the wiring for `BypassAlertDetected` event type:

    1. In `dlp-common/src/audit.rs`:
       - Verify `EventType::BypassAlertDetected` exists (added in Plan 04 Task 1)
       - Verify `routed_to_siem()` match arm returns `true` for `BypassAlertDetected`
       - Verify `triggers_alert()` match arm returns `true` for `BypassAlertDetected`
       - If either is missing, add the match arm

    2. In `dlp-server/src/siem_connector.rs`:
       - Verify `relay_event` calls `event.event_type.routed_to_siem()` before relaying
       - Document that `BypassAlertDetected` events are relayed with full alert details JSON
       - No code changes needed if the generic relay_event already checks routed_to_siem()

    3. In `dlp-server/src/alert_router.rs`:
       - Verify `send_alert` is called for crit severity bypass alerts (this happens in admin_api.rs handler)
       - Document that alert router receives `BypassAlertDetected` events when severity == "crit"
       - No code changes needed if the handler already calls send_alert conditionally

    4. Add unit tests in `dlp-common/src/audit.rs`:
       - `test_bypass_alert_detected_routed_to_siem` — verify routed_to_siem() returns true
       - `test_bypass_alert_detected_triggers_alert` — verify triggers_alert() returns true

    5. Add unit tests in `dlp-server/src/siem_connector.rs` (if test module exists):
       - `test_relay_skips_non_siem_events` — verify events with routed_to_siem=false are skipped
       - `test_relay_bypass_alert_detected` — verify BypassAlertDetected is relayed

    6. Add unit tests in `dlp-server/src/alert_router.rs` (if test module exists):
       - `test_send_alert_crit_severity` — verify crit bypass alert triggers email/webhook
  </action>
  <verify>
    <automated>cargo test -p dlp-common audit -p dlp-server siem_connector alert_router -- --test-threads=1</automated>
  </verify>
  <done>
    - `EventType::BypassAlertDetected` has routed_to_siem() == true
    - `EventType::BypassAlertDetected` has triggers_alert() == true
    - SIEM connector relays BypassAlertDetected events
    - Alert router sends crit severity bypass alerts
    - All audit routing tests pass
    - All SIEM/alert router unit tests pass
    - Clippy clean (-D warnings)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create end-to-end integration tests for bypass alert pipeline</name>
  <files>dlp-server/tests/bypass_alerts_integration.rs, dlp-agent/tests/etw_consumer_integration.rs</files>
  <read_first>
    - dlp-server/tests/admin_audit_integration.rs (integration test pattern)
    - dlp-server/src/admin_api.rs (bypass_batch_ingest_handler from Plan 05)
    - dlp-agent/src/etw_kernel_file.rs (EtwKernelFileConsumer from Plan 01)
    - dlp-agent/src/bypass_correlator.rs (BypassCorrelator from Plan 04)
    - .planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-VALIDATION.md (test map)
  </read_first>
  <action>
    Create integration tests that verify the complete bypass alert pipeline end-to-end.

    1. Create `dlp-server/tests/bypass_alerts_integration.rs`:

       a. `test_bypass_alert_ingest_routes_to_siem`:
          - Start test server with in-memory SQLite
          - Configure mock SIEM connector that records relayed events
          - POST /audit/bypass with 1 crit-severity alert
          - Verify SIEM relay received the event with EventType::BypassAlertDetected
          - Verify alert contains correct agent_id, pid, file_path

       b. `test_bypass_alert_crit_routes_to_alert_router`:
          - Start test server with mock alert router that records sent alerts
          - POST /audit/bypass with 1 crit-severity alert
          - Verify alert_router::send_alert was called
          - Verify alert contains correct severity="crit"

       c. `test_bypass_alert_warn_does_not_route_to_alert_router`:
          - Start test server with mock alert router
          - POST /audit/bypass with 1 warn-severity alert
          - Verify alert_router::send_alert was NOT called
          - Verify SIEM relay WAS called

       d. `test_bypass_alert_batch_ingest_mixed_severity`:
          - POST batch with 2 crit + 3 warn alerts
          - Verify SIEM relay received all 5
          - Verify alert router received only the 2 crit alerts

       e. `test_bypass_alert_siem_payload_structure`:
          - POST /audit/bypass with 1 alert
          - Verify SIEM payload JSON contains: event_type, timestamp, agent_id, details.reason, details.severity, details.file_path

    2. Create `dlp-agent/tests/etw_consumer_integration.rs` (Windows-only, #[ignore] by default):

       a. `test_etw_consumer_starts_and_parses_events` (#[cfg(windows)]):
          - Requires SYSTEM privilege and SeSystemProfilePrivilege
          - Start EtwKernelFileConsumer
          - Perform a known file operation (create temp file)
          - Verify at least one EtwFileEvent is received within 5 seconds
          - Verify event has non-zero pid, non-empty file_name
          - Mark as #[ignore] for CI (requires Windows + SYSTEM)

       b. `test_correlator_detects_missing_journal` (#[cfg(windows)]):
          - Requires hook DLL NOT loaded in test process
          - Start ETW consumer and correlator with mock server client
          - Perform file operation in a temp directory
          - Verify correlator produces BypassAlert with reason=NoHookJournal
          - Mark as #[ignore] for CI

    3. For server tests, use the existing test harness pattern from `admin_audit_integration.rs`:
       - Create in-memory SQLite pool
       - Build AppState with mock SIEM connector and mock alert router
       - Use `axum::Router` for HTTP testing via `tower::ServiceExt::oneshot`

    4. For mock SIEM connector, create a test-only `RecordingSiemConnector` that stores events in a `Arc<Mutex<Vec<AuditEvent>>>`.

    5. For mock alert router, create a test-only `RecordingAlertRouter` that stores events in a `Arc<Mutex<Vec<AuditEvent>>>`.
  </action>
  <verify>
    <automated>cargo test -p dlp-server bypass_alerts_integration -- --test-threads=1</automated>
  </verify>
  <done>
    - `dlp-server/tests/bypass_alerts_integration.rs` exists with 5+ integration tests
    - SIEM relay integration verified: BypassAlertDetected events reach SIEM
    - Alert router integration verified: crit severity triggers alert router
    - Alert router NOT called for warn severity
    - Batch ingest with mixed severity routes correctly
    - SIEM payload structure verified
    - `dlp-agent/tests/etw_consumer_integration.rs` exists with 2 Windows-only tests (#[ignore])
    - All server integration tests pass
    - Clippy clean (-D warnings)
  </done>
</task>

<task type="auto">
  <name>Task 3: Final integration verification and workspace build</name>
  <files>none (verification-only task)</files>
  <read_first>
    - .planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-VALIDATION.md (verification criteria)
  </read_first>
  <action>
    Run final verification to ensure all Phase 53 components integrate correctly:

    1. Build the entire workspace:
       ```bash
       cargo build --workspace
       ```

    2. Run all Phase 53-affected crate tests:
       ```bash
       cargo test -p dlp-agent -p dlp-hook-dll -p dlp-common -p dlp-server --lib
       ```

    3. Run integration tests (excluding #[ignore] tests):
       ```bash
       cargo test -p dlp-server --test bypass_alerts_integration
       ```

    4. Run clippy on all modified crates:
       ```bash
       cargo clippy -p dlp-agent -p dlp-hook-dll -p dlp-common -p dlp-server -- -D warnings
       ```

    5. Verify no unwrap() in library code:
       ```bash
       grep -rn "\.unwrap()" dlp-agent/src/etw_kernel_file.rs dlp-agent/src/bypass_correlator.rs dlp-hook-dll/src/hook_journal.rs dlp-common/src/path_hash.rs dlp-server/src/db/repositories/bypass_alerts.rs dlp-server/src/admin_api.rs || echo "No unwrap found"
       ```

    6. Verify all requirement IDs are addressed:
       - ETW-01: ETW consumer (Plan 01)
       - ETW-02: Hook journal (Plan 02)
       - ETW-03: Bypass correlator (Plan 04) + path hash (Plan 03)
       - ETW-04: Server-side storage (Plan 05)
       - ETW-05: SIEM + alert router (Plan 06)

    7. Update `53-VALIDATION.md`:
       - Mark all test rows as "green"
       - Set `nyquist_compliant: true`
       - Set `wave_0_complete: true`
  </action>
  <verify>
    <automated>cargo test --workspace --lib && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>
    - Full workspace build succeeds
    - All lib tests pass
    - All integration tests pass
    - Clippy clean for all modified crates
    - No unwrap() in library code paths
    - All 5 ETW requirements verified
    - VALIDATION.md updated with nyquist_compliant=true
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> SIEM | HTTPS outbound; audit events contain metadata only |
| Server -> Alert router | SMTP/webhook outbound; crit alerts trigger operator notification |
| Integration test -> mock connectors | Test-only boundary; verifies routing logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-22 | Information Disclosure | SIEM payload contains file paths | accept | File paths are metadata; no file content in alerts per D-22 |
| T-53-23 | Denial of Service | SIEM relay failure blocks alert ingestion | mitigate | relay_event is best-effort (`.await.ok()`); failure does not block DB insert |
| T-53-24 | Denial of Service | Alert router failure blocks alert ingestion | mitigate | send_alert is best-effort (`.await.ok()`); failure does not block DB insert |
| T-53-25 | Spoofing | Mock connectors in tests do not represent real security | accept | Test mocks verify routing logic; production uses real connectors |
</threat_model>

<verification>
- `cargo test --workspace --lib` passes
- `cargo test -p dlp-server --test bypass_alerts_integration` passes
- `cargo clippy --workspace -- -D warnings` is clean
- No `unwrap()` in library code paths
- All 5 ETW requirements (ETW-01 through ETW-05) have corresponding plans and tests
</verification>

<success_criteria>
1. `EventType::BypassAlertDetected` routes through `routed_to_siem()` (returns true) and `triggers_alert()` (returns true).
2. SIEM relay receives `BypassAlertDetected` events with full JSON payload.
3. Alert router receives crit severity bypass alerts.
4. Alert router does NOT receive warn/info severity bypass alerts.
5. Batch ingest with mixed severity routes correctly to both SIEM and alert router.
6. Integration tests verify end-to-end flow: ingest -> DB -> SIEM -> alert router.
7. Integration tests verify SIEM payload structure contains all required fields.
8. Full workspace build and test suite passes.
</success_criteria>

<output>
Create `.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-06-SUMMARY.md` when done.
</output>
