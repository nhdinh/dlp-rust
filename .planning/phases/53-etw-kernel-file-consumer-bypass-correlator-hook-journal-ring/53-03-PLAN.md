---
phase: 53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dlp-common/src/path_hash.rs
  - dlp-common/src/lib.rs
  - dlp-hook-dll/src/classification_cache.rs
autonomous: true
requirements:
  - ETW-03

must_haves:
  truths:
    - "Path normalization function is extracted from classification_cache.rs into dlp-common"
    - "Both hook DLL and bypass correlator call the SAME normalize_path function"
    - "FNV-1a 64-bit hash function exists in dlp-common and is used by both sides"
    - "Normalization handles NT prefixes (\\\\?\\), UNC, DOS paths, case-folding, backslash collapse"
    - "Normalization rejects 8.3 short names, ADS streams, volume GUID paths"
    - "path_hash convenience function normalizes then hashes in one call"
  artifacts:
    - path: "dlp-common/src/path_hash.rs"
      provides: "Shared path normalization and FNV-1a hashing"
      exports:
        - "normalize_path"
        - "path_hash"
        - "fnv1a_64"
    - path: "dlp-common/src/lib.rs"
      provides: "Module export for path_hash"
      exports:
        - "pub mod path_hash"
  key_links:
    - from: "dlp-common/src/path_hash.rs"
      to: "dlp-hook-dll/src/classification_cache.rs"
      via: "classification_cache now imports from dlp-common instead of inline"
      pattern: "use dlp_common::path_hash::normalize_path"
    - from: "dlp-common/src/path_hash.rs"
      to: "dlp-agent/src/bypass_correlator.rs"
      via: "correlator imports same normalize_path for ETW FileName hashing"
      pattern: "use dlp_common::path_hash::{normalize_path, path_hash}"
---

<objective>
Extract the path normalization and FNV-1a 64-bit hashing logic from `dlp-hook-dll/src/classification_cache.rs` into a shared `dlp-common/src/path_hash.rs` module. Both the hook DLL and the bypass correlator must use identical normalization to ensure path-hash correlation works across the user/kernel boundary.

Purpose: Eliminate the risk of path normalization mismatch between hook DLL and correlator, which would cause false bypass alerts (Pitfall 5 in RESEARCH.md).
Output: `dlp-common/src/path_hash.rs` with `normalize_path()`, `path_hash()`, and `fnv1a_64()`; `classification_cache.rs` updated to import from dlp-common.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-CONTEXT.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-RESEARCH.md
@.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-PATTERNS.md

## Key patterns from codebase

### Path normalization (from `dlp-hook-dll/src/classification_cache.rs`)
```rust
pub fn normalize_path(path: &str) -> Option<Cow<'_, str>> {
    if path.is_empty() { return None; }
    let s = if path.starts_with(r"\\?\") || path.starts_with(r"\\.\") {
        &path[4..]
    } else { path };
    if path.starts_with(r"\\.\") { return None; }
    if let Some(colon_pos) = s.find(':') {
        if colon_pos != 1 || s.len() < 2 || s.as_bytes()[1] != b':' {
            return None;
        }
        if s[2..].contains(':') { return None; }
    }
    if s.to_ascii_uppercase().contains("VOLUME{") { return None; }
    if is_eight_three_short_name(s) { return None; }
    let mut result = s.replace('/', "\\");
    // collapse backslashes, to-uppercase, strip trailing, etc.
    Some(Cow::Owned(result))
}
```

### FNV-1a hash (from `dlp-common/src/hash.rs`)
```rust
pub fn fnv1a_64(bytes: &[u8]) -> u64 {
    let mut h: u64 = 0xcbf29ce484222325;
    for &b in bytes {
        h ^= u64::from(b);
        h = h.wrapping_mul(0x100000001b3);
    }
    h
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dlp-common/src/path_hash.rs with extracted normalization and hashing</name>
  <files>dlp-common/src/path_hash.rs, dlp-common/src/lib.rs</files>
  <read_first>
    - dlp-hook-dll/src/classification_cache.rs (normalize_path function, lines 545-627)
    - dlp-common/src/hash.rs (fnv1a_64 function)
    - dlp-common/src/lib.rs (existing pub mod declarations)
    - .planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-CONTEXT.md (D-06)
  </read_first>
  <action>
    Create `dlp-common/src/path_hash.rs` as a new module in dlp-common. Per D-06:

    1. Extract `normalize_path(path: &str) -> Option<Cow<'_, str>>` from `classification_cache.rs` into this module. Preserve ALL existing behavior:
       - Empty path -> None
       - Strip `\\?\` and `\\.\` prefixes (NT device namespace)
       - Reject `\\.\` paths (device namespace) -> None
       - Validate single drive letter colon (e.g., `C:`) at position 1; reject multiple colons
       - Reject volume GUID paths containing "VOLUME{" (case-insensitive)
       - Reject 8.3 short names via `is_eight_three_short_name()` helper
       - Convert forward slashes to backslashes
       - Collapse consecutive backslashes
       - Convert to uppercase for case-insensitive comparison
       - Strip trailing backslash (except root `C:\`)

    2. Extract `is_eight_three_short_name(path: &str) -> bool` helper:
       - Detects patterns like `PROGRA~1`, `TEMP~123`, etc.
       - Looks for `~` followed by digits in the final path component

    3. Re-export `fnv1a_64(bytes: &[u8]) -> u64` from `dlp-common/src/hash.rs` in this module for convenience:
       ```rust
       pub use crate::hash::fnv1a_64;
       ```

    4. Add `pub fn path_hash(path: &str) -> Option<u64>` convenience function:
       - Calls `normalize_path(path)`
       - If Some(normalized), computes `fnv1a_64(normalized.as_bytes())` and returns Some(hash)
       - If None, returns None

    5. Add comprehensive doc comments for all public functions with:
       - Description
       - Arguments
       - Returns
       - Examples
       - Notes about Windows path formats handled

    6. In `dlp-common/src/lib.rs`, add `pub mod path_hash;` alongside other module declarations.

    7. Verify that `dlp-common/src/hash.rs` already has `pub fn fnv1a_64`. If not, add it there first.

    Per D-06: The normalization must be IDENTICAL between DLL and correlator. Any divergence will cause hash mismatches and false bypass alerts.
  </action>
  <verify>
    <automated>cargo test -p dlp-common path_hash -- --test-threads=1</automated>
  </verify>
  <done>
    - `dlp-common/src/path_hash.rs` exists and compiles
    - `normalize_path` produces identical output to the original classification_cache.rs version
    - `path_hash` convenience function normalizes then hashes
    - `fnv1a_64` is re-exported from hash.rs
    - `lib.rs` contains `pub mod path_hash;`
    - All doc comments present for public functions
    - All unit tests pass, clippy clean (-D warnings)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update classification_cache.rs to import from dlp-common</name>
  <files>dlp-hook-dll/src/classification_cache.rs</files>
  <read_first>
    - dlp-hook-dll/src/classification_cache.rs (normalize_path and fnv1a_64 definitions)
    - dlp-common/src/path_hash.rs (new module from Task 1)
  </read_first>
  <action>
    Modify `dlp-hook-dll/src/classification_cache.rs` to use the shared `dlp-common` path normalization:

    1. Remove the inline `normalize_path` function definition from `classification_cache.rs`.
    2. Remove the inline `is_eight_three_short_name` helper if it was private to normalize_path.
    3. Remove the inline `fnv1a_64` function definition (or verify it's not present inline).
    4. Add import at the top of the file:
       ```rust
       use dlp_common::path_hash::{normalize_path, fnv1a_64};
       ```
    5. Verify all call sites of `normalize_path` in `classification_cache.rs` still compile:
       - Cache lookup path normalization
       - Path hash computation for cache keys
       - Any test code that calls normalize_path directly
    6. If `classification_cache.rs` has `#[cfg(test)]` tests that test `normalize_path` directly, move those tests to `dlp-common/src/path_hash.rs` (or keep them as integration tests that import from dlp-common).

    Ensure the `dlp-hook-dll/Cargo.toml` already has `dlp-common` as a dependency (it should, as it's a workspace member).
  </action>
  <verify>
    <automated>cargo test -p dlp-hook-dll classification_cache -- --test-threads=1</automated>
  </verify>
  <done>
    - `classification_cache.rs` no longer contains inline `normalize_path` or `fnv1a_64`
    - `classification_cache.rs` imports `normalize_path` and `fnv1a_64` from `dlp_common::path_hash`
    - All cache lookup tests still pass
    - All path normalization tests pass (now in dlp-common)
    - No regression in classification_cache functionality
    - Clippy clean (-D warnings)
  </done>
</task>

<task type="auto">
  <name>Task 3: Unit tests for path_hash.rs</name>
  <files>dlp-common/src/path_hash.rs</files>
  <read_first>
    - dlp-common/src/path_hash.rs (the module being tested)
    - dlp-hook-dll/src/classification_cache.rs (existing test cases for normalize_path)
  </read_first>
  <action>
    Add comprehensive unit tests in `#[cfg(test)] mod tests`:

    1. `test_normalize_empty_path` — verify empty string returns None
    2. `test_normalize_nt_prefix_stripped` — verify `\\?\C:\\foo` becomes `C:\\FOO`
    3. `test_normalize_unc_path` — verify `\\server\\share\\file` is preserved (with backslash collapse)
    4. `test_normalize_dos_path` — verify `C:\\foo\\bar` becomes `C:\\FOO\\BAR`
    5. `test_normalize_forward_slashes` — verify `C:/foo/bar` becomes `C:\\FOO\\BAR`
    6. `test_normalize_backslash_collapse` — verify `C:\\\\foo` becomes `C:\\FOO`
    7. `test_normalize_trailing_backslash` — verify `C:\\foo\\` becomes `C:\\FOO`
    8. `test_normalize_root_kept` — verify `C:\\` stays `C:\\`
    9. `test_normalize_rejects_device_namespace` — verify `\\.\PhysicalDrive0` returns None
    10. `test_normalize_rejects_volume_guid` — verify `\\?\Volume{1234}\file` returns None
    11. `test_normalize_rejects_ads` — verify `C:\\foo:stream` returns None (multiple colons)
    12. `test_normalize_rejects_8_3_short_name` — verify `C:\\PROGRA~1` returns None
    13. `test_normalize_allows_long_name` — verify `C:\\Program Files` returns Some
    14. `test_path_hash_consistency` — verify same path always produces same hash
    15. `test_path_hash_case_insensitive` — verify `C:\\FOO` and `C:\\foo` produce same hash
    16. `test_path_hash_different_paths_different_hash` — verify `C:\\foo` and `C:\\bar` produce different hashes
    17. `test_fnv1a_64_known_value` — verify fnv1a_64(b"hello") produces known FNV-1a hash value
    18. `test_path_hash_none_on_invalid` — verify path_hash returns None for invalid paths

    These tests must be hermetic (no filesystem access, no Windows APIs). They test pure string manipulation.
  </action>
  <verify>
    <automated>cargo test -p dlp-common path_hash -- --test-threads=1</automated>
  </verify>
  <done>
    - All 18 unit tests pass
    - NT prefix stripping verified
    - UNC path handling verified
    - DOS path normalization verified
    - Case-folding verified
    - Backslash collapse verified
    - Volume GUID rejection verified
    - 8.3 short name rejection verified
    - ADS rejection verified
    - Path hash consistency and case-insensitivity verified
    - FNV-1a known value verified
    - No `unwrap()` in library code paths
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook DLL -> dlp-common | Shared library dependency; normalization must be byte-identical |
| Correlator -> dlp-common | Same shared library; ensures hash consistency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-09 | Tampering | Path normalization divergence between DLL and correlator | mitigate | Single shared function in dlp-common eliminates drift; 18 unit tests verify behavior |
| T-53-10 | Information Disclosure | Path hashes are reversible | accept | FNV-1a is a non-cryptographic hash; path data is not sensitive in this context |
| T-53-11 | Denial of Service | Normalization rejects valid paths | mitigate | Extensive test coverage (18 cases) ensures correct behavior for known Windows path formats |
</threat_model>

<verification>
- `cargo test -p dlp-common path_hash` passes (all unit tests)
- `cargo test -p dlp-hook-dll classification_cache` passes (no regression)
- `cargo clippy -p dlp-common -p dlp-hook-dll -- -D warnings` is clean
- `cargo build --workspace` succeeds
- No `unwrap()` in library code paths (grep for `\.unwrap\(\)` in path_hash.rs)
</verification>

<success_criteria>
1. `dlp-common/src/path_hash.rs` exists with `normalize_path`, `path_hash`, and `fnv1a_64`.
2. `normalize_path` handles NT prefixes, UNC, DOS paths, case-folding, backslash collapse.
3. `normalize_path` rejects 8.3 short names, ADS streams, volume GUID paths.
4. `path_hash` convenience function normalizes then hashes in one call.
5. `classification_cache.rs` imports `normalize_path` and `fnv1a_64` from `dlp_common::path_hash` (no inline duplication).
6. 18 unit tests cover all normalization edge cases and hash consistency.
7. Module is declared in `dlp-common/src/lib.rs`.
</success_criteria>

<output>
Create `.planning/phases/53-etw-kernel-file-consumer-bypass-correlator-hook-journal-ring/53-03-SUMMARY.md` when done.
</output>
